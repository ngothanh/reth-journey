# Week 103 — `perp-dex-core` assembly start: VSR cluster wiring + the order/event log = VSR log + async settle interface (`SettlementId`)

> **Phase**: 6
> **Month**: M26
> **Track**: Half-2 venue (primary, the capstone spine) + Half-1 Reth-spine (Fri, light)
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: TigerBeetle `src/vsr/replica.zig` (`on_prepare`, `commit_journal`, the *single hot-path log* discipline) + `src/state_machine.zig` (deterministic apply over the op-number) — *the source of truth for "the op log IS the journal"*; Hyperliquid / dYdX-v4 hybrid off-chain-match → on-chain-settle boundary (settle interface shape only); Spanner/Calvin TrueTime-vs-deterministic-sequence framing for the projection staleness contracts.
> **Crate(s) touched**: `perp-dex-core` v0.1 [NEW — capstone assembly crate] (the multi-node venue binary + the op union `VenueOp` + the settle facade). **Consumes** `consensus-vsr` (W74/W90, the NormalReplica + the hot-path log), `matching-engine` v1.0 (W74, book/STP/triggers), `risk-engine` v1.0 (W99, cross-margin), `liquidation-engine` v1.0 (W102, fund/waterfall), `ledger-deterministic` v1.0 (W92, settlement journal), `runtime-thread-per-core` v1.0 (W85, shard-by-account). **No new primitive built** — this week is *assembly + the settle seam*.
> **Bar**: (c) — `perp-dex-core` is the capstone on the HFT critical path / the multi-node replicated core (bar (c): VOPR + static-mem + zero-alloc + cross-arch determinism + cluster-level linearizability). The settle facade + projection-read facade carry the determinism invariants; the deploy-glue + the harness notes are bar (b). Each Build labels its bar inline.
> **Feeds into**: W104 `perp-dex-core` hybrid-boundary interface (off-chain-match / on-chain-settle stub wires *into this week's `settle()` signature*; RWA-aware oracle/funding seams sit beside the `VenueOp` union); W105 3-node VSR deploy + `ops-monitoring` (monitors *this* cluster's VSR/insurance-fund/op-number metrics); W107 projection fan-out (the two staleness contracts surfaced this week become the read planes); W108 **cluster-VOPR** (extends this week's single-log linearizability + replica-convergence assertions to crash/restart/partition; the insurance-fund tripwire StateChecker rides this `VenueOp` log); W109 Blog #4 ("VSR-log-as-source-of-truth"); W113 `perp-dex-core` v1.0 SHIP; W119 v1.5 (optimistic-settle + challenge window swaps into *this week's* `SettlementId` signature with no engine rewrite — §4A acceptance #4).
> **References** (consult when stuck — not scheduled as cover-to-cover): TigerBeetle `src/vsr/replica.zig` (`on_prepare`/`commit_journal`/`op` discipline) + `src/state_machine.zig` (op-number as identity); VSR paper (Liskov & Cowling 2012) §4 Normal Operation; Spanner OSDI'12 §4 (bounded-staleness reads) + Zanzibar "zookie" (op-token "at-least-this-fresh"); reth `crates/engine` (multi-subsystem assembly pattern, spine mirror); RECONCILED_PLAN_v3.md §4A v1.0 lock (settle interface block + acceptance #2/#4/#5/#8), §3 rows 3/5, §9 risk rows ("settle-stub becomes throwaway", "second hot-path log").
> **v3 modifications**: This week did **not** exist as a venue capstone in v2 — v2 used W103 for `vector-db` persistence + crash-recovery (WAL/Txn/integration). The **v3 deliverable is the `perp-dex-core` capstone assembly start** (vector-db persistence slips to the retained-optionality M31 buffer per INDEX §Phase-6; the WAL/snapshot/replay muscles it taught are *already inherited* from W26/W74/W92, "no primitive twice"). Three v3 locks govern every design choice this week: (1) **the order/event log IS the VSR log** — a single `VenueOp` op union flows through `consensus-vsr`; there is **no second hot-path log** (Kafka/Aeron fan-out reads a *projection* of the committed VSR log, off-path — W107). (2) **`SettlementId(snapshot_epoch, op_number, checksum)` = the VSR op-number** — settlement identity is *derived from consensus*, with **no side mapping table**; `finality_status` is read from the commit point; `Final = (op_number ≤ commit_num)`; idempotency/replay are free; the **stub sits behind an async-shaped signature** so v1.5's real chain + challenge window swap in *without an engine rewrite* (§4A acceptance #4). (3) **two projection staleness contracts** — control/risk reads are **read-after-commit from local applied state on the leader shard** (no consensus round-trip); analytics reads are **best-effort bounded-staleness (≤500 ms/≤1000 ops, metric not SLA) + op-number token**; a risk decision served from the analytics contract **must fail**.

> **[v3.1 reconciliation — acceptance #9 at dependency formation, Flag-A]** `perp-dex-core` depends on **`dyn ConsensusBackbone`** (the W91 trait), **never on `consensus-vsr` directly** — read the `consensus_vsr::NormalReplica<VenueSM>` / `StateMachine for VenueSM` wiring below as "the VSR adapter *behind* the trait." This week is where the dependency **forms**, so **acceptance #9 (the stub-swap test) lives HERE — Tuesday, not W117** (corrected from the stale "W113"; W117 only *re-verifies* it end-to-end). The **amended trait** is in force: client path is `propose → ProposalHandle` (not an op-number — see `submit() -> Result<OpToken,…>` below, where `OpToken` IS the `ProposalHandle`); commits resolve via `drain_committed` + `Commit::proposal_handle()`; abandonment via `drain_abandoned`; `SettlementId.op_number` + `finality_status` derive from **`commit_point()`** (the `commit_num()` references below are that trait method). Because the engine binds the *trait* not VSR, the W135 VSR→BFT swap touches only the adapter — proven at W103 by the Tuesday stub-swap test.

**The v3 separation this week locks in (read before Monday):** `perp-dex-core` is the **assembly crate** — it owns the **`VenueOp` op union** (the single thing that goes onto the VSR log), the **deterministic apply dispatch** (`apply(op_num, VenueOp)` fans into matching → risk → liquidation → ledger in a fixed order), the **`settle()` facade** (id = the VSR op-number), and the **two projection-read facades**. It does **not** re-implement matching, margin, liquidation, or the consensus protocol — those are consumed crates. The whole point of this week is to prove the **one-log lock** holds when four bar-(c) crates are composed onto a *single* `consensus-vsr` NormalReplica, and to plant the **settle seam** and the **read-plane split** so W104 (hybrid boundary), W105 (deploy), W107 (fan-out), W108 (cluster-VOPR), and W119 (v1.5 settle) slot in without a rewrite. Get the op union non-deterministic (any `HashMap` iteration in dispatch, any wall-clock, any side settle table) and the cluster diverges silently; conflate the two read planes and a stale tick liquidates the wrong account.

---

## Monday — `perp-dex-core` scaffold + the `VenueOp` op union (one log, no second log)

- [ ] Browse the four consumed crates' public command/op surfaces (`matching_engine::MatchCommand`, `risk_engine::MarginOp`, `liquidation_engine::LiqOp`, `ledger_deterministic::SettleOp`); map them into `notes/103_venue_op_map.md`. (~45 min)
  - 🎯 **Hardened acceptance**: the note lists every variant of all four crates' op enums with one line on which subsystem owns it, AND marks which become arms of the unified `VenueOp` vs which are *internal* (never on the wire — e.g. liquidation's reduce-only order is *emitted by* apply, not *submitted by* a client). The note names the **fixed apply order** (matching → risk → liquidation → ledger) and why that order is the only deterministic one.
  - *Mental model*: assembly is not "glue four servers together" — it is "fold four state machines into ONE pure function `apply: (State, VenueOp) → State` that the *single* VSR log drives in lockstep." The consumed crates are pure SMs already (W74/W99/W102/W92 each made them deterministic); this week makes their *composition* one SM.

- [ ] [HFT] **Build (bar (c))**: `crates/perp-dex-core/src/op.rs` + `crates/perp-dex-core/src/apply.rs` — the `VenueOp` op union (the **only** thing appended to the VSR log) + the deterministic `apply(op_num, VenueOp)` dispatch that fans into the four consumed SMs in a fixed order, byte-identically across replicas. (~total 215 min across 3 phases)
  - 🔧 **Why this matters**: this is the load-bearing claim of the entire capstone — **the order/event log, the settlement journal, and the replicated state machine are the *same* log** (the TigerBeetle pattern, §4A acceptance #2). If a teammate "scales reads" by adding a second hot-path log, the two logs disagree under partition, settlement double-applies, and cluster-VOPR (W108) can no longer prove linearizability. The 4 traps the debug session teaches: (1) a `HashMap`-keyed dispatch table whose iteration order leaks into apply → cross-replica divergence; (2) a `VenueOp` carrying a `String`/`Vec` → allocs on the apply path (bar-(c) violation) + the throughput cliff; (3) **non-fixed apply order** between subsystems (risk before matching on one replica, after on another) → divergence; (4) the **second-log betrayal** — emitting fills to a parallel Kafka topic *on the hot path*. **Your first attempt will fail ≥3 of the Phase-2 tests — the debug session IS the lesson.** Downstream: W104 wires the settle stub onto this `VenueOp`; W105 deploys it 3-node; W108 cluster-VOPR replays this exact apply discipline.

  - 🔧 **Decomposition prompt (silent test)**: this spec doesn't name primitives. The op union needs zero-alloc + `Copy`; the dispatch needs deterministic ordering (no `HashMap` iteration); the per-account state needs shard-by-account isolation (`runtime-thread-per-core`, W85); the apply path inherits the contiguity discipline from W74's `consensus-vsr`. The spec names none of them — your W74 (VSR apply) + W99 (incremental margin) + W102 (waterfall apply) muscles must combine. If dhat shows allocs, or the cross-replica hash diverges, or apply order is observable — your decomposition missed something specific.

  #### Phase 1 — Problem, prediction, design (~55 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in `crates/perp-dex-core/notes/103_op_prediction.md`:
    - P1. The type you'd reach for to dispatch a `VenueOp` variant to its subsystem — write the exact line (`match op { ... }`? `HashMap<Discriminant, fn>`? a `[fn; N]` table indexed by discriminant?). Predict its alloc + determinism properties.
    - P2. Predicted `size_of::<VenueOp>()` if `NewOrder` carries a `String symbol` and the liquidation arm carries a `Vec<Fill>`. Then predict allocs/apply at 1M orders/s.
    - P3. Predicted determinism outcome: two replicas apply `log[0..N]`; on replica A `apply` runs `risk.update()` *before* `matching.apply()`, on replica B *after*. Same state hash? yes/no — why.
    - P4. Predicted crash/partition outcome if fills are also written to a parallel Kafka topic on the hot path: after a partition heals, do the VSR log and the Kafka topic agree on order? What does settlement (keyed off which?) double-apply?
    - P5. **List every Phase-2 test you predict your first attempt will FAIL**, and why. Compare against reality after the tests run.
  - *Mental model*: `VenueOp` is the **single wire type of the venue** — the one thing `consensus_vsr::NormalReplica::handle_client_request` ever sees. `apply(op_num, VenueOp)` is a *deterministic fold*: it routes to matching, then feeds the resulting fills into risk, then risk's margin signal into liquidation, then committed fills into the ledger — **in that exact order on every replica**. The four subsystems stop being "services"; they become *sub-folds of one fold*.
  - *Contract* (signature-level, NEVER the body):
    ```rust
    #[derive(Clone, Copy)]                          // R2: every variant Copy, no String/Vec
    pub enum VenueOp {
        NewOrder(NewOrder), Cancel(CancelOrder), Amend(AmendOrder),
        MarkTickObserved(MarkTick),                 // input from oracle-mark (W80), not computed here
        AdvanceClock(SeqTick),
        SettleBatch(SettleRequest),                 // -> SettlementId = this op's op_number (Wed)
        // NO raw liquidation/funding variants: liquidation is EMITTED by apply, not submitted by clients
    }
    pub struct PerpDexCore { /* NormalReplica<VenueSM> + 4 consumed SMs + per-account shards */ }
    impl PerpDexCore {
        pub fn submit(&self, op: VenueOp) -> Result<OpToken, SubmitError>;   // leader: -> batched Prepare
        fn apply(&mut self, op_num: u64, op: VenueOp);                       // every replica: contiguous, det.
        pub fn state_hash(&self) -> [u8; 32];                               // canonical, sorted, cross-replica cmp
        pub fn commit_num(&self) -> u64;                                     // the finality boundary (Wed)
    }
    ```
  - *Back-of-envelope* (bar (c) — name the cost): target 1M orders/s ingest, ~100k accounts, mark ticks ~10/s (§4A capacity).
    - Dispatch cliff: a `HashMap<Discriminant, fn>` lookup ≈ ~25 ns probe + non-deterministic iteration if ever walked; a `match` on a `Copy` enum ≈ ~1–2 ns (jump table), 0 allocs, fully deterministic. At 1M ops/s: 25 ns × 1M = **25 ms/s of dispatch overhead vs ~2 ms/s** — and the HashMap path risks divergence, which is unbounded cost.
    - Apply-path alloc cliff: `String symbol` + `Vec<Fill>` ≈ ~3 allocs/op × 1M/s × ~50 ns = **150 ms of allocator wall-clock per second of trading** + cache pollution → p99 jitter. `Copy` op (`Symbol=[u8;8]`, fixed-cap inline fills) = 0 allocs, ~one cache line, memcpy ≈ 4 ns.
    - Apply-order cost: there is no perf cliff here — it is a *correctness* cliff. A non-fixed order is byte-divergence, the most expensive bug class (surfaces only in multi-replica long runs).
    - Second-log cost: a parallel hot-path Kafka write ≈ +1 network round-trip/op AND a divergence vector. The fix (projection read of the committed VSR log, W107) is *zero* hot-path cost.

  - **The problem this primitive solves**: composing four independently-deterministic SMs onto one VSR log introduces THREE new failure categories the individual crates never faced: (a) **composition non-determinism** (a `HashMap` in the dispatch, or a non-fixed inter-subsystem apply order, diverges replicas even though each subsystem is internally deterministic); (b) **op-union scope creep** (the temptation to put raw funding/liquidation *client commands* on the log, re-merging crates v3 split); (c) **the second-log betrayal** (a parallel hot-path log "for read scaling" that breaks the single-log lock). The fix: a `Copy` op union + a *fixed* apply order + a hard "no second hot-path log" rule (fan-out reads a projection, W107).

  - **Requirements** — write in `crates/perp-dex-core/notes/103_op_design.md`:
    - R1. `VenueOp` is the **only** type appended to the VSR log; `submit()` proposes it via `consensus_vsr::NormalReplica::handle_client_request`; every replica receives `apply(op_num, VenueOp)`.
    - R2. Every `VenueOp` variant is `Copy` (no `String`, no `Vec` inside): `Symbol=[u8;8]`, `OrderId=u64`, fixed-cap inline arrays only. `size_of::<VenueOp>() ≤ 64` (one cache line).
    - R3. **Determinism gate**: no `Instant::now()`, no `SystemTime`, no `rand::thread_rng`, no `HashMap`/`AHashMap` iteration, no `f64` anywhere in the apply path. CI grep + clippy `disallowed-types` enforce.
    - R4. **Contiguous apply**: `apply(op_num)` requires `op_num == self.applied_op + 1`; gap or duplicate is a panic (inherited from W74 `consensus-vsr`).
    - R5. **Fixed apply order**: `apply` always fans into the four subsystems in the order matching → risk → liquidation → ledger; the order is a documented invariant, never data-dependent, identical on every replica.
    - R6. **No raw liquidation/funding client op**: liquidation is *emitted by* the apply path (a reduce-only order appended as a follow-on op or applied in-fold), never submitted as a client `VenueOp`. A grep CI step asserts no client-facing `LiquidationStep`/`ApplyFunding` variant in `VenueOp`.
    - R7. **One log lock**: there is no second hot-path log; the apply path writes nothing to Kafka/Aeron directly. Fan-out (W107) reads a projection of the committed VSR log. A grep CI step asserts no `KafkaProducer`/`AeronPublication` *write* in `apply*`.
    - R8. **Zero alloc on apply**: `VenueOp` reused from a per-shard pool; 0 heap allocs across 1M ops (dhat-verified).
    - R9. **Cross-replica byte-identical**: 100 random scenarios × 10k ops; every replica's `state_hash()` matches at every commit point.
    - R10. **Cross-arch byte-identical**: x86 + aarch64 replicas on the same op stream → same state hash (no `f64`, no `HashMap` iteration).

  - **Design walk-through** D1–D8 — answer each in the design doc; **no peeking at TigerBeetle `replica.zig`/`state_machine.zig` until Phase 3**:
    - D1. Sketch `VenueOp`. Why is every variant `Copy`? What is the `size_of`? Which variants are client-submittable vs apply-emitted (liquidation)?
    - D2. Dispatch shape: a `match` on the enum vs a `HashMap<Discriminant, fn>` table. Which is deterministic AND zero-alloc? Write the `match` skeleton (no bodies).
    - D3. **Fixed apply order**: write the 4-step fold inside `apply`. Why is matching → risk → liquidation → ledger the only correct order? (Hint: liquidation needs the *post-trade* margin signal; the ledger needs *committed* fills.) What breaks if risk runs before matching?
    - D4. Determinism audit: name the 5 banned types/calls. Write the grep regex for the CI guard over `crates/perp-dex-core/src/apply*`.
    - D5. State-hash canonicalization: each subsystem already hashes its own state (W74/W99/W102). How does `PerpDexCore::state_hash` combine four sub-hashes into one canonical 32 bytes *deterministically* (sorted order, fixed concatenation — never a `HashMap` walk)?
    - D6. **Anti-shortcut audit** — name the "obvious" reach + one line on why it's wrong HERE:
      - *"`HashMap<Discriminant, Box<dyn Fn>>` dispatch — it's extensible"* → wrong: heap + non-deterministic iteration if ever walked; a `match` on a `Copy` enum is a jump table, 0 allocs, deterministic.
      - *"`VenueOp::NewOrder` can hold a `String symbol`, it's convenient"* → wrong: 3 allocs/apply = 150 ms/s allocator overhead at 1M/s. `Symbol=[u8;8]`.
      - *"let each replica apply subsystems in whatever order is convenient"* → wrong: non-fixed order = byte divergence; pin matching → risk → liquidation → ledger.
      - *"put `LiquidationStep` on the log so clients can trigger liq"* → wrong: liquidation is policy the engine *decides*, not a client op; it is apply-emitted. Re-adding it re-merges the crate boundary.
      - *"also publish fills to Kafka in apply, so dashboards are live"* → wrong (the v3 trap): a second hot-path log breaks the single-log lock; fan-out reads a *projection* of the committed VSR log (W107).
    - D7. **Pre-mortem #1 (non-fixed apply order — load-bearing):** write the EXACT 5-step interleaving where replica A runs `risk.update()` before `matching.apply()` and replica B after, on the same op. Show the resulting margin signal differs, liquidation fires on one replica only, and the state hashes diverge. **If you cannot write the bad interleaving, your apply-order story is wrong — that's the point.**
    - D8. **Pre-mortem #2 (second-log betrayal):** write the 5-step trace where someone adds a parallel hot-path Kafka topic that the apply path writes fills to directly. Show: (i) under partition the VSR log and Kafka topic disagree on order; (ii) settlement keyed off the wrong log double-applies; (iii) "no primitive twice" is violated; (iv) cluster-VOPR (W108) can't prove linearizability across two logs; (v) the fix — Kafka/Aeron read a *projection* of the committed VSR log (`log-distributed` W84 / W107), never an independent hot-path write target.
  - **Output**: `crates/perp-dex-core/notes/103_op_design.md` with R1–R10, D1–D8, the `VenueOp` sketch, the fixed-apply-order fold diagram, the two pre-mortem traces, the determinism grep.

  #### Phase 2 — Write the failing tests (~50 min)

  Each engineered so a naive first impl FAILS it. Tier annotated. All fail initially because `VenueOp`/`PerpDexCore` don't exist — expected.

  - [ ] **(NEWBIE) `venue_op_is_copy_and_one_cache_line`** — exercises R2. Compile-time trap on a `String`/`Vec`-carrying op.
    ```rust
    use static_assertions::{assert_impl_all, const_assert};
    assert_impl_all!(VenueOp: Copy);
    const_assert!(std::mem::size_of::<VenueOp>() <= 64);
    ```
  - [ ] **(NEWBIE) `apply_contiguity_panic_on_gap`** — exercises R4. `#[should_panic(expected = "apply gap")]`; apply op 1 then op 3.
  - [ ] **(NEWBIE) 🪤 scope-gate `venue_op_has_no_raw_liquidation_variant`** — exercises R6. Exhaustive `match` over `VenueOp` with no `_` arm; CI grep companion `! grep -rniE 'LiquidationStep|ApplyFunding' crates/perp-dex-core/src/op.rs`. A client-submittable liquidation variant fails to compile here.
  - [ ] **(NEWBIE) 🪤 one-log-lock `apply_writes_no_second_hot_path_log`** — exercises R7. CI grep `! grep -rnE 'KafkaProducer|AeronPublication|\.publish\(|producer\.send' crates/perp-dex-core/src/apply*` (zero matches).
  - [ ] **(SENIOR) 🪤 anti-shortcut `dhat_apply_path_zero_alloc`** — exercises R8. The obvious `String`/`Vec` op passes correctness but allocates; this fires on the bar.
    ```rust
    let _p = dhat::Profiler::builder().testing().build();
    let mut c = PerpDexCore::new_replica(); c.warm_pool();
    let before = dhat::HeapStats::get();
    for i in 1..=1_000_000u64 { c.apply(i, sample_new_order(i)); }
    let after = dhat::HeapStats::get();
    assert!(after.total_blocks - before.total_blocks <= 100,
        "apply allocated {} blocks over 1M ops — VenueOp must be Copy/pooled", after.total_blocks - before.total_blocks);
    ```
  - [ ] **(EXPERT) 🪤 cross-primitive `cross_replica_state_hash_identical`** — composes apply (this Build) with all four consumed SMs. Each subsystem is deterministic in isolation; a `HashMap`-keyed dispatch or non-fixed apply order diverges only under cross-replica composition. Exercises R9.
    ```rust
    for seed in 0..100u64 {
        let log = random_venue_scenario(seed, 10_000);
        let mut reps: Vec<_> = (0..3).map(|_| PerpDexCore::new_replica()).collect();
        for (i, op) in log.iter().enumerate() {
            let on = i as u64 + 1;
            for r in &mut reps { r.apply(on, *op); }
            let h0 = reps[0].state_hash();
            for r in &reps[1..] { assert_eq!(r.state_hash(), h0,
                "replica diverged at op {on} seed {seed} — HashMap dispatch or non-fixed apply order"); }
        }
    }
    ```
  - [ ] **(EXPERT) 🪤 fixed-order trap `apply_order_is_matching_risk_liq_ledger`** — drive an op that triggers a liquidation; assert liquidation observes the *post-trade* margin (risk ran after matching) and the ledger observes the *committed* fill (ledger ran last). A reversed order makes liquidation fire on a stale pre-trade margin → wrong result. Exercises R5.
  - [ ] **(EXPERT) `cross_arch_state_hash_identical`** — exercises R10. Same log on x86 + ARM CI matrix; `assert_eq!(x86_hash, arm_hash)`. Traps any `f64` in the composed path.
  - [ ] **(SENIOR) `lint_no_wall_clock_or_float_in_apply`** — exercises R3. CI: `! grep -rnE 'Instant::now|SystemTime|f64|f32' crates/perp-dex-core/src/apply*` + clippy `disallowed-types=["f64","f32"]` build-fails on an injected float.
  - [ ] **(SENIOR) negative-control `cross_replica_test_explores_real_ops`** — proves `cross_replica_state_hash_identical` isn't vacuous: assert the scenario actually triggered ≥1 fill AND ≥1 liquidation across the 100 seeds (else the test would pass even if liquidation never ran). If counts are 0, the trap shape isn't exercised and this fires.

  #### Phase 3 — Make the tests pass, one failure at a time (~55 min)

  Ordered newbie → expert — the ladder you actually walk.

  - **(NEWBIE) When `venue_op_has_no_raw_liquidation_variant` fails because you kept `LiquidationStep` "so clients can trigger liq":**
    - Symptom: the grep fires on `LiquidationStep`; the exhaustive match only compiles because you added it.
    - Why: liquidation is a *policy the engine decides* from the risk signal, not a client command; putting it on the wire re-merges the crate boundary v3 split.
    - Fix: delete the variant. Liquidation is *emitted by* `apply` (the fold computes the margin breach and applies the reduce-only follow-on in-fold). Clients submit orders/marks/settle; the engine decides liquidations.
    - Cost avoided: a re-merged scope boundary that kills the `liquidation-engine` seam and breaks "no primitive twice" (the W117 bet-readiness claim depends on it).
    - Muscle: **a client op union is the venue's scope contract — engine-decided policy is apply-emitted, never client-submittable.** Reapplies at: W104 hybrid boundary op set, W108 cluster-VOPR op generator, W119 ADL trigger (also apply-emitted).

  - **(NEWBIE) When `apply_writes_no_second_hot_path_log` fires on a `producer.send` in apply:**
    - Symptom: grep finds a Kafka/Aeron write inside the apply path ("for the live dashboard").
    - Why: you added a parallel hot-path log; under partition it disagrees with the VSR log.
    - Fix: remove the write. The committed VSR log is the *only* hot-path log; fan-out (W107 / `log-distributed` W84) subscribes to a *projection* of committed ops off-path.
    - Cost avoided: two logs disagreeing under partition → settlement double-apply → unbounded correctness loss; cluster-VOPR (W108) unable to prove linearizability.
    - Muscle: **one hot-path log; everything else reads a projection of the committed log.** Reapplies at: W107 projection fan-out, W108 cluster-VOPR, W84 `log-distributed`.

  - **(SENIOR) When `dhat_apply_path_zero_alloc` reports allocations:**
    - Symptom: heaptrack shows `VenueOp`-related allocations dominating; p99 apply latency erratic.
    - Why: a `VenueOp` variant carries `String symbol`/`Vec<Fill>`; ~3 allocs/op × 1M/s ≈ 150 ms/s allocator overhead + jitter.
    - Fix: every variant `Copy`; `Symbol=[u8;8]`, fixed-cap inline fill arrays; reuse from a per-shard pool. Op struct ≤ one cache line.
    - Cost avoided: ~150 ms/s allocator overhead → 0 allocs; restores predictable p99 (**bar (c) static-mem invariant**).
    - Muscle: **every type traversing a replicated apply path is `Copy` or pooled.** Reapplies at: W104 settle op, W108 VOPR op generator, W92 ledger `Transfer=[u8;128]`.

  - **(EXPERT) When `apply_order_is_matching_risk_liq_ledger` fails — liquidation fires on a stale margin:**
    - Symptom: liquidation triggers off the *pre-trade* margin; the just-executed fill that would have restored margin is ignored.
    - Why: the fold ran risk (or liquidation) before matching, so the margin signal predates the trade.
    - Fix: pin the order — matching first (produces fills), then risk (recomputes margin incrementally from those fills, W99), then liquidation (decides off the post-trade margin), then ledger (records committed fills). Document the order as an invariant in `apply.rs`.
    - Cost avoided: wrong-account / spurious liquidations — a correctness and reputational failure on the venue's backstop.
    - Muscle: **a composed fold has exactly one correct subsystem order; pin it and document it as an invariant.** Reapplies at: W104 settle-into-ledger ordering, W108 cluster-VOPR replay order, W119 ADL ordering.

  - **(EXPERT) When `cross_replica_state_hash_identical` diverges intermittently:**
    - Symptom: replicas converge for thousands of ops then diverge at one seed.
    - Why: the dispatch used a `HashMap<Discriminant, fn>` whose iteration leaked into a state hash, OR a subsystem's sub-hash combined via a `HashMap` walk instead of a fixed sorted concatenation.
    - Fix: `match` on the `Copy` enum (jump table, no iteration); `state_hash` concatenates the four sub-hashes in a *fixed* order and hashes the result. No `HashMap` anywhere in apply or hash.
    - Cost avoided: silent multi-replica divergence — the most expensive bug class; defeats the capstone's core claim.
    - Muscle: **any `HashMap` iteration order that reaches state is a divergence vector — `match` + fixed concatenation only.** Reapplies at: W108 cluster-VOPR StateChecker, W92 ledger snapshot canonical form.

  - **(EXPERT) When `cross_arch_state_hash_identical` diverges x86 vs ARM:**
    - Symptom: same log, different hashes across arch; small at first, compounding.
    - Why: an `f64` somewhere in the composed path (a price-band check, a careless `MarkTick`); x86 FMA ≠ ARM Neon rounding.
    - Fix: integer-only math (ticks/bps/lots) everywhere; `#![deny(clippy::float_arithmetic)]` on the crate; `f64` only at the presentation edge.
    - Cost avoided: compounding cross-arch divergence (**bar (c) violation** of cross-arch determinism).
    - Muscle: **cross-arch determinism = integer arithmetic only.** Reapplies at: W99 incremental margin, W102 waterfall math, W92 ledger balances.

  #### 5-year failure mode
  Write in `crates/perp-dex-core/notes/103_op_followup.md` after green: when instrument count grows past a single VSR group's apply budget (e.g. 100-symbol shards or options/exotics), one `VenueOp` log per group becomes the bottleneck — apply serializes all instruments through one fold. **Trigger**: single-group apply p99 > the per-mark-tick budget at the live instrument count, or aggregate ingest > one group's commit ceiling. **Migration**: instrument-group sharding — one VSR log *per instrument group* with a cross-group settlement barrier; the invariant you'd lose is *global* cross-margin netting across groups (it becomes a two-phase reconcile). Name the cross-group ordering primitive reused.
- [ ] Commit + log (~10 min)

## Tuesday — `perp-dex-core`: wire the `VenueSM` onto `consensus-vsr` (3-node cluster, single hot-path log)

- [ ] [HFT] **Build (bar (c))**: `crates/perp-dex-core/src/cluster.rs` — implement `StateMachine for VenueSM` so `consensus_vsr::NormalReplica<VenueSM>` drives the `VenueOp` log; stand up a 3-node in-process cluster harness; prove a single committed `VenueOp` log replicates byte-identically across all three. (~total 170 min across 3 phases)
  - 🔧 **Why this matters**: Monday built the *fold*; Tuesday makes it a *replicated* fold over the **one log**. This is §4A acceptance #2 ("3-node VSR survives crash/restart with no lost commits, and the order/event log is the VSR log"). The `NormalReplica<SM>` generic (W74) was *built* for exactly this plug-in — the venue SM slots into the same hot-path log as the matching engine and the ledger, with **no second consensus**. The 3 traps: (1) the SM's `apply` not idempotent under VSR re-delivery (W74 contiguity assumed, but state-transfer overlap, W76, can re-feed); (2) the leader buffering ops in a non-deterministic structure so two leaders propose different orders; (3) per-op fsync collapsing throughput (group-commit, inherited from W74, must be wired through). **Your first attempt will fail ≥2 of the Phase-2 tests.** Downstream: W105 deploys this 3-node for real; W108 cluster-VOPR drives crash/partition over it.

  #### Phase 1 — Problem, prediction, design (~45 min, no code yet)
  - 🎯 **Pre-prediction prompt** — seal in `crates/perp-dex-core/notes/103_cluster_prediction.md`: P1 the `StateMachine` trait method that VSR calls on commit (`apply(op_num, op)`?) and whether it must be idempotent (predict + why, ref W76 state-transfer overlap); P2 predicted replicated throughput at 3 replicas if you fsync per op vs group-commit per Prepare batch (two numbers); P3 predicted divergence if the leader buffers proposals in a `HashSet` (yes/no); P4 predicted outcome of `kill -9` mid-apply on the leader then view-change (committed ops lost? state hash match across survivors?); P5 the Phase-2 tests you predict failing.
  - *Mental model*: `VenueSM` is a *thin adapter* — it forwards VSR's committed `apply(op_num, VenueOp)` callback into Monday's fold and exposes `state_hash()`. The hard part is already solved in `consensus-vsr` (W74/W90); this Build proves the venue plugs in *without protocol changes* — the "one hot-path log" lock made concrete.
  - *Contract*:
    ```rust
    impl consensus_vsr::StateMachine for VenueSM {
        type Op = VenueOp;
        fn apply(&mut self, op_num: u64, op: VenueOp);   // forwards into PerpDexCore::apply (Mon)
        fn state_hash(&self) -> [u8; 32];                // forwards into PerpDexCore::state_hash
    }
    pub struct VsrVenueCluster { /* 3 × NormalReplica<VenueSM> + a deterministic message bus */ }
    impl VsrVenueCluster {
        pub fn new(n: usize) -> Self;
        pub fn leader(&self) -> &NormalReplica<VenueSM>;
        pub fn deliver_all(&mut self);                   // pump Prepare/PrepareOk to quiescence
        pub fn kill_leader_and_view_change(&mut self);   // stub election (full VC is W75)
    }
    ```
  - *Back-of-envelope* (bar (c)): inherits W74's batched-Prepare + group-commit numbers; the venue adds only the fold cost (~Monday's apply, ~tens of ns/op). Target: ≥30k `VenueOp`/s replicated at 3 replicas, p99 commit ≤2 ms LAN — *no regression* vs the bare `consensus-vsr` numbers (the venue SM must not be the bottleneck).
  - **Requirements** — write in `notes/103_cluster_design.md`: R1 `VenueSM: StateMachine` forwards `apply`/`state_hash` into Monday's `PerpDexCore`; R2 **idempotent apply** — re-delivery of an already-applied op (state-transfer overlap, W76) is a no-op, not a double-apply; R3 the leader buffers proposals in a *deterministic ordered* structure (a `Vec`, never a `HashSet`), so any leader proposes the same order; R4 group-commit fsync inherited from W74 (one fsync per Prepare batch), no per-op fsync; R5 3-node cluster: a committed op is byte-identical across all 3 replicas at every commit point; R6 `kill -9` mid-apply + view-change loses zero committed ops and survivors' `state_hash()` match; R7 **no second log** — the cluster carries exactly one `VenueOp` log (re-assert R7 from Monday at the cluster layer).
  - **Design walk-through** D1 the `StateMachine` adapter — how thin? what does it own beyond forwarding? D2 idempotency under re-delivery — how does `apply` detect an already-applied op without a `HashMap` (hint: `op_num ≤ applied_op` ⇒ no-op; rely on contiguity)? D3 leader proposal buffer ordering — why a `Vec` not a `HashSet`? D4 **Pre-mortem (load-bearing):** the 5-step interleaving where a state-transfer (W76) re-feeds ops `[N-5..N]` that the SM already applied; show why non-idempotent apply double-counts a fill and diverges, and how `op_num ≤ applied_op ⇒ no-op` saves it. **Output**: `103_cluster_design.md` with R1–R7, D1–D4, the adapter sketch, the re-delivery pre-mortem trace.

  #### Phase 2 — Write the failing tests (~40 min)
  - [ ] **(NEWBIE) `three_node_cluster_commits_1000_ops`** — `VsrVenueCluster::new(3)`; leader takes 1000 `VenueOp`s; `deliver_all`; assert every replica `commit_num == 1000`. Exercises R5.
  - [ ] **(SENIOR) 🪤 idempotent-apply `redelivery_of_applied_op_is_noop`** — apply ops 1..=100, then re-feed ops 96..=100 (simulating state-transfer overlap); assert `state_hash()` unchanged and no fill double-counted. Exercises R2.
    ```rust
    let h = c.replica(0).state_hash();
    for op_num in 96..=100 { c.replica(0).apply(op_num, log[op_num as usize - 1]); }  // re-delivery
    assert_eq!(c.replica(0).state_hash(), h, "re-delivered ops double-applied — apply not idempotent");
    ```
  - [ ] **(SENIOR) 🪤 leader-determinism `two_leaders_propose_same_order`** — two distinct nodes act as leader over the same pending set; assert the proposed `VenueOp` order is identical (traps a `HashSet` buffer). Exercises R3.
  - [ ] **(SENIOR) `group_commit_one_fsync_per_batch`** — instrument fsync count; submit a 50-op batch; assert `fsync_count == 1`, not 50 (inherited W74 discipline holds through the venue SM). Exercises R4.
  - [ ] **(EXPERT) 🪤 cross-primitive `kill_leader_midburst_no_lost_commits`** — leader takes 100k ops, `kill -9` mid-apply, stub view-change, assert post-VC `state_hash()` identical across the 2 survivors AND no committed op lost. Exercises R6. (Full view-change is W75; this stubs the election and asserts the SM invariant.)
  - [ ] **(NEWBIE) one-log-lock `cluster_carries_single_op_log`** — assert the cluster exposes exactly one log handle and no Kafka/Aeron *write* sink on the commit path. Exercises R7.
  - [ ] **(EXPERT · acceptance #9, Flag-A — the dependency-formation gate) `perp_dex_core_depends_on_trait_not_consensus_vsr`** — TWO halves, both at THIS week (where the dependency forms): **(i) the does-not-name grep** — `cargo tree -i consensus-vsr` from `perp-dex-core` shows it ONLY via the thin VSR-adapter crate (a `consensus-vsr-backbone` adapter impl'ing `ConsensusBackbone`), never the engine crates; `! grep -rn 'use consensus_vsr::' crates/perp-dex-core/src/` (the engine `src/` names the trait, not VSR). **(ii) the stub-swap-compiles test** — a no-op `StubBackbone` (single-node, `propose`/`drain_committed`/`drain_abandoned`/`commit_point`/`applied_point`/`role` all trivially implemented, `verify→true`) is accepted by `PerpDexCore::new_on::<B: ConsensusBackbone<Op = VenueOp>>(backbone: B)` with ZERO change to apply/settle/read — proving the engine binds the trait, and that the W135 VSR→BFT swap is an adapter change. A `perp-dex-core` that constructs `NormalReplica` inline FAILS half (i).
    ```rust
    // tests/backbone_swap.rs (compile + run)
    fn assert_engine_is_backbone_generic<B: ConsensusBackbone<Op = VenueOp>>(b: B) {
        let _core = PerpDexCore::new_on(b);     // engine binds the trait, not consensus-vsr
    }
    #[test] fn vsr_adapter_drives_engine() { assert_engine_is_backbone_generic(VsrBackboneAdapter::new(3)); }
    #[test] fn stub_backbone_drives_engine() { assert_engine_is_backbone_generic(StubBackbone::single_node()); } // MUST compile + run
    ```
    Exercises acceptance #9 (Flag-A: established here at dependency formation; re-verified end-to-end at W117). **This is the W103 HALT subject.**
  - [ ] **(SENIOR) negative-control `redelivery_test_actually_redelivers`** — proves `redelivery_of_applied_op_is_noop` isn't vacuous: assert the re-fed ops are non-empty AND were already in the applied range (else "no-op" is trivially true because nothing was re-fed).

  #### Phase 3 — Make the tests pass, one failure at a time (~45 min)
  - **(NEWBIE) When `cluster_carries_single_op_log` finds a second sink:** Symptom: a commit-path Kafka write slipped in. Why: "live dashboard" shortcut. Fix: remove it; fan-out reads a projection (W107). Muscle: **one hot-path log at the cluster layer too.** Reapplies at: W107, W108, W84.
  - **(SENIOR) When `redelivery_of_applied_op_is_noop` double-counts:** Symptom: a re-fed fill increments balance twice; hash changes. Why: `apply` mutated unconditionally on every callback. Fix: `if op_num <= self.applied_op { return; }` before the fold (lean on contiguity; no `HashMap` of seen ops). Muscle: **idempotent apply via the op-number watermark, not a seen-set.** Reapplies at: W76 state-transfer, W104 settle re-submit, W108 VOPR replay.
  - **(SENIOR) When `two_leaders_propose_same_order` diverges:** Symptom: two leaders propose different op orders from the same pending set. Why: pending buffered in a `HashSet` (seed-randomized iteration). Fix: a `Vec` in arrival order (or a deterministic priority); any leader replays the same sequence. Muscle: **leader proposal order must be deterministic across leaders.** Reapplies at: W75 view-change, W108 cluster-VOPR.
  - **(SENIOR) When `group_commit_one_fsync_per_batch` reports 50 fsyncs:** Symptom: per-op fsync through the venue SM. Why: the SM adapter triggered a sync per `apply` instead of letting VSR's batched-Prepare own the fsync boundary. Fix: the SM never fsyncs; durability is `consensus-vsr`'s group-commit (W74). Cost avoided: ~49 fsyncs × ~1 ms ≈ 49 ms/batch. Muscle: **durability lives in the consensus layer, not the SM.** Reapplies at: W104, W105, W92.
  - **(EXPERT) When `kill_leader_midburst_no_lost_commits` loses ops:** Symptom: a survivor is missing committed ops post-VC. Why: the SM applied an op *before* it was committed (uncommitted apply), so the killed leader's uncommitted tail vanished but a survivor had already shown it. Fix: apply only at the commit point (VSR `commit_num` advance), never on Prepare receipt; survivors converge on the committed prefix. Cost avoided: lost committed orders → the worst venue failure. Muscle: **apply at commit, never at propose/prepare.** Reapplies at: W75 view-change, W108 partition drills, W92 ledger commit.

  #### 5-year failure mode
  Write in `notes/103_cluster_followup.md`: at cross-DC latencies (50 ms RTT) the single-group commit latency dominates the per-mark-tick budget. **Trigger**: first cross-region deploy (post-v1.5, W118+). **Migration**: geo-aware flexible-quorum (Heidi Howard) + pipelined cumulative-ack Prepare; name the quorum primitive reused from `consensus-vsr`.
- [ ] Commit + log (~10 min)

## Wednesday — `perp-dex-core`: async settle interface — `SettlementId` = the VSR op-number (the seam)

- [ ] [HFT] **Build (bar (c))**: `crates/perp-dex-core/src/settle.rs` — `settle(batch) -> SettlementId(snapshot_epoch, op_number, checksum)` where the id **IS** the VSR op-number of the `SettleBatch` op; `finality_status(id) -> {Pending, Final, Reverted}` derived from the commit point (`Final = op_number ≤ commit_num`); the **stub sits behind the async-shaped signature** so W104/W119 swap a real chain + challenge window in *without an engine rewrite*. **No side mapping table.** (~total 175 min across 3 phases)
  - 🔧 **Why this matters**: this is §4A acceptance #4 — the load-bearing seam that makes the v1.0 settle-stub *not* throwaway (§9 risk row "settle-stub becomes throwaway"). Settlement identity is **derived from consensus** (the op-number), so idempotency/replay are *free* (resubmitting the same op yields the same id, a no-op) and there is **no auxiliary state to keep consistent across replicas**. The `snapshot_epoch` makes the id survive log compaction / view-change (op-numbers are epoch-relative), so an external client or chain holding a `SettlementId` is never invalidated by a renumber. The 4 traps: (1) a side `HashMap<BatchId, SettlementStatus>` table (the thing the spec *forbids* — extra replicated state + a divergence vector); (2) `finality_status` computed eagerly/cached instead of *derived* from the live commit point (stale finality); (3) a synchronous `settle() -> Result<Receipt>` signature that blocks on the stub and can't absorb a real chain's latency later (the rewrite trap); (4) an op-number that resets on snapshot, invalidating outstanding ids. **Your first attempt will fail ≥3 of the Phase-2 tests.** Downstream: W104 wires the real hybrid-boundary stub *into this exact signature*; W119 swaps optimistic-settle + challenge window in with no engine rewrite.

  #### Phase 1 — Problem, prediction, design (~50 min, no code yet)
  - 🎯 **Pre-prediction prompt** — seal in `crates/perp-dex-core/notes/103_settle_prediction.md`: P1 the type of `SettlementId` and whether it needs ANY storage beyond the op-number (predict + why — the §4A "no side mapping table" lock); P2 predicted `finality_status` cost if derived from `commit_num` (one compare) vs looked up in a side table (probe + the divergence risk); P3 predicted outcome of resubmitting the identical `SettleBatch` op twice — same id? double-settle? (idempotency from op-number); P4 predicted outcome of a snapshot/view-change renumber on an outstanding `SettlementId` if op-numbers are NOT epoch-relative (id invalidated?); P5 the Phase-2 tests you predict failing.
  - *Mental model*: settlement is **not a side ledger** — it is a *read over the VSR log*. The id is the op's position; the status is `op_number ≤ commit_num` evaluated *now*, against the *live* commit point. The async-shaped signature is a *promise to the future*: today it returns an instant-Final id; tomorrow the same signature returns a `Pending` id that a real chain later transitions to `Final`/`Reverted` — *with no change to the engine or the call sites*.
  - *Contract* (the §4A lock — NEVER the body):
    ```rust
    pub struct SettlementId(u64, u64, u128);   // (snapshot_epoch, op_number, checksum) — newtype, externally safe
    impl PerpDexCore {
        pub fn settle(&self, batch: SettleRequest) -> SettlementId;     // returns immediately; id = the SettleBatch op's VSR position
        pub fn finality_status(&self, id: SettlementId) -> Finality;    // derived from the commit point — no side table
    }
    pub enum Finality { Pending, Final, Reverted }
    // v1.0: settle() proposes a SettleBatch VenueOp; id = its op_number; Final = (op_number <= commit_num);
    //       Reverted unreachable (no fraud-proof/bonding/reorg in v1.0). checksum = dedup/validation key.
    ```
  - *Back-of-envelope* (bar (c)): `finality_status` = one `u64` compare against `commit_num` ≈ ~1 ns, zero allocs, zero extra state. A side `HashMap<BatchId, Status>` table = ~25 ns probe + replicated-state growth + a divergence vector — the cliff is not latency, it is *correctness + replication cost*. `settle()` itself is one `VenueOp` append (inherits Tue's batched-Prepare path).
  - **Requirements** — write in `notes/103_settle_design.md`: R1 `settle(batch)` proposes a `SettleBatch` `VenueOp`; the returned `SettlementId.op_number` IS that op's VSR op-number. R2 **no side mapping table** — `finality_status` derives purely from `op_number` vs the live `commit_num`; no auxiliary replicated state. R3 `Final = (op_number ≤ commit_num)`; `Pending` while not yet committed; `Reverted` unreachable in v1.0 (reserved for v1.5 challenge-window). R4 **idempotency/replay free** — resubmitting the identical batch (same `checksum`) yields the same id and is a no-op (the op-number is stable; `checksum` dedups). R5 **epoch-relative op-numbers** — `snapshot_epoch` makes the id survive log compaction / snapshot / view-change; an outstanding id is never invalidated by a renumber. R6 **async-shaped signature** — `settle` returns an id *immediately* (no block on the stub); the signature is the one a real chain + challenge window (v1.5) slots into with no engine rewrite (longer `Pending`; `Reverted` becomes reachable). R7 the `SettlementId` newtype hides the representation so it can change in v2 without an API break. R8 cross-replica: `finality_status` for the same id returns the same `Finality` on every replica that has reached the same `commit_num`.
  - **Design walk-through** D1 why is the op-number a *sufficient* identity (no side table)? what does `checksum` add (dedup/validation, not identity)? D2 `finality_status` derivation: write the one-line body shape (`if id.op_number <= self.commit_num { Final } else { Pending }`) and why caching it is wrong. D3 epoch-relativity: how does `snapshot_epoch` survive a compaction that renumbers ops? (op-numbers interpreted relative to their epoch's base.) D4 **the v1.5 swap (load-bearing):** show *exactly* how optimistic-settle + a challenge window slots into this signature — `Pending` lasts the challenge window; a successful challenge transitions the same op to `Reverted`; **no call-site or engine change**. Write the 4-step "v1.5 fits the seam" trace. D5 **Anti-shortcut audit:** *"keep a `HashMap<BatchId, Status>` so status lookup is O(1)"* → wrong (the §4A trap): extra replicated state + a divergence vector; the op-number already IS the status source. *"`settle() -> Result<Receipt>` synchronous"* → wrong: blocks on the stub, can't absorb a real chain's latency, forces a rewrite at v1.5; return an id immediately. D6 **Pre-mortem:** the 5-step trace where a side mapping table diverges across replicas (one replica's table missed an entry after a state-transfer) and an external client gets `Final` from one replica and `Pending` from another for the *same* id — show why deriving from `commit_num` makes this impossible. **Output**: `103_settle_design.md` with R1–R8, D1–D6, the v1.5-swap trace, the side-table divergence pre-mortem.

  #### Phase 2 — Write the failing tests (~45 min)
  - [ ] **(NEWBIE) `settle_id_is_the_op_number`** — `settle(batch)`; assert `id.op_number` equals the VSR op-number of the appended `SettleBatch` op. Exercises R1.
  - [ ] **(SENIOR) 🪤 no-side-table `finality_derived_not_stored`** — CI grep `! grep -rnE 'HashMap|BTreeMap' crates/perp-dex-core/src/settle.rs` (zero matches) + a unit test that advancing `commit_num` flips a previously-`Pending` id to `Final` *without any settle-side write*. Exercises R2/R3.
    ```rust
    let id = c.settle(batch);                 // op_number = 101, commit_num still 100
    assert_eq!(c.finality_status(id), Finality::Pending);
    c.advance_commit_to(101);                 // VSR commits; NO settle-side mutation
    assert_eq!(c.finality_status(id), Finality::Final, "finality not derived from commit point");
    ```
  - [ ] **(SENIOR) 🪤 idempotency `resubmit_same_batch_is_noop_same_id`** — submit the identical batch twice (same `checksum`); assert the same `SettlementId` both times and the venue state reflects one settlement, not two. Exercises R4.
  - [ ] **(EXPERT) 🪤 epoch-survival `settlement_id_survives_snapshot_renumber`** — get an id; force a snapshot/compaction that renumbers the op-number base (bump `snapshot_epoch`); assert `finality_status(old_id)` is still resolvable and correct. Exercises R5.
  - [ ] **(EXPERT) 🪤 seam-proof `v15_swap_fits_same_signature`** — a compile-time/behavioral fixture: a mock "optimistic-settle" `SettleBackend` plugs into the same `settle`/`finality_status` signature, produces a `Pending` that transitions to `Reverted` on a challenge, and **no call site changes**. Proves the signature is the v1.5 seam. Exercises R6/R7.
  - [ ] **(SENIOR) `finality_cross_replica_consistent`** — two replicas at the same `commit_num`; assert `finality_status(id)` matches across both. Exercises R8.
  - [ ] **(NEWBIE) negative-control `pending_actually_pending_before_commit`** — proves `finality_derived_not_stored` isn't vacuous: assert the id is genuinely `Pending` *before* commit (op_number > commit_num), else the `Final` assertion is trivially true.

  #### Phase 3 — Make the tests pass, one failure at a time (~50 min)
  - **(NEWBIE) When `settle_id_is_the_op_number` returns a fresh UUID instead of the op-number:** Symptom: the id is unrelated to the log position. Why: you minted an identity instead of *deriving* it from consensus. Fix: `settle` proposes the `SettleBatch` op and returns `SettlementId(epoch, that_op_number, checksum)`. Muscle: **identity derived from consensus, never minted.** Reapplies at: W104 settle wiring, W108 VOPR settle, W92 ledger op-id.
  - **(SENIOR) When `finality_derived_not_stored` fires on a `HashMap` in settle.rs:** Symptom: a `HashMap<BatchId, Status>` side table exists. Why: "O(1) status lookup" shortcut. Fix: delete it; `finality_status(id) = if id.op_number <= self.commit_num { Final } else { Pending }`. Cost avoided: extra replicated state + a divergence vector (the §4A lock). Muscle: **no side mapping table — derive status from the commit point.** Reapplies at: W104, W108, W119.
  - **(SENIOR) When `resubmit_same_batch_is_noop_same_id` double-settles:** Symptom: two settlements for one batch, two different ids. Why: no dedup; each `settle` minted a new op. Fix: `checksum` dedups — a resubmit with the same checksum returns the existing op's id (idempotent, free from the op-number being stable). Cost avoided: double-settlement (a funds-correctness failure). Muscle: **op-number identity gives idempotency for free; checksum is the dedup key.** Reapplies at: W104 settle, W108 replay, W92 ledger idempotency.
  - **(EXPERT) When `settlement_id_survives_snapshot_renumber` returns a wrong status after compaction:** Symptom: an outstanding id resolves incorrectly after a snapshot renumbered op-numbers. Why: the id stored an absolute op-number with no epoch; the renumber invalidated it. Fix: `snapshot_epoch` in the id; op-numbers interpreted relative to their epoch base; compaction bumps the epoch, old ids resolve against their epoch. Cost avoided: external clients/chains holding invalidated ids — a v1.5 integration breaker. Muscle: **consensus-derived external identities must be epoch-relative to survive compaction/view-change.** Reapplies at: W104 chain-facing id, W113 v1.0 ship, W119 challenge-window id.
  - **(EXPERT) When `v15_swap_fits_same_signature` forces a call-site change:** Symptom: the optimistic backend needs a different signature (sync vs async, or an extra param), so call sites change. Why: the v1.0 signature was synchronous (`-> Result<Receipt>`), can't model a long `Pending`. Fix: the async-shaped signature returns an id immediately; `finality_status` is the *only* status surface; the v1.5 backend just makes `Pending` last longer and `Reverted` reachable. Cost avoided: the engine rewrite §9 warns about ("settle-stub becomes throwaway"). Muscle: **design the v1.0 signature to be the v1.5 seam — async-shaped, status-by-derivation.** Reapplies at: W104 hybrid boundary, W119 v1.5 settle.

  #### 5-year failure mode
  Write in `notes/103_settle_followup.md`: at multi-chain settlement (v2) a single `op_number` identity can't name *which* chain finalized. **Trigger**: a second settlement chain added. **Migration**: extend the newtype to `SettlementId(snapshot_epoch, op_number, chain_id, checksum)` — the newtype boundary (R7) absorbs it without an API break; name the chain-routing seam.
- [ ] Commit + log (~10 min)

## Thursday — `perp-dex-core`: the two projection staleness contracts (control/risk vs analytics)

- [ ] [HFT] **Build (bar (c) for the read facades; bar (b) for the harness)**: `crates/perp-dex-core/src/read.rs` — two distinct read planes: **control/risk** = `read_after_commit()` served from **local applied committed state on the leader shard** (no consensus round-trip); **analytics** = `read_bounded(op_token)` = best-effort bounded-staleness (≤500 ms / ≤1000 ops, **metric not SLA**) + an op-number token gating "at least this fresh." A risk decision served from the analytics contract **must fail**. (~total 165 min across 3 phases)
  - 🔧 **Why this matters**: this is §4A acceptance #5 — the Spanner/Zanzibar insight that **one global staleness setting is wrong**; the two planes have opposite needs. A stale mark tick fed into a liquidation check = a *wrong liquidation* (no bounded staleness tolerable on the control plane); but forcing every dashboard reader through consensus doesn't scale (analytics tolerates bounded staleness with a freshness token). The 3 traps: (1) serving a risk read from the analytics (bounded-staleness) plane → a wrong liquidation off a stale tick; (2) the control read doing a consensus round-trip per read (it should read *local applied state*, no round-trip — §4A); (3) treating the analytics ≤500 ms as an *SLA* (enforcement/eviction machinery) rather than a *metric* (the lock says metric, not SLA, in v1.0). **Your first attempt will fail ≥2 of the Phase-2 tests.** Downstream: W107 turns these two facades into the live projection fan-out; W108 cluster-VOPR asserts a risk read never sees an uncommitted/stale value.

  #### Phase 1 — Problem, prediction, design (~45 min, no code yet)
  - 🎯 **Pre-prediction prompt** — seal in `crates/perp-dex-core/notes/103_read_prediction.md`: P1 where the control/risk read is served from (leader-local applied state? a consensus quorum read? a follower?) and why no round-trip; P2 predicted latency of a control read (local memory) vs an analytics read carrying an op-token; P3 predicted failure if a liquidation check reads via the analytics plane (wrong liquidation off a stale tick?); P4 what the op-number token (zookie) guarantees and what it does NOT (at-least-this-fresh, not exactly-this); P5 the Phase-2 tests you predict failing.
  - *Mental model*: two reads, two contracts. Control/risk reads the **leader shard's own applied snapshot of the VSR-applied log** — it has *already applied* every committed op up to its `applied_op`, so a read is local-memory and sees read-after-commit *without* a consensus round-trip. Analytics reads a *projection* that lags by a bounded (best-effort) amount and carries an op-number token so a reader can demand "at least as fresh as op N."
  - *Contract*:
    ```rust
    impl PerpDexCore {
        pub fn read_after_commit(&self) -> ControlSnapshot;          // leader-local applied state; risk/liquidation use this
        pub fn read_bounded(&self, min_op: Option<u64>) -> (AnalyticsView, u64);  // (view, freshness_op_token)
    }
    // ControlSnapshot: position/margin/mark as of the leader shard's applied_op — read-after-commit, no round-trip.
    // AnalyticsView: best-effort, lag <= 500ms/<=1000 ops (metric); token = the op-number the view reflects.
    ```
  - *Back-of-envelope* (bar (c)): control read = local memory access ≈ ~tens of ns, **no network**, no consensus round-trip — this is the whole point (§4A: "no consensus round-trip per read"); a quorum read would add ~1 RTT (~250 µs+) per read and not scale. Analytics read = a projection lag target ≤500 ms / ≤1000 ops, published as a Prometheus *metric* (W105), never enforced.
  - **Requirements** — write in `notes/103_read_design.md`: R1 `read_after_commit` is served from **leader-shard local applied committed state**, no consensus round-trip; it reflects every op committed up to the read point. R2 risk/liquidation/margin checks use **only** `read_after_commit`; a CI/type guard prevents a risk path from calling `read_bounded`. R3 `read_bounded` returns a best-effort view + an op-number freshness token; lag target ≤500 ms / ≤1000 ops is a **metric, not an SLA** (no enforcement/eviction machinery in v1.0). R4 the op-token gates "at least this fresh": `read_bounded(Some(N))` returns a view reflecting `op_number ≥ N` (or blocks/best-effort), never a view older than N when N is supplied. R5 **a risk decision served from the analytics contract fails** — an explicit guard (type-level or runtime assert) so a stale tick can never drive a liquidation. R6 cross-replica: `read_after_commit` on a replica at the same `applied_op` yields the same control snapshot.
  - **Design walk-through** D1 why is the control read *local* and round-trip-free? (the leader shard already applied the committed ops; reading its own applied state is read-after-commit by construction.) D2 why is analytics a *metric* not an *SLA* in v1.0? (no enforcement machinery; the lock keeps the core simple; W107 surfaces the metric.) D3 the op-token (zookie): what does "at least this fresh" buy a reader, and why is it not "exactly this version"? D4 **the guard (load-bearing):** how do you make it *impossible* (or loud) for a risk path to read the analytics plane? (distinct return types `ControlSnapshot` vs `AnalyticsView`; the liquidation/margin code only accepts `ControlSnapshot`.) D5 **Pre-mortem:** the 5-step trace where a liquidation check reads `read_bounded` (stale by 400 ms), sees a pre-recovery mark, and liquidates an account that was actually solvent. Show why the type-split (R5) makes this a *compile error*, not a runtime incident. **Output**: `103_read_design.md` with R1–R6, D1–D5, the type-split sketch, the wrong-liquidation pre-mortem.

  #### Phase 2 — Write the failing tests (~40 min)
  - [ ] **(SENIOR) `control_read_is_read_after_commit`** — commit op N; assert `read_after_commit()` reflects N immediately (no staleness window). Exercises R1.
  - [ ] **(SENIOR) 🪤 no-round-trip `control_read_does_no_consensus_roundtrip`** — instrument the consensus message bus; perform 1000 control reads; assert zero Prepare/PrepareOk messages generated by reads. Exercises R1.
    ```rust
    let before = c.bus_message_count();
    for _ in 0..1000 { let _ = c.read_after_commit(); }
    assert_eq!(c.bus_message_count(), before, "control read did a consensus round-trip — must read local applied state");
    ```
  - [ ] **(EXPERT) 🪤 wrong-plane-guard `risk_path_cannot_read_analytics`** — compile-time: the liquidation/margin API accepts only `ControlSnapshot`; passing an `AnalyticsView` fails to compile. Exercises R2/R5.
    ```rust
    // must NOT compile (documented as a trybuild compile-fail fixture):
    // let view = c.read_bounded(None).0; liquidation_engine.check(view);  // type error: expected ControlSnapshot
    ```
  - [ ] **(SENIOR) `analytics_token_gates_freshness`** — `read_bounded(Some(N))` never returns a view reflecting `op_number < N`. Exercises R4.
  - [ ] **(SENIOR) 🪤 metric-not-sla `analytics_staleness_is_metric_not_enforced`** — induce a 900 ms projection lag (> 500 ms target); assert `read_bounded` still *returns a view* (best-effort) and only the *metric* records the breach — no exception/eviction. Exercises R3.
  - [ ] **(EXPERT) `control_read_cross_replica_consistent`** — two replicas at the same `applied_op`; assert identical control snapshots. Exercises R6.
  - [ ] **(SENIOR) negative-control `roundtrip_counter_actually_counts`** — proves `control_read_does_no_consensus_roundtrip` isn't vacuous: assert a *write* (submit op) DOES increment the bus counter, so a zero on reads is meaningful.

  #### Phase 3 — Make the tests pass, one failure at a time (~45 min)
  - **(SENIOR) When `control_read_does_no_consensus_roundtrip` shows round-trips:** Symptom: each control read sends a Prepare/PrepareOk. Why: implemented as a quorum read for "safety." Fix: read the **leader shard's local applied state** — it has already applied every committed op up to `applied_op`, so a local read IS read-after-commit; no round-trip. Cost avoided: ~250 µs+/read and a non-scaling read path. Muscle: **read-after-commit is served from local applied state on the leader, not a consensus round-trip.** Reapplies at: W107 fan-out, W111 perf pass, W99 incremental-margin reads.
  - **(EXPERT) When `risk_path_cannot_read_analytics` compiles (it shouldn't):** Symptom: the liquidation check accepts an `AnalyticsView`. Why: both planes returned the same type (or `impl Trait`), erasing the contract. Fix: distinct types — `ControlSnapshot` vs `AnalyticsView`; risk/margin/liquidation signatures accept only `ControlSnapshot`. Cost avoided: a wrong liquidation off a stale tick — a funds + reputation failure (§4A acceptance #5: "a risk decision served from the analytics contract fails"). Muscle: **encode the staleness contract in the type, so a stale read into a risk path is a compile error.** Reapplies at: W107, W108, W119.
  - **(SENIOR) When `analytics_staleness_is_metric_not_enforced` throws/evicts at 900 ms:** Symptom: the analytics read errored/blocked when lag exceeded 500 ms. Why: treated the target as an SLA with enforcement. Fix: v1.0 lock — best-effort; return the view, record the breach as a metric (W105 surfaces it); no enforcement/eviction machinery. Muscle: **bounded-staleness in v1.0 is a published metric, not an enforced SLA.** Reapplies at: W105 metrics, W107 fan-out, W111 perf.
  - **(SENIOR) When `analytics_token_gates_freshness` returns a too-stale view:** Symptom: `read_bounded(Some(N))` returned `op_number < N`. Why: ignored the token. Fix: honor the op-token — return a view reflecting `≥ N` (best-effort: wait/advance, or return the freshest available with its true token and let the caller retry). Muscle: **the op-token (zookie) gates at-least-this-fresh; never silently serve older than requested.** Reapplies at: W107 consumer fan-out, W108 VOPR read assertions.

  #### 5-year failure mode
  Write in `notes/103_read_followup.md`: when analytics readers multiply (many dashboards/external metrics), best-effort projection fan-out from the leader becomes a fan-out bottleneck. **Trigger**: analytics read QPS > the leader's projection-publish budget. **Migration**: dedicated read-replicas serving the projection (consumer-group fan-out, the §4A v1.5 "richer consumer-group fan-out"); the invariant preserved is that the *control* plane stays leader-local; name the projection-replication primitive (`log-distributed` W84).
- [ ] Commit + log (~10 min)

## Friday — Reth PR maintenance + capstone-assembly DESIGN doc

- [ ] **PR maintenance**: address review comments on open Reth/alloy PRs; rebase on latest `main`; re-run `cargo test` + `cargo clippy` on touched crates. (~40 min)
  - 🎯 **Hardened acceptance**: every open PR is merged, has a fresh response to each reviewer thread, or is explicitly parked with a note in `notes/pr_tracker.md` (state + next action + date). The merged-PR count is a public track-record metric feeding the M30 bet-readiness narrative (§5).
- [ ] **Docs (bar (b))**: `crates/perp-dex-core/DESIGN.md` — the **assembly inheritance tree**: which bar-(c) crates `perp-dex-core` composes (`consensus-vsr`, `matching-engine`, `risk-engine`, `liquidation-engine`, `ledger-deterministic`, `runtime-thread-per-core`) with the exact `crate path → consumed-in-file → built-in-week`, plus the **one-log diagram** (single `VenueOp` VSR log; everything else a projection) and the **settle-seam + read-plane-split** as the two v1.0→v1.5 extension points. (~50 min)
  - 🎯 **Hardened acceptance**: DESIGN.md lists ≥6 inherited crates each with `path → consumed-in-file → week`; computes the assembly inheritance ratio (this crate is ≈*all* reuse + the op-union/settle/read facades — the highest-inheritance artifact in the curriculum, the literal proof of "no primitive twice"); and names the three §4A locks this week landed (single-log, `SettlementId`=op-number, two read contracts) with the acceptance criterion each satisfies. `cargo doc -p perp-dex-core` builds clean.
  - *Mental model*: the capstone's credibility is that it is ≈100% inherited substrate plus a thin assembly seam — DESIGN.md is where you *prove* the venue is composed, not rebuilt (README §"Inheritance Principle"; the W117 bet-readiness claim cites it).
- [ ] Commit + log (~10 min)

## Saturday — `perp-dex-core`: assembly smoke-integration + the one-log invariant in one drill

- [ ] [HFT] **Build (bar (c))**: `crates/perp-dex-core/tests/assembly_smoke.rs` — full-stack drill on the 3-node cluster: ingest a mixed `VenueOp` stream (orders + marks + a `SettleBatch`) that *triggers* at least one liquidation, settle a batch, read control + analytics planes, and assert **(a)** all 3 replicas byte-identical at every commit point, **(b)** the `SettlementId` resolves `Final` once `commit_num` passes it, **(c)** a risk read reflects the post-trade margin while the analytics read is allowed to lag, **(d)** exactly one hot-path log existed. (~total 110 min across 3 phases)
  - 🔧 **Why this matters**: composes Mon (op union) + Tue (cluster) + Wed (settle) + Thu (read planes) into the one drill W105 deploys and W108's cluster-VOPR generalizes. The trap: a smoke test that passes because it never actually exercised the composition (no liquidation triggered, no analytics lag, a single replica) — false green on the capstone's core claim. **Engineer the stream so a liquidation fires AND the analytics plane genuinely lags, so the composition is proven, not asserted.**

  #### Phase 1 — Problem, prediction, design (~25 min, no code yet)
  - 🎯 **Pre-prediction prompt** — seal in `notes/103_smoke_prediction.md`: P1 how you force a stream that triggers a liquidation (a mark tick that breaches an account's MM); P2 predicted state-hash agreement across 3 replicas (identical at every commit?); P3 predicted `finality_status` before vs after `commit_num` passes the settle op; P4 how you prove the composition actually ran (assert liquidation_count > 0 AND analytics_lag > 0, not just "no panic"); P5 the Phase-2 asserts you predict failing.
  - *Mental model*: a deterministic end-to-end assembly drill — known stream, known liquidation trigger, known settle op, known crash-free path — proving the four sub-builds compose into one replicated venue over one log.
  - *Contract*: a `#[test]` driving `VsrVenueCluster::new(3)` through ingest → settle → reads, diffing state hashes + finality + plane behavior + log count.
  - **Requirements** — write in `notes/103_smoke_design.md`: R1 the stream triggers ≥1 liquidation (composition proof, not just orders); R2 all 3 replicas byte-identical at every commit point; R3 the `SettlementId` is `Pending` before commit, `Final` after; R4 a control read reflects post-trade margin, an analytics read may lag; R5 exactly one hot-path log existed (assert log handle count == 1).
  - **Design walk-through** D1 how to assert the composition *actually ran* (assert `liquidation_count > 0` AND `analytics_lag_ms > 0`, not just recall/no-panic — the work-counter discipline). D2 **Pre-mortem:** the false-green where the stream triggered no liquidation and ran on one replica — how do you fail the test if the composition wasn't exercised? **Output**: `103_smoke_design.md`.

  #### Phase 2 — Write the failing tests (~30 min)
  - [ ] **(EXPERT, cross-primitive Mon+Tue+Wed+Thu) `assembly_drill_one_log_byte_identical`** — run the mixed stream on 3 replicas; assert byte-identical state at every commit, `SettlementId` resolves `Final` after `commit_num` passes it, control read shows post-trade margin, AND `liquidation_count >= 1` + `log_handle_count == 1`. Exercises R1–R5.
    ```rust
    let stats = cluster.run_mixed_stream(seed, /*orders*/ 5_000, /*marks triggering liq*/ true);
    assert!(stats.liquidations >= 1, "stream triggered no liquidation — composition not exercised");
    assert_eq!(stats.log_handles, 1, "more than one hot-path log — single-log lock broken");
    for cp in stats.commit_points { assert!(cp.all_replicas_hash_equal(), "replica divergence at commit {}", cp.op); }
    assert_eq!(cluster.finality_status(stats.settle_id), Finality::Final);
    ```
  - [ ] **(EXPERT, negative-control) `assembly_drill_actually_exercised_composition`** — the test-the-test: assert that if the stream triggers no liquidation (no breaching mark) OR runs single-replica, the harness FAILS its own `liquidations >= 1` / 3-replica guard. Proves the smoke test can't pass trivially. (Run as a `#[should_panic]` sub-case.)

  #### Phase 3 — Make the tests pass, one failure at a time (~25 min)
  - **(EXPERT) When `assembly_drill_one_log_byte_identical` passes but `liquidations == 0`:** Symptom: green with zero liquidations — false green. Why: the mark stream never breached any account's MM; the composition (matching → risk → liquidation) wasn't exercised. Fix: seed an account near MM and inject a mark tick that breaches it; assert the work counter. Muscle: **an integration test proves the path ran — assert the work counter, not just the outcome.** Reapplies at: W105 deploy smoke, W108 cluster-VOPR, W113 v1.0 ship gate.
  - **(EXPERT) When replicas diverge at a commit point:** Symptom: ≥1 replica's hash differs mid-stream. Why: a non-fixed apply order or a `HashMap` leak (Monday's bug surfacing under composition). Fix: re-confirm the fixed apply order + `match` dispatch + sorted state-hash concatenation; re-run the cross-replica unit test. Muscle: **composition surfaces determinism bugs the unit tests missed — the assembly drill is the catch-all.** Reapplies at: W108 cluster-VOPR, W113 ship gate.

  #### 5-year failure mode
  Write in `notes/103_smoke_followup.md`: as the venue grows, a single-process 3-node in-test cluster diverges from the real multi-host deploy (no real network faults). **Trigger**: the in-test cluster passes but a real multi-host deploy (W105) exposes a partition bug. **Migration**: promote the smoke drill into the cluster-VOPR (W108) with real fault injection (crash/restart/partition); the in-test version stays as a fast CI gate. Name the fault-injection harness reused.
- [ ] Commit + log (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
