# Week 128 — [v3] Finalist conversations / work-trials (bet-path execution)

> **Phase**: 7 (Bet Placement / Bridge)
> **Month**: M33
> **Track**: Bet-path execution — founding/core-eng finalist conversations at on-chain-derivatives venues + crypto-MM cash-seat bridge work-trials in parallel (no new crate code; the venue spine is the artifact)
> **Hours target**: 40h/wk (W118–W136 window; drops to 30h/wk at W137)
> **Mirror target**: n/a (bet-execution week — no codebase mirror). The artifacts you point finalists at: `perp-dex-core` v1.0 (3-node VSR, cluster-VOPR'd; W113) and the cross-margin + liquidation engine (`risk-engine` W99 / `liquidation-engine` W102 — the principal-defining work) and the flagship blog (W121).
> **Crate(s) touched**: n/a (maintenance only — keep `perp-dex-core` + Reth spine + Tempo option green; **no new code this week**). Any work-trial deliverable is throwaway scaffolding, NOT a workspace crate, and never the venue hot path.
> **Bar**: (n/a) — bet-execution week, no Build exercises. The bar this week is **evidentiary discipline**: every conversation produces a structured debrief + a §6-checklist scorecard the same evening.
> **Feeds into**: W129 remaining finalist conversations (carries this week's debrief deltas + recurring-objection patches); W130 bridge safety-net (crypto-MM cash seat activation if no GO-bet yet); W131 **Bet #1 decision** (GO only if §6 passes) OR engage bridge + keep outreach — the decision matrix this week builds is its primary input; W132 conditional relocation logistics (only if the §7 flip-trigger fired).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): `README.md` §6 (the seven-point ecosystem-selection checklist — the GO/NO-GO gate), §7 (shots-on-goal structure + Dubai/SG flip-trigger + "never quit a bridge before vest milestones"), §4A (the capstone v1.0 lock — what you can credibly claim a finalist team can probe), §5 M30/M36 gates, §9 risk register (the bet-specific risks finalists will surface); your own `bet_outreach/` + `interview_prep/` notes from W113–W127; the flagship blog (W121) as the artifact to point them at.
> **v3 modifications**: this week was the v2 "on-site / final interview rounds begin" (land a Tier-A HFT IC job). **v3 re-aims it from job-landing to bet-placement**: the "firms" become **on-chain-derivatives founding/core-eng venues** evaluated against the §6 seven-point checklist (a conversation that fails §6 is a *bridge*, not a *bet*), run **in parallel with crypto-MM cash-seat bridge work-trials** (the income floor, §7). The durable output is no longer an interview pass/fail — it is a **§6-scored decision matrix** feeding the W131 Bet #1 decision, plus a **flip-trigger reading** for the Dubai/SG relocation call. The matching-engine deep-dive becomes a **`perp-dex-core` / cross-margin-engine deep-dive** (the principal-defining artifact).

This is a **bet-execution week, not a build week**. The artifact each day produces is a **structured debrief + a §6 checklist scorecard**, captured the same evening while memory is fresh — debriefs are the only durable output of a finalist conversation, and they drive W129's prep deltas and W131's GO/NO-GO Bet #1 decision. Treat every conversation as a source of signal you must record against the §6 gate, not a "did they like me" event you forget. **A conversation that fails any of the seven §6 checks is a bridge job, not a bet — name that explicitly in the debrief; do not let warmth or comp launder a NO-GO into a maybe.** Day estimates below cover the debrief + §6 scoring + targeted recovery prep; the conversation itself is a full external block and is not double-counted in the 40h budget. Crypto-MM bridge work-trials run in parallel as the §7 income floor — kept alive, never the goal.

---

## Monday — Finalist conversation: venue A (founding/core-eng deep-dive: cross-margin + liquidation engine + the §6 gate)

- [ ] Pre-flight: re-read `bet_outreach/venueA_dossier.md` — the team, the people you'll talk to, their stack/architecture (CLOB-on-chain vs hybrid), their cap table, and the §6 checks you can only verify in conversation. (~30 min)
  - 🎯 **Hardened acceptance**: dossier names every person on the call + their role (founder / lead eng / investor-on-the-call), and lists the **specific §6 questions you must get answered live** — at minimum: §6.1 founding-tier allocation (what band, vesting/cliff), §6.2 tier-1 backing (which of {Paradigm, a16z crypto, Dragonfly, Polychain, 1kx, Multicoin, Jump, Wintermute} is on the cap table), §6.3 core-criticality (do I build the engine that *is* the company, or a peripheral integration), §6.6 remote-native (Vietnam-remote works, no colo requirement). Each question is tied to something you can only learn from W123–W124 recon or their public engineering writing, not generic "what's the culture."
  - *Mental model*: a finalist conversation is **bidirectional and gated** — they evaluate you, and you evaluate them against §6. The §6 questions are the cheapest, highest-signal way to both (a) show you did the homework 90% of candidates skip and (b) collect the GO/NO-GO data the W131 decision needs. The dossier converts pre-call adrenaline into a checklist so prep is a recall, not a scramble. A vague "great team!" with no §6 cell filled is a wasted finalist slot.
  - Output: `bet_outreach/venueA_dossier.md` updated with the people list + the §6 question set (≥1 per check you can verify live); `git log -1 --oneline` shows the pre-flight commit.
- [ ] Same-evening debrief: write `bet_debriefs/venueA_finalist.md` while memory is fresh. (~50 min)
  - 🎯 **Hardened acceptance**: debrief covers all four segments of a founding-eng finalist conversation — (1) **technical deep-dive** (the `perp-dex-core` / cross-margin + liquidation engine probe), (2) **architecture/design** (how you'd build *their* venue's hard part), (3) **founder/team fit + mutual vision**, (4) **the §6 gate** — with, per segment: the exact prompt/discussion, what you said, where you stumbled, and one concrete prep delta for W129. The technical-deep-dive section records which §4A design decisions you defended well vs. got pushed on: the **VSR-log-as-single-order/event-log** lock (no second hot-path log), the **const-30%-netting-benefit haircut** (why deterministic, not rolling correlation — VOPR replay safety), the **fee-accrued insurance fund + waterfall + tripwire-never-hit** invariant, the **incremental-margin hot path at 100k accounts / ~1M orders/s** (why full recompute blows the budget), the **`SettlementId(snapshot_epoch, op_number, checksum)`** = VSR op-number with free idempotency, and the **two projection staleness contracts** (control/risk = read-after-commit from local applied state on the leader shard; analytics = bounded-staleness + op-number token).
  - *Mental model*: the deep-dive into `perp-dex-core` + the cross-margin engine is where 127 weeks pay off or don't — founding-eng interviewers probe for whether you actually understand *why* the v1.0 core is deterministic (so cluster-VOPR can prove linearizability + replica convergence + insurance-fund-tripwire-never-hit), or just recite the words. The convergence cell (row 9, `risk-engine`) is the principal-defining artifact precisely because per-mark-tick portfolio recompute across all accounts is `jeff-dean`-hard ∧ `hft-review`-hard ∧ domain-hard simultaneously — expect them to push hardest there. Recording where you got pushed turns "I think it went okay" into a precise tightening list before venue B/C.
  - Output: `bet_debriefs/venueA_finalist.md` exists with 4 segment sections + a `## §6 scorecard` (all seven checks scored GO/NO-GO/UNKNOWN with the live evidence) + a `## Prep deltas for W129` list of ≥2 concrete items; `git log -1 --oneline` shows the Monday debrief commit.

## Tuesday — Recover + patch from venue A deltas + crypto-MM bridge work-trial (parallel income floor)

- [ ] Triage Monday's prep deltas: pick the 1–2 highest-leverage items and do a focused 60-min repair (re-derive the §4A answer you fumbled, re-draw the architecture you got pushed on). (~75 min)
  - 🎯 **Hardened acceptance**: each chosen delta from `venueA_finalist.md` is closed with a written corrected answer or re-drawn diagram in `bet_prep/patches.md`, dated and labeled with its source venue — not "reviewed" but a concrete artifact you could hand to the next finalist team. If the fumble was the cross-margin haircut justification or the VSR-single-log lock (the two most-probed §4A points), the patch must be crisp enough to deliver in 90 seconds from memory.
  - *Mental model*: recovery days are where the bet cycle compounds — the same probes recur across venues (especially the cross-margin + liquidation deep-dive and "why VSR not Raft / why not a second Kafka log on the hot path"), so a fumble at venue A patched Tuesday becomes a strength at venue B Wednesday. Skipping the patch wastes the most expensive signal you'll get all week. This is the same "any gap is a divergence vector — fix the root, don't memorize one answer" discipline you applied to replicated state machines, now applied to your own pitch.
  - Output: `bet_prep/patches.md` has ≥1 dated, sourced patch entry closing a Monday delta; `git log -1 --oneline` shows the Tuesday patch commit.
- [ ] **Crypto-MM bridge work-trial — block 1** (the §7 income floor, run in parallel): execute the take-home / paired latency task the crypto-MM desk assigned, anchored on Half-2 latency craft (matching, tail-latency, mechanical sympathy). (~150 min)
  - 🎯 **Hardened acceptance**: the work-trial deliverable is a **throwaway scaffold in a scratch dir (NOT a workspace crate)** that demonstrates the latency-craft the desk asked for (e.g. a tick-to-trade micro-bench with coordinated-omission-correct percentiles via `latency-lab` conventions, or a lock-free hot-path the desk specified). It points back at your real artifacts (`marketdata-kernelbypass` W90, `matching-engine` v1.0, `latency-lab`) as proof of depth without re-litigating them. Logged in `bet_outreach/bridge_cryptomm.md` with the task, time spent, and what it demonstrates.
  - *Mental model*: the crypto-MM cash seat is the **primary bridge / income floor** (§2, §7), underwritten by Half-2's latency craft — it funds runway *between* bets and is the reason `latency-lab` was pulled to M6. Running it in parallel with the bet finalist conversations is the shots-on-goal structure: Bridge₀ secures the floor so a NO-GO on every bet this cycle is *not* ruin. **Rule from §7: never quit a bridge before the live bet's vest milestones** — keep this warm even if venue A felt strong Monday.
  - Output: `bet_outreach/bridge_cryptomm.md` updated with the work-trial block-1 log + the scratch-dir path; `git log -1 --oneline` shows the Tuesday bridge commit.

## Wednesday — Finalist conversation: venue B (founding/core-eng deep-dive + §6 gate)

- [ ] Pre-flight: re-read `bet_outreach/venueB_dossier.md` + skim `bet_prep/patches.md` so Monday's fumbles don't repeat. (~30 min)
  - 🎯 **Hardened acceptance**: venue B dossier meets the same bar as venue A's (people list + §6 question set covering all seven checks, with §6.4 multi-billion-FDV ceiling and §6.5 token reality / pre-TGE entry / sane vesting explicitly added to the live-question set), AND you have noted which Tuesday patches apply to venue B's likely probe set (their architecture may stress the settle interface or the projection-staleness contracts harder than venue A).
  - *Mental model*: venue B should be measurably sharper than venue A because you arrive with a debrief + a patch list + a filled §6 scorecard template — the cycle is a learning loop, and the pre-flight is where last conversation's lesson gets loaded before it can repeat. §6.4/§6.5 (FDV ceiling + token reality) are the checks most candidates skip and most likely to silently fail a bet: a tier-1-backed team with a predatory unlock schedule dumping on early contributors is a NO-GO no matter how good the engineering is.
  - Output: `bet_outreach/venueB_dossier.md` updated to the same bar; `git log -1 --oneline` shows the pre-flight commit.
- [ ] Same-evening debrief: write `bet_debriefs/venueB_finalist.md`. (~50 min)
  - 🎯 **Hardened acceptance**: same 4-segment structure + `## §6 scorecard` as venue A, PLUS a `## vs venue A` line comparing on team/vision, technical depth of *their* hard problem, §6-pass strength (which has more GO cells), comp/allocation-tier signal, and remote-from-Vietnam friendliness — the first head-to-head input to the W131 decision matrix. The technical section again records which §4A points (VSR single-log, 30% haircut determinism, insurance-fund waterfall/tripwire, incremental-margin-at-100k, `SettlementId` idempotency, two staleness contracts) you defended vs. got pushed on.
  - *Mental model*: comparison data is only capturable while both conversations are fresh; by W131 the venues blur. Logging the head-to-head + the §6 GO-cell count now is what makes the Bet #1 decision evidence-based rather than recency-biased. Remember the Airwallex anti-pattern from §6.1: late-stage tiny employee equity is never a bet — if venue B turns out to be late-stage with founding-tier-in-name-only allocation, score §6.1 NO-GO regardless of how exciting the product is.
  - Output: `bet_debriefs/venueB_finalist.md` exists with 4 segments + `## §6 scorecard` + `## Prep deltas for W129` + a `## vs venue A` line; `git log -1 --oneline` shows the Wednesday debrief commit.

## Thursday — Recover + patch from venue B deltas + crypto-MM bridge work-trial block 2

- [ ] Triage Wednesday's deltas and repair the top 1–2 before venue C. (~75 min)
  - 🎯 **Hardened acceptance**: each chosen venue-B delta is closed in `bet_prep/patches.md` with a dated, sourced corrected answer; if a delta recurs across venue A AND venue B, tag it `[recurring]` — recurring fumbles are the highest-priority fix before venue C. (Likely recurring probes: "why is the v1.0 cross-margin haircut a deterministic constant instead of rolling empirical correlation?" → because empirical correlation injects non-deterministic floating state the VSR cluster must replicate bit-identically, and correlation breaks toward 1 in a crash so under-crediting is the safe default; the review trigger to swap it is wired to the v1.5/RWA milestone. And: "why isn't there a second hot-path log for read scaling?" → the order/event log IS the VSR log; Kafka-style fan-out reads a *projection* off the hot path via `log-distributed`, never an independent write target — a second hot-path log breaks linearizability and "no primitive twice.")
  - *Mental model*: a fumble that appears at two independent venues is a real gap in your model or pitch, not bad luck — tagging recurrence forces a root-cause fix (you can't crisply justify a §4A design tradeoff) rather than memorizing one answer. This is exactly the "5-year failure mode" discipline from the build weeks, applied to your own narrative: name the one shift that would force a redesign of your *story*, and patch it.
  - Output: `bet_prep/patches.md` has the Thursday patch entries with any `[recurring]` tags; `git log -1 --oneline` shows the Thursday patch commit.
- [ ] **Crypto-MM bridge work-trial — block 2** (income floor continued): finish + submit the desk's task; log the outcome/feedback. (~120 min)
  - 🎯 **Hardened acceptance**: the work-trial is submitted; `bet_outreach/bridge_cryptomm.md` records the desk's response (or expected timeline), and a one-line read on whether the bridge is **converging to an offer** (the §7 floor) — this is the safety-net input to W130. The deliverable stays a throwaway scratch artifact; nothing leaked into the workspace crates or the venue hot path.
  - *Mental model*: the bridge advancing in parallel is what lets W131 be a *clean* Bet #1 decision — if the floor is secured, you can hold out for a §6-passing bet rather than settling for a NO-GO venue out of runway anxiety. The bridge is the floor, not the goal (§2): a strong bridge *increases* your bet selectivity. Keep it warm through any vest milestone reasoning (§7).
  - Output: `bet_outreach/bridge_cryptomm.md` updated with the block-2 submission + outcome read; `git log -1 --oneline` shows the Thursday bridge commit.

## Friday — Finalist conversation: venue C (founding/core-eng deep-dive + §6 gate)

- [ ] Pre-flight: re-read `bet_outreach/venueC_dossier.md` + the `[recurring]`-tagged patches. (~30 min)
  - 🎯 **Hardened acceptance**: venue C dossier meets the people-list + seven-check §6-question bar; every `[recurring]` patch from the week is loaded and you can state the corrected answer (cross-margin-haircut determinism, VSR-single-log, insurance-fund waterfall/tripwire) from memory in under 90 seconds each.
  - *Mental model*: venue C is your sharpest shot of the week — you arrive with two debriefs, two filled §6 scorecards, and a patched recurring-fumble list, so this conversation should be your tightest. The pre-flight ensures hard-won lessons from A and B actually transfer. By now your `perp-dex-core` deep-dive should be reflexive: the v1.0 lock (3-node VSR, multi-instrument netting with 30% haircut, partial-liq + fee-accrued fund + waterfall, `SettlementId`=op-number, two staleness contracts, RWA-aware seams) is a *narrative*, not a recall exercise.
  - Output: `bet_outreach/venueC_dossier.md` updated to the same bar; `git log -1 --oneline` shows the pre-flight commit.
- [ ] Same-evening debrief: write `bet_debriefs/venueC_finalist.md`. (~50 min)
  - 🎯 **Hardened acceptance**: same 4-segment structure + `## §6 scorecard` + comparison lines `## vs venue A` AND `## vs venue B`, so the three-way ranking inputs are now complete. Plus a `## flip-trigger reading` note: did any §7 signal appear (≥N warm conversations clustering in one hub — Dubai crypto-native/Binance-dense vs Singapore institutional/Asia — OR a presence-contingent offer premium)? Record it for the W132 relocation call; until the trigger fires, Vietnam-remote remains the default and no move/lease is committed.
  - *Mental model*: with three debriefs done, you hold the full evidence base for the Bet #1 decision — the Friday debrief closes the data-collection phase of the cycle. The flip-trigger reading is the cheap-prep-not-pre-commit discipline from §7: you're sensing whether the geography decision (decided *at Bet #1 time*, hub follows the bet) is being forced, not pre-deciding it.
  - Output: `bet_debriefs/venueC_finalist.md` exists with 4 segments + `## §6 scorecard` + `## vs venue A` + `## vs venue B` + `## flip-trigger reading`; `git log -1 --oneline` shows the Friday debrief commit.

## Saturday — Recover + flush: synthesize the Bet #1 decision matrix (§6-gated)

- [ ] Build the **Bet #1 decision matrix** from the three debriefs: venues × the seven §6 checks + {founder/team + vision, technical depth of their hard problem, comp/allocation tier, remote-from-Vietnam friendliness, cycle timing}. (~60 min)
  - 🎯 **Hardened acceptance**: `bet_debriefs/decision_matrix.md` scores all 3 venues across the **seven §6 checks** (founding-tier allocation, tier-1 backing, core-criticality, multi-billion-FDV ceiling, token reality/pre-TGE/sane-vesting, remote-native, cycle timing) **plus** the soft dimensions, with a one-line evidence citation per cell pulled from the debriefs (no unsourced gut scores). The matrix **applies the §6 gate explicitly**: any venue failing *any one* of the seven checks is marked **NO-GO (= bridge, not a bet)** — a venue is GO only if all seven pass. The matrix ends with: (a) the current GO/NO-GO verdict per venue, (b) the current-leader pick *among GO venues only*, and (c) the single piece of missing §6 information that would change the verdict (the thing to probe in W129's remaining conversations or a follow-up with the leader).
  - *Mental model*: by now you have §6-scored data from 3 finalist conversations — synthesizing it into a gated matrix now (not at W131 when the decision is due) means the Bet #1 call is anchored on fresh evidence and the §6 checklist, not warmth or recency. The "missing info" line tells you exactly what to chase in W129–W130. **This is the deliverable the whole week exists to produce**: the W131 decision is "place Bet #1 (GO only if §6 passes) OR engage the bridge + keep outreach" — and this matrix is its primary input. Crucially, the §6 gate is what prevents the most expensive mistake in the whole plan: laundering an exciting-but-NO-GO venue (Airwallex anti-pattern: late-stage tiny equity; or predatory unlocks; or "you'd be a peripheral integration, not the engine") into a bet. If no venue is GO, that is a *valid and important* result — it means W130's bridge activation + continued outreach, not a forced bet.
  - Output: `bet_debriefs/decision_matrix.md` exists with a 3-venue × (7 §6 checks + soft dims) scored table, per-cell evidence citations, a per-venue GO/NO-GO verdict applying the §6 gate, a current-leader pick among GO venues, and a missing-§6-info line; `git log -1 --oneline` shows the Saturday synthesis commit.

---

## [v3.1 BFT apex — Track B] W128: integrate `BftReplica` behind `ConsensusBackbone` (the stub becomes real)

> Track B continues (W127 wired the O5 surface end-to-end). This week closes the **build half** of the apex: `BftReplica` is registered as a real `ConsensusBackbone` impl behind the same adapter the W103 stub-swap test used — so the W103 `stub_backbone_drives_engine` test now has a *real BFT sibling*, `bft_backbone_drives_engine`. Bar (c). After this, the apex is **fully built**; W129–W134 prove it under Byzantine faults, W135–W142 swap it into `perp-dex-core`.

- [ ] **[Tue +75min · Thu +75min]** [Venue · BFT] **Build (bar c)**: `crates/consensus-vsr-backbone/src/bft_adapter.rs` — the thin adapter that exposes `BftReplica` as `dyn ConsensusBackbone` (the same adapter crate the VSR impl uses; `perp-dex-core` depends on this crate's trait, never on `consensus-vsr` internals — W103/acceptance #9). Prove the four-type-param swap is real: `assert_engine_is_backbone_generic(BftReplica::new(4))` compiles and runs, exactly as `VsrReplica::new(3)` did. (~total 150 min)
  - 🔧 **Why this matters**: this is the payoff of the entire `ConsensusBackbone` design (W91) — the BFT impl, built from four monomorphized seams (W119–W125), satisfies the **frozen** outer trait with the same adapter shape as VSR. W103's `stub_backbone_drives_engine` proved the *type* swap was possible against an unimplemented stub; W128 proves it against the *real* BFT impl. The trap: the adapter leaking a BFT-specific method (a `view()` or `qc()` getter) onto the `ConsensusBackbone` surface to "make integration easier" → re-couples the engine to BFT, breaking acceptance #9 and the symmetry with VSR. The adapter must expose ONLY the trait methods; everything BFT-specific stays inside `BftReplica`.
  - *Contract*: `pub struct BftBackboneAdapter<SM>(BftReplica<SM>); impl<SM> ConsensusBackbone for BftBackboneAdapter<SM> { type Op = SM::Op; type Commit<'a> = ...; fn propose(&mut self, op) -> Result<ProposalHandle, NotLeader> {..} fn drain_committed(..){..} fn drain_abandoned(..){..} fn commit_point(&self)->OpNumber{..} fn applied_point(&self)->OpNumber{..} fn role(&self)->Role{..} }` — identical surface to `VsrBackboneAdapter`; NO extra method.
  - **Tests:**
    - `bft_backbone_drives_engine` — the W103 `assert_engine_is_backbone_generic::<BftBackboneAdapter<_>>` compiles AND runs (4-node BFT cluster drives `PerpDexCore::new_on(bft_backbone)`); the sibling of W103's `stub_backbone_drives_engine`, now real. Acceptance #9 holds for the real apex impl.
    - `bft_adapter_exposes_only_trait_methods` — the adapter's public API == the `ConsensusBackbone` trait surface exactly; a `grep`/API check finds no `view`/`qc`/`high_qc`/`tc` getter leaked. (Re-asserts acceptance #9 / O3 at the adapter boundary.)
    - `engine_diff_empty_vsr_vs_bft` — the `perp-dex-core` engine code (matching/risk/liquidation/ledger/apply/settle) is byte-identical whether bound to `VsrBackboneAdapter` or `BftBackboneAdapter` — only the constructor's type param differs. **This is the apex's core claim: VSR→BFT is a backend swap, not an engine rewrite.**
    - `bft_settlement_id_via_commit_point` — `SettlementId`/`finality_status` resolve from `BftReplica`'s `commit_point()` identically to VSR (W104) — the settle boundary is consensus-impl-agnostic (the dense `OpNumber` space, O4, is the same under both).
  - **Make-pass card (EXPERT · the apex claim):** `engine_diff_empty_vsr_vs_bft` shows a non-empty diff → symptom: integrating BFT required editing `perp-dex-core/src/apply.rs` or `settle.rs`; why: the adapter leaked a BFT concept the engine reached for, OR the engine had a latent `consensus_vsr::` reference (W103 acceptance #9 not fully held); fix: route everything through the trait; the adapter is the ONLY BFT-aware code; cost avoided: the apex becoming a rewrite (the exact failure the W91 seam + W118 design existed to prevent — a non-empty diff here means 30 weeks of seam discipline leaked); muscle: the consensus backend is swappable IFF the engine names only the trait — proven by an empty diff, not asserted. Reapplies: W135 the live perp-dex-core swap, W143 acceptance.
  - **Output**: `bft_adapter.rs`; `bft_backbone_drives_engine` green (the real-impl sibling of the W103 stub-swap); `engine_diff_empty_vsr_vs_bft` green (**the apex is a backend swap**); the BFT apex is now FULLY BUILT (gate + 4 seams + commit rule + liveness + integration). Remaining: prove it under Byzantine faults (W129) and swap it live into `perp-dex-core` (W135).
- [ ] **[Sat +10min]** [Venue · BFT] Milestone note → `notes/128_bft_built.md`: the BFT apex is built and satisfies `ConsensusBackbone` behind the W103 adapter with an empty engine-diff vs VSR. **Next W129–W134: Byzantine cluster-VOPR (the 8-scenario set from W118 Friday).** (~10 min)
- [ ] **[Sat +10min]** Commit + log (~10 min)

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md. Track B: **BFT apex fully built** — `BftReplica` satisfies `ConsensusBackbone` with an empty engine-diff vs VSR (the W103 stub-swap is now real). Next: Byzantine cluster-VOPR (W129–W134).
