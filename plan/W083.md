# Week 83 — Builder API impl + [Tempo] `tempo-payment-lane` scaffold + [HFT] `ledger-deterministic` v0.5 (the settlement SM on the VSR log)

> **Phase**: 5
> **Month**: M21
> **Track**: Half-2 venue (primary, the settlement spine) + Half-1 Reth-spine + Tempo (RWA-collateral option)
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: Flashbots builder API (header/block submission flow, `eth_sendBlockV*`) + TigerBeetle "Tiger Style" deterministic state-machine recovery (`ledger-deterministic` v0.5 — *the settlement SM that runs on the SAME apply discipline as W74's `consensus-vsr` NormalOperation*) + Aeron Archive recording layer (`messaging-aeron::archive`, mirrors real-logic/aeron `ArchiveRecorder`/`RecordingDescriptor` — the **off-hot-path** persistent journal, NOT a second log) + Glommio/Seastar allocation-free reactor (`runtime-thread-per-core::Reactor::poll`) + Tempo `payment-lane` (`tempo-payment-lane` scaffold).
> **Crate(s) touched**: `consensus-engine` (builder API, Reth spine); `ledger-deterministic` v0.5 [SHIP + TAG] — deterministic settlement SM (**runs on `consensus-vsr` NormalOperation from W74 — same `apply(op_num, op)` discipline, no second log**); `messaging-aeron` v0.7 (Archive recording + `RecordingDescriptor` port — off-hot-path mirror); `runtime-thread-per-core` v1.0 (zero-alloc audit begins on `Reactor::poll`); `tempo-payment-lane` [NEW scaffold]
> **Bar**: **(c)** for `ledger-deterministic`, `messaging-aeron`, `runtime-thread-per-core` (HFT critical path + Layer-1 substrate — VOPR + static-mem + zero-alloc); **(b)** for `consensus-engine` builder API and `tempo-payment-lane` (Reth/Tempo track). Bar-(c) Builds carry perf failure cards with Cost-avoided ns/cycle/alloc numbers; the builder-API and Tempo items carry correctness trap tests only. Each Build labels its bar inline.
> **Feeds into**: W84 full-stack integration (matching + ledger + messaging) consumes the Archive (`archive.replay`) + the v0.5 ledger SM + `log-distributed` v0.5's first **projection of the VSR log** (the off-path Kafka-fidelity fan-out); W85 marketdata-kernelbypass + `runtime-thread-per-core` **v1.0 SHIP** consume the zero-alloc reactor; W88 `ledger-deterministic` **v0.7 VOPR** drives `state_hash` + recovery 10M×/scenario, integrated with `consensus-vsr` VOPR; W90 `risk-engine` v0.5 reads the venue's committed settlement state; W91 `tempo-payment-lane` v0.1.0 + `consensus-engine` v1.0 consume the lane scaffold; W92 `ledger-deterministic` **v1.0 SHIP** (static-mem + io_uring + **VSR replication**) reapplies this week's determinism/crc/recovery lessons; W103 `perp-dex-core` capstone runs THIS ledger SM as the settlement journal on the 3-node VSR cluster (`SettlementId(snapshot_epoch, op_number, checksum)`).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): Flashbots `mev-boost` builder API (relay/builder spec, `eth_sendBlockV*`); Aeron Archive wiki (https://github.com/real-logic/aeron/wiki/Aeron-Archive) + `aeron-archive-codecs.xml`; TigerBeetle "Tiger Style" (allocation-free, deterministic recovery) + `src/vsr/replica.zig` apply discipline; Glommio reactor (typed-task storage); Tempo `tempoxyz/tempo` payment-lane source (Saturday diff only); README.md §3 rows 5/6, §4 reference map ("one log on the hot path"), §4A v1.0 lock (settlement identity, deterministic core).
> **v3 modifications**: (1) **Frontmatter re-narrated around the single-hot-path-log lock.** `ledger-deterministic` v0.5 is now explicitly *the settlement SM that runs on `consensus-vsr` NormalOperation* (W74) — the order/event log, the settlement journal, and the replicated SM are the **same** VSR log (TigerBeetle pattern; README.md §3 row 5 / §4 reference map). The Archive (`messaging-aeron`) and the (future) Kafka leg are **off-hot-path mirrors / journals**, never a second log. (2) **Downstream "Feeds into" re-aimed to the v3 sequence**: W92 ledger v1.0 = static-mem + io_uring + **VSR replication**; W103 `perp-dex-core` capstone runs this exact ledger SM; the settlement identity is the VSR op-number (`SettlementId`). (3) v2 ADD retained: aeron Archive recording (Tue) + descriptor port (Sat) + runtime v1.0 zero-alloc audit on `Reactor::poll()` (Wed). Day-load rebalanced so no day exceeds ~6 h.

**The v3 lock this week serves (read before Monday):** `ledger-deterministic` is **not** a standalone database — it is the venue's **settlement journal**, and per README.md §4A the settlement journal, the order/event log, and the replicated state machine are **one** VSR-replicated log (the W74 `consensus-vsr` NormalOperation hot path). Everything you build into the ledger SM this week — `apply(op_num, op)` contiguity, canonical `state_hash`, snapshot/restore, byte-identical recovery — is the *exact apply discipline* W74 locked for the matching engine. The ledger plugs into `NormalReplica<SM>` (W74 R13: SM-agnostic) without a protocol change. The Aeron Archive (Tue) is the **off-hot-path** persistent journal that lets late subscribers and forensic replay read a *projection* of that committed log — it is NEVER a second write target on the hot path. Get this separation right and W92 (ledger v1.0 + VSR replication) and W103 (capstone, `SettlementId` = VSR op-number) slot in without a rewrite.

---

## Monday — Header submissions (Reth spine builder API)

- [ ] Browse `reth`/`mev-boost` builder-API surface; map the header/block submission split into `notes/83_builder_api_map.md`. (~30 min)
  - 🎯 **Hardened acceptance**: the note names the two halves (`eth_sendBlockV*` header vs full block) and one line each on what the relay verifies in each, AND marks which concept feeds Mon (header) vs Tue (block) vs Wed (client). *Mental model*: read mev-boost as a source of named relay-side checks (the trust model), not cover-to-cover.
- [ ] **Build (bar (b))**: `crates/consensus-engine/src/builder/submit_header.rs` — implement the builder→relay header
  submission flow (mirrors Flashbots `eth_sendBlockV*` header half). (~total 130 min across 3 phases) — correctness-focused.
  - 🔧 **Why this matters**: mirrors the Flashbots builder API's header-submission step — the bid the relay sees
    before the full block. This is the Half-1 Reth-spine work (on-chain settlement-side fluency for the hybrid CLOB,
    §4A). Two traps a naive impl hits: (1) accepting a header whose `parent_hash` doesn't chain to the relay's
    current head (stale/forked bid silently wins the auction); (2) trusting a self-reported `value` without
    re-deriving it from the block's coinbase delta (a lying builder steals the slot). Downstream: W84 full-stack
    integration submits real blocks through this path; W91 `consensus-engine` v1.0 wires it to the payload builder.
    First-attempt impls fail ≥1 Phase-2 test — the debug session is the lesson.

  #### Phase 1 — Problem, prediction, design (~30 min, no code yet)
  - 🎯 **Pre-prediction prompt** — write sealed answers in `notes/83_submit_header_prediction.md`:
    - P1: the type you'd reach for to key submissions (exact import line, e.g. `use alloy_primitives::B256;`).
    - P2: predicted size of `SubmissionHeader` in bytes (`std::mem::size_of`), and whether it fits a cache line.
    - P3: what your first `validate()` does when handed a header whose `parent_hash` ≠ relay head — accept or reject?
    - P4: prediction — does your `value` field get re-derived or trusted as-submitted?
    - P5: list every Phase-2 test you predict your first attempt will FAIL. Compare to reality after tests run.
  - *Mental model*: a header submission is a *bid* — a claim "I can produce a block worth V on top of head H." The
    relay must verify the claim before trusting it; an unverified bid is an attack surface.
  - *Contract*: `struct SubmissionHeader { slot: u64, parent_hash: B256, block_hash: B256, value: U256, builder_pubkey: BlsPubkey, ... }`; `fn validate(&self, head: B256, slot: u64) -> Result<(), SubmitError>`. NEVER write the validation body here.
  - **Requirements** R1..R5 — write in `notes/83_submit_header_design.md`:
    - R1. `validate` rejects when `parent_hash != head` (`SubmitError::WrongParent`).
    - R2. `validate` rejects when `self.slot != current_slot` (`SubmitError::WrongSlot`).
    - R3. `value` is re-derived from coinbase balance delta, never trusted as-submitted.
    - R4. `validate` is total — no panics on malformed input (bad pubkey length, zero hashes).
    - R5. duplicate `(slot, builder_pubkey)` submissions are idempotent (higher `value` wins).
  - **Design walk-through** D1..D3 — answer in the design doc (no peeking at mev-boost source until Phase 3):
    - D1. Enumerate the validation order (cheap checks first: slot/parent before BLS signature verify, which is ~1 ms).
    - D2. **Pre-mortem (load-bearing):** write the 4-line bad sequence where a builder submits `value = U256::MAX`
      with a real-looking header and wins the auction without producing the value. Show where R3 cuts it off.
    - D3. **Anti-shortcut audit:** the "obvious" reach is `#[derive(PartialEq)]` + a `HashMap<u64, SubmissionHeader>`
      keyed by slot only — one line on why that drops the builder identity and lets one builder overwrite another.
  - **Output**: `notes/83_submit_header_design.md` with R1–R5, D1–D3, the lying-bid sketch.

  #### Phase 2 — Write the failing tests (~30 min)
  Each test is engineered so a naive `validate` fails it. File: `crates/consensus-engine/src/builder/submit_header.rs` `#[cfg(test)]`.
  - `header_rejects_wrong_parent` — **(NEWBIE)** submit header with `parent_hash = B256::ZERO` against `head = known`; assert `Err(WrongParent)`. Naive "accept anything well-formed" fails.
  - `header_value_re_derived_not_trusted` — **(SENIOR, anti-shortcut)** submit header claiming `value = U256::MAX` over a block whose coinbase delta is `1 gwei`; assert the stored/accepted value is `1 gwei`, not `MAX`. Naive trust-the-field fails.
  - `header_duplicate_builder_idempotent_higher_wins` — **(SENIOR)** submit twice for same `(slot, pubkey)` with `value` 5 then 3; assert stored value stays 5. Naive last-write-wins-unconditional fails.
  - `header_validate_total_on_garbage` — **(NEWBIE, negative-control on robustness)** feed zero hashes / wrong-length pubkey bytes; assert `Err`, never panic. Proves `validate` is total, not the test passing by luck.

  #### Phase 3 — Make the tests pass, one failure at a time (~30 min)
  - **(NEWBIE) When `header_rejects_wrong_parent` reports `Ok(())`:**
    - Symptom: stale-parent header accepted.
    - Why: `validate` skipped the `parent_hash == head` check (only checked well-formedness).
    - Fix: first line of `validate` — `if self.parent_hash != head { return Err(WrongParent); }`.
    - Cost avoided: a forked/stale bid wins the auction — protocol-level correctness, not perf.
    - Muscle: a bid is a claim about a *specific* head; bind it to that head or a fork wins the auction. Reapplies at: W84 block submission, W91 payload validation.
  - **(SENIOR) When `header_value_re_derived_not_trusted` reports stored value `U256::MAX`:**
    - Symptom: a lying builder's self-reported value is accepted verbatim and wins the slot.
    - Why: `value` was copied from the submission instead of computed from `post_coinbase_balance - pre_coinbase_balance`.
    - Fix: ignore the submitted `value`; compute it from the executed block's coinbase delta and use the computed value for ranking.
    - Cost avoided: a dishonest builder steals slots for free — the same "never rank on attacker self-reports" muscle that protects the venue's `MarkTickObserved` input (W74) and the W101 insurance-fund accrual.
    - Muscle: never rank on attacker-controlled self-reports — re-derive the metric from authenticated state. Reapplies at: W84 builder block ranking, W91 MEV-bid acceptance.
  - **(SENIOR) When `header_duplicate_builder_idempotent_higher_wins` reports stored value 3:**
    - Symptom: a builder lowers its own bid by resubmitting; the lower bid replaces the higher.
    - Why: unconditional last-write-wins on `(slot, pubkey)` insert.
    - Fix: `if new.value > existing.value { replace }` — keep the highest bid per builder.
    - Muscle: dedup keys need a tiebreak rule that matches the auction's intent. Reapplies at: W84 multi-builder auction, W91 lane bid ordering.

  #### 5-year failure mode
  Write in `notes/83_submit_header_followup.md`: when the relay moves to multi-slot / based-preconfirmation flow,
  single-slot `validate` no longer holds. Trigger: spec adds `target_slot != current_slot` semantics. Migration:
  generalize `validate(head, slot)` to a `SlotWindow` accept range.
- [ ] Commit + log (~10 min)

> **Day-load note**: builder-API map ~30 + header Build ~130 + commit ~10 ≈ 2.8 h. Light day by design — block
> submission (Tue) + ledger v0.5 (Tue) + aeron Archive (Tue) are the heavy lifts, so Monday stays focused.

## Tuesday — Block submissions + [HFT] `ledger-deterministic` v0.5 (settlement SM on the VSR apply path) + [NEW v2] aeron Archive recording

- [ ] **Build (bar (b))**: `crates/consensus-engine/src/builder/submit_block.rs` — implement the full block submission
  flow (mirrors Flashbots `eth_sendBlockV*` block half: re-execute, verify the header's claimed value). (~total 90 min
  across 3 phases) — correctness-focused.
  - 🔧 **Why this matters**: the block half closes the loop opened by Monday's header — the relay now re-executes
    the submitted block and must (1) confirm the resulting state root matches the header's claim and (2) confirm
    the realized coinbase value ≥ the bid. Two traps: a naive impl trusts the submitted `state_root` instead of
    recomputing it, and a naive impl compares realized value to *zero* instead of to the *bid*. Downstream: W84
    full-stack integration; W91 `consensus-engine` v1.0.
  - #### Phase 1 — Problem, prediction, design (~20 min, no code yet)
    - 🎯 **Pre-prediction prompt** — `notes/83_submit_block_prediction.md`: P1 the re-exec entry point you'd call
      (exact path, e.g. `exec_vm::execute_block`); P2 predicted re-exec latency for a 30M-gas block (Jeff-Dean: a
      block is ~300 txs × ~100 µs = ~30 ms); P3 do you recompute `state_root` or trust the submission?; P4 what do
      you compare realized value against — 0 or the bid?; P5 the Phase-2 tests you predict you'll fail.
    - *Mental model*: re-execution is the relay's trust anchor — the only way to know the bid is real is to run it.
    - *Contract*: `fn submit_block(&self, block: SubmissionBlock, bid: &SubmissionHeader) -> Result<Accepted, SubmitError>`.
    - *Back-of-envelope*: 30M-gas block re-exec ≈ 30 ms; at 12 s slots that's fine, but doing it on the hot auction
      path serializes bids — note the implication (re-exec off the auction-ranking lock).
    - **Requirements** R1..R4 (`notes/83_submit_block_design.md`): R1 recompute `state_root`, reject on mismatch;
      R2 realized coinbase value ≥ `bid.value` or reject (`ValueShortfall`); R3 `parent_hash`/`block_hash` consistent
      with header; R4 total on malformed block.
    - D1. validation order (structural checks before the 30 ms re-exec). D2. **Pre-mortem:** 4-line trace where a
      builder submits a header with a high `value` and a block that produces less — show R2 catching it. D3.
      **Anti-shortcut audit:** trusting submitted `state_root` (skips re-exec) — one line on why it defeats the whole purpose.
    - **Output**: `notes/83_submit_block_design.md` with R1–R4, D1–D3.
  - #### Phase 2 — Write the failing tests (~25 min) — file `submit_block.rs` `#[cfg(test)]`
    - `block_rejects_state_root_mismatch` — **(NEWBIE)** submit block whose `state_root` ≠ recomputed; assert `Err(StateRootMismatch)`. Naive trust-the-field fails.
    - `block_rejects_value_shortfall` — **(SENIOR, cross-primitive)** compose with Monday's header: header bids `value=5 ETH`, block realizes `3 ETH`; assert `Err(ValueShortfall)`. Each passes alone; composition fires. Naive "value≥0" passes wrongly.
    - `block_accepts_honest_submission` — **(negative-control)** honest block whose re-exec matches header and value≥bid is `Ok(Accepted)`; proves the validator isn't rejecting everything.
  - #### Phase 3 — Make the tests pass, one failure at a time (~25 min)
    - **(NEWBIE) When `block_rejects_state_root_mismatch` reports `Ok`:** Symptom: forged state root accepted. Why: used `block.state_root` directly. Fix: `let got = execute_block(&block)?.state_root; if got != block.state_root { return Err(StateRootMismatch); }`. Muscle: re-derive, never trust the submission. Reapplies at: W84 integration, W91 payload validate.
    - **(SENIOR) When `block_rejects_value_shortfall` reports `Ok`:** Symptom: bid says 5 ETH, block pays 3, accepted. Why: compared realized value to 0 (or skipped). Fix: `if realized < bid.value { return Err(ValueShortfall); }`. Cost: a dishonest builder wins slots for free — protocol-level theft. Muscle: the bid is the contract; the block must honor it. Reapplies at: W84 auction, W91 lane bid ranking.
  - #### 5-year failure mode
    `notes/83_submit_block_followup.md`: when blocks exceed re-exec budget (gas limit raise to 60M+), 30 ms re-exec
    on the auction path becomes the bottleneck. Trigger: re-exec > slot-time/N. Migration: optimistic relay (accept
    bid, re-exec async, slash on mismatch).
- [ ] [HFT] **Build (bar (c))**: `crates/ledger-deterministic/src/sm.rs` + `crates/ledger-deterministic/src/recovery.rs`
  — bring the deterministic settlement SM to v0.5: `apply(op_num, op)` (the **W74 VSR apply discipline**) + snapshot/restore
  + replay; 1M transfers, crash mid-batch, recovery yields byte-identical state. (~total 110 min across 3 phases).
  - 🔧 **Why this matters**: ledger v0.5 is the deterministic-SM baseline for the venue's **settlement journal**
    (mirrors TigerBeetle's "Tiger Style"). Per README.md §4A, **this SM runs on `consensus-vsr`
    NormalOperation (W74) — the order log, the settlement journal, and the replicated SM are the SAME VSR log, no
    second log.** v0.7 layers VOPR (W88); v1.0 layers static-mem + io_uring + **VSR replication** (W92); W103 deploys
    it 3-node with `SettlementId(snapshot_epoch, op_number, checksum)` = the VSR op-number. Byte-identical-recovery is
    the load-bearing axiom — every later module assumes `apply(op_stream)` post-crash produces the SAME state hash
    regardless of crash timing (the §4A "deterministic replay-safe core" principle). Three traps: a `state_hash` that
    `.collect()`s a fresh Vec (32 MB/call churn → VOPR throughput collapse), recovery that re-fsyncs the WAL it's
    reading (2500× too slow), and a snapshot taken mid-write that captures a torn account. First-attempt impls fail
    ≥1 Phase-2 test.

  #### Phase 1 — Problem, prediction, design (~25 min, no code yet)
  - 🎯 **Pre-prediction prompt** — write sealed answers in `notes/83_polish_prediction.md`:
    - P1: the collection you'd reach for to compute `state_hash` over all accounts (exact import line).
    - P2: predicted bytes allocated per `state_hash` call for 1M accounts (Jeff-Dean: 32 B/account → ?).
    - P3: predicted recovery time for 1M ops if recovery calls `wal.append + fsync` per op (fsync ≈ 300 µs/op → ?).
    - P4: prediction — does your `state_hash` over a `HashMap` give the same bytes on two runs? (iteration order!)
    - P5: list every Phase-2 test you predict your first attempt will fail.
  - *Mental model*: the settlement SM is a pure function `state' = apply(op_num, op)` on the **same VSR log** the
    matching engine applies (W74). Recovery just replays the op log into a fresh SM. If `apply` or `state_hash` has
    any nondeterminism (alloc address, wall-clock, map order, `f64`), two VSR replicas diverge and the cluster breaks.
    The ledger is *book-mode discipline applied to settlement* — same `apply(op_num)` contiguity rule W74 R4 locked.
  - *Contract*: `trait StateMachine { fn apply(&mut self, op_num: u64, op: &Op); fn state_hash(&self) -> [u8;32]; fn snapshot(&self, out: &mut Vec<u8>); fn restore(&mut self, bytes: &[u8]); }` on `LedgerSM { accounts: FixedMap<AccountId, Account>, applied_op: u64 }`. **This `StateMachine` is the SAME trait `NormalReplica<SM>` (W74 R13) is generic over** — the ledger plugs into the VSR hot path without a protocol change.
  - *Back-of-envelope* (bar (c)): `state_hash` over 1M accounts touching 32 B each = 32 MB read; at memcpy ~10 GB/s
    that's ~3 ms/call. If `.collect()` allocates that 32 MB *and* reallocs through doublings (~3× bytes touched ≈
    96 MB) + a 1M-element sort (~n log n ≈ 20M comparisons ≈ tens of ms), VOPR at 10 hashes/sec = 320 MB/s churn →
    allocator-bound, 4× slowdown. Cliff: scratch-reuse `state_hash` ≈ 3 ms vs collect+sort ≈ 30 ms = 10×.
  - **Requirements** R1..R8 — write in `notes/83_polish_design.md`:
    - R1. `apply` total (no panics, no allocs).
    - R2. `apply` pure of wall-clock — any time-like input arrives inside `op`, never read from the clock (W74 R5: SequenceClock).
    - R3. `state_hash()` canonical (order-independent for accounts, `applied_op` included).
    - R4. 100/100 random crash points yield identical `state_hash`.
    - R5. recovery < 2 s for 1M ops on M2.
    - R6. snapshot size O(active_accounts), not O(max_accounts).
    - R7. `state_hash` zero-alloc (pre-allocated scratch).
    - R8. **Apply contiguity (VSR lock):** `apply(op_num)` requires `op_num == self.applied_op + 1`; gap or duplicate panics (W74 R4 — VSR guarantees no gaps post-state-transfer, W76). This is the seam that lets the ledger ride the same VSR log as matching.
  - **Design walk-through** D1..D6 — answer in the design doc (no peeking at TigerBeetle source until Phase 3):
    - D1. Sketch `StateMachine` trait + `LedgerSM` with `FixedMap`; primitive table (HashMap vs BTreeMap vs FixedMap: Sync? alloc on insert? sorted iter? deterministic order?).
    - D2. **Pre-mortem (state_hash allocates fresh Vec):** `let tuples: Vec<_> = self.accounts.iter().map(...).collect(); tuples.sort(); blake3(&tuples)` allocates 1M × 32 B = 32 MB every call; VOPR at 10/sec = 320 MB/sec churn. Walk through pre-allocated scratch reused across calls.
    - D3. **Pre-mortem (recovery re-fsyncs):** naive recovery does `apply + wal.append + wal.fsync` per op → recovery as slow as fresh durable apply → 1M ops = ~5 min vs < 2 s target. Recovery NEVER writes the WAL.
    - D4. **Pre-mortem (HashMap iteration order):** `state_hash` over `std::collections::HashMap` hashes accounts in random-seed order → two runs differ → R3/R4 fail nondeterministically. Walk through sorted iteration or `FixedMap` with fixed order.
    - D5. **Pre-mortem (single-log betrayal — load-bearing):** 5-step trace where someone "speeds up settlement" by writing settlement records to a *second* durable log (e.g. directly to the Aeron Archive AND the VSR log as independent write targets). Show: (i) under partition the two logs disagree on op order; (ii) recovery from the wrong log double-applies a transfer (money created); (iii) "no primitive twice" is violated; (iv) cluster-VOPR (W108) can't prove linearizability across two logs; (v) the fix — the settlement journal IS the VSR log (W74); the Aeron Archive (this Tue) records a *projection* of the committed VSR log off the hot path, never an independent settlement write target.
    - D6. **Anti-shortcut audit:** `.collect()` inside `state_hash` (alloc), `SystemTime::now()` inside `apply` (nondeterminism), `HashMap` for accounts (random order), `f64` balance math (cross-arch divergence), a second settlement log (single-log betrayal) — one line each on why each is wrong HERE.
  - **Output**: `notes/83_polish_design.md` with R1–R8, D1–D6, the 320 MB/s churn sketch + the single-log trace.

  #### Phase 2 — Write the failing tests (~25 min)
  File: `crates/ledger-deterministic/src/sm.rs` `#[cfg(test)]` + a `benches/state_hash.rs` criterion bench.
  - `ledger_apply_contiguity_panic_on_gap` — **(NEWBIE)** `apply(1,..)` then `apply(3,..)`; `#[should_panic(expected = "apply gap")]`. Exercises R8 (the VSR seam). Naive `>=` accept fails.
  - `ledger_100_random_crash_points_identical_hash` — **(SENIOR)** 100 crash points across a 1M-op stream; recover from each; assert all 100 `state_hash` equal. Exercises R3/R4. Naive HashMap-order impl fails nondeterministically.
  - `ledger_recovery_under_2s_for_1m_ops` — **(SENIOR, anti-shortcut)** bench: kill+restart, replay 1M ops, assert < 2 s AND `wal.append_count_during_recovery == 0`. Exercises R5. Naive re-fsync impl fails (~5 min).
  - `ledger_state_hash_zero_alloc` — **(EXPERT, anti-shortcut)** dhat (release-mode) wraps 1000 `state_hash` calls; assert total allocs == 0 after warmup. Exercises R7. Naive `.collect()` impl fails (32 MB × 1000).
  - `ledger_state_hash_bench_lands_in_band` — **(negative-control)** criterion bench asserts the measured `state_hash` time lands in 1–10 ms for 1M accounts; if it reports < 100 µs the bench is measuring nothing (optimized away — `black_box` the result). Proves the bench bites.
  - `ledger_snapshot_o_active_accounts` — **(SENIOR)** snapshot a SM with 1k active of 1M capacity; assert snapshot bytes ≈ 1k × record-size, not 1M. Exercises R6.
  - `ledger_snapshot_then_replay_torn_write_safe` — **(EXPERT, cross-primitive)** snapshot DURING an `accounts[X]` mutation (compose with `recovery::truncate`); assert restored account is either fully-old or fully-new, never torn. Exercises adversarial single-writer/snapshot ordering.

  #### Phase 3 — Make the tests pass, one failure at a time (~60 min)
  - **(NEWBIE) When `ledger_apply_contiguity_panic_on_gap` passes but you wrote `if op_num >= self.applied_op + 1`:**
    - Symptom: gap test green, but a duplicate at the same `op_num` sneaks through (or a gap silently advances `applied_op`, dropping settlement ops).
    - Why: `>=` is permissive; the VSR apply path demands exact equality (W74 R4).
    - Fix: `assert_eq!(op_num, self.applied_op + 1, "apply gap at {op_num}"); self.applied_op = op_num;`. Strict `==` only.
    - Cost avoided: a dropped/replayed settlement op → money created or lost → replica divergence (correctness, bar (c)).
    - Muscle: contiguous apply uses `==`, never `>=`; identical to the matching engine's apply (W74). Reapplies at: W88 VOPR replay, W92 ledger v1.0 VSR apply, W103 capstone settlement apply.
  - **(SENIOR) When `ledger_100_random_crash_points_identical_hash` reports flaky/different hashes:**
    - Symptom: same op stream, same crash point, different `state_hash` across runs.
    - Why: accounts stored in `std::collections::HashMap`; `state_hash` iterates in random-seed order; BLAKE3 over a different order = different digest.
    - Fix: iterate in a canonical order — sort by `AccountId` (or use `FixedMap`/`BTreeMap` with fixed iteration) before hashing; include `applied_op`.
    - Cost avoided: nondeterminism that silently diverges VSR replicas — a correctness-class bar-(c) violation, not "slow." This is the SAME divergence class W74's `cross_replica_state_hash_identical` traps.
    - Muscle: any hash over a collection must fix the iteration order, or determinism is a coin flip. Reapplies at: W88 VOPR property check, W92 ledger v1.0 SHIP audit, W103 capstone cross-replica hash.
  - **(SENIOR, perf, bar (c) violation) When `ledger_recovery_under_2s_for_1m_ops` reports ~5 minutes:**
    - Symptom: `kill -9` + restart of a 1M-op ledger takes minutes.
    - Why: recovery treats replay like fresh apply — `sm.apply(op); wal.append(op); wal.fsync()` per op → ~300 µs/op (fsync-bound); 1M ops = ~5 min, 150× over the < 2 s target.
    - Fix: recovery NEVER writes the WAL (the WAL is what it's reading FROM) — just `sm.apply(op)` per entry; no fsync, no append. 1M ops × ~120 ns SM apply = ~120 ms. Harness asserts `wal.append_count_during_recovery == 0`.
    - Cost avoided: ~300 µs/op → ~120 ns/op = 2500× speedup.
    - Muscle: replay/recovery paths skip durability (data is already durable in the source) — a read-only sweep of `apply`. Reapplies at: W84 archive replay (reads, never writes back), W88 VOPR recovery scenarios, W92 ledger v1.0 SHIP recovery, W103 capstone follower catch-up.
  - **(EXPERT, perf, bar (c) violation) When `ledger_state_hash_zero_alloc` reports 32 MB Vec spikes:**
    - Symptom: dhat (release) shows 32 MB allocated per `state_hash` call; VOPR scenarios 4× slower under allocator pressure.
    - Why: `let mut tuples: Vec<_> = self.accounts.iter().map(...).collect(); tuples.sort(); blake3(&tuples)` allocates 1M × 32 B and reallocs through ~20 doublings (~3× bytes touched).
    - Fix: pre-allocate `scratch: Vec<(AccountId,u64,u64)>` once at construction with `capacity = max_accounts`; `state_hash` does `scratch.clear(); scratch.extend(iter); scratch.sort_unstable(); blake3(scratch.as_slice())`. Zero steady-state allocs. (Or `FixedMap`/`BTreeMap` for O(1) sorted iter, no sort.)
    - Cost avoided: 32 MB/call × 10 calls/sec = 320 MB/sec churn → 0 alloc; restores VOPR scenario throughput (bar (c) violation: allocator pressure injects nondeterministic latency into deterministic replay).
    - Muscle: hash/checksum over a collection reuses a pre-allocated scratch buffer — never `.collect()` inside a hash function. Reapplies at: W84 archive replay checksum, W88 VOPR property check (state_hash 10M×/scenario), W92 ledger v1.0 SHIP audit.
  - **(EXPERT, cross-primitive) When `ledger_snapshot_then_replay_torn_write_safe` reports a torn account:**
    - Symptom: restored account has new balance but old `flags` (or vice versa) — a half-applied record.
    - Why: snapshot read the account struct field-by-field while `apply` was mid-mutation (no single-writer fence / no copy-on-snapshot).
    - Fix: snapshot under the single-writer invariant — snapshot runs on the same core/turn as `apply`, never interleaved; or snapshot an `applied_op`-tagged immutable copy. Restore checks `applied_op` consistency.
    - Cost avoided: a torn snapshot that produces a state hash matching neither pre nor post — undetectable divergence (bar (c) violation; defeats W76-style state_transfer).
    - Muscle: snapshots respect the single-writer principle — never read state a writer is mutating. Reapplies at: W84 integration snapshot, W92 ledger v1.0 snapshot archival, W103 capstone checkpoint.

  #### 5-year failure mode
  `notes/83_polish_followup.md`: at 1B accounts, `state_hash` recomputation becomes 32 GB/call — flat-hash dies.
  Trigger: `active_accounts × 32 B > L3` and recompute time > VOPR step budget. Migration: `state_hash` trait
  abstraction; flat-sort impl for small ledgers + incremental Merkle impl for large (the same incremental-vs-full
  tension that forces incremental-margin in `risk-engine` at 100k accounts, §4A).
- [ ] [HFT] [NEW v2] **Build (bar (c))**: `crates/messaging-aeron/src/archive.rs` — Aeron Archive recording layer: every
  published frame (a **projection of the committed VSR log**, off the hot path) is also written to `mmap-queue::Cycle`.
  (~total 130 min across 3 phases).
  (Heavier crc32c/pretoucher cards trimmed to two perf failure cards; remaining cards are in the W84 replay Build.)
  - 🔧 **Why this matters**: Aeron Archive distinguishes Aeron from a pure in-memory bus — on-disk replayable
    history (mirrors real-logic/aeron `ArchiveRecorder`). Without it: no late subscriber, no forensic replay.
    **v3 framing**: the Archive is the **off-hot-path** persistent journal — it records a *projection* of the
    committed VSR log (W74), it is NEVER a second log on the matching/settlement hot path (README.md §4
    "one log on the hot path"). Two bar-(c) traps a naive impl hits: software crc32c (~250 ns for 256 B vs ~20 ns
    hardware — blows the 15% overhead budget) and an un-pretouched growing mmap (page-fault spikes in the publisher's
    `offer()` critical path). Downstream: W84 `archive.replay`; ledger v0.7 VOPR uses the Archive as the persistent
    journal of record; W84 `log-distributed` v0.5 reads the same projection for Kafka-fidelity fan-out. First-attempt
    impls fail ≥1 Phase-2 test.

  #### Phase 1 — Problem, prediction, design (~30 min, no code yet)
  - 🎯 **Pre-prediction prompt** — write sealed answers in `notes/83_archive_prediction.md`:
    - P1: the crc32c entry point you'd reach for (exact import — `crc32c` crate? `crc32fast`? a `std::arch` intrinsic?).
    - P2: predicted ns to crc a 256 B frame with a portable software impl vs a hardware intrinsic.
    - P3: predicted P99.9 effect of letting the archive mmap grow without a pretoucher (page-fault cost ≈ 1 µs).
    - P4: prediction — does recording advance the publication's term-position BEFORE or AFTER the frame is durable in the Cycle?
    - P5: list every Phase-2 test you predict your first attempt will fail.
  - *Mental model*: the recorder is a tee on a *read-side projection* — it must persist a frame to the Cycle BEFORE
    the subscriber's tail can observe that frame's term-position, or a subscriber reads a frame whose recording is
    incomplete. It records what the VSR log already committed; it is not the source of truth.
  - *Contract*: `struct ArchiveRecorder { cycle: Cycle, position: ArchiveOffset, pretoucher: Pretoucher }`; `fn record(&mut self, frame: &[u8]) -> ArchiveOffset`; ordering guarantee documented on `record`.
  - *Back-of-envelope* (bar (c)): at 1M frames/sec × 256 B = 256 MB/sec. Software crc32c ≈ 1 GB/s = ~250 ns/frame →
    250 ms/sec of CPU just on crc. Hardware crc32c (`_mm_crc32_u64` / `__crc32d`) ≈ 12 GB/s = ~20 ns/frame → 20
    ms/sec. Cliff: 12×. mmap grows past a 4 KiB page every 4096/256 = 16 frames; an un-pretouched minor page fault
    ≈ 1 µs lands in `offer()`'s critical path → P99.9 spike every ~16 µs.
  - **Requirements** R1..R8 — write in `notes/83_archive_design.md`:
    - R1. Frame written to mmap-queue BEFORE the Publication's term-position advances.
    - R2. `ArchiveOffset` strictly monotonic across term-buffer rotations.
    - R3. Crash mid-record leaves the tail recoverable via `recovery::truncate`.
    - R4. Recording adds ≤ 15% overhead to `offer()` P99.
    - R5. Archive position lag < 1 ms at 1M frame/s sustained.
    - R6. W84 replay byte-identical to original publication.
    - R7. Hardware-accelerated crc32c (`_mm_crc32_u64` x86 / `__crc32d` aarch64) on the hot path.
    - R8. Pretoucher (W77) paired with the archive Cycle to keep the next page warm.
  - **Design walk-through** D1..D5 — answer in the design doc (no peeking at Aeron source until Phase 3):
    - D1. Sketch `ArchiveRecorder` + `ArchiveOffset`; primitive table (crc32c crate vs `std::arch` intrinsic: latency? compile-time vs runtime dispatch? bar-(c) fit?).
    - D2. **Pre-mortem (software crc32c):** ~250 ns for 256 B at ~1 GB/s portable vs ~20 ns hardware (12×); at 1M frames/sec, 250 ms/sec vs 20 ms/sec. Walk through `target_feature` gating.
    - D3. **Pre-mortem (pretoucher absent):** archive Cycle grows past a page every 16 frames; ~1 µs page-fault in the publisher's `offer` critical path; P99.9 spikes every ~16 µs. Walk through pretoucher pairing.
    - D4. **Pre-mortem (recording lag):** `archive_position` falls behind `publication_position` under burst → late subscriber sees a gap. Backpressure the publication when archive lag > N MB.
    - D5. **Anti-shortcut audit:** runtime-dispatch crc (`crc32c` crate auto-detect — branch + indirect call per frame), un-pretouched mmap, advancing term-position before durability, treating the Archive as a *write target* instead of a *projection sink* — one line each on why each is wrong HERE.
  - **Output**: `notes/83_archive_design.md` with R1–R8, D1–D5, the 250 ms/sec crc sketch.

  #### Phase 2 — Write the failing tests (~30 min)
  File: `crates/messaging-aeron/src/archive.rs` `#[cfg(test)]` + `benches/archive_overhead.rs`.
  - `archive_overhead_under_15pct_at_p99` — **(SENIOR, anti-shortcut)** bench `offer()` with/without archive; assert P99 overhead ≤ 15%. Naive software-crc impl fails (~30%). Exercises R4.
  - `archive_crc32c_uses_hardware_accelerator` — **(EXPERT, anti-shortcut)** disasm the `record` hot path; assert the SSE4.2 `crc32` / aarch64 `crc32d` instruction is present (no `call crc32c::...`). Exercises R7. Naive crate-call impl fails.
  - `archive_pretoucher_eliminates_p99_9_page_fault_spike` — **(EXPERT, cross-primitive)** compose with W77 Pretoucher; bench P99.9 with/without; assert the ~1 µs spike-every-16-µs pattern is gone with the pretoucher. Exercises R8.
  - `archive_frame_durable_before_position_advances` — **(SENIOR)** inject a crash between `record` and term-position advance; assert no subscriber observed the un-recorded frame. Exercises R1.
  - `archive_offset_monotonic_across_rotation` — **(SENIOR)** record across a term-buffer rotation; assert `ArchiveOffset` strictly increases. Exercises R2.
  - `archive_bench_overhead_lands_in_band` — **(negative-control)** assert the measured baseline `offer()` time lands in a plausible band (e.g. 50–500 ns/frame); if it reports 0 ns the bench is optimizing the offer away — `black_box` the frame. Proves the overhead bench bites.

  #### Phase 3 — Make the tests pass, one failure at a time (~40 min)
  - **(EXPERT, perf, bar (c) violation) When `archive_crc32c_uses_hardware_accelerator` reports a `call` to a software crc:**
    - Symptom: archive recording adds ~30% overhead instead of ≤15%; CPU flamegraph shows ~20% in crc32c software; disasm shows no `crc32` instruction.
    - Why: the `crc32c` crate's portable software path (~1 GB/s = ~250 ns for 256 B) vs hardware (~12 GB/s = ~20 ns); even the crate's runtime auto-detect adds a branch + indirect call per frame.
    - Fix: gate on `target_feature = "sse4.2"` (x86) / `target_feature = "crc"` (aarch64); call `std::arch::x86_64::_mm_crc32_u64` / `std::arch::aarch64::__crc32d` directly, pre-selected at compile time (bar (c) — don't rely on the library's per-call runtime detection).
    - Cost avoided: ~230 ns/frame × 1M frames/s = ~230 ms/sec; restores `offer()` within the 15% overhead budget.
    - Muscle: any hot-path hash/checksum on a bar-(c) crate uses the hardware accelerator, compile-time selected. Reapplies at: W83 ledger journal crc32c, W84 archive replay verify, W92 ledger v1.0 io_uring buffer crc; BLAKE3 should use the AVX-512 path on server chips.
  - **(EXPERT, perf, bar (c) violation) When `archive_pretoucher_eliminates_p99_9_page_fault_spike` reports 3–5 µs spikes:**
    - Symptom: archive-enabled `offer()` P99.9 shows 3–5 µs spikes every ~16 µs; P99 fine but P99.9 fails the 15% gate.
    - Why: the archive writes ~200 MB/s; the Cycle mmap grows past a page every 4 KiB / 256 B = 16 frames; each new page = a minor page fault (~1 µs) in the publisher's `offer` critical path.
    - Fix: when constructing `ArchiveRecorder`, spawn a W77 `Pretoucher` with `pretouch_ahead = 64 pages` paired to the recorder's tail; the worker stays ≈ 256 KiB ahead, faulting pages on a background thread.
    - Cost avoided: the ~1 µs page-fault spike removed from the `offer()` critical path; restores the P99.9 budget.
    - Muscle: any growing mmap on a bar-(c) write path MUST be paired with a pretoucher — the W77 Mon principle is non-negotiable. Reapplies at: W92 ledger v1.0 io_uring journal, W89 marketdata-kernelbypass AF_XDP UMEM pretouch.

  #### 5-year failure mode
  `notes/83_archive_followup.md`: when CXL.mem replaces NVMe, disk-fsync semantics dissolve into cache-flush.
  Trigger: backend latency profile flips (fsync ~ms → flush ~µs). Migration: backend abstraction — SSD impl uses
  group commit, CXL impl uses single-flush.
- [ ] Commit + log (~10 min)

> **Day-load note**: block submission ~90 + ledger v0.5 ~110 + aeron Archive ~130 + commit ~10 ≈ 5.7 h. At the
> ceiling but under it. The runtime v1.0 zero-alloc audit (originally also on Tue) is moved to Wednesday to hold
> this day ≤ ~5.7 h.

## Wednesday — Builder client + runtime v1.0 zero-alloc audit + `tempo-payment-lane` scaffold + ledger v0.5 SHIP/tag

- [ ] **Build (bar (b))**: `crates/consensus-engine/src/builder/client.rs` — the builder client perspective (assembles
  payload, signs the bid, calls submit). (~90 min). Output: `client.rs` exists; `cargo test -p consensus-engine
  builder_client_` green (round-trips a header+block through Mon/Tue's `validate`/`submit_block`).
- [ ] [NEW v2 runtime v1.0] **Build (bar (c))**: `crates/runtime-thread-per-core/src/reactor.rs` — zero-alloc audit on
  `Reactor::poll()`: every hot path replaced with pre-allocated buffers. (~total 150 min across 3 phases).
  - 🔧 **Why this matters**: runtime v1.0 ships W85 Thu. Bar-(c) VOPR-grade requires `Reactor::poll` allocation-free
    so memory pressure can't starve the hot core or inject nondeterminism into replays (mirrors Glommio/Seastar
    typed-task storage; TigerBeetle's runtime is allocation-free by construction). This is the substrate `risk-engine`
    (W90/W99 — the principal-defining crate, shard-by-account, incremental margin at 100k accounts §4A) runs on, so a
    leaking reactor poisons the convergence cell's latency budget. Two traps: `spawn(impl Future)` that compiles to
    `Box::new` (~300 ns/spawn heap churn) and a dhat audit run in DEBUG that shows "0 allocs" while RELEASE allocates
    100/poll (different inlining). Downstream: matching-engine, marketdata, messaging-aeron, ledger v0.5, and
    risk-engine all run on top. First-attempt impls fail ≥1 Phase-2 test.

  #### Phase 1 — Problem, prediction, design (~35 min, no code yet)
  - 🎯 **Pre-prediction prompt** — write sealed answers in `notes/83_zero_alloc_prediction.md`:
    - P1: the spawn signature you'd reach for (exact — `fn spawn(f: impl Future + 'static)`? something typed?).
    - P2: predicted allocs per `poll()` for a naive `tokio`-style spawn at 100k spawns/sec (Box cost ≈ 50 ns malloc + 300 ns total).
    - P3: predicted dhat result in DEBUG vs the custom-counting-allocator result in RELEASE — same or different?
    - P4: prediction — does your error path `format!`/`String::from` anywhere (fault-injection)?
    - P5: list every Phase-2 test you predict your first attempt will fail.
  - *Mental model*: a thread-per-core reactor owns a fixed task set on one core; if `poll()` allocates, the
    allocator (shared, locked) reaches across cores and reintroduces the cross-core contention the whole runtime
    exists to avoid — and injects nondeterministic latency into deterministic replay (the §4A deterministic-core principle).
  - *Contract*: `struct Reactor { tasks: Slab<TaskFuture>, timers: HierarchicalWheel, scratch: ScratchBuffer }`; `fn poll(&mut self) -> usize`; `fn spawn<F: Future + 'static>(&mut self, f: F) -> TaskId`.
  - *Back-of-envelope* (bar (c)): a `Box::new(future)` = ~50 ns malloc + the eventual free (~50 ns) + an indirect
    `Box<dyn Future>::poll` vtable call (~5 ns mispredict-prone) = ~300 ns/spawn end-to-end. At 100k spawns/sec =
    ~30 ms/sec of CPU plus shared-allocator lock contention. Cliff: typed-slab spawn ≈ 5 ns (slab index write) vs
    Box-spawn ≈ 300 ns = 60×.
  - **Requirements** R1..R6 — write in `notes/83_zero_alloc_design.md`:
    - R1. `poll()` invokes ZERO `alloc::alloc` (dhat heap counter delta = 0).
    - R2. every container bounded by const generics or arena.
    - R3. error paths use `&'static str` codes, never `format!`/`String::from`.
    - R4. dhat `total_allocs` ≤ 10 over 1M poll iterations (preamble only).
    - R5. CI guard catches regression.
    - R6. `spawn` uses a typed slab, NOT `Box<dyn Future>`.
  - **Design walk-through** D1..D5 — answer in the design doc (no peeking at Glommio source until Phase 3):
    - D1. Sketch `Reactor` with `ArrayVec`/`Slab` + `HierarchicalWheel` + `ScratchBuffer`; primitive table (`Box<dyn Future>` vs typed slab vs enum-dispatch: alloc/spawn? indirect call? bounded?).
    - D2. **Pre-mortem (tokio::spawn pattern):** `spawn(impl Future + 'static)` → `Box::new(future)` because the runtime stores tasks heterogeneously in one queue; ~300 ns/spawn. Walk through a typed-task design with concrete-type slabs + a `TaskFuture` enum.
    - D3. **Pre-mortem (dhat profile vs release skew):** dhat-rs recompiles with `--features dhat-heap` → different inlining/allocator routing than release; shows "0 allocs" under dhat but 100/poll under release. Walk through running dhat in release + a custom counting allocator for cross-validation.
    - D4. **Pre-mortem (hidden alloc via trait object):** `tracing::event!` internally Box-es spans. Gate hot-path tracing behind `cfg(not(release_alloc_audit))`.
    - D5. **Anti-shortcut audit:** `Box<dyn Future>` spawn, `format!` in error paths, an unbounded `Vec` task queue, trusting a debug-build dhat audit — one line each on why each is wrong HERE.
  - **Output**: `notes/83_zero_alloc_design.md` with R1–R6, D1–D5, the 30 ms/sec spawn-churn sketch.

  #### Phase 2 — Write the failing tests (~30 min)
  File: `crates/runtime-thread-per-core/src/reactor.rs` `#[cfg(test)]`.
  - `reactor_poll_zero_alloc_1m_iterations` — **(EXPERT, anti-shortcut)** dhat (release) over 1M `poll()`; assert `total_allocs ≤ 10`. Exercises R4. Naive Box-spawn impl fails.
  - `reactor_no_box_dyn_future_in_spawn` — **(EXPERT)** assert `spawn` stores into a typed slab (no `Box::new`); grep the MIR/disasm or a marker test. Exercises R6.
  - `reactor_no_format_in_error_paths` — **(SENIOR, anti-shortcut)** fault-inject an error; dhat asserts the error path allocates 0. Exercises R3. Naive `format!` impl fails.
  - `reactor_arrayvec_bounded_under_load` — **(SENIOR, cross-primitive)** 10 producer-tasks + 10 consumer + timer-heavy load; assert alloc count stays ≤ 10 (composes spawn + timer wheel). Exercises R2.
  - `reactor_dhat_release_mode_matches_custom_counter` — **(EXPERT, negative-control)** run dhat-release AND a custom counting allocator in the same binary; assert they report the SAME alloc count. Proves the audit tool isn't lying. Exercises D3.

  #### Phase 3 — Make the tests pass, one failure at a time (~50 min)
  - **(EXPERT, perf, bar (c) violation) When `reactor_no_box_dyn_future_in_spawn` reports 100k allocs/sec:**
    - Symptom: dhat reports 100k allocs/sec attributed to `spawn`; profile shows `__libc_malloc`+`free` ≥10% of runtime CPU.
    - Why: `spawn(impl Future + 'static)` compiles to `Box::new(future)` (~300 ns/spawn end-to-end); the boxed future is dropped via an indirect call + free.
    - Fix: typed-task design — `spawn<F: Future + 'static>` stores `F` in a slab indexed by concrete type; expose ONE `TaskFuture` enum wrapping known futures (via `enum_dispatch` or hand-written); generic tasks live in their own typed slabs; the slab is heap-allocated once at startup. NO `Box<dyn Future>` anywhere. (Glommio + Seastar use this.)
    - Cost avoided: ~300 ns/spawn × 100k/s = ~30 ms/sec + shared-allocator lock contention eliminated.
    - Muscle: a thread-per-core runtime stores futures in typed slabs, not `Box<dyn>` — `tokio::spawn` is a bar-(b) pattern, NOT bar-(c). Reapplies at: W84 deterministic-harness task storage, W85 runtime v1.0 SHIP audit, W89 marketdata-kernelbypass task pool, W99 risk-engine shard reactor.
  - **(EXPERT, negative-control, bar (c) violation) When `reactor_dhat_release_mode_matches_custom_counter` reports a mismatch:**
    - Symptom: dhat audit passes (0 allocs); production bench shows allocs > 0; bisect reveals a release-build inline that allocates.
    - Why: dhat-rs recompiled with `--features dhat-heap` has different inlining/allocator routing/LLVM than the release build — "0 allocs" under dhat, 100/poll under release.
    - Fix: run the dhat audit in `--release` (with `[profile.release.package.dhat] opt-level = 0` to keep dhat instrumentation light while the rest stays optimized); pair with a custom counting `GlobalAlloc` baked into the same test binary; run both and cross-validate.
    - Cost avoided: undetected alloc regressions that pass a debug audit but fail in production (bar (c) violation — the audit gives false assurance).
    - Muscle: profile tools must run on PRODUCTION builds; never trust a debug-build audit. Reapplies at: W82 v1.5 SHIP perf gate, W84 archive replay perf bench, W85 runtime v1.0 SHIP audit, W92 ledger v1.0 SHIP audit, W99 risk-engine p99.99 report.
  - **(SENIOR, perf) When `reactor_no_format_in_error_paths` reports allocs on fault injection:**
    - Symptom: happy-path audit clean, but injecting an error allocates (e.g. `format!("task {} failed", id)`).
    - Why: error paths build owned `String`s.
    - Fix: a `&'static str` error-code table (or `thiserror` with no string interpolation); no allocation on the error path.
    - Cost avoided: alloc on the error path = nondeterministic latency exactly when the system is already degraded.
    - Muscle: error paths are hot under fault injection — keep them zero-alloc too. Reapplies at: W85 runtime v1.0 SHIP, W92 ledger v1.0 SHIP audit.

  #### 5-year failure mode
  `notes/83_zero_alloc_followup.md`: when WASM-compiled futures become a deployment target, the typed-slab approach
  must accommodate the WASM ABI. Trigger: a deploy target requires `wasm32`. Migration: enum dispatch via wasm-bindgen.
- [ ] [HFT] **SHIP + Tag `ledger-deterministic v0.5.0`** — deterministic settlement SM + double-entry + journal,
  running on the **W74 VSR apply discipline** (`apply(op_num, op)` contiguity, canonical `state_hash`, byte-identical
  recovery). (~20 min) Output: git tag `ledger-deterministic-v0.5.0` pushed; `cargo test -p ledger-deterministic`
  green; README Appendix E W83 row checked; `notes/83_ledger_ship.md` records the one-line v0.5→v0.7 next-step (VOPR, W88).
- [ ] [Tempo] **Build (scaffold, bar (b))**: `crates/tempo-payment-lane/` workspace member depending on `consensus-engine`
  + define the `LaneStrategy` trait. (~90 min) — crate-scaffold tier (no 3-phase arc — nothing to test yet).
  - File(s): `crates/tempo-payment-lane/Cargo.toml`, `crates/tempo-payment-lane/src/lib.rs`. Mirror: Tempo
    `payment-lane`. **v3 framing**: `tempo-*` is the retained **RWA-perps collateral-rails option leg** (§2 Option leg B)
    — not on the venue critical path, but the fintech/quote-to-cash edge. Deps: `consensus-engine` (Layer-7 inherits
    per README, target ratio ≥0.85). Define `trait LaneStrategy { fn select_transactions(&self, pool: &[PoolTx],
    gas_limit: u64) -> Vec<PoolTx>; }` with an empty default impl + a `TempoLaneStrategy { tip20_reservation_pct: u8 }`
    skeleton. Will hold the tip20-vs-general bucketing logic (impl Thu/Fri). Downstream consumer: W91
    `tempo-payment-lane` v0.1.0 + consensus-engine v1.0.
  - Output: `cargo build -p tempo-payment-lane` green; `LaneStrategy` trait + `TempoLaneStrategy` skeleton compile;
    workspace member listed in root `Cargo.toml`.
- [ ] Commit + log (~10 min)

> **Day-load note**: builder client ~90 + runtime zero-alloc ~150 + ledger SHIP/tag ~20 + lane scaffold ~90 + commit
> ~10 ≈ 6.0 h. At the ceiling. If it overflows, the lane scaffold (a crate-scaffold tier item) can slide to Thu,
> which is light (~70 min).

## Thursday — Builder integration tests + lane strategy impl

- [ ] **Build (integration, bar (b))**: `crates/consensus-engine/tests/builder_flow.rs` — end-to-end builder integration
  test (client → header submit → block submit → accept). (~40 min). Output: `cargo test -p consensus-engine --test
  builder_flow` green; one happy-path round-trip + one rejected-bid path asserted.
- [ ] [Tempo] **Build (bar (b))**: `crates/tempo-payment-lane/src/strategy.rs` — implement
  `TempoLaneStrategy::select_transactions` for the simple case: split the pool into tip20 vs general buckets (check
  `fee_token` from `tempo-tx-envelope`), fill the reservation from tip20 first, then general. (~70 min) — correctness-focused.
  - 🔧 **Why this matters**: mirrors Tempo's payment-lane fee-token reservation (the RWA-collateral-rails option leg).
    Trap: a naive split forgets the `gas_limit` cap and over-fills the block, or mis-buckets a tx whose `fee_token`
    is absent. Downstream: W91 v0.1.0.
  - #### Phase 1 (~15 min): 🎯 prediction `notes/83_lane_prediction.md` (P1 how you read `fee_token`; P2 predicted
    behavior when tip20 bucket alone exceeds `gas_limit`; P3 what happens to a tx with no `fee_token`; P4 the test
    you'll fail). *Contract*: `fn select_transactions(&self, pool: &[PoolTx], gas_limit: u64) -> Vec<PoolTx>`.
    R1: never exceed `gas_limit`; R2: tip20 reservation = `tip20_reservation_pct%` of `gas_limit`, filled tip20-first;
    R3: tx with absent `fee_token` → general bucket; R4: deterministic order for equal-priority txs.
    **Output**: `notes/83_lane_design.md` R1–R4 + a pre-mortem of the over-fill case.
  - #### Phase 2 (~25 min) — file `strategy.rs` `#[cfg(test)]`, synthetic pool of 100 txs:
    - `lane_respects_gas_limit` — **(NEWBIE)** tip20 bucket alone > `gas_limit`; assert total selected gas ≤ `gas_limit`. Naive concat fails.
    - `lane_reservation_filled_tip20_first` — **(SENIOR)** mixed pool; assert the first `tip20_reservation_pct%` of gas is tip20 txs. Naive priority-only sort fails.
    - `lane_absent_fee_token_goes_general` — **(SENIOR, negative-control)** tx with `fee_token = None`; assert it lands in general, never tip20, and isn't dropped. Proves the bucketer handles the missing field.
  - #### Phase 3 (~30 min):
    - **(NEWBIE) When `lane_respects_gas_limit` reports over-fill:** Symptom: selected gas > limit. Why: concatenated both buckets without a running gas accumulator. Fix: accumulate `gas_used`, stop at `gas_limit`. Muscle: a block builder is gas-bounded — every selection respects the cap. Reapplies at: W91 lane v0.1.0.
    - **(SENIOR) When `lane_reservation_filled_tip20_first` reports general txs in the reservation:** Symptom: reservation filled by highest-tip regardless of token. Why: sorted by priority before bucketing. Fix: bucket by `fee_token` first, then fill reservation from tip20 bucket, remainder from general. Muscle: reservations are categorical, not priority-ordered. Reapplies at: W91 lane v0.1.0.
  - #### 5-year failure mode `notes/83_lane_followup.md`: when Tempo adds N fee tokens, two-bucket split doesn't generalize. Trigger: > 2 reservation classes. Migration: `Vec<ReservationClass>` weighted fill.
- [ ] Commit + log (~10 min)

> **Day-load note**: integration test ~40 + lane impl ~70 + commit ~10 ≈ 2.0 h. Light — absorbs any spillover
> from Wednesday's lane scaffold.

## Friday — Reth PR + lane edge case

- [ ] **Reth PR** — file `notes/83_reth_pr.md` with the issue link, branch, and the PR URL once opened. (~150 min)
  Output: `notes/83_reth_pr.md` exists; PR opened against `paradigmxyz/reth`; CI green or review requested. (This is
  the Half-1 Reth-spine cadence — sustained upstream contribution feeding the "crypto-infra IC bridge" fallback, §2.)
- [ ] [Tempo] **Extend (bar (b))**: `crates/tempo-payment-lane/src/strategy.rs` — handle the edge case where the tip20
  reservation is unused: give the unused reservation gas to the general bucket. (~70 min). Genuine delta over Thu
  (not a re-build): adds the reclaim path only.
  - #### Phase 2 (~25 min): `lane_unused_tip20_reservation_reclaimed` — **(SENIOR, cross-primitive)** pool with
    zero tip20 txs; assert the full `gas_limit` is available to general (reservation reclaimed), AND `lane_respects_gas_limit`
    still holds. Composes with Thu's gas-cap test; passes Thu's tests in isolation, fires on the empty-tip20 case.
  - #### Phase 3 (~25 min): **(SENIOR) When the block under-fills:** Symptom: tip20 reservation reserved but empty,
    general capped below `gas_limit`, block wastes capacity. Why: reservation gas subtracted from general's budget
    even when tip20 bucket is empty. Fix: compute the actual tip20 gas used; release `reservation - tip20_used` back
    to general. Muscle: reservations are a ceiling, not a floor — reclaim the unused remainder (the same "reservation
    = ceiling not floor" principle the §4A insurance-fund waterfall obeys). Reapplies at: W91 lane v0.1.0.
  - Output: `cargo test -p tempo-payment-lane lane_unused` green; `notes/83_lane_design.md` updated with the reclaim rule.
- [ ] Commit + log (~10 min)

## Saturday — Flashbots docs + Tempo diff against upstream + Archive descriptor port

- [ ] **Study Flashbots additional docs** — file `notes/83_flashbots_notes.md`. (~30 min)
  - 🎯 **Hardened acceptance**: `notes/83_flashbots_notes.md` lists the 3 builder-API endpoints you mirrored
    (header submit, block submit, client) and one divergence between your `validate` and mev-boost's relay check.
  - *Mental model*: reading the spec now closes the loop on Mon-Tue's Build — you confirm your trust model matches
    the production relay's, before W84 wires it into the full stack.
- [ ] [Tempo] **Diff your prototype against upstream** — read Tempo's payment-lane impl in `tempoxyz/tempo`; compare
  to your `tempo-payment-lane`; note 3 design choices that differ; for each decide port/keep/document-trade; append
  to `notes/83_lane_design.md`. (~90 min)
  - 🎯 **Hardened acceptance**: `notes/83_lane_design.md` has a "Diff vs upstream" section with exactly 3 named
    divergences, each tagged PORT / KEEP / TRADE with one-line rationale citing the upstream file+line.
- [ ] [HFT] **Port (bar (c), port tier)**: `crates/messaging-aeron/src/archive/descriptor.rs` — Aeron Archive's
  `RecordingDescriptor` schema ported verbatim (correctness + wire-compat focus). (~180 min).
  - Fields: `recordingId`, `startPosition`, `stopPosition` (with `-1` sentinel for active), `sessionId`,
    `streamId`, `initialTermId`, `segmentFileLength`, `termBufferLength`, `mtuLength`, channel info, source identity.
  - 🔧 **Why this matters**: the Archive (W83-W84) ships this week as the **off-hot-path** journal. Its compatibility
    with downstream tooling (Aeron's own client lib, ops monitoring — W105 `ops-monitoring`) depends on
    `RecordingDescriptor` field-for-field identity. Diverging here means Aeron-compatible clients can't read your
    recordings; matching the schema plugs your Archive into the Aeron ecosystem. Mirror: real-logic/aeron
    `aeron-archive-codecs.xml`. Downstream: W84 replay reads this descriptor.
  - 🎯 **Hardened acceptance**:
    - **Schema mirrors verbatim**: field names + types + the `-1` sentinel for active recordings. Cite a reth-style
      `// DIVERGE:` comment for any necessary departure (likely none expected).
    - **Bounded replay test** `descriptor_bounded_replay_exact_length`: `replay(recording_id, start_position, length=1024)`
      returns exactly 1024 bytes from that position; assert content + position monotonicity.
    - **Open-ended replay test** `descriptor_open_ended_replay_tails_active`: `replay(recording_id, start_position,
      length=NULL_VALUE)` follows `stopPosition`; works on active recordings (`stopPosition = -1`) by tailing.
    - **Cross-tool compat** `descriptor_parses_with_aeron_codec`: emit a recording, dump its descriptor with Aeron's
      `aeron-archive-tool` (if available) — should parse without error. If the tool isn't available, write a manual
      parser matching `aeron-archive-codecs.xml`.
    - **Cited URL**: https://github.com/real-logic/aeron/wiki/Aeron-Archive.
  - **Expect to hit #1 (SENIOR) — `i64 stopPosition = -1` vs `Option<u64>` Rust idiom**:
    - Symptom: rewriting `-1` as `None` in Rust loses Aeron-tool compat — Aeron's archive-tool can't parse the descriptor.
    - Why: the wire format encodes the sentinel as `i64 = -1`; an `Option<u64>` changes the on-disk bytes.
    - Fix: keep `i64` with the `-1` sentinel on the wire (in the archive index files); expose `Option<u64>` only at
      the API surface (`fn stop_position(&self) -> Option<u64>` converts the sentinel). Wire = Aeron; API = Rust.
    - Muscle: a mirror's API may be idiomatic Rust, but its wire format MUST be the source's — don't conflate the two.
      The same wire-vs-API discipline applies to the `SettlementId` newtype (§4A — representation changes without an
      API break). Reapplies at: W84 archive replay decode, W92 ledger v1.0 snapshot format, W91 tempo-tx-envelope wire compat.
  - **5-year failure mode** (`notes/83_descriptor_followup.md`): when Aeron's `RecordingDescriptor` grows new fields
    (recent additions: signal cipher, compression), the port reapplies. Trigger: upstream codec version bump.
    Migration: version-tagged descriptor format.
  - Output: `cargo test -p messaging-aeron descriptor_` green (bounded + open-ended + cross-tool); descriptor schema
    matches `aeron-archive-codecs.xml` field-for-field.
- [ ] Commit notes (~10 min)

> **Day-load note**: Flashbots ~30 + lane diff ~90 + descriptor port ~180 + commit ~10 ≈ 5.2 h. Under the ceiling.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
