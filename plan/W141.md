# Week 141 — Public artifact update: `perp-dex-core` + the bet (v3 capstone-facing retro)

> **Phase**: 7 (bet placement / bridge)
> **Month**: M36
> **Track**: (career — destination landing at the bet/bridge) + the retained `perp-dex-core` public-artifact surface (Half-2 venue, §2)
> **Hours target**: 30h/wk (W137-W144 window)
> **Mirror target**: (n/a — settling/landing week; no upstream module mirrored. The artifacts referenced — `perp-dex-core` v1.0 cluster-VOPR, the cross-margin/liquidation engine, the hybrid-settle interface — were built and tagged W97-W117 and are not re-implemented here.)
> **Crate(s) touched**: (none new — maintenance only on the multi-node `perp-dex-core` operations rig; no crate version bumps. v1.5 RWA/ADL work, if live, is the W118-W119 track, not this week.)
> **Bar**: (n/a) — bet/landing week, no Build exercise (per SPEC §6: depth ≠ padding; do not manufacture a fake 3-phase Build on a settling week). The bar-(c) claims this week *cites* (cluster-VOPR green, per-mark-tick p99.99 at 100k accounts, tripwire-never-hit) were earned at W99/W102/W108/W113, not this week.
> **Feeds into**: W142 first quarterly-review prep (the public artifact's "what `perp-dex-core` v1.0 proves" framing is the raw material for the manager/founder 1:1 self-score and the vesting-milestone check); W144 M36 retrospective + plan close (this public post is the externally-facing twin of W144's private retro; its draft surfaces the metric gaps W144 Tue tallies, and the runtime tally tracked Wed/Sat rolls into the 4000-hour milestone the W144 close audits, alongside the 7-system coverage ≥85 close metric); the Bet #2 thesis seeds (W139, W144) — a clean public capstone artifact is the reputation input that raises the allocation tier of the *next* shot (§7 "each bet upgrades the next"); the destination-landing arc W134 start-at-bet → W135-W138 first month → W139-W143 settling, of which this is week 7-8 at the new venue.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): `RECONCILED_PLAN_v3.md` §4A (the v1.0 lock — the exact claims the public artifact may make and must NOT over-claim: 3-node VSR / order-log-is-VSR-log, const 30% netting haircut, fee-accrued fund + waterfall + tripwire-never-hit, `SettlementId`(snapshot_epoch, op_number, checksum), two projection staleness contracts, 100k-account incremental-margin hot path, RWA-aware *seams* not features), §6 (the ecosystem checklist — what a Bet #2 conversation screens for), §7 (shots-on-goal: reputation → next-tier allocation); `progress.md` (W1→now weekly logs + runtime tally — the source for every sourced claim; never write from memory); the prior public posts (Blog #4 W109 cluster-VOPR, #5 W112 cross-margin/liquidation, #6 W115 hybrid CLOB, #7/flagship W117/W121 — for voice consistency and to avoid re-publishing material already public); your venue's external-communications / confidentiality + token-disclosure policy (Monday — re-read before publishing anything public from inside a pre-TGE seat: strategy, cap-table, FDV, unvested-allocation, and counterparty details are all live wires); `chaos_log.md` (Saturday chaos-drill rotation pointer + the cluster-VOPR drill history from W108).
> **v3 modifications**: (1) **Deliverable re-aimed to the INDEX v3 W141 spec — "Public artifact update (`perp-dex-core` + the bet)."** v2 framed this week as a generic "from self-directed plan to destination *job*" retrospective. v3 makes the public artifact specifically about the **capstone** (`perp-dex-core` v1.0 — the replicated multi-node hybrid CLOB whose centerpiece is the cross-margin + liquidation engine) **and the bet** (founding/core-eng on an on-chain-derivatives venue), not a job landing. (2) **Confidentiality boundary upgraded from "HFT-seat NDA" to "pre-TGE token-equity seat."** The scrub now also covers token/cap-table/FDV/unvested-allocation disclosure, not just strategy — a different and stricter wire than v2's HFT-IC framing. (3) **The artifact's claims are bounded by the §4A v1.0 lock** — it may say "RWA-aware *seams*" and must NOT claim shipped RWA *features* (those are the W118-W119 v1.5 track); it may cite cluster-VOPR-proven invariants but must not over-state v1.5/v2 capabilities. (4) **Runtime tally re-pointed at the multi-node `perp-dex-core` ops rig** (3-node VSR cluster), not a single-process HFT rig, toward the W144 4000-hour milestone.

This is a bet/landing week, not a Build week. There is no new crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding (SPEC §6 light-week rule). Every item below instead carries an explicit artifact file + an Output line that proves it done, and the writing/judgment items (Monday artifact draft, Friday publish decision, Saturday rig recall) carry a hardened-acceptance pass criterion and a mental model of why the item exists now. The week's job: ship a **public artifact update on `perp-dex-core` v1.0 and the bet** — the externally-facing twin of next week's private W144 retro and a reputation input for Bet #2 — while keeping the pre-TGE confidentiality boundary intact and the multi-node operations rig's uptime unbroken toward the W144 4000-hour milestone. Personal/rig time is ≤4 hrs/wk now (the W138 new-normal at 30h/wk), so the rig touchpoints are touchpoint-sized, not project-sized.

---

**Monday — Public artifact draft: "What `perp-dex-core` v1.0 proved (and where the bet goes)"**

- [ ] Artifact: `blog/08_perp_dex_core_and_the_bet.md` (public draft). Write the capstone-facing artifact update closing the plan's public arc. Structure: (1) **the engine** — `perp-dex-core` v1.0 is a 3-node, fault-tolerant, multi-node hybrid CLOB where *the order/event log IS the VSR log* (one hot-path log, no second consensus); the centerpiece is the real-time cross-margin + liquidation engine. (2) **what it proved** — the §4A v1.0 acceptance, stated as *falsifiable* claims: multi-instrument cross-margin nets BTC-PERP + ETH-PERP crediting only 70% of the offset (const 30% netting haircut); 3-node VSR survives single-node crash/restart with no lost commits (cluster-VOPR'd); partial-liq against book + fee-accrued insurance fund (10% taker, declared param) absorbs the below-bankruptcy shortfall via the waterfall, **tripwire never hit** across explored schedules; `SettlementId`(snapshot_epoch, op_number, checksum) = the VSR op-number with no side mapping table (free idempotency); two projection staleness contracts (control/risk = read-after-commit from local applied state; analytics = bounded-staleness + op-token); per-mark-tick p99.99 budget met at ~100k accounts via the **incremental-margin hot path**. (3) **the architecture edge** — RWA-aware *seams* (abstract oracle iface, parameterized funding, instrument abstraction) present from v1.0 so RWA-perps slot in without a rewrite; this is the moat narrative, not a shipped feature. (4) **the bet** — why on-chain derivatives infra, why a founding/core-eng token-equity bet over a job, and the shots-on-goal frame (Bet #1 placed or bridge engaged; reputation compounds into Bet #2). Anonymize the *current venue* absolutely. (~35 min)
  - 🎯 Hardened acceptance: `blog/08_perp_dex_core_and_the_bet.md` exists with all four sections present and **at least one concrete, sourced claim per "what it proved" bullet** — a number or invariant pulled from `progress.md` / the W99/W102/W108/W113 tags / the `latency-lab` report, not a vibe ("it's fast" fails; "per-mark-tick p99.99 under budget at 100k accounts" with the figure passes). A claim-discipline pass confirms ZERO claims that exceed the §4A v1.0 lock: no "ships RWA-perps" (it's seams, v1.5 is W118-119), no "5-node / geo-distributed" (that's v1.5/v2), no "rolling empirical correlation" (it's the const 30% haircut). A confidentiality pass confirms ZERO mentions of (a) the current venue's name / city / country, (b) its strategy, cap table, backers, or FDV, (c) any token figure or unvested-allocation detail, (d) compensation. A post that over-claims past v1.0, or leaks one identifying/pre-TGE-sensitive detail, fails — reject it.
  - *Mental model*: this is the public-facing twin of next week's private W144 retrospective, and drafting it FIRST forces you to decide what `perp-dex-core` v1.0 *actually proved* — which is exactly the framing W142's self-score and W144's metric tally reuse, AND the reputation artifact that a Bet #2 conversation will read (§7: each bet upgrades the next, and a clean public capstone is what raises the next allocation tier). Two scrubs run at once and they are different wires: the **claim-discipline scrub** keeps the artifact *honest to the §4A lock* (over-claiming RWA features or geo-distribution is a credibility hole a founding-eng interviewer will find in 30 seconds — see the W74 "where does funding/liquidation live, and why not here?" probe); the **confidentiality scrub** keeps you inside a *pre-TGE token seat* where leaking the venue's FDV, backers, or your unvested allocation can breach a disclosure clause — a job-safety check, not a style note. Drafting from `progress.md` and the W99/W102/W108/W113 tags (not memory) is the same evidence-over-vibe discipline the whole plan ran on.
  - [HFT] Monday touchpoint (~15 min): review weekend runtime alerts on the multi-node `perp-dex-core` ops rig — any per-mark-tick p99.99 regressions, VSR view-changes / leader flaps, replica-divergence (StateChecker) events, insurance-fund-tripwire near-misses, or disk-fill warnings on a cluster node? Log the answer (even "none — 3/3 replicas healthy, leader stable") in `chaos_log.md` so the W144 audit has an unbroken trail.
  - Output: `blog/08_perp_dex_core_and_the_bet.md` exists meeting the hardened-acceptance criteria; `chaos_log.md` has a dated W141-Mon alert-review line naming cluster health (replica count + leader stability).
- [ ] Commit (draft) (`blog/08_perp_dex_core_and_the_bet.md` — mark `[DRAFT]` in the file header, not yet published). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday artifact-draft commit.

**Tuesday — Bet / day-job work**

- [ ] Artifact: append to `work/day_log.md` (private). Day-job substantive work at the bet/bridge — capture the one decision or deliverable that mattered today (a piece of the core engine you were brought in to build, per the W134-W136 ramp) in a single line so the running log stays useful for the W142 1:1 and the W144 retro. (~15 min)
  - *Mental model*: a one-line-per-day work log costs nothing and is the only durable record of "what did I actually ship at the venue?" — the question W142's quarterly self-score and the W144 retrospective both answer, and the same evidence a vesting-milestone or Bet #2 conversation leans on. Without it the firm-month blurs and the self-assessment becomes guesswork.
  - Output: `work/day_log.md` has a dated W141-Tue line.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday commit.

**Wednesday — Bet / day-job work + rig dashboard check**

- [ ] Artifact: update the HFT/rig row in `progress.md` (private). Day-job work, plus the [HFT] Wednesday touchpoint: check the multi-node `perp-dex-core` operations dashboards, capture the cumulative runtime-hours total (per-cluster, counted as cluster-up time), and update the `progress.md` rig row. Append today's one-line work note to `work/day_log.md`. (~30 min)
  - 🎯 Hardened acceptance: `progress.md` rig row carries a W141 runtime-hours figure that is monotonically ≥ the last recorded total (a non-increasing tally means a cluster node went down unnoticed — investigate before committing); the figure is on a credible glide path toward the W144 4000-hour milestone (≈3930h by W141 if the cluster has run continuously since the W138 ≈3800h mark); the row also notes replica health (3/3 or a flagged degradation), because a 2/3 cluster that is "still up" is a quorum risk the W144 audit must see.
  - *Mental model*: the 4000-runtime-hours bar-(a) milestone at W144 is not earned in the last week — it's earned by the tally never stalling and the cluster never silently dropping below quorum. A Wednesday glance that catches a dead replica or a silent stall now is the difference between a credible "≥4000 runtime hours, 3-node VSR, cluster-VOPR'd" claim at the close and discovering a multi-hundred-hour gap (or a degraded cluster) at the W144 audit. Cheap insurance on a number that took ~44 weeks to accumulate and on a claim the public artifact (Mon) and the §4A bar-(a) definition both rest on.
  - Output: `progress.md` rig row updated with the W141 runtime tally + replica health; `work/day_log.md` has a dated W141-Wed line.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday commit.

**Thursday — Bet / day-job work + Bet #2 thesis nibble**

- [ ] Artifact: append to `work/day_log.md` (private) + a 2-line nibble to `bets/bet2_thesis.md` (private). Day-job substantive work; capture today's one-line decision/deliverable. Then spend ~10 min on the Bet #2 thesis seed (started W139): note one ecosystem/venue observation from this week (a §6-checklist signal — founding-tier allocation seen, tier-1 backer spotted, a core-criticality role, a remote-native team) that the public artifact (Mon) might warm. (~20 min)
  - 🎯 Hardened acceptance: `work/day_log.md` has a dated W141-Thu line; `bets/bet2_thesis.md` gains ≥1 dated observation tagged with which §6 checklist item it touches (e.g. `[§6.2 tier-1 backing]`) — a generic "crypto is interesting" note without a checklist tag fails, because Bet #2 is screened by §6 just like Bet #1, not by vibes.
  - *Mental model*: the same running-log discipline as Tuesday, plus the §7 "each bet upgrades the next" mechanic — Bet #2's higher allocation tier is *sourced now* by tagging real signals against the §6 checklist while you're inside an active venue with a live network, not reconstructed cold later. The public artifact you draft this week is one of the warm-up inputs for exactly those conversations.
  - Output: `work/day_log.md` has a dated W141-Thu line; `bets/bet2_thesis.md` has a dated, §6-tagged observation.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday commit.

**Friday — Bet / day-job work + artifact polish & publish decision**

- [ ] Artifact: finalize `blog/08_perp_dex_core_and_the_bet.md` and record a publish/hold decision in its header (public). Day-job work, then a second editing pass on Monday's draft: tighten the four sections, run BOTH scrubs one more time (claim-discipline against the §4A lock + confidentiality against the pre-TGE/venue boundary), and decide publish-now vs hold-until-after-W142-1:1 — record the decision + one-line reason at the top of the file. If publishing, post it and capture the live URL; if holding, write the gate condition that releases it. Append today's one-line work note to `work/day_log.md`. (~30 min)
  - 🎯 Hardened acceptance: the file header carries `Status: published <URL>` OR `Status: hold — <gate condition>` (no ambiguous "maybe later"); if published, BOTH final scrubs are logged as done (`Claim-discipline-scrub: pass <date>` AND `Confidentiality-scrub: pass <date>` one-line notes) — publishing a pre-TGE-seat post without both logged final scrubs is the one unrecoverable mistake of the week (a leaked allocation/FDV detail or an over-claim that a founder later calls out both cost more than the post is worth). If holding, the gate condition is falsifiable (e.g. "release after the W142 manager 1:1 confirms no disclosure conflict"), not "later".
  - *Mental model*: separating "draft" (Monday) from "publish decision" (Friday) puts a 4-day cooling gap between writing and going public, which is exactly when you catch the over-claim or the identifying detail you were too close to see on Monday. Tying the hold-option to the W142 1:1 keeps the post from front-running a confidentiality/disclosure conversation you haven't had with your new venue yet — doubly important in a pre-TGE seat where what's publishable shifts with the venue's own disclosure timeline. A recorded gate condition (not a vague "later") is what makes the hold a decision instead of avoidance. Two logged scrubs (not one) because honesty-to-the-lock and confidentiality are independent failure modes — passing one does not imply the other.
  - Output: `blog/08_perp_dex_core_and_the_bet.md` header shows a published-URL or hold-with-gate status meeting the hardened-acceptance criteria; `work/day_log.md` has a dated W141-Fri line.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday commit.

**Saturday — Personal / rig: cluster uptime + cluster-VOPR recall**

- [ ] Artifact: append to `chaos_log.md` (private) + update the rig row in `progress.md`. [HFT] Saturday touchpoint — confirm the multi-node `perp-dex-core` ops rig is on track: capture the cumulative runtime tally (≥3900h, on track for 4000 by M36 end) and perform this week's small chaos drill from the **cluster-VOPR-adjacent rotation** (node process-kill → view-change recovery / network partition → quorum hold / clock skew / disk fill on one node / slow projection subscriber → bounded-staleness check; pick the next one not run in the prior weeks per `chaos_log.md`). Record: which fault, expected vs observed recovery behavior (leader re-elected? quorum held? no committed-op lost? StateChecker still convergent? insurance-fund tripwire untouched?), any divergence. Keep it touchpoint-sized — personal time is ≤4 hrs/wk now. (~30 min)
  - 🎯 Hardened acceptance: `chaos_log.md` gains a dated W141 entry naming the specific fault injected (rotated — not a repeat of last week's), with expected-vs-observed recovery recorded and any divergence flagged not silently dropped; the entry confirms the bar-(c) cluster invariants the public artifact (Mon) claims still hold under the injected fault (no lost commits + replica convergence + tripwire-never-hit); `progress.md` runtime tally is ≥ Wednesday's figure and credibly ≥3900h with replica health noted.
  - *Mental model*: the chaos drill is what keeps the artifact's bar-(c) claims ("3-node VSR survives single-node crash with no lost commits"; "tripwire never hit") *true* after the cluster has run for weeks — you are publishing those claims this week, so a Saturday drill that re-confirms them is the difference between an honest artifact and a stale one. Rotating the fault each week means that by W144 every cluster failure class has been re-exercised — a claim the M36 final audit (W144 Tue) checks. Skipping it because "the cluster's been fine" is exactly how a latent recovery or convergence bug survives to the close. This is the same single-writer / deterministic-recovery / cluster-VOPR discipline the W74 VSR-replicated matching engine, the W108 cluster-VOPR drill, and the W113 `perp-dex-core` v1.0 ship were built on — reused as a weekly heartbeat.
  - Output: `chaos_log.md` has a dated W141 cluster chaos-drill entry; `progress.md` runtime tally updated and ≥3900h with replica health.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
