# Week 14 — EIP deep dives + [NEW] `bufpool` v1.0 + medium Alloy PRs

> **Phase**: 2
> **Month**: M4
> **Track**: Multi (Reth `eth-eips` bar (b) + Layer-1 substrate `bufpool` bar (c))
> **Hours target**: 30h/wk (M1-M18)
> **Mirror target**: `alloy-eips` (eip1559 / eip4844 / eip7702 split, `BaseFeeParams`, `BlobTransactionSidecar`, `SignedAuthorization`); `bufpool` mirrors reth's `revm`-backed page cache + Erigon's buffer pool + the LRU-K policy of PostgreSQL's clock-sweep replacement, with Chronicle-Queue off-heap discipline.
> **Crate(s) touched**: **[NEW] created**: `crates/eth-eips/` (v0.1.0 Friday); **[NEW] `bufpool` v0.5→v1.0** ships Saturday (bar (c)); refactors `crates/eth-consensus/` and `crates/eth-storage-cache/` (→ v0.2.0, bufpool-backed).
> **Bar**: (c) for `bufpool` (Layer-1 substrate — static-memory + zero-alloc hot path + VOPR-style loom required); (b) for `eth-eips` (Reth data-type — fuzz + property + criterion).
> **Feeds into**: `wal` (W26 drains the dirty set on group-commit; pins WAL frames), `recovery` (W30), `storage-trie` (W31 page access via `PinnedPage`), `mmap-queue` (W79 page memory), `txn` (W42 MVCC frame pins), `mini-db` (W95/W100), `vector-db` (W104). First "primitive once" wire-up: `eth-storage-cache::Page` (W2) stops owning its memory and borrows from `bufpool::PinnedPage`. `eth-eips` feeds W15 (eip2 split + EOF), W16 rpc-types, exec-vm blob handling.
> **References** (consult when stuck — not scheduled as cover-to-cover): EIP-1559 spec + Paradigm base-fee analysis; EIP-4844 + proto-danksharding FAQ §"fee market"; EIP-7702 spec §"authorization tuple"; O'Neil/O'Neil/Weikum 1993 *The LRU-K Page Replacement Algorithm* (§3 backward-K-distance only); Maged Michael 2004 *Hazard Pointers* (8pp, §4 only); `haphazard` + `crossbeam_epoch` source (Phase-3 COMPARE only); Chronicle-Queue OpenHFT README + Peter Lawrey off-heap blog; TigerStyle "Primary Colors" framework.
> **v2 modifications**: none (W1-W14 unchanged from v1).

---

**Monday — Hazard pointers (concurrency drill #19) + EIP-1559 re-read**

- [ ] **Concurrency drill #19 of 28 — hazard pointers on a Treiber stack (Maged Michael 2004)** — `crates/concurrent/src/treiber.rs` + `crates/concurrent/src/hazard.rs`. (solo, ~total 250 min across 3 phases = 70+80+100)
  - 🔧 **Why this matters**: hazard pointers are the fundamental memory-reclamation pattern for lock-free
    structures — mirrors `haphazard`/`folly::HazardPointers`. Without them (or EBR, drill #23) a CAS-based
    `pop` can free a node another thread is about to dereference: ABA in disguise. Three traps: (1) protect
    AFTER load instead of protect-then-reload-verify; (2) retire-without-scan leaks; (3) scan reads a slot
    racily. First-attempt impls fail the loom UAF test below — that debug session IS the lesson. Downstream:
    W33 EBR (the alternative this compares against), W37 skiplist tower retirement, W42 deadlock-detector
    wait-for-graph nodes, and **W14 pin.rs sentinel (Friday)** silently requires reclamation discipline (the
    CAS-to-sentinel in `pin.rs` is the same protect-then-verify shape).

  #### Phase 1 — Problem, prediction, design (~70 min, no code yet)
  - 🎯 **Pre-prediction prompt** — sealed answers in `crates/concurrent/notes/14_drill19_prediction.md`:
    - P1. The primitive you'd reach for to publish/free the node (exact import: `core::sync::atomic::AtomicPtr`
      + your retire list type).
    - P2. Predicted `pop` latency overhead from one `protect` (predict ns; reality after bench).
    - P3. Predicted outcome if two threads `pop` the same head concurrently WITHOUT protection.
    - P4. Predicted leak count if you `retire` but never `scan_and_reclaim`.
    - P5. **List every Phase-2 test you predict your first attempt will fail.** Compare after green.
  - *Mental model*: a hazard pointer is a per-thread published "I am about to dereference this address" slot;
    a reclaiming thread refuses to free any address currently published. It trades a store + scan for the
    right to free safely.
  - *Contract*: `TreiberStack<T> { head: AtomicPtr<Node<T>> }` with `push(&self, v: T)` / `pop(&self) ->
    Option<T>`; `HazardRegistry` with `protect(&self, p: *mut Node<T>) -> HazardGuard`, `retire(&self, p)`,
    `scan_and_reclaim(&self)`. NEVER write the protect-loop body in this doc.
  - *Back-of-envelope*: `protect` = 1 release store to a per-thread slot in L1 (~0.5ns) + a re-load + compare
    (~1ns). `scan_and_reclaim` walks ≤ `num_threads` slots (≤64 × L1 ≈ 32ns) per retired batch. UAF cost if
    you skip it: undefined behavior — unbounded, a crash or silent corruption, not "slow".
  - **Requirements** R1..R6 in `crates/concurrent/notes/14_drill19_design.md`:
    - R1. `pop` never returns a node freed by a concurrent `pop`.
    - R2. No node is freed while any thread's hazard slot points at it.
    - R3. Every retired node is EVENTUALLY freed (no permanent leak).
    - R4. `protect` uses protect→reload-head→verify-unchanged→retry (publish before deref, re-validate after).
    - R5. `retire` appends to a per-thread retire list; `scan_and_reclaim` frees only addresses absent from
      all slots.
    - R6. Build the UNPROTECTED Treiber first and prove UAF under loom, THEN add protection.
  - **Design walk-through** D1..D4:
    - D1. Property table: raw `AtomicPtr` (Sync? yes; reclaim? no), `Arc` (reclaim? yes but atomic refcount
      contention + no ABA fix for stack), hazard pointer (per-thread slot, scan cost), EBR (epoch fence).
    - D2. **Pre-mortem (load-bearing):** write the exact 5-line interleaving: T1 reads `head=A`, T2 pops A and
      frees it, T1 dereferences `A->next` → UAF. Show where the hazard publish must sit to block it.
    - D3. **Anti-shortcut audit:** "just wrap nodes in `Arc`" → defeats the lock-free property (refcount CAS
      contention) and still ABA-races on the stack head; "free immediately in pop" → R2 violation.
    - D4. Predict the scan-frequency tradeoff: scan-too-rare = retire list grows (drill #20 measures this);
      scan-too-frequent = scan cost dominates pop.
  - **Output**: `notes/14_drill19_design.md` with R1..R6, D1..D4, the UAF interleaving sketch.

  #### Phase 2 — Write the failing tests (~80 min)
  - [ ] `treiber_unprotected_loom_uaf` *(NEWBIE — proves the problem exists)* — `#[cfg(loom)]`, 2 threads each
    push then pop the same node; a `Drop` impl flips a per-node `freed: AtomicBool` and a deref checks it.
    WITHOUT protection this MUST report a use-after-free poison; this test is RED until you build the
    protected version and gate the unprotected one as the negative control.
    ```rust
    #[cfg(loom)]
    #[test]
    fn treiber_unprotected_loom_uaf() {
        loom::model(|| {
            let s = Arc::new(UnprotectedTreiber::new());
            // 2 threads push+pop; Node::drop sets freed=true; pop's deref asserts !freed.
            // EXPECT: loom finds an interleaving where a popped node is dereferenced after free.
            assert!(loom_found_uaf(&s), "unprotected stack must exhibit UAF — if not, the test is too weak");
        });
    }
    ```
  - [ ] `treiber_protected_loom_no_uaf_no_double_free` *(SENIOR — cross-primitive: stack + hazard registry)*
    — same 2-thread loom workload on the PROTECTED stack; assert each pushed value popped exactly once AND
    each node freed exactly once after retirement (drop-counter == push-count at quiescence).
  - [ ] `hazard_protect_blocks_reclaim` *(SENIOR — anti-shortcut: protect-then-verify, not protect-after)* —
    T1 protects node A then yields; T2 retires A and calls `scan_and_reclaim`; assert A is NOT freed while
    T1's guard lives, IS freed after the guard drops + a second scan.
  - [ ] `hazard_retire_without_scan_leaks_then_scan_drains` *(NEWBIE)* — retire 100 nodes with no scan; assert
    retire-list len == 100 (proves no premature free); call `scan_and_reclaim`; assert freed == 100.
  - [ ] `treiber_loom_takes_real_time` *(negative-control "test-the-test")* — assert the loom model explored
    > 1 interleaving (`loom` iteration count > 1) and wall-time ≥ 100ms, so a no-op loom harness fails fast
    rather than passing vacuously.

  #### Phase 3 — Make the tests pass, one failure at a time (~100 min)
  - **(NEWBIE) When `treiber_unprotected_loom_uaf` does NOT report a UAF:**
    - Symptom: loom passes the unprotected stack — green where it must be red.
    - Why: your `Node::drop` poison or the deref check is missing, so loom can't observe the violation.
    - Fix: add `freed: AtomicBool` set in `Drop`; have `pop` assert `!freed.load(Acquire)` immediately after
      reading `head`. Now loom finds the free-then-deref order.
    - Muscle: a safety test must be able to FAIL on the broken impl — instrument the violation before you fix
      it. Reapplies at: W26 wal torn-write detector, W33 EBR drop-counter, W42 txn lost-update probe.
  - **(SENIOR) When `treiber_protected_loom_no_uaf_no_double_free` double-frees:**
    - Symptom: drop-counter > push-count; two threads retire+scan the same node and both free it.
    - Why: `scan_and_reclaim` lacks a "claim" step — both scanners see A absent from all slots and free it.
    - Fix: retire lists are PER-THREAD; only the owning thread frees its own retired nodes. A node is retired
      by exactly one thread (the popper that unlinked it).
    - Muscle: reclamation ownership is single-writer — the thread that unlinks owns the free. Reapplies at:
      W26 wal segment unlink, W37 epoch-gc, W42 lock release, **W14 pin.rs sentinel** (one evictor owns the frame).
  - **(EXPERT) When `hazard_protect_blocks_reclaim` frees a protected node:**
    - Symptom: T1's guard is live but T2 frees A; T1 derefs garbage.
    - Why: T1 protected A AFTER reading head; between read and publish, A was already unlinked — protect-after
      is too late.
    - Fix: protect→reload head→verify still == A→only then deref; if changed, retry. Publish-before-deref +
      re-validate.
    - Muscle: hazard protection must publish BEFORE the deref and re-validate the pointer didn't change.
      Reapplies at: W33 EBR pin-then-load, W37 skiplist hand-over-hand, **W14 pin.rs CAS-pin-then-recheck-gen**.
  - **5-year failure mode** (`notes/14_drill19_followup.md`): hazard pointers don't scale to structures with
    many live internal pointers (each needs a slot). Trigger: W37 skiplist tower walk needs per-level
    protection. Migration: switch to EBR (drill #23), which amortizes per-epoch instead of per-pointer.

- [ ] Re-read EIP-1559 base-fee mechanism + Paradigm analysis (lookup, not cover-to-cover). (~45 min)
- [ ] Commit + log (Monday). (~10 min)

**Tuesday — Extract `eth-eips/eip1559` + EIP-4844 (blobs) deep + KZG + extract `eth-eips/eip4844` + Alloy scan**

> Day-load note: the EIP-1559 Refactor (~100 min) moved here from Monday so Monday is drill #19 +
> rereads (~5.1h); Tuesday holds the two fee-math extractions it naturally belongs with plus the Alloy
> issue scan (moved off the overloaded Thursday so the claim precedes Friday's change). Tuesday = eip1559
> (100) + chain-test (30) + eip4844 read (45) + eip4844 (100) + alloy scan (40) + commit (10) = 325 min (~5.4h).

- [ ] **Refactor**: `crates/eth-eips/src/eip1559.rs` — move base-fee math out of `eth-consensus`; add
  `BaseFeeParams` for chain-specific overrides (Optimism/Base elasticity + denominator). (~total 100 min across 3 phases = 25+25+50)
  - 🔧 **Why this matters**: mirrors `alloy-eips::eip1559`. `calc_next_base_fee` is consensus-critical — one
    off-by-one in the rounding or a hardcoded 8/0.125 denominator splits the chain on any L2 with custom
    params. Three traps: integer-division rounding direction, the gas-target-vs-gas-used sign, and
    chain-param hardcoding. Downstream: W15 eip2 split, W16 rpc-types `feeHistory`, exec-vm block env.

  #### Phase 1 — Problem, prediction, design (~25 min, no code yet)
  - 🎯 **Pre-prediction prompt** — `crates/eth-eips/notes/14_eip1559_prediction.md`:
    - P1. Exact signature you'd reach for: `fn calc_next_base_fee(parent_gas_used, parent_gas_limit,
      parent_base_fee, params: BaseFeeParams) -> u64`.
    - P2. Predicted result when `parent_gas_used == gas_target` exactly (should be unchanged).
    - P3. Predicted rounding direction when usage is below target (delta floors vs ceils).
    - P4. Predict whether mainnet and Base produce the same next-base-fee for identical input (they must NOT —
      different denominators).
    - P5. List the Phase-2 tests you predict your first attempt fails.
  - *Mental model*: base fee is a PID-free feedback controller — it moves toward the gas target by at most
    1/denominator per block. `BaseFeeParams` parameterizes elasticity + change-denominator per chain.
  - *Contract*: `struct BaseFeeParams { max_change_denominator: u64, elasticity_multiplier: u64 }` with
    `BaseFeeParams::ethereum()`, `::optimism()`, `::base()`; the calc fn above. No body in the doc.
  - **Requirements** R1..R5 in `notes/14_eip1559_design.md`:
    - R1. `gas_target = parent_gas_limit / elasticity_multiplier`.
    - R2. used == target → base fee UNCHANGED.
    - R3. used > target → increase by `max(1, base * delta / target / denominator)` (floor, min +1 wei).
    - R4. used < target → decrease by `base * delta / target / denominator` (floor toward zero, never below 0).
    - R5. params injected, never hardcoded; `ethereum()`=denom 8/elast 2, L2 variants differ.
  - **Design walk-through** D1..D2:
    - D1. Walk a block exactly at target, one 2× over, one 50% under — hand-compute next base fee for mainnet
      AND Base.
    - D2. **Pre-mortem:** contributor hardcodes denominator 8. What test catches? (the Base vector.)
  - **Output**: `notes/14_eip1559_design.md` with R1..R5, D1..D2.

  #### Phase 2 — Write the failing tests (~25 min)
  - [ ] `base_fee_unchanged_at_target` *(NEWBIE)* — used == target → output == input. Exercises R2.
  - [ ] `base_fee_increase_min_one_wei` *(SENIOR — anti-shortcut against pure float math)* — tiny overage on a
    small base fee → integer formula yields +1 wei, never 0; a float impl that truncates to 0 fails.
    ```rust
    #[test]
    fn base_fee_increase_min_one_wei() {
        // base=7, used=target+1: float delta rounds to 0; spec mandates min +1.
        assert_eq!(calc_next_base_fee(target+1, limit, 7, BaseFeeParams::ethereum()), 8);
    }
    ```
  - [ ] `base_fee_chain_params_diverge` *(SENIOR — cross-primitive: same fn, two param sets)* — identical
    input through `ethereum()` vs `base()` produces DIFFERENT next base fee. Catches hardcoded denominator.
  - [ ] `base_fee_decrease_floors_not_below_zero` *(NEWBIE)* — used == 0 → never panics, never wraps below 0.
  - [ ] `base_fee_mainnet_block_15_000_000_vector` *(negative-control — real fixture)* — pull the real
    parent header; computed next base fee == the actual child header's base fee, proving the vector is live.

  #### Phase 3 — Make the tests pass, one failure at a time (~50 min)
  - **(NEWBIE) When `base_fee_increase_min_one_wei` returns the input unchanged:**
    - Symptom: small overage produces no increase; chain stalls at low fees.
    - Why: `base * delta / target / denominator` floors to 0 for small base; you dropped the `max(1, …)`.
    - Fix: increase = `max(1, base * delta / target / denominator)`. Spec mandates a minimum +1 wei bump.
    - Muscle: integer fee formulas need an explicit minimum step or feedback dies in rounding. Reapplies at:
      W15 blob base fee, exec-vm gas refund floor.
  - **(SENIOR) When `base_fee_chain_params_diverge` shows identical outputs:**
    - Symptom: mainnet and Base agree where they must differ; L2 blocks would be rejected.
    - Why: denominator/elasticity hardcoded as `8`/`2` instead of read from `params`.
    - Fix: thread `BaseFeeParams` through; `gas_target = limit / params.elasticity_multiplier`, divide by
      `params.max_change_denominator`.
    - Muscle: any consensus constant that varies per chain is a parameter, never a literal. Reapplies at: W15
      eip2 chain config, W31 storage layout versioning, W91 consensus-engine fork config.
  - **5-year failure mode** (`notes/14_eip1559_followup.md`): if EIP-4844-style multi-dimensional fee markets
    generalize (per-resource base fees), `BaseFeeParams` grows a resource dimension. Trigger: W15 blob fee
    lands; Migration: make the calc generic over a `FeeResource` enum.
- [ ] Test `eip1559` against mainnet, Optimism, Base genesis params. (~30 min)
  - 🎯 Hardened acceptance: `cargo test -p eth-eips eip1559_` green incl. all 3 chain vectors; `base_fee_chain_params_diverge` proves Base ≠ mainnet.

- [ ] Read EIP-4844 fee-market section + proto-danksharding FAQ (lookup-when-stuck, not cover-to-cover). (~45 min)
  - 🎯 Hardened acceptance: in `notes/14_eip4844_design.md`, you can state from memory the blob-gas update rule
    (`excess_blob_gas` accumulation) and why `fake_exponential` is used instead of float exp.
- [ ] **Refactor**: `crates/eth-eips/src/eip4844.rs` — move blob fee math out of `eth-consensus`; add
  `BlobTransactionSidecar { blobs, commitments, proofs }`; KZG verification as a trait `KzgVerifier`
  (placeholder impl Tuesday, real `c-kzg` wiring deferred to W15). (~total 100 min across 3 phases = 25+25+50)
  - 🔧 **Why this matters**: mirrors `alloy-eips::eip4844`. `calc_blob_fee` uses `fake_exponential` — a
    fixed-point approximation of `e^x`; a float impl diverges from consensus. Three traps: the `excess_blob_gas`
    accumulator update direction, `fake_exponential` factor/numerator/denominator ordering, and sidecar
    length-coupling (blobs.len() == commitments.len() == proofs.len()). Downstream: W15 real KZG, W16
    `eth_blobBaseFee` RPC, exec-vm blob-carrying tx validation.

  #### Phase 1 — Problem, prediction, design (~25 min)
  - 🎯 **Pre-prediction prompt** — `notes/14_eip4844_prediction.md`:
    - P1. Primitive for the exponential: `fn fake_exponential(factor: u64, numerator: u64, denominator: u64)
      -> u64` (integer Taylor series), NOT `f64::exp`.
    - P2. Predict `calc_blob_fee(excess_blob_gas=0)` (should be `MIN_BLOB_BASE_FEE`).
    - P3. Predict whether a float `e^x` impl matches the integer vector at large `excess_blob_gas` (it won't —
      rounding drift).
    - P4. Predict what `BlobTransactionSidecar::validate` returns when `blobs.len() != proofs.len()`.
    - P5. List Phase-2 tests your first attempt fails.
  - *Mental model*: blob base fee is an exponential controller on `excess_blob_gas`; `fake_exponential` keeps
    it deterministic across all nodes by avoiding floating point entirely.
  - *Contract*: `fn calc_blob_fee(excess_blob_gas: u64) -> u128`; `fn fake_exponential(f,n,d) -> u128`;
    `struct BlobTransactionSidecar { blobs: Vec<Blob>, commitments: Vec<Commitment>, proofs: Vec<Proof> }`
    with `validate(&self, v: &dyn KzgVerifier) -> Result<(), BlobError>`. No bodies in the doc.
  - **Requirements** R1..R5 in `notes/14_eip4844_design.md`:
    - R1. `fake_exponential` is an integer Taylor accumulation; output `u128` (no float anywhere).
    - R2. `calc_blob_fee(0) == MIN_BLOB_BASE_FEE` (1 wei).
    - R3. `excess_blob_gas` accumulates: `max(0, parent_excess + parent_blob_gas_used - TARGET)`.
    - R4. Sidecar `validate` rejects mismatched lengths BEFORE touching KZG.
    - R5. `KzgVerifier` is a trait; placeholder always-Ok impl this week, `c-kzg` at W15.
  - **Design walk-through** D1..D2:
    - D1. Hand-run `fake_exponential(MIN_BLOB_BASE_FEE, excess, FRACTION)` for excess = 0, TARGET, 2×TARGET.
    - D2. **Pre-mortem:** contributor uses `(MIN as f64 * (excess as f64 / FRAC as f64).exp()) as u128`. What
      test catches? (the large-excess integer vector.)
  - **Output**: `notes/14_eip4844_design.md` with R1..R5, D1..D2.

  #### Phase 2 — Write the failing tests (~25 min)
  - [ ] `blob_fee_min_at_zero_excess` *(NEWBIE)* — `calc_blob_fee(0) == 1`. Exercises R2.
  - [ ] `fake_exponential_matches_spec_vectors` *(SENIOR — anti-shortcut: integer not float)* — the EIP-4844
    reference vectors at increasing excess; a `f64::exp`-based impl drifts by ≥1 at large excess and fails.
    ```rust
    #[test]
    fn fake_exponential_matches_spec_vectors() {
        // last vector has large excess where float exp() drifts past integer Taylor result.
        for (excess, expected) in EIP4844_BLOB_FEE_VECTORS {
            assert_eq!(calc_blob_fee(excess), expected, "excess={excess}");
        }
    }
    ```
  - [ ] `sidecar_rejects_length_mismatch` *(SENIOR — cross-primitive: sidecar + verifier ordering)* —
    `blobs.len()=2, proofs.len()=1` → `Err(LengthMismatch)` and the `KzgVerifier` is NEVER called (assert
    via a counting mock verifier).
  - [ ] `excess_blob_gas_floors_at_zero` *(NEWBIE)* — parent below target → next excess == 0, no underflow.
  - [ ] `blob_fee_no_float_in_asm` *(negative-control)* — `cargo asm` / a unit check confirms `calc_blob_fee`
    emits no `mulsd`/`divsd`/`call …exp` (proves the integer path is real, not float behind a cast).

  #### Phase 3 — Make the tests pass, one failure at a time (~50 min)
  - **(SENIOR) When `fake_exponential_matches_spec_vectors` drifts at large excess:**
    - Symptom: small-excess vectors pass, large-excess off by 1+; nodes disagree on blob fee → consensus split.
    - Why: you computed `e^x` in `f64` then cast; float rounding ≠ the integer Taylor series the spec pins.
    - Fix: implement `fake_exponential` as the integer accumulation in the EIP (loop adding
      `numerator_accum * numerator / (denominator * i)`), all `u128`.
    - Muscle: consensus math must be bit-exact integer — float is non-deterministic across hardware. Reapplies
      at: W15 KZG field arithmetic, W83 ledger fixed-point pricing, **W14 bufpool has no float in the hot path**.
  - **(SENIOR) When `sidecar_rejects_length_mismatch` calls the verifier anyway:**
    - Symptom: mismatched sidecar reaches KZG, which panics or returns a confusing error.
    - Why: you validated lengths AFTER (or inside) the verify loop.
    - Fix: length check first, return `Err(LengthMismatch)` before any KZG call; verifier only sees
      well-formed input.
    - Muscle: validate structural invariants before invoking an expensive/foreign verifier. Reapplies at: W15
      KZG batch verify, W42 txn write-set validation, W77 aeron frame header check.
  - **5-year failure mode** (`notes/14_eip4844_followup.md`): when full danksharding raises blob count and
    introduces 2D fee markets, `calc_blob_fee` becomes per-dimension. Trigger: EIP-7594 PeerDAS. Migration:
    parameterize over blob-dimension count.
- [ ] Browse alloy issues, PREFER `alloy-eips`; identify 3-5 candidates, pick one, claim it. (~40 min)
  - 🎯 Hardened acceptance: `notes/14_alloy_candidate.md` names the issue #, the file it touches, and a
    one-line repro/fix sketch — not just "found an issue". (Claimed here so Friday's change + Saturday's PR
    have an issue to work; moved off the overloaded Thursday.)
- [ ] Commit notes + design doc. (~10 min)

**Wednesday — [NEW] `bufpool` core: LRU-K + pin/unpin + concurrency drill #20**

> Day-load note: this day was overweight in v1 (drill #20 + 4 Build exercises). Final distribution, each
> day's summed phase totals ≤ ~360 min: Wed = drill #20 (140) + `lru_k` (135) + commit (10) = 285 min (~4.75h);
> `pin.rs` (135) moves to **Friday** (it is the protect-then-verify twin of Monday's drill #19, so it pairs
> with the eip7702 sighash work); the `dirty` tracker (135) + `lib.rs` facade (210) stay on **Thursday**
> (= 355 min, ~5.9h); the `why_pool` memo, the bench, the Primary-Colors convention, and the Alloy scan move
> to **Saturday**; the wire-up stays on Saturday.

- [ ] **Concurrency drill #20 of 28 — hazard-pointer scan + reclamation under contention** — extends
  `crates/concurrent/src/hazard.rs` with threshold-triggered scan; measurement in `notes/14_drill20.md`.
  (solo, ~total 140 min across 3 phases = 50+40+50; extends drill #19's already-built registry)
  - 🔧 **Why this matters**: drill #19 proved correctness under loom's exhaustive search; this measures the
    reclamation/latency tradeoff under genuine 16-thread contention — directly informing bufpool eviction
    churn and W33's EBR choice. Two traps: (1) op-count-triggered scan that grows unbounded under bursts;
    (2) scan-time pause that spikes p99. bar (c) two-track perf applies. Downstream: W33 EBR amortization,
    W42 deadlock-detector graph reclamation, **W14 bufpool 16-thread eviction soak**.

  #### Phase 1 — Problem, prediction, design (~50 min)
  - 🎯 **Pre-prediction prompt** — `notes/14_drill20_prediction.md`:
    - P1. Scan trigger you'd reach for (op-count vs retired-list-size threshold).
    - P2. Predict steady-state retired-list size for scan-every-{1, 100, 10000} retirements at 16 threads.
    - P3. Predict the per-op p99 latency hit at scan time (the scanning thread blocks while walking slots).
    - P4. Predict which trigger keeps the retired list bounded AND p99 low.
    - P5. List which Phase-2 measurement assertions your first guess will miss.
  - *Mental model*: reclamation is a memory-vs-CPU dial; threshold-on-retired-size amortizes better than
    op-count because it self-tunes to the actual retire rate.
  - *Back-of-envelope*: each hazard slot is an L1-resident read ≈ 0.5-1ns (Jeff-Dean L1 number), so a scan of
    ≤16 slots ≈ 8-16ns; round to ~16ns per scan. At scan-every-1 that's ~16ns ADDED to every ~5ns pop (~4×
    the pop cost — unacceptable); at scan-every-1000 it amortizes to ~0.016ns/pop but the retired list holds
    ~1000 nodes × 32B ≈ 32KB transient. Cliff: scan-every-1 vs scan-every-1000 = ~1000× CPU difference for
    ~32KB memory. (Note: this matches drill #19's ≤64×L1 ≈ 32ns figure — same ~0.5ns/slot L1 cost.)
  - **Requirements** R1..R4 in `notes/14_drill20_design.md`:
    - R1. Scan triggered by retired-list length ≥ threshold (NOT op count).
    - R2. Retired list stays bounded under sustained 16-thread churn.
    - R3. Scan-time p99 hit measured and recorded per trigger.
    - R4. Backpressure path: if retired list exceeds a ceiling, the retiring thread scans inline (never grows
      without bound).
  - **Design walk-through** D1..D2:
    - D1. Property table: op-count trigger (simple; ignores actual rate), size threshold (self-tuning),
      time trigger (decoupled from rate; bursts overwhelm).
    - D2. **Pre-mortem:** 10× burst with op-count trigger → retired list grows faster than scan drains → OOM.
  - **Output**: `notes/14_drill20_design.md` with R1..R4, D1..D2 + the scale-curve plan.

  #### Phase 2 — Write the failing tests (~40 min)
  - [ ] `hazard_retired_list_bounded_under_churn` *(SENIOR — anti-shortcut: size-threshold vs op-count)* — 16
    threads × 100k push/pop with size-threshold scan; assert max retired-list len ≤ 2× threshold at all times.
  - [ ] `hazard_scan_p99_recorded` *(perf — bar (c))* — record per-op latency distribution; assert p99 < 5×
    p50 (scan pauses don't dominate). Fails if scan-every-1.
  - [ ] `hazard_burst_does_not_oom` *(EXPERT — cross-primitive: backpressure + retire)* — inject a 10× retire
    burst; assert retired-list len stays ≤ ceiling (inline-scan backpressure fires).
  - [ ] `hazard_no_correctness_regression_loom` *(negative-control)* — re-run drill #19's
    `treiber_protected_loom_no_uaf_no_double_free` under loom; assert still green (perf change didn't break safety).
  - [ ] `hazard_scale_curve_recorded` *(negative-control)* — assert `notes/14_drill20.md` contains
    retired-list-size and p99 numbers for N ∈ {2,4,8,16}; the test reads the file and fails if a row is missing.

  #### Phase 3 — Make the tests pass, one failure at a time (~50 min)
  - **(SENIOR) When `hazard_retired_list_bounded_under_churn` grows without bound:**
    - Symptom: retired-list len climbs steadily; eventual OOM.
    - Why: op-count trigger (scan every N ops) ignores the actual retire rate; under high contention retires
      outpace scans.
    - Fix: trigger scan when retired-list len ≥ threshold; the rate self-tunes.
    - Cost avoided: unbounded memory growth (OOM crash) — bar (c) violation (static-memory invariant).
    - Muscle: amortized reclamation triggers on the resource that's growing (list size), not a proxy (op
      count). Reapplies at: W33 EBR epoch-advance threshold, W42 GC trigger, **W14 bufpool dirty-set drain threshold**.
  - **(perf, EXPERT) When `hazard_scan_p99_recorded` shows p99 = 20× p50:**
    - Symptom: tail latency spikes at every scan; throughput jitters.
    - Why: scan-every-1 makes every pop pay the full slot-walk (~16ns for 16 L1 slots) inline.
    - Fix: raise threshold so scans amortize; offload large scans to a background reclaimer if the inline pause
      still spikes.
    - Cost avoided: ~16ns × every-pop vs ~0.016ns amortized = ~1000× CPU on the hot path (bar (c) violation).
    - Muscle: never run an O(threads) scan on every hot-path op — amortize. Reapplies at: W37 epoch-gc advance,
      W74 matching-engine GC, **W14 bufpool eviction scan**.
  - **5-year failure mode** (`notes/14_drill20_followup.md`): sustained bursts can still outrun
    threshold-triggered scans. Trigger: 100× burst. Migration: hard backpressure — block the retirer until the
    list drains below ceiling (push-to-retire).

- [ ] **Build**: `crates/bufpool/src/lru_k.rs` — `LruK<const K: usize>` (K=2) eviction policy. (~total 135 min across 3 phases = 30+30+75)
  - 🔧 **Why this matters**: LRU-K (O'Neil 1993) is the canonical buffer-pool eviction; plain LRU-1 is
    scan-vulnerable (a sequential scan flushes the hot working set). Four traps: (1) evicting a pinned page,
    (2) backward-K-distance off-by-one collapsing it to LRU-1, (3) per-touch heap alloc on the hottest path,
    (4) L1 thrashing from cold metadata mixed with hot. First-attempt impls (BTreeSet-priority) fail
    `lru_k_touch_no_alloc` AND `lru_k_scan_resistance` — that's the lesson. Downstream: W26 wal page cache,
    W31 storage-trie node cache, W95 mini-db, W104 vector-db. **bar (c)** — two-track perf required.

  #### Phase 1 — Problem, prediction, design (~30 min, no code yet)
  - 🎯 **Pre-prediction prompt** — `crates/bufpool/notes/14_lru_k_prediction.md`:
    - P1. The priority structure you'd reach for (predict: `BTreeSet<(Instant, FrameId)>` — the wrong one)
      with its exact import.
    - P2. Predict per-touch latency for the BTreeSet approach (predict ns) vs the Vec approach.
    - P3. Predict allocations per 10k touches for BTreeSet (predict count).
    - P4. Predict whether a 1000-page sequential scan evicts the 100-page hot set under LRU-1 vs LRU-2.
    - P5. List every Phase-2 test you predict your first (BTreeSet) attempt fails.
  - *Mental model*: LRU-K evicts the page whose K-th-most-recent access is oldest (largest backward
    K-distance). Pages with < K accesses get K-distance = ∞ → evicted first → that's the scan resistance.
  - *Contract*: `LruK<const K: usize>` with `touch(&mut self, id: FrameId)` (O(1), no alloc) and
    `evict_victim(&self, is_pinned: impl Fn(FrameId) -> bool) -> Option<FrameId>`. `FrameMeta {
    access_times: [Monotonic; K], … }`. NEVER the body in the doc.
  - *Back-of-envelope*: target ≤100ns/touch at 100k touches/s. A BTreeSet insert = O(log n) pointer-chasing +
    a node `malloc` (~50ns) → ~200ns + 1 alloc/touch = 100k allocs/s on the HOTTEST path. The Vec+ring
    approach = 1 L1 write to an 8B word (~0.5ns) + a rotate (~2ns), 0 allocs. Cliff: 200ns+alloc vs ~3ns =
    ~60× and the alloc churn alone is a bar (c) violation.
  - **Requirements** R1..R8 in `crates/bufpool/notes/14_lru_k_design.md`:
    - R1. `LruK<const K: usize>`, instantiated K=2.
    - R2. `Vec<FrameMeta>` indexed by `FrameId`; `FrameMeta { access_times: [Monotonic; K], n_seen: u8 }`.
    - R3. `touch(id)`: rotate the K-ring (newest-first), increment `n_seen` saturating; O(1), zero alloc.
    - R4. `evict_victim(is_pinned)`: among UNPINNED frames, pick max backward-K-distance; pin set is INPUT.
    - R5. Pages with `n_seen < K` have K-distance = ∞ (evicted before any full-history page).
    - R6. Hot path = ZERO allocations per touch; Vec pre-allocated at `new(capacity)`.
    - R7. Hot fields (access_times, n_seen) packed; any cold per-frame data in a parallel Vec (SoA), so a
      touch reads ONE cache line.
    - R8. ≤100ns per touch p50 at 100k touches/s.
  - **Design walk-through** D1..D3:
    - D1. Property table: `BTreeSet` (sorted? yes; alloc/touch? yes; latency? O(log n)), `BinaryHeap`
      (alloc at build only; stale entries), Vec+ring scan-on-evict (0 alloc/touch; evict O(n) but evict is rare).
    - D2. **Pre-mortem (load-bearing):** write the scan interleaving — 1000 one-shot touches then evict: under
      LRU-1 the hot pages are now "least recent" and get flushed; show why the K-th-access rule keeps them.
    - D3. **Anti-shortcut audit:** `BTreeSet<(Instant, FrameId)>` (allocs/touch — wrong here); `HashMap` LRU
      (no K-distance); `std::collections::LinkedList` (cache-hostile pointer chase).
  - **Output**: `notes/14_lru_k_design.md` with R1..R8, D1..D3, the scan interleaving sketch.

  #### Phase 2 — Write the failing tests (~30 min)
  - [ ] `lru_k_evict_skips_pinned` *(SENIOR — cross-primitive: policy + pin set)* — the PINNED frame is the
    NATURAL victim (highest K-distance); `evict_victim` must skip it and return the hotter unpinned frame. A
    pin-ignoring impl returns the pinned frame and fails — this is the trap.
    ```rust
    #[test]
    fn lru_k_evict_skips_pinned() {
        let mut p = LruK::<2>::new(4);
        p.touch(F2); p.touch(F2);          // F2 HOT: full K-history → SMALL backward-K-distance
        p.touch(F1);                        // F1 COLD: n_seen<K → K-distance ∞ → the natural victim
        let pinned = |f| f == F1;
        // Naive max-K-distance returns Some(F1) — WRONG, F1 is pinned. Only a pin-aware impl skips it.
        assert_eq!(p.evict_victim(pinned), Some(F2),
            "must SKIP pinned natural-victim F1 and pick the hotter unpinned F2");
    }
    ```
  - [ ] `lru_k_k_distance_uses_kth_access` *(SENIOR)* — frame with 5 accesses, K=2; K-distance computed from
    `access_times[1]` (2nd-most-recent), NOT the 1st or 5th.
  - [ ] `lru_k_scan_resistance` *(SENIOR — the defining property)* — 1000 sequential one-shot touches + 100
    repeated touches on a hot page; the hot page is NOT chosen as victim.
  - [ ] `lru_k_touch_no_alloc` *(perf — bar (c), anti-shortcut)* — `dhat`/`#[global_allocator]` counter: 10k
    touches after warm-up = 0 heap allocations. A BTreeSet impl fails here.
  - [ ] `lru_k_touch_p50_under_100ns` *(perf — bar (c))* — criterion: touch p50 ≤ 100ns; assert the bench
    actually ran ≥ 10k iters (negative-control on the bench).
  - [ ] `lru_k_evict_when_all_unpinned_under_k_returns_min` *(NEWBIE)* — all frames `n_seen < K` → returns the
    one with the oldest single access, never panics.
  - [ ] `lru_k_evict_all_pinned_returns_none` *(NEWBIE — negative-control on eviction contract)* — every frame
    pinned → `evict_victim` returns `None` (caller must surface `PoolExhausted`, never block).

  #### Phase 3 — Make the tests pass, one failure at a time (~75 min)
  - **(SENIOR) When `lru_k_evict_skips_pinned` evicts the pinned page:**
    - Symptom: holder's `PinnedPage` dangles or its data is reloaded into a different frame.
    - Why: `evict_victim` picked max-K-distance without consulting the pin predicate.
    - Fix: `evict_victim` takes `is_pinned` and skips pinned frames; returns `None` if every unpinned frame is
      ineligible.
    - Cost avoided: a dangling page pointer → UAF / data corruption (correctness, not slow).
    - Muscle: eviction policy and pin set are COUPLED — policy must never select a pinned resource. Reapplies
      at: W26 wal segment retention, W42 txn MVCC GC, W74 matching-engine order pool.
  - **(SENIOR) When `lru_k_scan_resistance` flushes the hot pages:**
    - Symptom: a sequential scan evicts the random-access hot set — LRU-K collapsed to LRU-1.
    - Why: backward-K-distance off-by-one — you used the K-th access EVER, or `access_times[0]` (most recent),
      not the K-th MOST RECENT.
    - Fix: K-distance = `now - access_times[K-1]` with `access_times` newest-first; `n_seen < K` ⇒ ∞.
    - Cost avoided: working-set thrash → cache hit rate collapse (the whole reason LRU-K exists).
    - Muscle: LRU-K's scan resistance IS the K-th-access rule; the off-by-one silently degrades it to LRU-1.
      Reapplies at: W34 bloom counting decay, W42 MVCC version retention, W104 vector-db segment cache.
  - **(perf, SENIOR) When `lru_k_touch_no_alloc` reports >0 allocations (bar (c) violation):**
    - Symptom: `dhat` shows ~10k allocs for 10k touches; 100k allocs/s on the hottest path.
    - Why: `BTreeSet`/`BinaryHeap`-per-touch rebuilds the priority structure, allocating a node each time.
    - Fix: pre-allocate `Vec<FrameMeta>` at `new(capacity)`; `touch` only writes the ring in place; priority
      is computed by a scan AT EVICTION (rare), not maintained per-touch.
    - Cost avoided: ~50ns/touch malloc + allocator-lock contention under 16 threads = bar (c) zero-alloc
      hot-path violation.
    - Muscle: a hot eviction policy allocates ONCE at construction; per-access work is O(1) in-place.
      Reapplies at: W26 wal ring, W34 bloom counter, W42 txn lock graph, W74 matching order ring.
  - **5-year failure mode** (`notes/14_lru_k_followup.md`): when persistent memory blurs the cache/storage
    boundary, eviction becomes per-tier (DRAM + PMEM). Trigger: a PMEM tier appears. Migration: parameterize
    `LruK` over tier and run one instance per tier.
- [ ] Commit + log (Wednesday). (~10 min)

**Thursday — `bufpool` dirty tracker + `BufferPool` facade**

> Day-load note: the bench (45), Primary-Colors convention (75), and Alloy scan (40) moved OFF this day (to
> Saturday/Tuesday) to bring it under cap. Thursday = dirty (135) + lib facade (210) + commit (10) = 355 min (~5.9h).

- [ ] **Build**: `crates/bufpool/src/dirty.rs` — `DirtyPageTracker` (sharded `HashSet<PageId>` + atomic
  count + double-buffer drain). Marks pages for write-back; `wal` (W26) drains on group commit. (~total 135 min across 3 phases = 30+30+75)
  - 🔧 **Why this matters**: the dirty tracker is the bufpool↔wal seam — its double-buffer is the durability
    contract. Three traps: (1) single-buffer drain that silently clears a write made DURING fsync (lost
    durability), (2) single-mutex bottleneck, (3) count derived from `set.len()` requiring a lock. First
    attempts fail `dirty_drain_double_buffer_durability` — a lost write that only shows on crash. Downstream:
    W26 wal group-commit, W42 txn write-set, W80 ledger snapshot. **bar (c)** — durability is correctness.

  #### Phase 1 — Problem, prediction, design (~30 min, no code yet)
  - 🎯 **Pre-prediction prompt** — `crates/bufpool/notes/14_dirty_prediction.md`:
    - P1. The set primitive (predict `Mutex<HashSet<PageId>>` single) vs sharded; exact imports.
    - P2. Predict mark_dirty p50 for single-mutex vs sharded (ns).
    - P3. Predict the outcome when a write to P50 lands AFTER `drain` snapshots but BEFORE fsync finishes,
      with a single buffer.
    - P4. Predict whether `count()` can be lock-free.
    - P5. List Phase-2 tests your first attempt fails.
  - *Mental model*: the dirty set is the list of pages the wal must persist; draining it must atomically hand
    the OLD set to the flusher while new writes accumulate in a fresh set — otherwise a concurrent write is
    lost on crash.
  - *Contract*: `DirtyPageTracker` with `mark_dirty(&self, p: PageId)`, `drain(&self) -> HashSet<PageId>`
    (snapshot + swap), `count(&self) -> u64` (atomic load). No body in the doc.
  - *Back-of-envelope*: mark_dirty target p50 ≤ 50ns. A single global `Mutex<HashSet>` insert under 16 threads
    serializes (~25ns lock + contention → plateau). Sharded by `page % N` → per-shard lock rarely contended
    (~10ns). `count()` from `set.len()` needs the lock (25ns + serialization); a separate `AtomicU64` load = ~1ns.
  - **Requirements** R1..R5 in `crates/bufpool/notes/14_dirty_design.md`:
    - R1. `DirtyPageTracker { shards: [Mutex<HashSet<PageId>>; N], count: AtomicU64 }`, N = num_cores.
    - R2. `mark_dirty(p)`: shard by `p % N`; insert; if newly inserted, `count.fetch_add(1)`.
    - R3. `drain()`: double-buffer — atomically swap each shard's set with a fresh empty set; the swapped-out
      sets are the snapshot; writes during fsync accumulate in the new sets.
    - R4. `count()`: `AtomicU64` load, NO mutex.
    - R5. mark_dirty p50 ≤ 50ns; ≥10× scaling at N=16.
  - **Design walk-through** D1..D2:
    - D1. Property table: single `Mutex<HashSet>` (serializes), sharded mutex sets (scales; dedup per shard),
      lock-free set (complex; overkill here).
    - D2. **Pre-mortem (load-bearing):** 5-line trace — drain clears set → wal fsyncs snapshot S1 → B writes
      P50 and marks dirty (single buffer: insert into already-cleared set) → next drain happens before P50's
      data hits disk → P50 lost on crash. Show how double-buffer swap fixes it.
  - **Output**: `notes/14_dirty_design.md` with R1..R5, D1..D2, the lost-write trace.

  #### Phase 2 — Write the failing tests (~30 min)
  - [ ] `dirty_drain_double_buffer_durability` *(EXPERT — cross-primitive: drain + concurrent mark, the
    durability trap)* — thread A drains snapshot S1; thread B marks P50 mid-fsync; assert P50 appears in the
    NEXT drain (never silently dropped).
  - [ ] `dirty_mark_no_mutex_contention` *(perf — bar (c), anti-shortcut)* — 16 threads marking distinct
    pages; throughput ≥ 10× single-thread. Single global mutex plateaus at ~4×.
  - [ ] `dirty_count_lock_free` *(perf — bar (c), negative-control)* — instrument with a counting `Mutex`
    wrapper; assert `count()` acquires ZERO locks.
  - [ ] `dirty_mark_p50_under_50ns` *(perf — bar (c))* — criterion p50 ≤ 50ns; assert ≥10k iters ran.
  - [ ] `dirty_drain_is_atomic_swap` *(SENIOR)* — concurrent drain + mark; assert no PageId is both in the
    snapshot AND still in the live set (no duplication, no loss).

  #### Phase 3 — Make the tests pass, one failure at a time (~75 min)
  - **(EXPERT) When `dirty_drain_double_buffer_durability` drops P50 (bar (c) durability violation):**
    - Symptom: B's write to P50 vanishes after a crash — wal fsynced S1 without P50; B's mark cleared by the
      single-buffer drain; the next drain doesn't see P50.
    - Why: single buffer — drain cleared everything; B's mark_dirty after the clear inserted into a set the
      flusher already snapshotted-and-emptied, so the dirty bit is lost.
    - Fix: double-buffer — drain swaps in a fresh empty set atomically; the OLD set is the snapshot flushed;
      concurrent marks land in the new set and survive to the next drain.
    - Cost avoided: silent data loss on crash — the worst bar (c) failure (durability is correctness, not perf).
    - Muscle: a flush/drain op MUST double-buffer; never let writes-during-flush clear dirty bits. Reapplies
      at: W26 wal group-commit, W42 txn write-set commit, W80 ledger snapshot.
  - **(perf, SENIOR) When `dirty_mark_no_mutex_contention` plateaus at N=4 (bar (c) violation):**
    - Symptom: a single `Mutex<HashSet>` serializes every write; 16 threads no faster than 4.
    - Why: one global mutex on the hot write path.
    - Fix: shard `[Mutex<HashSet>; N=num_cores]`, key by `page % N`; dedup per shard at drain.
    - Cost avoided: a write-path scaling ceiling (~25ns serialized) — defeats thread-per-core (bar (c)).
    - Muscle: any global set on the hot write path is the scalability ceiling — shard it. Reapplies at: W26
      wal pending queue, W42 txn write set, W74 matching pending orders.
  - **(perf, NEWBIE) When `dirty_count_lock_free` shows a lock acquisition (bar (c) violation):**
    - Symptom: `count()` calls `.lock().len()`, serializing readers against writers.
    - Why: count derived from set length.
    - Fix: maintain a separate `AtomicU64`, incremented only when `set.insert` returns true; `count()` is a
      pure atomic load.
    - Cost avoided: ~25ns + serialization per count vs ~1ns load; the count is read often by the flusher.
    - Muscle: read-only summary state on a hot path lives in an atomic, never behind the data lock. Reapplies
      at: W26 wal pending count, W74 matching order count.
  - **5-year failure mode** (`notes/14_dirty_followup.md`): persistent-memory write-through removes the dirty
    concept entirely. Trigger: a PMEM backing tier. Migration: gate the tracker behind `#[cfg(disk_backed)]`.
- [ ] **Build**: `crates/bufpool/src/lib.rs` — `BufferPool<P: PageProvider>` facade composing `lru_k` + `pin`
  + `dirty`: `new(capacity, k=2)`, `pin(id) -> Result<PinnedPage, PoolFull>`, `mark_dirty(id)`,
  `flush_all(&self, sink: &mut dyn WriteBack) -> Result<()>`. (~total 210 min across 3 phases = 45+30+135)
  - 🔧 **Why this matters**: the public facade is where the three primitives compose — and where the bar (c)
    perf traps move from per-primitive to system-level. Three traps: (1) page-table serialization wasting the
    lock-free primitives, (2) holding the LRU lock across the load I/O (one miss stalls everyone), (3)
    flush_all blocking writes. First attempts fail the eviction-serialization and flush-blocking benches.
    Downstream: every storage-trie / wal / mmap-queue / mini-db page access. **bar (c)** — VOPR-style loom + two-track perf required.

  #### Phase 1 — Problem, prediction, design (~45 min, no code yet)
  - 🎯 **Pre-prediction prompt** — `crates/bufpool/notes/14_bufpool_lib_prediction.md`:
    - P1. The page-table primitive (predict `Mutex<HashMap<PageId,FrameId>>`) vs `DashMap`; exact imports.
    - P2. Predict pin-HIT p50 (should be ~30ns: shard lookup + atomic inc).
    - P3. Predict the throughput of OTHER pins during one ~200µs miss, if the LRU lock is held across the load.
    - P4. Predict mark_dirty p99 during a 10ms flush_all if flush takes the dirty lock.
    - P5. List the Phase-2 tests your first attempt fails.
  - *Mental model*: the pool is a fixed array of frames + a concurrent page→frame index + an eviction policy +
    a dirty set; the hot path (pin-hit) must be lock-free, and the slow paths (miss, flush) must never block
    the hot path.
  - *Contract*: `BufferPool<P: PageProvider>` as above; `Frame { pin_count: AtomicU64, gen: AtomicU64, data:
    UnsafeCell<[u8; PAGE_SIZE]> }`; `trait WriteBack { fn write(&mut self, id: PageId, data: &[u8]) ->
    io::Result<()> }`. No body in the doc.
  - *Back-of-envelope*: pin-hit budget p50 ≤ 30ns / p99 ≤ 100ns / p99.9 ≤ 500ns. A page-table `Mutex<HashMap>`
    lookup serializes (~25ns + contention) → kills the ~7ns atomic pin. A miss loads from disk (~200µs); if the
    LRU lock is held across it, that one 200µs lock-hold serializes ALL pin attempts behind it: lock-acquisition
    throughput is capped at 1 / 200µs ≈ 5,000 ops/s, so the pool collapses from ~10M ops/s to ~5,000 ops/s — a
    ~2000× drop, even though the other pages are already resident. flush_all fsync ≈ 10ms; if it holds the dirty
    lock, every mark_dirty waits 10ms (p99 = 10ms vs target 100ns = 100,000× cliff).
  - **Requirements** R1..R8 in `crates/bufpool/notes/14_bufpool_lib_design.md`:
    - R1. `BufferPool { frames: Vec<CachePadded<Frame>>, page_table: DashMap<PageId, FrameId>, lru:
      Mutex<LruK<2>>, dirty: DirtyPageTracker, provider: P }`.
    - R2. `Frame { pin_count: AtomicU64, gen: AtomicU64, data: UnsafeCell<[u8; PAGE_SIZE]> }`.
    - R3. Pin-hit p50 ≤ 30ns, p99 ≤ 100ns, p99.9 ≤ 500ns.
    - R4. Scale curve: at N=16, ≥10× single-thread pin-hit throughput.
    - R5. Loom (VOPR-style): 4-thread (2 pinners + 2 evictors); no UAF, no double-evict, no leaked pin.
    - R6. Eviction selects the victim under the LRU lock (~50ns), RELEASES the lock, THEN loads (sentinel locks
      the specific frame for the load duration).
    - R7. `flush_all` uses the dirty double-buffer; NEVER blocks `mark_dirty`.
    - R8. Pin-storm (all frames pinned) → `Err(PoolFull)`, NEVER OOM, NEVER block forever.
  - **Design walk-through** D1..D3:
    - D1. Walk the pin-MISS path: lookup → miss → select-victim(under lru lock) → release lru → CAS frame
      sentinel → load → install → drop sentinel. Mark exactly where the lock is held and where the I/O happens.
    - D2. **Pre-mortem (load-bearing):** contributor holds the LRU lock across the load. 5-line trace: T1
      misses, holds lru lock, blocks on 200µs disk read; T2..Tn all block on the lru lock trying to pin DIFFERENT
      (already-resident) pages. Show the release-before-I/O fix.
    - D3. **Anti-shortcut audit:** `Mutex<HashMap>` page table (serializes hot path); one big `RwLock<Pool>`
      (writer starvation + holds across I/O); `Arc<Mutex<Frame>>` per frame (alloc + indirection).
  - **Output**: `notes/14_bufpool_lib_design.md` with R1..R8, D1..D3, the lock-across-I/O trace.

  #### Phase 2 — Write the failing tests (~30 min)
  - [ ] `bufpool_pin_hit_p50_under_30ns` *(perf — bar (c), anti-shortcut)* — criterion pin-hit p50 ≤ 30ns;
    a `Mutex<HashMap>` page table reports ~200ns and fails. Assert ≥10k iters ran.
  - [ ] `bufpool_scale_16x_at_n16` *(perf — bar (c))* — N=16 pin-hit throughput ≥ 10× N=1.
  - [ ] `bufpool_loom_4_thread_safe` *(EXPERT — cross-primitive: pin + lru + dirty under loom, VOPR-style)* —
    `#[cfg(loom)]` 2 pinners + 2 evictors; no UAF, no double-evict, no leaked pin at quiescence (pin counts all 0).
  - [ ] `bufpool_eviction_does_not_serialize_other_pins` *(perf — bar (c), the lock-across-I/O trap)* — during
    one miss with a 200µs simulated load, 100k pins of OTHER resident pages complete; assert their throughput
    is within 10% of the no-miss baseline.
  - [ ] `bufpool_flush_all_does_not_block_writes` *(perf — bar (c), cross-primitive: facade + dirty
    double-buffer)* — flush_all simulating a 10ms fsync running; concurrent `mark_dirty` p99 < 100ns.
  - [ ] `bufpool_pin_storm_returns_err` *(SENIOR — negative-control on the exhaustion contract)* — pin every
    frame; the next pin returns `Err(PoolFull)`, no OOM, no hang (bounded-time return).

  #### Phase 3 — Make the tests pass, one failure at a time (~135 min)
  - **(perf, SENIOR) When `bufpool_pin_hit_p50_under_30ns` reports ~200ns (bar (c) violation):**
    - Symptom: the page-table lookup dominates; the lock-free pin counter's ~7ns is wasted.
    - Why: `Mutex<HashMap<PageId, FrameId>>` serializes every lookup.
    - Fix: `DashMap` (sharded) for the hot page table — shard-local lookup + atomic increment ≈ 30ns; route
      the rare pin-MISS coordination through a secondary `DashMap<PageId, Arc<Mutex<LoadState>>>`.
    - Cost avoided: ~170ns/pin + contention vs ~30ns; the whole pool's lock-free design negated (bar (c)).
    - Muscle: concurrent map for the hot table; mutexes only for rare coordination paths. Reapplies at: W26
      wal segment lookup, W42 txn lock table, W95 mini-db page table.
  - **(perf, EXPERT) When `bufpool_eviction_does_not_serialize_other_pins` collapses to ~5,000 ops/s during a miss (bar (c) violation):**
    - Symptom: all 100k pins of OTHER pages block during the single ~200µs miss load.
    - Why: the LRU lock is held across the disk read.
    - Fix: select the victim under the lru lock (~50ns), RELEASE it, then CAS the specific frame to the
      sentinel and load. Other threads pin OTHER frames freely (this is the drill #19 protect-then-act shape).
    - Cost avoided: a 200µs serialized lock-hold caps lock-acquisition throughput at 1 / 200µs ≈ 5,000 ops/s —
      throughput collapses from ~10M ops/s to ~5,000 ops/s, a ~2000× drop (bar (c)).
    - Muscle: NEVER hold a hot-path lock across I/O — release before the syscall, re-acquire after. Reapplies
      at: W26 wal append-during-fsync, W42 commit-during-replication, W76 aeron NAK retransmit.
  - **(perf, SENIOR) When `bufpool_flush_all_does_not_block_writes` shows mark_dirty p99 = 10ms (bar (c) violation):**
    - Symptom: every flush stalls the write path for the full fsync duration.
    - Why: flush_all took an exclusive lock on the dirty set.
    - Fix: `flush_all` calls `dirty.drain()` (double-buffer from `dirty.rs`); it operates on the snapshot while
      writes accumulate in the new set.
    - Cost avoided: 10ms write-path stall per flush vs 100ns target = 100,000× (bar (c)).
    - Muscle: a flush op MUST NOT block writes — snapshot/double-buffer off the hot path. Reapplies at: W26 wal
      group-commit, W42 commit, W80 ledger snapshot.
  - **5-year failure mode** (`notes/14_bufpool_lib_followup.md`): when io_uring async I/O matures, the sentinel
    pattern composes with completion-based futures. Trigger: W90 io_uring integration. Migration: `PinFuture`
    instead of a synchronous pin; the sentinel becomes the future's resource claim.
- [ ] Commit notes (Thursday). (~10 min)

**Friday — `bufpool` pin tracking + medium-difficulty Alloy PR work + EIP-7702 in `eth-eips`**

> Day-load note: `pin.rs` (135) moved here from Wednesday — it is the protect-then-verify twin of Monday's
> drill #19 (the CAS-to-sentinel evictor handshake is the hazard-pointer protect/retire shape), so it pairs
> with the eip7702 domain-separated-sighash work. Friday = pin (135) + alloy change (45) + reread (30) +
> eip7702 (90) + tag (5) + commit (10) = 315 min (~5.25h).

- [ ] **Build**: `crates/bufpool/src/pin.rs` — `PinnedPage<'a>` RAII guard; `pool.pin(id) -> PinnedPage`
  increments a per-frame pin count, `Drop` decrements; pinned pages are evict-immune. (~total 135 min across 3 phases = 30+30+75)
  - 🔧 **Why this matters**: pin tracking is the concurrency contract between readers/writers and eviction —
    the same protect-then-verify shape as drill #19's hazard pointer. Four traps: (1) leak via `mem::forget`,
    (2) pin-during-eviction race (evictor reads count 0, pinner increments, evictor frees), (3)
    mutex-bottleneck pin table, (4) false sharing across adjacent frame counters. First-attempt impls fail the
    loom race test + the false-sharing offset test. Downstream: W26 wal frame pins, W42 txn frame locks, W74
    matching-engine. **bar (c)** — two-track perf required.

  #### Phase 1 — Problem, prediction, design (~30 min, no code yet)
  - 🎯 **Pre-prediction prompt** — `crates/bufpool/notes/14_pin_prediction.md`:
    - P1. The pin-count primitive (predict: `Mutex<HashMap<PageId,u32>>` — the wrong one) vs the right one
      (`AtomicU64` per frame). Exact imports.
    - P2. Predict pin p50 for the mutex-map vs per-frame atomic (ns).
    - P3. Predict the concurrent outcome: evictor reads count==0 while a pinner is mid-increment.
    - P4. Predict the false-sharing throughput loss when 8 frame counters share one 64B line, 16 threads.
    - P5. List every Phase-2 test your first attempt fails.
  - *Mental model*: a pin is a temporary "do not evict" claim; eviction must atomically transition a frame
    0→sentinel before reusing it, exactly like a hazard-pointer protect/retire handshake.
  - *Contract*: `PinnedPage<'a> { frame: &'a Frame }` (Drop decrements); `BufferPool::pin(&self, id: PageId) ->
    Result<PinnedPage<'_>, PoolFull>`; `pin_count(&self, id) -> u64`; `into_raw`/`from_raw` for intentional FFI
    leaks. No body in the doc.
  - *Back-of-envelope*: pin target p50 ≤ 30ns. `AtomicU64::fetch_add` uncontended ≈ 5-10ns (L1, no syscall);
    a `Mutex` lock/unlock ≈ 25ns uncontended and serializes under contention (→ no scaling). False sharing:
    8 counters/line means 16 threads ping-pong the line via MESI ≈ +30-50% latency. Cliff: per-frame atomic
    (~7ns, scales) vs mutex-map (~25ns, plateaus at N=4) = scaling cliff at 16 threads.
  - **Requirements** R1..R7 in `crates/bufpool/notes/14_pin_design.md`:
    - R1. `PinnedPage<'a>` RAII; `Drop` decrements pin count exactly once.
    - R2. Pin count is `AtomicU64` PER FRAME (never a shared mutex map).
    - R3. Eviction is 2-phase: CAS pin_count 0→`MAX` sentinel; if CAS fails, the frame is pinned — pick
      another victim. With the sentinel set, no new pin succeeds.
    - R4. Per-frame metadata is `CachePadded<Frame>` so adjacent frame counters never share a cache line.
    - R5. Page→Frame mapping via `DashMap` (sharded concurrent map), not a global mutex.
    - R6. Pin p50 ≤ 30ns; ZERO allocations on the pin hot path.
    - R7. `into_raw`/`from_raw` for intentional leaks (FFI); a plain `mem::forget` must be caught by a debug
      assertion (pin-count won't return to 0).
  - **Design walk-through** D1..D3:
    - D1. Property table: `Mutex<HashMap>` (Sync; serializes; 1 lock/pin), `RwLock` (read-shared but write
      starves), `Vec<AtomicU64>` (per-frame; false-shares), `Vec<CachePadded<Frame>>` (per-frame; isolated).
    - D2. **Pre-mortem (load-bearing):** 5-line race: evictor loads pin_count==0 → pinner `fetch_add` → evictor
      proceeds to free → pinner now holds a `PinnedPage` to a freed frame. Show where the CAS-to-sentinel blocks it.
    - D3. **Anti-shortcut audit:** `Arc<AtomicU64>` per page (alloc + indirection — wrong, frames are
      fixed-index); global `Mutex` (serializes); `RwLock` (writer starvation under pin storms).
  - **Output**: `notes/14_pin_design.md` with R1..R7, D1..D3, the race interleaving sketch.

  #### Phase 2 — Write the failing tests (~30 min)
  - [ ] `pin_drop_decrements_count` *(NEWBIE)* —
    ```rust
    let p = pool.pin(id)?;
    let before = pool.pin_count(id);
    drop(p);
    assert_eq!(pool.pin_count(id), before - 1);
    ```
  - [ ] `pin_during_eviction_race_safe` *(EXPERT — cross-primitive: pin + eviction sentinel, loom)* —
    `#[cfg(loom)]`: thread A evicts frame, thread B pins the same frame; assert EITHER (eviction completed and
    B got `Err(PoolFull)`/retried elsewhere) OR (B's pin succeeded and eviction aborted via failed CAS) — never
    "B holds a pin to a freed frame".
  - [ ] `pin_count_no_mutex_contention` *(perf — bar (c), anti-shortcut)* — 16-thread pin/unpin throughput
    ≥ 10× single-thread. A mutex-map impl plateaus at ~4×.
  - [ ] `pin_count_no_false_sharing` *(perf — bar (c), cross-primitive: pin + CachePadded layout)* —
    ```rust
    let line = if cfg!(target_arch="aarch64") {128} else {64};
    assert!(offset_of!(Frame_array, [1].pin_count) - offset_of!(Frame_array, [0].pin_count) >= line,
        "adjacent frame pin counts share a cache line — false sharing");
    ```
  - [ ] `pin_latency_under_30ns_p50` *(perf — bar (c))* — criterion: pin-hit p50 ≤ 30ns; assert ≥10k iters ran.
  - [ ] `pin_forget_caught_in_debug` *(negative-control)* — `mem::forget(pin)` in a debug build trips the
    pin-count leak assertion at pool drop (proves the leak is detectable, not silent).

  #### Phase 3 — Make the tests pass, one failure at a time (~75 min)
  - **(EXPERT) When `pin_during_eviction_race_safe` finds B holding a freed frame:**
    - Symptom: loom reports B's `PinnedPage` references a frame the evictor freed; data race / UAF.
    - Why: eviction READ pin_count==0 then proceeded; B's `fetch_add` landed in between (TOCTOU).
    - Fix: 2-phase eviction — `compare_exchange(0, MAX)` on pin_count; only on success does the evictor own the
      frame; on failure (B already incremented) pick another victim. Sentinel `MAX` blocks new pins.
    - Cost avoided: use-after-free / torn page read — undefined behavior (correctness, bar (c) safety).
    - Muscle: "remove a shared resource" needs an atomic claim (CAS-to-sentinel), never read-then-act.
      Reapplies at: W26 wal segment unlink, W37 epoch-gc reclamation, W42 txn lock release, drill #19 hazard retire.
  - **(perf, SENIOR) When `pin_count_no_mutex_contention` plateaus at N=4 (bar (c) violation):**
    - Symptom: 16-thread throughput ≈ 4-thread throughput; CPUs idle on lock wait.
    - Why: `Mutex<HashMap<PageId,u32>>` serializes every pin.
    - Fix: `AtomicU64` per frame in `Vec<Frame>`; `DashMap` only for the PageId→FrameId mapping.
    - Cost avoided: a hard scaling ceiling (~25ns serialized vs ~7ns parallel) — defeats the thread-per-core
      design (bar (c)).
    - Muscle: per-resource counts live in per-resource atomics, never a shared map. Reapplies at: W26 wal
      segment refcount, W37 epoch-gc refcount, W42 lock counts, W74 per-symbol counters.
  - **(perf, SENIOR) When `pin_count_no_false_sharing` shows delta < cache line (bar (c) violation):**
    - Symptom: 16-thread pin throughput drops 30-50% vs the isolated-counter ideal; perf shows HITM events.
    - Why: `Vec<AtomicU64>` packs 8 counters per 64B line; threads on different frames thrash the SAME line.
    - Fix: `Vec<CachePadded<Frame>>`; keep cold per-frame fields in a parallel Vec so the hot counter owns its line.
    - Cost avoided: 30-50% throughput from MESI line ping-pong; green `cargo test`, RED bench (bar (c)).
    - Muscle: a per-resource atomic written by different threads needs its own cache line — `repr(C)` +
      `CachePadded`. Reapplies at: W26 wal per-segment counters, W74 matching per-symbol, W77 aeron per-publication.
  - **5-year failure mode** (`notes/14_pin_followup.md`): when async-await wraps page access, Drop-based RAII
    pins don't compose (cancellation drops mid-await, releasing a pin the caller still logically holds).
    Trigger: W90 io_uring async pin path. Migration: async-aware `PinHandle` with explicit `unpin().await`.
- [ ] Substantive change in `alloy-eips` or `alloy-consensus` (the claimed issue). (~45 min)
  - 🎯 Hardened acceptance: `cargo test` green in the alloy fork; the diff is a behavior change (not a doc/typo),
    referenced by issue # in the commit.
- [ ] Re-read EIP-7702 authorization-tuple section (lookup-when-stuck). (~30 min)
- [ ] **Refactor**: `crates/eth-eips/src/eip7702.rs` — move `Authorization` + `SignedAuthorization` out of
  `eth-consensus`; recover the signer from the authorization tuple. (~total 90 min across 3 phases = 20+25+45)
  - 🔧 **Why this matters**: mirrors `alloy-eips::eip7702`. The authorization signs over
    `keccak(MAGIC || rlp([chain_id, address, nonce]))` — a wrong MAGIC byte or field order recovers the WRONG
    signer, silently authorizing the wrong delegation. Two traps: the `0x05` MAGIC prefix and the chain_id==0
    "any chain" semantics. Downstream: W15 eip2 set-code tx, W16 rpc-types, exec-vm 7702 delegation.

  #### Phase 1 — Problem, prediction, design (~20 min)
  - 🎯 **Pre-prediction prompt** — `crates/eth-eips/notes/14_eip7702_prediction.md`:
    - P1. The hash you'd sign over (predict the exact preimage incl. the MAGIC byte).
    - P2. Predict the recovered signer if you OMIT the MAGIC prefix (wrong address).
    - P3. Predict the validity semantics when `chain_id == 0`.
    - P4. Predict what happens to the recovered signer if you RLP the fields in the wrong order.
    - P5. List the Phase-2 tests your first attempt fails.
  - *Mental model*: a 7702 authorization lets an EOA delegate its code to a contract; the signature binds
    chain_id + delegate address + nonce so it can't be replayed cross-chain or out of nonce order.
  - *Contract*: `struct Authorization { chain_id: U256, address: Address, nonce: u64 }` with
    `signature_hash(&self) -> B256`; `struct SignedAuthorization { inner: Authorization, sig: Signature }`
    with `recover_authority(&self) -> Result<Address, RecoveryError>`. No body in the doc.
  - **Requirements** R1..R4 in `notes/14_eip7702_design.md`:
    - R1. `signature_hash = keccak256(0x05 || rlp([chain_id, address, nonce]))`.
    - R2. `recover_authority` ecrecovers from `signature_hash` + sig.
    - R3. `chain_id == 0` ⇒ valid on ANY chain; otherwise must match the current chain.
    - R4. Round-trip against an EIP-7702 test vector (known authority).
  - **Design walk-through** D1..D2:
    - D1. Hand-build the preimage for a known authority; confirm the MAGIC byte position.
    - D2. **Pre-mortem:** contributor omits `0x05`. What test catches? (the known-authority vector.)
  - **Output**: `notes/14_eip7702_design.md` with R1..R4, D1..D2.

  #### Phase 2 — Write the failing tests (~25 min)
  - [ ] `auth_recovers_known_authority` *(SENIOR — vector trap)* — a known EIP-7702 signed authorization
    recovers the expected address; an impl missing the `0x05` MAGIC recovers a DIFFERENT (wrong) address.
    ```rust
    #[test]
    fn auth_recovers_known_authority() {
        let signed = SignedAuthorization::from_vector(EIP7702_VECTOR);
        assert_eq!(signed.recover_authority().unwrap(), EIP7702_VECTOR.expected_authority);
    }
    ```
  - [ ] `auth_chain_id_zero_valid_any_chain` *(SENIOR)* — `chain_id == 0` validates on mainnet AND on Base;
    a nonzero chain_id validates ONLY on the matching chain.
  - [ ] `auth_wrong_field_order_recovers_wrong` *(negative-control — proves the test bites)* — encode the RLP
    fields in the wrong order; assert the recovered authority is NOT the expected one (the test must be able to fail).
  - [ ] `auth_round_trip_encode_decode` *(NEWBIE)* — encode then decode a `SignedAuthorization`; identity.

  #### Phase 3 — Make the tests pass, one failure at a time (~45 min)
  - **(SENIOR) When `auth_recovers_known_authority` recovers the wrong address:**
    - Symptom: ecrecover returns a valid-but-wrong address; delegation would be authorized for the wrong EOA.
    - Why: the signed preimage omitted the `0x05` MAGIC prefix (or hashed the RLP without it).
    - Fix: `signature_hash = keccak256([0x05].concat(rlp([chain_id, address, nonce])))`.
    - Muscle: a domain-separated signature must include its MAGIC/domain byte in the preimage, or it recovers
      the wrong signer. Reapplies at: W6 eth-consensus tx sighash, exec-vm 7702 delegation, W66 tempo envelope sig.
  - **(SENIOR) When `auth_chain_id_zero_valid_any_chain` rejects on Base:**
    - Symptom: a `chain_id == 0` authorization fails on chains other than mainnet.
    - Why: you required `chain_id == current_chain` unconditionally.
    - Fix: `chain_id == 0` is a wildcard — valid on any chain; only enforce equality for nonzero chain_id.
    - Muscle: read the spec's special-case values (0 == wildcard) — don't assume strict equality. Reapplies at:
      W15 eip2 chain config, W91 consensus-engine fork rules.
  - **5-year failure mode** (`notes/14_eip7702_followup.md`): if a future EIP changes the MAGIC byte or adds
    fields to the tuple, the preimage builder must version. Trigger: an EIP-7702 successor. Migration:
    `signature_hash` dispatches on an authorization version tag.
- [ ] Tag `eth-eips v0.1.0`. (~5 min)
- [ ] Commit + log (Friday). (~10 min)

**Saturday — Alloy PR submitted + bench + bench-convention + first "primitive once" wire-up + tag `bufpool v1.0`**

> Day-load note: the bufpool bench (45), the Primary-Colors bench-convention Build (75) moved here from the
> overloaded Thursday — the bench must run before the `why_pool` memo (which cites its numbers) and before
> tagging v1.0 (the bar (c) audit reads the bench). Saturday = alloy PR (25) + page wire-up (60) + bench (45)
> + Primary-Colors (75) + why_pool (75) + tag (10) + commit (10) = 300 min (~5h).

- [ ] Finish + open the Alloy PR; motivation references the `eth-eips` design notes. (~25 min)
  - 🎯 Hardened acceptance: PR is open with a CI-green diff and a body linking the design note rationale.
- [ ] **Wire-up** (glue, no new algorithm — composes existing `Page` + `PinnedPage`):
  `crates/eth-storage-cache/src/page.rs` — `Page` no longer owns its memory; it borrows from
  `bufpool::PinnedPage`. First "primitive once" wire-up: Page memory now comes from ONE pool that storage-trie
  (W31), wal (W26), and mini-db (W95) will all share. (~60 min)
  - 🔧 **Why this matters**: this is the inheritance moment — `eth-storage-cache::Page` (W2) stops being a
    standalone owner and becomes a thin view over a pooled, pinned frame. Trap: a lifetime/Drop ordering bug
    where the `Page` outlives its `PinnedPage` (use-after-unpin). Downstream: every page consumer from W26 on.
  - 🎯 Hardened acceptance: `Page<'a>` carries the `PinnedPage<'a>` lifetime; `cargo test -p eth-storage-cache`
    green; `eth-storage-cache` bumped to v0.2.0 depending on `bufpool`. A `trybuild` compile-fail test
    (`tests/ui/page_outlives_pin.rs`) constructs a `Page` from a `PinnedPage`, drops the pin, then uses the
    `Page` — and the captured `.stderr` MUST contain the exact borrow-check error
    `` error[E0505]: cannot move out of `pinned` because it is borrowed `` (or, for the let-scope variant,
    `` error[E0597]: `pinned` does not live long enough ``). Asserting the specific E0505/E0597 string is what
    proves the test traps the lifetime/Drop-order bug rather than passing on any unrelated compile failure (a
    typo would emit a different code, e.g. E0425/E0412, and the assertion would fail).
- [ ] criterion bench `crates/bufpool/benches/bufpool.rs`: 1M ops mixed read/write at hit rates 50/75/90/99%;
  LRU-K vs naive LRU-1 vs FIFO; record numbers in `notes/14_bufpool_bench.md`. (~45 min)
  - 🎯 Hardened acceptance: bench shows LRU-K beats LRU-1 on the scan-heavy (50% hit) workload by ≥15% hit
    rate; numbers land in a plausible band (pin-hit 20-50ns, miss 100-300µs).
- [ ] **Build (convention)**: `crates/bufpool/benches/bufpool.rs` + `notes/bench_primary_colors.md` +
  `scripts/check_bench_tags.sh` — adopt TigerStyle's "Primary Colors" bench-tagging convention; bufpool is the
  first adopter. (~total 75 min across 3 phases = 20+20+35)
  - 🔧 **Why this matters**: bench discipline at design stage — every hot-path bench names which physical
    resource it stresses (`[NET-BW]`/`[NET-LAT]`/`[STORAGE-BW]`/`[STORAGE-LAT]`/`[MEM-BW]`/`[MEM-LAT]`/`[CPU]`).
    Adopting now means W26 wal, W30 recovery, W37 epoch-gc, W42 txn, W76 aeron, W82 matching, W92 ledger inherit
    it instead of re-inventing. Trap: a tag that includes setup contaminates the timed section → flaky CI.

  #### Phase 1 — Convention design (~20 min)
  - **Requirements** R1..R7 in `notes/bench_primary_colors.md`:
    - R1. Every criterion bench fn id starts with one tag in `[…]`, e.g. `[MEM-LAT] bufpool_pin_hit_p50`,
      `[STORAGE-BW] bufpool_dirty_flush_throughput`, `[CPU] lru_k_touch`.
    - R2. Bufpool's benches (pin-hit, dirty-flush, lru-k touch) tagged today.
    - R3. The doc defines each of the 7 tags (1 line + 1 example each).
    - R4. `scripts/check_bench_tags.sh` greps `**/benches/*.rs`, flags untagged fns. WARN at W14; fail-CI at W26.
    - R5. NO multi-color bench — split if a workload stresses two resources.
    - R6. New tags require a workspace-level commit, not ad-hoc per crate.
    - R7. *Source*: TigerStyle "Primary Colors Framework".
  - **Output**: `notes/bench_primary_colors.md` + `scripts/check_bench_tags.sh`.

  #### Phase 2 — Failing assertions (~20 min)
  - [ ] `bench_tags_doc_defines_seven` *(NEWBIE)* — the doc defines all 7 tags (test greps the file).
  - [ ] `bufpool_benches_all_tagged` *(SENIOR)* — every fn in `crates/bufpool/benches/*.rs` starts with `[TAG]`.
  - [ ] `check_bench_tags_script_flags_untagged` *(negative-control)* — run the script on a deliberately
    untagged bench fixture; assert it exits non-zero (proves the linter actually catches).
  - [ ] `no_multi_color_bench` *(SENIOR)* — no bench id has 2 tags.

  #### Phase 3 — One failure at a time (~35 min)
  - **(SENIOR) When a `[STORAGE-LAT]`-tagged bench fluctuates across runs:**
    - Symptom: numbers depend on filesystem cache state; CI flakes after a reboot.
    - Why: the setup phase (file open / page warm) contaminates the timed section.
    - Fix: split `setup` (untagged) from `bench` (tagged) via `iter_batched` / `iter_custom`; the tag describes
      only the timed section.
    - Muscle: a perf tag describes ONLY the timed section — setup is excluded by construction. Reapplies at:
      every bench in wal (W26), mmap-queue (W79), aeron (W77+), ledger (W83+).
  - **5-year failure mode** (`notes/bench_primary_colors_followup.md`): when GPU/NIC offload becomes
    mainstream, new tags (`[GPU]`, `[NIC-OFFLOAD]`) are needed. Trigger: a GPU/NIC bench. Migration: add via a
    workspace-level commit, never ad-hoc.
- [ ] **Build**: `crates/bufpool/docs/why_pool_in_rust.md` — Chronicle Queue's off-heap discipline mapped onto
  bufpool's pre-alloc + recycle. Why does a GC-free language still need a pool? (~total 75 min across 3 phases = 20+20+35)
  - 🔧 **Why this matters**: bufpool's reason-to-exist is non-obvious in Rust ("no GC, why pool?"). Chronicle
    Queue solves the SAME problem in Java; the memo locks the rationale as one source of truth. Trap: copying
    Chronicle's bytes-off-heap discipline VERBATIM (mmap everything) instead of borrowing the problem shape.
    Downstream: W26 wal, W79 mmap-queue, W95 mini-db all cite this memo.

  #### Phase 1 — Doc plan (~20 min)
  - **Requirements** R1..R5 in the doc:
    - R1. Three sections: (a) Chronicle's Java problem (GC pauses, churn, fragmentation), (b) bufpool's Rust
      problem (alloc cost, fragmentation, cache locality, predictable lifetime), (c) the shared answer + where
      they diverge.
    - R2. One concrete number per claim: jemalloc small alloc ~50-100ns, glibc contended ~200-500ns, Chronicle
      100+TB throughput claim cited, bufpool's bench numbers (the criterion run earlier today).
    - R3. The "no GC ⇒ no pool needed" misconception addressed head-on.
    - R4. A decision tree: 3-4 "use bufpool when" leaves + 1-2 anti-leaves.
    - R5. *Source*: github.com/OpenHFT/Chronicle-Queue ; chronicle.software/queue ; Peter Lawrey blog.
  - **Output**: `crates/bufpool/docs/why_pool_in_rust.md`.

  #### Phase 2 — Failing assertions (~20 min)
  - [ ] `doc_three_sections` *(NEWBIE)* — Chronicle / bufpool / shared sections all present.
  - [ ] `doc_numbers_with_sources` *(SENIOR)* — every numeric claim has a citation (test greps for a number
    without an adjacent source marker and fails).
  - [ ] `doc_addresses_no_gc_misconception` *(SENIOR)* — an explicit paragraph rebutting "no GC ⇒ no pool".
  - [ ] `doc_decision_tree_present` *(negative-control)* — 3-4 use leaves + 1-2 anti-leaves (test counts them).

  #### Phase 3 — One failure at a time (~35 min)
  - **(SENIOR) When the doc proposes mmap'ing ALL bufpool buffers from day one:**
    - Symptom: API gets clumsy (always `&[u8]`), transmute risk, mmap setup dominates short-lived buffers.
    - Why: copied Chronicle's bytes-off-heap discipline verbatim instead of borrowing only the problem shape.
    - Fix: reserve bufpool for mmap-backed page-sized buffers; use ordinary allocations for small/short
      buffers. The Chronicle analogy informs WHICH problem, not the implementation.
    - Muscle: a mirror is for the problem shape, not the implementation language. Reapplies at: W79 mmap-queue
      (Chronicle excerpt-cursor analog), W77 aeron Image (off-heap term buffer analog).
  - **5-year failure mode** (`notes/why_pool_followup.md`): if Rust gets a stdlib slab allocator or bumpalo
    lands broadly, bufpool's reason narrows to page-cache-aware allocation (LRU-K + dirty + mmap). Trigger:
    stdlib slab stabilizes. Migration: defer raw object pooling to stdlib; keep the page-cache layer.
- [ ] Tag `bufpool v1.0.0` (bar (c) audit: zero-alloc hot path + static-memory + loom-clean before tagging).
  Update `eth-storage-cache` to v0.2.0 depending on `bufpool`. (~10 min)
- [ ] Commit + log (Saturday). (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
