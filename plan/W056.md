# Week 56 — Test vector push + [NEW] `consensus-raft` v0.1: election

> **Phase**: 4
> **Month**: M14
> **Track**: Multi
> **Hours target**: 30h/wk (M1-M18)
> **Mirror target**: exec-vm test-vector convergence (Ethereum execution-spec-tests) + Layer-4 `consensus-raft` v0.1 — mirrors etcd-io/raft (`raft/raft.go`) + tikv/raft-rs (`src/raft.rs`); election RPCs per Ongaro Figure 2
> **Crate(s) touched**: exec-vm (vX → vY, test-vector convergence); `crates/consensus-raft/` [NEW] v0.1
> **Bar**: (c)   ← `consensus-raft` is on the HFT critical path (matching-engine failover) and is a Layer-4 consensus substrate. exec-vm work this week is bar (b) maintenance.
> **Feeds into**: W59 `consensus-raft` v0.3 (log replication consumes `RaftState` + `Role::Leader` + `LogStorage`), W62 membership change (consumes transition methods), W67 `consensus-raft` v1.0 (pre-vote + snapshots), W74 matching-engine v1.0 (raft-replicated order book; demands `election_timeout ≤ 30 ms`), W90 ledger-deterministic / consensus-vsr (state-machine + persist-before-ack discipline inherited).
> **References** (consult when stuck — NOT scheduled as cover-to-cover tasks):
>   - Ongaro & Ousterhout, "In Search of an Understandable Consensus Algorithm (Extended)" (2014) — §5.2 (election), §5.3 (conflict-index fast catchup), §5.4.1 (election restriction), Figure 2 (state + RPC summary), Figure 3 (invariants). Ongaro thesis §9.6 (Pre-Vote) for the W67 extension.
>   - tikv/raft-rs `src/raft.rs::{tick, campaign, step_candidate, step_leader}`; etcd-io `raft/raft.go::Raft::campaign`, `raft/node.go` — production reference for handler ordering.
>   - alloy-rlp encode/decode derive + `bytes::Bytes::slice` (zero-copy framing) for `rpc.rs`.
> **v2 modifications**: none

**[NEW] crate created**: `crates/consensus-raft/`. Layer-4 consensus primitive, **bar (c)**. Inherited by matching-engine v1.0 (W74) and mini-db's distributed mode stub (W99-100, scope-trimmed).

**Monday — Ethereum tests integration + raft paper internalize**

- [ ] **Maintain**: `crates/exec-vm/tests/` — wire the execution-spec-tests fixture set into the exec-vm harness (download + index fixtures, add a `#[test]` that iterates them). (~30 min)
  - *Mental model*: exec-vm (bar (b)) is the Reth-track EVM; convergence on the official test vectors is the week's correctness gate (Sunday acceptance = majority green). This is the consumer of the trie + receipt + log work from prior weeks.
  - **Output**: `cargo test -p exec-vm --test specvectors` runs and reports a pass count; the failing-vector list is captured for Tue/Wed triage.
- [ ] **Internalize** the Raft state model into `crates/consensus-raft/notes/56_raft_paper.md` (Ongaro Figure 2 + §5; consult the paper as a lookup, do NOT read cover-to-cover). (~45 min)
  - 🎯 **Hardened acceptance**: `notes/56_raft_paper.md` exists and contains (a) the Figure-2 state table reproduced, (b) the 3 RPC signatures, (c) the 5 invariants verbatim, (d) the §5.4.1 election-restriction rule written as a boolean expression, (e) the randomized-timeout rationale in one sentence. Self-quiz: cover the doc and re-derive the 4 ordered RequestVote rules from memory.
  - *Mental model*: Raft is the canonical distributed-consensus protocol (etcd, Consul, CockroachDB, TiKV, redpanda, tigerbeetle all run Raft variants). This note is the spec the Tue–Thu Build exercises are tested against; getting Figure 2 + §5.4.1 wrong here propagates into every later raft week (W59/W62/W67) and the matching-engine (W74).
  - **Design** (Raft state machine, Figure 2):
    ```text
    Persistent state (on all servers, must survive crash):
      current_term: u64                  // latest term seen, monotonic
      voted_for: Option<NodeId>          // candidate this node voted for in current_term
      log: Vec<LogEntry>                 // command + term + index

    Volatile state (all servers):
      commit_index: u64                  // highest log entry known committed
      last_applied: u64                  // highest log entry applied to state machine

    Volatile state (leader only):
      next_index: HashMap<NodeId, u64>   // next log entry to send to each follower
      match_index: HashMap<NodeId, u64>  // highest log entry replicated on each follower

    Roles: Follower | Candidate | Leader
    ```
    **Invariants** (paper §5.4 + Figure 3):
    1. **Election Safety**: at most one leader per term.
    2. **Leader Append-Only**: leaders never overwrite/delete their log entries (only append).
    3. **Log Matching**: if two logs have an entry with same (term, index), all entries up to that index are identical.
    4. **Leader Completeness**: if entry committed in term T, present in all leaders of term > T.
    5. **State Machine Safety**: if a server has applied entry at index i, no other server applies a different entry at same i.
  - **Build steps** (today: paper-only):
    1. (~10 min) Sketch the 3 RPCs from Figure 2: RequestVote (params: term, candidate_id, last_log_index, last_log_term; returns: term, vote_granted) and AppendEntries (params: term, leader_id, prev_log_index, prev_log_term, entries, leader_commit; returns: term, success).
    2. (~10 min) Election rules: candidate increments term; votes for self; broadcasts RequestVote. On majority votes → leader. Election timeout = random 150-300 ms (asymmetric to avoid split votes).
    3. (~10 min) Election restriction (§5.4.1): voter grants vote ONLY IF candidate's log is "at least as up-to-date" as voter's (last_log_term newer OR same term + last_log_index ≥). Prevents leader-completeness violation.
    4. (~10 min) Vote semantics: voted_for is per-term. Reset to None when term increases. Persisted BEFORE replying.
    5. (~5 min) Heartbeat semantics: leader sends empty AppendEntries every ≤ election_timeout/3 to suppress follower elections.
  - **Pitfalls** (named, distributed-systems):
    - *Split votes*: 5-node cluster with synchronized timeouts → all become candidates simultaneously → split votes → no leader. **Symptom**: cluster live-locks at election. **Fix**: RANDOMIZED election timeout in `[150ms, 300ms]`. Critical: random per-NODE (each node samples independently) AND per-ATTEMPT (re-randomize on each election round).
    - *Term confusion (stale leader)*: leader at term 5 partitioned; new leader elected at term 6. Old leader rejoins, sends AppendEntries with term=5. Follower sees term 5 < 6 → reject. But naive impl might accept. **Symptom**: 2-leader scenario; conflicting commits. **Fix**: ALL RPC handlers compare term first: `if rpc.term < self.term { return Err(StaleTerm); }`. If `rpc.term > self.term`, step down to Follower immediately.
    - *Byzantine voting (vote-twice)*: a faulty follower votes for two candidates in same term. **Symptom**: two candidates each get N/2 votes; both think they won. **Fix**: voted_for persisted PERSISTENTLY (durable WAL fsync before vote-grant reply); election term tracked monotonically. Raft is NOT Byzantine-fault-tolerant (use BFT W64+ for that); but persistent voted_for prevents accidental double-vote.
    - *Pre-vote phase missing*: candidate increments term to start election → wins → discovers it's outdated → steps down. Term inflation churns useless. **Symptom**: term churn under unreliable network. **Fix**: implement Pre-Vote extension (Ongaro thesis §9.6): pre-RequestVote without incrementing term, only escalate to real RequestVote if pre-vote majority would grant.
    - *Liveness loss under asymmetric partitions*: node A reachable→B but B unreachable→A. A times out, becomes candidate, increments term repeatedly. Each successful split election forces real leader to step down. **Symptom**: cluster never makes progress despite quorum being available. **Fix**: Pre-Vote + ensure candidate can RECEIVE RequestVote replies before promoting term increment.
  - **Test scenarios** (paper-level reasoning):
    - 5-node cluster, kill leader → election within 1 timeout window. Verify majority of remaining nodes converge on same new leader.
    - Network partition splits 3-2: majority side elects leader, makes progress. Minority side cannot elect leader (no majority of original 5 reachable).
    - Reunite partition: minority's old leader (still at lower term) discovers higher term via heartbeat, steps down.
  - **Output**: `notes/56_raft_paper.md` committed; pre-vote variant noted as a future (W67) extension.
  - **Downstream**: every subsequent raft week reads this — Wed (state machine), Thu (election RPCs), W59 (log replication), W62 (membership change), W67+ (raft v1.0).

**Tuesday — General state tests + raft scaffold**

- [ ] **Maintain**: `crates/exec-vm/` — run the GeneralStateTests subset; triage from Monday's failing-vector list. (~30 min)
  - **Output**: `cargo test -p exec-vm --test specvectors generalstate` failing count strictly lower than Monday's; diffs noted in `notes/56_specvector_triage.md`.
- [ ] **Build (crate-scaffold)**: `crates/consensus-raft/Cargo.toml` workspace member. (~20 min)
  - *Mirror*: etcd-io/raft + tikv/raft-rs crate boundary. *Deps* (workspace): `time` (HLC last-seen), `wal` (durable log + group-commit), `p2p` (transport), `txn` (state-machine integration), `eth-network-codec` (wire), `backpressure` (RPC queue), `tracing`, dev-deps `proptest`, `criterion`, `dhat`, `arrayvec`. *Downstream*: W74 matching-engine, W90 ledger. *It will hold*: the 8 modules declared in `lib.rs` below.
  - **Output**: `Cargo.toml` added to `[workspace.members]`; `cargo metadata -p consensus-raft` resolves all deps; `cargo check -p consensus-raft` green (empty crate).
- [ ] **Build**: `crates/consensus-raft/src/lib.rs` with module headers: `state.rs`, `election.rs`, `log.rs`, `rpc.rs`,
  `membership.rs`, `snapshot.rs`. (~total 200 min across 3 phases)
  - 🔧 **Why this matters**: module boundaries set the contract for downstream consumers. Mixing election logic into the RPC handler (a common production bug) makes the codebase un-debuggable. The state machine MUST be isolated from RPC wire format; log persistence MUST be isolated from in-memory volatile state. Downstream HFT consumer (W74 matching-engine, requires `election_timeout ≤ 30 ms`) and ledger (W90 VSR) both inherit this surface — get it wrong and you can't optimize without an API break.

  #### Phase 1 — Problem, requirements, design (~60 min, no code yet)

  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/consensus-raft/notes/56_lib_prediction.md` (compare to reality after tests run):
    - P1: the primitive you'd reach for to make `RaftNode` shareable across the runtime — write the exact line (`Arc<Mutex<RaftNode<S>>>`? channel? something else?).
    - P2: predicted throughput ceiling for a single leader gated by per-op fsync at ~10 µs/fsync (ops/s), and again with 100-op group commit.
    - P3: predicted outcome when 8 runtime threads concurrently call `step(rpc)` on a `Mutex<RaftState>`-wrapped node (election convergence p99?).
    - P4: predict whether `async fn propose` desugars to a heap-allocated future on the hot path (yes/no + why).
    - P5: **list every Phase-2 test you predict your first attempt will fail.**
  - *Mental model*: this is the public-surface freeze for the whole `consensus-raft` arc (W56→W67) — module boundaries decide what W74/W90 can optimize without an API break. A scaffold has no runtime cost, but a wrong `propose`/`apply_callback` signature is a permanent tax.
  - *Contract* (signature-level — NEVER the body): `pub mod {state, election, log, rpc, replication, membership, snapshot, node}`; `RaftNode<S: StateMachine>` exposes `fn tick(&mut self, now: Instant)`, `fn step(&mut self, rpc: RaftRpc)`, `fn propose(&self, cmd: C) -> ProposalId` (synchronous, non-blocking), `apply_callback: impl Fn(LogEntry)` (single-thread). `LogStorage` trait: `append`, `read`, `last_index`, `last_term`, `truncate`, `fsync` (separate from append for group commit).
  - **The problem this primitive solves**: production Raft impls that conflate election + replication + state-machine (e.g., one big `Raft` struct doing everything) become un-debuggable; testing election requires a network mock, testing log replication requires fsync mock, testing apply requires state-machine mock — coupled together means none can be tested in isolation. Boundaries enforce testability.

  - **Requirements** — write in `crates/consensus-raft/notes/56_lib_design.md`:
    - R1. 8 pub modules: `state`, `election`, `log`, `rpc`, `replication`, `membership`, `snapshot`, `node`. No others publicly visible.
    - R2. `state` module owns role transitions; no other module mutates `current_term` directly.
    - R3. `log` module is the SINGLE writer to the durable WAL via `wal::Wal`. Other modules read via `LogStorage` trait.
    - R4. `rpc` module: no logic, just wire format + codec via `eth-network-codec`.
    - R5. `node` module glues everything; only async-aware module in the crate.
    - R6. `propose(cmd: C) -> ProposalId` is NON-BLOCKING and returns immediately; caller polls. NEVER takes a `&Mutex<RaftState>` — single-thread apply.
    - R7. `apply_callback: Fn(LogEntry)` invoked from the apply loop on a SINGLE thread (no internal locks needed for state machine).
    - R8. Workspace deps: `wal`, `p2p`, `time`, `txn`, `eth-network-codec`, `backpressure`, `tracing`.

  - **Design walk-through**:
    - D1. Why `propose` non-blocking instead of `async fn propose -> Result<()>`? Walk through the bar-(c) failure: at 10k ops/s, blocking propose = 10k contended futures = task scheduler trash. Non-blocking propose returns an ID; caller polls via `apply_callback` or a `JoinHandle`-style future from a SHARED waker pool.
    - D2. Why single-threaded apply (no `Arc<Mutex<S>>`)? Mutex cost (~25 ns uncontended, ~500 ns contended) × every apply = the throughput killer. Design the dispatch: apply runs on its own thread; commands cross via channel.
    - D3. `LogStorage` trait surface: `append(entry) -> Lsn`, `read(idx) -> Option<LogEntry>`, `last_index()`, `last_term()`, `truncate(after)`, `fsync()`. Why `fsync` separate from `append`? (Group commit — multiple appends share one fsync.)
    - D4. `replication.rs` vs `election.rs` boundary: replication updates `next_index`/`match_index` per follower; election handles term + vote. Where does heartbeat live? (Hint: in `replication.rs` — heartbeat IS empty AppendEntries; sharing the dispatch path.)
    - D5. **Pre-mortem (load-bearing)**: a downstream wraps `RaftNode<S>` in `Arc<Mutex<RaftNode<S>>>` for "ease of use." Write the EXACT 4-5 line bad interleaving (thread A holds the mutex inside `step(append_entries)` awaiting fsync → threads B..H block on `step(request_vote)` → election timer fires but the timeout handler also needs the mutex → convergence p99 jumps from 20 ms to 500 ms).
    - D6. **Anti-shortcut audit**: for each motivation name the obvious reach and why it's wrong HERE — (a) "make it Send+Sync" → `Arc<Mutex<RaftState>>` (serializes every RPC; wrong, use single-writer + channel); (b) "easy async propose" → `async fn propose -> Result` (heap future per call; wrong, return `ProposalId` synchronously); (c) "durable vote" → `wal.fsync()` per call (no group commit; wrong, use `append_and_fence`).
  - **Output**: `notes/56_lib_design.md` with R1–R8 + answers to D1–D6 + the bad-interleaving sketch + module-deps DAG.

  #### Phase 2 — Write the failing tests (~50 min)
  Each is engineered so a naive "one big `Arc<Mutex<Raft>>` struct, `async fn propose`" first attempt FAILS it. Tier + trap-shape annotated.

  - [ ] (NEWBIE) `lib_module_layout_matches_design` — `cargo doc --no-deps` parsed: 8 modules visible, no extras. Exercises R1.
  - [ ] (SENIOR) `only_state_mutates_current_term` — `grep`-scan: outside `state.rs`, no `current_term = ` or `current_term +=` writes. Exercises R2.
  - [ ] (SENIOR) `only_log_writes_to_wal` — `grep`-scan: outside `log.rs`, no `wal::Wal::append`/`fsync` direct calls. Exercises R3.
  - [ ] (SENIOR) `rpc_module_has_no_logic` — `grep`-scan: `rpc.rs` contains only `struct` and `derive` lines + impl `Encodable`/`Decodable`; no `if rpc.term`, no `match` on state. Exercises R4.
  - [ ] **(EXPERT — anti-shortcut trap)** `propose_does_not_block` — `cargo asm` on `RaftNode::propose`: no await points, no `Box::pin`/`__pin` symbol; returns `ProposalId` synchronously. The obvious `async fn propose` passes correctness but FAILS this (heap future on the hot path = bar (c) violation). Exercises R6.
  - [ ] **(EXPERT — anti-shortcut trap)** `node_has_no_mutex_on_state` — `grep`/`syn`-scan: `node.rs` contains no `Mutex<RaftState>`, `RwLock<RaftState>`, or `parking_lot::Mutex<RaftState>`. The "make it Send+Sync" shortcut passes `cargo check` but serializes all RPCs. Exercises R6, R7.
  - [ ] (NEWBIE) `workspace_compiles` — `cargo check --workspace`. Exercises R8.
  - [ ] **(EXPERT — negative-control / test-the-test)** `asm_audit_actually_inspects_propose` — sanity-check the `propose_does_not_block` harness itself: feed it a deliberately-`async` decoy fn and assert the audit REPORTS the `Box::pin` symbol (so we know a real await would be caught), then assert it finds NONE in the real `propose`. Proves the asm scan isn't silently matching empty output.

  #### Phase 3 — Make the tests pass, one failure at a time (newbie → expert) (~90 min)

  - **(EXPERT) When `node_has_no_mutex_on_state` matches `Mutex<RaftState>`:**
    - Symptom: every RPC handler takes a mutex; criterion bench shows ~50% throughput collapse at 8-thread receive load; election convergence p99 spikes from 20 ms to 500 ms.
    - Why: shared-state convenience — wrapping the whole RaftState in a mutex "to make it `Send + Sync` for the runtime" forces serialization of every read.
    - Fix: single-threaded raft loop owns `RaftState` exclusively; RPCs arrive via channel; apply runs on its own thread; cross-thread access is by message-passing, never shared memory.
    - Cost avoided: ~25 ns mutex acquire uncontended escalating to ~500 ns + cache-line bounce contended, × every RPC at 10k/s = throughput pinned to one logical CPU; election p99 20 ms → 500 ms (bar (c) violation).
    - Muscle: state-machine throughput is gated by its serialization point — one mutex on the state = one logical CPU regardless of cores; single-writer + message passing instead. Reapplies at: W74 matching-engine OrderBook (single-writer per shard), W90+ ledger replica state.

  - **(SENIOR) When `only_state_mutates_current_term` finds writes outside `state.rs`:**
    - Symptom: a "convenience" `election.rs` line does `self.persistent.current_term = rpc.term` directly.
    - Why: skipping the `become_follower(term, leader)` method bypasses the persist + role-reset side effects.
    - Fix: every term mutation goes through `state.rs::become_follower / become_candidate / become_leader`; those methods own the persist call.
    - Muscle: every safety-critical state field has a single mutation path. Reapplies at: W64+ bft view-number mutation, W90+ ledger view-change.

  - **(EXPERT) When `propose_does_not_block` shows `await` points:**
    - Symptom: `cargo asm` reveals state-machine future spilled into a `Box::pin`; downstream caller's call site allocates per propose.
    - Why: `async fn propose(&self, cmd) -> ProposalId` desugars to a future; the inner code awaits the apply-channel send.
    - Fix: `fn propose(&self, cmd) -> ProposalId` — synchronous; pushes onto a `crossbeam::channel::Sender` or a lockless queue; returns the assigned ID; caller polls completion separately.
    - Cost avoided: 1 heap alloc (~50 ns) + ~64-128 B future per propose × 10k/s = ~500 µs/s allocator time + cache pressure; eliminated to zero (bar (c) violation when on the order path).
    - Muscle: hot-path APIs are synchronous + non-blocking; async appears only at the I/O boundary. Reapplies at: W74 matching-engine `submit_order`, W76+ aeron `publish`.

  - **5-year failure mode** (write in `notes/56_lib_followup.md`): when async runtimes adopt structured-concurrency (`task::scope`), the channel-based propose model is awkward. Name the future-shape migration that lets `propose` remain non-blocking while plugging into structured-concurrency cleanly.
  - *Back-of-envelope* (bar (c) — module layout decisions cascade into how downstream HFT/ledger consume raft):
    - Scaffold itself is zero-runtime-cost; this is about freezing the right public surface. The performance ceiling for matching-engine (W74) and ledger (W90, via VSR) is set by what `RaftNode::propose(cmd)` returns and how `apply_callback` is dispatched.
    - Decision today: `propose(cmd: C) -> ProposalId` is non-blocking and returns immediately (caller polls); `apply_callback: Fn(LogEntry)` is called from the apply loop on a SINGLE thread (no Mutex on the state machine). This pre-empts the "mutex-around-state-machine" trap that kills throughput.
    - The wal sync (in `log.rs`) is the throughput ceiling: at ~10 µs/fsync on NVMe, raft tops out at ~100k ops/s/leader without group commit; with batching at 100 ops/sync → ~1M ops/s ceiling. Module layout must NOT force per-op sync at the API boundary.
  - **Design**:
    ```rust
    // crates/consensus-raft/src/lib.rs
    pub mod state;       // Role, Persistent, Volatile state + transitions
    pub mod election;    // election_timer, RequestVote handling, vote-granting rules
    pub mod log;         // LogEntry, LogStorage trait, wal-backed impl
    pub mod rpc;         // RequestVote / AppendEntries wire types + codec
    pub mod replication; // leader-side: next_index/match_index, AppendEntries dispatch (W59)
    pub mod membership;  // joint-consensus change (W62)
    pub mod snapshot;    // log compaction (W67+)
    pub mod node;        // top-level RaftNode glue

    pub use state::{Role, RaftState};
    pub use rpc::{RequestVote, AppendEntries, RaftRpc};
    pub use log::{LogEntry, LogStorage};
    ```
    **Invariants**:
    - `state` module owns role transitions; no other module mutates `current_term` directly.
    - `log` module is the SINGLE writer to the durable WAL via `wal::Wal`.
    - `rpc` module: no logic, just wire format + codec via `eth-network-codec`.
    - `node` glues everything; it's the only async-aware module.
  - **Build steps**:
    1. (~30 min) Lib.rs module declarations + re-exports.
    2. (~30 min) Stub each module with `//! TODO` docstring and empty types.
    3. (~30 min) Cargo.toml deps: `wal` (durable log), `p2p` (transport), `time` (HLC for last-seen), `txn` (state-machine integration), `eth-network-codec` (wire), `backpressure` (RPC queue), `tracing`.
    4. (~30 min) Top-level `RaftNode<S: StateMachine>` glue type with `tick()`, `step(rpc)`, `propose(cmd)` skeleton signatures (impl lands Wed-Thu).
    5. (~30 min) `cargo check --workspace` green.
  - **Acceptance**: workspace builds; module boundaries committed.
  - **Downstream**: all subsequent raft work fits into these slots.
- [ ] Commit + log (~10 min)

**Wednesday — More failure fixing + raft state machine skeleton**

- [ ] **Maintain**: `crates/exec-vm/` — continue test-vector triage from Tuesday's list. (~30 min)
  - **Output**: `cargo test -p exec-vm --test specvectors` failing count down again; remaining failures categorized (precompile / gas / trie) in `notes/56_specvector_triage.md`.
- [ ] **Build**: `crates/consensus-raft/src/state.rs` — `Role` enum (Follower, Candidate, Leader).
  `Persistent { current_term, voted_for, log }` and `Volatile { commit_index, last_applied }`. (~total 200 min across 3 phases)
  - 🔧 **Why this matters**: state separation (persistent vs volatile) IS the safety boundary. Mixing them = data loss on crash. Persistent fields MUST be fsync'd before any RPC reply that depends on them — `voted_for` lost across a crash means double-voting in the same term means TWO leaders means committed data lost. W59 replication, W62 membership, W67+ snapshots, W74 matching-engine, and W90 ledger ALL build on this state machine — get the persistence boundary or the role-transition reset wrong and the entire stack is unsound.

  #### Phase 1 — Problem, requirements, design (~60 min, no code yet)

  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/consensus-raft/notes/56_state_prediction.md`:
    - P1: the primitive you'd reach for to store `next_index`/`match_index` per peer — exact import line (`HashMap<NodeId, u64>`? `ArrayVec`? `Vec`?).
    - P2: predicted fsync latency for `persist_term_and_vote` on NVMe vs SATA SSD (ns/µs), and the resulting election-RTT floor.
    - P3: predicted outcome of a crash injected AFTER `voted_for = Some(x)` but BEFORE the fsync completes, then recovery + a same-term RequestVote from a different candidate.
    - P4: predict allocs/call for `become_leader` if it does `HashMap::new()` for both maps on a 7-node cluster (count the HEAP allocs — note that `HashMap` inserts do NOT each malloc; only the backing-table allocation does — plus count the cold first-touch writes separately).
    - P5: **list every Phase-2 test you predict your first attempt will fail.**
  - *Mental model*: `RaftState` is the single source of truth for one replica. The persistent/volatile split IS the crash-safety boundary: lose `voted_for` across a crash → double-vote in one term → two leaders → committed data lost. This state machine is consumed by election (Thu), replication (W59), membership (W62), snapshot (W67), matching-engine (W74), ledger (W90).
  - *Contract*: `Persistent { current_term: u64, voted_for: Option<NodeId> }` (fsync'd before dependent reply); `Volatile { commit_index: u64, last_applied: u64 }` (never persisted, `last_applied ≤ commit_index`); `enum Role { Follower{..}, Candidate{..}, Leader{ next_index, match_index, .. } }` mutated ONLY via `become_follower(term, leader)` / `become_candidate()` / `become_leader()`. `current_term` read is a bare u64 load, never behind a lock.
  - **The problem this primitive solves**: a vote-before-fsync ack creates a window where the candidate believes its vote was granted but the disk doesn't yet know — crash there, recovery forgets the vote, the same term grants a different vote → 2 candidates each get majority. Election convergence under churn collapses when every fsync is sync-per-call (no group commit). Role-transition that re-allocates HashMaps on every churn cycle adds GC-style jitter.

  - **Requirements** — write in `crates/consensus-raft/notes/56_state_design.md`:
    - R1. `Persistent { current_term: u64, voted_for: Option<NodeId> }` FSYNC'd before any RPC reply or election step that depends on them.
    - R2. `Volatile { commit_index: u64, last_applied: u64 }` — never persisted; `last_applied ≤ commit_index` invariant always holds.
    - R3. `Role` enum (Follower/Candidate/Leader) with per-state fields; transitions go through `become_follower(term, leader)` / `become_candidate()` / `become_leader()` methods exclusively.
    - R4. `term` monotonically non-decreasing across ALL transitions; `commit_index` monotonically non-decreasing.
    - R5. Legal transitions: Follower↔Candidate; Candidate→Leader (majority votes) or →Follower (higher term); Leader→Follower (higher term). NEVER Leader→Candidate directly.
    - R6. `become_leader` pre-allocated `next_index` and `match_index` (ArrayVec or with_capacity HashMap) — no realloc on role transition.
    - R7. Persist call uses the wal layer's GROUP-COMMIT path; never bypass with a sync-per-call fsync.
    - R8. Hot read (`current_term` in RPC handler): single u64 load ≤ 5 ns; NEVER wrapped in `Mutex`.

  - **Design walk-through**:
    - D1. Why is fsync-before-reply load-bearing for safety? Walk the bad interleaving: (1) handler grants vote, (2) handler returns reply over the wire, (3) async fsync queued, (4) crash, (5) recovery sees voted_for=None, (6) different candidate requests same term, (7) handler grants again. Two votes in one term → two leaders.
    - D2. `Role::Leader { next_index: HashMap<NodeId, u64> }`: at 7 nodes, what's the cost of `HashMap::new` + 7 inserts vs `ArrayVec<(NodeId, u64), 16>` + 7 pushes? When (cluster size N) does HashMap start winning?
    - D3. Group-commit at the wal layer: write the API. `wal.append_and_fence(bytes) -> impl Future<Output = ()>` — the future resolves when this byte's batch's fsync completes. Why does the raft code MUST NOT call `wal.fsync` directly?
    - D4. `commit_index` regression scenario: a snapshot install that mistakenly sets `commit_index = snapshot.last_index` can DECREASE the value if `snapshot.last_index < current_commit_index`. Write the guard.
    - D5. **Pre-mortem (load-bearing)**: under partition healing, leadership churns 10×/minute. Write the EXACT 4-step path from "HashMap::new in `become_leader`" to "1.4 KB allocated per role transition × 600 transitions/hour = ~840 KB allocator churn + allocator-lock shadow on sibling raft threads."
    - D6. **Anti-shortcut audit**: (a) "durable vote" → `wal.fsync()` per call (no group commit → election-RTT floor inflates and iostat queue spikes under churn; wrong, use `append_and_fence`); (b) "per-peer index map" → `HashMap::new()` in `become_leader` (re-alloc every churn cycle; wrong, pre-alloc + `.clear()`); (c) "make state shareable" → `Mutex<RaftState>` (kills the bare-u64 `current_term` read; wrong, single-writer).
  - **Output**: `notes/56_state_design.md` with R1–R8 + answers to D1–D6 + the bad-interleaving sketch + state-transition diagram with persist+reset side-effects on each edge.

  #### Phase 2 — Write the failing tests (~50 min)
  Engineered to trap a naive "fsync-per-call, `HashMap::new` per transition, reply-before-persist" first attempt. Tier + trap-shape annotated.

  - [ ] **(EXPERT — anti-shortcut trap)** `vote_before_fsync_unsafe` — inject crash between `voted_for = Some(x)` and `persist()`: recovery sees `voted_for = None` (vote lost; the safe outcome). Now flip: inject crash between `persist()` return and reply send: recovery sees `voted_for = Some(x)`, and a subsequent vote-attempt for a different candidate in the same term is REJECTED. Exercises R1.
  - [ ] **(SENIOR — anti-shortcut trap)** `term_monotonic` — NOT just a property over `become_*` calls (those only ever raise term, so a naive impl passes by accident). Tighten it two ways: (a) feed `step(rpc)` a *fuzzed, out-of-order* stream of RPCs including ones carrying `rpc.term < self.current_term`, and assert `current_term` never decreases across the whole stream; (b) assert at the type level that `current_term` has no public setter and is only reachable through `become_*` / `step` — a `compile_fail`/`grep` check that no `pub fn set_current_term` or external `state.persistent.current_term = ` write exists. A first attempt that exposes the field or that lets a stale-term `step` clobber the term FAILS (a). Exercises R4 + R2.
  - [ ] (SENIOR) `commit_index_monotonic` — property: never decreases across snapshot install or replication update. Exercises R4.
  - [ ] (NEWBIE) `last_applied_le_commit_index` — invariant assertion in apply loop. Exercises R2.
  - [ ] **(EXPERT — anti-shortcut trap)** `become_leader_zero_alloc` — `dhat` over 1000 `become_leader` calls: zero allocs after warmup (pre-allocated maps). The `HashMap::new()` reach passes correctness, FAILS this. Exercises R6.
  - [ ] **(EXPERT — cross-primitive trap)** `state_uses_group_commit_wal` — composes `state.rs` with the W26 `wal` group-commit path: `grep`/`syn`-scan asserts `state.rs` calls `wal.append_and_fence` (group-commit), NOT `wal.fsync` directly. `persist_term_and_vote` is correct in isolation but false-shares the fsync queue when it bypasses group commit. Exercises R7.
  - [ ] **(EXPERT — anti-shortcut trap)** `current_term_read_under_5ns` — criterion `iter_custom`: 1M reads of `state.persistent.current_term`; p50 ≤ 5 ns. Goes red the instant `RaftState` is wrapped in a lock. Exercises R8.
  - [ ] (SENIOR) `no_mutex_on_state` — `compile_fail` / `grep`-scan: code that wraps `RaftState` in `Mutex`/`RwLock` must not exist in the crate. Exercises R8.
  - [ ] (SENIOR) `transition_resets_role_specific_fields` — leader→follower transition clears `next_index`/`match_index` (or marks them stale); candidate→follower clears `votes_received`. Exercises R3.
  - [ ] (SENIOR) `direct_leader_to_candidate_forbidden` — `compile_fail` or runtime assert: no method `Leader::become_candidate()` exists. Exercises R5.
  - [ ] **(EXPERT — negative-control / test-the-test)** `crash_injector_actually_crashes` — prove the crash-injection harness used by `vote_before_fsync_unsafe` actually severs durability: inject a crash on a KNOWN-persisted value and assert recovery returns it; inject a crash on a KNOWN-unpersisted value and assert recovery returns the OLD value. If both return the same thing, the injector is a no-op and `vote_before_fsync_unsafe` proves nothing.

  #### Phase 3 — Make the tests pass, one failure at a time (newbie → expert) (~90 min)

  - **(EXPERT — perf, bar (c) violation) When `state_uses_group_commit_wal` finds direct `wal.fsync` calls and election convergence p99 spikes to 200+ ms under churn:**
    - Symptom: election convergence p99 jumps from ~20 ms (steady) to 200+ ms during partition churn; `iostat -x 1` shows the wal device at 100% util with `avgqu-sz` climbing; the entire raft thread stalls on fsync queue.
    - Why: `persist_term_and_vote(term, vote)` calls `wal.append + wal.fsync` synchronously per RPC handler invocation. On NVMe (~10 µs/fsync) the leader election RTT floor is ~3 × 10 µs = 30 µs base; on SATA SSD it's ~600 µs; under churn the fsync rate climbs and stalls.
    - Fix: group-commit at the `wal` layer (W26 design). Batch concurrent `persist_term_and_vote` calls into a single fsync; callers await a per-batch `Notify`. The wal layer supports this — `state.rs` must NOT bypass with `wal.fsync` "convenience" calls.
    - Cost avoided: ~10 µs/fsync × (batch_size - 1) per election round. For a 7-node cluster batching all 7 vote-state writes into one fsync = ~60 µs saved per election; under churn (100 elections/s peak) = ~6 ms/s CPU, but more importantly removes the iostat queue-depth spike.
    - Muscle: fsync is amortizable; batch concurrent durability requests behind one fsync. The application layer expresses "MUST be durable before reply"; the wal layer chooses WHEN to actually call fsync. **Reapplies at**: W59 raft AppendEntries log persistence (group-commit per heartbeat), W83+ ledger journal writes via io_uring, W90+ ledger checkpoint flush, W74 matching-engine durable command log.

  - **When `become_leader_zero_alloc` reports allocs per call under churn:**
    - Symptom: `dhat` shows ~2-4 allocs per `become_leader` on a 7-node cluster (the two `HashMap::new()` backing-table allocations + occasional grow; individual inserts do NOT each heap-alloc), plus N cold-memory writes to first-touch the buckets; under chaos testing with network jitter (`become_leader` firing ~100×/min), allocator churn + cold-page faults visible in `perf stat`; allocator-lock contention shadow on other raft threads.
    - Why: each leadership transition allocates fresh `HashMap::new()` for `next_index` and `match_index`; the first insert into each forces a backing-table allocation (~2-4 allocs total across both maps as they grow), and every transition cold-writes N entries.
    - Fix: at `RaftNode::new`, pre-allocate `next_index: HashMap::with_capacity(MAX_PEERS)` and `match_index: HashMap::with_capacity(MAX_PEERS)` (or `ArrayVec<(NodeId, u64), MAX_PEERS=16>` for small clusters). `become_leader` calls `.clear()` + re-populates values; no realloc.
    - Cost avoided: ~2-4 allocs × ~50 ns + N cold-memory writes (~1 KB first-touch) per transition × ~100 transitions/min under stress = small in steady, large in tail; eliminates allocator-lock contention shadow.
    - Muscle: any "transient state that resets on lifecycle event" is a candidate for in-place reset over realloc. Pre-allocate at construction; `.clear()` on transition. **Reapplies at**: W59 raft replication scratch buffers, W64+ bft `voted_blocks_per_view`, W74 matching-engine session-level state on reconnect, W90+ ledger view-change buffers.

  - **When `vote_before_fsync_unsafe` reproduces split-brain:**
    - Symptom: simulated crash between `persist_term_and_vote` queueing the write and the disk syncing reveals `voted_for = None` on recovery; same term then grants vote to a different candidate; quorum math produces 2 leaders.
    - Why: handler called `wal.append_async(vote_record)` and immediately sent the reply; recovery doesn't see the vote.
    - Fix: handler awaits the group-commit completion (`wal.append_and_fence(...).await`) BEFORE sending the reply. Reply happens after disk confirmation.
    - Muscle: persist FIRST, ack SECOND — applies to every leader-election protocol. **Reapplies at**: W64+ bft pre-commit vote, W90+ ledger view-change ack, W83+ ledger journal commit.

  - **When `commit_index_monotonic` fires after a snapshot install:**
    - Symptom: a snapshot at `snapshot.last_index = 100` installed when `commit_index = 150` causes commit_index to drop to 100; state machine "unapplies" entries.
    - Why: snapshot install code mistakenly writes `volatile.commit_index = snapshot.last_index` without guard.
    - Fix: `volatile.commit_index = volatile.commit_index.max(snapshot.last_index)`; OR explicit assert `assert!(snapshot.last_index <= volatile.commit_index)` and reject snapshot install if violated.
    - Muscle: monotonic invariants need explicit guards or .max() guards at every mutation site. Reapplies at: W67+ snapshot install, W90+ ledger checkpoint install.

  - **(SENIOR) When `term_monotonic` regresses on the fuzzed out-of-order RPC stream:**
    - Symptom: the fuzzer replays a stale-term `step(rpc)` (rpc.term < current_term) after a higher term was already seen; `current_term` drops back to the stale value; the type-level check also flags a `pub` field write from a test helper.
    - Why: the handler assigned `self.persistent.current_term = rpc.term` unconditionally (or the field is `pub` and a "convenience" path writes it), instead of only stepping UP via `become_follower(higher_term, _)` and rejecting `rpc.term < current_term` outright.
    - Fix: make `current_term` private with the only mutation path being `become_*`; in every handler, `if rpc.term < self.current_term { return reject; }` BEFORE any term write, and `if rpc.term > self.current_term { become_follower(rpc.term, None); }`.
    - Muscle: a monotonic safety field needs BOTH a single private mutation path AND a guard that rejects regressions at every entry point — encapsulation alone isn't enough if the one writer can still go backwards. Reapplies at: W64+ bft view-number, W90+ ledger op-num.

  - **When `direct_leader_to_candidate_forbidden` reports the transition is reachable:**
    - Symptom: a leader receives an `election_timer_fired` event and directly calls `become_candidate`; bypasses the term-step-down rule.
    - Why: missing role-check in `tick`'s timer-fired branch.
    - Fix: `tick` matches on `self.role` first: if `Leader`, election timer firing is meaningless (we ARE the leader); silently ignore. Candidate-only path goes through `become_candidate`. To go Leader→Candidate, you MUST first see a higher term and `become_follower(higher_term, None)`.
    - Muscle: state transitions are a typed graph; every edge has a method, no edge is implicit. Reapplies at: W64+ bft view transitions, W74 matching-engine session lifecycle.

  - **5-year failure mode** (write in `notes/56_state_followup.md`): when persistent-memory (Optane-style) replaces SSD as the durability layer, fsync latency drops from ~10 µs to ~100 ns; group-commit's batch size shrinks and the perf tradeoff inverts. Name the migration: how does the wal layer detect the underlying device and adjust batch policy?
  - *Back-of-envelope* (bar (c) — every RequestVote handler + every leader heartbeat reads/writes RaftState):
    - Workload: 7-node cluster (HFT-typical for failure resilience without overhead), heartbeat 50 ms, election timeout 200-400 ms ⇒ heartbeats: 20/s × 6 followers = 120 sends/s on leader; AppendEntries on a busy node: ~10k ops/s during catchup.
    - `persist_term_and_vote`: 2 × u64 + 1 × Option<NodeId> = ~24 B written, but fsync is the killer — **~10 µs on enterprise NVMe *with a power-loss-protected (PLP) write cache*, ~100-200 µs on SATA SSD, ~1-10 ms on spinning disk**. The 10 µs figure is the aggressive end and assumes PLP; on a commodity NVMe without PLP, fsync is closer to the jeff-dean ~300 µs table value because the device must actually flush to NAND. This sets the election RTT floor; you cannot vote faster than your fsync, so the floor is device-dependent — measure it on the target hardware, don't assume 10 µs.
    - Role transition (`become_follower/candidate/leader`): updates `Role` enum (32-64 B), resets HashMaps; ≈ ~50 ns when maps are pre-allocated; ≈ 500-1000 ns + ~50 ns/peer malloc if HashMaps are freshly allocated on every transition. At 1 transition/s steady-state, this is negligible; under leadership churn (network blips) it spikes.
    - Memory: `RaftState` ≈ ~1-4 KB (depends on log handle + state_machine size); easily fits in L1.
    - Hot path read (`current_term` check in every RPC handler): single u64 load ≈ **0.5-5 ns** depending on cache state. Goes red only if behind a `Mutex<RaftState>` — see perf pitfall.
  - **Design**:
    ```rust
    pub enum Role {
        Follower { leader_id: Option<NodeId>, last_heartbeat: Instant },
        Candidate { votes_received: HashSet<NodeId>, election_started_at: Instant },
        Leader {
            next_index: HashMap<NodeId, u64>,
            match_index: HashMap<NodeId, u64>,
            last_heartbeat_sent: Instant,
        },
    }

    pub struct Persistent {
        pub current_term: u64,
        pub voted_for: Option<NodeId>,
        // log lives in log::LogStorage (wal-backed); persistent owns only its WAL-sync handle
    }

    pub struct Volatile {
        pub commit_index: u64,
        pub last_applied: u64,
    }

    pub struct RaftState<S> {
        pub node_id: NodeId,
        pub role: Role,
        pub persistent: Persistent,
        pub volatile: Volatile,
        pub log: LogStorage,
        pub state_machine: S,
        pub config: RaftConfig,
    }
    ```
    **Invariants**:
    - `persistent.current_term` and `persistent.voted_for` are FSYNC'd before any RequestVote response or election step.
    - `volatile.commit_index` monotonically non-decreasing.
    - `volatile.last_applied ≤ volatile.commit_index` ALWAYS.
    - Role transitions: Follower → Candidate (election timeout); Candidate → Leader (majority votes) or → Follower (higher term seen); Leader → Follower (higher term seen). NEVER: Leader → Candidate directly.
  - **Build steps**:
    1. (~30 min) Role enum with per-state fields.
    2. (~30 min) Persistent struct + wal-backed persist method. Critical: `persist_term_and_vote(&mut self, term, vote) -> Result<(), Error>` must FSYNC before returning.
    3. (~30 min) Volatile struct (no persistence needed).
    4. (~30 min) RaftState top-level + `become_follower(term, leader)`, `become_candidate()`, `become_leader()` methods. Each transition must call `persist_term_and_vote` if term advances.
    5. (~20 min) Tests: simulate crash mid-vote → recovery rejects re-voting in same term (voted_for persisted).
    6. (~10 min) Property: term monotonic across role transitions.
  - **Pitfalls**:
    - *vote-before-fsync*: replying yes to RequestVote before voted_for is on disk → crash → vote forgotten → can vote for different candidate same term → 2 leaders. **Symptom**: split-brain after crash. **Fix**: persist first, ack second.
    - *commit_index regression*: bug allowing commit_index to decrease (e.g., during snapshot install) violates SS Safety. **Symptom**: state machine applies entry then "unapplies". **Fix**: explicit monotonic guard `assert!(new >= self.volatile.commit_index)`.
    - *Role-transition skip*: directly mutating role without via transition method → forgetting to reset volatile leader fields. **Symptom**: stale next_index from previous leadership. **Fix**: all transitions go through `become_*` methods that reset role-specific fields.
    - **Expect to hit (perf) #1 — per-vote fsync without group commit (bar (c) violation)**: `persist_term_and_vote(term, vote)` calls `wal.append + wal.fsync` synchronously per RPC handler invocation. On NVMe (~10 µs/fsync) the leader election RTT floor is ~3 × 10 µs = 30 µs (vote-self fsync + N peer-reply fsyncs); on SATA SSD it's ~600 µs; on a misconfigured fs (no fsync barrier) you get correctness loss. Worse: under churn (network instability triggering repeated elections), the fsync rate climbs and stalls the entire raft thread. **Symptom**: election convergence p99 spikes from ~20 ms to 200+ ms during partition churn; `iostat -x 1` shows the wal device at 100% util with avgqu-sz climbing.
      - Fix: group-commit at the `wal` layer (W26 design): batch concurrent `persist_term_and_vote` calls into a single fsync; callers await a per-batch `Notify`. The wal layer already supports this — the raft code must NOT bypass it with a "convenience" sync-per-call.
      - Cost avoided: ~10 µs/fsync × (batch-size - 1) per election round. For a 7-node cluster batching all 7 vote-state writes into one fsync = ~60 µs saved per election; under churn (100 elections/s peak) = ~6 ms/s CPU, but more importantly removes the iostat queue depth spike.
      - Muscle: fsync is amortizable; if you can batch concurrent durability requests behind a single fsync, do it. The application layer expresses "this MUST be durable before reply"; the wal layer chooses WHEN to actually call fsync.
      - **Reapplies at**: W59 raft AppendEntries log persistence (group-commit per heartbeat tick), W83+ ledger journal writes via io_uring, W90+ ledger checkpoint flush, W74 matching-engine durable command log.
    - **Expect to hit (perf) #2 — `Role::Leader { next_index: HashMap<NodeId, u64> }` re-allocates on every `become_leader` (bar (c) violation)**: each leadership transition allocates fresh `HashMap`s for `next_index` and `match_index`; under leadership churn (network partition healing + Pre-Vote off), this happens dozens of times per minute. Per transition: 2 × HashMap::new + 2 × N inserts ≈ ~100-200 ns + ~2-4 backing-table allocs. **Symptom**: `dhat` shows ~2-4 allocs per `become_leader` on a 7-node cluster (backing-table allocs, not per-insert) + N cold-page first-touch writes; under chaos testing with network jitter, allocator churn is visible in `perf stat`.
      - Fix: pre-allocate the maps once at `RaftNode::new` as `ArrayVec<(NodeId, u64), MAX_PEERS>` (MAX_PEERS=16 for the workspace) or `HashMap::with_capacity(N)` retained across role transitions; `become_leader` just resets values, no realloc.
      - Cost avoided: ~2-4 backing-table allocs × ~50 ns + N cold-memory writes (~1 KB first-touch) per transition × ~100 transitions/min under stress = small in steady state, large in tail; eliminates allocator-lock contention shadow on other raft threads.
      - Muscle: any "transient state that resets on lifecycle event" is a candidate for in-place reset over realloc. Pre-allocate at construction; reset (clear) on transition.
      - **Reapplies at**: W59 raft replication scratch buffers, W64+ bft `voted_blocks_per_view`, W74 matching-engine session-level state on reconnect, W90+ ledger view-change buffers.
  - **Test scenarios**: crash-after-vote test (persist_term_and_vote called, then crash injected before reply); recovery sees voted_for set. Same-term re-vote attempt rejected.
  - **Acceptance**: crash-recovery test green; term monotonic; commit_index monotonic.
  - **Downstream**: election + replication consume RaftState's transition methods.
- [ ] Commit + log (~10 min)

**Thursday — Validator tests + raft election RPCs + election DESIGN (Phase 1)**

> **Day-load note (G8)**: the election module is "the hardest single module to write correctly," so its arc is split across Thu→Fri. Thursday carries the `rpc.rs` Build (full 3-phase) + only **Phase 1 (design, no code)** of `election.rs`; Phases 2–3 (the trap tests + failure-card debug session) land Friday with the full focused block they need. Thu load ≈ 30 + 200 + 60 + 10 = **300 min**; Fri load ≈ 140 + 30 + 10 = **180 min**. No day exceeds ~5 h.

- [ ] Run validator test suite (`crates/consensus-engine/tests/`). (~30 min)
  - **Output**: `cargo test -p consensus-engine validator_` green; failing count (if any) noted in `notes/56_validator_triage.md` — this is the bar-(b) consensus-engine maintenance gate, not the raft work.
- [ ] **Build**: `crates/consensus-raft/src/rpc.rs` — RequestVote, AppendEntries types. (~total 200 min across 3 phases)
  - 🔧 **Why this matters**: RPC types are the wire contract — they MUST be stable across versions or rolling upgrade breaks. Schema-version bugs in Raft are silent (decode succeeds but field semantics drifted) and corrupt the cluster's view of itself. Allocation per entry-decode at 10k ops/s × 100 entries/batch is the throughput killer; layout-aware `LogEntry` placement decides whether the apply loop is L1-bound or L2-bound.

  #### Phase 1 — Problem, prediction, design (~60 min, no code yet)

  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/consensus-raft/notes/56_rpc_prediction.md` (compare to reality after tests run):
    - P1: predicted ALLOCS per RPC for the default derived `Decodable for Vec<LogEntry>` on a 100-entry AppendEntries — count the outer `Vec` allocation + per-entry alloc + per-`Bytes` wrapper alloc, and give the total (and the per-second figure at 20 RPCs/s × 6 followers).
    - P2: predicted L1-dcache-load-misses PER ENTRY for a 40 B AoS `LogEntry` (index/term/data/kind) vs a ≤ 16 B SoA `EntryHeader`, and the resulting apply-loop throughput delta in % (cache lines touched per entry: AoS vs SoA).
    - P3: predict whether alloy-rlp's *derived* `Decodable` tolerates a MISSING trailing `Option` field on a v1-encoded frame decoded by a v2 parser (yes/no + why) — this is the rolling-upgrade trap; name the exact decode error you expect if it does NOT.
    - P4: predicted encode µs for a 100 × 1 KB heartbeat-shape AppendEntries (RLP header ~50 ns + per-entry encode cost × 100); give the µs total and whether it fits the 25 µs budget.
    - P5: **list every Phase-2 test you predict your first attempt will fail.**
  - *Mental model*: `rpc.rs` is the WIRE CONTRACT for the whole raft cluster — the bytes one node emits must be decodable by every peer across a rolling upgrade, forever. Two coupled forces govern it: (a) schema evolution must be append-only-optional so vN and vN+1 nodes interoperate without a flag day, and (b) the decode/apply hot path must be zero-alloc + cache-dense or the receive path becomes the cluster's IPC bottleneck. Consumed by election (this Thu, decodes `RequestVote`), replication (W59, decodes `AppendEntries`), membership (W62), matching-engine (W74), ledger (W90).

  - **The problem this primitive solves**: a rolling upgrade where node A speaks RPCv1 and node B speaks RPCv2 must decode each other's messages OR fail cleanly — never silently drop a field. The hot-path decode at 10k ops/s × 100 entries/batch × 6 followers × 2400 allocs/RPC is the IPC bottleneck. The apply-loop scan over `entries.iter().filter(...)` is cache-line-bound: poor layout = 30% throughput loss.

  - **Requirements** — write in `crates/consensus-raft/notes/56_rpc_design.md`:
    - R1. 4 RPC types: `RequestVote`, `RequestVoteReply`, `AppendEntries`, `AppendEntriesReply`. Each carries `term: u64` as the FIRST field.
    - R2. `AppendEntriesReply` carries `conflict_index: Option<u64>` and `conflict_term: Option<u64>` for §5.3 etcd-style fast catchup.
    - R3. RLP encoding stable: schema evolution = APPEND `Option<_>` fields only; never remove or reorder.
    - R4. `RaftRpc` enum wraps all 4 types: `enum RaftRpc { Vote(...), VoteReply(...), Append(...), AppendReply(...) }`.
    - R5. Custom decode: `AppendEntries::decode_into(&mut self, frame: &Bytes) -> Result<()>` decodes ENTRIES into a borrowed `&mut Vec<LogEntry>` scratch buffer; zero alloc per RPC.
    - R6. `LogEntry` layout: split hot (index, term, kind) from cold (data payload). Hot fields packed at ≤ 16 B each in `entries_headers: Vec<EntryHeader>`; cold in `entries_data: BytesMut`. Apply-loop scans the header array only.
    - R7. Encode budget: ≤ 25 µs per heartbeat (header + 100 entries) on a 1 KB-per-entry workload.
    - R8. Decode budget: ≤ 30 µs per heartbeat including zero-copy slicing of inbound `Bytes`.

  - **Design walk-through**:
    - D1. Why RLP over protobuf/cap'n proto? Cite RLP's append-only schema-evolution discipline matches Raft's rolling-upgrade requirement. What forms of schema change are SAFE (append optional)? UNSAFE (rename, reorder, remove)?
    - D2. Conflict-index optimization: write the §5.3 alternative — when AppendEntries fails because of log mismatch, follower returns the (term, first_index_of_term) where its log diverges; leader skips next_index back to that point in one round instead of decrementing one at a time. Cost without the optimization: O(log_len) RPC rounds to converge.
    - D3. Zero-copy entry decode: write the API for `AppendEntries::decode_into(scratch: &mut Vec<LogEntry>, frame: &Bytes)`. Where does `LogEntry::data` get its bytes? (Hint: `Bytes::slice(frame, range)` shares the refcount with the inbound frame — zero copy.)
    - D4. SoA vs AoS for entries: walk the cache analysis. `Vec<LogEntry>` (AoS) with 40-byte entries = 1.6 entries per cache line; iterating to filter on `index > last_applied` touches every entry's full layout. SoA splits index/term/kind into a dense header table + data payloads in a separate buffer. At 100 entries × 10 scans/s = 1k iters/s, what's the L1-miss reduction?
    - D5. **Pre-mortem**: write the 3-step path from "alloy-rlp default `Decodable for Vec<T>`" to "2400 allocs/s/follower-leader pair under load AND 20% IPC degradation in the receive path."
  - **Output**: `notes/56_rpc_design.md` with R1–R8 + answers to D1–D5 + `LogEntry` SoA layout diagram with byte offsets.

  #### Phase 2 — Write the failing tests (~50 min)

  - [ ] `rpc_roundtrip_property` — 1000 random instances of each RPC type: encode→decode → equality. Exercises R1, R3.
  - [ ] `rpc_backward_compat_appends_optional` — encode v1 (no `conflict_index`); decode as v2: `conflict_index = None`, no error. Exercises R3.
  - [ ] `rpc_forward_compat_extra_fields` — encode v2 (with `conflict_index = Some(5)`); decode as v1: parser tolerates trailing optional bytes. Exercises R3.
  - [ ] `append_entries_decode_zero_alloc` — decode 1000 AppendEntries into pre-allocated scratch `Vec<LogEntry>`; `dhat` shows zero allocs after warmup. Exercises R5.
  - [ ] `log_entry_data_shares_frame_buffer` — assert via pointer-equality: `entries[i].data.as_ptr()` is within the inbound `frame: Bytes` range. Exercises R5.
  - [ ] `log_entry_header_size_under_16b` — `const _: () = assert!(size_of::<EntryHeader>() <= 16);`. Exercises R6.
  - [ ] `apply_loop_l1_bound` — `perf stat -e L1-dcache-load-misses,L1-dcache-loads` on 1M-iter apply scan: miss ratio ≤ 0.5 per iter (one cache line per ~2 entries). Exercises R6.
  - [ ] `bench_encode_under_25us` — criterion: heartbeat-shape AppendEntries with 100 × 1 KB entries; p50 ≤ 25 µs. Exercises R7.
  - [ ] `bench_decode_under_30us` — mirror: decode same shape; p50 ≤ 30 µs. Exercises R8.
  - [ ] `term_field_first_in_all_rpcs` — `grep`/`syn` audit: every `RaftRpc` variant's RLP layout has `term` as the first field. Exercises R1.

  #### Phase 3 — Make the tests pass, one failure at a time (~90 min)

  - **When `append_entries_decode_zero_alloc` reports ~200 allocs/RPC and `bench_decode_under_30us` p99 = 80 µs:**
    - Symptom: `dhat` shows `alloy_rlp::Decodable::decode` as a top allocator; `perf stat` shows ~20% IPC degradation in the receive path under sustained load (allocator-lock contention shadow).
    - Why: alloy-rlp's default `Decodable for Vec<T>` allocates a fresh `Vec` per RPC; per-entry `LogEntry { data: Bytes }` allocates a fresh `Bytes` (refcounted but the wrapper still allocs). For 100-entry AppendEntries: 1 outer Vec + 100 entry allocs + 100 Bytes refcells ≈ ~200 allocs per RPC × 20 RPCs/s/follower × 6 followers ≈ 24k allocs/s on a busy leader.
    - Fix: custom `AppendEntries::decode_into(scratch: &mut Vec<LogEntry>, frame: &Bytes) -> Result<()>` that clears (not drops) `scratch` between RPCs; entries decode in place. Use `bytes::Bytes::slice(frame, range)` so entry payloads share refcount with the inbound frame — zero copy.
    - Cost avoided: ~200 allocs × ~50 ns ≈ ~10 µs/RPC × 20 RPCs/s/follower × 6 followers ≈ 1.2 ms/s CPU on leader; cumulatively ~7 ms/s across cluster + the allocator-lock shadow. On hot replication paths this is 3-5% throughput.
    - Muscle: every wire codec on a hot path must support "decode into a borrowed buffer" + zero-copy slicing; the default `Decodable -> Owned` is a bar-(b) shortcut. **Reapplies at**: W55 gossip (W52 Sat perf #2), W64+ bft block-proposal decode, W76+ aeron fragment decode (mandatory there), W94 block-stm txn-set decode.

  - **When `apply_loop_l1_bound` reports ~1.5 L1-misses per entry:**
    - Symptom: criterion bench shows apply-loop ~30% slower than expected pointer-walk; `perf stat` shows ~1.5 L1-dcache-load-misses per entry; criterion `iter_custom` exposes the cache-miss tax.
    - Why: `LogEntry { index: u64, term: u64, data: Bytes, kind: u8 }` ≈ 32-40 B + padding; two consecutive entries straddle one cache line (~80 B > 64 B); iterating `entries.iter().filter(|e| e.index > last_applied)` does 2 cache-line loads per entry.
    - Fix: SoA — `entries_headers: Vec<EntryHeader { index, term, kind, data_offset, data_len }>` packed at 16 B each (4 per cache line); `entries_data: BytesMut` holds concatenated payloads. Filter/scan operates on the dense header array; data accessed only on apply.
    - Cost avoided: ~50% L1-miss reduction on apply-loop scans; at 10k ops/s × 100 entries/scan = 1M iters/s, saving ~5 ns/iter ≈ 5 ms/s CPU on the apply thread alone.
    - Muscle: data-oriented layout — when iterating to filter on a few hot fields, separate hot fields (small, dense) from cold fields (data payloads). SoA-over-AoS pattern. **Reapplies at**: W74 matching-engine `OrderBook::price_levels` (split price + qty hot from order metadata cold), W76+ aeron `Image::position_array`, W94 block-stm `txn_writeset` index.

  - **When `rpc_backward_compat_appends_optional` fails to decode v1:**
    - Symptom: v1 AppendEntries (encoded without `conflict_index`/`conflict_term`) decodes as v2 → error "unexpected end of input"; rolling upgrade breaks.
    - Why: `RlpDecodable` derive expects ALL declared fields; trailing `Option<_>` decoded eagerly.
    - Fix: use alloy-rlp's `#[rlp(skip_default)]` or hand-write the decode path: trailing `Option<_>` fields tolerate EOF and decode to `None`. Document the schema-evolution rule in the design doc.
    - Muscle: append-only schema evolution requires trailing-optional tolerance in the decoder. Reapplies at: every wire-protocol crate touched.

  - **When `term_field_first_in_all_rpcs` fails on one variant:**
    - Symptom: `RequestVoteReply` has `vote_granted` as the first field; downstream tooling (peer-debug viewer) breaks expecting `term` first.
    - Why: lazy field ordering during scaffold.
    - Fix: reorder so `term` is always first. The handler's "if rpc.term > self.current_term then step down" rule executes BEFORE any other state mutation; having `term` first lets the handler short-circuit on a partial decode.
    - Muscle: protocol invariants (like "term first") are enforced at the type-definition site, not at the handler. Reapplies at: W64+ bft `View<term, ...>` first-field, W90+ ledger op-num first-field.

  - **5-year failure mode** (write in `notes/56_rpc_followup.md`): when QUIC's stream-multiplexing replaces TCP's single-stream + framing, the wire-frame `Bytes` may be split across stream-frames; zero-copy `Bytes::slice` assumes contiguous bytes. Name the migration to `BytesChain` (cons-cell list of Bytes) that preserves zero-copy across stream boundaries.
  - *Back-of-envelope* (bar (c) — every heartbeat + every AppendEntries flows through encode/decode):
    - Workload: leader heartbeats 20/s × 6 followers = 120 wire encodes/s; under load (10k ops/s) AppendEntries can batch ~100 entries/RPC ⇒ ~100 ops × 20 batches/s × 6 followers = 12k entry-encodes/s.
    - `RequestVote` ≈ 80 B; AppendEntries header ≈ 64 B + entries (~512 B each for tx-sized commands). Per RPC: RLP encode ~50 ns header + ~200 ns/entry × 100 entries = ~20 µs encode per heartbeat. Then Noise encrypt (W54): ~700 ns + ~50 ns/KB ≈ ~5 µs for a 50 KB AppendEntries.
    - At 20 batched RPCs/s/follower × 6 followers = 120 RPCs/s; each ~25 µs encode+encrypt = 3 ms/s CPU on the leader — acceptable, but goes 10× if RLP encoding allocates per field (which the codegen sometimes does in alloy-rlp's older versions).
    - Decode mirrors encode; on the receive side, the follower's path is decode → state-machine apply. The codec must NOT allocate per entry — entries are decoded into a reused `Vec<LogEntry>` scratch buffer owned by the connection.
  - **Design**:
    ```rust
    #[derive(RlpEncodable, RlpDecodable)]
    pub struct RequestVote {
        pub term: u64,
        pub candidate_id: NodeId,
        pub last_log_index: u64,
        pub last_log_term: u64,
    }

    #[derive(RlpEncodable, RlpDecodable)]
    pub struct RequestVoteReply {
        pub term: u64,
        pub vote_granted: bool,
    }

    #[derive(RlpEncodable, RlpDecodable)]
    pub struct AppendEntries {
        pub term: u64,
        pub leader_id: NodeId,
        pub prev_log_index: u64,
        pub prev_log_term: u64,
        pub entries: Vec<LogEntry>,
        pub leader_commit: u64,
    }

    #[derive(RlpEncodable, RlpDecodable)]
    pub struct AppendEntriesReply {
        pub term: u64,
        pub success: bool,
        pub conflict_index: Option<u64>, // optimization §5.3 (skip many entries on conflict)
        pub conflict_term: Option<u64>,
    }
    ```
    **Invariants**:
    - All RPCs carry `term` first — handler MUST compare to self.current_term before processing.
    - `conflict_index/term` is an etcd-style optimization (Section 5.3 alternative): follower returns the index where its log diverges, leader skips to that point instead of decrementing next_index one at a time.
    - RLP encoding ensures cross-version compatibility (add fields by extending struct tail with `Option<_>`).
  - **Build steps**:
    1. (~30 min) Define RequestVote + Reply types with RlpEncodable derive.
    2. (~30 min) AppendEntries + Reply with conflict-index optimization fields.
    3. (~30 min) `RaftRpc` enum wrapping all: `enum RaftRpc { Vote(RequestVote), VoteReply(RequestVoteReply), Append(AppendEntries), AppendReply(AppendEntriesReply) }`.
    4. (~30 min) Property test: encode→decode roundtrip for 1000 random instances.
    5. (~30 min) Backward-compat test: encode old struct, decode new (with extra Option field) → Option = None.
  - **Pitfalls**:
    - *Schema-version breakage*: removing a field or changing type → rolling upgrade impossible. **Symptom**: new node can't decode old peer's messages. **Fix**: only APPEND optional fields; never remove or reorder.
    - *Missing conflict optimization*: without conflict_index, follower with very stale log requires O(log_len) AppendEntries rounds to catch up. **Symptom**: minutes to converge after partition heal. **Fix**: implement conflict-index/term return.
    - **Expect to hit (perf) #1 — `entries: Vec<LogEntry>` decoded per-RPC freshly (bar (c) violation)**: alloy-rlp's `Decodable for Vec<T>` allocates a fresh `Vec` and per-entry `LogEntry { data: Bytes }` allocates fresh `Bytes` (which is ref-counted but the wrapper struct still allocs). For a 100-entry AppendEntries: 1 outer Vec + 100 entry allocs + 100 Bytes refcells ≈ ~200 allocs per RPC, ~2400 allocs/s/follower-leader pair under load. **Symptom**: `dhat` shows `alloy_rlp::Decodable::decode` as a top allocator; `perf stat` shows ~20% IPC degradation in the receive path under sustained load (allocator-lock contention).
      - Fix: implement a custom decoder for `AppendEntries` that decodes into a borrowed `&mut Vec<LogEntry>` from the connection's scratch buffer (cleared, not dropped, between RPCs); use `bytes::Bytes::slice` on the wire frame's `BytesMut` so entry payloads share refcount with the frame buffer (zero-copy).
      - Cost avoided: ~200 allocs × ~50 ns ≈ ~10 µs/RPC × 20 RPCs/s/follower × 6 followers = ~1.2 ms/s CPU on the leader; cumulatively ~7 ms/s across the cluster + the allocator-lock shadow. On hot replication paths this is 3-5% throughput.
      - Muscle: every wire codec on a hot path must support "decode into a borrowed buffer" + zero-copy slicing of the wire frame; the default `Decodable -> Owned` is a bar-(b) shortcut.
      - **Reapplies at**: W55 gossip (already covered W52 Sat perf #2), W64+ bft block-proposal decode, W76+ aeron fragment decode (mandatory there), W94 block-stm txn-set decode.
    - **Expect to hit (perf) #2 — `LogEntry { data: Bytes }` straddles cache lines for index probing (bar (c) violation)**: when the follower applies entries, the hot read is `entry.index` and `entry.term` (8 + 8 = 16 B). If `LogEntry` has layout `{ index: u64, term: u64, data: Bytes, kind: u8 }` ≈ 32 B + padding = ~40 B, two consecutive entries straddle one cache line (~80 B > 64 B), so iterating `entries.iter().filter(|e| e.index > last_applied)` does 2 cache-line loads per entry instead of 1. **Symptom**: criterion bench shows apply-loop ~30% slower than expected pointer-walk; `perf stat` shows ~1.5 L1-misses per entry.
      - Fix: split into hot/cold arrays: `entries_indices: Vec<EntryHeader { index, term, kind, data_offset }>` packed at 16-24 B each (fits 3-4 per cache line) + `entries_data: BytesMut` (the concatenated payloads). Filter/scan operates on the dense header array; data accessed only when actually applying.
      - Cost avoided: ~50% L1 cache-miss reduction on apply-loop scans; at 10k ops/s × 100 entries/scan = 1M iterations/s, saving ~5 ns/iter ≈ 5 ms/s CPU on the apply thread alone.
      - Muscle: data-oriented layout — when iterating to filter on a few hot fields, separate hot fields (small, dense) from cold fields (data payloads). This is the SoA-over-AoS pattern.
      - **Reapplies at**: W74 matching-engine `OrderBook::price_levels` (split price + qty hot from order metadata cold), W76+ aeron `Image::position_array` (positions hot, subscriber-state cold), W94 block-stm `txn_writeset` index.
  - **Acceptance**: 1000-instance roundtrip green; backward-compat test green.
  - **Downstream**: election.rs (this Thu), replication.rs (W59), membership (W62).
- [ ] **Build (Phase 1 only — design, no code)**: `crates/consensus-raft/src/election.rs` — election timeout (150-300 ms randomized), vote casting, term increment. (~60 min today; Phases 2–3 = ~140 min land **Friday**)
  - 🔧 **Why this matters**: the election loop IS Raft's safety boundary. Get the randomized timeout wrong → split votes; get the vote-restriction wrong → leader completeness violation → committed data lost. This is the hardest single module to write correctly; etcd, raft-rs, hashicorp/raft each had 3+ election-related bugs in their first year. W74 matching-engine demands `election_timeout ≤ 30 ms` for HFT failover SLOs — every microsecond of fsync, clock read, or HashMap alloc compounds. Because the debug session (Phase 3) is the lesson and deserves a fresh focused block, **today is design-only**; you write zero code — you write the sealed predictions + the design doc, then sleep on it. Tomorrow you write the failing tests and walk the failure ladder.

  #### Phase 1 — Problem, requirements, design (~60 min, no code yet — TODAY)

  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/consensus-raft/notes/56_election_prediction.md` (compare to reality after Friday's tests run):
    - P1: the primitive you'd reach for to hold `votes_received` for a 7-node cluster — write the exact import line (`HashSet<NodeId>`? `ArrayVec<NodeId, 16>`? `BTreeSet`?) and your predicted per-insert latency in ns.
    - P2: predicted split-vote rate over 100 elections on 5 nodes with a FIXED 200 ms timeout vs randomized `[150, 300]` ms (give two percentages).
    - P3: predicted election-RTT floor on a sub-ms LAN, decomposed into (fsync × how many) + (encode/encrypt × how many RPCs) + (network RTT × how many) — give the µs total.
    - P4: predict whether your first `start_election` will call `Instant::now()` internally even though `tick(now)` already has the timestamp (yes/no + the ns cost if yes, using vDSO ≈ 25 ns).
    - P5: **list every Phase-2 test you predict your first attempt will fail.**
  - *Mental model*: the election module decides *who leads*; its safety rests on two coupled invariants — (a) at most one vote per node per term, durable across crash, and (b) only a node with an at-least-as-complete log may win. Break (a) → two leaders; break (b) → committed data overwritten. Consumed by replication (W59 promotes on `Role::Leader`), membership (W62), matching-engine failover (W74, `election_timeout ≤ 30 ms`), ledger VSR view-change (W90).
  - *Contract* (signature-level — NEVER the body): `fn tick(&mut self, now: Instant)`; `fn start_election(&mut self, now: Instant)`; `fn handle_request_vote(&mut self, rpc: RequestVote) -> RequestVoteReply`; `fn handle_request_vote_reply(&mut self, from: NodeId, reply: RequestVoteReply)`. Every time-aware method takes `now: Instant` — NO `Instant::now()` inside. `votes_received: ArrayVec<NodeId, MAX_PEERS=16>`, pre-allocated, `.clear()` on `start_election`.
  - **The problem this primitive solves**: synchronized election timeouts (no randomization) cause live-lock: all N nodes time out simultaneously, vote for self, no one gets majority, repeat forever. Skipping the §5.4.1 election restriction lets a candidate with stale logs become leader → committed entries from previous leader are overwritten → data loss. Clock-read overhead on a 1 kHz tick (HFT-tuned) compounds across shards.

  - **Requirements** — write in `crates/consensus-raft/notes/56_election_design.md`:
    - R1. `tick(now: Instant)` is THE entry point; takes time as a parameter (NO `Instant::now()` calls inside).
    - R2. Election timeout RE-RANDOMIZED at the start of EACH election (not just at startup): `election_timeout = random_in(150..300).milliseconds()`.
    - R3. `start_election`: term++ + voted_for=self + `persist().await` (group-commit fsync) + broadcast RequestVote.
    - R4. `handle_request_vote` enforces 4 rules in order: (1) higher term → step down, (2) lower term → reject, (3) voted_for must be None or candidate, (4) election restriction (§5.4.1): `last_log_term > self.log.last_term() OR (eq AND last_log_index >= self.log.last_index())`.
    - R5. `handle_request_vote_reply`: if reply.term > current_term → step down; collect vote; promote on majority `votes > peers / 2 + 1`.
    - R6. Persist (term + voted_for) BEFORE reply on vote grant; uses group-commit wal path.
    - R7. `votes_received` is `ArrayVec<NodeId, MAX_PEERS=16>` with linear-scan dedup; NOT `HashSet`.
    - R8. Heartbeat interval ≤ election_timeout / 3 (e.g., 60 ms heartbeat for 200 ms election) — prevents jitter-induced spurious elections.
    - R9. Hot path budgets: tick (steady state) ≤ 30 ns/role; election RTT (LAN) ≤ ~150 µs floor (2× group-commit fsync).

  - **Design walk-through**:
    - D1. Why is randomized timeout load-bearing? Walk the live-lock: 5 synchronized followers, all hit timeout at T=200 ms, all become candidates at term T+1, all vote self, no majority, election timer re-fires at T=400 ms, repeat. With randomization in [150, 300], the variance breaks the lockstep.
    - D2. The §5.4.1 election restriction: write the bad-case scenario where granting vote to a shorter-log candidate violates leader completeness. (Hint: committed entry at index 100 on majority; candidate at index 50 with same term wins; new leader doesn't have entry 100; future ops at index 100 conflict and overwrite.)
    - D3. `votes_received: ArrayVec<NodeId, 16>` vs `HashSet<NodeId>`: at N=7, `HashSet::insert` is ~50 ns + ~50 ns alloc on first; linear-scan over 7 ArrayVec elements is ~5 ns. When does HashSet start winning? (~N=64.)
    - D4. Clock reads on a 1-kHz tick wheel × 16 shards: a 25 ns vDSO read per `Instant::now()` × 4 redundant calls per tick = 1.6 µs/ms = 0.16% CPU just on clocks. How does passing `now: Instant` as a parameter eliminate this?
    - D5. Group-commit on vote persist: walk the case where TWO concurrent vote-grants from different RPC handlers can batch into a single fsync. What's the latency cost (microseconds added) vs the throughput gain (fsync-per-batch instead of fsync-per-call)?
    - D6. **Pre-mortem**: write the 5-step lost-data scenario from "election restriction bypassed" to "committed entry overwritten on partition heal."
    - D7. **Quorum-arithmetic margin (discover this yourself under tutoring mode)**: the design sketch below promotes on `votes_received.len() > self.peers().len() / 2 + 1`. Before you copy it, derive majority for a 5-node cluster from first principles on paper: majority of 5 = 3. Note that `peers()` may or may not include self, and that `> n/2 + 1` vs `>= n/2 + 1` vs `> n/2` differ by one. Write the correct expression in your design doc and a one-line proof (e.g. "majority = ⌊N/2⌋ + 1, counting self's vote"). The sketch is deliberately left as-is so you catch the off-by-one — that bug shipping is exactly how a cluster elects with a sub-majority.
  - **Output**: `notes/56_election_design.md` with R1–R9 + answers to D1–D7 + sequence diagram for a 5-node election under partition + heal. **Stop here for Thursday** — Phases 2 and 3 are tomorrow.
- [ ] Commit + log (Thursday: rpc.rs green + election design doc committed) (~10 min)

**Friday — election TESTS + debug ladder (Phases 2–3) + reth PR**

> Continues the `crates/consensus-raft/src/election.rs` Build started Thursday. You have the sealed predictions and the design doc; now write the engineered failing tests and walk the failure ladder newbie→expert. (~140 min Build + 30 min reth PR + 10 min commit = ~180 min.)

- [ ] **Build (Phases 2–3 — continues `crates/consensus-raft/src/election.rs` from Thursday)**: election timeout (150-300 ms randomized), vote casting, term increment. (~140 min)
  - 🔧 **Reminder**: first-attempt impls (fixed timeout, `HashSet` votes, `Instant::now()` inside methods, reply-before-fsync, `last_log_index`-only restriction) each fail ≥1 test below — the debug session is the lesson. Re-open `notes/56_election_prediction.md` (P5) before you start and check your predicted-failures against what actually goes red.

  #### Phase 2 — Write the failing tests (~50 min)

  - [ ] **(SENIOR — anti-shortcut trap, traps the D7 quorum off-by-one)** `election_safety_holds_under_random_schedules` — sim 5-node cluster; 1000 random schedules (drop/delay/reorder). Assert BOTH directions, because safety alone is passed *by accident* by a too-strict (super-majority) quorum that simply never elects:
    - **Safety**: at most 1 leader per term in every run.
    - **Liveness + exact-majority**: in every run where no partition is active and a quorum is reachable, exactly one leader IS elected within N timeout windows, AND that leader was promoted on the MINIMUM correct majority — `⌊N/2⌋ + 1` votes counting self (3 for N=5), not more. Capture the winning candidate's `votes_received.len()` at the instant of `become_leader` and assert it equals 3. The planted sketch `votes_received.len() > self.peers().len() / 2 + 1` over-counts (it needs 4 votes for N=5 under the peers-include-self convention, and is also wrong for peers-exclude-self), so this run-time capture goes RED. This is the test that mechanically confirms the majority expression you hand-derived in **D7** — when it passes, your `⌊N/2⌋ + 1` derivation was correct; when it's red, your `>`/`>=` / `+1` choice or your peers-include-self assumption is off by one.
    - Exercises R3, R4, R5.
  - [ ] (SENIOR) `single_node_clean_majority_elects` — sibling/minimal-repro of the above: drive a clean (no drops, no delay) 5-node run; assert exactly one leader emerges and that it was promoted with EXACTLY 3 votes (not 4). This is the fastest red signal for the planted `> n/2 + 1` over-count and is cheap to bisect against D7. Exercises R5.
  - [ ] `randomized_timeout_low_split_rate` — 100 elections on 5 nodes; split-vote rate < 5% with [150, 300] ms randomization, AND assert a leader IS elected in every no-partition run (a too-strict quorum could keep split-rate low simply by never electing — assert election actually happens). Exercises R2, R5.
  - [ ] `election_restriction_holds` — candidate with `last_log_term < majority's` → vote denied; candidate with longest log wins. Exercises R4.
  - [ ] `crash_during_vote_no_double` — crash injected between `persist().await` return and reply send; recovery sees `voted_for = Some(x)`; subsequent attempt to vote for different candidate same term REJECTED. Exercises R6.
  - [ ] `votes_received_zero_alloc` — `dhat` over 1000 elections: zero allocs in `votes_received` after warmup. Exercises R7.
  - [ ] `tick_passes_now_parameter` — `compile_fail` (or signature audit): `tick(&mut self)` (no now param) must not exist; all timer methods take `now: Instant`. Exercises R1.
  - [ ] `partition_minority_cannot_elect` — 5-node partition 3-2: minority side increments term repeatedly, never elects. Exercises R5.
  - [ ] `partition_heal_old_leader_steps_down` — partition + heal: minority's old leader (lower term) receives heartbeat with higher term → steps down. Exercises R4, R5.
  - [ ] `heartbeat_interval_under_election_third` — `const _: () = assert!(HEARTBEAT_INTERVAL_MS * 3 <= ELECTION_TIMEOUT_MIN_MS);`. Exercises R8.
  - [ ] `bench_tick_under_30ns_per_role` — criterion `iter_custom`: 1M ticks/role; p50 ≤ 30 ns. Exercises R9.

  #### Phase 3 — Make the tests pass, one failure at a time (~90 min)

  - **When `votes_received_zero_alloc` reports allocs per election and `dhat` shows HashSet allocs:**
    - Symptom: `dhat` shows election-flow allocs proportional to election rate (~1-3 backing-table allocs per `HashSet` as it grows 1→5 votes, plus the cold-write/hash cost on each insert); under chaos test injecting partition healing, allocator pressure visible; criterion bench shows ~500 ns alloc+hash overhead per election.
    - Why: `votes_received: HashSet<NodeId>` allocates a fresh backing table per election; `HashSet::insert` is ~50 ns (bucket + hash + insert), and the table reallocs once or twice as the set grows from 1 to 5 votes when `with_capacity(N)` is forgotten; total ~300-500 ns per election + churn cost.
    - Fix: `votes_received: ArrayVec<NodeId, MAX_PEERS>` with linear-scan `contains` (~5 ns for 7 elements); pre-allocated at `RaftNode::new`, `.clear()` on `start_election`.
    - Cost avoided: ~300-500 ns per election × ~100 elections/s under churn = ~30-50 µs/s CPU + zero alloc; bigger win is consistency of latency (no GC-style jitter).
    - Muscle: container choice is workload-dependent — when N small and bounded (≤ 32), ArrayVec + linear scan beats HashMap/HashSet on throughput AND latency. HashSet pays for itself only beyond N ~ 64. **Reapplies at**: W59 raft `match_index` updates, W64+ bft `voted_validators_for_block`, W74 matching-engine `top_n_levels`, W90+ ledger replicas-in-quorum.

  - **When `tick_passes_now_parameter` shows `Instant::now()` calls inside `start_election` / `broadcast_heartbeat`:**
    - Symptom: at sub-ms timer granularity (HFT election_timeout = 30 ms ⇒ tick rate higher), clock reads dominate; on 16-shard runtime with each shard ticking 1 kHz × 4 redundant reads × 25 ns = ~1.6 µs/ms = 0.16% CPU on clocks alone.
    - Why: spec-correct `tick(now)` reads `now`, but then `start_election` calls `Instant::now()` again "for the election_started_at timestamp," then `broadcast_heartbeat` does it once more. Each is ~25 ns vDSO.
    - Fix: `tick`, `start_election`, `broadcast_heartbeat`, and `become_candidate` ALL take `now: Instant` parameter from the caller; reuse the value. Caller (the runtime loop) calls `Instant::now()` ONCE per tick boundary.
    - Cost avoided: ~25 ns × 3 redundant reads/tick × 1 kHz = ~75 µs/s/shard; on 16-shard runtime ≈ ~1.2 ms/s CPU. Reflects the bar-(c) discipline: every clock read on the hot path is intentional and counted (W003 muscle: vDSO clock ≈ 25 ns).
    - Muscle: clock reads aren't free; on a hot loop, pass `now: Instant` as parameter, never re-read. The timer wheel pattern (W58+) makes this explicit. **Reapplies at**: W58 timer wheel (single `Instant::now` per tick wheel rotation), W74 matching-engine `OrderBook::tick(now)`, W76+ aeron flow-control rate-limit, W83+ ledger TigerBeetle-style deterministic clock injection.

  - **(SENIOR) When `single_node_clean_majority_elects` / `election_safety_holds_under_random_schedules` report no leader (or a leader with 4 votes) on a clean 5-node run — the D7 quorum off-by-one:**
    - Symptom: a clean 5-node run never produces a leader (cluster live-locks at Candidate even with all 5 reachable), OR the captured `votes_received.len()` at `become_leader` is 4, not 3; `election_safety_holds_under_random_schedules`'s liveness assertion goes red even though the ≤1-leader-per-term assertion stays green (the bug is conservative on safety — too few elections — but a liveness failure).
    - Why: the design sketch promotes on `votes_received.len() > self.peers().len() / 2 + 1`. A majority of N is `⌊N/2⌋ + 1` counting self. For N=5 with `peers()` EXCLUDING self (len 4): `4/2 + 1 = 3`, and `len > 3` requires 4 votes — one too many. For `peers()` INCLUDING self (len 5): `5/2 + 1 = 3`, `len > 3` again requires 4 — still one too many. Either convention over-counts the majority by one; the `+1` belongs inside the floor, not added to the threshold, and `>` should be `>=` (or drop the `+1` and keep `>`).
    - Fix: derive once and hard-code the convention. With `votes` counting self and `n = total cluster size`: `if votes >= n / 2 + 1 { become_leader() }` (integer `n/2` IS `⌊N/2⌋`). Equivalently `votes > n / 2`. Make `n` the cluster size explicitly (not `peers().len()` whose self-inclusion is ambiguous) so the off-by-one can't re-enter through the convention.
    - Cost avoided: not a perf cost — a liveness bug. A sub-majority `>` (drop the `+1`) elects on 2 votes for N=5 → split-brain (two leaders, committed-data loss); the planted super-majority `> n/2+1` never elects with exactly a bare quorum reachable (e.g. exactly 3 of 5 up) → the cluster is unavailable precisely when it should fail over. For W74 matching-engine that means a missed `≤ 30 ms` failover SLO during the one event it exists to survive.
    - Muscle: quorum arithmetic is a derive-from-first-principles-then-test invariant, never a copied expression — pin the self-counting convention and let a runtime vote-count assertion (not just a ≤1-leader check) guard it, because safety tests pass an over-strict quorum by accident. **Reapplies at**: W62 membership change (quorum during joint-consensus = majority of BOTH old and new configs), W64+ bft 2f+1 / 3f+1 quorum thresholds, W90+ ledger VSR f+1 view-change quorum.

  - **When `randomized_timeout_low_split_rate` shows ~40% split-vote rate:**
    - Symptom: 5-node cluster live-locks on partition events; election convergence p99 exceeds 2 seconds; split votes account for 40% of attempts.
    - Why: election_timeout fixed at 200 ms across all nodes; synchronization → simultaneous candidacy → no majority.
    - Fix: `self.election_timeout = rand::thread_rng().gen_range(150..300).milliseconds()` set on entry to each new election (in `start_election`). Each node sees a different timeout next round.
    - Muscle: synchronization is the enemy of consensus; randomization breaks the lockstep. Reapplies at: W64+ bft view-timeout, W76+ aeron retransmit backoff.

  - **When `election_restriction_holds` reports a short-log candidate wins:**
    - Symptom: candidate with `last_log_index = 50` defeats voters with `last_log_index = 100`; new leader doesn't have entries 51-100; future ops conflict-overwrite committed entries.
    - Why: `handle_request_vote` checks `last_log_index >= self.log.last_index()` only when `last_log_term == self.log.last_term()` — but the code skipped the term comparison entirely.
    - Fix: explicit two-step check: `let log_ok = (rpc.last_log_term > self.log.last_term()) || (rpc.last_log_term == self.log.last_term() && rpc.last_log_index >= self.log.last_index());`. Vote only if `log_ok && can_vote`.
    - Muscle: leader-completeness depends on the term-first comparison; missing it loses committed data. Reapplies at: W64+ bft block-extension rule, W90+ ledger view-change quorum.

  - **When `crash_during_vote_no_double` reproduces a double-vote:**
    - Symptom: vote granted in term T; crash after reply sent but before fsync completed; recovery sees `voted_for = None`; vote-attempt by different candidate same term granted → 2 candidates each get majority.
    - Why: handler sent reply BEFORE awaiting `persist().await` completion.
    - Fix: `handle_request_vote` awaits the group-commit `wal.append_and_fence(...)` BEFORE constructing the reply; reply happens after disk confirmation.
    - Muscle: persist FIRST, ack SECOND — applies to every leader-election protocol. Reapplies at: W64+ bft pre-commit vote, W90+ ledger view-change ack.

  - **When `partition_heal_old_leader_steps_down` keeps the old leader:**
    - Symptom: minority's old leader (term T) receives heartbeat from majority's new leader (term T+5); ignores it and keeps acting as leader; 2 leaders for some interval.
    - Why: heartbeat-handler doesn't compare `rpc.term` to `self.persistent.current_term` first.
    - Fix: EVERY RPC handler's first check is `if rpc.term > self.persistent.current_term { self.become_follower(rpc.term, None); }`. Then proceed with handler logic.
    - Muscle: term-comparison is the universal first check in every RPC handler. Reapplies at: W64+ bft view-comparison, W90+ ledger op-num comparison.

  - **5-year failure mode** (write in `notes/56_election_followup.md`): when "Pre-Vote" extension lands (raft-thesis §9.6), the election protocol gains a pre-flight RPC that checks if a candidate WOULD win before incrementing term. Name the migration: where does `pre_vote_request` slot into `tick`/`start_election`, and how does it preserve safety while reducing churn?
  - *Back-of-envelope* (bar (c) — election latency is critical for matching-engine failover SLO; HFT target: ≤ 100 ms p99 leader recovery):
    - Election timeout: 150-300 ms (Ongaro paper) — but HFT shops often run **20-50 ms** with high-quality networks (sub-ms LAN) for fast failover. The matching-engine W74 will demand `election_timeout ≤ 30 ms`.
    - Steady-state CPU per tick: `now.duration_since(*last_heartbeat)` = ~25 ns (vDSO monotonic clock); branch on role + compare; ~30 ns/tick. Tick rate 1 kHz ⇒ ~30 µs/s = ~0.003% CPU. Trivial.
    - Election round (when triggered): `start_election` does (1) term++ + voted_for=self (~5 ns CPU) + (2) `persistent.persist()` ⇒ fsync ~10 µs on PLP-cached enterprise NVMe (DOMINANT — this is the floor; ~300 µs on commodity NVMe without PLP, so the floor is hardware-dependent) + (3) broadcast 6 RequestVote RPCs (~5 µs encode/encrypt each, ~30 µs total + transport ~50 µs LAN) ⇒ end-to-end election RTT floor ≈ **~100-200 µs LAN + fsync × 2 (self + winning peer-reply)** on PLP NVMe. So a fast LAN cluster with PLP-cached storage can elect in ~10 µs × 2 fsync + a few network RTTs ≈ ~150 µs minimum; without PLP the fsync term dominates and the floor moves to ~600 µs+.
    - Hot read of `current_term` (in every RPC handler, ~10k/s): u64 load from L1 ≈ 0.5-1 ns. Goes red if you ever wrap RaftState in Arc<Mutex<_>> — that's a ~25 ns mutex acquire on uncontended + cache-line bounce.
    - `votes_received: HashSet<NodeId>` for 7-node clusters: HashSet on 7 elements is ~50 ns/insert/lookup vs ArrayVec linear scan ~10 ns. Use ArrayVec.
  - **Read first**: Ongaro §5.2 (leader election), §5.4.1 (election restriction); raft-rs `src/raft.rs::campaign`, `step_candidate`; etcd `raft/raft.go::Raft::campaign`.
  - **Design**:
    ```rust
    impl<S: StateMachine> RaftState<S> {
        pub fn tick(&mut self, now: Instant) {
            match &mut self.role {
                Role::Follower { last_heartbeat, .. } => {
                    if now.duration_since(*last_heartbeat) > self.election_timeout {
                        self.start_election(now);
                    }
                }
                Role::Candidate { election_started_at, .. } => {
                    if now.duration_since(*election_started_at) > self.election_timeout {
                        self.start_election(now);  // re-start
                    }
                }
                Role::Leader { last_heartbeat_sent, .. } => {
                    if now.duration_since(*last_heartbeat_sent) > self.heartbeat_interval {
                        self.broadcast_heartbeat();
                    }
                }
            }
        }

        fn start_election(&mut self, now: Instant) {
            self.persistent.current_term += 1;
            self.persistent.voted_for = Some(self.node_id);
            self.persistent.persist().unwrap();  // FSYNC before any RPC
            self.role = Role::Candidate {
                votes_received: HashSet::from([self.node_id]),
                election_started_at: now,
            };
            self.election_timeout = random_in(150..300).milliseconds();  // RE-RANDOMIZE
            // broadcast RequestVote to all peers
            let rpc = RequestVote {
                term: self.persistent.current_term,
                candidate_id: self.node_id,
                last_log_index: self.log.last_index(),
                last_log_term: self.log.last_term(),
            };
            for peer in self.peers() { self.send(peer, RaftRpc::Vote(rpc.clone())); }
        }

        pub fn handle_request_vote(&mut self, rpc: RequestVote) -> RequestVoteReply {
            // Rule 1: if rpc.term > current_term, step down to follower FIRST
            if rpc.term > self.persistent.current_term {
                self.become_follower(rpc.term, None);
            }
            // Rule 2: reject stale-term requests
            if rpc.term < self.persistent.current_term {
                return RequestVoteReply { term: self.persistent.current_term, vote_granted: false };
            }
            // Rule 3: voted_for must be None or candidate
            let can_vote = self.persistent.voted_for.map_or(true, |v| v == rpc.candidate_id);
            // Rule 4: election restriction (§5.4.1)
            let log_ok = (rpc.last_log_term > self.log.last_term())
                || (rpc.last_log_term == self.log.last_term()
                    && rpc.last_log_index >= self.log.last_index());
            let granted = can_vote && log_ok;
            if granted {
                self.persistent.voted_for = Some(rpc.candidate_id);
                self.persistent.persist().unwrap();  // FSYNC before reply
                // reset election timer
                if let Role::Follower { last_heartbeat, .. } = &mut self.role {
                    *last_heartbeat = Instant::now();
                }
            }
            RequestVoteReply { term: self.persistent.current_term, vote_granted: granted }
        }

        pub fn handle_request_vote_reply(&mut self, from: NodeId, reply: RequestVoteReply) {
            // step down if reply has higher term
            if reply.term > self.persistent.current_term {
                self.become_follower(reply.term, None);
                return;
            }
            // collect vote
            if let Role::Candidate { votes_received, .. } = &mut self.role {
                if reply.vote_granted && reply.term == self.persistent.current_term {
                    votes_received.insert(from);
                    if votes_received.len() > self.peers().len() / 2 + 1 {
                        // we won — but check still relevant for current term
                        self.become_leader();
                    }
                }
            }
        }
    }
    ```
    **Invariants**:
    - Term monotonic across all transitions (compile-time-ish: only mutated by `become_follower(higher_term)` and `start_election` which increments).
    - voted_for + current_term FSYNC'd BEFORE any reply that depends on them.
    - Election timeout RE-RANDOMIZED at start of each election (not just once at startup).
    - Election-restriction (Rule 4) checks log-up-to-date BEFORE granting vote.
  - **Build steps** (granular):
    1. (~20 min) `start_election` with term-increment + vote-self + persist + RPC broadcast.
    2. (~20 min) `handle_request_vote` with all 4 rules. Critical: re-randomize timeout on grant.
    3. (~20 min) `handle_request_vote_reply` collects votes; promotes on majority.
    4. (~20 min) `tick(now)` dispatches per-role timeout handling.
    5. (~30 min) Test harness: in-memory 5-node sim with controllable network (drop/delay/reorder). Run 1000 random schedules, verify election safety holds (at most 1 leader per term).
    6. (~20 min) Adversarial test: partition leader; verify minority side cannot elect; reunite; verify old leader steps down.
    7. (~20 min) Random-timeout test: 100 elections, measure split-vote rate. Expect < 5% with 150-300ms randomization on 5 nodes.
  - **Pitfalls** (named, distributed-systems):
    - *Split vote without randomization*: synchronized timeouts → all 5 nodes become candidates simultaneously → 5 vote-for-self, no one gets majority. **Symptom**: live-lock. **Fix**: `rand::thread_rng().gen_range(150..300)` per-node, per-election.
    - *Term confusion*: candidate at term T receives reply with term T+5; if it just collects votes without checking term, advances to leader at term T while a real leader exists at T+5. **Symptom**: two leaders. **Fix**: `if reply.term > current_term: step_down`.
    - *Byzantine accidental double-vote*: process crashes mid-vote; voted_for not yet persisted; on restart, votes again in same term for different candidate. **Symptom**: 2 candidates each get majority (impossible by quorum math but possible under accidental Byzantine). **Fix**: persist voted_for SYNCHRONOUSLY (fsync) before reply.
    - *Heartbeat interval too long*: with election_timeout = 200ms and heartbeat = 150ms, jitter can starve heartbeats below 200ms → spurious elections. **Symptom**: leader churn. **Fix**: heartbeat ≤ election_timeout / 3 (60ms heartbeat for 200ms election).
    - *Election restriction bypass*: granting vote to candidate whose last_log_term is lower → leader completeness violation → committed entry lost. **Symptom**: data loss on partition heal. **Fix**: explicit check `last_log_term` first (NOT just `last_log_index`).
    - **Expect to hit (perf) #1 — `votes_received: HashSet<NodeId>` allocates per election; SmallVec/ArrayVec on 7-node cluster is the right tool (bar (c) violation)**: at 7 nodes, `HashSet::insert` is ~50 ns (bucket compute + hash + insert) + ~50 ns alloc on first insert; total ~300-500 ns per election + ~50 ns alloc overhead. Under churn (Pre-Vote off, repeated elections) this multiplies. Worse: `HashSet::with_capacity(N)` is forgotten 95% of the time → realloc as the set grows from 1 to 5 votes. **Symptom**: `dhat` shows election-flow allocs proportional to election rate; under chaos test injecting partition healing, allocator pressure visible.
      - Fix: `votes_received: ArrayVec<NodeId, MAX_PEERS>` with linear-scan `contains` (~5 ns for 7 elements, faster than HashSet's hash+bucket); pre-allocated at `RaftNode::new`, cleared on `start_election`.
      - Cost avoided: ~300-500 ns per election × ~100 elections/s under churn = ~30-50 µs/s CPU + zero alloc; bigger win is the consistency of latency (no GC-style jitter).
      - Muscle: container choice is workload-dependent — when N is small and bounded (≤ 32 typical), ArrayVec + linear scan beats HashMap/HashSet on both throughput and latency. HashSet pays for itself only beyond N ~ 64.
      - **Reapplies at**: W59 raft `match_index` updates (ArrayVec<(NodeId, u64), MAX_PEERS>), W64+ bft `voted_validators_for_block`, W74 matching-engine `top_n_levels`, W90+ ledger replicas-in-quorum.
    - **Expect to hit (perf) #2 — `tick(now)` called at 1 kHz allocates a fresh `Instant::now()` via `std::time::Instant` (~25 ns vDSO) + 4 branch checks; OK alone, but if the timer wheel uses `Instant::now` PER ROLE rather than once per tick, you 4× the cost (bar (c) violation, latent)**: spec-correct `tick` calls `now.duration_since(*last_heartbeat)` then potentially `Instant::now()` again inside `start_election`, then again inside `broadcast_heartbeat`. Each is ~25 ns vDSO. **Symptom**: at sub-ms timer granularity (HFT election_timeout = 30 ms ⇒ tick rate higher to detect timeouts in time) this scales linearly; on a 16-shard runtime with each shard ticking 1 kHz = 16 × 25 ns × 4 reads = ~1.6 µs/ms = 0.16% CPU on clocks alone.
      - Fix: caller passes `now: Instant` once per tick; `tick`, `start_election`, `broadcast_heartbeat` all take `now: Instant` parameter and reuse the value. Already correct in the design sketch above (`fn tick(&mut self, now: Instant)`) — the perf bug is when downstream code calls `Instant::now()` inside the methods anyway.
      - Cost avoided: ~25 ns × 3 redundant reads/tick × 1 kHz = ~75 µs/s/shard; on 16-shard runtime = ~1.2 ms/s CPU. Trivial individually, but reflects the bar-(c) discipline that EVERY clock read on the hot path is intentional and counted (W003 muscle: `vDSO clock ≈ 25 ns`).
      - Muscle: clock reads are not free; on a hot loop, pass `now: Instant` as a parameter rather than re-reading. The timer wheel pattern (W58+) makes this explicit.
      - **Reapplies at**: W58 timer wheel design (single `Instant::now` per tick wheel rotation), W74 matching-engine `OrderBook::tick(now)`, W76+ aeron flow-control rate-limit, W83+ ledger TigerBeetle-style deterministic clock injection.
  - **Test scenarios** (adversarial):
    - 5-node cluster, kill leader, ≥ 99% of runs elect new leader within 2× election_timeout. <5% split votes.
    - Network partition 3-2: minority side increments term repeatedly but never elects. Majority side has stable leader.
    - Reunite partition: minority's term has advanced 5x without electing; majority leader receives stale-but-higher term → steps down → new election with the higher term.
    - Crash-during-vote: vote granted → process crash before sending reply → restart → vote still recorded; cannot grant to different candidate in same term.
    - Election restriction: 5-node cluster; candidate has shorter log than majority → vote denied; node with longest log wins.
  - **Acceptance**: 1000 random schedules zero leader-safety violations; partition tests pass; split-vote rate < 5%; election convergence p95 < 2×election_timeout.
  - **Downstream**: W59 (replication consumes Leader role); W62 (membership change adds joint-consensus state); W67+ (raft v1.0 polishes pre-vote extension).
- [ ] **reth PR cadence** (`reth` fork: storage or evm good-first-issue). (~30 min)
  - **Output**: branch pushed / PR opened (or comment on an existing one); link recorded in `notes/56_pr_log.md`. Standing weekly cadence toward the backup-job merged-PR milestone.
- [ ] Commit + log (Friday: election test suite green + reth PR pushed) (~10 min)

**Saturday — revm PR**

- [ ] **revm PR cadence** (`revm` fork: contribution / good-first-issue). (~30 min)
  - **Output**: branch pushed / PR opened (or review comment); link recorded in `notes/56_pr_log.md`. Standing weekly cadence.
- [ ] Commit + log (~10 min)

## Sunday — Rest + End Month 14 review

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.

- [ ] Crate (exec-vm) passing majority of test vectors. (~30 min)
- [ ] [Tempo] 20 min: Tempo releases skim. Check Tempo PR queue for any open PRs that look like good-first-issue
  material. Bookmark for W60. (~20 min)
