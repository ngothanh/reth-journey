# Week 140 — Permanent housing

> **Phase**: 7
> **Month**: M36
> **Track**: (career — destination landing)
> **Hours target**: 30h/wk (W137-W144)
> **Mirror target**: (n/a — career/landing week; no upstream module)
> **Crate(s) touched**: (none — maintenance only on the Reth + HFT operations rig; no new code)
> **Bar**: (n/a) — career/landing week, no Build exercise (per SPEC §6: depth ≠ padding; do not manufacture fake Builds)
> **Feeds into**: W141 optional Phase-7-close blog post (a stable home base + uninterrupted rig uptime are preconditions for the writing block); W144 M36 retrospective + plan close (this week's runtime tally rolls into the 4000+ runtime-hours milestone and the final inheritance audit); the destination-landing arc W134 arrival → W135-W138 first month → W139-W143 settling, of which this is week 6 at the new firm.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): destination-geography rental/tenancy-law portals + official renter-rights pages (Monday/Saturday housing — official sources only, lookup-when-stuck); `progress.md` HFT row (Wednesday runtime tally); `chaos_log.md` (Saturday chaos-drill history, for the rotation pointer).
> **v2 modifications**: none

This is a career/landing week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding (SPEC §6 light-week rule). Every item below instead carries an explicit artifact file + an Output line that proves it done, and the two decision/learning items (Monday neighborhood choice, Saturday rig maintenance) carry a hardened-acceptance pass criterion and a mental model of why the item exists now. The week's job: convert ~6 weeks of lived destination geography into a committed permanent-housing decision while keeping the HFT operations rig's uptime unbroken toward the W144 4000-hour milestone — personal-project time is now ≤4 hrs/wk (the W138 new-normal), so the rig maintenance is touchpoint-sized, not project-sized.

**Monday — Housing search: neighborhood decision**

- [ ] Artifact: `relocation/housing_decision.md` (private). By now you have ~6 weeks of lived impression of the destination geography (commute to office, neighborhood feel, noise, grocery/transit access). Commit to a target neighborhood and open the permanent-housing search: record the chosen neighborhood + 2-3 runners-up, the decisive criteria (commute minutes door-to-desk, rent band, lease length, furnished/unfurnished), and the search channels you'll use (agency vs direct-listing portal). Also do the 15-min HFT Monday touchpoint (below) before standup. (~30 min)
  - 🎯 Hardened acceptance: `housing_decision.md` names ONE committed target neighborhood (not a shortlist) with ≥3 decisive criteria each given a concrete value (e.g. "commute ≤ 35 min door-to-desk", "rent ≤ X/mo"), plus ≥2 named runners-up with the one-line reason each lost. A decision without a falsifiable criterion is a vibe, not a decision — reject it.
  - *Mental model*: temporary/corporate housing has a clock on it (and a premium price); the longer the permanent search drags, the more it eats the W141 writing block and the W144 close. Committing to ONE neighborhood with measurable criteria now — instead of an open-ended "keep looking" — is what turns a stressful relocation tail into a bounded task. The criteria are also what let an agent or a listing filter work for you instead of you scrolling.
  - [HFT] Monday touchpoint (~15 min): review weekend runtime alerts on the operations rig — any P99 spikes, replica-divergence events, or disk-fill warnings? Log the answer (even "none") in `chaos_log.md` so the W144 audit has an unbroken trail.
  - Output: `relocation/housing_decision.md` exists meeting the hardened-acceptance criteria; `chaos_log.md` has a dated W140-Mon alert-review line.
- [ ] Commit notes (`relocation/housing_decision.md` private). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday housing-decision commit.

**Tuesday — Work**

- [ ] Artifact: append to `work/day_log.md` (private). Day-job substantive work — capture the one decision or deliverable that mattered today in a single line so the running log stays useful for the manager 1:1 cadence and the W144 retro. (~15 min)
  - *Mental model*: a one-line-per-day work log costs nothing and is the only durable record of "what did I actually ship in month 1?" — the question you'll be asked at the 90-day review and the one the W144 retrospective answers. Without it, month 1 blurs.
  - Output: `work/day_log.md` has a dated W140-Tue line.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday commit.

**Wednesday — Work + rig dashboard check**

- [ ] Artifact: update the HFT row in `progress.md` (private). Day-job work, plus the [HFT] Wednesday touchpoint: check the operations dashboards, capture the cumulative runtime-hours total, and update the `progress.md` HFT row. Append today's one-line work note to `work/day_log.md`. (~30 min)
  - 🎯 Hardened acceptance: `progress.md` HFT row carries a W140 runtime-hours figure that is monotonically ≥ the last recorded total (a non-increasing tally means the rig went down unnoticed — investigate before committing); the figure is on a credible glide path toward the W144 4000-hour milestone (≈3900h by W140 if continuous since the W138 3800h mark).
  - *Mental model*: the 4000-runtime-hours bar (c) milestone at W144 is not earned in the last week — it's earned by the tally never stalling. A Wednesday glance that catches a silent rig outage now is the difference between hitting 4000h and discovering a 200-hour gap at the close. The dashboard check is cheap insurance on a number that took 47 weeks to accumulate.
  - Output: `progress.md` HFT row updated with the W140 runtime tally; `work/day_log.md` has a dated W140-Wed line.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday commit.

**Thursday — Work**

- [ ] Artifact: append to `work/day_log.md` (private). Day-job substantive work; capture today's one-line decision/deliverable. (~15 min)
  - *Mental model*: same running-log discipline as Tuesday — the value is in the unbroken daily cadence, not any single entry.
  - Output: `work/day_log.md` has a dated W140-Thu line.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday commit.

**Friday — Work + housing viewings scheduled**

- [ ] Artifact: append to `relocation/housing_decision.md` a `## Viewings` section (private). Day-job work, then book concrete viewings for the committed neighborhood from Monday: list each as {address/listing-id, date/time, rent, lease length, channel}. Append today's one-line work note to `work/day_log.md`. (~30 min)
  - 🎯 Hardened acceptance: ≥2 viewings booked with a concrete date/time each (not "will book soon"); each carries rent + lease length so Saturday's visit is a comparison, not a first look.
  - *Mental model*: scheduling the viewings on Friday lands them in Saturday's personal-project window, which is the only block left now that the firm owns the weekday. Booking ≥2 forces a side-by-side comparison — one viewing is an anchor-bias trap, two is a decision.
  - Output: `relocation/housing_decision.md` has a `## Viewings` section with ≥2 dated entries; `work/day_log.md` has a dated W140-Fri line.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday commit.

**Saturday — Housing viewings + rig chaos drill**

- [ ] Artifact: append a `## Visit notes` section to `relocation/housing_decision.md` (private). Attend the booked viewings; for each, record the verdict against Monday's decisive criteria (commute, rent, lease, condition) as a ✓/✗ per criterion, and a one-line overall lean. If a place clears all criteria, note it as a candidate to apply for. (~60 min)
  - 🎯 Hardened acceptance: each attended viewing is scored ✓/✗ against every Monday criterion (no "felt nice" without the criterion check); at least one viewing ends with an explicit decision tag `[apply]` / `[pass]` / `[backup]` — a viewing with no verdict was wasted.
  - *Mental model*: scoring each place against the same fixed criteria from Monday is what keeps a tired Saturday-afternoon decision honest — the criteria, not the mood of the visit, pick the home. This is the same decision-discipline (fixed rubric → tagged verdict) used in the W120 outreach classification, reused for the highest-stakes personal decision of the landing arc.
  - Output: `relocation/housing_decision.md` has a `## Visit notes` section scoring each viewing with at least one `[apply]`/`[pass]`/`[backup]` tag.
- [ ] **Rig maintenance (chaos drill)**: artifact append to `chaos_log.md` (private). [HFT] Saturday touchpoint — perform this week's small chaos drill from the rotation (process kill / network partition / clock skew / disk fill / slow subscriber; pick the next one not run in the prior weeks per `chaos_log.md`). Capture: which fault, expected vs observed recovery behavior, and any divergence. Keep it touchpoint-sized — personal-project time is ≤4 hrs/wk now (the W138 new-normal). (~30 min)
  - 🎯 Hardened acceptance: `chaos_log.md` gains a dated W140 entry naming the specific fault injected (rotated — not a repeat of last week's), with expected-vs-observed recovery recorded; any divergence is flagged, not silently dropped.
  - *Mental model*: the chaos drill is what keeps the bar (c) "deterministic recovery under fault" claim true after the rig has been running unattended for weeks. Rotating the fault each week means that by W144 every failure class has been exercised at least once — a claim the M36 final audit (W144 Tue) checks. Skipping it because "the rig's been fine" is exactly how a latent recovery bug survives to the close.
  - Output: `chaos_log.md` has a dated W140 chaos-drill entry meeting the hardened-acceptance criteria.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
