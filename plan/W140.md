# Week 140 — Permanent housing (hub if relocated, else remote base)

> **Phase**: 7 (Bet Placement / Bridge — M31–M36)
> **Month**: M36 (Settling + M36 retrospective)
> **Track**: (career — destination landing, week 6 at the bet/bridge) — no engineering track this week; the `perp-dex-core` operations rig is in maintenance-only mode
> **Hours target**: 30h/wk (W137–W144 window; the W137 step-down from 40h/wk is in effect — personal-project time is ≤4 hrs/wk, the W138 new-normal)
> **Mirror target**: (n/a — career/landing week; no upstream module, no crate, no `apply()` discipline to mirror)
> **Crate(s) touched**: (none — maintenance-only touchpoints on the `perp-dex-core` capstone rig: 3-node VSR cluster uptime, insurance-fund tripwire watch, cluster-VOPR chaos rotation. No new code; no version bump.)
> **Bar**: (n/a) — career/landing week, no Build exercise. Per README.md §6 light-week rule (depth ≠ padding): do **not** manufacture a fake 3-phase Build / trap-test arc where there is no primitive to build. The bar-(c) claim being *protected* this week is the capstone's "deterministic recovery under fault" + the 4000-runtime-hours milestone — protected by the Saturday cluster-VOPR drill + the Wednesday tally, not by new code.
> **Feeds into**: W141 (public artifact update — `perp-dex-core` + the bet: a stable home base + unbroken rig uptime are preconditions for the writing block); W142 (settle + first quarterly review prep — the manager-1:1 / vesting cadence this week feeds); W144 (M36 retrospective + Bet #2 prep — this week's runtime tally rolls into the **4000+ runtime-hours** milestone and the final inheritance audit; the housing decision closes the relocation tail so the retro is clean; 8-system coverage ≥80 as the close metric). Position in the landing arc: W134 arrival → W135–W138 first month → **W139–W143 settling**, of which this is the second settling week.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): destination-geography rental/tenancy-law portals + official renter-rights pages (Monday/Saturday housing — **official sources only, lookup-when-stuck**; if relocated under the §7 flip-trigger this is the Dubai/SG hub, else it is the remote base); README.md §7 (geography flip-trigger — *only* relevant if the move actually fired at W131/W132); `progress.md` HFT/capstone row (Wednesday runtime tally); `chaos_log.md` (Saturday cluster-VOPR rotation pointer); `work/day_log.md` (the running month-1 work record).
> **v3 modifications**: (1) **Reframed from v2 "Tier-A HFT firm landing" to the v3 bet-path.** The destination is now either **Bet #1 placed** (founding/core-eng token-equity seat at a §6-checklist-passing on-chain-derivatives venue) **or the crypto-MM cash bridge engaged** (the income floor) — the housing decision is conditioned on *which* outcome fired and on whether the §7 flip-trigger relocated me at all (hub follows the bet; else remote base). (2) **The maintained rig is `perp-dex-core`, not `mini-db`.** The Monday/Wednesday/Saturday [HFT] touchpoints now watch the **3-node VSR cluster** (replica divergence, insurance-fund tripwire, cluster-VOPR rotation) — the v3 capstone — instead of the v2 mini-db ops rig. (3) **Saturday chaos drill is now a cluster-VOPR drill** drawn from the W108 fault rotation (crash/restart/partition/insurance-fund-tripwire search), keeping the W113 v1.0 "cluster-VOPR clean" claim live. (4) Narrative seeds **Bet #2 thesis** continuity (W139) and the W144 close, not a "passed probation at the HFT firm" frame.

This is a **career/landing week, not a Build week.** There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding (README.md §6 light-week rule: do not manufacture fake Builds). Every item below instead carries an explicit **artifact file + an Output line that proves it done**, and the two decision/learning items (Monday neighborhood choice, Saturday cluster-rig drill) carry a **hardened-acceptance pass criterion** and a **mental model** of why the item exists *now*. The week's job: convert ~6 weeks of lived destination geography (whether a relocated hub or the established remote base) into a **committed permanent-housing decision** while keeping the `perp-dex-core` operations rig's uptime unbroken toward the W144 **4000-hour** milestone — personal-project time is now ≤4 hrs/wk (the W138 new-normal), so rig maintenance is **touchpoint-sized, not project-sized.**

> **Branch note (read first):** the shape of Monday depends on what fired at W131/W132.
> - **If the §7 flip-trigger fired** (warm-conversation cluster or presence-contingent offer premium → relocated to the Dubai/SG hub that follows the bet): this is a genuine permanent-housing search in a new city.
> - **If it did NOT fire** (bet/bridge is remote-first, no move): "permanent housing" is the **remote base** decision — lock the long-term lease / home-office setup where you already are, and the criteria collapse to commute=0, workspace quality, and runway cost. Either way the week's discipline (committed decision + measurable criteria + tagged verdict) is identical; only the geography differs. Do **not** invent a relocation that did not happen.

---

## Monday — Permanent-housing decision (hub or remote base) + capstone Monday touchpoint

- [ ] **Artifact**: `relocation/housing_decision.md` (private). By now you have ~6 weeks of lived impression of the destination geography — if relocated: commute door-to-desk, neighborhood feel, noise, grocery/transit access; if remote: the existing-base trade-offs (cost-of-runway, workspace, internet reliability for the rig). Commit to a target neighborhood (or "stay + long-lease the current base") and open the permanent-housing search: record the chosen target + 2–3 runners-up, the **decisive criteria** (commute minutes door-to-desk, rent band, lease length, furnished/unfurnished, internet SLA for rig-on-call), and the search channels (agency vs direct-listing portal). Do the Monday [HFT] touchpoint (below) **before** standup. (~30 min)
  - 🎯 **Hardened acceptance**: `housing_decision.md` names **ONE committed target** (not a shortlist) with **≥3 decisive criteria each given a concrete value** (e.g. "commute ≤ 35 min door-to-desk", "rent ≤ X/mo", "internet ≥ 500 Mbps symmetric for on-call rig access"), plus **≥2 named runners-up** with the one-line reason each lost. A decision without a falsifiable criterion is a vibe, not a decision — reject it. *(Remote-base case: the committed target is "long-lease current base" or a named alternative; commute=0 is one criterion, but ≥2 others must still carry concrete values.)*
  - *Mental model*: temporary/corporate housing has a clock on it (and a premium price); the longer the permanent search drags, the more it eats the W141 public-artifact writing block and the W144 close. Committing to ONE target with measurable criteria now — instead of an open-ended "keep looking" — is what turns a stressful relocation tail into a bounded task. The criteria are also what let an agent or a listing filter work for you instead of you scrolling. This is the **same fixed-rubric → tagged-verdict discipline** the bet-path used on the §6 ecosystem-selection checklist (W116) and the W120 outreach classification — reused here for the highest-stakes personal decision of the landing arc.
  - **[HFT] Monday touchpoint** (~15 min): review weekend alerts on the `perp-dex-core` rig — any p99.99 spikes on the per-mark-tick path, **replica-divergence events across the 3-node VSR cluster**, **insurance-fund tripwire approaches**, disk-fill warnings on the VSR log, or cluster-VOPR background-run failures? Log the answer (even "none") in `chaos_log.md` so the W144 audit has an unbroken trail.
  - **Output**: `relocation/housing_decision.md` exists meeting the hardened-acceptance criteria; `chaos_log.md` has a dated **W140-Mon** alert-review line naming the VSR-cluster/insurance-fund check.
- [ ] Commit notes (`relocation/housing_decision.md`, private). (~10 min)
  - **Output**: `git log -1 --oneline` shows the Monday housing-decision commit.

## Tuesday — Work (the bet or the bridge)

- [ ] **Artifact**: append to `work/day_log.md` (private). Substantive day-one-job work — at the **bet** (the core engine I was brought in to build) or the **bridge** (crypto-MM cash seat). Capture the one decision or deliverable that mattered today in a single line so the running log stays useful for the manager-1:1 cadence and the W144 retro. (~15 min)
  - *Mental model*: a one-line-per-day work log costs nothing and is the only durable record of "what did I actually ship in month 1?" — the question you'll be asked at the W142 quarterly-review prep / 90-day review, and the one the W144 retrospective answers. Without it, month 1 blurs. At a token-equity bet this log also quietly tracks **whether the criticality I was promised is real** (am I on the critical path, or adjacent?) — a §6 re-check that feeds the Bet #2 thesis (W139/W144).
  - **Output**: `work/day_log.md` has a dated **W140-Tue** line.
- [ ] Commit notes (private). (~10 min)
  - **Output**: `git log -1 --oneline` shows the Tuesday commit.

## Wednesday — Work + capstone rig dashboard check (runtime tally)

- [ ] **Artifact**: update the capstone/HFT row in `progress.md` (private). Day-job work, plus the [HFT] Wednesday touchpoint: check the `perp-dex-core` operations dashboards (per-mark-tick p99.99 at the live account scale, 3-node VSR commit health, insurance-fund balance + tripwire margin, projection-staleness lag on the analytics token), capture the **cumulative runtime-hours total**, and update the `progress.md` row. Append today's one-line work note to `work/day_log.md`. (~30 min)
  - 🎯 **Hardened acceptance**: `progress.md` row carries a **W140 runtime-hours figure that is monotonically ≥ the last recorded total** (a non-increasing tally means the rig went down unnoticed — investigate **before** committing); the figure is on a credible glide path toward the **W144 4000-hour** milestone (≈ 3900h by W140 if continuous since the W138 ~3800h mark). The same glance asserts the **insurance-fund tripwire has never been hit** (the v1.0 invariant) — record the current fund balance + the closest-approach margin.
  - *Mental model*: the **4000-runtime-hours** bar-(c) milestone at W144 is not earned in the last week — it's earned by the tally **never stalling**. A Wednesday glance that catches a silent rig outage now is the difference between hitting 4000h and discovering a 200-hour gap at the close. For the v3 capstone the same glance protects two *named v1.0 invariants*: the **insurance-fund tripwire-never-hit** invariant and **cross-replica byte-identical state** (no divergence event in the log). The dashboard check is cheap insurance on a number that took dozens of weeks to accumulate **and** on the two claims the W144 audit + the bet-conversation 1-pager rest on.
  - **Output**: `progress.md` capstone row updated with the W140 runtime tally + insurance-fund balance/closest-approach; `work/day_log.md` has a dated **W140-Wed** line.
- [ ] Commit notes (private). (~10 min)
  - **Output**: `git log -1 --oneline` shows the Wednesday commit.

## Thursday — Work

- [ ] **Artifact**: append to `work/day_log.md` (private). Substantive day-job work; capture today's one-line decision/deliverable. (~15 min)
  - *Mental model*: same running-log discipline as Tuesday — the value is in the unbroken daily cadence, not any single entry. Over the settling weeks (W139–W143) this log is also the raw material for the **Bet #2 thesis seeds** (W139): each bet upgrades the next, and the upgrade is only visible if month-1 is written down as it happens, not reconstructed from memory.
  - **Output**: `work/day_log.md` has a dated **W140-Thu** line.
- [ ] Commit notes (private). (~10 min)
  - **Output**: `git log -1 --oneline` shows the Thursday commit.

## Friday — Work + housing viewings scheduled

- [ ] **Artifact**: append to `relocation/housing_decision.md` a `## Viewings` section (private). Day-job work, then book concrete viewings for the committed target from Monday: list each as `{address/listing-id, date/time, rent, lease length, channel, internet-SLA-confirmed?}`. *(Remote-base case: this is the lease-renewal / home-office-setup booking — a landlord call or a vendor appointment, still ≥1 concrete action with a date.)* Append today's one-line work note to `work/day_log.md`. (~30 min)
  - 🎯 **Hardened acceptance**: **≥2 viewings booked with a concrete date/time each** (not "will book soon"); each carries rent + lease length so Saturday's visit is a comparison, not a first look. *(Remote-base case: ≥1 concrete dated action with a verifiable next step.)*
  - *Mental model*: scheduling the viewings on Friday lands them in Saturday's personal-project window, which is the only block left now that the firm owns the weekday. Booking ≥2 forces a side-by-side comparison — one viewing is an anchor-bias trap, two is a decision. The Friday-schedule / Saturday-execute split is the same time-boxing the plan used all through Phase 7 to keep relocation logistics from bleeding into the engineering block.
  - **Output**: `relocation/housing_decision.md` has a `## Viewings` section with ≥2 dated entries; `work/day_log.md` has a dated **W140-Fri** line.
- [ ] Commit notes (private). (~10 min)
  - **Output**: `git log -1 --oneline` shows the Friday commit.

## Saturday — Housing viewings + capstone cluster-VOPR drill

- [ ] **Artifact**: append a `## Visit notes` section to `relocation/housing_decision.md` (private). Attend the booked viewings; for each, record the verdict against Monday's decisive criteria (commute, rent, lease, condition, internet-SLA) as a **✓/✗ per criterion**, and a one-line overall lean. If a place clears all criteria, note it as a candidate to apply for. (~60 min)
  - 🎯 **Hardened acceptance**: each attended viewing is **scored ✓/✗ against every Monday criterion** (no "felt nice" without the criterion check); at least one viewing ends with an explicit decision tag **`[apply]` / `[pass]` / `[backup]`** — a viewing with no verdict was wasted.
  - *Mental model*: scoring each place against the same fixed criteria from Monday is what keeps a tired Saturday-afternoon decision honest — the criteria, not the mood of the visit, pick the home. This is the same decision-discipline (fixed rubric → tagged verdict) used in the W120 outreach classification and the W116 ecosystem filter, reused for the highest-stakes personal decision of the landing arc.
  - **Output**: `relocation/housing_decision.md` has a `## Visit notes` section scoring each viewing with at least one `[apply]`/`[pass]`/`[backup]` tag.
- [ ] **Rig maintenance (cluster-VOPR drill)**: artifact append to `chaos_log.md` (private). **[HFT] Saturday touchpoint** — perform this week's small drill from the **cluster-VOPR rotation** established at W108 (leader crash/restart, single-replica restart, network partition + heal, clock skew across replicas, disk-fill on the VSR log, slow projection subscriber, **insurance-fund tripwire search**; pick the next one not run in the prior weeks per `chaos_log.md`). Capture: which fault, **expected vs observed recovery behavior**, post-recovery **cross-replica state-hash equality**, and any divergence. Keep it touchpoint-sized — personal-project time is ≤4 hrs/wk now (the W138 new-normal). (~30 min)
  - 🎯 **Hardened acceptance**: `chaos_log.md` gains a dated **W140** entry naming the **specific fault injected (rotated — not a repeat of last week's)**, with expected-vs-observed recovery recorded **and** a post-drill confirmation that all surviving replicas converged to a **byte-identical state-hash** and the **insurance-fund tripwire did not fire** spuriously; any divergence is flagged, not silently dropped.
  - *Mental model*: the cluster-VOPR drill is what keeps the bar-(c) **"deterministic recovery under fault"** claim — the headline of the W113 `perp-dex-core` v1.0 ship and the W117 bet-readiness gate — true after the rig has been running unattended for weeks. Rotating the fault each week means that by W144 every failure class has been exercised at least once on the *live* cluster, not just in the W108 simulator — a claim the M36 final audit (W144) checks and a question every founding-eng / crypto-MM interviewer asks ("does it actually recover, or did you just test it once?"). Skipping it because "the cluster's been fine" is exactly how a latent view-change or snapshot-replay bug survives to the close.
  - **Output**: `chaos_log.md` has a dated **W140** cluster-VOPR entry meeting the hardened-acceptance criteria.
- [ ] Commit + log. (~10 min)
  - **Output**: `git log -1 --oneline` shows the Saturday commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.

> **Week-close pointer (for the Sunday ritual):** two threads must both advance to keep W141–W144 clean — (1) the **housing decision** reaches at least one `[apply]` candidate (the relocation/remote-base tail is closing), and (2) the **runtime tally** stayed monotonic with the insurance-fund tripwire un-hit (the 4000h + bar-(c) claims stayed live). If either stalled, that is the single most important note to carry into the W141 ritual, because both are W144-close preconditions.
