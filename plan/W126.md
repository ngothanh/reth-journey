# Week 126 — [v3] First founding/core-eng conversations (apply §6 to live bet candidates)

> **Phase**: 7 (Bet Placement / Bridge)
> **Month**: M33
> **Track**: Bet-path (founding/core-eng token-equity bet on an on-chain-derivatives venue) + crypto-MM cash bridge in parallel
> **Hours target**: 40h/wk (W118–W136 window; drops to 30h/wk at W137)
> **Mirror target**: n/a (bet-conversation week — no upstream module to mirror; the artifact you carry IS `perp-dex-core` v1.0 + the cross-margin/liquidation engine, already shipped W113/W102/W99)
> **Crate(s) touched**: n/a (no new code — maintenance-only on the venue capstone + Reth spine; this week converts the shipped artifact into bet conversations)
> **Bar**: (n/a — bet-conversation week; the BAR being applied is the §6 ecosystem-selection checklist, not a code bar)
> **Feeds into**: W127 deep-dive conversations / trial collaborations + crypto-MM bridge interviews in parallel (this week's per-conversation §6 scorecards become W127's deep-dive shortlist and the trial-collab targets); W128 finalist conversations / work-trials (the firm-by-firm §6 GO/BRIDGE scorecard built here is the input); W131 **Bet #1 decision** (GO only if §6 passes — the scorecards seeded this week mature into that call); README.md §5 M36 (Bet #1 placed OR bridge engaged) and §6 (ecosystem-selection checklist applied per opportunity); §7 flip-trigger evaluation (warm-conversation cluster → Dubai/SG decision).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): README.md **§6 (the seven-point ecosystem-selection checklist — THIS is the week's rubric)**, §7 (shots-on-goal structure + Dubai/SG pre-stage/flip-trigger), §1 bars (a)–(d) and §3 convergence cell (your pitch is the `risk-engine` cross-margin engine); your own `notes/portfolio_*.md` 1-pagers from the SHIP rituals (W99 `risk-engine`, W102 `liquidation-engine`, W113 `perp-dex-core`) — lookup-when-stuck during a conversation post-mortem, never re-read cover-to-cover; the firm-specific public docs (token economics, cap table, founding-team backgrounds) only for the venue you talk to that day (just-in-time, not a reading project); your W116 ecosystem shortlist + W115 §6-filtered venue list.
> **v3 modifications**: this week is **fully re-aimed**. v2 W126 was "first-round interviews begin" — phone screens for a Tier-A HFT IC *job*, with a coding-screen post-mortem template. v3 replaces that with **first founding/core-eng conversations on the bet path**: the loop is no longer "pass a coding screen" but "**run §6 against a live bet candidate and decide GO-candidate / bridge / drop**." The post-mortem template is replaced by a **per-conversation §6 scorecard** (founding-tier allocation, tier-1 backing, core-criticality, FDV ceiling, token reality, remote-native, cycle timing). The artifact you bring is the shipped `perp-dex-core` v1.0 + the cross-margin/liquidation engine (the principal-defining artifact, §3 convergence cell), not a LeetCode warm-up. The crypto-MM cash-seat bridge runs **in parallel** (W127) so no conversation is load-bearing for runway — that is the §7 "no shot is all-in" discipline. Dubai/SG remains **prep-only**; this week only *evaluates* whether the §7 flip-trigger is firing, it does not relocate.

This is a **bet-conversation week, not a Build week**. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding. Every item below instead carries an explicit artifact file + an Output line that proves it done, and the per-conversation work carries a hardened-acceptance pass criterion (a filled §6 scorecard) and a mental model of why the item exists now. The week's job: run the **first four founding/core-eng conversations** with venues off your W116 §6-filtered shortlist without burning the day-job, and turn each one into a structured §6 scorecard so W127–W128 deep-dives are targeted at your *measured* GO-candidates, not a vibe. The discipline this week is the §6 rubric, not charisma — a conversation you don't score against §6 is a wasted shot, because §6 is the gate that separates a **bet** (founding-tier, tier-1-backed, core-critical) from a **bridge job** (income floor only). The single most important muscle: **a venue that fails any one of the seven §6 points is not a bet — it is at best a bridge — and you must say so on the scorecard the same day, before the excitement of "they liked my engine" rationalizes the gap away.**

The scorecard artifact is one append-only file, `notes/bet_conversations_w126.md`, with one section per venue. Each section uses a fixed template so the Saturday cross-venue analysis can pattern-match across them (the seven rows ARE §6 — do not invent your own criteria):
```
## Venue N — <name> — <date> — <role being discussed> — <who I spoke to (name/role)>
- Stage / round: <seed / A / B / pre-TGE?> ; remote posture: <remote-native / hub-required>
- §6.1 Founding-tier allocation:  <PASS/FAIL/UNKNOWN> + evidence (band quoted? "we'll see"? scraps?)
- §6.2 Tier-1 backing:           <PASS/FAIL/UNKNOWN> + which of {Paradigm,a16z-crypto,Dragonfly,Polychain,1kx,Multicoin,Jump,Wintermute} on the cap table
- §6.3 Core-criticality:         <PASS/FAIL/UNKNOWN> + evidence (do I build the matching/risk/settlement core, or a peripheral integration?)
- §6.4 Multi-billion-FDV ceiling: <PASS/FAIL/UNKNOWN> + comparable (Hyperliquid/dYdX/GMX/Variational/Ostium) + TAM note
- §6.5 Token reality:            <PASS/FAIL/UNKNOWN> + pre-TGE? vesting (~1y cliff/4y)? entry-FDV ≥10× headroom? unlock-dump risk?
- §6.6 Remote-native:            <PASS/FAIL/UNKNOWN> + Vietnam-remote works? colo/on-site demand?
- §6.7 Cycle timing:             <PASS/FAIL/UNKNOWN> + cycle-tailwind inside the vesting window?
- §6 VERDICT:  GO-CANDIDATE (all 7 pass) / BRIDGE-ONLY (income floor, ≥1 fail) / DROP (multiple structural fails)
- Their interest signal: <"would bring you in" / "let's keep talking" / "we'll be in touch" / unclear>
- Which artifact landed: <perp-dex-core v1.0 / cross-margin engine / cluster-VOPR / blog> + their reaction
- Open §6 UNKNOWNs → feeds W127: <the ONE diligence question this conversation tells me to chase next>
- Flip-trigger note (§7): <does this venue cluster in Dubai or SG? on-site premium hinted? — track, do NOT decide>
```

**Monday — Founding/core-eng conversation: venue 1**

- [ ] Pre-call prep + the conversation itself + same-day §6 scorecard. Artifact: `notes/bet_conversations_w126.md` (create it; private). 15 min before the call, skim your own W113 `perp-dex-core` portfolio 1-pager and the venue's public token/cap-table docs (lookup, not re-study) so you can lead with the **cross-margin + liquidation engine** (the §3 convergence cell — domain-hard ∧ latency-hard ∧ systems-hard, the thing no peripheral hire can claim). Run the conversation. Immediately after, fill the venue-1 section of the template above while it's fresh. (~90 min: 15 prep + 45 conversation + 30 scorecard)
  - 🎯 Hardened acceptance: the venue-1 section has all seven §6 rows scored PASS/FAIL/UNKNOWN with *evidence* (not blank, not "seems fine"); the §6 VERDICT line is one of GO-CANDIDATE / BRIDGE-ONLY / DROP and is *consistent with the seven rows* (any FAIL ⟹ not GO-CANDIDATE); the "Open §6 UNKNOWNs → feeds W127" line names exactly one concrete diligence question (e.g. "confirm whether the founding-eng grant is in the 0.5–2% band or employee scraps — ask for the band in writing") that can be chased in W127.
  - *Mental model*: the value of a first bet conversation is not "did they like me" — it's the **§6 diagnosis**. You have four conversations in five days; if each yields a §6 verdict + one named UNKNOWN, you enter W127's deep-dives with a four-venue, evidence-based GO/BRIDGE map instead of re-litigating everyone. Scoring §6 within 30 min of the call (before memory and flattery decay) is the whole technique — at +24h "they were excited about my engine" will have quietly overwritten "they wouldn't name an allocation band," which is the §6.1/§6.5 FAIL that actually decides whether this is a bet. The Airwallex anti-pattern (§6.1: late-stage, tiny employee equity → never a bet) is exactly what an unscored conversation lets you walk into.
  - Output: `notes/bet_conversations_w126.md` exists with a complete `## Venue 1` section (all 7 §6 rows + verdict); `grep -c '^## Venue' notes/bet_conversations_w126.md` ≥ 1.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday scorecard commit.

**Tuesday — Founding/core-eng conversation: venue 2**

- [ ] Pre-call prep + conversation + same-day §6 scorecard. Artifact: append `## Venue 2` section to `notes/bet_conversations_w126.md`. Same loop as Monday, tuned to venue 2's stack and cap table. Before the call, re-read Monday's "Open §6 UNKNOWNs" line — if Monday surfaced an UNKNOWN that venue 2 might also have (e.g. vague token vesting), pre-write the §6.5 diligence question so you actually *ask* it this time. (~90 min)
  - 🎯 Hardened acceptance: venue-2 section has all 7 §6 rows + a consistent verdict; if Monday's UNKNOWN recurs for venue 2 (e.g. both dodge the allocation band), the scorecard records it as a **pattern** ("2/2 venues won't name a band — sharpen the ask: request the founding-eng equity band in writing before any work-trial") — a recurring §6 dodge is a high-priority W127 diligence item and a yellow flag on the whole shortlist.
  - *Mental model*: two conversations in two days lets you start closing the §6 loop immediately — Tuesday is your first chance to test whether Monday's diagnosed UNKNOWN is real and answerable under direct questioning, before it costs you a work-trial at a venue that was never a bet. Same §6 dodge twice (especially on §6.1 allocation or §6.5 token reality) is the strongest possible signal that the shortlist's *top* of funnel is weak and you should widen outreach (the M24 controllable-outbound lever, §5).
  - Output: `notes/bet_conversations_w126.md` has a complete `## Venue 2` section; venue count ≥ 2.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday scorecard commit.

**Wednesday — Day-job balancing + mid-week §6 consolidation (recovery day, no conversation)**

- [ ] Coast-mode day-job + mid-week consolidation. Artifact: append a `## Mid-week consolidation` note to `notes/bet_conversations_w126.md`. Deliberately under-spend on the day-job today (do what's required, ship nothing ambitious) to protect energy for Thu/Fri conversations — burnout mid-cycle is the documented Phase-7 risk (README.md §9: "M24 outbound response rate low" + the interview/outreach-cycle saturation noted for Phase 7), and you are still drawing a salary that funds the runway between bets (§7 Bridge₀). Spend 30 min consolidating: re-read venue 1 + venue 2 scorecards, write the 1–2 *cross-venue* §6 patterns (which of the seven points keeps coming back PASS, which keeps coming back FAIL/UNKNOWN across both), and pick the single highest-leverage diligence question to walk into Thursday with. (~50 min)
  - 🎯 Hardened acceptance: consolidation note names ≥1 cross-venue §6 pattern (or explicitly "no common pattern yet, n=2") tied to a specific §6 point number; names the one pre-Thursday diligence question chosen and why (which §6 point it de-risks); records a one-line energy/burnout self-check (green/yellow/red) so the risk is tracked, not ignored; records a one-line **§7 flip-trigger status** ("0/2 venues hub-clustered — no flip; Vietnam-remote default holds" — track, do not act).
  - *Mental model*: the mid-week no-conversation day exists on purpose — four bet conversations back-to-back-to-back is how you arrive at the W127 deep-dives depleted and mis-score §6 under fatigue. Coasting the day-job here is not slacking; it is **capacity management + runway protection** for the part of the cycle that actually decides where Bet #1 lands. The consolidation also prevents four isolated scorecards from never being synthesized until Saturday — by then it's too late to sharpen the Thu/Fri §6 questions. And the flip-trigger line is the §7 discipline made concrete: you *watch* whether warm conversations cluster in Dubai (crypto-native) or SG (institutional), but you do **not** relocate until the trigger fires (a cluster of warm convos in one hub, or a presence-contingent offer premium).
  - Output: `notes/bet_conversations_w126.md` has a `## Mid-week consolidation` section with §6 pattern(s) + chosen diligence question + energy self-check + flip-trigger status line.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday consolidation commit.

**Thursday — Founding/core-eng conversation: venue 3**

- [ ] Pre-call prep + conversation + same-day §6 scorecard. Artifact: append `## Venue 3` to `notes/bet_conversations_w126.md`. Use Wednesday's chosen diligence question as the conversation's sharpened ask (work it in early — §6.1/§6.5 answers harden the verdict; don't leave them UNKNOWN). Same loop. (~90 min)
  - 🎯 Hardened acceptance: venue-3 section has all 7 §6 rows + a consistent verdict; the "Which artifact landed" line records whether leading with the **cross-margin + liquidation engine** (vs leading with the full `perp-dex-core` system) produced a stronger §6.3 core-criticality signal ("they immediately framed me as the risk-engine owner" = strong §6.3; "they asked if I'd do integrations" = §6.3 FAIL / peripheral = BRIDGE-ONLY). The "Open §6 UNKNOWNs → feeds W127" line is distinct from prior venues' OR explicitly marks a recurring §6 gap (recurring = escalate to a W127 written-diligence block).
  - *Mental model*: by the third conversation you should be able to compare your *positioning* across venues, not just their offers — a founding-engineer pitch lives or dies on §6.3 core-criticality, and the convergence-cell artifact (the `risk-engine` cross-margin engine, §3) is your strongest claim to *being the company's core*, not a peripheral hire. Note in the scorecard which framing makes them treat you as core vs as an integrator — that framing IS the §6.3 signal, and §6.3 is the difference between a founding-tier grant and employee scraps.
  - Output: `notes/bet_conversations_w126.md` has a complete `## Venue 3` section; venue count ≥ 3.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday scorecard commit.

**Friday — Founding/core-eng conversation: venue 4**

- [ ] Pre-call prep + conversation + same-day §6 scorecard. Artifact: append `## Venue 4` to `notes/bet_conversations_w126.md`. Same loop. This is the last first-pass conversation of the cycle — note explicitly whether your §6 verdicts are *converging* (a clear GO-candidate or two emerging) or *scattered* (everyone BRIDGE-ONLY / nobody clears all seven), because that distinction changes the W127 strategy. (~90 min)
  - 🎯 Hardened acceptance: venue-4 section has all 7 §6 rows + a consistent verdict; a one-line verdict at the end of the section: "GO-candidates emerging: <venue list>" or "no GO-candidate yet — all BRIDGE-ONLY on §6.<n>; widen outreach + lean on crypto-MM bridge (W127)". The flip-trigger note records the running hub-cluster count across all 4 venues.
  - *Mental model*: converging on ≥1 GO-candidate means W127 is a *deepen-and-diligence* week (chase the UNKNOWNs, set up a trial collaboration with the top GO-candidate). Scattered/all-BRIDGE means you have a **funnel problem**, not a pitch problem — and W127 should both widen outreach AND prioritize the crypto-MM cash-seat bridge so runway is never the thing that forces a bad bet (§7: "never quit a bridge before vest milestones"; the bridge is the income floor underwritten by your Half-2 latency craft). You can only make this call with four §6 scorecards in hand, which is exactly why they are scored identically — to make GO/BRIDGE comparable across venues.
  - Output: `notes/bet_conversations_w126.md` has a complete `## Venue 4` section; venue count ≥ 4.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday scorecard commit.

**Saturday — Recover + cross-venue §6 synthesis (all 4) + bet/bridge scorecard**

- [ ] Cross-venue §6 synthesis + GO/BRIDGE scorecard + W127 plan. Artifact: `notes/bet_scorecard_w126.md` (private). Read all four venue sections + the mid-week note. Produce: (1) a **§6 GO/BRIDGE matrix** — the four venues as rows, the seven §6 points as columns, each cell PASS/FAIL/UNKNOWN, with the final GO-CANDIDATE / BRIDGE-ONLY / DROP verdict per venue; (2) a **ranked diligence backlog** — the open §6 UNKNOWNs across all four, ranked by (does-it-flip-a-verdict × how-hard-to-confirm), each attributed to which venue(s) raised it; (3) a concrete **W127 plan** — which 1–2 GO-candidates to deepen / set up a trial collaboration with, which to drop, and a one-line note that the crypto-MM bridge interviews run in parallel (so no single conversation is load-bearing for runway); (4) a **§7 flip-trigger verdict** — "no cluster, Vietnam-remote default holds" OR "warm conversations clustering in <Dubai/SG> — begin cheap prep (visa + token-tax), still no relocation." (~50 min)
  - 🎯 Hardened acceptance: the §6 matrix has all 4 venues × 7 §6 points filled (no blank cells — UNKNOWN is allowed and counts as "diligence needed"); every venue's verdict is *consistent* with its row (a GO-CANDIDATE has zero FAILs and its UNKNOWNs are listed in the diligence backlog); the diligence backlog is *ranked* and each item cites which venue(s) surfaced it (no unsourced "I should research X"); the W127 plan names ≤2 deepen-targets + any drops + the parallel-bridge line; the flip-trigger verdict is one of the two stated forms and matches the running hub-cluster count. The plan must be schedulable into W127 as-is.
  - *Mental model*: this is the deliverable the whole week feeds — without it, four conversations leave you with vague optimism instead of a **bet decision pipeline**. The §6 matrix forces you to spend W127 deepening venues that *actually clear the bet gate*, not whichever founder was most charismatic. The matrix converts "I talked to four venues" into "venue 2 is the GO-candidate, venue 1 is BRIDGE-ONLY (§6.1 fails — late-stage scraps), venues 3–4 drop — and I run crypto-MM interviews in parallel so runway never forces my hand" — a **decision, not a vibe** — which is the direct input to the W131 Bet #1 GO/no-go call (GO only if §6 passes). This is the §7 shots-on-goal structure operating at the conversation level: you are not picking one all-in shot, you are filtering a funnel down to the §6-passing few while keeping the bridge alive.
  - Output: `notes/bet_scorecard_w126.md` exists with the 4×7 §6 GO/BRIDGE matrix, a ranked + venue-attributed diligence backlog, a W127 plan (≤2 deepen-targets + drops + parallel-bridge line), and a §7 flip-trigger verdict.
- [ ] Commit + log (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday synthesis commit.

---

## [v3.1 BFT apex — Track B] W126: happy-path BFT commit end-to-end (4-node cluster, all four seams composed)

> Track B continues (W125 discharged the liveness card). All four seams + the commit rule now compose into a working `BftReplica = Replica<SM, BftQuorum, SignedPeers, PipelinedHotStuffView, DetectEquivocation>`. This week proves they **commit end-to-end** on a 4-node cluster, no faults yet (Byzantine VOPR is W129+). Bar (c).

- [ ] **[Tue +90min · Thu +90min]** [Venue · BFT] **Build (bar c)**: `crates/consensus-vsr/tests/bft_happy_path.rs` + the `BftReplica` wiring in `bft/mod.rs` — stand up a 4-node in-process `BftReplica` cluster (reusing the W103 `VsrVenueCluster` harness shape, swapping the replica type) and drive a clean op stream through the full pipeline: leader proposes a block (extends high-QC) → 2f+1 **persist-then-sign** votes (`SignedPeers`; each replica persists its voting safety-state — last-voted-round + high-QC — via the `Storage` seam BEFORE the vote leaves the replica: the W118 durability decision, in-scope here) → leader forms a QC (`BftQuorum`+aggregate) → chains the next block → 2-chain rule commits (consecutive rounds) → ops get dense `OpNumber`s → `drain_committed` delivers them with `proposal_handle()`. (~total 180 min)
  - **Phase 1 (compact — seal in `notes/126_happy_path_prediction.md` before coding):**
    - *Prediction (sealed)*: write the two ways this pipeline can stall on a CLEAN network (no faults, no Byzantine nodes) — before reading the traps below.
    - *Back-of-envelope*: per view ≈ 1 broadcast RTT (~100–300 µs LAN / ~µs sim-bus) + batch-verify ~50–80 µs + persist-before-vote ~20–100 µs (group-committed, overlappable) → a commit needs 2 consecutive views ≈ ~700 µs LAN median; predict the pipelined-vs-one-commit-at-a-time throughput ratio for 1000 ops (it is ~2× per added pipeline stage, not marginal).
    - *Pre-mortem (interleaving)*: replica votes at round r → crashes before its safety-state write hits disk → restarts, replays round r, sees the *conflicting* proposal first → votes again → an HONEST replica has equivocated; `DetectEquivocation` fires evidence against your own node in W129 scenario #1.
  - 🧮 **Paper drill** — write in `notes/126_happy_path_drill.md`:
    - D-QUORUM on the 4-node f=1 cluster (replicas R0–R3, R0 leader) hand-tracing one full happy-path commit: draw the four replicas + their logs; mark the BFT quorum (2f+1 = 3 of 3f+1 = 4) and show any two such quorums intersect in ≥1 *honest* node; trace block B_k `propose (extends high-QC) → 3 prevotes → QC_prevote → 3 precommits → QC_precommit`, then chain B_{k+1} at the consecutive round and apply the **2-chain rule** to commit B_k; mark each replica's high-QC after every step. Negative: drop one QC link (e.g. B_{k+1} proposed on a STALE high-QC, breaking the consecutive-round chain) and show no commit fires — the 1-chain trap (W123) under composition.
    - D-LIN on the committed log: take the dense `OpNumber` sequence committed across the 1000-op run and show it is a single total order each replica applies identically (each commit point = one linearization point); then exhibit the divergence — replica R2 applies op n+1 before op n (a wrong apply-order / a second log) and show its `state_hash()` diverges from R0/R1/R3 at that commit, the anomaly `bft_4node_commits_1000_ops` catches.
    - D-FAIL on a single crashed replica: table the scenario — R3 crashes → blast radius (does any committed op stall?) → recovery path (cluster still forms a 2f+1=3 quorum from {R0,R1,R2}) → invariant stressed (quorum overlap with f=1 survives one fault); show the commit still fires on 3/4 and conclude "survives single-replica failure with no lost commit because 2f+1 ≤ honest-and-up nodes."
  - 🔧 **Why this matters**: this is the integration moment for the four seams — each was unit-tested in isolation (W119–W124); W126 proves they *compose* into a live commit pipeline. It mirrors the W103/W108 VSR cluster harness exactly (same `MessageBus`/`Storage`/`Clock` seams — the deterministic-sim discipline), so a green BFT happy-path maps onto the same VOPR scaffolding W129 drives with faults. The traps: (1) the pipeline stalls because the leader proposes the *next* block before the previous QC forms (pipelining requires proposing on the high-QC each view, not waiting for full commit); (2) a vote signed over the wrong digest (the block-hash the QC certifies must be the exact bytes the SM applies — a serialization mismatch = no QC ever forms); (3) commit advancing on a 1-chain (W123 trap resurfacing under composition).
  - *Contract*: `pub type BftReplica<SM> = Replica<SM, BftQuorum, SignedPeers, PipelinedHotStuffView, DetectEquivocation>;` + `BftCluster::new(4)` (4-node, f=1) with the deterministic `Clock`/`MessageBus` from `runtime-thread-per-core` (W43-class) so the test replays byte-identically.
  - **Tests:**
    - `bft_4node_commits_1000_ops` — 4-node cluster, clean network; 1000 ops; assert every replica reaches the same `commit_point()` and byte-identical `state_hash()` at every commit (the BFT analogue of W103's VSR convergence test). N=4 tolerates f=1 (quorum 3).
    - `bft_pipelines_without_full_commit_stall` — assert throughput reflects pipelining (a new block proposed each view on the high-QC, not one-commit-at-a-time); a non-pipelined impl is correct-but-slow and is flagged (feeds the W135 latency budget).
    - `bft_vote_digest_matches_applied_bytes` — the digest the QC certifies equals the digest of the bytes the SM applies; a serialization mismatch (no QC forms / wrong block commits) FAILS. Cross-seam (QC × SM).
    - `bft_commit_point_equals_dense_op_contiguous` — `commit_point()` advances over contiguous dense `OpNumber`s (O4) across the 1000-op run; no gaps.
    - 🪤 **trap `crash_restart_does_not_double_vote` (SENIOR · durability)** — a replica votes at round r and crashes before persisting its safety-state; on restart it MUST NOT vote a conflicting block at r (with the correct persist-before-vote ordering the unpersisted vote was never sent; the harness asserts no two conflicting signed votes from one replica at any round across the crash). Traps the send-then-persist ordering.
    - 🪤 **negative-control `planted_double_propose_yields_evidence` (SENIOR)** — plant a leader that double-proposes at one height on the happy-path harness and assert `DetectEquivocation` emits `Evidence` — a test-the-test proving the composed cluster's detection seam is live (not monomorphized away), before W129 relies on it under real Byzantine scheduling.
  - **Make-pass card (SENIOR):** `bft_4node_commits_1000_ops` stalls → symptom: cluster commits a few ops then hangs; why: leader waits for full commit before proposing the next block (no pipelining) AND a vote-digest mismatch prevented QC formation; fix: propose each view on the current high-QC (pipeline), and certify the QC over the canonical block digest the SM applies; cost avoided: a "BFT that works in theory" that can't reach the throughput a venue needs; muscle: pipelined HotStuff proposes continuously on the high-QC; the QC digest IS the applied-bytes digest. Reapplies: W129 (faults on this pipeline), W135 (latency on this pipeline).
  - **Make-pass card (SENIOR · durability):** `crash_restart_does_not_double_vote` fails → symptom: VOPR scenario #1 fires equivocation `Evidence` against an **HONEST** replica after a crash-restart; why: the vote was sent before the safety-state (last-voted-round + high-QC) was persisted, so the restarted replica had no memory of having voted at r; fix: persist last-voted-round + high-QC BEFORE the vote leaves the replica (group-committed NVMe write ~20–100 µs/round, inside the W118 2 ms envelope); cost avoided: an honest node turning Byzantine on every crash — safety degraded to f−1 tolerance without any adversary; muscle: in BFT, voting state is durable state — persist-before-send, never send-then-persist. Reapplies at: W129 scenarios #1/#6, W135 (the persistence term in the latency budget).
  - **Output**: `bft_happy_path.rs` green; `BftReplica` composes all four seams; 4-node clean commit + convergence proven; reuses the W103/W108 deterministic harness (ready for W129 fault injection).
- [ ] **[Sat +10min]** [Venue · BFT] Handoff note → `notes/126_bft_happy_path.md`: BftReplica commits end-to-end on 4 nodes (persist-before-vote in the loop); reuses the W108 cluster-VOPR harness. **Next W127: handle/abandonment wiring end-to-end** (propose→ProposalHandle / drain_committed+proposal_handle / drain_abandoned incl. permanent-prune) on the real BFT cluster. (~10 min)
- [ ] **[Sat +10min]** Commit + log (~10 min)

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md. Track B: 4-node BFT happy-path commit green (all four seams compose); next is end-to-end handle/abandonment wiring (W127).
