# Week 24 — Phase 2 close + Phase 3 prep (`consensus-engine` scaffold, 14-crate workspace audit)

> **Phase**: 2
> **Month**: M6
> **Track**: Reth (primary), Tempo (awareness)
> **Hours target**: 30h/wk (M1-M18)
> **Mirror target**: `reth` top-level architecture (node-builder component graph); `consensus-engine` placeholder mirrors `reth-engine`/`reth-consensus` (Engine API fork-choice analogue — full build lands W75 core methods, v1.0 W91)
> **Crate(s) touched**: **[NEW]** `crates/consensus-engine/` (scaffold only); workspace (14-crate `cargo build` + dependency-graph audit); `.github/workflows/ci.yml`
> **Bar**: (b) — `consensus-engine` is a Reth-track Layer-5 product crate (bar b). This week ships only its scaffold; no hot-path code yet.
> **Feeds into**: W75 `consensus-engine` core methods (fork-choice, payload validation); W91 `consensus-engine` v1.0; immediate Phase-3 storage-trie build (W25 `wal` v0.1 is the critical new dependency for storage-trie).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): *Mastering Ethereum* — consensus / The Merge chapter (PoW→PoS, finality) — lookup only; reth docs — node-builder component diagram & Engine API (`engine_forkchoiceUpdated`, `engine_newPayload`); gakonst reth-architecture talk + Paradigm Frontiers talk (component-map lookup, not cover-to-cover).
> **v2 modifications**: none (W15-W28 unchanged from v1)
>
> **Load note**: this is a Phase-close / Phase-3-prep week — review, audit, CI, and a single crate *scaffold* (no
> 3-phase trap arc: there is nothing testable yet, per SPEC §6 crate-scaffold tier). Heaviest day is Thursday (CI +
> README graph ≈ 1.5h); Wednesday and Friday are deliberately light to leave headroom for any spillover Phase-2 PRs.
> The real trap-test Build arcs resume W25 (`wal` v0.1, bar (c)).

**Monday — The Merge consensus model + `consensus-engine` placeholder crate**

- [ ] **Recall + map**: `notes/24_merge_consensus.md` — capture the PoW→PoS Merge model: where the execution
  layer (reth) ends and the consensus layer (CL client) begins, and the 3 Engine API entry points reth exposes
  (`engine_newPayload`, `engine_forkchoiceUpdated`, `engine_getPayload`). (~30 min)
  - 🎯 Hardened acceptance: `notes/24_merge_consensus.md` names all 3 Engine API methods AND draws the
    EL↔CL boundary (who proposes blocks, who validates, who decides the canonical head). One sentence each on
    what would break if you skipped fork-choice persistence across restart.
  - *Mental model*: `consensus-engine` (scaffolded today, built W75/W91) is reth's side of this boundary — it
    receives fork-choice updates from a CL client and drives execution + storage. Knowing the boundary now is
    what makes the W75 core-methods build legible instead of a wall of unfamiliar RPC method names.
- [ ] **Build (scaffold)**: `crates/consensus-engine/Cargo.toml` + empty `crates/consensus-engine/src/lib.rs` —
  new workspace member, the placeholder for reth's consensus + Engine API re-implementation. (~20 min)
  - 🔧 **Why a scaffold now, not a build**: per SPEC §6 a crate-scaffold has nothing testable yet, so it gets a
    path + mirror + deps + downstream + Output line — NOT a 3-phase trap arc. The real arc lands W75.
  - *Mirror*: `reth-engine` / `reth-consensus` (Engine API fork-choice analogue; see README Inheritance Map ~L237).
  - *Deps it will eventually pull* (declare none yet, just note in a `// TODO(W75):` comment in lib.rs):
    `eth-consensus`, `eth-stage`, `exec-vm`, `storage-trie`, and later `consensus-bft` (README Appendix E ~L817).
  - *Downstream consumers*: W75 core methods → W91 v1.0; Tempo `tempo-payment-lane` v0.1.0 (W91) inherits the
    priority/fork-choice idea (README ~L823).
  - *lib.rs contents today*: a doc comment stating the crate's purpose + EL/CL boundary, a `// TODO(W75):` list of
    the core methods (`new_payload`, `fork_choice_updated`, `get_payload`), and nothing else.
  - **Output**: `cargo build -p consensus-engine` green (empty lib); `crates/consensus-engine/` appears in the
    workspace `members` list; `git status` shows the new crate dir tracked.
- [ ] **Workspace audit**: `cargo build --workspace` green across all 14 crates — `eth-primitives`, `eth-rlp`,
  `eth-storage-cache`, `eth-network-codec`, `eth-consensus`, `eth-eips`, `eth-rpc-types`, `eth-trie`, `eth-stage`,
  `exec-vm`, `eth-primitives-derive`, `time`, `backpressure`, `bufpool`, plus the `storage-trie` and
  `consensus-engine` scaffolds. (~15 min)
  - 🎯 Hardened acceptance: `cargo build --workspace 2>&1 | tee notes/24_workspace_build.txt` ends with
    `Finished` and zero warnings; the build log lists every one of the 14 crates + 2 scaffolds compiling. A
    silent drop (a crate not in `members`) fails this — grep the log for each crate name.
- [ ] Commit + log (~10 min)

**Tuesday — Reth architecture: map every reth component to YOUR crate**

- [ ] **Architecture map**: `notes/24_reth_component_map.md` — watch the gakonst reth-architecture talk + Paradigm
  Frontiers talk as a *lookup* (skip to the component-diagram sections), then map each reth component to the
  workspace crate that mirrors it. (~75 min)
  - 🎯 Hardened acceptance: `notes/24_reth_component_map.md` is a 2-column table (reth component → your crate)
    covering at minimum: `reth-primitives`→`eth-primitives`, `reth-codecs/rlp`→`eth-rlp`, `reth-db`/storage→
    `storage-trie`+`bufpool`, `reth-stages`→`eth-stage`, `reth-evm`/revm→`exec-vm`, `reth-engine`/consensus→
    `consensus-engine`, `reth-trie`→`eth-trie`. Each row notes whether your crate is shipped, scaffold, or future.
  - *Mental model*: the inheritance principle made literal — every exercise mirrors an upstream module. This map
    IS the inheritance ledger for the Reth track; it's what you cite when a future PR review asks "what upstream
    does this correspond to?" and what the W91 retrospective measures inheritance-ratio against.
- [ ] **[Tempo] toolchain smoke**: `git clone https://github.com/tempoxyz/tempo /tmp/tempo` then
  `cargo build --release` (run in background). Confirms your toolchain builds against their pinned reth revision.
  On failure: record the failure mode in `notes/tempo_build_blockers.md` and move on — do NOT debug. (~20 min)
  - **Output**: either `/tmp/tempo/target/release/` populated (build green), OR `notes/tempo_build_blockers.md`
    has a dated entry with the exact error + the pinned reth rev from their `Cargo.lock`. Either outcome is a pass.
- [ ] Commit + log (~10 min)

**Wednesday — Phase 3 scope + Month 7 outline (light day)**

- [ ] **Phase 3 outline**: `notes/24_phase3_outline.md` — read the Phase 3 section of the plan as a lookup, then
  outline the Month 7 approach. (~45 min)
  - 🎯 Hardened acceptance: `notes/24_phase3_outline.md` names, in dependency order, the Month-7 crate chain:
    `wal` v0.1 (W25-26) → `storage-trie` MDBX + B-tree (W27-28) → `recovery` v0.5 + MVCC (W29-30). It must
    flag, in bold, that **`wal` v0.1 (W25) is a hard prerequisite for `storage-trie` durability** and that `wal`
    is a **bar (c)** crate (so its build resumes the full trap-test arc + two-track perf rules).
  - *Mental model*: Phase 3 is the storage spine. Sequencing it wrong (building the trie before the WAL) means
    durability is bolted on after the fact — the classic mistake this outline exists to prevent.
- [ ] Commit + log (~10 min)

**Thursday — Phase 3 scaffolding: CI gates + workspace README dependency graph**

- [ ] **CI pipeline**: `.github/workflows/ci.yml` — a reusable gate that runs `cargo fmt --check`, `cargo clippy
  -D warnings`, `cargo nextest run --workspace`, and `cargo miri test` (miri on a weekly schedule, not per-push).
  (~60 min)
  - 🎯 Hardened acceptance: push a deliberately-broken branch (e.g. an unformatted file + a `clippy::needless_return`)
    and confirm the PR check goes RED on exactly fmt + clippy; revert and confirm GREEN. The miri job appears on
    the weekly `schedule:` trigger, not on `push:`. `git log` shows the red→green CI transition.
  - *Mental model*: CI is the durable acceptance bar for every future bar (b)/(c) crate — fmt/clippy/nextest now,
    and the `miri` weekly job is the seat for the loom/miri trap-tests that resume at W25. Wiring it before
    Phase 3 means W25's first concurrency PR lands into a gate that already runs miri, not one you scramble to add.
- [ ] **Workspace README dependency graph**: `README.md` at workspace root — an ASCII/Mermaid dependency graph of
  all 14 crates + 2 scaffolds showing who depends on whom (Layer-1 substrate at the bottom: `time`, `bufpool`,
  `backpressure`; product crates on top). (~30 min)
  - 🎯 Hardened acceptance: the graph in workspace-root `README.md` has no cycles, places `bufpool`/`time` as
    leaf dependencies (nothing they depend on inside the workspace), and shows `storage-trie` + `consensus-engine`
    as scaffold nodes with dashed/“(scaffold)” edges to their future deps. Cross-check each edge against the actual
    `[dependencies]` in each crate's `Cargo.toml` — a drawn edge with no matching dep fails this.
  - *Mental model*: the dependency graph is the buildability invariant of the workspace; a cycle here is a compile
    error waiting to happen, and the layer ordering is the same substrate-first discipline the whole plan rests on.
- [ ] Commit + log (~10 min)

**Friday — Final Phase 2 PRs (light day, headroom for spillover)**

- [ ] **Wrap outstanding PRs**: address any open review comments on the Phase-2 crate PRs and merge/close them.
  Record the final state in `notes/24_phase2_pr_ledger.md`. (~30 min)
  - 🎯 Hardened acceptance: `notes/24_phase2_pr_ledger.md` lists every Phase-2 PR with status (merged / closed /
    carried to W25) and a one-line reason for each carry-over. Zero PRs in an ambiguous "open, no plan" state.
- [ ] Commit + log (~10 min)

**Saturday — Phase 2 review + shipped-crate verification**

- [ ] **Phase 2 assessment**: `notes/24_phase2_review.md` — full self-assessment of Phase 2 against its goals
  (what shipped, what slipped, what carried to Phase 3). (~30 min)
  - 🎯 Hardened acceptance: `notes/24_phase2_review.md` answers 3 concrete questions: (1) which crates hit their
    target version, (2) which slipped and the root cause, (3) the single biggest risk carried into Phase 3
    (expected answer: `wal`/`storage-trie` durability sequencing). No vibes-only "went well" lines.
- [ ] **Shipped-crate verification**: confirm each Phase-2 deliverable is at its tagged version by running its test
  suite. Record results in `notes/24_phase2_review.md`. (~20 min)
  - 🎯 Hardened acceptance: for each of — `eth-primitives` v0.2, `eth-rlp` v0.2, `eth-storage-cache` v0.2
    (bufpool-backed), `eth-network-codec` v0.2, `eth-consensus` v0.4, `eth-eips` v0.2, `eth-rpc-types` v0.1,
    `eth-trie` v0.2, `eth-stage` v0.0.1, `exec-vm` v0.1, `eth-primitives-derive` v0.1, **`time` v0.1**,
    **`backpressure` v0.1**, **`bufpool` v1.0**, `storage-trie` scaffold, `consensus-engine` scaffold —
    run `cargo test -p <crate>` and confirm green + the version in `Cargo.toml` matches the claimed tag. A version
    drift (tag says v0.2, Cargo.toml says v0.1) fails this check.
  - *Mental model*: `bufpool` v1.0 and `time` v0.1 are bar (c) Layer-1 substrate — their green suite is the
    foundation everything in Phase 3 (`wal`, `recovery`, `storage-trie`) is about to stack on. Verifying them now
    means a Phase-3 failure is in the new code, not a rotted dependency.
- [ ] **Progress ledger**: update `progress.md` — Phase 2 closed, Phase 3 opened. (~30 min)
  - **Output**: `progress.md` shows Phase 2 marked complete with the shipped-crate list and Phase 3 listed as
    in-progress starting W25.
- [ ] **[Tempo] end-of-phase metrics**: append to `progress.md` — orientation depth target 1 (should hit); TIPs
  read target 1, actual 3 (TIP-1020 W20, TIP-20 W22, TIP-1031 W23); Tempo PRs 0 (correct — first Tempo PR is W60).
  (~10 min)
  - 🎯 Hardened acceptance: `progress.md` Tempo section records all 3 TIPs by id+week and explicitly states PRs=0
    is on-plan (not a miss), so the W95 calibration doesn't mis-read it as a slip.
- [ ] Commit + log (~10 min)

**Sunday — End Phase 2 + Phase 3 prep**

- [ ] Full rest. (~5 min)
- [ ] Phase 3 starts tomorrow (W25 `wal` v0.1 — bar (c), trap-test arc resumes). Skim `notes/24_phase3_outline.md`
  one last time so Monday opens cold-start-ready. (~30 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
