# Week 113 — Binance-alumni map activation + bet-outreach ramp (founders AND crypto-MM desks)

> **Phase**: 6
> **Month**: M28
> **Track**: (bet-path) — Binance-alumni map activation + bet-outreach ramp (the §7 shots-on-goal warm-up); zero Build, but the entire week is in service of the M30 Bet-Readiness Gate (W117)
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: (bet-path — no upstream module mirrored. The artifacts this week *points at* are the shipped venue: `perp-dex-core` v1.0 (W103–W109, 3-node VSR + cluster-VOPR), `risk-engine` v1.0 (W99, the principal-defining artifact), `liquidation-engine` v1.0 (W102), the Blog #4/#5 posts (W109/W112). The decay-edge recall targets mirror TigerBeetle VSR, ScyllaDB reactor, Aeron threading — the talking points a founding-eng / crypto-MM conversation probes.)
> **Crate(s) touched**: (none — bet-outreach + portfolio-packaging + decay-edge recall against the *already-shipped* venue crates; `perp-dex-core` v1.0 is frozen since W109 and is not edited this week)
> **Bar**: (n/a) — bet-path / outreach week, no Build. The artifacts it packages are bar (c) (the venue critical path) and bar (b) (the Reth spine); no new code is written here so no bar applies. *The discipline this week imports instead of a code bar: every outreach claim must be a falsifiable, linkable artifact (a commit, a VOPR run, a p99.99 number), never an unquantified adjective — the §6 checklist is the rubric.*
> **Feeds into**: W114 (public-visibility + M30 bet-readiness prep — consumes this week's network map + outreach tracker), W115 (Blog #6 hybrid-CLOB / on-chain-settle interface — cites the conversations sourced here), W116 (ecosystem-selection §6 filter applied to ≥5 venues — the shortlist seed is built this week), W117 (M30 **Bet-Readiness Gate**: ≥2 founding/core-eng conversations advanced + shortlist ≥5 — *this week is where the "≥20 targeted conversations, ≥25% response, ≥3 Binance-alumni warm intros" M24-criterion progress is converted into M30 advanced-conversation count*), W120/W122/W125 (Phase-7 active-outreach lane — this week's tracker, scripts, and 1-pagers ARE the rehearsal corpus + contact source list)
> **References** (consult only when a recall check shows a gap or a §6 line needs a definition — never read cover-to-cover):
>   - RECONCILED_PLAN_v3.md §6 (ecosystem-selection checklist — the 7-pass GO/no-go gate), §7 (shots-on-goal structure + geography flip-trigger), §1 bars (a)/(c) end-state, §5 M30 gate, §4A v1.0 acceptance (the talking points the venue clears)
>   - TigerBeetle VSR: https://github.com/tigerbeetle/tigerbeetle/blob/main/docs/internals/vsr.md (recall target — the venue's hot-path log)
>   - ScyllaDB/Seastar reactor: https://github.com/scylladb/seastar/blob/master/doc/tutorial.md (recall target — runtime-thread-per-core mirror)
>   - Aeron threading: https://github.com/real-logic/aeron/wiki/Best-Practices-Guide (recall target — messaging-aeron mode justification)
>   - §6 backing-tier set (recall target, do not cold-read): {Paradigm, a16z crypto, Dragonfly, Polychain, 1kx, Multicoin, Jump, Wintermute}
> **v3 modifications**: This file **replaces the superseded v2 W113** ("Network warm-up + interview-prep ramp" for the v2 `mini-db` job-hunt). Three structural changes per the v2→v3 remap: **(1) the destination flips from "Tier-A HFT IC job" to the §7 bet-path** — outreach now points at *two* destinations on the Binance-alumni map: **on-chain-derivatives founders (the bets)** AND **crypto-MM desks (the income floor/bridge)**, governed by the §6 ecosystem-selection checklist. **(2) the portfolio centerpiece flips from `mini-db` to `perp-dex-core` v1.0** — the elevator pitch, the 1-pagers, and the resume now lead with the replicated multi-node venue (3-node VSR, cluster-VOPR, the `risk-engine` cross-margin convergence cell), not a generic LSM KV. **(3) "interview prep" is re-narrated as "bet-conversation + crypto-MM-screen prep"** — the decay-edge recall (VSR / reactor / Aeron) is retained because those are exactly the systems a founding-eng deep-dive and a crypto-MM latency screen probe, but the framing is "can I defend the venue I built" not "can I pass an algo screen for a salaried role." The LeetCode lane is **demoted to a thin crypto-MM-screen maintenance dose** (the bridge needs it; the bet does not).

**Bet-outreach activation week — every day pairs a NETWORK/PORTFOLIO action with a DECAY-EDGE recall against the shipped venue**

This week sits 4 weeks before the **M30 Bet-Readiness Gate** (W117). The venue (`perp-dex-core` v1.0) shipped at W109 and has been operated + blogged (W110–W112); the artifacts are *fresh and public*. The job of this week is to convert "I built it" into "the right people know I built it and want to bring me in" — the bar-(c) of §1 bar (c) (activated network) and the seed of the §6 shortlist. The protocol per day: **(A)** one outreach/portfolio action that produces a *linkable, falsifiable* artifact (a tracker row, a 1-pager, a sent message), then **(B)** one decay-edge recall + 3-min bet-conversation rehearsal against a venue talking point a founder/MM will actually probe. Decay-edge protocol (unchanged from the proven loop): (1) close all docs/source, (2) write from memory in `notes/recall_<topic>.md`, (3) open source + mark hit/miss/gap, (4) rehearse 3-min answer aloud + record. Hit threshold ≥70% recall vs source; any topic <60% triggers a re-do at W120. **The §6 checklist is the spine of the outreach work — no message goes out that isn't sized against the 7-pass GO gate.**

---

## Monday — Binance-alumni map (the source list) + VSR decay-edge recall

- [ ] **(A) Build the Binance-alumni + warm-network map** — file: `notes/network_map.md`. This is the single source list every Phase-7 outreach (W120/W122/W125) draws from; building it now (cold, 4 weeks early) forces you to surface the *real* depth-≥2 contacts rather than improvising the day-of. Two destination columns per the v3 split: **`derivatives-founder?`** and **`crypto-MM-desk?`**. For each contact: name/handle, source (Binance-alumni / Reth maintainer / Tempo maintainer / conference / Twitter / prior colleague), depth (1–3), last contact, can-they-vouch (y/n), and which destination(s) they map to. (~60 min)
  - 🎯 **Hardened acceptance**: ≥12 named contacts, each carrying depth + last-contact + vouch + both destination flags; ≥4 at depth ≥2; ≥3 explicitly tagged Binance-alumni (the §1 bar-(c) metric is "≥3 Binance-alumni warm intros logged"); ≥5 tagged `derivatives-founder?` and ≥3 tagged `crypto-MM-desk?` (the two §7 destinations both populated — a map pointed at only one destination fails the v3 lock).
  - *Mental model*: the §7 shots-on-goal structure has TWO targets — the bet (founder) and the floor (crypto-MM). A network map that only lists founders strands the income-floor leg; one that only lists MM desks strands the bet. This list is the controllable input to the M24-relabelled outbound metric (≥20 targeted conversations / ≥25% response / ≥3 Binance warm intros) and the M30 gate's "≥2 advanced conversations." It is built now, not at W120, precisely so the depth-≥2 names are discovered while there's still time to warm the depth-1 ones.
  - **Output**: `notes/network_map.md` exists with ≥12 contacts, each with depth + vouch + both destination flags; ≥3 Binance-alumni; both destination columns ≥ their thresholds.
- [ ] **(A2) Stand up the outreach tracker** — file: `notes/outreach_tracker.md` (the §6/§7 dashboard reused through W134). Columns: contact, destination (founder / MM), §6-prescreen (which of the 7 checks are *known* GO/UNKNOWN/FAIL for this target — only relevant for founder-destination), status (not-contacted / sent / replied / advanced / dead), artifact-linked (which 1-pager / blog was attached), next-action-date. Seed it with the Monday map; no messages sent yet. (~20 min)
  - 🎯 **Hardened acceptance**: tracker exists; every Monday-map contact has a row; the §6-prescreen column is populated for every founder-destination row (even if mostly UNKNOWN — the point is to see, before outreach, *which* venues already visibly fail a §6 check so you don't spend a shot on them).
  - *Mental model*: §7 says "a bet is GO only if all seven §6 pass; otherwise it's a bridge job, not a bet." Pre-screening the tracker means you never burn a warm intro on a venue that's already a visible §6 FAIL (e.g., late-stage tiny-equity = bar-1 fail = Airwallex anti-pattern). The tracker is the artifact W117's gate reads to count "advanced conversations."
  - **Output**: `notes/outreach_tracker.md` exists; rows seeded; §6-prescreen filled for founder rows.
- [ ] **(B) Tái-dựng (VSR — the venue's hot-path log)** — file: `notes/recall_vsr_state_machine.md`. Close source. Whiteboard the VSR state machine from memory — states {normal, view_change, recovery}, DoViewChange quorum (f+1), primary selection, log-gap fill, state-transfer — AND the §4A lock that makes it the venue's centerpiece: **the order/event log IS the VSR log; `SettlementId(snapshot_epoch, op_number, checksum)` = the op's VSR log position; `finality_status` is derived from the commit point with no side mapping table**. Then check vs the TigerBeetle VSR doc + mark hit/miss/gap. This is ~23 weeks past `consensus-vsr` v1.0 (W90) and you've been *using* it through the capstone, so it's a real decay edge. ≥80% recall threshold; <80% = redo at W120. (~120 min)
  - 🎯 **Hardened acceptance**: 3-min bet-conversation answer "walk me through how your venue replicates and why the order log and the settlement journal are the same log" recorded as `notes/bet_convo_vsr.m4a` (or linked in the note); whiteboard diagram (states + transitions + quorum sizes) embedded/photographed; the answer *names the §4A lock* (one hot-path log, SettlementId = op-number, free idempotency on resubmit) — because that single-log discipline is the architecture-judgment point a founding-eng interviewer is actually probing, not the textbook VSR states.
  - *Mental model*: a founder building a venue will ask "why VSR and not Raft, and how does settlement get its identity?" The strong answer is the §4A one: VSR because the order log, settlement journal, and replicated SM must be the *same* log (no primitive twice); `SettlementId` = the op-number so finality is derived, not stored, and resubmit is a free no-op. If you can't whiteboard the state machine that underpins that claim, the claim is hollow. This recall converts "I shipped a VSR cluster" into "I can defend every design choice in it." Reapplies at: W115 (Blog #6 hybrid-CLOB post), W122–W125 bet-conversation lane.
  - **Output**: `notes/recall_vsr_state_machine.md` exists with state diagram + the §4A-lock recall + recording link; score recorded (X% vs source).
- [ ] Commit notes (`notes/network_map.md`, `notes/outreach_tracker.md`, `notes/recall_vsr_state_machine.md`). (~10 min)

## Tuesday — `perp-dex-core` elevator pitch + 1-pager + crypto-MM-screen LeetCode dose + reactor recall

- [ ] **(A) Write the `perp-dex-core` v1.0 bet-conversation 1-pager** — file: `notes/portfolio_perp_dex_core_v1_0.md` (the document W120/W122 attaches and W117's gate links). This *replaces* the v2 `mini-db` 1-pager. Structure (mirror the W74 SHIP-day 1-pager shape, re-aimed at the venue): (~60 min)
  - 1-sentence elevator: *"A replicated, fault-tolerant, multi-node hybrid CLOB at bar (c): 3-node VSR where the order/event log IS the consensus log; real-time multi-instrument cross-margin (BTC-PERP + ETH-PERP, deterministic netting) recomputed incrementally per mark tick at ~100k accounts; partial liquidation + fee-accrued insurance fund with a tripwire that cluster-VOPR never hit."*
  - 3 bullets on technique depth: (a) the **convergence cell** — `risk-engine` per-mark-tick incremental margin (delta on position/price change; full recompute only on circuit-breaker/param change), the only artifact that is simultaneously jeff-dean-hard, hft-review-hard, and domain-hard; (b) **one hot-path log** — VSR order/event/settlement log, `SettlementId` = op-number, free idempotency; (c) **deterministic replicated core** — integer-only math, BTreeMap iteration, SequenceClock, byte-identical across replicas + arch, proven by cluster-VOPR.
  - 1 bullet on architecture judgment: the **two staleness contracts** — control/risk reads are read-after-commit from local applied state on the leader shard (no consensus round-trip); analytics reads are bounded-staleness + op-number token. A risk decision served from the analytics contract is a correctness bug.
  - 1 bullet on the differentiator narrative: **RWA-aware seams in v1.0** (abstract oracle interface, parameterized funding, instrument abstraction) — "the engine is architected for RWA-perps; here's the seam" — delivered as features in v1.5 (the tier-raiser).
  - 1 bullet on outcome: cluster-VOPR StateChecker green (linearizability + replica convergence + insurance-fund tripwire never hit at the declared 10%-taker accrual rate, swept deterministically 5/10/20%); per-mark-tick p99.99 budget met at 100k accounts (`latency-lab` report).
  - 3 probe questions a founding-eng interviewer asks + the crisp answer: (i) "how does cross-margin net across instruments?" → 70% of the netting offset credited (const 30% haircut ≈ 0.7 corr, deterministic — under-credits, never over-credits; review-trigger wired to the RWA milestone); (ii) "what happens when a node dies mid-batch?" → VSR view-change, un-acked ops re-proposed, client_table dedupe prevents double-apply, no committed op lost (cluster-VOPR proves it); (iii) "how do you settle on-chain?" → async signature `settle(batch) -> SettlementId`, `finality_status -> {Pending, Final, Reverted}`, v1.0 instant-Final stub behind the *real* hybrid-boundary interface; v1.5 slots a real chain + challenge window into the SAME signature with no engine rewrite.
  - 🎯 **Hardened acceptance**: 1 page; every claim is a linkable artifact (a commit, a VOPR run id, a `latency-lab` number) — zero unquantified adjectives; the elevator sentence leads with the **convergence cell** (cross-margin), not the order book, because that's the principal-defining artifact per §3.
  - *Mental model*: this 1-pager is the §1 bar-(b) proof in conversation form. The centerpiece is deliberately the `risk-engine` cross-margin convergence cell (§3 row 9), not matching — because matching is table-stakes a founder has seen ten times, and per-mark-tick incremental cross-margin at 100k accounts is the thing almost no candidate has actually built. Leading with the hardest-and-rarest artifact is the move that earns a founding-tier conversation.
  - **Output**: `notes/portfolio_perp_dex_core_v1_0.md` exists, 1 page, convergence-cell-led, every claim linkable.
- [ ] **(B1) Crypto-MM-screen LeetCode maintenance dose** — file: `notes/leetcode_log.md`. The crypto-MM *bridge* (the income floor) still runs an algo screen; the *bet* (founder) does not. So this lane is a **thin maintenance dose, not the v2 daily-hard cram**: 1 medium-or-hard per *bridge-relevant* topic (heaps/intervals/sliding-window/concurrency), 2–3×/week through W125, logged with problem #, topic, time, retry-flag. Today: 1 problem, logged. (~30 min)
  - 🎯 **Hardened acceptance**: 1 problem solved + logged with topic tagged to a crypto-MM-screen category; the log header states the v3 demotion explicitly ("maintenance for the MM-bridge screen, not the bet").
  - *Mental model*: §7's bridge is the crypto-MM cash seat, hireable on Half-2 latency craft — and those desks do run a coding screen. But the v3 north star is the bet, where the conversation is "defend your venue," not "invert this tree." So this is calibrated maintenance for the *floor*, not the headline prep it was in v2. Over-investing here is a v3 anti-pattern.
  - **Output**: `notes/leetcode_log.md` has today's entry with the MM-bridge topic tag + the demotion header.
- [ ] **(B2) Tái-dựng (ScyllaDB/Seastar reactor)** — file: `notes/recall_seastar_reactor.md`. Close source. Recall the reactor + cooperative-scheduling model (no preemption; 500µs task quota; >20ms stall warning) AND map it to *your* `runtime-thread-per-core` decision in the venue: **shard-by-account, 16 shards, ~62.5k accounts/shard/s = ~62ms/s/shard at 10 mark ticks — comfortable; per-trade marks would blow it, which is exactly why incremental margin is mandatory (§4A back-of-envelope)**. Check vs source. ≥70% = master; <60% = redo W120. (~60 min)
  - 🎯 **Hardened acceptance**: 3-min answer "why thread-per-core for the risk engine, and where does cooperative scheduling bite?" recorded; the answer cites *your* shard-by-account back-of-envelope (the §4A capacity math), not just Seastar's generic model; ≥2 concrete failure modes (long CPU-bound margin recompute starves the shard reactor; a full portfolio recompute on a mark tick blows the 500µs quota → why it's reserved for circuit-breaker/param events only).
  - *Mental model*: a crypto-MM latency screen and a founding-eng deep-dive both probe "why thread-per-core, and what's the failure mode." The strong answer ties Seastar's model to the *specific* capacity decision you recorded up front (incremental margin mandatory at 100k accounts because full recompute blows the reactor budget) — that's the jeff-dean capacity-math move that separates "I read about Seastar" from "I made the call." Reapplies at: W122–W125 bet/MM lane.
  - **Output**: `notes/recall_seastar_reactor.md` exists with the §4A back-of-envelope mapping + recording link; score recorded.
- [ ] Commit notes. (~10 min)

## Wednesday — §6 ecosystem-selection filter dry-run + Aeron decay-edge recall

- [ ] **(A) §6 ecosystem-selection filter — first dry-run on ≥5 named venues** — file: `notes/ecosystem_shortlist_v1.md` (the seed of the W116 ≥5-venue filter + the W117 shortlist ≥5). For each of ≥5 named on-chain-derivatives venues (e.g. from the Hyperliquid / dYdX / GMX / Variational / Ostium comparable set + any earlier-stage targets surfaced on the Binance-alumni map), score all 7 §6 checks GO / UNKNOWN / FAIL with a one-line evidence note each: (1) founding-tier allocation, (2) tier-1 backing (≥1 of {Paradigm, a16z crypto, Dragonfly, Polychain, 1kx, Multicoin, Jump, Wintermute}), (3) core-criticality (do I build the engine that IS the company?), (4) multi-billion-FDV ceiling, (5) token reality (pre-TGE, sane vesting, ≥10× headroom, no predatory unlock), (6) remote-native (Vietnam-remote works), (7) cycle timing (seed–Series B / pre-TGE with a plausible tailwind). (~75 min)
  - 🎯 **Hardened acceptance**: ≥5 venues each scored on all 7 checks with one-line evidence; ≥1 venue carried forward as "shortlist candidate — no visible FAIL, ≥4 GO" (this is the §1 bar-(b)/M30 "shortlist ≥5 passing the §6 checklist" seed — a venue with any visible FAIL is a *bridge*, not a *bet*, and is tagged as such); the doc names which §6 lines are currently UNKNOWN-and-must-be-asked-in-the-first-conversation (this turns the checklist into a conversation agenda).
  - *Mental model*: §6 is "a bet is GO only if all seven pass; otherwise it's a bridge job, not a bet." Running the filter *before* outreach does two things: it stops you wasting a warm intro on a visible-FAIL venue (bar-1 late-stage-tiny-equity = Airwallex anti-pattern), and it turns the UNKNOWN cells into the exact questions to ask in the first conversation (allocation band, cap table, vesting, remote policy). The shortlist this seeds is what the M30 gate (W117) counts.
  - **Output**: `notes/ecosystem_shortlist_v1.md` exists with ≥5 venues × 7 checks scored + evidence + ≥1 carried-forward candidate + the UNKNOWN-questions list.
- [ ] **(A2) Update the resume to lead with `perp-dex-core`** — file: `resume_2027.md` (private). *Replaces* the v2 `mini-db`-centric resume. Lead bullet: the replicated multi-node venue (3-node VSR, cluster-VOPR, ~100k-account cross-margin, p99.99 budget met). Then: N production crates in the workspace, the convergence-cell `risk-engine`, Reth-spine PRs, ~1900+ runtime hours (tracking to the M30 2000h milestone). No geography, no comp figures, no firm names; every bullet a quantified artifact; 1 page. (~25 min)
  - 🎯 **Hardened acceptance**: lead bullet is `perp-dex-core` v1.0 (not `mini-db`, not matching-engine alone); every bullet quantified (number + crate + verifiable claim); zero unquantified adjectives; fits 1 page; runtime-hours figure consistent with the W110–W112 tracker.
  - *Mental model*: the resume is the document W120 outreach attaches and the W117 gate references. Drafting it now (4 weeks early) lets it marinate and be revised across W114–W116 rather than rushed. Leading with the venue (not the demoted `mini-db`) is the single most important v3 edit — the whole strategy re-aim is "the capstone is the venue."
  - **Output**: `resume_2027.md` exists (private), 1 page, `perp-dex-core`-led, every bullet quantified.
- [ ] **(B) Tái-dựng (Aeron threading modes)** — file: `notes/recall_aeron_threading_modes.md`. Close source. Recall Aeron's 4 threading modes — DEDICATED (3 threads), SHARED_NETWORK (2), SHARED (1), INVOKER (0) — when to pick each, what each thread does (sender/receiver/conductor), AND the §4 lock: in the venue, **Aeron is `messaging-aeron` inter-node transport + off-hot-path MD fan-out reading a *projection* of the VSR log; Aeron Cluster's Raft is a design mirror only — the hot-path replicated SM is VSR, not Raft.** Check vs source. ≥3/4 = master; <3/4 = redo W120. (~75 min)
  - 🎯 **Hardened acceptance**: 4 modes recalled with thread counts + roles + a "use X when spare cores ≥ Y" decision tree; the note states which mode `messaging-aeron` picked and why; AND the §4 lock is recalled (Aeron transport is off the hot path; Aeron Cluster Raft is a mirror; the venue's hot-path log is the VSR log — no second hot-path consensus).
  - *Mental model*: a founding-eng interviewer who knows the space will ask "Aeron Cluster does Raft — why didn't you use it for the matching hot path?" The strong answer is the §4 resolution of "Kafka vs VSR vs Aeron-Cluster": one log on the hot path (VSR, TigerBeetle pattern); Aeron is transport + off-path fan-out; Aeron Cluster's Raft and Kafka's partitioned log are *mirrors/projections*, never a second hot-path log. Recalling the threading taxonomy proves you can justify the transport mode choice; recalling the §4 lock proves you understand why it's off the hot path. Reapplies at: W122–W125 bet lane.
  - **Output**: `notes/recall_aeron_threading_modes.md` exists with 4 modes + decision tree + the §4 off-hot-path lock + recording; score recorded (X/4).
- [ ] Commit notes (`notes/ecosystem_shortlist_v1.md`, `resume_2027.md` private, `notes/recall_aeron_threading_modes.md`). (~10 min)

## Thursday — Draft the outreach messages (founder + crypto-MM variants) + cross-margin bet-rehearsal

- [ ] **(A) Draft the two outreach message templates** — file: `notes/outreach_scripts.md`. Per the v3 two-destination split, draft TWO templates, each a *short* (≤120-word) warm-intro-friendly message: (~60 min)
  - **Founder variant** ("would bring you in"): leads with the convergence cell ("I built a 3-node VSR perp-DEX-core whose centerpiece is per-mark-tick incremental cross-margin at ~100k accounts — here's the cluster-VOPR run"), names the §6-relevant ask in one line (founding-tier, core-criticality), attaches the `perp-dex-core` 1-pager + Blog #4/#5 links. The goal-state is the §1 bar-(c) "would bring you in" advance.
  - **Crypto-MM variant** (the bridge): leads with latency craft ("matching p99.99 at <X, kernel-bypass MD path, deterministic ledger — here's the `latency-lab` tick-to-trade report"), positions as the income-floor seat hireable on Half-2 craft, attaches the latency report + matching-engine perf numbers.
  - Both: ≤120 words, one falsifiable artifact link in the first two sentences, a single clear ask, no comp/geography mention (geography is the §7 flip-trigger decided at Bet #1 time, not now).
  - 🎯 **Hardened acceptance**: both templates exist; each ≤120 words; each leads with a *different* artifact appropriate to its destination (convergence-cell/VOPR for founders; latency-report for MM); each has exactly one ask; a "fill-in" slot per template for the per-contact §6 UNKNOWN-question (founder) or desk-specific hook (MM). **No messages sent today** — these are drafted now so W120/W122 sends are calibrated, not improvised.
  - *Mental model*: the two §7 destinations need two different hooks. A founder cares about the rarest-hardest artifact (cross-margin convergence cell + fault tolerance proof); an MM desk cares about latency craft (the thing that underwrites the bridge seat). One generic message stranded between both lands neither. Drafting both now, against the fresh artifacts, is what makes the W120/W122 outbound a *controllable* metric (M24 relabel) rather than a scramble.
  - **Output**: `notes/outreach_scripts.md` exists with founder + MM templates, each ≤120 words, artifact-led, single-ask, fill-in slot.
- [ ] **(A2) Send ≤3 low-risk warm pings (depth-≥2 only)** — log in `notes/outreach_tracker.md`. To stay controllable and not burn warm intros prematurely, send at most 3 *low-stakes* re-connect pings to depth-≥2 contacts (not the cold founding-eng asks — those are W120/W122). A "saw your post / shipped my venue, would value a catch-up" re-warm, no ask yet. Mark status=sent + artifact-linked in the tracker. (~25 min)
  - 🎯 **Hardened acceptance**: ≤3 depth-≥2 pings sent + logged with date + the artifact linked; tracker status updated to `sent`; these are re-warms (no §6 ask yet) — the cold founding-eng outreach is deliberately held for W120/W122 so the M24 outbound metric is hit with calibrated messages, not premature ones.
  - *Mental model*: the §1 bar-(c) metric is "≥5 warm intros logged; ≥2 conversations advanced to 'would bring you in'." You advance toward it by re-warming depth-≥2 contacts *before* the cold ask, so when W120's ask lands the relationship is already live. Sending the cold founding-eng ask today (4 weeks before the artifacts are fully packaged at W116) would waste the shot — hence ≤3 re-warms only.
  - **Output**: `notes/outreach_tracker.md` updated with ≤3 `sent` re-warm rows.
- [ ] **(B) Bet-rehearsal (the convergence cell — `risk-engine` cross-margin)** — file: `notes/bet_convo_cross_margin.md`. 5-min answer to the founding-eng probe: *"Walk me through your cross-margin engine — how does it net risk across instruments, how does it stay real-time at 100k accounts, and how is it deterministic enough to replicate?"* Cover, with *your own numbers*: the const 30% netting-benefit haircut (≈0.7 corr, credits 70% of the offset, deterministic, review-trigger wired to the RWA milestone); per-mark-tick **incremental** margin (delta on position/price change; full recompute only on circuit-breaker/param change — and *why*, via the §4A back-of-envelope: 100k × 10 ticks/s = 1M recomputes/s = ~1 CPU-s/s naïvely, only fits with shard-by-account + incremental); reads from local applied state on the leader shard (no consensus round-trip); integer-only math so the recompute replicates byte-identically and survives cluster-VOPR. Record + self-rate ≥4/5. (~90 min)
  - 🎯 **Hardened acceptance**: aloud recording linked; the answer leads with the *back-of-envelope* (why incremental is mandatory, not a fallback) — the jeff-dean capacity-math move; names the deterministic-haircut rationale (empirical correlation injects non-deterministic floating state the VSR cluster must replicate identically; correlation→1 in a crash, so under-crediting is safe); self-rating ≥4/5 on fluency.
  - *Mental model*: this is *the* answer the whole plan back-plans to. The convergence cell (§3 row 9) is the principal-defining artifact — simultaneously jeff-dean-hard, hft-review-hard, domain-hard. A founder who hears a fluent version of this answer (capacity math up front, determinism rationale, the wired v1.5 review trigger) is hearing someone who has built the hardest part of a venue, not read about it. This rehearsal is the single highest-leverage 90 minutes of the week. Reapplies at: W121 flagship blog, W122–W125 bet lane (this is the flagship talking point).
  - **Output**: `notes/bet_convo_cross_margin.md` exists with recording link + back-of-envelope-led structure + determinism rationale + self-rating.
- [ ] Commit notes (`notes/outreach_scripts.md`, `notes/outreach_tracker.md`, `notes/bet_convo_cross_margin.md`). (~10 min)

## Friday — Geography cheap-prep (no relocation) + liquidation/insurance-fund bet-rehearsal

- [ ] **(A) Geography cheap-prep — visa + token-tax eligibility for BOTH hubs** — file: `notes/geography_prep.md`. Per §7 ("pre-stage optionality, do NOT pre-commit"): the relocation decision happens at **Bet #1 time** and the hub follows the bet (Dubai = crypto-native/Binance-dense; Singapore = institutional/Asia). Now = cheap prep only: confirm (i) visa eligibility for a remote founder/engineer in *both* Dubai and Singapore, (ii) pre-TGE token-grant tax treatment in *both*. No move, no lease, no commitment. Also: note the **explicit flip-trigger** that would later convert prep→move (≥N warm conversations cluster in one hub OR a presence-contingent offer premium appears) and confirm it has NOT fired yet. (~45 min)
  - 🎯 **Hardened acceptance**: `notes/geography_prep.md` covers visa eligibility + pre-TGE token-tax treatment for *both* Dubai and Singapore (one line each, with a source pointer for the UNKNOWN ones to chase at W120); the flip-trigger is stated explicitly and marked NOT-FIRED; the doc opens with the §7 lock "Vietnam-remote is the default until the trigger fires — no pre-commitment."
  - *Mental model*: §7 + the §9 risk row ("best venues want SG/Dubai presence") resolve to "pre-stage, don't pre-commit." Doing the cheap prep now (visa + tax for both hubs) means that *if* the flip-trigger fires at Bet #1 time you can move fast — but you've spent zero on a relocation that may never be needed. Confirming the trigger hasn't fired keeps Vietnam-remote the locked default. This is the lowest-cost item that de-risks the highest-cost decision.
  - **Output**: `notes/geography_prep.md` exists with both-hub visa + token-tax notes + the flip-trigger marked NOT-FIRED.
- [ ] **(A2) Token2049-style event-trip recon list (no booking)** — append to `notes/geography_prep.md`. §7 says "compress network density without relocating: use targeted event trips (Token2049 Dubai/SG, etc.) to source Bet #1 and warm the Binance-alumni map in person." List the next 2–3 relevant events (date, hub, which Binance-alumni / founder contacts from Monday's map are likely there). No booking yet — this is W123's recon lane; today just seeds the target list. (~20 min)
  - 🎯 **Hardened acceptance**: ≥2 events listed with date + hub + ≥1 named contact from `network_map.md` likely attending each; explicitly marked "recon-only, booking deferred to W123."
  - *Mental model*: event trips are the §7 mechanism to compress in-person network density without relocating — a Token2049 Dubai trip can advance 3 warm conversations in 3 days. Seeding the target list now (against the fresh Monday map) means W123's recon is calibrated to *who is actually there*, not a generic conference visit.
  - **Output**: `notes/geography_prep.md` has an event-recon section with ≥2 events + named contacts + the booking-deferred mark.
- [ ] **(B) Bet-rehearsal (liquidation engine + insurance fund — the moat)** — file: `notes/bet_convo_liquidation.md`. 4-min answer to the probe: *"How does your liquidation engine prevent socialized loss, and is the insurance fund real or stubbed?"* Cover: margin-ratio triggers → **partial** liquidation against the book; the **waterfall** (remaining position margin → insurance fund); the fund is **fee-accrued (default 10% of taker fee, a declared model param)**, NOT stubbed — exercised in cluster-VOPR; the rate is swept **deterministically** (5/10/20%) in the harness, never randomized in-core (randomizing it reframes "does the fund self-fund?" into "does *some* rate survive?" — the wrong question, and it injects non-determinism the VSR cluster would have to replicate); the **tripwire invariant** (the point the fund can't cover the next shortfall) was **never hit** across all VOPR-explored schedules at the declared rate — and a schedule that *would* hit it precisely defines the v1.5 ADL/socialized-loss seam. Record + self-rate. (~90 min)
  - 🎯 **Hardened acceptance**: aloud recording linked; the answer explicitly states "the fund is exercised in cluster-VOPR, not stubbed" (the §9 "insurance fund stubbed away kills the moat" risk, answered); names the deterministic-sweep rationale and the tripwire→v1.5-ADL seam; self-rating ≥4/5.
  - *Mental model*: the backstop is the moat — a founder evaluating a venue engineer will probe whether the hard, unglamorous part (the insurance fund + waterfall + the invariant that it never gets exhausted) is real or hand-waved. The strong answer cites the cluster-VOPR run that proves the tripwire was never hit at the declared rate, and frames the v1.0→v1.5 seam (a tripwire-hitting schedule defines exactly when ADL must engage). This is the §4A acceptance #7 made into a conversation. Reapplies at: W115 Blog #6, W122–W125 bet lane.
  - **Output**: `notes/bet_convo_liquidation.md` exists with recording link + the "real not stubbed / VOPR-exercised" claim + the tripwire→ADL seam + self-rating.
- [ ] Commit + log (`notes/geography_prep.md`, `notes/bet_convo_liquidation.md`). (~10 min)

## Saturday — Week consolidation + M30-gate progress audit + 5-year reflection

- [ ] **(A) M30 Bet-Readiness Gate progress audit** — file: `notes/m30_gate_progress.md`. The M30 gate (W117) requires: capstone v1.0 + cluster-VOPR (✅ since W109) + 2000h runtime + flagship blog + **ecosystem shortlist ≥5 passing §6** + **≥2 founding/core-eng conversations advanced**. Audit *this week's contribution* to each open item: runtime-hours tracking (on pace for 2000h by W117?); shortlist progress (≥5 venues scored Wed — how many no-visible-FAIL candidates?); conversations (re-warms sent Thu — how many on a path to "advanced" by W117?); the M24 outbound metric (≥20 targeted convos / ≥25% response / ≥3 Binance warm intros — where does the map + tracker put us?). For each open item, write the *one concrete W114–W116 action* that closes it. (~45 min)
  - 🎯 **Hardened acceptance**: every M30-gate line item has a current-status row (DONE / ON-PACE / AT-RISK) + the single next action that closes it by W117; the doc explicitly states whether the §6 shortlist is on track for ≥5 and whether ≥2 advanced conversations is plausible given this week's re-warms (if AT-RISK, names the mitigation — §9 "M24 outbound response rate low" mitigations: ship the flagship blog earlier, widen Binance-alumni outreach, temporarily lower target tier for the bridge).
  - *Mental model*: this week is 4 weeks before the gate that decides "place Bet #1 now, or engage bridge income and keep iterating" (§5 M30). Auditing the gate *now*, while there's still W114–W116 to course-correct, is what turns the gate from a pass/fail surprise into a managed approach. The two soft items (shortlist + advanced conversations) are the ones this week directly moves — so the audit is the honest read on whether the outreach ramp is working.
  - **Output**: `notes/m30_gate_progress.md` exists with per-line-item status + next-action + the AT-RISK mitigations.
- [ ] **(B) Decay-edge gap consolidation + W120 re-do punch-list** — file: `notes/recall_gaps_W113.md`. Collect every recall score from this week (VSR Mon, reactor Tue, Aeron Wed) + every <70%/<80% topic into a single W120 re-do punch-list, each with a 1-line lookup pointer. Note which talking points are now *flagship-ready* (≥ threshold, recorded, defensible) vs which need a W120 redo before the W122–W125 bet lane. (~30 min)
  - 🎯 **Hardened acceptance**: every recall topic from the week has a score + a verdict (flagship-ready / W120-redo); each redo carries a lookup pointer; the flagship-ready set is explicitly the corpus the W121 flagship blog + W122 conversations draw from.
  - *Mental model*: the bet-conversation lane (W122–W125) is the rehearsal payoff of all these recalls. Consolidating the gaps now turns "I should review my systems knowledge" into a finite, schedulable W120 punch-list — so the cold founding-eng conversations land on *defensible, recorded, threshold-cleared* talking points, not improvisation.
  - **Output**: `notes/recall_gaps_W113.md` exists with per-topic score + verdict + W120 lookup pointers.
- [ ] **5-year reflection (the bet-path, not a code failure mode)** — append to `notes/m30_gate_progress.md`: state the one shift over the next 5 years that would force a re-aim of this entire outreach posture. (Candidate: the on-chain-derivatives niche cools before Bet #1 — §9 row 1. Then the transferable core (jeff-dean systems + hft-review latency + Reth spine) re-bridges to crypto-MM or crypto-infra IC, the §6 checklist defers GO until a real bet exists, and the same `perp-dex-core` artifact + 1-pagers re-point at AI-infra/DB-infra fallbacks via `vector-db`/`lsm-core`. The network map's *two destinations* are exactly the hedge: the MM-desk column is the floor that survives the bet column drying up.) (~15 min)
  - 🎯 **Hardened acceptance**: names one concrete 5-year shift + the re-aim it forces + why the two-destination map (founder ∥ MM) already hedges it (the §7 "no skill stranded on a single ecosystem's death" lowest-risk objective).
  - **Output**: appended to `notes/m30_gate_progress.md`.
- [ ] Commit + log (`notes/m30_gate_progress.md`, `notes/recall_gaps_W113.md`). (~10 min)

## Sunday — Rest + End Month 28 review

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
