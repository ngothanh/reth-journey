# Week 114 — Public visibility + M30 bet-readiness prep

> **Phase**: 6
> **Month**: M29
> **Track**: (career/visibility) — public-visibility push + 2000hr-milestone trajectory + **M30 Bet-Readiness Gate pre-stage** (the W117 gate this week converts from a surprise verdict into a checklist)
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: (n/a — visibility/bet-readiness week; no upstream module mirrored) — the artifact this week mirrors is the **§6 ecosystem-selection checklist** and the **§5 M30 gate criteria**, not a codebase
> **Crate(s) touched**: (none — visibility/bet-readiness). The capstone `perp-dex-core` v1.0 is already tagged (W113); this week markets it and audits it against the gate, it does not extend it. The standing perf/soak rig from W111 keeps running in the background (logged Saturday).
> **Bar**: (n/a) — career/visibility week, no Build exercises and no engineered trap tests. Manufacturing a 3-phase Build here would be padding. Every item instead carries an explicit **artifact file**, an **Output** line proving it done, a 🎯 **Hardened-acceptance** pass criterion, and a *Mental model* of why the item exists in THIS week (it pre-stages the W117 Bet-Readiness Gate).
> **Feeds into**: W115 Blog #6 (hybrid CLOB / on-chain-settle interface — this week's technical post + talk abstract seed the narrative) + ecosystem-shortlist build (§6); W116 flagship-blog draft + the §6 filter applied to ≥5 venues (this week's shortlist *seeds* that filter); W117 **M30 Bet-Readiness Gate** (the five-criterion audit this week pre-stages, with three weeks of runway to flip red→green) + Blog #7 (Phase 6 retro); W120 warm-network activation (the Binance-alumni replies + founder/desk DMs logged here are the W120 contact base); W122 bet-outreach response-rate metric (this week establishes the baseline)
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): RECONCILED_PLAN_v3.md §5 (M30 row — bet-readiness gate criteria: capstone v1.0 + cluster-VOPR + 2000h + flagship blog + ecosystem shortlist ≥5 + ≥2 founding/core-eng convos), §6 (ecosystem-selection checklist: founding-tier allocation, tier-1 backing, core-criticality), §7 (geography — pre-stage Dubai/SG, do NOT pre-commit; the flip-trigger; compress network via event trips, not relocation), §9 (risk register — "M24/M30 outbound response rate low is *controllable*: ship flagship blog earlier, widen Binance-alumni outreach, temporarily lower target tier for Bridge₀"), §A (W114–W117 re-slotting: flagship blog + ecosystem shortlist); INDEX.md W114 line ("Public visibility + M30 bet-readiness prep").
> **v3 modifications**: (1) **Reframed from v2 "M30 Path-E confirmation" to the v3 "Bet-Readiness Gate."** v2 this week pre-staged the five v2 criteria ("is there HFT recruiter inbound?", "HFT crate quality?", Path-E confirm-or-pivot). v3 re-aims every item at the §5 M30 bet-readiness criteria: **`perp-dex-core` v1.0 + cluster-VOPR + 2000h + flagship blog + ecosystem shortlist ≥5 + ≥2 founding/core-eng conversations**. The audited artifact is the **capstone** (book + `oracle-mark` + `risk-engine` + `liquidation-engine` on a 3-node VSR cluster), not "matching-engine + ledger as a paper-trade HFT rig." (2) **Outbound metric is controllable, not inbound-passive.** The v2 "is there a recruiter DM?" question becomes the v3 controllable outbound metric (§9): ≥20 targeted on-chain-derivatives conversations, response-rate tracked, ≥3 Binance-alumni warm intros — the thing *you* drive, not the thing that lands. (3) **Audience swapped HFT-IC → founding/core-eng + crypto-MM desks.** Wednesday's community engagement and the technical post target on-chain-derivatives founders and crypto-MM engineers (the bet-path + the bridge), not Tier-A HFT recruiters. (4) **Geography pre-stage seeded (§7).** Friday's gate prep adds a Dubai/SG flip-trigger line — pre-stage cheap optionality (visa + token-tax eligibility, both hubs), do NOT pre-commit; the hub follows the bet at Bet #1 time (W131). (5) Tempo demoted from "Path-D kept warm against the gate" to a standing RWA-collateral-option maintenance pass — the RWA narrative now lives inside the capstone's v1.0 seams (the `oracle-mark` RWA-aware seam), so Tempo is a parallel option, not a competing path.

**What this week is for (read before Monday):** W117 is a **hard decision gate** — the §5 M30 Bet-Readiness Gate. It decides whether you **place Bet #1** (engage a founding/core-eng role at a §6-checklist-passing on-chain-derivatives venue) or **engage the bridge** (crypto-MM cash seat) and keep iterating outbound. The gate has six criteria: (1) `perp-dex-core` v1.0 shipped at bar (c); (2) cluster-VOPR run with bug writeups; (3) 2000h cumulative runtime; (4) flagship blog public; (5) ecosystem shortlist ≥5 venues passing the §6 checklist; (6) ≥2 founding/core-eng conversations advanced. **This week pre-stages all six with three weeks of runway** so any red item still has time to flip to green before gate day. Every artifact below maps to exactly one gate criterion, and Friday's gate-prep doc draws them together into a single red/amber/green dashboard. The week is light per-day (the heaviest item is Thursday's high-visibility Reth PR at ~150 min) precisely because the *thinking* — the honest self-audit against the gate — is the load-bearing work, not new code.

---

## Monday — Technical post: the capstone centerpiece (cross-margin + liquidation)

- [ ] `notes/visibility/114_technical_post.md` — draft, publish, and pin one thoughtful technical post on the **`perp-dex-core` centerpiece**: the real-time multi-instrument cross-margin + liquidation engine on a 3-node VSR cluster where the order/event log IS the VSR log. Pin the most recent capstone artifact (Blog #5, the principal artifact, W112) to the profile. (~45 min)
  - Output: `notes/visibility/114_technical_post.md` holds the final post text + the live URL; the Blog #5 (cross-margin + liquidation) post is pinned (URL captured in the file).
  - 🎯 **Hardened acceptance**: the post is technical — it names a concrete primitive AND a number (e.g. "per-mark-tick incremental margin recompute at 100k accounts, p99.99 under budget" or "const 30% netting-benefit haircut keeps the cross-margin deterministic across replicas" or "fee-accrued insurance fund at 10% taker with a tripwire-never-hit invariant"), ≤280 chars or a threaded equivalent, and the pinned-post URL resolves. A platitude with zero technical specifics fails. A post about HFT/matching-engine-perf *without* tying back to the on-chain-derivatives venue fails the v3 audience targeting.
  - *Mental model* (v3): cadence is the point — "one thoughtful technical post per week from here" builds the W122 outbound evidence base, and the M30 gate's controllable-outbound metric (§9) compounds with each post. The pinned artifact is now **Blog #5 (the principal artifact)**, not the v2 mini-db post — any founder or crypto-MM engineer who lands on the profile sees the cross-margin + liquidation engine (the hardest founding-engineer proof, §3) in the first screen, before W117's "is there outbound traction?" question is even asked. The post's subject is the capstone *centerpiece* deliberately, because §3 names cross-margin + liquidation as the convergence cell that proves founding-engineer credibility.

## Tuesday — Talk proposal + 2000hr-trajectory checkpoint

- [ ] `notes/visibility/114_talk_proposal.md` — write and submit a talk proposal aimed at an on-chain-derivatives / distributed-systems audience (a crypto-infra or systems track, not a generic "learning journey"): **"Building a replicated, cross-margined perp-DEX core — the order log IS the consensus log."** (~50 min)
  - Output: `notes/visibility/114_talk_proposal.md` holds the abstract + outline + the submission confirmation (CFP portal receipt or email); `status` field = `submitted`.
  - 🎯 **Hardened acceptance**: the abstract names the concrete thesis (a hybrid CLOB whose **single hot-path log is the VSR log** — order events, fills, and async settlement on one log; cross-margin + liquidation as the convergence cell) with ≥1 worked example (e.g. the cluster-VOPR insurance-fund-tripwire search, or the const-30%-haircut determinism guarantee) and a measurable hook (a latency/throughput/scale number). Submission receipt captured. A vague "I'll talk about my learning journey" abstract fails. An HFT-only abstract that never mentions on-chain settle / hybrid boundary fails the v3 audience.
  - *Mental model* (v3): this is a visibility multiplier into the bet-path. Submitting now (W114) keeps the CFP window open and the abstract is a **dry run of the W116 flagship-blog narrative** (and the W121 flagship post). The thesis — "the order log IS the consensus log" — is the §4A v1.0 lock stated as a talk title; rehearsing it now hardens the story a founder hears in the W126 founding-eng conversations.
- [ ] `notes/visibility/114_hours_trajectory.md` — pull the current cumulative runtime-hours total; extrapolate the weekly rate to W117; state surplus/shortfall vs the **2000h M30 target** (§5). (~30 min)
  - Output: `notes/visibility/114_hours_trajectory.md` records: current cumulative hours (a NUMBER), the W114→W117 weekly rate (40h/wk window), the projected W117 total, and the gap (signed) to 2000h with a red/amber/green rating.
  - 🎯 **Hardened acceptance**: the projection is arithmetic, not vibes — `current + (weeks_to_W117 × weekly_rate) = projected`, compared to 2000. If the projection lands short, the note states the concrete catch-up action (and whether it's even reachable without burnout — a forced-march to 2000h that wrecks the capstone polish is a worse outcome than a logged 1950h with a clean v1.0). Amber/red both require a dated action before W117.
  - *Mental model* (v3): 2000h is one of the six gate criteria and it's the only one that's pure trajectory — it can't be sprinted in week 117. Checking it now, with three weeks of runway, is the difference between "on track, coast" and "amber, add 4h/wk." Doing this Tuesday (not buried in Friday's omnibus) gives it room to breathe.

## Wednesday — Engage on-chain-derivatives + crypto-MM community

- [ ] `notes/visibility/114_community_engagement.md` — identify 3–5 engineers visible on the **on-chain-derivatives / crypto-MM** axis (founders or core engineers at perps venues; crypto-MM-desk engineers; VSR/TigerBeetle/distributed-systems voices in the space) and leave respectful, technically-substantive replies on relevant threads. (~40 min)
  - Output: `notes/visibility/114_community_engagement.md` lists each engineer (handle + which §6 axis they sit on: founder / core-eng / crypto-MM-desk / infra) and the permalink to each reply left.
  - 🎯 **Hardened acceptance**: ≥3 engineers logged, **at least 2 of them on the bet-path axis** (derivatives founder/core-eng or crypto-MM desk — not generic Rust/HFT), each with a reply permalink that adds technical substance (a number, a counter-example, a primitive — cross-margin netting, insurance-fund waterfall, VSR op-number contiguity, cluster-VOPR partition search). A "great post!" reply, or three replies all on generic-HFT threads, fails the v3 targeting.
  - *Mental model* (v3): this is warm-network seeding for the **W120 activation** and the **W122 outbound** — and it directly populates the M30 gate's "≥3 Binance-alumni warm intros / ≥20 targeted conversations" controllable metric. Replies that demonstrate venue-domain knowledge (not just systems chops) convert a cold profile into a recognized one *before* the gate audits outbound traction. The §6 axis tag on each contact is deliberate: it's the first column of the W116 ecosystem-shortlist spreadsheet.

## Thursday — High-visibility Reth PR + Binance-alumni map activation

- [ ] `notes/visibility/114_reth_pr.md` — pick and open a Reth PR touching an area with active multi-maintainer review traffic (not a typo fix; an area where ≥2 maintainers will see it). (~150 min)
  - Output: `notes/visibility/114_reth_pr.md` records the PR URL, the file(s) touched, which maintainers own that area (named), and the diff summary; PR is opened (status `open`).
  - 🎯 **Hardened acceptance**: the PR compiles + passes `cargo test` for the touched crate locally *before* opening; the chosen area has ≥2 active maintainers (named in the note) so it gets multi-maintainer eyes. A green-but-trivial PR, or one nobody will review, fails the visibility intent.
  - *Mental model* (v3): merged Reth PRs are the **crypto-infra-IC bridge fallback** signal (§7 — the Half-1 spine that backstops the bet if it goes red) and the single strongest "maintainer visibility" datapoint for the gate's flagship-credibility column. This is the heaviest item of the week (~150 min) — it carries Thursday alone, which is why the other five days stay ~30–80 min. It is *not* on the gate criteria directly, but it strengthens the bridge and keeps the spine warm against a red gate.
- [ ] `notes/visibility/114_binance_alumni_map.md` — activate the Binance-alumni map (§3 "(c) Activated network", §5): list every reachable Binance-alumni contact, tag each by current company/role and §6 fit (derivatives founder / core-eng at a checklist-passing venue / crypto-MM desk), and draft the warm-intro ask for the top 5. (~60 min)
  - Output: `notes/visibility/114_binance_alumni_map.md` is a table — contact, current company, role, §6-axis fit, last-contact date, warm-intro draft (or "sent" with date). Track count toward the **≥3 warm intros / ≥5 warm contacts** M30 sub-metric.
  - 🎯 **Hardened acceptance**: ≥5 alumni contacts mapped with a §6-fit tag each; ≥3 carry a *drafted-or-sent* warm-intro ask (not just "exists in my network"). A bare contact list with no asks fails — the gate metric is *activated* intros, not a rolodex.
  - *Mental model* (v3): §3 lists "Binance-alumni map pointed at on-chain-derivatives founders/early-joins" as an end-state bar; §5 makes "≥3 Binance-alumni warm intros / ≥2 conversations advanced" a gate criterion. The map is the single highest-leverage outbound asset you have — these are people who already know your work-rate. Activating it in W114 (not W120) front-loads the conversation pipeline so that by the gate, the "≥2 founding/core-eng conversations advanced" box has a real chance of being green, not aspirational.

## Friday — M30 Bet-Readiness Gate prep (the load-bearing day)

- [ ] `notes/visibility/114_m30_gate_prep.md` — pre-stage the W117 **Bet-Readiness Gate** (§5 M30 row) by drafting honest answers to the six gate criteria, each as a red/amber/green dashboard row with a dated gap-closure action. This is the week's most important artifact: it converts the gate from a surprise verdict into a checklist whose red items still have three weeks to flip green. (~90 min)
  - Output: `notes/visibility/114_m30_gate_prep.md` with one section per criterion below, each carrying a current-state answer (a NUMBER or a concrete artifact) + a red/amber/green self-rating + the gap-closure action dated before W117.
    - [ ] **(1) `perp-dex-core` v1.0 shipped at bar (c)?** Confirm the W113 tag exists; audit it against the §4A v1.0 lock: 3-node VSR (order/event log IS the VSR log); async settle (`SettlementId(snapshot_epoch, op_number, checksum)`, `finality_status ∈ {Pending, Final, Reverted}`); multi-instrument cross-margin with const 30% netting-benefit haircut (deterministic); fee-accrued insurance fund (10% taker default) + waterfall (position-margin → fund) + **tripwire-never-hit invariant**; incremental-margin hot path at ~100k accounts / ~1M orders/s; two projection staleness contracts (control/risk = read-after-commit from local applied state on the leader shard; analytics = best-effort bounded-staleness + op-number token); RWA-aware seams present. (~20 min)
    - [ ] **(2) Cluster-VOPR run + bug writeups?** Pull the W108 cluster-VOPR results: cumulative scenario count, crash/restart/partition coverage, the insurance-fund-tripwire search outcome (the invariant must have held across all schedules), and any bug-find writeups produced. ≥3 supporting artifacts public (§4A acceptance). (~15 min)
    - [ ] **(3) 2000h cumulative runtime?** Carry forward Tuesday's `114_hours_trajectory.md` number + rating; restate the projected W117 total and gap. (~10 min)
    - [ ] **(4) Flagship blog public?** Status of Blog #5 (W112, the principal artifact, already public) + the W116 flagship draft + W121 flagship post pipeline; is the "architected for RWA-perps" narrative drafted? (~15 min)
    - [ ] **(5) Ecosystem shortlist ≥5 venues passing the §6 checklist?** Seed the shortlist (full build is W116): list candidate on-chain-derivatives venues and score each against §6 — **founding-tier allocation** (equity/token in the founding band, not late-employee scraps), **tier-1 backing** (credible cap-table / runway), **core-criticality** (would you build the core engine, not glue). Count how many pass today. (~15 min)
    - [ ] **(6) ≥2 founding/core-eng conversations advanced?** Pull from Thursday's Binance-alumni map + Wednesday's community log: how many conversations are live, and how many have advanced toward "would bring you in" (§3 (c))? State the controllable-outbound numbers: targeted conversations (target ≥20), response rate, warm intros (target ≥3). (~15 min)
  - 🎯 **Hardened acceptance**: all six criteria answered with a NUMBER or a concrete artifact (not "feels on track"); each carries a red/amber/green rating and a one-line gap-closure action dated before W117. Any criterion left as prose-without-evidence fails. **Crucially**: if any criterion is red, the note must state whether it is *flip-able in three weeks* (e.g. outbound count, shortlist size = yes; 2000h trajectory = maybe; a fundamentally unshippable capstone = no — and that last case is the gate's whole point, surfaced early).
  - *Mental model* (v3): W117 is the pivot point of the entire 36-month plan — GO-bet or engage-bridge. Drafting the answers now mirrors §5's M30 criteria *exactly* so there is zero drift on gate day. The §9 risk register is explicit that the most likely red item — outbound response rate — is **controllable**: ship the flagship blog earlier, widen Binance-alumni outreach, lower the target tier for Bridge₀. This Friday doc is where you decide which of those levers to pull, with runway to pull them.
- [ ] `notes/visibility/114_geography_prestage.md` — pre-stage the §7 Dubai/SG optionality **cheaply** (do NOT pre-commit; the hub decision is at Bet #1 time, W131): note the visa + token-tax-eligibility basics for **both** hubs, and write the flip-trigger condition that would move the relocation decision (a warm-conversation cluster in one hub, or a presence-contingent offer premium). (~30 min)
  - Output: `notes/visibility/114_geography_prestage.md` records: Dubai vs SG one-liner (Dubai = crypto-native / Binance-dense; SG = institutional / Asia-facing, §7), visa + token-tax eligibility notes for each, and the explicit flip-trigger sentence. Status = `pre-staged, not committed`.
  - 🎯 **Hardened acceptance**: the note keeps it cheap — eligibility facts only, no leases, no flights booked beyond the §7 event-trip recon (W123). The flip-trigger is a single falsifiable condition, and the note explicitly states "remote-native is the default; relocate only if the trigger fires." A note that pre-commits to a hub fails the §7 discipline.
  - *Mental model* (v3): §7 is emphatic — **pre-stage, do not pre-commit**. Geography is an accelerant, not a gate. Vietnam-remote is locked; the hub follows the bet. Writing the flip-trigger now means that if a warm-conversation cluster forms in Dubai or SG during W120–W128 outreach, the relocation decision is already framed, not improvised under offer pressure.

## Saturday — Capstone standing-ops check + Tempo RWA-option maintenance

- [ ] `notes/ops/114_capstone_ops_check.md` — confirm the standing `perp-dex-core` rig from W111 (per-mark-tick p99.99 perf pass at 100k accounts) and the multi-node paper-trade venue (live since W106) are still green; log uptime + any drift. (~30 min)
  - Output: `notes/ops/114_capstone_ops_check.md` records: cluster uptime since last check, the latest per-mark-tick p99.99 number vs budget, insurance-fund-tripwire invariant status (must read "never hit"), and any VSR view-change / partition events from `ops-monitoring`. Commit hash captured.
  - 🎯 **Hardened acceptance**: the rig is green or the note states the exact drift + a dated fix; the insurance-fund tripwire invariant is confirmed never-hit (a hit here would be a v1.5 ADL-trigger finding, logged for W119, not a silent skip). A skipped check fails — the gate's "cluster-VOPR'd, operating" claim depends on a continuous green log.
  - *Mental model* (v3): the M30 gate doesn't just want a *tagged* capstone — it wants one that's **operating** (live deploy since W106, sustained ops). A weekly green log is the auditable evidence behind the gate's criterion (1)+(2). This is the lightweight standing-ops cadence, not new build work — the capstone is done, this keeps it honest.
- [ ] `notes/tempo/114_maintenance.md` — run the standing Tempo maintenance pass (build + test green, dependency/spec drift check) and log the outcome. (~30 min)
  - Output: `notes/tempo/114_maintenance.md` records `cargo test` status for the Tempo crate + any drift noted; commit hash captured.
  - 🎯 **Hardened acceptance**: the Tempo workspace builds + tests green; if the RWA-collateral option is dormant this week, the note explicitly states "no changes, parked" rather than being skipped silently.
  - *Mental model* (v3): Tempo is the parallel **RWA-collateral-rails option** (§2, §4A v1.5). The RWA narrative now lives primarily inside the capstone's v1.0 RWA-aware seams (the `oracle-mark` seam), so Tempo is a kept-warm option, not a competing path — a weekly green-or-parked log means that if a founding-eng conversation turns toward RWA-perps, the Tempo rails are auditable at a glance, not stale.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
