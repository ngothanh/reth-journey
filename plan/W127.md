# Week 127 — Deep-dive bet conversations + trial collaborations + crypto-MM bridge interviews (parallel)

> **Phase**: 7 (bet-path — founding/core-eng token-equity bet, NOT job-landing)
> **Month**: M33 — *Active bet outreach + ecosystem filtering*
> **Track**: Bet-path primary (on-chain-derivatives founding/core-eng conversations) ∥ Bridge income (crypto-MM cash seat — the income floor, hireable on Half-2 latency craft)
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: n/a — bet-path week, no new crate; the **artifacts are the `perp-dex-core` v1.0 deep-dive + the §6 GO/NO-GO scorecards**, not a mirror
> **Crate(s) touched**: n/a (maintenance only — keep `perp-dex-core` cluster-VOPR green, one Reth PR alive; **no v1.0/v1.5 feature work this week** — the engine is the evidence, this week you *deploy the evidence in conversations*)
> **Bar**: (n/a) — bet-path week, no shippable crate. The bar that matters this week is the **§6 ecosystem-selection checklist** (the GO-only-if-all-seven-pass gate) and the **outbound discipline metric** (deep-dives advanced, not screens passed).
> **Feeds into**: W128 finalist conversations / work-trials (this week's per-venue §6 scorecard + each trial-collab artifact carries forward as the finalist dossier); W129 remaining finalist conversations; W130 crypto-MM bridge activation (the parallel bridge track started this week becomes the safety net if no GO-bet lands); **W131 Bet #1 decision** (the §6 scorecards built across W125–W129 ARE the GO/NO-GO decision matrix); README.md §5 **M30 bet-readiness gate → M36 Bet #1 placed OR bridge engaged**
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): README.md **§6 ecosystem-selection checklist** (the seven GO conditions — the load-bearing reference all week), **§4A v1.0 lock** (the interface/invariant shapes you defend in every deep-dive: `SettlementId`=VSR op-number, const-30% netting haircut, fee-accrued insurance fund + waterfall, two projection staleness contracts, incremental-margin hot path at 100k accts/1M orders/s), **§7 shots-on-goal + geography flip-trigger**, **§9 risk register**; your own `notes/bet_outreach/` from W125–W126 (the target list + first-conversation debriefs); your flagship blog (W114–W117) + the cluster-VOPR bug-find writeups as the concrete-artifact anchors; the venue's public docs/whitepaper **only for the venue you deep-dive that day** (just-in-time, never a reading project).
> **v3 modifications**: **Whole-week reframe from the v2 file's "second-round technical interviews (3 firms)" to the v3 bet-path.** v2 treated W127 as generic Tier-A-HFT job rounds; v3 §A re-aims Phase 7 to **bet placement / bridge** — so this week runs (1) **deep-dive conversations / trial collaborations** with on-chain-derivatives founding teams (each gated through the §6 checklist), in **parallel** with (2) **crypto-MM bridge interviews** (the income floor underwritten by latency craft). The deliverable is no longer a firm scorecard for picking a salary — it is a **§6 GO/NO-GO scorecard per venue** feeding the W131 Bet #1 decision, plus a live bridge pipeline so runway is never the thing that forces a bad bet. The "interview *is* the artifact" discipline from the v2 career weeks is retained; the schema is the §6 checklist, not a generic fit score.

This is a **bet-path week, not a Build week and not a generic job-hunt week**. There is no crate, no upstream mirror, no engineered trap-test arc — manufacturing one would be padding. The week runs **two tracks in parallel**: the **bet track** (deep-dive conversations + a trial collaboration with the most promising founding/core-eng opportunity) and the **bridge track** (crypto-MM cash-seat interviews — the income floor per §7). Each item names its artifact file and a pass criterion. The load-bearing output is the **§6 GO/NO-GO scorecard per venue**: a bet is GO *only if all seven §6 conditions pass*; otherwise it is a bridge job, not a bet, and you treat it as such. The discipline this week is **honest §6 application** (the Airwallex anti-pattern — late-stage tiny equity dressed up as "founding" — is exactly what the checklist exists to catch), not interview performance.

**The two-track structure (read before Monday):** Bet conversations and bridge interviews are *not* competing for the same slot — they are the two destinations the Binance-alumni map points at (§2). The bet track decides *whether a GO-bet exists*; the bridge track guarantees *runway exists regardless*, so you never enter a bet from desperation (§7: "never quit a bridge before vest milestones"; size each bet so failure ≠ ruin). Recovery days sit between the heavy conversation days because — exactly as in the v2 career weeks — back-to-back high-stakes conversations with zero recovery is the documented way candidates degrade by the third one (back-pressure-as-correctness, applied to yourself).

The §6 scorecard is one append-only file, `notes/bet_outreach/W127_scorecards.md`, with one section per venue, using a fixed schema so the W131 decision matrix can diff them directly:
```
## Venue N — <name> — <date> — <stage/backing> — <my role if I joined>
1. Founding-tier allocation?   GO / NO / UNKNOWN  — <evidence: band, % or token units, source>
2. Tier-1 backing?             GO / NO / UNKNOWN  — <which of Paradigm/a16zcrypto/Dragonfly/Polychain/1kx/Multicoin/Jump/Wintermute>
3. Core-criticality?           GO / NO / UNKNOWN  — <do I build the engine that IS the company? which component?>
4. Multi-billion-FDV ceiling?  GO / NO / UNKNOWN  — <TAM + comparable: Hyperliquid/dYdX/GMX/Variational/Ostium>
5. Token reality?              GO / NO / UNKNOWN  — <pre-TGE? cliff/vest? entry FDV headroom ≥10×? unlock predation?>
6. Remote-native?              GO / NO / UNKNOWN  — <VN-remote works? colo/on-site required?>
7. Cycle timing?               GO / NO / UNKNOWN  — <seed–B / pre-TGE? vesting-window tailwind?>
VERDICT: GO (all 7 pass) | BRIDGE-NOT-BET (≥1 fails) | DEFER (≥2 UNKNOWN — what to ask next round)
```

---

## Monday — Deep-dive conversation: venue 1 (founding/core-eng technical + architecture)

- [ ] Pre-conversation load: re-read `notes/bet_outreach/venue1_brief.md` + the §6 checklist; load the venue's architecture from their public docs/whitepaper. (~45 min)
  - 🎯 **Hardened acceptance**: the brief names the venue's **matching/settlement architecture** (on-chain-settle? hybrid CLOB? app-chain like dYdX v4, or a single-sequencer L1 like Hyperliquid?), the **founder/team** you're speaking with, and **≥2 questions that prove you read their actual design** (e.g. "you run a single sequencer — what's your view-change / liveness story?", "how do you handle cross-margin netting today?") — never a generic "what's the culture like." It also names which §6 cells you already know vs the ≥1 you must close *this* conversation.
  - *Mental model*: a founding-engineer deep-dive is a **peer architecture review**, not a screen — they are deciding whether you can own the engine that *is* the company (§6.3), and you are deciding whether all seven §6 conditions hold. Walking in with their consensus/settlement vocabulary loaded converts the first 10 minutes from a pitch into two engineers comparing notes. The 126 prior weeks + your shipped `perp-dex-core` v1.0 gave you the vocabulary; this 45 min loads it into working memory and points it at the §6 gaps.
  - Output: `notes/bet_outreach/venue1_brief.md` updated with a top-of-file `READY:` line listing the venue's architecture in one sentence, your ≥2 informed questions, and the §6 cell(s) this conversation must close.
- [ ] 90–120 min deep-dive conversation: their architecture + your `perp-dex-core` v1.0 as the evidence. (~120 min)
  - 🎯 **Hardened acceptance**: during the conversation you (a) **walk one §4A v1.0 design choice as a defended decision** — e.g. "`SettlementId` IS the VSR op-number, so finality is `op_number ≤ commit point` with no side mapping table and free idempotency" — narrating the *back-of-envelope* (Jeff-Dean numbers) and the *alternative you rejected and why*; (b) **name one trap you'd write a VOPR scenario for** (the insurance-fund tripwire, a divergence vector) — the same muscle from every Build week; and (c) **close ≥1 §6 UNKNOWN cell** for this venue. Treat "here's how I'd architect that, and here's the failure interleaving I'd test" as the pass; a hand-wavy "yeah we could do that" as the fail.
  - *Mental model*: you are not interviewing — you are demonstrating that the **convergence cell (`risk-engine`: per-mark-tick portfolio recompute, zero-alloc, no spot analog)** is something you have *already built and VOPR'd*, which is precisely the founding-engineer proof (bar b). This is W074 Phase-1 design discipline, out loud, with a shipped artifact behind it. The venue is also being measured by you against §6 — listen for the allocation band and the cap table as hard as you answer.
  - Output: conversation happened; raw scratch in `notes/bet_outreach/venue1_raw.md` (architecture they described, questions asked both ways, where the §4A defense landed or wobbled, any allocation/backing/token signals) written within 60 min of finishing while memory is fresh.
- [ ] Same-day §6 scorecard for venue 1. (~30 min)
  - 🎯 **Hardened acceptance**: `notes/bet_outreach/W127_scorecards.md` has a venue-1 section in the fixed schema with **all seven §6 cells filled** (GO/NO/UNKNOWN + evidence), a one-line VERDICT, and — separately — three tagged lines: {what I demonstrated well, what I stumbled defending (each tagged `[reprep]` if it recurs at venue 2/3), the single biggest §6 risk for this venue}. No §6 cell may be left blank — an unanswered cell becomes `UNKNOWN` with the exact question to ask next round.
  - *Mental model*: memory of a conversation decays within hours; an unwritten §6 scorecard is a lost decision input you cannot recover at W131. The `[reprep]` tags are the feed-forward into Tuesday's targeted prep; the `UNKNOWN→question` lines are the agenda for W128's finalist round. **The Airwallex anti-pattern lives in cell 1 (founding-tier allocation) and cell 5 (token reality)** — if those two are NO or you can't get evidence, the verdict is BRIDGE-NOT-BET no matter how good the engineering culture feels.
  - Output: `notes/bet_outreach/W127_scorecards.md` venue-1 section complete (7 cells + verdict + 3 tagged lines); `grep -c '\[reprep\]' notes/bet_outreach/W127_scorecards.md` ≥ 0 (the count is Tuesday's backlog).
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the venue-1 §6-scorecard commit.

## Tuesday — Recover + targeted re-prep + bridge-track warm-up

- [ ] Recover deliberately: light 4-hour day-job/maintenance day, no bet prep before noon — protect cognition for Wednesday's trial collaboration. (~30 min logged)
  - 🎯 **Hardened acceptance**: a one-line dated entry in `notes/bet_outreach/recovery_log.md` confirming the morning was off-prep. Back-to-back high-stakes conversations with no recovery is the documented way you degrade by the third one.
  - *Mental model*: conversation performance is a depletable resource like the hot path's L1 cache — running deep-dives + a trial collab + bridge interviews with zero recovery is the human equivalent of thrashing. The recovery day is back-pressure-as-correctness applied to yourself (the same invariant the matching engine enforces under 10× load).
  - Output: `notes/bet_outreach/recovery_log.md` has a dated Tuesday entry.
- [ ] Targeted re-prep from venue-1 `[reprep]` tags only (no broad re-study). (~50 min)
  - 🎯 **Hardened acceptance**: for every `[reprep]` item, write a 3–5 line "if challenged again, here's the crisp defended answer" card; if the stumble was a *design* gap (not a nerves gap), the card cites the exact §4A interface/invariant or the cluster-VOPR scenario that already proves it. If venue 1 surfaced a §6 UNKNOWN you can close with prep (e.g. researching their cap table), do that here.
  - *Mental model*: the highest-ROI prep before venue 2 is the delta you just measured, not a generic re-read. This is the debug loop — Monday produced the failing tests (where your §4A defense wobbled); Tuesday makes exactly those pass. The cards become reusable defenses for every later conversation.
  - Output: `notes/bet_outreach/reprep_cards.md` has one card per Monday `[reprep]` tag.
- [ ] **Bridge track**: schedule/confirm crypto-MM cash-seat interviews and prep the latency-craft pitch. (~40 min)
  - 🎯 **Hardened acceptance**: at least one crypto-MM bridge interview (Wintermute/Jump-style remote desk) is **scheduled** for this week or next, and `notes/bridge/mm_pitch.md` has a 5-bullet latency-craft pitch anchored on **shipped, measured artifacts**: the `latency-lab` tick-to-trade report (p99.99, coordinated-omission-corrected), the matching-engine single-shard p99 ≤5µs / ≥1M orders/s numbers, and one mechanical-sympathy war-story (false-sharing or NUMA finding from `perf`). Each bullet cites a real number or a real commit, not an adjective.
  - *Mental model*: the bridge is the floor that makes the bet *safe* (§7) — runway must exist regardless of any single venue. The crypto-MM seat is hireable on **Half-2's latency craft**, which is exactly why `latency-lab` was pulled to M6: it is the underwriting for the income floor. Running this in parallel means you never enter a Bet #1 conversation from a position of runway desperation, which is how people take bad allocations.
  - Output: `notes/bridge/mm_pitch.md` exists (5 number-anchored bullets); a bridge interview is on the calendar (log the date in the file).
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday re-prep + bridge-prep commit.

## Wednesday — Trial collaboration with the leading venue (a real, scoped technical task)

- [ ] Scope the trial-collab task and confirm acceptance with the venue before starting. (~30 min)
  - 🎯 **Hardened acceptance**: `notes/bet_outreach/trial_scope.md` names a **concrete, bounded deliverable** (e.g. "draft the view-change correctness invariants for their sequencer", "design the cross-margin netting interface for a second instrument", "review their settlement-finality model against the `SettlementId`=op-number pattern and write the diff") — with an explicit definition-of-done and a time box (a trial is hours-to-a-day, not an unpaid feature build). It also records **what the trial is testing on each side**: them → can you own the engine; you → §6.3 core-criticality + §6.6 remote-native working style.
  - *Mental model*: a trial collaboration is the single highest-signal §6 instrument — it directly tests cell 3 (core-criticality: do they let you near the engine that *is* the company?) and surfaces the real working culture (cell 6). An unbounded trial is a red flag (free labor, not a bet); a crisply scoped one with mutual acceptance is exactly the W074 Phase-1 discipline of stating the contract before writing code.
  - Output: `notes/bet_outreach/trial_scope.md` with deliverable + definition-of-done + time box + what-it-tests-both-ways.
- [ ] Execute the trial-collab deliverable, applying the same design discipline as a Build week. (~150 min)
  - 🎯 **Hardened acceptance**: the deliverable applies the plan's muscle visibly — for a design task: a property table of the primitives, a back-of-envelope on the latency/capacity budget, ≥1 named failure interleaving you'd write a VOPR/loom scenario for, and the alternative you rejected with a one-line reason. For a code task: it compiles/tests green and carries at least one trap-test in the spirit of the determinism-gate / zero-alloc discipline. The artifact must be something **they can read and immediately see founding-engineer signal**, mapping to a §4A interface where relevant (so your `perp-dex-core` v1.0 vocabulary transfers directly).
  - *Mental model*: this is where "I built a replicated, cluster-VOPR'd perp-DEX-core" becomes "watch me reason at that level on *your* system in real time." It is bar (b) made interactive. Keep the deterministic-core instinct visible (no `f64` in a replay-safe path, identity derived from consensus, contracts split per consumer) — those are the exact instincts that distinguish a founding engineer from a senior IC.
  - Output: the trial deliverable committed/shared with the venue + a copy/summary in `notes/bet_outreach/trial_deliverable.md`.
- [ ] Same-day debrief + update venue-1 (or trial-venue) §6 scorecard. (~30 min)
  - 🎯 **Hardened acceptance**: the trial-venue §6 section is updated — specifically **cell 3 (core-criticality)** and **cell 6 (remote-native)** should move from UNKNOWN toward GO/NO based on the actual collaboration; note explicitly whether the trial felt like *engine ownership* or *peripheral integration* (the §6.3 distinction that decides bet-vs-bridge). Any new `[reprep]` tags carry to Thursday.
  - *Mental model*: a trial that felt like real engine ownership is the strongest possible GO signal for cell 3; a trial that felt like glue-code/integration is a quiet NO on the most important §6 condition — record it honestly now, because at W131 you will be tempted to rationalize it away.
  - Output: trial-venue §6 scorecard updated (cells 3 + 6 moved off UNKNOWN with evidence); new `[reprep]` tags logged.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday trial-collab commit.

## Thursday — Recover + crypto-MM bridge interview (the income floor)

- [ ] Recover: protect cognition for the bridge interview + Friday; log the off-prep morning. (~30 min logged)
  - 🎯 **Hardened acceptance**: dated Thursday entry in `notes/bet_outreach/recovery_log.md`.
  - *Mental model*: same depletable-resource discipline as Tuesday; the bridge interview is real and underwrites your runway — do not walk into it fatigued from the trial collab.
  - Output: `notes/bet_outreach/recovery_log.md` has a Thursday entry.
- [ ] Crypto-MM bridge interview: lead with latency craft, anchored on measured artifacts. (~120 min)
  - 🎯 **Hardened acceptance**: during the interview you **narrate the tick-to-trade path with real numbers** from `latency-lab` (rdtsc, coordinated-omission-corrected p99.99) and **handle one tail-latency / mechanical-sympathy question** at depth (false-sharing, NUMA placement, allocator jitter, or batched-vs-per-event syscall trade-offs — the `sendmmsg` / group-commit instincts). Treat a correct first-principles latency derivation + a named real bottleneck you found as the pass; a confident wrong order-of-magnitude as the fail.
  - *Mental model*: the crypto-MM seat is the **primary bridge** (§1 bar d, §7) and it is hireable specifically on Half-2's `hft-review` craft — matching, tail-latency, mechanical sympathy. This interview is the floor that lets you say "no" to a bad bet allocation. It is *not* a fallback you're embarrassed by; it is the structural reason the 2–3-shot bet thesis is low-risk (§7: "no skill stranded").
  - Output: raw debrief in `notes/bridge/mm_interview_debrief.md` within 60 min (questions, where you stalled, their latency-bar signal, next-step + by-when).
- [ ] Targeted re-prep from accumulated `[reprep]` tags (venue 1 + trial) for Friday's venue-2 deep-dive. (~40 min)
  - 🎯 **Hardened acceptance**: one crisp defended-answer card per outstanding `[reprep]`; if a stumble has now recurred across venue 1 + the trial, escalate it — write a deeper card grounded in the §4A lock and rehearse it aloud once. Tag any twice-recurring item `[2x]`.
  - *Mental model*: a §4A-defense stumble that survives two encounters is a real gap in how you explain your own engine, not bad luck. Two-strike escalation prevents it from costing you the venue-2 conversation, which may be the best-fit bet.
  - Output: `notes/bet_outreach/reprep_cards.md` extended; twice-recurring items tagged `[2x]`.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday bridge-debrief + re-prep commit.

## Friday — Deep-dive conversation: venue 2 (apply the tightened §6 + §4A defense)

- [ ] Pre-conversation load: `notes/bet_outreach/venue2_brief.md` + reprep cards (especially `[2x]`) + the §6 cells to close. (~30 min)
  - 🎯 **Hardened acceptance**: brief carries venue 2's architecture + your ≥2 informed questions + the specific §6 cell(s) this conversation must close; you can recite every `[2x]` card cold; the brief explicitly notes how venue 2's allocation/backing is *expected* to compare to venue 1 (so you're listening for the diff that matters at W131).
  - *Mental model*: same loading drill as Monday, now with venue-1 lessons baked in — this is the feed-forward paying off. By the second deep-dive your §4A defense should be a peer conversation, not a recitation.
  - Output: `notes/bet_outreach/venue2_brief.md` has a `READY:` line + the §6 cells to close.
- [ ] 90–120 min deep-dive conversation: venue 2. (~120 min)
  - 🎯 **Hardened acceptance**: narrate one §4A design choice with back-of-envelope; consciously clear any `[2x]` stumble cleanly (the proof the re-prep worked); name one VOPR-able trap; close ≥1 §6 UNKNOWN. If venue 2's architecture differs materially from venue 1 (e.g. app-chain vs single-sequencer), surface **how your engine's seams map to *their* shape** — that adaptability is the founding-engineer signal.
  - *Mental model*: the second deep-dive is where the per-venue loop compounds — you are now in conversations on a tightened version of yourself, and you are diffing two real GO candidates against §6 in real time.
  - Output: raw scratch in `notes/bet_outreach/venue2_raw.md` within 60 min of finishing.
- [ ] Same-day §6 scorecard for venue 2. (~30 min)
  - 🎯 **Hardened acceptance**: venue-2 section in the **identical fixed schema** (7 cells + verdict + 3 tagged lines); explicitly note whether any venue-1 `[reprep]`/`[2x]` recurred and how it went (your re-prep effectiveness signal). The comparison to W131 only works if the schema is identical across venues — keep the format byte-for-byte the same.
  - *Mental model*: the W131 Bet #1 decision is a *diff* across §6 scorecards; a schema drift between venue 1 and venue 2 destroys that diff. Discipline on the schema now is the cheap insurance for the most important decision of the plan.
  - Output: `notes/bet_outreach/W127_scorecards.md` venue-2 section complete; carries new `[reprep]` tags for W128.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the venue-2 §6-scorecard commit.

## Saturday — Consolidate the bet-readiness picture + keep the evidence alive

- [ ] Build the cross-venue §6 comparison matrix + a GO/BRIDGE/DEFER ranking. (~50 min)
  - 🎯 **Hardened acceptance**: `notes/bet_outreach/W127_comparison.md` is a table with one row per venue and columns {stage/backing, §6 cells-passing count (n/7), VERDICT, my role-if-joined (engine-critical y/n), biggest §6 risk, the single question that flips the verdict, follow-up owed + by-when}. Every venue has a follow-up owed with a date. The matrix ends with an explicit **W131-facing line**: which venue(s) are live GO candidates, which are BRIDGE-NOT-BET, and which are DEFER-pending-one-answer.
  - *Mental model*: this single artifact is the load-bearing output of the whole week — it converts ephemeral conversations into the **W131 Bet #1 decision input**, and it enforces the §6 honesty that the entire bet thesis depends on. A venue with great engineers but a NO on cell 1 (allocation) or cell 5 (token reality) is a *bridge job*, and naming it as such now prevents the W131 rationalization that ends in a tiny-equity Airwallex-pattern trap.
  - Output: `notes/bet_outreach/W127_comparison.md` with a complete table + the W131-facing GO/BRIDGE/DEFER line + dated follow-ups for every venue.
- [ ] Bridge-pipeline + geography-prep upkeep. (~30 min)
  - 🎯 **Hardened acceptance**: `notes/bridge/pipeline.md` lists every crypto-MM conversation with its stage + next step + by-when (no bridge lead sits silently stale — runway is the thing that must never force a bad bet). Plus a one-line check on the **§7 geography flip-trigger**: do any warm conversations cluster in one hub (Dubai = crypto-native/Binance-dense; SG = institutional/Asia), or has any presence-contingent offer premium appeared? If not, **Vietnam-remote remains default — no move, no lease** (cheap prep only: confirm visa + pre-TGE token-tax eligibility stays current for both hubs).
  - *Mental model*: §7 is explicit — *pre-stage, do not pre-commit*. The flip-trigger (warm-conversation cluster OR presence-premium) is the only thing that converts prep into a move; until it fires, relocation is a distraction with a lease attached. Tracking it as a one-line weekly check keeps the optionality alive without paying for it.
  - Output: `notes/bridge/pipeline.md` updated; `notes/bet_outreach/geography_check.md` has a dated one-line flip-trigger status.
- [ ] Keep the evidence alive: `perp-dex-core` cluster-VOPR green + one Reth PR maintained. (~50 min)
  - 🎯 **Hardened acceptance**: `perp-dex-core` cluster-VOPR StateChecker still runs green (linearizability + replica convergence + insurance-fund tripwire-never-hit at the declared accrual rate — README.md §4A acceptance #7); every open Reth PR you own is either rebased-clean-and-CI-green or carries a posted dated next-step comment. No artifact rots during bet season — a live, green, multi-node-VOPR'd venue + a maintained upstream PR are the two most clickable credibility signals a founder or hiring manager has.
  - *Mental model*: every conversation this week asked "show me you can own the engine"; a green cluster-VOPR run and a live Reth PR are the proof, and letting them rot mid-bet-season undercuts the exact narrative you're selling (bar b). Maintenance here is not busywork — it is keeping the bar-(b) evidence load-bearing while you deploy it in conversations.
  - Output: cluster-VOPR run log shows green; `gh pr list --author @me --repo paradigmxyz/reth` shows each PR green or with a dated next-step comment.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday consolidation + maintenance commit.

---

## [v3.1 BFT apex — Track B] W127: handle/abandonment wiring end-to-end (the O5 surface on the real BFT cluster)

> Track B continues (W126 proved happy-path commit). This week wires the **amended `ConsensusBackbone` surface** (O5: `propose→ProposalHandle`, `drain_committed`+`Commit::proposal_handle()`, `drain_abandoned`) end-to-end through the `BftReplica` cluster — a client/ledger driving it sees commits AND abandonments resolve correctly, with the W125 permanent-prune detector live. Bar (c). This is the proof that the O5 amendment + the BFT impl together honor the full client contract.

- [ ] **[Tue +90min · Thu +75min]** [Venue · BFT] **Build (bar c)**: `crates/consensus-vsr/tests/bft_handle_lifecycle.rs` — drive a `ReplicatedLedger<BftReplica>` (the W92 ledger generic over the trait, now bound to the BFT impl) and assert the full handle lifecycle on a 4-node BFT cluster: `submit → ProposalHandle`; committed ops resolve via `drain_committed`+`proposal_handle()`; orphaned-before-slot ops resolve via `drain_abandoned` (W125 permanent-prune); routine view rotation drains nothing; every handle terminates exactly once. (~total 165 min)
  - **Phase 1 (compact — seal in `notes/127_lifecycle_prediction.md` before coding):**
    - *Prediction (sealed)*: the W125 detector has just marked handle h abandoned — write the exact chain of calls that must run before the client's `tx_pending[h]` resolves, and what the client observes if any link is missing. Seal it.
    - *Back-of-envelope*: the handle map sits on the submit path — a slab-indexed `FixedMap` (`ProposalHandle(u64)` → slot) ≈ ~20 ns lookup, zero rehash, 0 allocs; a `HashMap` rehash at a 2^k boundary = a multi-µs stall + alloc spike exactly when the book is busiest.
    - *Pre-mortem (interleaving)*: detector pushes h to `pending_abandoned` → the ledger's tick loop drains only `drain_committed` → `tx_pending[h]` never resolves → the client hangs precisely during a Byzantine-induced view change — the O5 leak resurrected one layer up, with every unit below it green.
  - 🔧 **Why this matters**: this closes the loop the O5 amendment opened — the ledger (and `perp-dex-core`) depend on `ConsensusBackbone`, and W127 proves the *real BFT impl* satisfies that contract identically to VSR (the whole point of the seam). It composes the W92 `ReplicatedLedger<B>` (handle-keyed `tx_pending`, drain via `drain_abandoned`) with the W125 BFT permanent-prune detector — the ledger code is **unchanged** from its VSR binding; only the type param `B` differs. The trap: an abandonment that the ledger doesn't observe (the detector fired but the surface didn't deliver to `drain_abandoned`'s sink) → a hung `tx_pending` entry → the exact O5 leak, now under BFT. The cross-seam proof: BFT abandonment (W125, high-QC-passed-it) flows through the SAME `drain_abandoned` the VSR ledger used for supersession — no ledger change.
  - *Contract*: reuse `ReplicatedLedger<B: ConsensusBackbone<Op=Transfer>>` (W92) with `B = BftReplica<LedgerSM>`; the apply path (`drain_committed` → resolve by handle) and drain path (`drain_abandoned` → `Err(Abandoned)`) are byte-identical to the VSR binding (diff = empty).
  - **Tests:**
    - `bft_committed_op_resolves_by_handle` — a `submit` whose block commits resolves the `tx_pending[handle]` via `drain_committed`+`proposal_handle()` with the correct result. (The W92 ledger path, now on BFT.)
    - `bft_orphaned_op_resolves_abandoned` — a `submit` orphaned by a pacemaker view-change resolves `Err(Abandoned)` via `drain_abandoned` (W125 detector → surface → ledger sink), not a hang. The O5 leak provably closed under real BFT.
    - `bft_routine_rotation_drains_nothing` — leader rotates every view (BFT-native); `tx_pending` is NOT drained, no spurious `Abandoned` for committed ops. (`test_routine_rotation_does_not_drain_pending`, W91, now under the real BFT view manager — behavioral swap-safety confirmed on the apex impl.)
    - `bft_ledger_code_unchanged_from_vsr` — `diff` the `ReplicatedLedger` source used with `BftReplica` vs `VsrReplica`: **empty** (the ledger binds the trait; only the type param differs — acceptance #9 holds for the BFT impl, not just the stub).
    - 🪤 **negative-control `lifecycle_check_has_teeth` (SENIOR)** — plant the unwired-sink bug (the W125 detector fires but `drain_abandoned` is never drained by the ledger loop) and assert `bft_orphaned_op_resolves_abandoned` FAILS within its bounded-view window — proves the test detects the wiring gap via a bounded check, not by an infinite hang or a vacuous timeout pass.
  - **Make-pass card (EXPERT):** `bft_orphaned_op_resolves_abandoned` hangs → symptom: the W125 detector marked the handle abandoned but the ledger's `tx_pending` entry never resolves; why: `drain_abandoned`'s sink wasn't wired to the ledger's drain callback (the detector populated `pending_abandoned` but nothing drained it to the ledger); fix: the ledger's drain loop calls `backbone.drain_abandoned(&mut |h| resolve(tx_pending.remove(h), Err(Abandoned)))` every tick, same as it drains commits; cost avoided: the O5 leak under BFT (a hung client at the worst time — a Byzantine-induced view change); **Cost avoided (bar-c, measured)**: the `tx_pending` handle map is slab-indexed (`ProposalHandle(u64)` → slot): ~20 ns/lookup, zero rehash, 0 allocs on the submit path — vs a `HashMap` whose 2^k-boundary rehash is a multi-µs stall + alloc spike under load; muscle: every backbone terminal surface (committed AND abandoned) must be drained by the caller every tick. Reapplies: W128 integration, W135 the perp-dex-core swap.
  - **Output**: `bft_handle_lifecycle.rs` green; `ReplicatedLedger<BftReplica>` works with an empty diff vs the VSR binding; the O5 surface (commit + abandon) honored end-to-end on real BFT; behavioral swap-safety (no-drain-on-rotation) confirmed on the apex impl.
- [ ] **[Sat +10min]** [Venue · BFT] Handoff note → `notes/127_bft_handle_lifecycle.md`: the amended `ConsensusBackbone` surface is honored end-to-end by the real BFT impl; ledger code unchanged. **Next W128: integrate `BftReplica` behind `ConsensusBackbone` (the W103 stub-swap test now passes with the REAL BFT impl, not the stub).** (~10 min)
- [ ] **[Sat +10min]** Commit + log (~10 min)

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md. Track B: O5 handle/abandonment surface honored end-to-end on the real BFT cluster (ledger code unchanged); next is integration behind `ConsensusBackbone` (W128).

**This week's ritual focus (bet-path):** review the `W127_comparison.md` GO/BRIDGE/DEFER ranking against README.md §5 (the M30→M36 arc) and §7 (2–3 shots, never one all-in). Answer in your weekly log: (1) Is there ≥1 live GO candidate heading into W128 finalist conversations, or is the honest read "no GO yet → lean on the bridge"? (2) Is the bridge pipeline healthy enough that runway will never force a bad allocation? (3) Did any venue tempt me to soften a §6 NO into a "well, almost" — and did I hold the line? The discipline the whole plan back-plans to is here: a bet is GO **only if all seven §6 conditions pass**; everything else is a bridge, and a bridge is a floor, not a failure.
