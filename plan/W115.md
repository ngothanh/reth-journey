# Week 115 — Blog post #6: vector-db inheritance + interview-prep deepening

> **Phase**: 6
> **Month**: M29
> **Track**: (career/visibility) Public visibility push + interview-prep deepening + 1500hr milestone
> **Hours target**: 40h/wk (M19-M34)
> **Mirror target**: (none — this is the public-write-up week for the `vector-db` v0.5 shipped W104; no upstream module is mirrored. The post DESCRIBES how `mini-db` (W100) + bufpool/bloom/wal/time backed vector-db, but writes no code.)
> **Crate(s) touched**: (none — `vector-db` is frozen at v0.5 from W104; this week documents it, does not edit it)
> **Bar**: (n/a) — career/visibility/blog week. The crate it discusses, `vector-db`, is bar (b); no code is written here so no bar applies.
> **Feeds into**: W116-W117 (Blog #7 Phase-6 retro reuses the inheritance graph + numbers), W118-W119 interview prep (the vector-db inheritance story + the systems-design mock become portfolio talking points), W121 flagship blog post (cites the published vector-db post + the quantization recall), W125 applications (blog #6 is a linked artifact in outreach)
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): README §"Inheritance Map" (lines ~699-828) for vector-db's dependency edges; README §APPENDIX inheritance-ratio table (line ~820, `vector-db v0.5 ... ≥0.70`); your own `notes/` from W101-W104 for the as-built HNSW/quantization/filter numbers; Qdrant quantization article (https://qdrant.tech/articles/what-is-vector-quantization/), BQ article (https://qdrant.tech/articles/binary-quantization/), Qdrant 1.18 release notes (https://qdrant.tech/blog/qdrant-1.18.x/, TurboQuant).
> **v2 modifications**: none

**Monday — Blog post #6 draft**

- [ ] **Draft** `blog/2027-XX-XX-vector-db-inheritance.md` — "vector-db: HNSW + quantization + filtered search in one weekend", centered (again) on inheritance discipline: how the `mini-db` (W100) LSM stack backed the payload store, and how bufpool/bloom/wal/time were reused untouched. Open with the dependency graph (vector-db ← mini-db, bufpool, bloom, wal, time, + segment-manager glue). State the measured LOC-inheritance ratio (target ≥0.70 per README ~line 820) and HOW it was measured. (~45 min)
  - 🎯 Hardened acceptance: draft contains (1) the rendered dependency graph with the 5+ named upstream crates, (2) the exact inheritance-ratio number AND the one-line method that produced it (`inherited_LOC / total_LOC`, with what counts as "inherited"), (3) at least one concrete "this primitive was built once at W.. and reused untouched in vector-db" example (e.g. bloom from W31, bufpool, the mini-db payload store from W100). `ls blog/2027-XX-XX-vector-db-inheritance.md` shows the draft.
  - *Mental model*: Blog #6 is the second public proof (after Blog #5 mini-db) that "build the primitive once" compounds — vector-db reused an entire shipped database as a subsystem. It exists now because vector-db just shipped (W104, 11 weeks ago) and the as-built numbers in `notes/` from W101-W104 are still recoverable; writing it now (vs at W121) means the flagship post can cite a lived, already-published artifact instead of reconstructing it.
- [ ] Commit `blog/2027-XX-XX-vector-db-inheritance.md` draft. (~15 min)
  - Output: `git log -1 --stat` shows the draft committed.

**Tuesday — Blog post #6 edit + post**

- [ ] **Edit pass** on `blog/2027-XX-XX-vector-db-inheritance.md` — tighten claims, verify every quantitative claim (ratio, LOC, crate count, recall %, compression ratio) against `notes/` from W101-W104 and the README inheritance table. (~25 min)
  - 🎯 Hardened acceptance: every number in the post has a checkable source noted inline or in `notes/blog6_sources.md`; no number appears that you cannot point to its origin. Edit-pass diff committed.
  - *Mental model*: A post about inheritance discipline that contains an unsourced number undermines its own thesis. The edit pass is what makes the post citable in W116/W121/W125 — a maintainer or interviewer who clicks through must find the claims hold.
- [ ] **Publish + amplify** `blog/2027-XX-XX-vector-db-inheritance.md` to the live blog; cross-post (canonical link), X/Twitter announcement thread, r/rust + r/MachineLearning optional. Record live URLs + timestamps in `notes/blog6_distribution.md`. (~30 min)
  - 🎯 Hardened acceptance: `notes/blog6_distribution.md` lists the canonical URL + each cross-post URL with timestamps; the canonical link resolves publicly (open in a browser, confirm 200 + correct content).
  - *Mental model*: Distribution converts a private note into a portfolio asset. The recorded URLs are the exact links pasted into W120/W125 outreach — capturing them now avoids re-hunting later.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --stat` shows `notes/blog6_distribution.md` committed; weekly log updated.

**Wednesday — LeetCode Hard ramp**

- [ ] **Shift to LeetCode Hard daily** — interview prep ramps toward the W118-W119 curriculum. Today's session: 1-2 Hard problems, logged in `notes/leetcode_log.md` (problem #, topic, time-to-solve, whether solved unaided, the one insight). Bias topic selection toward graph/heap/DP — the categories most likely in destination-tier screens. (~45 min)
  - 🎯 Hardened acceptance: `notes/leetcode_log.md` has today's dated entry with problem #, topic tag, time-to-first-correct, and a one-line takeaway; any unsolved problem records the specific stuck-point (not just "couldn't do it") so it becomes a study target.
  - *Mental model*: The W118-W119 interview-prep curriculum assumes the LeetCode reflex is already warm — starting the Hard ramp at W115 means prep week is "polish + systems design," not "relearn DP under time pressure." The stuck-point log surfaces weak categories while there is still runway before applications open at W125.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --stat` shows `notes/leetcode_log.md` committed.

**Thursday — Distributed-systems mock interview**

- [ ] **Mock systems-design interview** — find a friend or use an AI interviewer. Topic: "design a market-data fan-out for 10M msg/s." Drive it as a real interview (clarify requirements, sketch the architecture, defend trade-offs). Capture the design + every interviewer pushback + your answer in `notes/mock_marketdata_fanout.md`. Pull from lived artifacts: `messaging-aeron` (W77-79 fan-out), `marketdata-kernelbypass` (W84-90 kernel-bypass), `mmap-queue` (W31), `concurrent` SPSC/MPMC rings (W04-W11). (~50 min)
  - 🎯 Hardened acceptance: `notes/mock_marketdata_fanout.md` contains (1) the back-of-envelope sizing (10M msg/s × payload → bytes/s, fits in cache-line/page math), (2) the chosen fan-out topology with the single-writer / back-pressure decision named, (3) ≥3 interviewer pushbacks each with your answer, and (4) a self-scored "where I fumbled" list. At least one answer must cite a specific crate YOU built (not a generic textbook answer).
  - *Mental model*: A market-data fan-out is the exact systems-design question a destination-tier HFT firm asks — and it maps 1:1 onto crates already in the portfolio (aeron, marketdata, mmap-queue). The mock now turns "I built these" into "I can defend these live," which is the entire point of building them. The fumble-list feeds the W118-W119 prep so weak spots are drilled before real rounds at W126-W129.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --stat` shows `notes/mock_marketdata_fanout.md` committed.

**Friday — Reth PR maintenance + Tempo skim**

- [ ] **Reth PR follow-through** — check open/in-flight Reth PRs for review comments; address any, or push the next small contribution. Log state in `notes/reth_pr_tracker.md` (PR #, status, next action, date). (~35 min)
  - 🎯 Hardened acceptance: `notes/reth_pr_tracker.md` reflects every open PR with a concrete dated next action; reviewer comments are either addressed (commit pushed) or recorded as a dated TODO.
  - *Mental model*: Merged Reth PRs gate the backup-job milestone; momentum must not lapse in a visibility-heavy week. A weekly tracker keeps the contribution pipeline visible so no PR silently rots before the W125 application window.
- [ ] **Tempo skim** — 15-min skim of Tempo repo activity since last check; note anything Path-D-relevant in `notes/tempo_watch.md`. (~15 min)
  - 🎯 Hardened acceptance: `notes/tempo_watch.md` has a dated entry — either "no material change" or the specific change + whether it affects Path D viability.
  - *Mental model*: Path D (Tempo route) stays a live backup through Phase 7; a cheap weekly skim keeps it warm without derailing the primary track.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --stat` shows the two Friday notes committed; weekly log updated.

**Saturday — Tempo maintenance + Qdrant quantization recall**

- [ ] **Tempo maintenance** — Commit + log any Tempo upkeep. (~10 min)
  - Output: `git log -1` shows the Tempo maintenance commit; weekly log note added.
- [ ] **Tái-dựng (Qdrant quantization)** — close source. From memory, list 3 quantization methods (SQ scalar ~4×, PQ product, BQ binary 32×/~40×-speedup) + their recall trade-offs + their memory ratios in `notes/recall_qdrant_quantization.md`. Then check vs https://qdrant.tech/articles/what-is-vector-quantization/ and https://qdrant.tech/articles/binary-quantization/ (BQ 40× speedup). (~60 min)
  - 🎯 Hardened acceptance: ≥2/3 methods recalled with correct compression ratio + recall floor, each written to `notes/recall_qdrant_quantization.md` BEFORE opening the source; the diff-vs-source pass annotates each miss. Bonus: TurboQuant (Qdrant v1.18, https://qdrant.tech/blog/qdrant-1.18.x/) added the "2× SQ compression at similar recall" claim — recall that and you over-perform.
  - *Mental model*: 11 weeks past the W101-W104 vector-db ship, the quantization trade-offs have decayed. Recall them now, before the W116 Blog #7 retro and the W121 flagship blog draft require citing them — so the public posts quote lived knowledge, not freshly-regurgitated docs. The closed-book-first protocol is what proves the knowledge is actually retained, not re-read.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
