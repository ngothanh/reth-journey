# Week 130 — Bridge safety net: crypto-MM cash-seat activation (if no GO-bet yet)

> **Phase**: 7 (Bet Placement / Bridge)
> **Month**: M34
> **Track**: (career — bet-path: income-floor / bridge leg of the shots-on-goal structure)
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: (n/a — career/bridge week; no upstream module, no crate hot path)
> **Crate(s) touched**: (none — `perp-dex-core` v1.5 + Reth/Tempo forks in maintenance only)
> **Bar**: (n/a — no Build; the bar this week is *decision quality + leverage*, not a code bar)
> **Feeds into**: W131 **Bet #1 decision** (the crypto-MM bridge offers + the GO/NO-GO §6 scorecard pre-written this week ARE the inputs W131 decides against; the bridge becomes the BATNA that anchors any founding-eng negotiation — a candidate with a signed cash floor negotiates token allocation from leverage, not need); W132 resignation + (conditional) relocation logistics (the chosen path gates the 30-day-notice timing and the §7 flip-trigger evaluation planned at W124); W134 start at the bet (or bridge) — remote-first, hub only if triggered; M36 destination-landing milestone (RECONCILED_PLAN_v3.md §A: "Bet #1 placed OR bridge engaged").
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): RECONCILED_PLAN_v3.md §1 end-state bar (d) (financial runway: crypto-MM cash seat as the *primary* bridge), §6 ecosystem-selection checklist (the 7 GO criteria — founding-tier allocation, tier-1 backing, core-criticality, ≥$1B-FDV ceiling, token reality, remote-native, cycle timing), §7 shots-on-goal (Bridge₀ → Bet #1) + the Dubai/SG flip-trigger; your own `progress.md` bet-outreach tracker (founding/core-eng conversations W126–W129 + crypto-MM desk pipeline opened W113/W127); `relocation_research.md` (Dubai + Singapore visa + token-tax prep built W120, lookup-when-stuck per geography); compensation/comp-band notes recorded during crypto-MM desk conversations.
> **v3 modifications**: this week was the v2 *"offer wait + Tier-B safety-net activation"* (a generic Tier-A-HFT-IC job-landing week). v3 re-aims it to the **bet-path**: the goal is no longer "land an HFT IC offer," it is **secure the income floor** (a crypto-MM cash seat per §1 bar (d) / §7) *so the W131 Bet #1 decision is made from leverage, not desperation*. Tier-A/Tier-B firm tracking → **two parallel pipelines**: (1) the GO-bet pipeline (founding/core-eng conversations, scored against the §6 7-criteria checklist) and (2) the crypto-MM cash-seat bridge pipeline (the floor). The Saturday decision-criteria pre-write becomes the **§6 GO/NO-GO scorecard + bridge-vs-bet rubric** (not a generic comp/fit rubric). The maintenance day keeps `perp-dex-core` v1.5 interview-ready (a live capstone is the artifact that produced the founding-eng conversations), not a generic Reth portfolio. References swapped from README v2 risk register to RECONCILED_PLAN_v3.md §1/§6/§7.

This is a bet-path career week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding the plan with exactly the busywork the SPEC forbids. The week's real job is a **leverage job**: per §7, a bet placed from a position of "I have no income floor" is not a bet, it is desperation, and desperation is the single biggest destroyer of allocation-tier outcomes. So this week (a) keeps the GO-bet conversations warm without going needy, (b) activates the **crypto-MM cash-seat bridge** as the income floor so W131 never decides from zero leverage, and (c) pre-commits the §6 GO/NO-GO scorecard + the bridge-vs-bet rubric BEFORE any offer lands, so the W131 decision is made by sober-you, not offer-day-adrenaline-you. Every item below carries an explicit artifact file + an Output line that proves it done; the Saturday scorecard carries a hardened-acceptance pass criterion and a mental model of why pre-commitment is the highest-leverage artifact of the whole week.

**The v3 frame this week locks in (read before Monday):** there are **two pipelines, not one**. Pipeline A = the **GO-bet** (founding/core-eng token-equity bet on an on-chain-derivatives venue) — scored against §6's 7 criteria; a bet is GO *only if all seven pass*. Pipeline B = the **bridge** (crypto-MM cash seat, remote, Wintermute/Jump-style) — the income floor of §1 bar (d), underwritten by your Half-2 latency craft. These are not competitors; per §7 the bridge *funds the runway between bets* and is the BATNA that lets you negotiate a bet from leverage. The W131 decision is: **GO Bet #1 (all 7 of §6 pass) OR engage the bridge and keep outbound**. This week makes sure both pipelines have real, scored rows by Saturday so W131 decides against data, not vibes. Geography (Dubai/SG) stays **pre-staged, not pre-committed** — the §7 flip-trigger is evaluated at W131/W132, not forced this week.

---

**Monday — Crypto-MM cash-seat bridge: desk A activation**

- [ ] Artifact: append to `progress.md` under a `## Bridge pipeline (crypto-MM cash seat)` heading (private). Decision gate first, written at the top: `GO-bet in hand: yes/no` (a GO-bet = a founding/core-eng offer that passes all 7 of §6, not just any conversation). If no GO-bet is locked by W130, **activate the bridge**: open crypto-MM cash-seat desk A (Wintermute / Jump-crypto / crypto-MM tier). Log {desk, role (quant-dev / core-latency IC), date sent, referral channel (Binance-alumni map?), expected cash comp band, remote-from-Vietnam: yes/no}. The crypto-MM cash seat is the **primary bridge** of §1 bar (d) — it is the floor that guarantees you walk into W131 with at least one path that pays, not zero. (~30 min)
  - *Mental model*: per §7, a founder/recruiter for a token-equity bet can smell a candidate with no income floor — and they will price your allocation accordingly (down). Opening the crypto-MM bridge turns "please grant me founding-tier tokens" into "I have a paying floor; I'm choosing whether your bet beats it," which is the single biggest lever on the W131 allocation tier. The bridge also caps downside: §7 says *never quit a bridge before vest milestones of the live bet* — a signed cash seat makes the whole shots-on-goal structure survivable if Bet #1 craters post-TGE.
  - 🎯 Hardened acceptance: `progress.md` opens the Bridge section with the `GO-bet in hand: yes/no` gate decision + reason; desk A logged with all six fields incl. the remote-from-Vietnam flag (§6 criterion 6 / bar (d) remote-native); if the gate said "GO-bet in hand, skip bridge," the skip reason is written explicitly (and you note whether you still open the bridge as a negotiation anchor per §7), not blank.
  - Output: `progress.md` Bridge section exists with the gate line + desk A row; `grep -A20 'Bridge pipeline' progress.md` shows the gate decision and at least one desk row with the remote flag.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday bridge-desk-A commit.

**Tuesday — Crypto-MM cash-seat bridge: desk B activation**

- [ ] Artifact: append desk B to the `## Bridge pipeline (crypto-MM cash seat)` section of `progress.md` (private). Same logging discipline as Monday: {desk, role, date sent, channel, expected cash comp band, remote-from-Vietnam}. Two crypto-MM desks (not one) so a single rejection still leaves a floor — redundancy in the income floor is cheap insurance. If desk B is institutional-leaning (crypto-MM at a TradFi-adjacent shop), note whether it still satisfies the remote-native requirement; a non-remote desk is a weaker floor given the Vietnam-remote lock (§ frontmatter geography). If a GO-bet locked since Monday, record that + decide explicitly whether desk B is still worth opening as a §7 anchor. (~30 min)
  - *Mental model*: §7's secondary fallbacks (crypto-infra IC on the Reth spine, Multiplier coast) exist precisely so *no skill is stranded* — but the *primary* floor is crypto-MM cash because it is underwritten by exactly the Half-2 latency craft you spent W57–W92 building, so it is the highest-probability, fastest-close bridge. Two open desks halve the chance the floor collapses and give the Saturday rubric two real bridge rows to rank against the bet rows, not one hypothetical.
  - 🎯 Hardened acceptance: desk B row present in `progress.md` with the same six fields as desk A; the remote-from-Vietnam flag is explicit (not blank); if a GO-bet arrived, the timestamp + the keep/skip decision for desk B is recorded with the §7 anchor reasoning.
  - Output: `progress.md` Bridge section now lists ≥2 desks; a quick count (e.g. `grep -c` on your row marker) reflects desk A + desk B rows, each with a remote flag.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday bridge-desk-B commit.

**Wednesday — Track GO-bet conversations (Pipeline A)**

- [ ] Artifact: append to `progress.md` under a `## GO-bet pipeline (founding / core-eng)` heading (private). Check in with each founding/core-eng conversation still live (finalist conversations + work-trials ran W126–W129). One polite, low-pressure message per venue: "Thanks again for the deep-dive — any update on next steps or a timeline I should plan around as I sequence my decision?" Don't be needy; do be present. Log per venue: {venue, last-contact date, their stated next-step + expected timeline, your follow-up date, and a one-line §6-criteria status flag (e.g. `allocation-tier: confirmed founding-band / unknown`; `tier-1 backing: Paradigm on cap table ✓`; `core-criticality: I build the matching/risk core ✓`)}. (~30 min)
  - *Mental model*: a bet-decision week is a coordination problem — you want all GO-bet conversations to converge in a tight window so W131 compares them side-by-side (and against the bridge floor) instead of being forced to commit to the first founder's "we need an answer this week" before the others mature. A gentle timeline check nudges processes to converge without signaling desperation. Capturing the §6-criteria status *now*, per venue, means Saturday's scorecard already has the raw data — you are not reconstructing whether Venue X had tier-1 backing under offer-day pressure.
  - 🎯 Hardened acceptance: every live GO-bet conversation has a row with {last contact, stated next-step/timeline, your follow-up date, ≥1 §6-criteria status flag}; venues that already gave a hard yes/no are tagged closed so the tracker reflects reality; at least one row carries an explicit allocation-tier or tier-1-backing note (the two §6 criteria most likely to fail a venue).
  - Output: `progress.md` GO-bet pipeline section lists each live venue with its timeline + a §6 flag; `git diff --stat` shows `progress.md` updated.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday GO-bet-status commit.

**Thursday — `perp-dex-core` v1.5 + Reth/Tempo maintenance**

- [ ] Artifact: `notes/130_maintenance.md` (private). Keep the portfolio alive during the decision window: pull latest on the forks, run `cargo build` + `cargo test` across the workspace (esp. `perp-dex-core` v1.5, `risk-engine`, `liquidation-engine` — the principal artifacts that *produced* these founding-eng conversations), confirm the cluster-VOPR drill still runs green, and triage any new upstream issue / PR comment on your merged Reth/Tempo work (a founder or crypto-MM desk may reference-check this week). Log: {what you pulled, build/test green or the named failure + plan, cluster-VOPR last-run status, any PR comment replied to or "no action needed + why"}. This is maintenance, not a Build — no new feature, no trap arc. (~30 min)
  - *Mental model*: your shipped `perp-dex-core` v1.0/v1.5 + the flagship blog (W121) + the VOPR bug-find writeups are end-state bar (b) of §1 — they are the *founding-eng credibility* that opened Pipeline A and the *latency craft* that underwrites the crypto-MM bridge in Pipeline B. Letting any of it bit-rot during the decision window is the one own-goal available right now. A green build + a still-green cluster-VOPR + a timely reply to a maintainer keeps the capstone demo-ready and the relationship warm exactly when a reference check or a "can you show me the engine live?" may fire.
  - 🎯 Hardened acceptance: `notes/130_maintenance.md` records a build/test result (green, or the named failure + plan) AND a cluster-VOPR last-run status; any open PR comment on your work is either replied to or logged as "no action needed + why"; if `perp-dex-core` v1.5 fails to build, that is flagged as a decision-affecting risk (a broken capstone undercuts both pipelines), not buried.
  - Output: `notes/130_maintenance.md` exists with build/test + cluster-VOPR status + triage notes; `cargo test` result captured in the file.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday maintenance commit.

**Friday — Floor + bet conditional follow-through (sizing the safety net)**

- [ ] Artifact: append to `progress.md` (private). Decision gate first, written at the top of Friday's entry: `Floor secured: yes/no` AND `GO-bet viable: yes/no`. (Floor secured = ≥1 crypto-MM desk responding positively or a comparable bridge in hand. GO-bet viable = ≥1 conversation plausibly passing all 7 of §6.) Then act conditionally, NOT as a quota:
  - If the floor is **thin** (both desks silent/rejected, no other bridge) AND no GO-bet: open crypto-MM desk C (or a §7 secondary fallback — crypto-infra IC on the Reth spine), logging the same six fields. Do NOT pad with extra desks if the floor is already solid — record "floor secured, no further bridge apps" and stop.
  - If the **GO-bet pipeline is thin** (no conversation passes ≥5 of §6) AND outbound bandwidth exists: send 1–2 targeted new founding/core-eng outreach messages via the Binance-alumni map / §6 shortlist, logging {venue, channel, §6 pre-screen — does it plausibly pass criteria 1/2/3?}. Do NOT outbound to venues that already fail a hard §6 criterion (e.g. late-stage, tiny employee equity — the explicit "Airwallex anti-pattern" of §6.1). (~30 min)
  - *Mental model*: this is two conditional gates, not two quotas — opening more bridge apps than you need wastes signal and dilutes follow-up energy, and outbounding to §6-failing venues wastes the scarcest resource (warm-intro credibility). The honest double gate ("is my floor actually secured, and is a real bet actually viable?") sizes both pipelines to the real risk instead of reflexive busywork — the same anti-padding discipline the SPEC demands of the plan itself. Per §6, a bet that fails one criterion is a *bridge job, not a bet*; chasing it dilutes the GO-bet pipeline.
  - 🎯 Hardened acceptance: Friday entry opens with both gates — `Floor secured: yes/no — <reason>` and `GO-bet viable: yes/no — <reason>`; bridge desk C / fallback logged only if the floor gate said thin; new GO-bet outreach logged only if the bet gate said thin AND each new target passes a §6 pre-screen (no Airwallex-anti-pattern venues); if both gates said "secured/viable," the no-further-action decision is written, not blank.
  - Output: `progress.md` Friday entry shows both gate decisions; conditional rows present only where the gate justified them, each with a §6 note where applicable.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday conditional-follow-through commit.

**Saturday — §6 GO/NO-GO scorecard + bridge-vs-bet rubric pre-write**

- [ ] Artifact: `progress.md` under a `## Bet #1 decision: §6 scorecard + bridge-vs-bet rubric (pre-written W130)` heading (private). BEFORE any offer lands, write the rubric W131 scores every option against. Two parts:
  - **Part 1 — §6 GO/NO-GO scorecard (per GO-bet candidate)**: a row per founding/core-eng candidate with all **7 §6 criteria** as explicit pass/fail columns, each with a concrete bar (not a vibe): (1) **founding-tier allocation** — equity/token grant in the founding band, NOT late-employee scraps (the Airwallex anti-pattern is an automatic FAIL); (2) **tier-1 backing** — ≥1 of {Paradigm, a16z crypto, Dragonfly, Polychain, 1kx, Multicoin, Jump, Wintermute} on the cap table (named, verified); (3) **core-criticality** — I build the matching/risk/settlement engine that *is* the company, not a peripheral integration; (4) **≥$1B-FDV ceiling** — TAM + named comparables (Hyperliquid/dYdX/GMX/Variational/Ostium) support it; (5) **token reality** — pre-TGE entry, ≈1y cliff / 4y vest, entry FDV leaves ≥10× headroom, no predatory unlock; (6) **remote-native** — Vietnam-remote works, no colo/on-site requirement; (7) **cycle timing** — seed–Series B / pre-TGE with a plausible cycle tailwind inside the vesting window. **A bet is GO only if ALL 7 pass** (§6 rule, restated at the top of the block). Any FAIL ⇒ it is a bridge job, not a bet.
  - **Part 2 — bridge-vs-bet rubric**: the rule W131 uses to choose between a GO-bet and engaging the crypto-MM bridge. Capture: the cash-comp floor number for the bridge; the runway-months it buys (§1 bar (d): ≥6–12 months at each bet entry); the rule for "engage bridge AND keep Bet #1 outbound" (§A/§7: bridge if zero bets pass all 7); a written tie-breaker (e.g. if two bets both pass §6, rank by allocation-tier × FDV-ceiling × tier-1-backing strength); and the §7 **flip-trigger** note (relocate to Dubai/SG only if a warm-conversation cluster or a presence-contingent allocation premium fires — evaluated W131/W132, NOT pre-committed). (~30 min)
  - 🎯 Hardened acceptance: Part 1 lists ALL 7 §6 criteria as explicit pass/fail columns, each with a concrete bar (named investors for #2, a named comparable + FDV figure for #4, cliff/vest/headroom numbers for #5, a hard remote requirement for #6); the "GO only if all 7 pass" rule is restated; at least one candidate is scored, or "no candidate yet — scorecard ready" is written. Part 2 carries a numeric cash floor, a runway-months figure tied to §1 bar (d), the bridge-engage rule, a written tie-breaker, AND the §7 flip-trigger note citing `relocation_research.md`. A criterion with no bar is a vibe, not a criterion — reject and tighten.
  - *Mental model*: offer day floods you with dopamine and time pressure — the two worst conditions for a high-stakes, multi-year, illiquid-token decision. Pre-committing the §6 scorecard while you are calm is a **precommitment device**: it lets sober-you bind offer-day-you, so the W131 GO/NO-GO is computed against the §6 bar you set on reflection, not improvised under a founder's "we close the round Friday — are you in?" §6 exists precisely because the failure mode of a token bet is *agreeing to a non-bet* (tiny allocation, no tier-1 backing, peripheral role) under social pressure and calling it a founding seat. This single artifact most changes the W131 outcome — and it is the gate between a $20–100M tail bet and a glorified employee-equity job.
  - Output: `progress.md` contains the `Bet #1 decision: §6 scorecard + bridge-vs-bet rubric` block with all 7 §6 criteria (each with a bar) + Part 2's numeric floor, runway-months, engage-rule, tie-breaker, and flip-trigger note; `grep -A40 'Bet #1 decision' progress.md` shows every §6 criterion carries a concrete bar (a name, a number, or a hard requirement).
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday §6-scorecard commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
