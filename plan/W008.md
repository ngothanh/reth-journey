# Week 8 — Foundry PR + revm familiarization + `eth-consensus` Receipt/Log

> **Phase**: 1
> **Month**: M2
> **Track**: Reth (primary)
> **Hours target**: 30h/wk (M1-M18)
> **Mirror target**: `alloy-consensus::Receipt` + `alloy-primitives::Bloom`; revm-primitives Database trait shape
> **Crate(s) touched**: `eth-consensus` v0.2 → v0.3
> **Feeds into**: `exec-vm` Phase 4, future `bloom` crate (W34).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): *Mastering Ethereum* ch13 (the EVM).
> **v2 modifications**: none (W1-W14 unchanged from v1)

**Monday — Foundry issue hunt + claim + concurrency drill #7**

- [ ] **Concurrency drill #7 of 28 — Mutex with weakened ordering (predict the bug from the design alone)**
  (solo, ~5 hrs). Pure prediction drill — almost no new code. Take your `SpinMutex` from drill #6 and answer
  before running anything: what specific bad observation does each of these single-ordering changes admit?
  Write your predictions for ALL six variants before touching loom. Then verify each:
    1. `compare_exchange(false, true, Release, Relaxed)` for lock; `store(false, Release)` for unlock.
    2. `compare_exchange(false, true, Relaxed, Relaxed)` + `store(false, Release)`.
    3. `compare_exchange(false, true, Acquire, Relaxed)` + `store(false, Relaxed)`.
    4. `compare_exchange(false, true, AcqRel, Acquire)` + `store(false, Release)` — does the extra Release
       on the CAS-success path do anything beyond what Acquire-success does?
    5. `compare_exchange(false, true, SeqCst, SeqCst)` + `store(false, SeqCst)` — overkill how exactly?
    6. Replace the CAS-spin with `swap(true, AcqRel)` (test-and-set style) — is the lost-update bug different?
  Constraints: no peek at Mara Bos Ch 4 or `parking_lot` source until COMPARE.
  - **Spec**: write predictions for all 6 in `notes/08_drill7.md` BEFORE running anything. For each variant
    state: which Ordering rule it violates, the specific lost/duplicate write a 2-thread schedule could
    produce, and your confidence (0-10). Low-confidence predictions are the ones you'll learn the most from.
  - **Build / Loom**: implement each variant in its own `#[cfg(loom_v1)]` etc. gate. Run loom on each. Record
    actual outcome vs your prediction. **The goal is to be wrong sometimes and understand WHY.**
  - **Compare**: open `parking_lot::raw_mutex::lock_unchecked` source. Their CAS uses what ordering and why
    do they ALSO have an explicit fence in one branch? Note: parking_lot has a separate "park on contention"
    path; you're only comparing the fast path.
  - **Symptom card**: which prediction were you most wrong about? That's the rule you didn't actually have.
    Phrase it; reapplies at: W11 MPMC, W42 LockManager, W74 raft.
  - **Numbers**: predict per-variant Ordering cost on aarch64 (where x86 hides everything via TSO). Each
    Acquire = ~3 ns LDAR, each Release = ~3 ns STLR, each SeqCst load/store ≈ ~5-7 ns. Predict the
    aggregate cost of each of your 6 variants under 2-thread lock/unlock at 1M iterations. Within 30%.
  - **5-year failure mode**: a newer ISA with stronger sequenced-Acquire-Release pairing (e.g., RISC-V
    Ztso extension) could make some of your "wrong" variants accidentally correct. Trigger: cross-arch
    deployment. Fix: never rely on stronger-than-spec hardware behavior; the spec is the contract.
  - **Production handoff**: prediction-before-measurement is the skill every production crate's hardened
    acceptance will silently test — at W11 you'll predict throughput at N=16 before bench, at W26 you'll
    predict fsync amplification, at W74 you'll predict p99.9. Measurement-without-prediction = pattern-
    matching, not engineering.


**Tuesday — First Foundry PR**

- [ ] Implement + submit. (~120 min)
- [ ] Commit + log (~10 min)

**Wednesday — revm overview (read with `exec-vm` Phase 4 in mind) + concurrency drill #8**

- [ ] **Concurrency drill #8 of 28 — Adaptive `Backoff` 3-stage ladder (design + measure)** (solo, ~5 hrs).
  W4's `Backoff` you implemented mechanically. Now derive its three stages from first principles: why three,
  why those thresholds, what each costs in cycles.
  Build `crates/concurrent/src/adaptive_backoff.rs` — `Backoff { step: Cell<u32> }` with `spin()` that
  escalates through `core::hint::spin_loop()` → `thread::yield_now()` → `thread::park()` with explicit
  step transitions. Measure each stage's cycle cost.
  Constraints: no peek at `crossbeam_utils::Backoff` source, no peek at LMAX Disruptor's WaitStrategy
  hierarchy, no peek at the seastar `BackoffIdleStrategy` until COMPARE.
  - **Spec**: write predictions for stage transition thresholds in `notes/08_drill8.md`. After how many spins
    does context-switch cost (~10 µs) become cheaper than continued spinning? After how many yields does
    park (~300 ns wake + indefinite sleep) become better? Justify with the latency numbers from the
    /jeff-dean / /hft-review references.
  - **Build**: implement the 3-stage ladder. Each stage emits a tracing event with current `step`. Build
    a microbenchmark: 4 producer threads contending on a single AtomicU64 fetch_add, with one consumer
    pulling values and waiting on a flag — measure throughput at each ladder stage by deliberately fixing
    `step` to spin-only, yield-only, park-only, then your adaptive choice.
  - **Loom**: not the primary tool here — Backoff isn't a correctness primitive, it's a perf primitive.
    Loom-verify only that park is woken correctly by unpark. The bulk of this drill is criterion + perf
    profile reading.
  - **Compare**: open `crossbeam_utils::Backoff::spin` source. Their step thresholds are 4, 10, 30. Why
    those numbers? What workload characterization led to them? Compare to LMAX's SleepingWaitStrategy
    documentation.
  - **Symptom card**: name the failure mode at the wrong threshold ("park-too-soon kills latency under
    burst; spin-too-long pegs CPU at idle"); rule (the right threshold depends on offered-load distribution
    not on theoretical wake-up cost); reapplies at: W4 Backoff (the production version), W11 MPMC retry,
    W42 LockManager waiter wait.
  - **Numbers (perf-bearing)**: predict from /hft-review §1 — spin = 1 cycle/iter (~0.5 ns), `yield_now` =
    1-10 µs context switch, `park` = ~300 ns wake + indefinite sleep. Predict the crossover N (producer
    count) where each stage stops paying. Predict CPU utilization at idle for each fixed-stage strategy.
  - **Scale curve (Gap #2)**: measure at N=1,2,4,8,16 producers contending. Document throughput AND CPU
    utilization at each. The cliff is where adaptive wins both metrics — that's your useful range.
  - **5-year failure mode**: when core count grows past 64-128, park wake latency rises (cross-NUMA wake);
    spin becomes relatively better. Trigger: deployment on 128-core ARM Neoverse. Fix: per-NUMA wake
    targeting.
  - **Production handoff**: this Backoff lands at W11 MPMC try_recv retry, W42 LockManager waiter wait,
    W74 matching-engine apply-loop idle. W11's hardened acceptance silently requires this primitive's
    adaptive ladder — naive busy-spin will fail the CPU-utilization gate.


- [ ] Cross-reference revm-primitives::Database against your eth-storage-cache::StateCache. Adjust StateCache if needed. (~30 min)
- [ ] Commit notes. (~10 min)

**Thursday — revm-interpreter + `Receipt` build**

- [ ] Read revm-primitives, compare with eth-primitives. (~45 min)
- [ ] Read revm-interpreter — opcode dispatch, gas. Trace ADD end-to-end. (~90 min)
- [ ] **Build**: `crates/eth-consensus/src/receipt.rs` — Receipt + ReceiptEnvelope. RLP derive. (~150 min)
  - *Mental model*: a receipt is the *result* of executing one transaction — it records whether the tx
    succeeded, how much cumulative gas the block had burned by the end of this tx, the logs it emitted, and
    the bloom of those logs. Receipts are RLP-encoded into the block's `receiptsRoot`; mismatching one byte
    is a consensus split. `ReceiptEnvelope` adds the EIP-2718 typed-transaction wrapper, exactly like
    `TxEnvelope` for transactions.
  - *Contract*: `struct Receipt { status: Eip658Value, cumulative_gas_used: u64, logs: Vec<Log>, bloom: Bloom }`
    deriving `RlpEncodable`/`RlpDecodable` (W5 Friday). `Eip658Value` is an enum — `Eip658(bool)` post-Byzantium
    or `PostState(B256)` pre-Byzantium. `enum ReceiptEnvelope { Legacy(Receipt), Eip2930(Receipt),
    Eip1559(Receipt), Eip4844(Receipt) }` with `encode_2718`/`decode_2718` that prepend/strip the 1-byte type
    id for non-legacy variants. Mirror `alloy-consensus::Receipt` + `ReceiptEnvelope` field-for-field.
  - *encode, roughly*: (1) compute the logs bloom (W34 / Friday's `log.rs` — for the receipt it is the OR of
    every log's bloom); (2) RLP a 4-element list `[status_or_root, cumulative_gas_used, bloom, logs]`; (3) for
    a typed envelope, prepend the EIP-2718 type byte *outside* the RLP list. Step 3's framing (typed byte is
    not part of the list payload, but `receiptsRoot` hashes the type byte + list together) is the real exercise.
  - **Expect to hit #1 — cumulative vs per-tx gas**: storing this tx's own `gas_used` in the
    `cumulative_gas_used` field. Symptom: `receiptsRoot` matches for the first tx in a block and diverges for
    every later one; per-receipt `gas_used` (a derived value) comes out wrong.
    - Fix: the field is the *running block total* through this tx; per-tx `gas_used` = this receipt's
      cumulative minus the previous receipt's cumulative (the first receipt's gas_used equals its cumulative).
    - Muscle: a receipt stores a block-cumulative counter, not a per-tx delta. Reapplies at: W13 block
      validation, W49 revm execution loop, W77 STF validation.
  - **Expect to hit #2 — status-vs-state-root field is fork-dependent**: hard-coding the first field as a
    `bool` status. Symptom: pre-Byzantium receipts (the field is a 32-byte post-state root, EIP-658 changed
    it to a 0/1 status) fail to decode, or decode a 32-byte root as a giant integer.
    - Fix: model the field as `Eip658Value` — RLP-decode by length: a 32-byte string is a `PostState` root,
      a 0/1 scalar is an `Eip658` status; encode each back to its original shape.
    - Muscle: a consensus field whose *type* changed at a fork must be an enum, decoded by inspecting the
      bytes. Reapplies at: W13 historical block replay, W7 EIP-7702 envelope.
  - **Expect to hit #3 — typed receipt: type byte inside vs outside the RLP list**: encoding an EIP-1559
    receipt as a 5-element list `[type, status, gas, bloom, logs]`, or hashing the bloom of one log instead
    of the OR of all logs. Symptom: `receiptsRoot` mismatch only for blocks containing typed txs; legacy-only
    blocks pass.
    - Fix: typed envelope = `type_byte ++ rlp([status, gas, bloom, logs])` — the type byte is concatenated
      *before* the 4-element list, never a list element; the receipt's `bloom` is `logs.iter().fold(...)`.
    - Muscle: EIP-2718 framing prepends a raw type byte outside the RLP payload — same shape as `TxEnvelope`.
      Reapplies at: W9 TxEnvelope encode, W13 receipts trie, W15 EIP-7685 requests.
- [ ] Commit + log (~10 min)

**Friday — `Log` + `Bloom`**

- [ ] Walk evm.codes — the top 20 opcodes — as hands-on prep for W009's interpreter. (~30 min)
- [ ] **Build**: `crates/eth-consensus/src/log.rs` — Log + bloom_filter(logs). Note: the Bloom filter here is a
  fixed-size 2048-bit Ethereum bloom. The general `bloom` crate (classic + counting + scalable) ships W34. (~150 min)
  - *Mental model*: a `Log` is one `LOG0..LOG4` event a contract emitted — an emitting `address`, up to 4
    indexed `topics`, and opaque `data`. The Ethereum bloom is a *fixed* 2048-bit (256-byte) filter that lets
    a light client ask "could this block contain a log touching address/topic X?" without downloading every
    log. It is not a tunable bloom — size, hash, and bit count are all fixed by the consensus spec.
  - *Contract*: `struct Log { address: Address, topics: Vec<B256>, data: Bytes }` deriving
    `RlpEncodable`/`RlpDecodable`. `struct Bloom([u8; 256])`. `Bloom::accrue(&mut self, input: &[u8])` ORs the
    3 bits for one item in; `Log::bloom(&self) -> Bloom` accrues the address + every topic; a free
    `logs_bloom(logs: &[Log]) -> Bloom` ORs every log's bloom together. Mirror `alloy-primitives::Bloom`.
  - *accrue, roughly*: (1) `keccak256(input)` → 32-byte hash; (2) take the first three big-endian *byte-pairs*
    of that hash (bytes 0-1, 2-3, 4-5); (3) for each pair, mask to its low 11 bits → a bit index in `0..2048`;
    (4) set that bit in the 256-byte array. Step 3 (correct 11-bit extraction *and* correct big-endian
    byte/bit addressing into the array) is the real exercise.
  - **Expect to hit #1 — bit-index extraction off by the wrong 11 bits**: taking the *high* 11 bits of each
    16-bit pair, or masking 12 bits, or using one byte instead of a pair. Symptom: your bloom is a valid
    256-byte blob but never matches Geth's `logsBloom`; `eth_getLogs`-style membership tests miss real logs.
    - Fix: index = `u16::from_be_bytes([h[2i], h[2i+1]]) & 0x07FF` — exactly the low 11 bits (`2^11 = 2048`).
    - Muscle: the Ethereum bloom uses the low 11 bits of each of the first three keccak byte-pairs — no other
      slice. Reapplies at: W13 block bloom validation, W34 `bloom` crate, W39 lsm-core SSTable bloom.
  - **Expect to hit #2 — big-endian bit ordering inside the byte array**: addressing bit `n` as
    `bytes[n/8]` bit `n%8` little-endian, i.e. bit 0 = LSB of byte 0. Symptom: the *right number* of bits are
    set but in mirrored positions; your `logsBloom` is a byte-permutation of Geth's and never matches.
    - Fix: Ethereum numbers bits MSB-first from the *end* — set `bytes[255 - n/8] |= 1 << (n % 8)` (or the
      equivalent `bytes[256 - 1 - n/8]`); verify against a known block's `logsBloom` hex.
    - Muscle: the bloom's bit order is big-endian over the whole 2048-bit field, not per-byte little-endian.
      Reapplies at: W13 bloom cross-check, W34 `bloom` crate bit addressing.
  - **Expect to hit #3 — block bloom is an OR, never a re-hash**: computing the block `logsBloom` by hashing
    all logs' bytes together, or by `accrue`-ing receipts instead of bitwise-ORing their blooms. Symptom:
    block-header `logsBloom` mismatch; the header hash diverges → consensus split.
    - Fix: the block bloom = bitwise OR of every receipt's bloom = bitwise OR of every log's bloom; OR is
      associative so receipt-level and log-level aggregation must agree — assert they do.
    - Muscle: blooms compose by bitwise OR only; aggregation never re-hashes. Reapplies at: W13 block bloom,
      W34 scalable bloom union, W39 lsm-core level bloom merge.
  - **Expect to hit #4 — treating a bloom hit as a definite hit**: a downstream filter assumes a set bit
    means the log is present. Symptom: `eth_getLogs`-style queries return false positives — a block whose
    bloom matches but contains no matching log — and any code that skips re-checking the actual logs is wrong.
    - Fix: a bloom match is "maybe present"; always re-scan the block's real logs to confirm. False negatives
      are impossible, false positives are expected.
    - Muscle: a bloom answers "definitely not" or "maybe" — never "definitely yes". Reapplies at: W34 `bloom`
      crate semantics, W39 lsm-core SSTable lookups, W41 LCS read path.
- [ ] Commit notes. (~10 min)

**Saturday — PR cleanup + tag `eth-consensus v0.3.0`**

- [ ] Address all reviewer feedback. (~60 min)
- [ ] Tag `eth-consensus v0.3.0`. (~5 min)
- [ ] Commit + log (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.

- [ ] Target check: 3+ Alloy PRs, 1+ Foundry PR. (~15 min)
