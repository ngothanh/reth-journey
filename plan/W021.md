# Week 21 — `eth-rlp` extension + maintainer engagement

> **Phase**: 2
> **Month**: M6
> **Track**: Reth (primary), Tempo (awareness)
> **Hours target**: 30h/wk (M1-M18)
> **Mirror target**: alloy-rlp + alloy-rlp-derive — trie-friendly node encoding (`alloy_trie::nodes::{BranchNode, ExtensionNode, LeafNode}`, Hex-Prefix compact path from yellow paper §C) + `#[rlp(trailing)]` (mirrors `alloy_rlp::Header` short/long-form split and alloy-consensus `Header` fork-evolution decode).
> **Crate(s) touched**: eth-rlp (v0.1.0 → v0.2.0); eth-rlp-derive (proc-macro, this crate's sibling)
> **Bar**: (b)   ← Layer-0 bootstrap codec; correctness-focused (consensus byte-identity), perf optional.
> **Feeds into**: W22 eth-stage (node-level codec), W23-24 storage-trie scaffold, W31-32 storage-trie persistent MPT + proof verification (consume `encode_compact_path`), W42 txn/block-import header codec (consumes `#[rlp(trailing)]`), W64 RPC `eth_getBlockByNumber`, W66 tempo-tx-envelope (inherits eth-rlp).
> **References** (consult when stuck — NOT scheduled as cover-to-cover tasks): Yellow Paper Appendix C (Hex-Prefix + Trie); EIP-2718 (typed-tx envelope); alloy-rlp `Header`/`Encodable`/`Decodable` rustdoc; alloy-trie `nodes` module source (consult ONLY in Phase 3, not before); `ethereum/tests` TrieTests README.
> **v2 modifications**: none (W15-W28 unchanged from v1)

---

**Monday — `eth-rlp` extension: trie-friendly node encoding**

- [ ] Lookup-only (skip unless Phase-1 design gets stuck): yellow paper §C Hex-Prefix table + alloy-trie `nodes` rustdoc — for the 4-row HP table and the 17-slot branch layout only. (~20 min)

- [ ] **Build**: `crates/eth-rlp/src/trie.rs` — `encode_compact_path`, `encode_leaf_node`, `encode_extension_node`, `encode_branch_node`, and the `ChildRef` enum. (~total 165 min across 3 phases)
  - 🔧 **Why this matters**: MPT nodes have three shapes (branch, extension, leaf), each with its own RLP
    serialization including a 1-byte Hex-Prefix (HP / "compact path") byte that intermixes structural info
    (leaf vs extension; even- vs odd-length nibble path) into the first byte. Mirrors `alloy_trie::nodes`.
    First-attempt impls reliably fail ≥1 test below — the four traps are: (1) the odd-nibble case stuffs the
    leading nibble into the prefix byte while the even case pads with `0x00` (the parity asymmetry), (2) the
    inline-vs-hash threshold is **strict** `< 32`, (3) the branch list is **17** slots not 16, (4) the
    single-nibble round-trip where the prefix byte IS the whole payload. The debug session walking these IS the
    lesson. Centralizing here means eth-trie, storage-trie (W23-24, W31-32) and mini-db (W95) all route through
    one HP table — a parity bug fixed once is fixed everywhere. Downstream: W22 eth-stage, W31-32 persistent
    MPT + proof verifier, W66 tempo-tx-envelope.

  #### Phase 1 — Problem, prediction, design (~45 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in
    `crates/eth-rlp/notes/21_trie_rlp_prediction.md`. Compare to reality after tests run:
    - P1. The exact import line you'd reach for the RLP list header (predict it before checking):
      `use alloy_rlp::Header;` — yes/no, and what method emits the list prefix?
    - P2. Predict the encoded bytes for `encode_compact_path([3, 7, 5], is_leaf=false)` — write the 2 bytes.
    - P3. Predict the byte length of `encode_branch_node([all None; 16], None)` decoded element COUNT.
    - P4. Predict: for a child whose full RLP encoding is exactly 32 bytes, does the branch inline it or hash it?
    - P5. **List every Phase-2 test you predict your first attempt will FAIL** (name them). The most common
      miss is `compact_path_odd_*` and `round_trip` at length 1 — did you predict those?
  - *Mental model*: HP encoding is a 4-bit-packing scheme that smuggles two booleans (is_leaf, is_odd) into the
    top nibble of byte 0, then packs the nibble path two-per-byte. It exists because RLP has no native nibble
    type, and the trie must distinguish leaf from extension nodes that are otherwise structurally identical.
  - *Contract* (signature-level — NEVER the body):
    ```rust
    pub enum ChildRef { Inline(Bytes), Hash(B256) }   // Inline iff child RLP len < 32, strictly
    pub fn encode_compact_path(nibbles: &[u8], is_leaf: bool, out: &mut dyn BufMut);
    pub fn encode_leaf_node(path: &[u8], value: &[u8], out: &mut dyn BufMut);
    pub fn encode_extension_node(path: &[u8], child: &ChildRef, out: &mut dyn BufMut);
    pub fn encode_branch_node(children: &[Option<ChildRef>; 16], value: Option<&[u8]>, out: &mut dyn BufMut);
    ```
  - *Back-of-envelope* (bar (b) — informational, not gated): the HP encode is pure byte-shuffling, ~one pass
    over `nibbles.len()/2` bytes, no alloc if `out` is pre-sized. The load-bearing cost is NOT speed — it is
    **consensus byte-identity**: a single wrong prefix byte produces a well-formed RLP list whose keccak hash
    diverges from mainnet, and the node is silently rejected by every other client. Cliff: correct vs off-by-one
    parity = byte-identical vs total state-root divergence (a binary correctness cliff, not a perf gradient).
  - **Requirements** R1..R9 — write in `crates/eth-rlp/notes/21_trie_rlp_design.md`:
    - R1. `encode_compact_path(nibbles, is_leaf, out)` writes the HP-prefix byte then packed nibble pairs.
    - R2. HP flag nibble (yellow paper §C): `0x0` = extension/even, `0x1` = extension/odd, `0x2` = leaf/even,
      `0x3` = leaf/odd. The flag occupies the HIGH nibble of byte 0.
    - R3. `encode_leaf_node(path, value, out)` writes a 2-element RLP list `[compact_path, value]`.
    - R4. `encode_extension_node(path, child, out)` writes a 2-element RLP list `[compact_path, child_ref]`.
    - R5. `encode_branch_node(children, value, out)` writes a **17-element** RLP list: 16 child slots + 1 value
      slot. Absent children/value encode as `0x80` (RLP empty string).
    - R6. `ChildRef::Inline` iff the child's full RLP encoding is **strictly** `< 32` bytes; else `::Hash` (keccak).
    - R7. Nibbles within a byte: MSB-first. `(byte >> 4, byte & 0x0F)` is the pair, in that order.
    - R8. Round-trip: `encode(decode(bytes)) == bytes` for every node shape at nibble lengths 0, 1, 2, 31, 32, 33, 64.
    - R9. Pass `ethereum/tests::TrieTests::{trietest, trieanyorder}` byte-identical.
  - **Design walk-through** D1..D6 — answer in the design doc; do NOT open alloy-trie source until Phase 3:
    - D1. Write the HP-prefix-byte formula from `(is_leaf, odd)`. What is byte 0 for `is_leaf=true, [3,7,5]` (odd)?
    - D2. The even case appends `0x00` padding into the low nibble of byte 0 (prefix is the whole first byte); the
      odd case stuffs the FIRST path nibble into the low nibble of byte 0. Why asymmetric? (Hint: nibbles are 4
      bits; an odd count leaves one stray nibble that has nowhere to pair, so it rides in the prefix byte.)
    - D3. Why strictly `< 32` not `<= 32`? (Hint: 32 = keccak output size; at exactly 32 both inline and hash
      occupy 32 bytes — ambiguous, so spec forces hash to break the tie.)
    - **D4. Pre-mortem (load-bearing)** — write the EXACT bad trace for the even/odd off-by-one that yields
      INTERMITTENT state-root divergence:
      ```
      path [3,7,5] (odd, len 3), is_leaf=true
      naive: push prefix 0x30, then pack pairs starting at nibble[0]:
             0x30, (3<<4|7)=0x37, (5<<4|0)=0x50   → 3 bytes [0x30, 0x37, 0x50]
      correct: stuff nibble[0] into prefix low nibble: 0x30|0x3 = 0x33,
             then pack from nibble[1]: (7<<4|5)=0x75 → 2 bytes [0x33, 0x75]
      blocks with EVEN-length paths still hash correctly → "works on my testnet"
      blocks with ODD-length paths diverge → fails on mainnet at random heights
      ```
      Mainnet has both parities in ~equal proportion, so the bug looks "intermittent" — that's why it survives a
      naive test suite and detonates in production.
    - **D5. Anti-shortcut audit** — name the obvious reach and why it's wrong HERE:
      - "Hand-roll the RLP list header (`if len<=55 {0xc0+len} else {...}`)" → wrong: the near-55-byte boundary
        and the long-form length-of-length prefix are exactly where hand-rolls drift from alloy. Use `Header::encode`.
      - "Compare `child.len() <= 32`" → wrong: strict `<` (D3). The `<=` reach passes 31-byte tests, fails at 32.
      - "Treat the odd leading nibble as a normal data nibble" → wrong: it must ride in the prefix byte (D2).
    - D6. RLP list header: use `alloy_rlp::Header { list: true, payload_length }.encode(out)`. What breaks if you
      hand-roll it near the 55-byte boundary? (Answer in doc.)
  - **Output**: `crates/eth-rlp/notes/21_trie_rlp_design.md` with R1–R9, D1–D6, the 4-row HP table, and the
    bad-interleaving sketch from D4.

  #### Phase 2 — Write the failing tests (~35 min)

  All tests live in `crates/eth-rlp/src/trie.rs` `#[cfg(test)] mod tests`. Each is engineered so at least one
  common naive impl FAILS it.

  - [ ] **(NEWBIE) `compact_path_even_extension`** — `encode_compact_path(&[1, 2], false)` → `[0x00, 0x12]`.
    ```rust
    let mut out = Vec::new();
    encode_compact_path(&[1, 2], false, &mut out);
    assert_eq!(out, [0x00, 0x12], "even-extension prefix must be 0x00 then packed 0x12");
    ```
    Exercises R2.
  - [ ] **(SENIOR) `compact_path_odd_extension`** — `encode_compact_path(&[3, 7, 5], false)` → `[0x13, 0x75]`.
    ```rust
    let mut out = Vec::new();
    encode_compact_path(&[3, 7, 5], false, &mut out);
    assert_eq!(out, [0x13, 0x75],
        "odd case must stuff first nibble 3 into prefix low nibble (0x10|0x3=0x13), then pack from nibble[1]");
    ```
    Traps the parity asymmetry (D2). Exercises R2, R7.
  - [ ] **(NEWBIE) `compact_path_even_leaf`** — `encode_compact_path(&[1, 2], true)` → `[0x20, 0x12]`. Exercises R2.
  - [ ] **(SENIOR) `compact_path_odd_leaf`** — `encode_compact_path(&[3, 7, 5], true)` → `[0x33, 0x75]`. Exercises R2.
  - [ ] **(NEWBIE) `leaf_node_is_2_element_list`** — `encode_leaf_node(&[1, 2], b"value", out)` decodes to a
    2-element RLP list. Exercises R3.
  - [ ] **(SENIOR) `branch_node_is_17_element_list`** — `encode_branch_node(&[None; 16], None, out)`; decode
    yields exactly **17** elements, all `0x80`.
    ```rust
    let mut out = Vec::new();
    encode_branch_node(&core::array::from_fn(|_| None), None, &mut out);
    let header = Header::decode(&mut &out[..]).unwrap();
    let count = count_list_elements(&out);   // helper that walks the payload
    assert_eq!(count, 17, "branch must always emit 16 children + 1 value slot");
    ```
    Traps the 16-vs-17 miss. Exercises R5.
  - [ ] **(SENIOR) `embedded_child_under_32_bytes_inlined`** — child with full RLP = 31 bytes; assert branch's
    encoding contains the child's RAW bytes (not a hash). Exercises R6 strict-`<` (the 31 side).
  - [ ] **(EXPERT) `embedded_child_exactly_32_bytes_hashed`** — child with full RLP == 32 bytes; assert branch
    contains `keccak256(child)`, NOT raw. Traps `<=` vs `<`. Exercises R6 boundary.
  - [ ] **(SENIOR) `round_trip_every_nibble_length`** — property test over lengths `[0, 1, 2, 31, 32, 33, 64]`
    × {leaf, extension} × random nibble values; `encode → decode → encode` byte-identical. The length-1 case is
    the trap (single nibble lives entirely in the prefix byte). Exercises R8.
  - [ ] **(EXPERT) `trie_tests_byte_identical`** — load `ethereum/tests::TrieTests::{trietest, trieanyorder}`;
    encode every node; bytes match the fixture's expected root. Exercises R9.
  - [ ] **(NEGATIVE-CONTROL) `fixture_loader_actually_loads_nodes`** — proves the fixture harness isn't a no-op:
    ```rust
    let cases = load_trie_test_cases();
    assert!(cases.len() >= 5, "fixture loader returned {} cases — harness is reading nothing", cases.len());
    assert!(cases.iter().any(|c| c.expected_root != B256::ZERO),
        "every expected_root is zero — fixtures parsed as empty, test_byte_identical would pass vacuously");
    ```
    Without this, `trie_tests_byte_identical` could "pass" by iterating an empty case list. Test-the-test.

  #### Phase 3 — Make the tests pass, one failure at a time (~75 min)

  Ordered newbie→expert — the ladder you actually walk.

  - **(NEWBIE) When `branch_node_is_17_element_list` decodes to 16 elements:**
    - Symptom: branch RLP decodes as a 16-element list; expected 17.
    - Why: encoder omitted the 17th "value at this branch" slot (almost always empty in modern Ethereum, but
      spec-required by yellow paper §C).
    - Fix: always emit the 17th slot, defaulting to `0x80` (RLP empty) when no value present.
    - Muscle: every branch node has 17 RLP slots even when the 17th is always empty in practice; omitting it is a
      non-canonical encoding other clients reject. Reapplies at: W23-24 storage-trie node codec, W31 persistent
      MPT, W32 proof verification.
  - **(SENIOR) When `compact_path_odd_extension` produces `[0x10, 0x37, 0x50]` (3 bytes, wrong):**
    - Symptom: encoder emits prefix `0x10` then packs from nibble[0], giving 3 bytes; expected 2 bytes `[0x13, 0x75]`.
    - Why: the first nibble (`3`) must be stuffed into the LOW 4 bits of the prefix byte, not treated as a normal
      data nibble. Prefix becomes `(flag<<4) | nibbles[0]` = `0x10 | 0x03 = 0x13`.
    - Fix: `if odd { out.push((flag << 4) | nibbles[0]); pack(&nibbles[1..], out) } else { out.push(flag << 4); pack(nibbles, out) }`
      — note the slice from index 1 in the odd branch.
    - Cost avoided: silent INTERMITTENT state-root divergence (passes even-path testnet, fails odd-path mainnet) —
      the single hardest trie bug to diagnose because it correlates with no obvious input property.
    - Muscle: HP's odd case stuffs the leading nibble into the prefix byte; even case pads `0x00`. The asymmetry
      is THE bug to defend against. Reapplies at: W31 persistent MPT key encoding, W32 proof verifier path
      assembly, W95 mini-db trie index.
  - **(SENIOR) When `round_trip_every_nibble_length` fails on length 1 (single nibble):**
    - Symptom: `[5]` → encode `[0x35]` (correct prefix) → decode → `[]` (empty); round-trip is `[5]→[0x35]→[]`.
    - Why: decoder reads the prefix, sees odd-leaf, then expects further data bytes — finds none, returns empty.
      The case "the low nibble of the prefix IS the only nibble" is unhandled.
    - Fix: in the decoder, after extracting prefix flag, if `odd && no_remaining_bytes` then the single nibble is
      `prefix & 0x0F` — return `[prefix & 0x0F]`. Add explicit tests at lengths 1, 3, 5, 63.
    - Muscle: HP decode must handle the single-nibble case where the prefix byte is the entire payload. Reapplies
      at: W31 persistent MPT key decode, W32 proof verifier.
  - **(EXPERT) When `embedded_child_exactly_32_bytes_hashed` reports a 32-byte raw inline instead of a hash:**
    - Symptom: branch at the boundary (child encoded len = 32) contains the child's raw bytes; expected the keccak hash.
    - Why: encoder used `<=` instead of `<`. The two interpretations agree for ALL sizes except exactly 32.
    - Fix: `if child.encoded_len() < 32 { ChildRef::Inline(bytes) } else { ChildRef::Hash(keccak256(bytes)) }`.
      Keep tests at both 31 and 32 to nail the boundary.
    - Cost avoided: consensus break at exactly the 32-byte child case — rare in random data, common in real tries
      (many node encodings cluster near 32 bytes), so it survives unit tests and fails on mainnet replay.
    - Muscle: the inline-vs-hash threshold is strict-`<`-32; off-by-one breaks consensus only at the boundary.
      Reapplies at: W23-24 storage-trie embedding, W31 persistent MPT, W32 proof verification.
  - **(EXPERT) When `fixture_loader_actually_loads_nodes` fails (zero cases / all-zero roots):**
    - Symptom: loader returns `len()==0` or every `expected_root == B256::ZERO`.
    - Why: fixture path resolution or JSON field name wrong; the harness silently parsed nothing, so the "real"
      test passes vacuously.
    - Fix: resolve the `ethereum/tests` submodule path at compile time (`env!("CARGO_MANIFEST_DIR")` + relative),
      assert the parsed case count and a non-zero root.
    - Muscle: a fixture/bench/loom test that can pass on empty input proves nothing — always pair it with a
      test-the-test negative control. Reapplies at: every fixture-driven suite (W32 proof fixtures, W42 block
      fixtures), and every criterion/loom harness in bar (c) weeks (W26 wal, W58 matching-engine).

  #### 5-year failure mode
  Write in `crates/eth-rlp/notes/21_trie_rlp_followup.md` after green: when Verkle tries replace MPT, HP
  encoding becomes obsolete — Verkle uses fixed-width 32-byte keys with NO odd/even parity flags. TRIGGER:
  Verkle activation on a target chain (measurable: a fork block where header carries a Verkle state root).
  Migration: gate HP encoders behind `cfg(feature = "mpt")`, add a parallel `verkle::encode_node` module; the
  branch/leaf/extension split collapses to a single fixed-arity Verkle node.

- [ ] **Refactor** (genuine delta, NOT a re-build): `crates/eth-rlp/src/eips.rs` — route the existing
  `EipTransactionRlp` typed-envelope helper (built W16) through `alloy_rlp::Header` instead of its hand-rolled
  list-header arithmetic, so the EIP-2718 type-byte + payload share ONE header path with `trie.rs`. (~50 min)
  - 🔧 **Why this matters**: W16's `EipTransactionRlp` hand-rolled the RLP list header; `trie.rs` (today) uses
    `Header::encode`. Two header code paths drift. This is a scope-limited consolidation, NOT re-encoding the
    transaction. The delta is exactly: delete the hand-rolled `if len<=55` arithmetic, call `Header::encode`.
  - 🎯 **Hardened acceptance**: a regression test `eip2718_envelope_byte_identical_after_refactor` snapshots the
    pre-refactor output of one EIP-1559 and one EIP-4844 tx, then asserts the post-refactor output is byte-
    identical (`assert_eq!` on the exact `Vec<u8>`). A near-55-byte-payload tx is included to exercise the
    short/long-form boundary that hand-rolls get wrong.
  - *Mental model*: one header encoder, one source of truth — the W16 hand-roll was acceptable in isolation but
    becomes a divergence risk now that `trie.rs` standardizes on `Header`.
  - **Output**: `eips.rs` no longer contains manual `0xc0 + len` arithmetic (`grep -n '0xc0' crates/eth-rlp/src/eips.rs`
    returns nothing); `cargo test -p eth-rlp eip2718_envelope_byte_identical_after_refactor` green.

- [ ] Commit + log (~10 min)

**Tuesday — `eth-rlp-derive`: `#[rlp(trailing)]` for fork-evolution decoding**

- [ ] Lookup-only (skip unless stuck in Phase-1): alloy-rlp-derive `Field::attrs` handling + alloy-consensus
  `Header` fork-field list — for the `syn` attribute-parsing pattern and the field ordering only. (~30 min)

- [ ] **Extend** (genuine delta to the W5 derive — adds ONE attribute, does not re-build the derive):
  `crates/eth-rlp/eth-rlp-derive/src/lib.rs` — support `#[rlp(trailing)]` on struct fields. (~total 145 min across 3 phases)
  - 🔧 **Why this matters**: hard forks add fields to consensus structs (most often `Header`): EIP-1559 added
    `base_fee`, EIP-4895 `withdrawals_root`, EIP-4844 `blob_gas_used`/`excess_blob_gas`. Old headers must decode
    without these (the RLP list is SHORTER, not null-padded); new ones must include them. `#[rlp(trailing)]`
    marks fields whose absence on decode is `None`/`Default`. Without it, every Header schema change forks the
    codec. Mirrors alloy-consensus `Header` decode. First-attempt impls reliably fail ≥1 test: the three traps
    are (1) decoder asserts EXACT list length instead of a RANGE, (2) encoder silently corrupts on "None before
    Some" (a wire hole), (3) the compile-time guard on the field type is aspirational (absent codegen). The
    debug session IS the lesson. Downstream: W42 block-import header codec, W64 RPC `eth_getBlockByNumber`.

  #### Phase 1 — Problem, prediction, design (~30 min, no code yet)

  - 🎯 **Pre-prediction prompt (BEFORE Phase 2)** — write in `crates/eth-rlp/notes/21_rlp_trailing_prediction.md`:
    - P1. The exact `syn` API you'd reach for to read field attributes (predict before checking):
      `field.attrs.iter()...` + `Attribute::path()` / `parse_nested_meta`? Write your guess.
    - P2. Predict: a frontier header (no trailings) has how many fewer RLP list elements than a Cancun header?
    - P3. Predict the encoder's behavior for `{ base_fee: None, withdrawals_root: Some(_) }` — what SHOULD it do?
    - P4. Predict whether `#[rlp(trailing)] foo: NonDefaultType` should compile. (No — predict the error string.)
    - P5. **List every Phase-2 test you predict your first attempt will FAIL.** The usual misses are
      `decoder_missing_trailing_yields_none` (exact-length assert) and `compile_fail_*` (no guard codegen).
  - *Mental model*: `#[rlp(trailing)]` makes the decoder accept a list-length RANGE and the encoder truncate from
    the end at the last `Some`. It exists because RLP positional optionals can only be omitted from the tail —
    there is no "this slot is absent" marker mid-list.
  - *Contract* (codegen-level, not the macro body):
    ```rust
    // derive splits fields into (mandatory: Vec<Field>, trailing: Vec<Field>) at parse time.
    // Decodable: reads mandatory_count items, then min(remaining, trailing_count) trailings, defaults the rest.
    // Encodable: encodes mandatory, then trailings up to the last non-None/non-default, panics on None-before-Some.
    // Compile guard: each trailing field T must be Option<_> or T: Default.
    ```
  - *Back-of-envelope* (bar (b) — informational): codegen cost is zero at runtime; the decode is one length
    subtraction + a bounded loop. Load-bearing cost is **fork-compatibility correctness**: a wrong length-bound
    means a pre-London header (14 fields) fails to decode against a 20-field struct — every historical block
    becomes unreadable. Binary correctness cliff, not a perf gradient.
  - **Requirements** R1..R8 — write in `crates/eth-rlp/notes/21_rlp_trailing_design.md`:
    - R1. `#[rlp(trailing)]` recognized on struct fields by both `RlpEncodable` and `RlpDecodable` derives.
    - R2. Decoder reads `N = mandatory_count + observed_trailing_count` items where
      `mandatory_count <= list.len() <= mandatory_count + trailing_count`. Missing trailings → `None`/`Default`.
    - R3. Encoder encodes mandatory fields, then trailing fields in declaration order, emitting NO bytes past the
      last `Some`/non-default trailing.
    - R4. Encoder PANICS at encode time on any `None` (or default) trailing FOLLOWED by a `Some`/non-default —
      that combination is wire-ambiguous (a "hole").
    - R5. Compile-time error if a `#[rlp(trailing)]` field is neither `Option<T>` nor `T: Default`.
    - R6. Round-trip: pre-London header (no trailings) byte-identical; Cancun header (all trailings) byte-identical.
    - R7. Decoding a 1k-block mainnet sample (genesis → current, across forks) never panics.
    - R8. Pass alloy's `header_rlp_roundtrip` fixtures (port them).
  - **Design walk-through** D1..D6 — answer in the design doc; do NOT open alloy-rlp-derive source until Phase 3:
    - D1. Parse-time: how do you partition fields into `(mandatory, trailing)`? Which `syn` API? Must every
      trailing field follow every mandatory field, or can they interleave? (Answer: trailings must be a
      contiguous suffix — error otherwise.)
    - D2. Decoder codegen: what happens when the list has MORE items than total fields? (Answer: error
      `TooManyItems` — never silently accept.)
    - D3. Encoder "skip from end": walk trailings in reverse, find the last `Some`/non-default index, encode
      `0..=that_index`. Write the codegen pseudocode in the doc.
    - **D4. Pre-mortem (load-bearing)** — write the EXACT bad trace for the exact-length-assert bug:
      ```
      Header struct: 15 mandatory + 5 trailing = 20 declared fields
      frontier block on wire: RLP list with 15 elements (no trailings)
      naive decoder codegen: assert!(list.len() == 20)  → panic "expected 20 items, got 15"
      correct: observed_trailings = 15 - 15 = 0; read 15 mandatory, default 5 trailings
      result of bug: EVERY pre-London block fails to decode → node cannot sync history
      ```
    - **D5. Anti-shortcut audit** — name the obvious reach and why it's wrong HERE:
      - "Decode all N declared fields, treat missing as decode error" → wrong: pre-fork lists are legitimately
        short; the length is a RANGE (R2).
      - "Encode every field including trailing Nones as `0x80`" → wrong: a `None` slot followed by a real `Some`
        is ambiguous; and trailing Nones must be truncated, not emitted (R3, R4).
      - "Trust the field type is `Option` without a guard" → wrong: a `#[rlp(trailing)]` on a non-Option,
        non-Default type compiles to a decoder that can't default — must be a compile error (R5).
    - D6. R4 "None-before-Some": should this be `panic!` or `Result::Err`? (Answer in doc: encode-time panic is
      acceptable because the combination is statically unreachable in correct code; document the exact message.)
  - **Output**: `crates/eth-rlp/notes/21_rlp_trailing_design.md` with R1–R8, D1–D6, the bad-trace from D4, and the
    encoder codegen sketch.

  #### Phase 2 — Write the failing tests (~25 min)

  Tests in `crates/eth-rlp/eth-rlp-derive/tests/trailing.rs` (runtime) + `tests/ui/` (trybuild).

  - [ ] **(NEWBIE) `frontier_header_no_trailings_round_trip`** — header with all trailing fields `None`; encode
    then decode produces an equivalent struct. Exercises R2, R3, R6.
  - [ ] **(NEWBIE) `cancun_header_all_trailings_round_trip`** — all trailings `Some`; encode then decode
    byte-identical. Exercises R6.
  - [ ] **(SENIOR) `decoder_missing_trailing_yields_none`** — encode a header with only `base_fee` Some, others
    None; decode; assert `withdrawals_root` is `None` and decode did NOT panic on the short list.
    ```rust
    let h = Header { base_fee: Some(7), withdrawals_root: None, blob_gas_used: None, excess_blob_gas: None, ..mandatory() };
    let mut buf = Vec::new(); h.encode(&mut buf);
    let got = Header::decode(&mut &buf[..]).expect("short list must decode");
    assert_eq!(got.withdrawals_root, None);
    ```
    Traps the exact-length-assert bug. Exercises R2.
  - [ ] **(SENIOR) `encoder_rejects_none_before_some`** — `Header { base_fee: None, withdrawals_root: Some(b256) }`;
    encode PANICS with a clear message.
    ```rust
    let r = std::panic::catch_unwind(|| { let mut b = Vec::new(); bad_header().encode(&mut b); });
    assert!(r.is_err(), "encoder must panic on None-before-Some, not silently corrupt");
    ```
    Traps the silent-corruption path. Exercises R4.
  - [ ] **(SENIOR) `encoder_omits_trailing_nones_from_end`** — header with `excess_blob_gas: None,
    blob_gas_used: None`; encoded list has exactly N-2 elements (the two tail Nones omitted). Exercises R3.
  - [ ] **(EXPERT) `compile_fail_trailing_on_non_option_no_default`** — trybuild UI test on
    `#[rlp(trailing)] foo: NonDefaultType`; build FAILS with the EXACT string `trailing requires Option or Default`.
    Traps the missing compile-time guard. Exercises R5.
  - [ ] **(EXPERT) `mainnet_1k_block_sample_decode_no_panic`** — 1k headers across forks; decode each; zero
    panics; assert at least one frontier (short) and one Cancun (full) header are present in the sample.
    Exercises R7.
  - [ ] **(EXPERT) `alloy_header_rlp_roundtrip_fixtures_pass`** — port alloy's fixture suite; all pass. Exercises R8.
  - [ ] **(NEGATIVE-CONTROL) `trybuild_harness_catches_a_known_bad_case`** — proves the trybuild UI harness is
    wired and actually fails on bad input: a deliberately-broken `tests/ui/known_bad.rs` MUST produce a non-empty
    `.stderr`. Without it, `compile_fail_*` could pass because trybuild silently found no test files.
    ```rust
    // in the trybuild driver:
    let t = trybuild::TestCases::new();
    t.compile_fail("tests/ui/*.rs");   // assert the dir is non-empty BEFORE running:
    assert!(std::fs::read_dir("tests/ui").unwrap().count() > 0, "no UI tests — compile_fail is vacuous");
    ```
    Test-the-test for the proc-macro guard.

  #### Phase 3 — Make the tests pass, one failure at a time (~45 min)

  Ordered newbie→expert.

  - **(SENIOR) When `decoder_missing_trailing_yields_none` panics with "expected 20 items, got 15":**
    - Symptom: decoder treats trailing fields as mandatory; a 15-element frontier list fails against a 20-field struct.
    - Why: codegen emits `assert!(list.len() == total_field_count)` instead of a range check.
    - Fix: partition fields at compile time; emit
      `let observed = list.len().checked_sub(mandatory_count).filter(|&n| n <= trailing_count).ok_or(TooManyItems)?;`
      read `mandatory_count` items normally, read `observed` trailings, default the remaining `trailing_count - observed`.
    - Muscle: a trailing-aware decoder accepts a length RANGE (mandatory ≤ len ≤ total), not an exact length.
      Reapplies at: W42 block-import header codec, W67 raft AppendEntries with versioned fields, W64 RPC types.
  - **(SENIOR) When `encoder_rejects_none_before_some` does NOT panic and produces ambiguous bytes:**
    - Symptom: encoder emits `base_fee` slot as `0x80` (empty) and `withdrawals_root` as a real B256; decoder
      reads back BOTH as `None` (the empty slot looks like "missing from end"), silently corrupting data.
    - Why: encoder walks fields linearly with no "no holes" check.
    - Fix: before emitting, scan trailings in declaration order; if a `Some`/non-default follows a `None`/default,
      `panic!("#[rlp(trailing)] field {} is Some but preceding {} is None — unrepresentable on wire", a, b)`.
    - Muscle: positional optional wire formats require a "no holes" invariant; the encoder MUST panic on a hole,
      never silently corrupt. Reapplies at: W42 block-import header encoding, W64 RPC response building.
  - **(EXPERT) When `compile_fail_trailing_on_non_option_no_default` does NOT fail to compile:**
    - Symptom: `#[rlp(trailing)] foo: NonDefaultType` compiles; the load-bearing compile-time guard is missing.
    - Why: the derive emits no trait-bound assertion for trailing fields.
    - Fix: for each trailing field, codegen
      `const _: fn() = || { fn assert_impl<T: TrailingCompatible>() {} assert_impl::<#field_type>(); };`
      where `TrailingCompatible` has blanket impls for `Option<T>` and `T: Default`.
    - Muscle: derive macros enforce field-level bounds at the call site via codegen'd `const _: fn() = ...`
      helpers; absent helper = aspirational bound. Reapplies at: every derive needing field-level bounds (W5
      RlpEncodable, W4 SimpleEncode).
  - **(EXPERT) When `trybuild_harness_catches_a_known_bad_case` reports zero UI tests / empty stderr:**
    - Symptom: `tests/ui` is empty or the `.stderr` snapshot is missing, so `compile_fail_*` passes vacuously.
    - Why: trybuild silently succeeds when its glob matches no files, or when `TRYBUILD=overwrite` regenerated
      empty expectations.
    - Fix: assert `read_dir("tests/ui").count() > 0` before running, and commit the `.stderr` snapshot.
    - Muscle: a compile-fail/loom/bench harness that passes on empty input proves nothing — pair it with a
      test-the-test. Reapplies at: W4 derive UI tests, W32 proof fixtures, W26 wal loom harness.

  #### 5-year failure mode
  Write in `crates/eth-rlp/notes/21_rlp_trailing_followup.md` after green: when an EIP REMOVES or reorders a
  field in the MIDDLE of a consensus struct (EIP-7610-shape), the declaration-order trailing model breaks —
  trailings are only valid as a contiguous suffix. TRIGGER: any fork spec that deletes a non-tail header field.
  Migration: add `#[rlp(version = "n")]` per-field for spec-versioned decoding (decode against the schema for the
  block's fork, not a single struct shape).

- [ ] Tag `eth-rlp v0.2.0` (and `eth-rlp-derive` to match). Output: `git tag eth-rlp-v0.2.0`; `cargo test -p eth-rlp -p eth-rlp-derive` green. (~5 min)
- [ ] Commit + log (~10 min)

**Wednesday — Fourth revm PR**

- [ ] Pick + implement one revm issue. Output: a PR opened against `bluealloy/revm` with a green CI run, linked in
  `notes/pr_tracker.md` (PR #, area, reviewer). 🎯 Hardened acceptance: the PR has a test that fails on `main`
  and passes on the branch (not docs-only). (~120 min)
- [ ] Commit + log (~10 min)

**Thursday — Second Foundry PR**

- [ ] Pick + implement one Foundry issue. Output: a PR opened against `foundry-rs/foundry`, CI green, logged in
  `notes/pr_tracker.md`. 🎯 Hardened acceptance: includes a regression test or a reproducible before/after. (~120 min)
- [ ] Commit + log (~10 min)

**Friday — Maintainer engagement**

- [ ] Update `notes/maintainer_tracker.md`: map maintainers per area (alloy-eips: gakonst/yash; revm: rakita;
  reth-trie: rakita/mattsse). Output: the file lists each area, its maintainers, and the 1-line review-style
  signal you've observed from each (so future PRs target the right reviewer). *Mental model*: a maintainer map is
  a routing table — knowing who reviews trie code vs codec code shortens your PR feedback loop, which gates the
  backup-job milestone. (~20 min)
- [ ] Engage thoughtfully in ONE issue discussion (substantive comment, not a +1). Output: link to the comment in
  `notes/maintainer_tracker.md`. 🎯 Hardened acceptance: the comment references a specific code location and
  proposes a concrete option, not a question you could answer yourself. (~30 min)
- [ ] [Tempo] Identify which Tempo maintainers overlap with your Reth tracker; cross-reference gakonst, rakita,
  joshieDo in `notes/maintainer_tracker.md` under a `## Tempo overlap` heading. No outreach yet. Output: the
  overlap section exists. (~15 min)
- [ ] Commit notes (~10 min)

**Saturday — Consolidation**

- [ ] Review all open PRs; close out review comments. Output: every open PR either has all review threads
    resolved or a logged reply in `notes/pr_tracker.md`. 🎯 Hardened acceptance: `gh pr list --author @me`
    shows no PR with an unanswered review comment older than this week. (~30 min)
- [ ] Commit + log (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
