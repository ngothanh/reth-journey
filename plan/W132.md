# Week 132 — Resignation + relocation logistics begin

> **Phase**: 7
> **Month**: M34
> **Track**: (career — destination landing)
> **Hours target**: 40h/wk (M19-M34)
> **Mirror target**: (n/a — career/relocation week; no upstream module)
> **Crate(s) touched**: (none — Reth + Tempo are in maintenance-only mode; no new code this week)
> **Bar**: (n/a)
> **Feeds into**: W133 day-job final-2-weeks + ramp-down (consumes this week's transition doc + successor map); W134 arrival at destination + onboarding (consumes the visa-status record, short-term-rental booking, and live bank account); W135-W144 first month at new firm (consumes the maintenance-cadence commitment as the boundary that protects ramp-up time). README §"Resignation timing" (line ~1164): 30-day notice, don't burn bridges, help with transition.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): destination-geography immigration/visa portals + the destination firm's immigration counsel (Monday visa research — official sources only, lookup-when-stuck); destination-geography banking onboarding requirements (Wednesday — proof-of-address / tax-ID prerequisites); README §"Destination landing in Phase 7" (line ~1047) for the W125→W144 timeline anchors.
> **v2 modifications**: none

This is a career/relocation logistics week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding. Every item below instead carries an explicit artifact file + an Output line that proves it done; the two judgment/decision items (Wednesday insurance-gap decision, Saturday maintenance-cadence decision) carry a hardened-acceptance pass criterion and a mental model of why the item exists now. The week's job: convert the accepted offer (W131) into the irreversible logistics — notice submitted, visa pipeline opened, money + housing reachable on arrival, knowledge handed off — so that W134 arrival is a non-event and the W135+ ramp-up isn't sabotaged by unresolved life admin.

**Monday — Visa paperwork**

- [ ] Artifact: `relocation/visa_pipeline.md`. Open the immigration pipeline with the destination firm's HR / immigration counsel: confirm the exact visa category they sponsor, the documents you must supply (passport scan, degree, employment letter, etc.), who files what, and the dates each step is due. Log every step with {owner, document, due-date, status} and the official-source URL for each timeline number. Begin paperwork — supply whatever is on you today. (~30 min)
  - 🎯 Hardened acceptance: `visa_pipeline.md` lists every pipeline step as `{step, owner (you|firm|counsel), document, due-date, status, source-URL}`; every processing-timeline number carries a source link or an explicit `[UNVERIFIED — confirm with counsel]` tag — no unsourced dates; the critical-path step (the one whose deadline gates W134 arrival) is flagged.
  - *Mental model*: visa processing is the single longest-lead, least-controllable item in the whole relocation — it gates the W134 arrival date, and a missed early document can push the start date by weeks. Opening it on day 1 of W132 (rather than reacting later) buys the slack that turns arrival into a non-event. Recording owner + due-date per step is what lets you chase counsel instead of discovering a stall too late.
  - Output: `relocation/visa_pipeline.md` exists with ≥1 row per pipeline step; `grep -c 'source-URL\|UNVERIFIED' relocation/visa_pipeline.md` accounts for every timeline number; the critical-path step is marked.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Monday visa-pipeline commit.

**Tuesday — Housing search**

- [ ] Artifact: `relocation/housing.md`. Research destination-geography housing. Book (or shortlist with prices + dates) a short-term rental that covers the arrival window; record the permanent-housing decision as explicitly deferred 2-3 months with the trigger date to revisit. Log {neighborhood, monthly cost, lease length, commute/area notes, link} for each candidate, and the one you'll book. (~30 min)
  - Output: `relocation/housing.md` lists ≥2 short-term candidates with cost + dates + link, names the one to book (or marks it booked), and records the permanent-housing deferral with a concrete revisit date; `grep -c '^| \|^- ' relocation/housing.md` ≥ 2.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday housing commit.

**Wednesday — Logistics: shipping, bank account, insurance gap**

- [ ] Artifact: `relocation/banking_and_shipping.md`. Start the destination bank-account setup: list the account prerequisites (proof of address, tax ID, visa status) and which you can satisfy pre-arrival vs. on-arrival, with the order of operations. Add the shipping plan: what ships, what flies with you, rough cost + lead time. (~30 min)
  - Output: `relocation/banking_and_shipping.md` lists bank-account prerequisites each tagged `pre-arrival | on-arrival` with a dependency note, plus a shipping line-item list with cost + lead time; the first actionable bank step is marked as started.
- [ ] Decision: health-insurance gap coverage. In `relocation/insurance_gap.md`, decide how you're covered for the window between leaving the current employer's plan (resignation effective ~W133+30d) and the new plan's start — record the chosen option, its cost, and the exact dates it covers. (~30 min)
  - 🎯 Hardened acceptance: `insurance_gap.md` names the gap window as concrete start/end dates, lists ≥2 coverage options (e.g. travel-medical, marketplace/private bridge, new-employer early-enrollment) with cost, and records the chosen one with zero uncovered days inside the window — if any day is uncovered, that is flagged explicitly as an accepted risk, not left silent.
  - *Mental model*: the dangerous part of an international move isn't the cost — it's the silent uncovered gap between two insurance plans, where one accident becomes a five-figure liability with no recourse. Forcing the window into explicit start/end dates and proving zero uncovered days (or consciously accepting the risk) is the difference between a managed transition and a latent landmine. This decision must precede booking flights because some bridge policies must be bought before departure.
  - Output: `relocation/insurance_gap.md` exists; gap window has explicit dates; chosen option + cost recorded; uncovered days = 0 or an explicit accepted-risk note.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday logistics commit.

**Thursday — Day-job transition handoff**

- [ ] Artifact: `relocation/transition_doc.md`. Write the day-job transition document: for each active project you own, record {project, current state, open risks, where the code/docs live, the named successor or coverage plan, the one thing that will break if nobody owns it}. This is the doc you'll hand off in W133. (~30 min)
  - Output: `relocation/transition_doc.md` has one section per active project, each naming a successor or explicit coverage plan and a single "biggest bus-factor risk" line; no project is left without an owner; the doc is ready to share verbatim in W133.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday transition-doc commit.

**Friday — Notify warm network**

- [ ] Artifact: `outreach/log_new_role_announce.md` (private). Send a short, genuine thank-you + new-role announcement to the warm contacts who helped over the prior phases (the maintainers, referrers, and intros from W120). Log each {name, channel, date, the specific help you're thanking them for}. No ask — this is gratitude + signal that the bet paid off. (~30 min)
  - Output: `outreach/log_new_role_announce.md` lists each contact with name + channel + date + the specific past help referenced; `grep -c '^- ' outreach/log_new_role_announce.md` ≥ 3; no message contains a new ask.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Friday network-notify commit.

**Saturday — Reth + Tempo maintenance wind-down planning**

- [ ] Decision: ongoing maintenance cadence. In `notes/132_maintenance_cadence.md`, set the post-relocation maintenance level for Reth and Tempo from the new role: target 2-3 hrs/wk (evenings/weekends), what stays in scope (issue triage, small PRs, public commitment) vs. what's dropped (new-feature work), and the weekly time slot. The public commitment stays; depth dials down. (~30 min)
  - 🎯 Hardened acceptance: `132_maintenance_cadence.md` names a concrete weekly time-budget (hrs + which evenings/weekend slot), an explicit in-scope vs. dropped split (≥2 items each side), and a re-evaluate trigger (e.g. "if new-role ramp exceeds X hrs/wk for 2 weeks, drop to issue-triage-only"). A cadence with no dropped column is rejected — the point is the boundary.
  - *Mental model*: the failure mode of every "I'll keep contributing on the side" plan is unbounded scope colliding with a demanding new-job ramp (W135-W144), ending in silent abandonment that costs more credibility than an honest dial-down. Writing the in-scope/dropped split and a re-evaluate trigger NOW — before the ramp pressure hits — turns "depth dials down" from a vibe into a defended boundary that protects both the new job and the public commitment.
  - Output: `notes/132_maintenance_cadence.md` exists with a weekly time-budget, an in-scope vs. dropped table (≥2 each), and a re-evaluate trigger.
- [ ] [Tempo] Honest note in `notes/132_maintenance_cadence.md`: if the accepted role is at a Tempo design partner, this maintenance becomes part of the day job (in-scope, day-job hours); if not, it's evening/weekend per the cadence above. Record which branch applies and the consequence for the time budget. (~30 min)
  - Output: `132_maintenance_cadence.md` contains a clearly-marked Tempo branch that states which case applies (design-partner vs. not) and the resulting time-budget consequence.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday maintenance-cadence commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
