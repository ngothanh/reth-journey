# Week 132 — Resignation + (conditional) relocation logistics — only if the flip-trigger fired

> **Phase**: 7 (Bet Placement / Bridge)
> **Month**: M34
> **Track**: (career — bet-path transition: founding/core-eng bet entry OR crypto-MM cash bridge; relocation is CONDITIONAL)
> **Hours target**: 40h/wk (W118–W136 window; drops to 30h/wk at W137)
> **Mirror target**: (n/a — career/transition week; no upstream codebase module this week)
> **Crate(s) touched**: (none — `perp-dex-core` / Reth-spine / Tempo are in maintenance-only mode this week; no new code. The capstone's *public artifacts* — repo, blogs #4–#7, latency report, cluster-VOPR writeups — are the asset being protected, not extended.)
> **Bar**: (n/a — no Build arc; judgment/decision items carry hardened-acceptance + a mental model instead)
> **Feeds into**: W133 day-job final-2-weeks + ramp-down (consumes this week's transition doc + successor map + the maintenance-cadence boundary); W134 start at the bet (or bridge) — remote-first, hub only if triggered (consumes the visa-status record IFF relocating, the short-term-rental booking, and the live banking path); W135–W138 first month ramping into the core engine I was brought in to build (consumes the maintenance-cadence commitment as the boundary that protects ramp-up time); W139 Bet #2 thesis seeds. README.md §5 M36 ("Bet #1 placed OR bridge engaged — **no pre-committed relocation**"), §7 geography block (hub follows the bet; flip-trigger prep→move).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): README.md §7 geography ("Dubai = crypto-native/Binance-dense; Singapore = institutional/Asia-facing") + the **explicit flip trigger** (≥N warm conversations cluster in one hub OR a presence-contingent offer premium); §6 ecosystem-selection checklist (the GO-bet gate that W131 already applied — this week assumes its output); destination-hub immigration/visa portals + the bet/bridge firm's immigration counsel (Monday — official sources only, lookup-when-stuck); destination-hub **pre-TGE token-tax treatment** (Wednesday — the §7 "cheap prep" item now becomes load-bearing if relocating); destination-hub banking onboarding requirements (Wednesday — proof-of-address / tax-ID prerequisites).
> **v3 modifications**: (1) **Relocation is now CONDITIONAL, gated on the §7 flip-trigger** — v2 treated relocation as a foregone "destination landing." v3: the default is **Vietnam-remote**; this week branches at the top (Monday) into RELOCATING vs STAYING-REMOTE, and every downstream item carries the branch. (2) **The transition is a *bet entry*, not a job change** — frontmatter, the Friday network notify, and the Saturday wind-down are re-aimed from "new firm" to "the bet (founding/core-eng token-equity) OR the crypto-MM cash bridge," with the §6 checklist as the thing W131 already decided. (3) **Token-tax treatment becomes load-bearing** — the §7 "cheap prep" (pre-TGE token tax for both hubs) moves from a deferred chore into Wednesday's decision IFF relocating, because the bet's upside is token-equity and the hub choice changes its tax treatment. (4) **Maintenance wind-down covers the whole portfolio**, not just Reth+Tempo — the `perp-dex-core` repo + blogs #4–#7 + the cluster-VOPR/latency artifacts are the public proof that sourced the bet; their maintenance cadence (does the repo go stale the moment the bet starts?) is the load-bearing Saturday decision, with Reth/Tempo as the secondary leg.

This is a career/bet-transition logistics week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding. The week's job: convert the **W131 decision** (Bet #1 GO if §6 passed, else engage the crypto-MM bridge + keep outreach) into the irreversible logistics — notice submitted, the relocation branch *resolved* (the §7 flip-trigger either fired or it didn't), visa/token-tax pipeline opened IFF relocating, money + housing reachable on arrival, the day-job knowledge handed off, and the public capstone artifacts put on a defended maintenance cadence — so that **W134 start at the bet/bridge is a non-event** and the W135+ ramp into the core engine isn't sabotaged by unresolved life admin or a silently-rotting portfolio. Each item carries an explicit artifact file + an Output line that proves it done; the four judgment/decision items (Monday relocation-branch, Wednesday insurance-gap + token-tax, Saturday maintenance-cadence) carry a hardened-acceptance pass criterion and a mental model of why the item exists now.

**The branch this week resolves (read before Monday):** there are TWO orthogonal outcomes from W130–W131, and this week must pin BOTH explicitly:

- **Outcome A — entry type:** (A1) **Bet #1 GO** (founding/core-eng token-equity, §6 all-seven passed) — the transition is into the venue I'll build the core engine for; OR (A2) **Bridge engaged** (crypto-MM cash seat, hireable on the latency-craft half) while Bet #1 outreach continues. The Friday network-notify and Saturday wind-down differ in tone but not in structure.
- **Outcome B — geography:** (B1) **RELOCATING** — the §7 flip-trigger fired (≥N warm conversations clustered in one hub, OR a presence-contingent offer premium appeared); hub = **Dubai** if the bet is crypto-native/Binance-dense, **Singapore** if institutional/Asia-facing. (B2) **STAYING REMOTE** — the trigger did NOT fire; Vietnam-remote remains the default; the visa/housing/shipping items collapse to a 10-minute "N/A — staying remote, trigger not fired" stamp and the week's hours redirect to W133 handoff prep + Saturday's portfolio-maintenance decision.

Monday pins both outcomes in writing first; every later item reads the branch and either executes the relocation path or records the explicit no-op. **Do not silently assume relocation** — the v2 plan's biggest defect was treating the move as foregone.

---

**Monday — Resolve the branch + (if relocating) open the visa pipeline**

- [ ] Decision: pin the W131 outcome + the §7 flip-trigger. In `relocation/branch_decision.md`, record (i) **entry type** A1 (Bet #1 GO) or A2 (bridge engaged) with a one-line pointer to the W131 §6-checklist result that justifies it; (ii) **geography** B1 (relocating — which hub + why per §7: Dubai=crypto-native vs SG=institutional) or B2 (staying remote — trigger NOT fired); (iii) the explicit **flip-trigger evidence** (the warm-conversation cluster count, or the presence-contingent offer premium, or "neither — staying remote"). (~30 min)
  - 🎯 Hardened acceptance: `branch_decision.md` states A1|A2 AND B1|B2 unambiguously (no "probably"); the geography branch cites the §7 trigger by name and gives the *evidence* that fired it (a number of warm conversations in the hub, or the offer-premium delta) OR explicitly states "trigger did not fire → staying remote"; if B1, the chosen hub (Dubai|Singapore) is justified against the bet's character (crypto-native vs institutional) per §7. A B1 with no trigger evidence is rejected — relocation without a fired trigger is exactly the v2 anti-pattern v3 removed.
  - *Mental model*: relocation is the single most expensive, least-reversible decision in the whole plan, and v3's discipline is that it is **pre-staged, never pre-committed** (§7) — the hub *follows the bet*, decided at Bet #1 time, only when the flip-trigger fires. Writing the branch down on day 1 forces the question "did the trigger actually fire?" before any visa/lease money is spent. If B2, the rest of the week is dramatically lighter and those hours move to handoff + portfolio maintenance — which is the correct default, not a failure.
  - Output: `relocation/branch_decision.md` exists; A-branch and B-branch both stated; flip-trigger evidence recorded; `git log -1 --oneline` will show this as the first commit.
- [ ] **[IF B1 — relocating]** Artifact: `relocation/visa_pipeline.md`. Open the immigration pipeline with the bet/bridge firm's HR / immigration counsel for the chosen hub: confirm the exact visa category they sponsor (note Dubai vs SG differ materially — UAE employment/golden-visa vs SG Employment Pass/Tech.Pass), the documents you must supply (passport scan, degree, employment/engagement letter, etc.), who files what, and the dates each step is due. Log every step with {step, owner (you|firm|counsel), document, due-date, status, source-URL}. Begin paperwork — supply whatever is on you today. (~45 min)
  - 🎯 Hardened acceptance: `visa_pipeline.md` lists every pipeline step as `{step, owner, document, due-date, status, source-URL}`; every processing-timeline number carries a source link or an explicit `[UNVERIFIED — confirm with counsel]` tag (no unsourced dates); the **critical-path step** (the one whose deadline gates W134 start) is flagged; the chosen-hub visa *category* is named, not generic.
  - *Mental model*: visa processing is the single longest-lead, least-controllable item in any relocation — it gates the W134 start date, and a missed early document can push the start by weeks. For a token-equity bet the start date can interact with a vesting-cliff start; for a crypto-MM bridge it gates payroll. Opening it on day 1 of W132 buys the slack that turns arrival into a non-event. Recording owner + due-date per step is what lets you chase counsel instead of discovering a stall too late.
  - Output: `relocation/visa_pipeline.md` exists with ≥1 row per pipeline step; the critical-path step is marked; every timeline number is sourced or tagged UNVERIFIED.
- [ ] **[IF B2 — staying remote]** Stamp the no-op: in `relocation/visa_pipeline.md`, write a single block "N/A — flip-trigger did not fire (see branch_decision.md); remaining Vietnam-remote; no visa action. Re-evaluate at the §7 trigger if a warm-conversation cluster or presence-premium appears later." Redirect the freed time to a head-start on Thursday's transition doc. (~10 min)
  - Output: `relocation/visa_pipeline.md` contains the explicit staying-remote stamp + the re-evaluate trigger.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Monday branch + visa commit.

**Tuesday — Housing (conditional) + remote-work continuity**

- [ ] **[IF B1 — relocating]** Artifact: `relocation/housing.md`. Research the chosen-hub housing. Book (or shortlist with prices + dates) a short-term rental that covers the arrival window; record the permanent-housing decision as **explicitly deferred 2–3 months** (W140 anchor) with the trigger date to revisit. Log {neighborhood, monthly cost, lease length, commute/area notes, link} for each candidate, and the one you'll book. (~45 min)
  - Output: `relocation/housing.md` lists ≥2 short-term candidates with cost + dates + link, names the one to book (or marks it booked), records the permanent-housing deferral with a concrete W140 revisit date; `grep -c '^| \|^- ' relocation/housing.md` ≥ 2.
- [ ] **[IF B2 — staying remote]** Artifact: `relocation/remote_continuity.md`. The "housing" item becomes a **remote-work continuity** item: confirm the home workstation/network meets the bet/bridge's collaboration needs (timezone overlap window with the team, redundant connectivity, a quiet on-call-capable setup if the crypto-MM seat needs it). Log {requirement, current state, gap, fix + cost}. (~30 min)
  - 🎯 Hardened acceptance: `remote_continuity.md` names the team's working-hours window and your committed overlap hours, lists ≥1 connectivity-redundancy plan (so a single ISP outage doesn't take you offline during market hours / a deploy), and flags any gap with a fix + cost. A remote bet/bridge with no connectivity-redundancy plan is the silent reliability landmine for a derivatives-venue or MM seat.
  - Output: `relocation/remote_continuity.md` exists with the overlap window, a redundancy plan, and any gap fixed or flagged.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday housing/continuity commit.

**Wednesday — Logistics: banking + insurance gap + (if relocating) pre-TGE token tax**

- [ ] **[IF B1 — relocating]** Artifact: `relocation/banking_and_shipping.md`. Start the destination-hub bank-account setup: list the account prerequisites (proof of address, tax ID, visa status) and which you can satisfy pre-arrival vs on-arrival, with the order of operations. Add the shipping plan: what ships, what flies with you, rough cost + lead time. (~30 min)
  - Output: `relocation/banking_and_shipping.md` lists bank-account prerequisites each tagged `pre-arrival | on-arrival` with a dependency note, plus a shipping line-item list with cost + lead time; the first actionable bank step is marked as started.
- [ ] **[IF B1 — relocating, AND entry type A1 — token-equity bet]** Decision: pre-TGE token-tax treatment. In `relocation/token_tax.md`, record the chosen hub's treatment of the bet's **token-equity grant** (pre-TGE allocation, vesting/cliff, what is taxed at grant vs vest vs TGE vs sale), citing official sources, and what residency change (if any) the relocation triggers for that treatment. This is the §7 "cheap prep" item now made load-bearing because the bet's *entire upside* is the token. (~45 min)
  - 🎯 Hardened acceptance: `token_tax.md` names the chosen hub (Dubai|Singapore) and states, with a source-URL or an explicit `[UNVERIFIED — confirm with a tax advisor]` tag per claim, the taxable events across grant→cliff→vest→TGE→sale for a pre-TGE engineer allocation; it records whether the relocation changes tax residency and the date that change takes effect; it flags the single highest-value open question to put to a tax advisor before signing anything. Token-equity treatment left to "I'll figure it out after I move" is the most expensive avoidable error in the whole bet — the hub is chosen partly *for* this treatment (§7).
  - *Mental model*: the difference between a 0% and a 20%+ effective tax on a token-equity windfall is, at the $20–100M target, literally millions — and it is decided by *which hub you establish residency in and when*, not by the engineering. §7 makes hub choice follow the bet; this item makes the tax consequence of that choice explicit *before* the move is irreversible. For a crypto-MM cash bridge (A2) this collapses to ordinary salary-tax and is a 10-minute note instead.
  - Output: `relocation/token_tax.md` exists; taxable events enumerated + sourced/tagged; residency-change effect + date recorded; the top advisor question flagged.
- [ ] Decision: health-insurance gap coverage. In `relocation/insurance_gap.md`, decide how you're covered for the window between leaving the current employer's plan (resignation effective ~W133+30d) and the new plan's start (bet/bridge onboarding, or self-coverage if remote-contractor). Record the chosen option, its cost, and the exact dates it covers. (~30 min)
  - 🎯 Hardened acceptance: `insurance_gap.md` names the gap window as concrete start/end dates, lists ≥2 coverage options (e.g. travel-medical if relocating, marketplace/private bridge, new-employer early-enrollment, or self-funded if the bet pays as a contractor) with cost, and records the chosen one with **zero uncovered days** inside the window — if any day is uncovered, that is flagged explicitly as an accepted risk, not left silent.
  - *Mental model*: the dangerous part of a transition isn't the cost — it's the silent uncovered gap between two insurance plans, where one accident becomes a five-figure liability with no recourse. A token-equity bet may pay as a contractor with no group plan at all, making this *more* acute, not less. Forcing the window into explicit start/end dates and proving zero uncovered days (or consciously accepting the risk) is the difference between a managed transition and a latent landmine. This must precede booking flights/coverage because some bridge policies must be bought before departure.
  - Output: `relocation/insurance_gap.md` exists; gap window has explicit dates; chosen option + cost recorded; uncovered days = 0 or an explicit accepted-risk note.
- [ ] **[IF B2 — staying remote]** Stamp the no-op for banking/shipping/token-tax-residency in `relocation/banking_and_shipping.md`: "N/A — staying remote; existing Vietnam banking + tax residency unchanged. Token-tax (if A1) handled under current residency — see token_tax.md note." Still complete `insurance_gap.md` (a contractor bet/bridge can still drop your group plan). (~15 min)
  - Output: `relocation/banking_and_shipping.md` carries the staying-remote stamp; `insurance_gap.md` still completed.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday logistics commit.

**Thursday — Day-job transition handoff**

- [ ] Artifact: `relocation/transition_doc.md`. Write the day-job transition document: for each active project you own, record {project, current state, open risks, where the code/docs live, the named successor or coverage plan, the one thing that will break if nobody owns it}. This is the doc you'll hand off in W133. (~45 min)
  - 🎯 Hardened acceptance: `transition_doc.md` has one section per active project, each naming a successor or explicit coverage plan and a single "biggest bus-factor risk" line; no project is left without an owner; the doc is ready to share verbatim in W133. Leaving the day job clean is not sentiment — it protects the §7 *secondary bridge* (crypto-infra IC at this employer is a fallback re-entry if the bet and the crypto-MM seat both fall through), so don't burn the bridge.
  - *Mental model*: you may need this employer again — the §7 shots-on-goal structure explicitly keeps "no skill stranded," and a graceful exit keeps the crypto-infra-IC fallback warm. A clean, owned handoff costs one afternoon and preserves an option worth months of runway.
  - Output: `relocation/transition_doc.md` has one owned section per active project + a bus-factor line each.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday transition-doc commit.

**Friday — Notify the warm network (gratitude + signal the bet landed)**

- [ ] Artifact: `outreach/log_bet_landed_announce.md` (private). Send a short, genuine thank-you + outcome announcement to the warm contacts who helped over Phases 6–7 (the Binance-alumni intros from W113/W120, the founders/desk contacts from W122–W128, the maintainers/referrers). The message differs by branch: A1 = "joining {venue} as founding/core eng on the matching/risk/settlement core"; A2 = "taking a crypto-MM seat at {desk} as the bridge while I keep building." Log each {name, channel, date, the specific help you're thanking them for}. No ask — this is gratitude + the signal that the bet (or the bridge to it) is live, which compounds the network for **Bet #2** (§7: each bet upgrades the next). (~45 min)
  - 🎯 Hardened acceptance: `log_bet_landed_announce.md` lists each contact with name + channel + date + the specific past help referenced; `grep -c '^- ' outreach/log_bet_landed_announce.md` ≥ 3 and includes ≥1 Binance-alumni intro and ≥1 founder/desk contact from the outreach phase; no message contains a new ask. The announcement is framed so it seeds Bet #2 relationships (warmth banked, not spent).
  - *Mental model*: §7's terminal logic is **2–3 sequential shots, each upgrading the next** — the reputation and network from this entry are exactly what raise the allocation tier reachable at Bet #2. A gratitude note with no ask is the highest-ROI relationship investment available; it converts "person who helped once" into "person who roots for your next move."
  - Output: `outreach/log_bet_landed_announce.md` exists; ≥3 contacts incl. ≥1 alumni intro + ≥1 founder/desk; no new ask.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Friday network-notify commit.

**Saturday — Portfolio + Reth/Tempo maintenance wind-down planning**

- [ ] Decision: ongoing maintenance cadence for the **public capstone portfolio** (the asset that sourced the bet). In `notes/132_maintenance_cadence.md`, set the post-transition maintenance level for the `perp-dex-core` repo + blogs #4–#7 + the cluster-VOPR / latency-report / RWA-seam-narrative artifacts: target a concrete weekly time-budget (evenings/weekends if A1 keeps it off-hours; or note if A2's bridge leaves more capacity), what stays in scope (issue triage, the public README/repo not going stale, responding to founding-eng reviewers who find it) vs what's dropped (capstone v1.5 feature work UNLESS it becomes the day-job — see below), and the weekly slot. The public proof must not silently rot the moment the bet starts. (~30 min)
  - 🎯 Hardened acceptance: `132_maintenance_cadence.md` names a concrete weekly time-budget (hrs + which evening/weekend slot), an explicit **in-scope vs dropped** split (≥2 items each side) for the perp-dex-core portfolio, and a **re-evaluate trigger** (e.g. "if the bet's ramp exceeds X hrs/wk for 2 weeks, drop the portfolio to issue-triage-only — but never let the public repo 404"). A cadence with no dropped column is rejected — the point is the boundary. The capstone repo + flagship blog staying *reachable and accurate* is non-negotiable because they remain the proof for Bet #2.
  - *Mental model*: the failure mode of every "I'll keep the portfolio fresh on the side" plan is unbounded scope colliding with a demanding founding-eng ramp (W135–W138), ending in a stale repo that *undercuts* the very reputation that sourced the bet — and that you'll need again for Bet #2. Writing the in-scope/dropped split + a re-evaluate trigger NOW, before the ramp pressure hits, turns "I'll maintain it" from a vibe into a defended boundary protecting both the new role and the public asset.
  - Output: `notes/132_maintenance_cadence.md` exists with a portfolio weekly time-budget, an in-scope vs dropped table (≥2 each), and a re-evaluate trigger.
- [ ] Branch note — capstone v1.5 as day-job vs side-project. In `notes/132_maintenance_cadence.md`, record which branch applies: if the bet (A1) *is* an on-chain-derivatives venue, the `perp-dex-core` v1.5 differentiator work (RWA features + ADL + multi-collateral, the W118–W119 tier-raiser) may **become part of the day job** (in-scope, day-job hours — the bet is literally building this); if A2 (crypto-MM bridge) or A1 at a venue with a different stack, v1.5 stays evening/weekend per the cadence above. State which case applies and the consequence for the time budget. (~30 min)
  - Output: `132_maintenance_cadence.md` contains a clearly-marked v1.5 branch stating venue-day-job vs side-project and the resulting time-budget consequence.
- [ ] Secondary leg — Reth + Tempo maintenance. In `notes/132_maintenance_cadence.md`, set the cadence for the Half-1 spine (Reth) and the Tempo/RWA-collateral option leg: the public commitment stays (issue triage, small PRs) so the crypto-infra-IC fallback bridge (§7 secondary) stays warm; depth dials down (no new-feature work). Note the Tempo branch: if the bet is at a Tempo design partner, this folds into the day job; else evening/weekend. (~20 min)
  - Output: `132_maintenance_cadence.md` has a Reth/Tempo section with an in-scope/dropped split and the Tempo design-partner branch resolved.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday maintenance-cadence commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.

**Phase-7 ritual additions (this week):**
- [ ] Update the bet-path tracker: record the resolved branch (A1|A2 entry, B1|B2 geography) and the W134 start mode (remote vs hub) so W133–W134 read from a single source of truth.
- [ ] §7 shots-on-goal note: with Bet #1 placed (or the bridge engaged), log the first seed for **Bet #2** (which adjacency the network + reputation + capstone now reach) — this is the W139 thesis-seed input. Each bet upgrades the next; start banking the upgrade now.
- [ ] Confirm the maintenance-cadence boundary (Saturday) is in writing *before* W135 ramp pressure — re-read it once and verify it has a dropped column and a re-evaluate trigger.
