# Week 122 — Inbound triage + bet-outreach (≥20 targeted conversations; response-rate metric)

> **Phase**: 7 (Bet Placement / Bridge)
> **Month**: M32
> **Track**: Bet-path (founding/core-eng token-equity outreach on on-chain-derivatives venues) **+** crypto-MM cash bridge (the income floor) — bar (n/a)
> **Hours target**: 40h/wk (W118–W136 window)
> **Mirror target**: (none — bet-outreach/ops week; no upstream module mirrored)
> **Crate(s) touched**: (none — no plan-crate version bump this week; the artifact this week *sells* is `perp-dex-core` v1.0/v1.5 + the flagship blog, not a new crate)
> **Bar**: (n/a) — bet-outreach/ops week, no critical-path component. The *bar that matters this week is the §5 M24/M30 controllable-outbound metric*, not a bar-(c) crate gate.
> **Feeds into**: W123–W124 event-trip recon (intro calls + warm conversations sourced here are taken/advanced in-region, **no relocation**); W125 active outreach (this week's categorized pipeline + §6-filtered target list seeds the W125 outbound batch); W126–W129 founding/core-eng conversations & work-trials (this week's cadence is the warm-up); W130–W131 Bet #1 decision OR crypto-MM bridge engage (this week's response-rate data is an input to that GO/bridge call); W120 warm-network activation continues here (Binance-alumni map → derivatives founders **and** crypto-MM desks)
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): W121 flagship blog post (the cross-margin + liquidation-engine artifact driving inbound; "architected for RWA-perps"); `RECONCILED_PLAN_v3.md` §1 (the four readiness bars — esp. bar (c) activated network ≥5 warm intros / ≥2 "would bring you in", and bar (d) crypto-MM cash bridge), §5 M24/M30 (the controllable-outbound metric this week is measured against: ≥20 targeted conversations, response rate ≥25%, ≥3 Binance-alumni warm intros), §6 (the seven-point ecosystem-selection checklist — every bet target is filtered through it), §7 (shots-on-goal + Dubai/SG pre-stage flip-trigger); the ecosystem/destination list compiled W115–W116 (≥5 venues already passing the §6 filter); `notes/recall_perp_dex_core.md` from the W117 retro
> **v3 modifications**: this week was the v2 "Inbound triage + interview-prep continuity" career week (HFT job-hunt framing: Tier-A HFT recruiters, LeetCode/systems-design interview prep, Tempo Path D, reth PR profile-keeping). v3 re-aims it end-to-end to the **bet-path**: (1) the inbound being triaged is from the **W121 flagship post about `perp-dex-core`** (cross-margin + liquidation engine), categorized as **founding/core-eng-bet** / **crypto-MM-bridge** / **crypto-infra-IC-fallback** / **curiosity** / **ignore** — *not* "hiring-HFT/hiring-crypto"; (2) the centerpiece is the **§5 controllable-outbound metric** (≥20 targeted on-chain-derivatives conversations, ≥25% response, ≥3 Binance-alumni warm intros) — replacing the v2 uncontrollable "1 HFT recruiter inbound"; (3) every bet target is run through the **§6 seven-point ecosystem checklist** (founding-tier allocation, tier-1 backing, core-criticality, multi-B FDV, token reality, remote-native, cycle timing); (4) "interview prep" is replaced by **bet-conversation prep** — the talking points are *lived numbers from `perp-dex-core`* (cluster-VOPR, VSR-log-as-source-of-truth, incremental-margin at 100k accounts), not LeetCode; (5) the bridge leg is **crypto-MM cash seat** (Wintermute/Jump-style, remote — bar (d) floor) underwritten by the latency craft, not a generic crypto-infra job; (6) Dubai/SG appear only as **cheap pre-stage** (the §7 flip-trigger is *evaluated*, never pre-committed); (7) Tempo is kept warm only as the **RWA-collateral option leg** that strengthens the v1.5 RWA-perps narrative, not as a parallel Path-D job hunt.

This is a light bet-outreach/ops week — there are **NO Build exercises and none should be manufactured** (consistent with the §A re-slotting: W118–W119 carried the `perp-dex-core` v1.5 build; this week is outreach + pipeline). The deliverable is a clean, categorized inbound pipeline **plus** a §6-filtered outbound target list driven to the **§5 controllable metric** (≥20 targeted conversations, ≥25% response, ≥3 warm intros), so W123–W124 recon and W125+ active outreach start from data, not memory — and so the W130–W131 Bet #1 / bridge decision has real response-rate evidence. Every item below names its file and an Output line; recall/decision items carry a hardened acceptance and a mental model for why the item exists *now*.

**The bet-path framing this week locks in (read before Monday):** the terminal state is **not a job** — it is a **founding/core-engineer token-equity bet** on an on-chain-derivatives venue (§1 bar (b)), with a **crypto-MM cash seat as the income floor/bridge** (bar (d)). So the Binance-alumni map points at **two** destinations — derivatives founders (the bets) and crypto-MM desks (the floor) — and the artifact you lead with is **`perp-dex-core`** (the replicated, cluster-VOPR'd hybrid CLOB whose centerpiece is the cross-margin + liquidation engine), not a list of crates. The §6 checklist is the gate: a contact is a **bet** only if it can plausibly pass all seven points; otherwise it is a **bridge** (crypto-MM cash) or a **fallback** (crypto-infra IC on the Reth spine). Every triage category, every reply template, and every conversation this week is in service of that distinction.

---

## Monday — Inbound triage from the flagship post (categorize against the bet-path)

- [ ] **Triage inbound from the W121 flagship post** → `notes/W122_inbound_pipeline.md` (private). For every person who reached
  out via the W121 flagship post (DMs, email, GitHub, blog comments, X/Farcaster replies), record one row: name, channel,
  firm/venue, role-hint, and a **bet-path category** — `bet:founding-eng` (on-chain-derivatives venue, plausibly §6-passing) /
  `bridge:crypto-MM` (Wintermute/Jump-style cash seat) / `fallback:crypto-infra-IC` (Reth-spine role) / `curiosity` /
  `ignore` (sales pitch, recruiter spam for unrelated roles). One row per contact, markdown table. (~40 min)
  - 🎯 Hardened acceptance: every inbound message from the W121 post is represented as exactly one row; each row has a
    non-empty bet-path category; `ignore` rows are excluded from the W123 call shortlist and the §6 target list. Output:
    `notes/W122_inbound_pipeline.md` exists with a contacts table; counts of `bet:*` / `bridge:*` / `fallback:*` rows are
    stated at the top (these three counts feed the Friday metric tally).
  - *Mental model*: the W121 flagship post was the public artifact built specifically to generate **bet-path** inbound —
    its whole pitch is "I built the cross-margin + liquidation engine that *is* a perp venue." Triaging it NOW (while the
    post is fresh and replies arrive) means W123 recon calls and W125 outreach work from a concrete, §6-aware pipeline,
    not from scrolling DMs under deadline pressure. **Zero bet-path inbound is a valid, informative outcome**: if the
    `bet:*` count is 0, that is the §5/§9 signal to lean harder on *controllable outbound* (Tue/Wed) and not wait on
    inbound — the M24 risk note explicitly says response-rate is controllable (ship blog earlier, widen Binance-alumni
    outreach, temporarily lower target tier for the bridge).
- [ ] **Draft per-category reply templates** → append to `notes/W122_inbound_pipeline.md`. Four short reply stubs tuned to
  the bet-path: `bet:founding-eng` (lead with `perp-dex-core` v1.0 — 3-node VSR where the order/event log *is* the VSR log,
  cluster-VOPR green, incremental-margin cross-margin at 100k accounts; gently surface the RWA-aware seam as the
  differentiator); `bridge:crypto-MM` (lead with the latency craft — `matching-engine` sub-µs p99, `latency-lab`
  tick-to-trade + coordinated-omission report, `messaging-aeron`/`marketdata-kernelbypass`); `fallback:crypto-infra-IC`
  (lead with the Reth spine — merged reth/revm PRs, `exec-vm` block-stm, `storage-trie`); `curiosity` (warm thanks + link,
  no ask). (~25 min)
  - 🎯 Hardened acceptance: 4 reply templates exist; the `bet`, `bridge`, and `fallback` templates each name ≥2 concrete
    shipped artifacts (crate version or merged PR) **by name with one lived number**, not generic claims. Output: templates
    appended under a `## Reply templates` heading.
  - *Mental model*: response-rate collapses with reply latency, and the *content* of the reply is the first filter the
    other side applies. A founding-eng founder and a crypto-MM desk lead want different things — the founder wants the
    engine + the RWA moat narrative; the MM desk wants the tail-latency craft. Pre-writing per-category stubs (each
    leading with the right artifact + a real number) means Tuesday's outbound and any same-day inbound responses go out in
    minutes and land on the right note.
- [ ] Commit notes (private repo). (~10 min)

**Output for Monday**: `notes/W122_inbound_pipeline.md` — categorized contacts table (bet-path categories) + `bet/bridge/fallback` counts at top + 4 reply templates.

---

## Tuesday — §6 ecosystem filter + build the targeted outbound list

- [ ] **Run every candidate venue through the §6 ecosystem-selection checklist** → `notes/W122_ecosystem_targets.md`. Take
  the W115–W116 shortlist (≥5 venues already passing) **plus** any `bet:founding-eng` inbound from Monday, and score each
  against the seven §6 points: (1) founding-tier allocation, (2) tier-1 backing {Paradigm, a16z crypto, Dragonfly,
  Polychain, 1kx, Multicoin, Jump, Wintermute}, (3) core-criticality (I build the engine that *is* the company),
  (4) multi-B-FDV ceiling, (5) token reality (pre-TGE, sane vest/cliff, ≥10× headroom), (6) remote-native (Vietnam-remote
  works), (7) cycle timing. Mark each point GO/UNKNOWN/NO with a one-line basis. (~75 min)
  - 🎯 Hardened acceptance: ≥5 venues scored across all 7 points; a venue is tagged **BET** only if all 7 are GO or
    GO-pending-verification with no hard NO (a single hard NO → tag **BRIDGE-or-skip**, per §6 "GO only if all seven
    pass"); each UNKNOWN has a named next-step (e.g. "confirm cap table via Crunchbase/announcement", "ask warm intro for
    vest terms"). Output: `notes/W122_ecosystem_targets.md` with one scored block per venue + a BET/BRIDGE tag.
  - *Mental model*: the §6 checklist is the discipline that keeps a *bet* from degrading into a *job* — the Airwallex
    anti-pattern (late-stage, tiny employee equity → never a bet) is exactly what points (1)+(5) screen out. Scoring NOW,
    before any conversation, means W125–W129 conversations are aimed only at venues that *can* clear the bar, so finite
    outreach energy isn't spent advancing a contact that fails point (3) core-criticality or point (4) FDV ceiling. A
    BRIDGE-tagged venue isn't wasted — it routes to the crypto-MM/crypto-infra column instead.
- [ ] **Compile the targeted outbound list (toward the ≥20 metric)** → append a `## Outbound batch (toward ≥20)` section to
  `notes/W122_ecosystem_targets.md`. Merge: BET-tagged venues (founding/core-eng outreach) + crypto-MM desks (bridge) +
  any `fallback` contacts. For each, log the warm-intro path if one exists (which Binance alumna/alumnus can introduce —
  cross-reference the W120 Binance-alumni map), else mark `cold`. Target a list of **≥20 named targets** so the week can
  realistically log ≥20 *conversations initiated* by Friday. (~45 min)
  - 🎯 Hardened acceptance: ≥20 named targets listed, each tagged `bet`/`bridge`/`fallback` and `warm`/`cold`; the count of
    `warm` (Binance-alumni-introducible) targets is stated (this feeds the ≥3-warm-intros sub-metric). Output: the
    `## Outbound batch` table.
  - *Mental model*: §5's metric is **controllable** precisely because it counts *outbound conversations initiated*, not
    *inbound received* — the whole point of the M24 fix was to replace "1 HFT recruiter inbound" (uncontrollable) with a
    target I drive. A ≥20-name list built today is what makes the Friday tally honest rather than aspirational. Warm intros
    (point: bar (c) ≥5 warm intros / ≥2 "would bring you in") convert far higher than cold, so the warm count is the
    leading indicator of the M30 gate.
- [ ] Commit notes. (~10 min)

**Output for Tuesday**: `notes/W122_ecosystem_targets.md` — §6 scorecards (BET/BRIDGE tags) + a ≥20-name outbound batch with warm/cold + warm-intro count.

---

## Wednesday — Send outreach + schedule conversations into W123–W124

- [ ] **Send the first outbound wave + reply to all `bet:*`/`bridge:*` inbound** → update `notes/W122_inbound_pipeline.md`
  and `notes/W122_ecosystem_targets.md` with a shared `## Conversations log` (one canonical table). For each `warm` target,
  send the intro request to the connecting Binance alumna/alumnus (or take the intro if already offered); for each `bet`/
  `bridge` target reachable directly, send the matching reply template; propose two W123–W124 slots **in both local and
  destination timezones** (the recon trip is in-region). Log: contact, category, channel, sent-timestamp, proposed slots,
  status. (~90 min)
  - 🎯 Hardened acceptance: every `bet:*` and `bridge:*` contact (inbound) and every `warm` outbound target has a sent
    message and ≥1 proposed slot logged; the running count of **conversations initiated** is stated at the top of the table
    (this is the §5 ≥20 numerator). Output: `## Conversations log` table, one row per contact, status column populated.
  - *Mental model*: W123–W124 is the event-trip recon window — the chance to convert a thread into an in-person /
    in-timezone conversation, which is far higher signal than a cold async DM and is exactly how the §1 bar (c) "would
    bring you in" conversations get advanced. Scheduling a week ahead respects calendars and lets the trip itinerary form
    around real meetings. Sending warm-intro requests *first* (they have the longest lead time and best conversion) is the
    sequencing that maximizes the Friday warm-intro count.
- [ ] **Pre-stage Dubai/SG eligibility (cheap prep ONLY — no commitment)** → `notes/W122_geography_prestage.md`. Confirm
  two facts for *both* hubs: (a) visa eligibility for a Vietnam-remote engineer / founding-eng hire; (b) pre-TGE token-tax
  treatment. No move, no lease, no commitment — just the two facts on file so a Bet #1-time decision is fast. (~30 min)
  - 🎯 Hardened acceptance: `notes/W122_geography_prestage.md` records, per hub (Dubai, Singapore): visa-route note +
    token-tax-treatment note, each with a source link or "needs a specialist — flagged". A one-line reminder that the
    **§7 flip-trigger is NOT yet evaluated** (that's Thursday + W124), and that **Vietnam-remote remains the default**.
  - *Mental model*: §7 is explicit — *pre-stage, do not pre-commit*. The hub follows the bet (Dubai = crypto-native /
    Binance-dense; SG = institutional / Asia), and that's decided at Bet #1 time, not now. Doing the cheap visa + token-tax
    homework this week (when the recon trip is being planned anyway) means that *if* the flip-trigger later fires, the move
    is a logistics exercise, not a research project — and *until* it fires, no relocation cost is incurred.
- [ ] Commit notes. (~10 min)

**Output for Wednesday**: `## Conversations log` (with conversations-initiated count at top) + `notes/W122_geography_prestage.md` (Dubai/SG visa + token-tax facts, no commitment).

---

## Thursday — Bet-conversation prep: the `perp-dex-core` talking points (lived numbers, not LeetCode)

- [ ] **Founding-eng conversation 1-pager** → `notes/W122_bet_conversation_1pager.md`. Write the founding/core-eng pitch
  built entirely on **lived `perp-dex-core` numbers** (no generic claims): (a) 1-sentence elevator — "I built a replicated,
  fault-tolerant, multi-node hybrid CLOB whose centerpiece is a real-time cross-margin + liquidation engine"; (b) the
  hard-systems proof — 3-node **VSR** where the order/event log *is* the VSR log (no second hot-path log), cluster-VOPR
  green (linearizability + replica convergence + insurance-fund tripwire never hit); (c) the convergence-cell proof —
  per-mark-tick **incremental margin** at ~100k accounts / ~1M orders/s, full recompute only on circuit-breaker/param
  change, p99.99 budget met (cite the `latency-lab` report); (d) the moat — multi-instrument cross-margin (BTC+ETH netting
  with the const 30% haircut) + fee-accrued insurance-fund waterfall; (e) the differentiator narrative — **RWA-aware seams
  in v1.0, RWA features in v1.5** ("the engine is architected for RWA-perps — here's the seam"). (~60 min)
  - 🎯 Hardened acceptance: the 1-pager states ≥5 concrete numbers/invariants from `perp-dex-core` (e.g. node count,
    account scale, orders/s, a p99.99 figure, the haircut %, the insurance accrual %), each tied to the §4A acceptance
    criterion it satisfies; the RWA-seam paragraph is present and explicitly framed as "positioning from v1.0, feature
    from v1.5". Output: `notes/W122_bet_conversation_1pager.md`.
  - *Mental model*: a founding-eng conversation is won by demonstrating you've already built the company's core engine
    once — and the §1 bar (b) artifact is *exactly* that. Pulling the talking points from your own shipped capstone (not
    from interview drills) means the conversation cites lived design decisions and measured numbers — the precise
    differentiator the flagship post advertises. The RWA-seam line is the three-way-fit edge (§4A): it makes the fintech
    moat credible the moment a Bet #1 conversation starts, *before* any RWA feature ships.
- [ ] **Crypto-MM bridge 1-pager + decay-guard recall** → `notes/W122_bridge_1pager.md` + `notes/recall_hft_stack.md`. The
  bridge (bar (d) income floor) is sold on the **latency craft**, so: write the crypto-MM elevator (sub-µs matching p99,
  tick-to-trade with coordinated-omission handled, kernel-bypass-aware feed handler), then — **closing all docs** — recall
  from memory the three latency-craft crates you can speak to (`matching-engine`, `messaging-aeron`,
  `marketdata-kernelbypass`) and for EACH state its mirror, headline latency number, and hardest bug fixed; verify against
  your crate READMEs/benches and correct mismatches. (~45 min)
  - 🎯 Hardened acceptance: bridge 1-pager states ≥3 lived latency numbers; `recall_hft_stack.md` has ≥3/3 crates recalled
    with a concrete latency number each, mismatches corrected against real bench numbers. Decay context: the
    `matching-engine` core was built ~W58–W82 (deep-decay edge by W122) and these are the exact crates a `bridge:crypto-MM`
    conversation will probe. Output: both files.
  - *Mental model*: the bridge is the *floor*, not the goal — but a real crypto-MM cash seat (Wintermute/Jump-style) is
    what funds runway between bets (§7), so it must be sold as confidently as the bet. It is underwritten by Half-2's
    latency craft (exactly why `latency-lab` was pulled to M6), so the MM conversation must lead with lived tail-latency
    numbers, not the venue narrative. Forcing the recall NOW, from memory, means an MM desk call cites a real number rather
    than fumbling for the README mid-conversation.
- [ ] Commit + log. (~10 min)

**Output for Thursday**: `notes/W122_bet_conversation_1pager.md` (perp-dex-core, ≥5 numbers, RWA-seam) + `notes/W122_bridge_1pager.md` + `notes/recall_hft_stack.md` (3/3 crates verified).

---

## Friday — Reth-spine PR continuity + the §5 metric tally

- [ ] **Advance one in-flight reth/revm PR (keep the crypto-infra fallback live)** → the relevant `reth/` working branch.
  Address review comments on an open PR, or land one small, well-scoped fix/test. The goal is an unbroken contribution
  cadence visible on the profile a `fallback:crypto-infra-IC` contact checks — and a live signal that the Reth spine (the
  secondary bridge, §7) is current. (~60 min)
  - 🎯 Hardened acceptance: either a PR comment is addressed with a pushed commit, OR a new PR is opened with a green CI
    run; PR URL recorded. Output: PR URL logged in `notes/reth_pr_tracker.md` with status (`pushed` / `opened`).
  - *Mental model*: the public GitHub graph is part of the artifact a crypto-infra-IC fallback contact evaluates, and the
    Reth spine is the §7 "no skill stranded" insurance — if the on-chain-derivatives niche cools before Bet #1, the
    transferable spine keeps me hireable as crypto-infra IC while I re-bridge and re-bet. One small PR keeps that fallback
    column credible through the outreach window without diverting real time from the bet-path.
- [ ] **§5 controllable-outbound metric tally** → `notes/W122_outreach_metrics.md`. Compute, from the `## Conversations log`,
  the three §5 numbers: (1) **conversations initiated** (target ≥20), (2) **response rate** = replies / initiated (target
  ≥25%), (3) **Binance-alumni warm intros logged** (target ≥3). State each against its target with GO / SHORT. For any
  SHORT, write the controllable corrective per §9 (ship/repromote the flagship post, widen Binance-alumni outreach,
  temporarily lower target tier toward the bridge). (~30 min)
  - 🎯 Hardened acceptance: all three metrics computed with explicit numerator/denominator and a GO/SHORT verdict vs the
    §5 target; every SHORT has a named controllable corrective scheduled into W123/W125. Output:
    `notes/W122_outreach_metrics.md` with the three-line scorecard + correctives.
  - *Mental model*: this tally is the *spine* of the week — it is the M24/M30 readiness signal made measurable on a weekly
    cadence. Because the metric is controllable (it counts outbound, not inbound), a SHORT is never "bad luck" — it is a
    direct instruction to do more of a thing I control. Tracking it weekly from here means the W117/M30 bet-readiness gate
    (≥2 founding-eng conversations advanced, shortlist ≥5) is hit by accumulation, not by a last-minute scramble.
- [ ] **End-of-week pipeline sweep** → `notes/W122_inbound_pipeline.md` + `## Conversations log`. Confirm every `bet:*` and
  `bridge:*` contact has a status and a next action; flag non-responders `follow-up-W123`; flag any `bet` target that has
  surfaced a hard §6 NO since Tuesday as `re-tag BRIDGE` (or drop). (~20 min)
  - 🎯 Hardened acceptance: no `bet:*`/`bridge:*` row has an empty next-action; non-responders flagged `follow-up-W123`;
    §6-failing targets re-tagged. Output: both tables fully populated, follow-up count + re-tag count stated.
  - *Mental model*: pipelines rot without a weekly sweep, and over the W123–W124 recon trip a warm lead going cold is the
    most expensive loss. The §6 re-tag step is the discipline that keeps the *bet* column honest — a venue that fails the
    checklist after a first conversation belongs in the bridge column, not advanced as a bet (§6: "a bet is GO only if all
    seven pass; otherwise it is a bridge job, not a bet").
- [ ] Commit + log. (~10 min)

**Output for Friday**: reth PR URL logged + `notes/W122_outreach_metrics.md` (≥20 / ≥25% / ≥3 scorecard with GO-SHORT + correctives) + swept pipeline.

---

## Saturday — Tempo/RWA option-leg maintenance + perp-dex-core decay-guard recall

- [ ] **Tempo (RWA-collateral option leg) maintenance pass** → Tempo working branch / `notes/tempo_maintenance_log.md`.
  Rebase any open Tempo PR on upstream, re-run its tests, address any new review comments. Frame it as the **RWA-perps
  collateral-rails** option leg (§2 option-leg B), not a parallel job hunt: it keeps the v1.5 RWA-perps narrative concrete
  (RWA collateral has somewhere real to settle). (~30 min)
  - 🎯 Hardened acceptance: open Tempo PR (if any) is rebased and tests green, OR a log entry records "no open PR, branch
    current." Output: status line in `notes/tempo_maintenance_log.md` noting how the leg connects to the v1.5
    RWA-collateral story.
  - *Mental model*: Tempo is a retained *option leg* (the fintech/quote-to-cash edge), not the critical path — but keeping
    it warm makes the RWA-perps differentiator credible: when a `bet:founding-eng` conversation asks "what's the RWA
    collateral path?", the answer cites a live rails contribution, not a slide. It's the cheapest way to keep the three-way
    fit (systems + latency + fintech) tangible during outreach.
- [ ] **Tái-dựng (perp-dex-core bet talking points)** → `notes/recall_perp_dex_core.md`. Close all docs. From memory, state
  the capstone's three load-bearing v1.0 invariants you'll be asked about in a founding-eng conversation — (a) **the
  order/event log IS the VSR log** (one hot-path log, no second consensus); (b) **`SettlementId(snapshot_epoch, op_number,
  checksum)` = the VSR op-number**, `finality_status → {Pending, Final, Reverted}` derived from the commit point, free
  idempotency, no side mapping table; (c) **incremental-margin hot path mandatory at ~100k accounts** (full recompute only
  on circuit-breaker/param change) — then verify against `RECONCILED_PLAN_v3.md` §4A and your capstone README, correcting
  any drift. (~45 min)
  - 🎯 Hardened acceptance: ≥3/3 invariants recalled with the §4A-correct shape (the `SettlementId` triple named; the
    "one log" lock stated; the incremental-margin trigger boundary stated); mismatches against §4A corrected in
    `notes/recall_perp_dex_core.md`. Decay context: capstone assembly was W103–W113 (~9–19 weeks ago) — these are the exact
    invariants a founding-eng interviewer probes ("how do replicas converge?", "where does settlement identity come
    from?", "how does margin hit budget at 100k accounts?").
  - *Mental model*: the W123–W124 founding-eng conversations will ask "walk me through your engine." Forcing recall of the
    three hardest invariants NOW, from memory, means the conversation cites the *locked* §4A shapes (which are what make
    the artifact credible as Bet #1 readiness) rather than a fuzzy approximation — the same lived-knowledge discipline that
    produced the W121 flagship post. Getting the `SettlementId` triple or the one-log lock wrong in a conversation signals
    you didn't really build it; getting it crisp signals you did.
- [ ] Commit + log. (~10 min)

**Output for Saturday**: `notes/tempo_maintenance_log.md` (status + RWA-leg connection) + `notes/recall_perp_dex_core.md` (3/3 invariants verified vs §4A).

---

---

## [v3.1 BFT apex — Track B] W122: QC formation + chaining (`Quorum × Authenticator` → the `Certificate` type, O3)

> Track B continues (W121 completed the `ViewManager` seam). This week builds the **quorum certificate** — where `BftQuorum` (W119) and `SignedPeers` (W119) compose: a QC is 2f+1 verified votes aggregated into the opaque `Certificate` associated type (W91 O3). Bar (c). QC chaining (each block carries its parent's QC) is the backbone the W123 2-chain commit rule walks.

- [ ] [Venue · BFT] **Build (bar c)**: `crates/consensus-vsr/src/bft/qc.rs` — `Qc` formation (`form_qc(votes) -> Option<Qc>` requires `BftQuorum::reached` verified votes) + QC chaining (a proposed block references `parent_qc`), exposed as the `ConsensusCommit::certificate()` opaque `Certificate` (O3: callers never inspect its shape). (~total 150 min)
  - 🔧 **Why this matters**: the QC is the BFT analogue of VSR's "quorum of PrepareOk" — but where VSR counts acks (`Cred=()`), BFT **aggregates signatures** (`Cred=Signature`). This is the exact `Quorum × Authenticator` coupling W91 D7/O3 flagged: `Cert::from_votes(threshold, &[Signature])`. The trap O3 warned of: a `count: usize`-based certificate a BFT impl can't satisfy — the cert must hold the aggregated proof, opaque to call sites, so the outer trait's `Commit::certificate()` is impl-agnostic. Chaining trap: a block that doesn't carry a valid `parent_qc` (or carries one below the high-QC) must be rejected — else a Byzantine leader forks the chain.
  - *Contract* (signature-level): `pub struct Qc { view: u64, block: Digest, sigs: AggSig /* or Vec<(ReplicaId, Signature)> at N=4 */ } fn form_qc(&self, votes: &[Vote]) -> Option<Qc> { let v = votes.iter().filter(verified).count(); if self.quorum.reached(v, self.n) { Some(Qc{ .., sigs: aggregate(votes) }) } else { None } }` + `impl ConsensusCommit for BftCommit { fn certificate(&self) -> &Certificate { &self.qc } /* opaque */ }`. The `Certificate` type stays opaque at the outer-trait boundary (acceptance #9 / O3).
  - **Tests:**
    - `qc_forms_at_quorum_verified_votes` — `form_qc` returns `Some` at `BftQuorum::threshold(n)` *verified* votes, `None` below, and `None` if any vote is forged (uses `SignedPeers::verify_batch`, W119 — composes Quorum × Auth). Traps a count-only cert.
    - `qc_chaining_rejects_stale_parent` — a block whose `parent_qc` is below the replica's high-QC is rejected (ties to W120 high-QC monotonicity). Traps the chain-fork.
    - `certificate_is_opaque_at_trait_boundary` — `Commit::certificate()` returns an opaque handle; a call site (the W103 `use_consensus<B>` / ledger) can pass it on but the trait exposes no count/sig internals (O3: cert shape doesn't leak — re-asserts acceptance #9's "no CFT vocabulary on the outer trait").
    - `vsr_certificate_still_a_count` — the VSR `Replica` build's `Certificate` is still the cheap count-based proof (monomorphized); adding the BFT QC cert must not change the VSR cert representation (the seam is per-impl).
  - **Make-pass card (EXPERT · O3):** `certificate_is_opaque_at_trait_boundary` leaks a `sig_count` getter onto the outer trait → symptom: the ledger/W103 call site reads `cert.sig_count()`, coupling it to BFT; why: the cert exposed its shape; fix: `Certificate` is an associated type, opaque — callers hold it, never inspect; the conditional-(b) light-client verifier (if it ever fires) gets a *separate* verification method, not a field. cost avoided: re-coupling the engine to the consensus impl (defeats acceptance #9 / the W135 swap); muscle: a certificate crosses the trait boundary as an opaque proof, never as inspectable fields. Reapplies: W135 BFT swap, the conditional-(b) on-chain verifier.
  - **Output**: `bft/qc.rs` with `form_qc` + chaining + the opaque `Certificate`; tests green; VSR cert representation unchanged; O3 confirmed impl-only (cert opaque at the boundary).
- [ ] [Venue · BFT] Handoff note → `notes/122_bft_track.md`: QC formation + chaining shipped; the `Quorum × Authenticator → Certificate` coupling (O3) is impl-only/opaque. **Next W123: the Jolteon 2-chain commit rule** (a block commits when it + its direct child both have QCs → advances `commit_point()`; OpNumber densification O4; pipeline depth O2). (~10 min)
- [ ] Commit + log (~10 min)

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
