# Week 17 — `exec-vm` expansion (DOUBLES opcode coverage)

> **Phase**: 2
> **Month**: M5
> **Track**: Reth (primary), Tempo (awareness)
> **Hours target**: 30h/wk (M1-M18)
> **Mirror target**: revm-interpreter (opcode dispatch, instruction tables, Env)
> **Crate(s) touched**: exec-vm (v0.0.1 → v0.1.0)
> **Feeds into**: Phase 4 EVM execution; revm PR informed perspective
> **v2 modifications**: none (W15-W28 unchanged from v1)

**Crate extended**: exec-vm v0.0.1 → v0.1.0.

**Monday — ME ch13 part 1 + `Env` types + concurrency drill #25**

- [ ] **Concurrency drill #25 of 28 — Michael-Scott unbounded MPMC queue (the canonical lock-free queue)**
  (solo, ~6 hrs). MS queue is the most-cited lock-free FIFO. Pointer-linked nodes, separate head/tail
  CASs, dummy node at head. The interesting bug: a producer can CAS tail to a node whose `next` is still
  null (the next CAS hasn't happened yet) — and a consumer trying to dequeue must HELP the producer
  complete it. That's the helping pattern (drill #26 next).
  Build `crates/concurrent/src/msqueue.rs` — `MsQueue<T>` with `enqueue(v)` and `dequeue() -> Option<T>`
  using `epoch_gc::Atomic<Node<T>>` for memory reclamation (you have drills 23-24 behind you). Workload:
  4 producers + 4 consumers, fixed iteration count, assert FIFO and no lost items.
  Constraints: no peek at `crossbeam_queue::SegQueue` source, no peek at MS's 1996 paper *Simple, Fast, and
  Practical Non-Blocking and Blocking Concurrent Queue Algorithms* until COMPARE. Read W14's hazard-
  pointer work or W16's EBR for the memory reclamation strategy you'll use.
  - **Spec**: invariants for FIFO per-producer and no-lost / no-duplicate items. Predict the failure modes
    of the naive 2-step enqueue (CAS tail.next, then CAS tail): what if the second CAS hasn't happened yet
    when another producer arrives?
  - **Build**: enqueue + dequeue with EBR reclamation (drill #23-24). Single-thread smoke.
  - **Loom**: 2 producer + 2 consumer test, 8 total items, assert FIFO per-producer. Iterate orderings;
    max 3. Loom should expose the unset-tail-next case — when it does, you'll naturally derive the helping
    pattern (drill #26 makes it explicit).
  - **Compare**: open `crossbeam_queue::SegQueue` source. They use SEGMENTS (drill #11/12's SPSC ring
    applied at segment level) — NOT pure MS — to amortize node allocation. Why segments? What's the perf
    win vs pure MS?
  - **Symptom card**: bug shape (consumer dequeue sees tail with `next == null` — the producer hasn't
    finished); rule (a multi-CAS lock-free operation can leave inconsistent intermediate states; consumers
    must HELP, not retry); reapplies at: W26 WAL pool, W42 deadlock-detector wait-queue, drill #26.
  - **Numbers**: predict per-op CAS count (best 2, worst loops); cache-line touches per op (head +
    tail + node header + node.next ≈ 4 lines / op). Aggregate throughput at 4P+4C: ~5-15M op/sec.
  - **5-year failure mode**: under sustained heavy producer load, tail becomes a contention hotspot. The
    Vyukov-style bounded MPMC (drill #13) avoids this via per-slot seq. Trigger: throughput target
    exceeds MS's per-op cost budget. Fix: switch to bounded variant.
  - **Production handoff**: MS unbounded MPMC lands at W26 WAL pool (used for record batching),
    W42 deadlock-detector's wait-queue, drill #26 helping pattern next. The reclamation strategy you
    chose (hazp from #19 vs EBR from #23) silently was tested — if you chose neither, your loom test
    UAFed.
- [ ] ME ch13 first half. Memorize top 20 opcodes. (~60 min)
- [ ] **Build**: `crates/exec-vm/src/env.rs` — Env, BlockEnv, TxEnv, CfgEnv mirroring revm_primitives::Env. (~total 150 min across 3 phases)
  - 🔧 **Why this matters**: Env is the interpreter's outside-world parameterization. The 3-way split
    (cfg / block / tx) is load-bearing for W92 block-stm (cache-friendly re-execution) and Foundry
    (forked simulations).

  #### Phase 1 — Problem, requirements, design (~30 min)

  - **Requirements** — write in `crates/exec-vm/notes/17_env_design.md`:
    - R1. `Env { cfg: CfgEnv, block: BlockEnv, tx: TxEnv }` — three sub-envs.
    - R2. `CfgEnv { chain_id, spec_id, disable_balance_check, disable_base_fee, disable_block_gas_limit
      }`. Default = mainnet CANCUN.
    - R3. `BlockEnv { number, timestamp, gas_limit, coinbase, basefee: U256, difficulty: U256,
      prevrandao: Option<B256>, blob_excess_gas_and_price: Option<BlobExcessGasAndPrice> }`. Post-merge
      difficulty repurposed as RANDAO mixhash.
    - R4. `TxEnv { caller, gas_limit, gas_price, transact_to: TxKind, value, data: Bytes (Arc-backed),
      nonce, chain_id, access_list, gas_priority_fee, blob_hashes, max_fee_per_blob_gas }`.
    - R5. `effective_gas_price = min(max_fee, basefee + max_priority_fee)` for EIP-1559.
    - R6. `tx.gas_limit <= block.gas_limit` asserted at entry to `transact`.
    - R7. *Read first*: revm `crates/primitives/src/env.rs`.

  - **Design**:
    - D1. Why split Env? Walk through what changes per call (TxEnv per tx, BlockEnv per block, CfgEnv per
      chain).
    - D2. **Pre-mortem**: contributor `as u64`'s a U256 tx.value. What test catches?

  #### Phase 2 — Failing tests (~30 min)

  - [ ] `env_serde_round_trip_byte_identical` — every Env field serializes/deserializes losslessly.
  - [ ] `env_effective_gas_price_property` — property-test: monotone non-decreasing in max_priority,
    bounded above by max_fee.
  - [ ] `env_tx_gas_limit_boundary` — gas_limit == block.gas_limit accepted; +1 rejected.
  - [ ] `env_eip1559_underflow_returns_err` — basefee > max_fee → `Err(GasPriceLessThanBasefee)`, no
    panic.
  - [ ] `env_blob_fields_pre_cancun_none` — Shanghai blocks have `blob_excess_gas_and_price = None`.
  - [ ] `env_inheritance_ratio_one` — script-checked: field-for-field matches revm.

  #### Phase 3 — One failure at a time (~90 min)

  - **When `env_eip1559_underflow_returns_err` panics:**
    - Symptom: `basefee > max_fee_per_gas` triggers `basefee.saturating_sub(max_fee)`-style underflow.
    - Why: didn't validate at entry.
    - Fix: validate_tx returns `Err(InvalidTransaction::GasPriceLessThanBasefee)` BEFORE computing
      effective_gas_price.
    - Muscle: error type for known-invalid inputs is checked control flow, never panic. Reapplies at:
      every validate_* fn in exec-vm + W49 revm.

  - **When `env_serde_round_trip_byte_identical` shows U256 truncation:**
    - Symptom: tx.value > u128::MAX truncates silently.
    - Why: `as u64` on a U256.
    - Fix: type-check fields at construction; use `try_into` with explicit error.
    - Muscle: U256 → narrower types must use try_into, never `as`. Reapplies at: every U256 boundary
      in exec-vm + W49 revm.

  - **5-year failure mode** (`notes/17_env_followup.md`): when account abstraction makes TxEnv polymorphic
    (auth signatures, paymasters), the struct grows. Migration: `TxEnv` becomes generic over auth
    scheme.
- [ ] **Build**: `From<&TxEnvelope> for TxEnv`, `From<&Header> for BlockEnv`. (~total 90 min across 3 phases)
  - 🔧 **Why this matters**: bridges consensus layer to execution layer. Without these converters, every
    `execute_block` site has 30 lines of plumbing. Downstream: W53 Handler, W92 block-stm.

  #### Phase 1 — Requirements (~20 min)

  - R1. `TryFrom<&TxEnvelope> for TxEnv` — validation can fail, returns `InvalidTransaction`.
  - R2. `From<&Header> for BlockEnv` — total/infallible.
  - R3. Exhaustive match on all 5 tx types (Legacy, Eip2930, Eip1559, Eip4844, Eip7702). No wildcards.
  - R4. Type-1 access_list preserved; legacy always empty `vec![]`, never None.
  - R5. Type-3 blob_hashes non-empty; all 32-byte versioned (start with 0x01).
  - R6. Caller recovered upstream into RecoveredTx; From operates on that.

  #### Phase 2 — Failing tests (~20 min)

  - [ ] `tx_envelope_to_tx_env_exhaustive` — compile-fail when a new TxEnvelope variant is added but
    not matched.
  - [ ] `tx_envelope_to_tx_env_inheritance_ratio_one` — for all 5 types, output matches reth-evm
    fill_tx_env.
  - [ ] `header_to_block_env_post_merge_prevrandao` — Header with mix_hash != ZERO becomes
    `prevrandao: Some(mix_hash)`.
  - [ ] `header_to_block_env_blob_gas` — Header.excess_blob_gas populates BlobExcessGasAndPrice.
  - [ ] `tx_blob_hashes_versioned_prefix_check` — Type-3 with non-0x01-prefixed hash rejected.

  #### Phase 3 — One failure at a time (~50 min)

  - **When `tx_envelope_to_tx_env_exhaustive` compiles with wildcard:**
    - Symptom: contributor uses `_ => …` to handle EIP-7702; Prague-era tx replays panic.
    - Why: wildcard mask.
    - Fix: explicit match arm for each variant. Let compiler complain when new types ship.
    - Muscle: exhaustive match for known-set discriminators, never wildcard. Reapplies at: W53 Handler
      dispatch, W92 block-stm tx type handling.

  - **When caller-recovery borrows the envelope across an await:**
    - Symptom: lifetime errors when `recover_signer().await` borrows the envelope.
    - Why: From running over &TxEnvelope and recovering inside.
    - Fix: pre-recover into RecoveredTx upstream; From operates on owned RecoveredTx.
    - Muscle: async-await needs ownership, not borrows. Reapplies at: every async-bridge layer.

  - **5-year failure mode**: when account-abstraction tx types proliferate, TryFrom impls grow. Migration:
    macro-generated From per variant.
- [ ] Commit notes. (~10 min)

**Tuesday — ME ch13 part 2 + `instructions/system.rs`**

- [ ] ME ch13 second half. (~60 min)
- [ ] **Build**: `crates/exec-vm/src/instructions/system.rs` — RETURN, REVERT, INVALID, SELFDESTRUCT (skeleton — full
  impl Phase 4 needs journal). (~total 60 min across 3 phases)
  - 🔧 **Why this matters**: system opcodes encode terminal frame actions. INVALID must consume all gas;
    REVERT vs RETURN differ only by tag.

  #### Phase 1 — Requirements (~10 min)

  - R1. RETURN/REVERT: pop (offset, length); resize memory (with quadratic gas); copy to
    return_data_buffer; set tag.
  - R2. STOP: no-op, tag = Stop.
  - R3. INVALID: `gas.spend_all()`; tag = InvalidOpcode.
  - R4. SELFDESTRUCT skeleton: pop recipient; TODO journal (Phase 4).
  - R5. Macros `pop!`, `resize_memory!` charge gas before any state change.
  - R6. *Read first*: revm `instructions/system.rs` and `instructions/control.rs`.

  #### Phase 2 — Failing tests (~10 min)

  - [ ] `return_simple_bytecode_yields_value` — `0x60 0x42 0x60 0x00 0x52 0x60 0x20 0x60 0x00 0xF3` →
    return = `[0x00...42]` (32-byte word).
  - [ ] `revert_carries_payload` — REVERT with 4-byte payload; instruction_result == Revert; bytes match.
  - [ ] `invalid_consumes_all_gas` — gas before = 100; INVALID; gas after = 0.
  - [ ] `stop_consumes_no_gas` — STOP; gas unchanged.
  - [ ] `return_at_huge_offset_oog_before_tag` — RETURN at offset 2^16; OOG, tag not Return.

  #### Phase 3 — One failure at a time (~40 min)

  - **When `invalid_consumes_all_gas` reports partial:**
    - Symptom: later opcodes get free fuel.
    - Why: only base gas charged.
    - Fix: `interpreter.gas.spend_all()` inside INVALID.
    - Muscle: INVALID is total burn, not base cost. Reapplies at: W53 full opcode set, W49 revm.

  - **When `return_at_huge_offset_oog_before_tag` sets tag = Return:**
    - Symptom: gas charged AFTER state mutated (tag set).
    - Why: macro `resize_memory!` called after setting tag.
    - Fix: macro charges first; OOG short-circuits before tag set. Never call memory.resize directly.
    - Muscle: gas is a gate, never a receipt. Reapplies at: every opcode.

  - **5-year failure mode**: when Frame becomes async-aware (cancellation), tag setting needs to be
    atomic with state. Migration: TaggedFrameResult enum.
- [ ] Commit notes. (~10 min)

**Wednesday — evm.codes deep + `instructions/stack.rs` + concurrency drill #26**

- [ ] **Concurrency drill #26 of 28 — the helping pattern (consumer completes producer's half-step)** (solo, ~5 hrs).
  Drill #25 exposed the half-step. The fix is non-obvious: when consumer sees tail with `next != null`,
  the consumer ALSO does a CAS to move tail forward — taking over a step that "belongs to" the producer.
  This is the *helping pattern*, the lock-free generalization of mutual aid.
  Take drill #25's `MsQueue` and explicitly implement helping in both enqueue (when you see another producer's
  half-step, complete it before retrying) and dequeue (same). Re-verify under loom.
  Constraints: no peek at MS 1996 paper's "fast path / slow path" section or `crossbeam_queue::SegQueue`'s
  helper logic until COMPARE.
  - **Spec**: write out the exact ops a consumer must perform when it sees `tail.next != null`. Predict the
    correctness invariant ("any thread that observes the half-step is responsible for completing it") and
    the liveness invariant ("no thread can be blocked indefinitely waiting for another's step"). Note:
    helping converts non-blocking algorithms from obstruction-free to lock-free.
  - **Build**: extend drill #25 with helping. Re-run drill #25's loom harness; helping should not regress
    correctness AND should reduce loom's exploration time (fewer "blocked on half-step" branches).
  - **Loom**: same harness as drill #25, but now also assert: in any schedule where a consumer arrives
    after a producer's first CAS, the consumer's dequeue eventually succeeds (no waits-for chain). This
    is loom's `loom::sync::Notify`-like progress assertion.
  - **Compare**: open `crossbeam_queue::SegQueue::push` and `pop` source. They have segment-boundary
    helping (push helping advance segment, pop helping deallocate emptied segments). Note the parallel to
    your node-level helping. Then re-read MS's paper §3 — the "make tail point to last node" step.
  - **Symptom card**: bug shape ("producer stuck — consumer can't proceed because tail is stale"); rule
    ("helping is what makes a lock-free algorithm progress-guaranteed; without helping it's only
    obstruction-free"); reapplies at: W74 raft (followers help leader by completing leader's incomplete
    AppendEntries via state-transfer), W42 OCC abort cascades, W26 group commit ACKs.
  - **Numbers**: predict per-op helping CAS cost (one extra CAS per ~1% of ops at low contention; up to
    50% under burst). Predict loom exploration reduction with helping (~30-50%).
  - **5-year failure mode**: helping conflates "I'm helping" with "I'm doing my own work" — debuggers
    can't tell. Trigger: an obscure production bug in helping path. Fix: tracing-instrument helping
    distinctly.
  - **Production handoff**: helping lands at W74 raft (followers complete leader's incomplete
    AppendEntries via state-transfer — the canonical "helper completes a remote half-step"), W42 OCC
    abort cascades, W26 group commit's drain. W74's hardened acceptance includes a kill-leader-mid-burst
    scenario where helping is what makes recovery succeed.


- [ ] **Build**: `crates/exec-vm/src/instructions/stack.rs` — PUSH0..PUSH32, DUP1..DUP16, SWAP1..SWAP16, POP. All 96. (~total 105 min across 3 phases)
  - 🔧 **Why this matters**: 96 of 140 opcodes are stack manipulation. Hot-path determines macro
    throughput. Boxed `[U256; 1024]` is the canonical layout. Downstream: every opcode handler.

  #### Phase 1 — Requirements (~15 min)

  - R1. `Stack { data: Box<[U256; 1024]>, len: usize }`. Heap-allocated.
  - R2. `push_n::<N>` for N=1..=32; PUSH0 is separate handler.
  - R3. `dup_n::<N>` reads `data[len - N]`; `swap_n::<N>` swaps top with `data[len-1-N]`.
  - R4. Underflow / overflow checks via `require_stack!(N)` macro BEFORE unsafe access.
  - R5. Bytecode pre-padded with 32 zero bytes; PUSH32 at code-end zero-extends.
  - R6. PUSH0 gated on SpecId >= SHANGHAI; pre-Shanghai dispatches to invalid.
  - R7. Throughput ≥ 250 M ops/s single-threaded.

  #### Phase 2 — Failing tests (~15 min)

  - [ ] `stack_dup1_empty_underflow` — DUP1 on empty stack returns StackUnderflow (no panic).
  - [ ] `stack_push_at_1024_overflow` — 1024 pushes then 1 more → StackOverflow.
  - [ ] `stack_push32_at_code_end_zero_extends` — code = `[PUSH32, 0x01, 0x02]`; PUSH32 reads 30 zero
    bytes; pushes `0x0102_00...00`.
  - [ ] `stack_push0_pre_shanghai_invalid` — code = `[0x5f]` at Berlin → InvalidOpcode; at Shanghai →
    pushes 0.
  - [ ] `stack_box_avoids_stack_overflow_at_depth` — 1024-deep CALL chain doesn't SIGSEGV.
  - [ ] `stack_throughput_250m_per_sec` — bench.

  #### Phase 3 — One failure at a time (~75 min)

  - **When `stack_dup1_empty_underflow` panics or UB:**
    - Symptom: debug panics; release UB via get_unchecked.
    - Why: missing len-check.
    - Fix: `require_stack!(interp, 1)` macro early-returns StackUnderflow. Then get_unchecked is safe.
    - Muscle: every unsafe access guarded by an explicit precondition check. Reapplies at: W53.

  - **When `stack_push32_at_code_end_zero_extends` panics:**
    - Symptom: bytecode index OOB.
    - Why: PUSH32 reads `pc+1..pc+33`; code ends at `pc+3`.
    - Fix: pre-pad bytecode with 32 zero bytes at load (revm pattern).
    - Muscle: input invariants enforced at load, not per-read. Reapplies at: W15 EOF parser.

  - **When `stack_push0_pre_shanghai_invalid` pushes 0 on Berlin:**
    - Symptom: pre-Shanghai contracts with `0x5f` byte execute as PUSH0; should INVALID.
    - Why: jump table doesn't gate on spec_id.
    - Fix: at frame entry, swap PUSH0 handler with invalid based on `cfg.spec_id`.
    - Muscle: hard-fork-gated opcodes via jump-table swap. Reapplies at: every Shanghai+/Cancun+ opcode.

  - **When `stack_box_avoids_stack_overflow_at_depth` SIGSEGVs at depth ~10:**
    - Symptom: 32 KiB stack frame per recursive CALL exhausts host stack.
    - Why: `[U256; 1024]` inline.
    - Fix: `Box<[U256; 1024]>` — heap once per frame.
    - Muscle: large fixed buffers go on heap when the wrapper lives on the host stack. Reapplies at:
      every recursive frame.

  - **5-year failure mode**: when 1024 depth proves too narrow (account abstraction nests deeper),
    bump or remove. Migration: parameterize MAX_DEPTH.
- [ ] Manual trace simple bytecode through interpreter. (~30 min)
- [ ] [Tempo] 30 min at end of day: read `tempoxyz/tempo` repo top-level Cargo.toml. Note which reth-* and revm-* crates
  it pins. Confirm: every Reth crate you've been mirroring is also depended on by Tempo. Add 3-line note to
  tempo_orientation.md. (~30 min)
- [ ] Commit + log (~10 min)

**Thursday — `instructions/contract.rs` (CALL family)**

- [ ] **Build**: `crates/exec-vm/src/instructions/contract.rs` — CALL, CALLCODE, DELEGATECALL, STATICCALL. EIP-150
  63/64ths gas. (~total 105 min across 3 phases)
  - 🔧 **Why this matters**: CALL is the only opcode that allocates a new frame. EIP-150 63/64ths prevents
    stack-exhaustion attacks. CPS-style frame management (no Rust recursion) is the load-bearing pattern;
    W92 block-stm uses frame boundary as parallelism unit.

  #### Phase 1 — Requirements (~15 min)

  - R1. `gas_for_callee = min(local_limit, gas_left - gas_left/64)` post-Tangerine.
  - R2. STATICCALL sets is_static=true; child rejects SSTORE/LOG/CREATE/SELFDESTRUCT.
  - R3. DELEGATECALL preserves caller+value from parent; storage targets parent's address.
  - R4. CALL with value>0 grants 2300 gas stipend (and value!=self).
  - R5. CPS-style: emit `InterpreterAction::Call`; outer loop spawns new frame. NEVER recurse in Rust.
  - R6. Cold-access surcharge (EIP-2929): cold=2600, warm=100. Host returns is_cold flag.

  #### Phase 2 — Failing tests (~15 min)

  - [ ] `call_63_64ths_cap` — local_limit=10000, gas_left=1000; effective=floor(1000 - 1000/64)=985.
  - [ ] `call_no_recursion_at_depth_1024` — 1024-deep CALL doesn't SIGSEGV.
  - [ ] `staticcall_rejects_sstore` — STATICCALL → child runs SSTORE → child halts with StateChange.
  - [ ] `delegatecall_writes_to_parent_storage` — DELEGATECALL; callee SSTORE; parent's slot mutated.
  - [ ] `call_with_value_grants_2300_stipend` — value > 0; gas_for_callee includes +2300.
  - [ ] `call_cold_account_charges_2600` — first call to addr; cold cost.

  #### Phase 3 — One failure at a time (~75 min)

  - **When `call_no_recursion_at_depth_1024` SIGSEGVs:**
    - Symptom: deep CALL chain blows host stack.
    - Why: `call_inner(...)` recurses in Rust.
    - Fix: CPS. Emit `InterpreterAction::Call { inputs }`. Set tag = CallOrCreate. Outer loop pops the
      action and spawns a new Frame on the heap.
    - Muscle: lock-free is "no holding a Rust call frame across mutable state". Reapplies at: W53
      Handler, W92 block-stm.

  - **When `call_with_value_grants_2300_stipend` shows callee OOG:**
    - Symptom: a legitimate ETH transfer fails.
    - Why: stipend forgotten.
    - Fix: `if value > 0 && to != self { gas_for_callee += 2300 }`.
    - Muscle: EIP-150 has a non-monotone stipend rule; encode it as a special-case at the call site.

  - **When `staticcall_rejects_sstore` mutates parent state:**
    - Symptom: child's SSTORE leaks to parent.
    - Why: child's journal frame not reverted on the static violation.
    - Fix: child's frame gets its own journal checkpoint; static violation reverts on
      InstructionResult::StateChangeDuringStaticCall.
    - Muscle: child frame state changes only commit on clean return. Reapplies at: W50 journaling.

  - **5-year failure mode**: when account-abstraction calls (4337/7702 paymaster) extend the call protocol,
    inputs need polymorphism. Migration: trait-object CallInputs.
- [ ] **Build**: `crates/exec-vm/src/instructions/create.rs` — CREATE, CREATE2 with init code analysis. (~total 105 min across 3 phases)
  - 🔧 **Why this matters**: CREATE/CREATE2 produce new bytecode at runtime. EIP-3860 size limit, EIP-3541
    0xEF rejection, EIP-1014 CREATE2 address scheme — each bug = consensus split.

  #### Phase 1 — Requirements (~15 min)

  - R1. CREATE: pop (value, offset, len); CREATE2: + salt.
  - R2. init_code_size ≤ MAX_INITCODE_SIZE = 49,152 (Shanghai). Reject otherwise.
  - R3. Init-code gas charge: `2 * ceil(len / 32)` post-Shanghai, 0 before.
  - R4. CREATE addr = `keccak256(rlp(sender, nonce))[12..]`. CREATE2 addr = `keccak256(0xff || sender ||
    salt || keccak(init_code))[12..]`.
  - R5. Nonce bumps BEFORE address derivation.
  - R6. EIP-3541: post-deploy, if `returned_code[0] == 0xEF` and spec_id >= LONDON, revert.
  - R7. Collision: if address has code, revert without burning extra gas.
  - R8. CPS-style action emission (like CALL).

  #### Phase 2 — Failing tests (~15 min)

  - [ ] `create_address_eip_1014_vectors` — 4 official `(sender, salt, init_code) → address` triples.
  - [ ] `create_init_code_size_boundary` — len=49152 accept; len=49153 reject with InitCodeSizeLimit.
  - [ ] `create_0xEF_prefix_rejected_post_london` — bytecode starting `0xEF` on London → revert.
  - [ ] `create_0xEF_accepted_pre_london` — same on Berlin → accept.
  - [ ] `create_nonce_bump_before_address` — record nonce; CREATE; assert address computed from
    pre-bump nonce + 1 (i.e. bump happened first).
  - [ ] `create_collision_reverts_without_burning_gas` — addr already has code; CREATE reverts; gas not
    fully consumed.

  #### Phase 3 — One failure at a time (~75 min)

  - **When `create_nonce_bump_before_address` computes from old nonce:**
    - Symptom: wrong CREATE address; mainnet replay diverges silently.
    - Why: bumped nonce AFTER derivation.
    - Fix: `host.inc_nonce(sender)` before address compute.
    - Muscle: order-dependent state mutations are spec-defined — copy exactly.

  - **When `create_0xEF_prefix_rejected_post_london` accepts:**
    - Symptom: post-London replay accepts a contract that mainnet rejects.
    - Why: missing EIP-3541 check.
    - Fix: post-deploy hook gated on `spec_id >= LONDON`.
    - Muscle: deploy-time validation gated by fork. Reapplies at: every EIP that bans/restricts
      bytecode shapes (EOF will add more).

  - **When `create_collision_reverts_without_burning_gas` burns all gas:**
    - Symptom: collision case wastes gas the caller expected to keep.
    - Why: revert path goes through gas.spend_all() incorrectly.
    - Fix: collision is a structured revert; only the CREATE base cost burns. Caller keeps the rest.
    - Muscle: per-failure-mode gas accounting; not all reverts are equal. Reapplies at: W50 journaling.

  - **5-year failure mode**: when EOF replaces CREATE/CREATE2 with EOFCREATE, this whole module is
    legacy. Migration: gate behind `#[cfg(legacy_evm)]`.
- [ ] Test: simple call-with-return via two hand-rolled programs. (~30 min)
- [ ] Commit + log (~10 min)

**Friday — `instructions/host.rs` extension against `StateCache` + concurrency drill #27**

- [ ] **Concurrency drill #27 of 28 — linearization-point migration across a CAS chain** (solo, ~5 hrs).
  Drill #25's MS queue had one obvious LP — the tail's CAS. Real lock-free data structures often have LPs
  that *move* depending on which CAS succeeds. The skiplist insert is the canonical example: level-0 CAS
  is the LP, but only if higher-level CASes also succeed; otherwise the LP retroactively shifts.
  Build `crates/concurrent/src/skiplist_mini.rs` — a SIMPLIFIED skiplist (just 2 levels, integer keys, no
  delete). Insert does CAS at level 0 then level 1. Question: where's the LP? Answer differs based on the
  schedule.
  Constraints: no peek at Herlihy-Shavit Ch 9, no peek at `crossbeam_skiplist` source until COMPARE.
  - **Spec**: define LP rigorously. State what it means for the LP to "move": a concurrent reader observing
    the insert might see different snapshots depending on when they ran. Predict 3 schedules: (a) both
    CASes succeed without interleaved reads — LP is level-0; (b) level-0 succeeds, level-1 fails, reader
    arrives between — LP is level-0 but reader sees a partial insert; (c) level-0 succeeds, reader arrives,
    level-1 fails — does reader's observation count as observing the insert?
  - **Build**: 2-level skiplist with explicit CAS sequence; instrument each CAS with a tracing event.
  - **Loom**: 2 inserters + 2 readers test. Asserts: every reader that observes the inserted key at level
    0 must eventually be able to observe the FULL insert (level-1 link). This is the "any observation
    forces completion" invariant — the helping pattern again (drill #26).
  - **Compare**: open `crossbeam_skiplist::SkipList::insert` source. Their LP discussion + helping for
    incomplete inserts. Then read Herlihy-Shavit Ch 9.7 — the formal LP definition + the "external
    linearization" technique.
  - **Symptom card**: bug shape (reader observes partial insert, no helping fires → reader's observation
    "doesn't count" → liveness violation in upper-level code); rule (LP is per-schedule; any reader that
    observes a half-step is obligated to complete it OR retry); reapplies at: W37 skiplist (the real build),
    W42 OCC commit/abort, W74 raft Prepare/Commit split.
  - **Numbers**: predict the LP-shift probability under concurrent insert load (~5-10% of inserts will
    have a reader observe partial state under high concurrency). Predict the helping CAS cost: same as
    drill #26.
  - **5-year failure mode**: complex multi-level data structures (skiplist with N levels) compound LP
    complexity exponentially. Trigger: needing a 32-level skiplist for petabyte data. Fix: limit
    structural complexity OR formally verify LP invariants (TLA+ style).
  - **Production handoff**: LP-aware design lands at W37 skiplist (every insert/delete has 2-3 CAS
    chains with shifting LPs), W42 OCC commit-vs-abort timing, W74 raft Prepare-vs-Commit boundary
    (the LP for "this op is committed" shifts based on which quorum member observed first). W37's
    hardened acceptance silently tests LP correctness via concurrent-reader determinism property test.


- [ ] **Build**: extend host.rs with BALANCE, EXTCODESIZE, EXTCODEHASH, EXTCODECOPY, BLOCKHASH, COINBASE, TIMESTAMP,
  NUMBER, DIFFICULTY/PREVRANDAO, GASLIMIT, CHAINID, SELFBALANCE, BASEFEE, BLOBHASH, BLOBBASEFEE. (~total 90 min across 3 phases)
  - 🔧 **Why this matters**: Host trait is the seam between pure interpreter and outside world. Several
    fork-gated opcodes (PREVRANDAO/DIFFICULTY collision, BLOBBASEFEE pre-Cancun) and EIP-2929 cold/warm
    accounting traps.

  #### Phase 1 — Requirements (~20 min)

  - R1. Every state-read returns `(value, is_cold)`. EIP-2929: cold=2600, warm=100.
  - R2. BLOCKHASH valid range = `[current-256, current-1]`; outside returns B256::ZERO.
  - R3. Env-only opcodes (COINBASE, TIMESTAMP, etc.) route through `interp.env()`, no Host call.
  - R4. EXTCODEHASH of EOA returns `keccak256(b"")`, NOT zero (EIP-1052 corner).
  - R5. BLOBBASEFEE gated on spec_id >= CANCUN; pre-Cancun dispatches to invalid.
  - R6. EXTCODECOPY charges memory expansion + `3 * ceil(len/32)` copy cost.

  #### Phase 2 — Failing tests (~20 min)

  - [ ] `host_cold_balance_charges_2600` — first BALANCE on addr; gas -= 2600.
  - [ ] `host_warm_balance_charges_100` — second BALANCE; gas -= 100.
  - [ ] `host_blockhash_out_of_range_zero` — BLOCKHASH(current-300) returns 0.
  - [ ] `host_extcodehash_of_eoa_is_keccak_empty` — EOA's EXTCODEHASH = `keccak256(b"")`.
  - [ ] `host_blobbasefee_pre_cancun_invalid` — Shanghai + opcode 0x4A → InvalidOpcode.
  - [ ] `host_extcodecopy_charges_memory_and_copy` — gas decreased by memory expansion + copy cost.

  #### Phase 3 — One failure at a time (~50 min)

  - **When `host_extcodehash_of_eoa_is_keccak_empty` returns 0:**
    - Symptom: contracts probing via EXTCODEHASH(eoa) see 0; should see keccak(b"").
    - Why: code_hash returns ZERO for empty-code accounts.
    - Fix: host's code_hash returns keccak(b"") for EOA/empty-code; only B256::ZERO for nonexistent
      accounts per EIP-1052.
    - Muscle: spec-defined sentinel values are NOT zero. Reapplies at: every "what does this return for
      missing X" question.

  - **When `host_blobbasefee_pre_cancun_invalid` runs as no-op:**
    - Symptom: opcode 0x4A on Shanghai pushes some value.
    - Why: jump table doesn't gate 0x4A on spec.
    - Fix: at frame entry, jump-table swap 0x4A → invalid for spec_id < CANCUN.
    - Muscle: fork-gated opcodes via jump-table swap. Reapplies at: every Cancun+ opcode.

  - **5-year failure mode**: when account abstraction adds `AUTH`/`AUTHCALL` host methods, trait grows.
    Migration: `HostExtension` trait composing on Host.
- [ ] All routed through Host trait → eth-storage-cache::StateCache. (~30 min)
- [ ] Commit + log (~10 min)

**Saturday — `instructions/log.rs` + ethereum-tests subset green + concurrency drill #28 (capstone)**

- [ ] **Concurrency drill #28 of 28 — capstone integration: build a production-shaped MPMC throughput rig**
  (solo, ~8 hrs spread over Sat-Sun). The capstone integrates everything: drill #13's Vyukov MPMC, drill
  #14's sharded credits, drill #15's adaptive Backoff, drill #16's CachePadded, drill #19's hazard
  pointers (OR drill #23-24 EBR — your choice), drill #25-26's helping pattern, drill #27's LP analysis.
  Build `crates/concurrent/benches/mpmc_throughput_rig.rs` — a sustained-load microbench that runs
  variants of all your foundational primitives at scale and lets you compare against `crossbeam_channel::
  bounded`. Goal: your version within 30% of crossbeam's throughput, OR you discover what crossbeam does
  that you don't.
  Constraints: no direct copying from `crossbeam_channel` — your code is yours. But READING crossbeam
  source IS allowed (it's the COMPARE phase from earlier drills, but now systematic).
  - **Spec**: write a single `notes/17_capstone.md` listing every concurrency rule you've internalized
    across drills #1-27. For each, name the primitive you'd use it in. This is the artifact you'll grep
    when an HFT bug feels familiar in W26/W74/W77.
  - **Build**: assemble the rig. Two channels: (a) yours (Vyukov + sharded credits + adaptive Backoff +
    CachePadded + EBR), (b) `crossbeam_channel::bounded(N)`. Same workload: 4 producers + 4 consumers,
    8 byte payloads, 10 second sustained run, ops/sec + p50/p99/p99.9 latency.
  - **Loom**: not the tool — the capstone is a perf comparison. But: re-run drill #13's loom test on your
    finalized Vyukov to confirm no regression.
  - **Compare**: at the end, write `notes/17_capstone_compare.md`: where you matched crossbeam, where you
    didn't, what crossbeam knows that you don't (yet). The "yet" is your continuing work.
  - **Symptom card**: write your *meta-symptom card* — the rule for HOW you debug concurrency now. Three
    sentences: (a) the first place you look when given a concurrent bug; (b) the question you ask before
    writing any fix; (c) the tool order you reach for (loom? perf? thinking?). This is the artifact you
    re-read at the start of every concurrent build for the rest of the plan.

  **Completion criterion**: throughput within 30% of `crossbeam_channel`; symptom card committed; you can
  now read W11/W26/W37/W42/W74/W77/W82 concurrent Build lines and pattern-match each bug to one of these
  28 drills BEFORE looking at the spec. That's the muscle.

  **Production-recognition exit gate (the real-task graduation, not homework)**: prepare
  `tests/synthetic_pr_review.rs` — a 50-line Rust patch you'll write yourself that LOOKS like a plausible
  feature contribution to one of your crates (matching-engine, WAL, or bufpool — pick one). Embed 3
  latent concurrency bugs drawn from drills #1–27. NO comments naming the bugs; NO test labels giving them
  away. Make the patch read like real code a colleague would submit. Then:
    1. Take a 24-hour break (away from the file).
    2. Open the file cold. Set a 10-minute timer. Read it like a PR you've been asked to review.
    3. Spot the 3 bugs without prompts. For each: name the drill # it corresponds to, the schedule that
       triggers it, the fix.
    4. If you spot all 3 in 10 min → you have the reflex. You can confidently approach W26/W42/W74.
    5. If you miss any → those drill numbers are the ones whose pattern hasn't taken root yet. Redo just
       the SYMPTOM CARD (not the whole drill) for each missed pattern, then re-try the PR review with
       fresh 3 bugs.

  **Why this works**: PR review is exactly the muscle you'll exercise 5× per week at any HFT firm. The
  gate isn't "remember the rule"; it's "spot the rule's absence in plausible-looking code." That's
  production-reflex, the only thing that prevents you shipping a race condition at 3am.

  **Numbers (capstone)**: predict your rig's throughput within 30% of crossbeam BEFORE running the bench.
  Predict p99.9. After bench: if you missed by >30%, the gap names the primitive you didn't fully
  internalize.
  **5-year failure mode (capstone)**: name 3 things that, if they change in the next 5 years (core count,
  memory model on a new ISA, workload pattern, cache topology, allocator implementation), would force
  rewrites somewhere in your rig. State which primitive in your rig is most fragile.
  **Production handoff (capstone)**: this rig IS the foundation for W11 backpressure + W26 WAL + W42 txn
  + W74 matching-engine. Every later production-crate hardened acceptance silently requires you to spot
  the decomposition and pull from drills 1–27. Drill #28 IS the bridge.



- [ ] **Build**: `crates/exec-vm/src/instructions/log.rs` — LOG0..LOG4. (~total 105 min across 3 phases)
  - 🔧 **Why this matters**: LOGs affect receipts root + bloom filter — consensus-critical even though they
    don't affect state root. Order-of-operations (static check FIRST, then pop, then gas) matters.

  #### Phase 1 — Requirements (~15 min)

  - R1. `log<const N: usize>` generic; 5 monomorphic entries (LOG0..LOG4).
  - R2. `check_staticcall!` is the FIRST statement; STATICCALL → StateChangeDuringStaticCall.
  - R3. Gas formula: `375 + N*375 + len*8`.
  - R4. Pop all stack args BEFORE memory/gas charge.
  - R5. Logs journal per-frame; revert on frame failure.

  #### Phase 2 — Failing tests (~15 min)

  - [ ] `log0_empty_data_charges_375` — LOG0 with offset=0, len=0; gas -= 375.
  - [ ] `log2_1kib_data_charges_correctly` — LOG2 with 1024-byte data; gas -= 375 + 750 + 8*1024.
  - [ ] `log_in_staticcall_rejected` — STATICCALL → LOG1; child halts with StateChangeDuringStaticCall.
  - [ ] `log_static_check_first_no_gas_burned` — static-violation case; gas NOT charged for log cost.
  - [ ] `log_reverted_on_frame_failure` — frame REVERTs; parent doesn't inherit child's logs.

  #### Phase 3 — One failure at a time (~75 min)

  - **When `log_static_check_first_no_gas_burned` shows partial gas burn:**
    - Symptom: static violation but gas charged for memory resize and log cost.
    - Why: check_staticcall after pops/gas charge.
    - Fix: `check_staticcall!` is statement #1 in the handler body.
    - Muscle: precondition checks ORDER before state changes; static violation is precondition.

  - **When `log_reverted_on_frame_failure` shows logs in parent:**
    - Symptom: child fails; parent's receipts root includes child's logs.
    - Why: logs propagated unconditionally.
    - Fix: per-frame `Vec<Log>`. Parent inherits child's logs ONLY on clean return (commit). On revert,
      drop child's logs.
    - Muscle: state changes journal per-frame; commit on success, drop on revert. Reapplies at: W50
      journaling.

  - **5-year failure mode**: when log indexing moves off-chain (Reth's static-files), LOG semantics stay
    but storage shifts. Migration: separate consensus log (bloom) from query log (extension).
- [ ] Total opcode count: 60+. Pass GeneralStateTests/stArithmetic + stMemoryTest subsets. (~30 min)
- [ ] Tag `exec-vm v0.1.0`. README documents opcode coverage matrix. (~5 min)
- [ ] Commit + log (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
