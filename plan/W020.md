# Week 20 — `eth-trie` expansion

> **Phase**: 2
> **Month**: M5
> **Track**: Reth (primary), Tempo (awareness)
> **Hours target**: 30h/wk (M1-M18)
> **Mirror target**: alloy-trie (`BranchNodeCompact`, `TrieWalker`, `ProofRetainer`, `HashBuilder`, `Nibbles`, `verify_proof`) + reth_trie (`StateRoot`, `StorageRoot`, `TrieUpdates`)
> **Crate(s) touched**: eth-trie (v0.1 → v0.2)
> **Bar**: (b)   ← Reth-track mirror crate (alloy-trie/reth-trie). Correctness-focused: property + fuzz + criterion + perf-regression CI. Not on the bar-(c) HFT/Layer-1 list.
> **Feeds into**: W22 `eth-stage`::MerkleStage (drives walker + StateRoot for staged sync); W23-24 storage-trie scaffold; W27+ storage-trie::mdbx (implements both storage traits atop MDBX); W31 persistent MPT (serializes BranchNodeCompact to MDBX); W44 storage-trie v1.0; W91 consensus-engine; W92 block-stm (validates parallel exec by comparing state roots); Phase 5 RPC `eth_getProof`.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): ethereum.org MPT docs + yellow paper Appendix C/D (nibble order, RLP node forms); EIP-1186 (`eth_getProof` proof shape); reth source `crates/trie/trie/src/nodes/branch.rs`, `walker.rs`, `proof/retainer.rs`, `hash_builder/mod.rs` (open ONLY at Phase 3, never before); alloy-trie `BranchNodeCompact`/`Nibbles`; EIP-6800 Verkle (5-year follow-ups only).
> **v2 modifications**: none (W15-W28 unchanged from v1)

**Crate extended**: eth-trie v0.1 → v0.2. Six Build/Refactor arcs ship the incremental state-root engine: `BranchNodeCompact` (compact node form) → `TrieStorage` split + `CachedStorage` → `TrieWalker` (streaming cursor) → `ProofRetainer` + `verify_proof` → `StateRoot` orchestrator → `StorageRoot`. The inheritance milestone is `mainnet_block_1_reconstruction` on Friday: byte-identical to mainnet `header.state_root` or your node forks.

**Three trap shapes this week** (one Build each carries the named shape; all six carry tiers + ordered cards):
- **Cross-primitive trap**: Friday `StateRoot` composes `TrieWalker` (Wed) + `ProofRetainer` (Thu) + `BranchNodeCompact` (Mon) — each green in isolation; the *composition* (`insert_order_does_not_affect_root`, `walker_clean_subtree_uses_cached_hash`) is where a reversed nibble walk or a stale hash-builder field fires.
- **Anti-shortcut trap**: Monday `compact_encode` "just allocate a fresh `Vec<u8>` per call" passes correctness, fails the throughput bar (`bench_round_trip_5m_per_sec`); Thursday `retain` "just `targets.iter().any(starts_with)`" passes, fails `bench_100_targets_under_5ms`.
- **Negative-control test-the-test**: each perf bench carries a paired `*_negative_control` that pins the measured number into a physically-plausible band so a bench that measures nothing fails loudly instead of passing green.

**Monday — MPT deeper theory + `BranchNodeCompact`**

- [ ] **Build**: `crates/eth-trie/src/branch_compact.rs` — BranchNodeCompact mirroring `alloy_trie::nodes::BranchNodeCompact`. (~total 140 min across 3 phases)
  - 🔧 **Why this matters**: a naive MPT branch node carries 16 child hashes (16 × 32 = 512 bytes). 90%+ of
    branches in real Ethereum state are sparse (≤4 children). `BranchNodeCompact` — a bitmask-indexed
    representation that stores only present children — cuts intermediate-trie storage by ~4×. This single
    representation choice dominates trie-db size. The two specific traps: (1) **nibble ordering** (MSB/low-bit-first
    iteration — reverse it and every state root is silently wrong), and (2) **wire-vs-DB form confusion**
    (RLP for proofs, compact for MDBX — mix them and proofs fail to verify). First-attempt impls fail ≥1 test
    below — the debug session IS the lesson. Downstream consumers: W20 ProofRetainer (Thu, holds these),
    W22 MerkleStage (reads/writes from DB), W31 persistent MPT (serializes to MDBX as the intermediate-trie table).

  #### Phase 1 — Problem, requirements, design (~40 min, no code yet)

  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/eth-trie/notes/20_branch_compact_prediction.md`:
    - P1: the exact import line + container you'll reach for to store the hashes (predict: `smallvec::SmallVec<[B256; 4]>`).
    - P2: predict the compact-form byte size for a 3-child branch (3 × u16 masks + len-prefix + 3 × 32-byte hashes).
    - P3: predict the index into `hashes` for nibble 5 given `hash_mask = 0b0000_0000_0010_1100` (the popcount formula).
    - P4: predict whether a naive `(state_mask >> i) & 1` loop starting at `i = 15` yields nibbles in correct order.
    - P5: **list every Phase-2 test you predict your first attempt will fail.** Compare to reality after tests run.

  - *Mental model*: `BranchNodeCompact` is the space-optimized DB form of an MPT branch — three 16-bit bitmasks
    say "which of the 16 child slots are present / stored-in-DB / hashed", and a packed `SmallVec` holds only the
    present hashes. It exists because storing 16 fixed slots wastes ~80% of bytes across ~700M intermediate nodes.

  - *Contract*: the signature-level interface — NEVER the solution body:
    ```rust
    pub struct BranchNodeCompact {
        pub state_mask: TrieMask,  // newtype around u16
        pub tree_mask: TrieMask,
        pub hash_mask: TrieMask,
        pub hashes: SmallVec<[B256; 4]>,
        pub root_hash: Option<B256>,
    }
    impl BranchNodeCompact {
        pub fn try_new(state: u16, tree: u16, hash: u16, hashes: SmallVec<[B256; 4]>) -> Result<Self, BranchError>;
        pub fn iter_children(&self) -> impl Iterator<Item = (u8, ChildSlot)> + '_;
        pub fn set_child(&mut self, nibble: u8, child: ChildRef);
        pub fn rlp_encode(&self, out: &mut dyn BufMut);          // 17-element wire form
        pub fn compact_encode(&self, out: &mut dyn BufMut);      // masks + packed hashes, DB form
        pub fn compact_decode(buf: &[u8]) -> Result<Self, BranchError>;
    }
    ```

  - *Back-of-envelope*: 16-slot branch = 512 B; compact 3-child branch = 3×2 (masks) + 1 (len) + 3×32 = 103 B
    → ~5× smaller. Across 700M intermediate nodes: 512 B × 700M ≈ 358 GB vs 103 B × 700M ≈ 72 GB. The codec runs
    on every state-root walk; a per-call `Vec<u8>` alloc (~50 ns malloc) × 100M nodes/recompute = ~5 s of pure
    alloc. Cliff ratio: caller-supplied `BufMut` (0 allocs) vs fresh-`Vec`-per-call (~50 ns each) ≈ the difference
    between a 200 ns/node and an 800 ns/node round trip — 4× on the hottest path in the crate.

  - **Requirements** — write in `crates/eth-trie/notes/20_branch_compact_design.md`:
    - R1. Struct with `state_mask: u16`, `tree_mask: u16`, `hash_mask: u16`, `hashes: SmallVec<[B256; 4]>`,
      `root_hash: Option<B256>`.
    - R2. Invariant 1: `hash_mask.count_ones() == hashes.len()`.
    - R3. Invariant 2: `tree_mask & !state_mask == 0` (tree-bit must be a subset of state-bit).
    - R4. Invariant 3: `hash_mask & !state_mask == 0` (hash-bit must be a subset of state-bit).
    - R5. Invariant 4: constructor `try_new(state, tree, hash, hashes)` rejects any tuple violating R2/R3/R4
      with a typed error; never panics on malformed input.
    - R6. RLP form (wire/proof representation): 17-element list — 16 child slots + value slot. Empty slots
      encode as `0x80` (RLP empty string).
    - R7. Compact form (DB representation): three u16 masks (little-endian) + length-prefixed Vec<B256>.
      Compact form is what hits MDBX; RLP form is what proofs ship.
    - R8. `iter_children` yields `(nibble: u8, ChildSlot)` in **MSB-first nibble order** (slot 0 first, slot
      15 last).
    - R9. `set_child(nibble, ChildRef)` maintains all three masks consistently — if you set a child, its
      `state_mask` bit MUST flip on; if you set its hash, `hash_mask` flips on.
    - R10. Encode → decode → encode is byte-identical on the compact form (canonical encoding; no
      ambiguity).

  - **Design walk-through** — answer each in your design doc; no peeking at
    `reth/crates/trie/trie/src/nodes/branch.rs` until Phase 3:
    - D1. The 3 masks (`state`, `tree`, `hash`) have a containment relationship — write it out as a Venn
      diagram. Which mask is always the largest? What does it mean if a bit is in `state` but not in `tree`?
      (Hint: that child exists but is *embedded* — inlined into the parent, not stored separately in DB.)
    - D2. The `hashes` field stores hashes "in nibble order, only for bits set in hash_mask". Write the
      indexing formula: given `nibble = 5` and `hash_mask = 0b0000_0000_0010_1100`, what's the index into
      `hashes`? (Hint: `popcount(hash_mask & ((1 << nibble) - 1))`.)
    - D3. The 17th RLP slot — the "value at this branch" slot — exists per yellow paper but is almost
      always empty in modern Ethereum (accounts terminate at leaf nodes, not branch nodes). Why is it
      still required in RLP encoding? What happens if you omit it?
    - D4. Embedded children: a child whose RLP encoding is < 32 bytes is inlined into the parent's RLP
      (not hashed). The threshold is **strictly less than 32**, not `<=`. Why? (Hint: think about what a
      hash IS — keccak256 output is 32 bytes; embedding anything 32 bytes or larger would conflict with
      hash-vs-inline disambiguation.)
    - D5. `SmallVec<[B256; 4]>` inline capacity: 90% of branches have ≤4 hashes after compaction. Bump
      the inline size to 16 — what happens to memory? (Hint: `B256 = 32 B`, so `[B256; 16] = 512 B` on
      the stack per branch node; cache thrash.) Justify the choice of 4.
    - D6. **Anti-shortcut audit**: name the "obvious" reach for each motivation and one line on why it's
      wrong HERE: (a) `Vec<B256>` instead of `SmallVec` (heap alloc on every small branch — 90% of branches);
      (b) `compact_encode(&self) -> Vec<u8>` returning an owned buffer (fresh alloc per node — fails the
      throughput bar, see Back-of-envelope); (c) `HashMap<u8, B256>` for children "for convenience" (loses
      nibble order — R8 dies, state root wrong). Each is correct-looking and bar-fatal.
    - D7. **Pre-mortem (load-bearing)**: write the bad-input scenario in 4-6 steps where a malformed
      compact-form blob (e.g. `hash_mask` bits don't match `hashes.len()`) gets decoded without error and
      produces a `BranchNodeCompact` that later state-root computation reads with the wrong child hashes —
      yielding a silent consensus fork. If you cannot write the scenario, your `try_new`/`decode` is missing
      the invariant guard.

  - 🧮 **Paper drill** — write in `notes/20_branch_compact_drill.md`:
    - D-LAYOUT on a 3-child compact blob (`state_mask=0b0000_0000_1010_0100`, `tree_mask=0`,
      `hash_mask=0b0000_0000_1010_0100`, 3×B256): draw the byte map — the three little-endian u16 masks
      (bytes 0-5), the length prefix byte, then the 3 packed 32-byte hashes — marking each field's offset and
      where nibble 2/5/7's hash lands in the packed `hashes` Vec via `popcount(hash_mask & ((1<<nib)-1))`.
    - D-LAYOUT on the malformed blob: same byte map but with `hash_mask.count_ones()=3` and only 2 hashes
      appended — circle the byte offset where `compact_decode` must detect the length mismatch (the R2 guard).

  - **Output**: `notes/20_branch_compact_design.md` with R1–R10 + answers to D1–D7 + the bad-input scenario;
    plus `notes/20_branch_compact_drill.md` with the byte-map drawings.

  #### Phase 2 — Write the failing tests (~30 min)

  Each test is engineered so at least one common naive impl FAILS it. Tier annotated; real assert-level blocks.

  - [ ] **(NEWBIE) `try_new_rejects_hash_mask_supersetof_state_mask`** — exercises R4.
    ```rust
    #[test]
    fn try_new_rejects_hash_mask_supersetof_state_mask() {
        // hash bit 1 set, but state bit 1 NOT set — hash must be subset of state
        let r = BranchNodeCompact::try_new(0x0001, 0x0000, 0x0002, smallvec![B256::ZERO]);
        assert!(matches!(r, Err(BranchError::InvalidMask)),
            "hash_mask superset of state_mask must reject, got {r:?}");
    }
    ```
  - [ ] **(NEWBIE) `try_new_rejects_tree_mask_supersetof_state_mask`** — same shape for tree; exercises R3.
  - [ ] **(SENIOR) `try_new_rejects_hashes_length_mismatch`** — exercises R2 + R5.
    ```rust
    #[test]
    fn try_new_rejects_hashes_length_mismatch() {
        // hash_mask says 3 hashes, only 2 provided
        let r = BranchNodeCompact::try_new(0x0007, 0x0000, 0x0007, smallvec![B256::ZERO, B256::ZERO]);
        assert!(matches!(r, Err(BranchError::LengthMismatch { expected: 3, got: 2 })),
            "hash_mask.count_ones()=3 with 2 hashes must reject, got {r:?}");
    }
    ```
  - [ ] **(SENIOR) `iter_children_yields_msb_first`** — exercises R8.
    ```rust
    #[test]
    fn iter_children_yields_msb_first() {
        let n = BranchNodeCompact::try_new(0b0000_0000_1010_0100, 0, 0, smallvec![]).unwrap();
        let nibbles: Vec<u8> = n.iter_children().map(|(nib, _)| nib).collect();
        assert_eq!(nibbles, vec![2, 5, 7], "children must yield in nibble order 0..15, got {nibbles:?}");
    }
    ```
  - [ ] **(NEWBIE) `set_child_updates_all_masks`** — exercises R9.
    ```rust
    #[test]
    fn set_child_updates_all_masks() {
        let mut n = BranchNodeCompact::default();
        n.set_child(3, ChildRef::Hash(B256::repeat_byte(0xAB)));
        assert_ne!(n.state_mask & (1 << 3), 0, "state_mask bit 3 must flip on");
        assert_ne!(n.hash_mask & (1 << 3), 0, "hash_mask bit 3 must flip on");
        assert_eq!(n.hashes.len(), 1);
    }
    ```
  - [ ] **(SENIOR) `rlp_encode_is_17_element_list`** — exercises R6. Encode a branch with one child at
    nibble 5; decode the RLP list header; assert exactly 17 elements, slot 5 holds the child, slots 0-4/6-16
    are `0x80`.
  - [ ] **(SENIOR) `compact_round_trip_byte_identical`** — exercises R7 + R10 (canonical encoding).
    ```rust
    #[test]
    fn compact_round_trip_byte_identical() {
        let original = arbitrary_valid_branch();   // proptest-style fixture
        let mut buf1 = Vec::new();
        original.compact_encode(&mut buf1);
        let decoded = BranchNodeCompact::compact_decode(&buf1).unwrap();
        let mut buf2 = Vec::new();
        decoded.compact_encode(&mut buf2);
        assert_eq!(buf1, buf2, "compact encoding is non-canonical: re-encode differs");
    }
    ```
  - [ ] **(EXPERT, cross-primitive) `embedded_child_inline_under_32_bytes`** — exercises D4. A child whose
    *full RLP* encoding is 30 bytes is inlined into the parent's RLP; assert the parent's encoded bytes
    contain the child's raw 30 bytes (not a 32-byte hash). Test the boundary at 31, 32, 33.
  - [ ] **(SENIOR, negative-input) `compact_decode_rejects_inconsistent_masks`** — exercises D7. Hand-craft a
    compact blob with `hash_mask.count_ones() = 3` but only 2 B256 hashes appended; `compact_decode` returns
    `Err(BranchError::LengthMismatch { .. })`. The invariant R2 must be re-checked at the decode boundary,
    not only at `try_new`.
  - [ ] **(EXPERT, anti-shortcut) `bench_round_trip_5m_per_sec`** — criterion bench on a fixed 10k-branch
    fixture; assert ≥ 5 M nodes/sec encode+decode (⇒ ≤ 200 ns/node). A `compact_encode` that allocates a fresh
    `Vec<u8>` per call passes every correctness test above but lands at ~1.2 M/sec — the bar trap.
    ```rust
    // benches/branch_codec.rs — bench startup asserts SmallVec stays inline (heap-free) for ≤4 hashes
    fn bench_round_trip(c: &mut Criterion) {
        let nodes = fixture_10k_sparse_branches();
        let mut buf = Vec::with_capacity(256);     // reusable, caller-supplied
        c.bench_function("branch_round_trip", |b| b.iter(|| {
            for n in &nodes { buf.clear(); n.compact_encode(&mut buf);
                black_box(BranchNodeCompact::compact_decode(&buf).unwrap()); }
        }));
    }
    ```
  - [ ] **(EXPERT, negative-control) `bench_round_trip_physically_plausible`** — paired test-the-test: assert
    the bench's measured ns/node lands in `[20 ns, 5000 ns]`. Below 20 ns the codec is doing nothing (compiler
    elided the work — add `black_box`); above 5000 ns the harness is broken. A green bench outside this band is
    a harness bug regardless of the throughput number.
    ```rust
    #[test]
    fn bench_round_trip_physically_plausible() {
        let ns = measure_single_node_round_trip_ns();
        assert!(ns >= 20.0 && ns <= 5000.0,
            "round trip {ns:.0} ns/node outside physical band [20, 5000] — bench is broken", );
    }
    ```

  - 🪤 **Trap-test discipline reminders** (add tests fitting these shapes if not above):
    - *Cross-primitive trap*: `embedded_child_inline_under_32_bytes` composes branch encoding with the
      child-RLP threshold the hash builder (Fri) depends on — passes branch encode in isolation, fires on
      shallow-trie composition.
    - *Anti-shortcut trap*: `bench_round_trip_5m_per_sec` — the obvious `-> Vec<u8>` per-call reach passes
      correctness, fails the throughput bar.
    - *Negative-control test-the-test*: `bench_round_trip_physically_plausible` pins the measured number to a
      possible band so a no-op bench fails loudly.

  #### Phase 3 — Make the tests pass, one failure at a time (~70 min)

  Read each card *after* you see the failure. Order is newbie-first, expert-last — the ladder you actually walk.

  - **(NEWBIE) When `iter_children_yields_msb_first` yields children in REVERSE order:**
    - Symptom: expected `[2, 5, 7]`, got `[7, 5, 2]`. The iter's bit-scan walks high-to-low.
    - Why: nibble order is MSB-first per yellow paper §C — bit 0 (LSB) of `state_mask` corresponds to
      nibble 0 (the first child); but a naive `(state_mask >> i) & 1` iteration starting at i=15 walks
      backward.
    - Fix: iterate `for i in 0..16 { if (state_mask >> i) & 1 == 1 { yield (i as u8, ...); } }`. Add an
      assertion in the test that compares against a hand-encoded `eth_getProof` fixture.
    - Muscle: nibble ordering in MPT is MSB-first / low-bit-of-mask-first; off-by-one or reversed iteration
      fails every state-root check silently. Reapplies at: W20 walker.rs, W20 state_root.rs, W21 RLP
      compact-path encoding, W31 persistent MPT.

  - **(SENIOR) When `compact_decode_rejects_inconsistent_masks` PASSES (decode accepts the bad blob):**
    - Symptom: hand-crafted blob with `hash_mask.count_ones() = 3` and 2 hashes decodes to a
      `BranchNodeCompact { hashes: vec![h1, h2] }` — no error. Later state-root computation reads
      `hashes[2]` and panics on index out of bounds.
    - Why: `compact_decode` reads `state_mask, tree_mask, hash_mask` then reads `len_prefix` hashes —
      but doesn't cross-check `hash_mask.count_ones() == len_prefix`. The two are consistent in valid
      blobs; the decoder trusts.
    - Fix: after reading the masks AND the hash count, assert `hash_mask.count_ones() as usize ==
      hashes.len()`; otherwise return `Err(LengthMismatch)`. This is the same invariant R2 — enforce it
      at the boundary (decode), not just at construction (`try_new`).
    - Muscle: every wire-format invariant must be re-checked at decode time; never assume the producer
      respected it. Reapplies at: W20 walker.rs (cursor invariants), W21 RLP `#[rlp(trailing)]` (decoder
      validates field count), W26 wal `record.rs` (every kind/flag combo re-validated).

  - **(EXPERT) When `embedded_child_inline_under_32_bytes` produces an encoding that doesn't match the reth fixture:**
    - Symptom: the parent's encoded bytes contain a 32-byte hash where the test expected the child's raw
      30 bytes. Roots diverge on shallow tries.
    - Why: the encoder branched on `child.encoded_len() <= 32` (or `< 32` but using the wrong size — e.g.
      it counted the RLP header bytes when it should count only the body). The threshold is **strictly
      less than 32**, measured on the FULL encoded form (header + body).
    - Fix: `if child.full_encoded_len() < 32 { write child's bytes inline } else { write keccak(child).bytes() }`.
      Test against the reth fixture at exactly 31, 32, and 33 byte encodings to nail the boundary.
    - Muscle: the inline-vs-hash threshold is a strict-less-than at exactly 32 bytes, measured on the
      full RLP encoding; off-by-one fails consensus on every shallow trie. Reapplies at: W20 state_root.rs
      hash builder, W21 trie RLP encoders.

  - **(EXPERT, perf) When `bench_round_trip_5m_per_sec` reports 1.2 M/sec:**
    - Symptom: criterion shows ~800 ns/node round trip. Threshold (≥5M/sec ⇒ ≤200 ns/node) fails 4×.
      (The `bench_round_trip_physically_plausible` negative-control still passes — 800 ns is inside [20,5000] —
      confirming the bench measures something real; the failure is a true regression, not a harness bug.)
    - Why: every `BranchNodeCompact` constructed in the bench heap-allocates a new `SmallVec<[B256; 4]>`
      even though the inline capacity should keep it on the stack — OR your `compact_encode` allocates
      a fresh `Vec<u8>` per call instead of writing into a reusable `BufMut`.
    - Fix: (1) verify `SmallVec::new()` for ≤4 hashes is heap-free via `dhat`; (2) make `compact_encode`
      take `&mut impl BufMut` so callers can pool the output buffer. Re-bench.
    - Cost avoided: ~600 ns/node × 100M intermediate nodes per full-state recompute = ~60 sec of pure
      alloc on every state-root walk.
    - Muscle: a hot codec must accept a caller-supplied buffer; allocating per-call destroys throughput
      even with cheap allocators. Reapplies at: W20 state_root.rs, W21 RLP encode helpers, W26 wal
      `record.rs` encode path.

  - **#### 5-year failure mode** (write in `notes/20_branch_compact_followup.md` after green): when reth
    extends `BranchNodeCompact` with a fourth mask (e.g. a "verkle-witness" bit when EIP-6800 ships),
    the wire format will need a version byte or a flags field. Migration: add a `flags: u8`
    leading byte in compact-form encoding; old decoders error on `flags != 0`. Trigger condition:
    verkle-trie consensus activation (currently scheduled for ~2027-2028).
- [ ] Commit + log (~10 min) — `Output: cargo test -p eth-trie branch_compact_ green; cargo bench -p eth-trie branch_round_trip ≤200 ns/node`.

**Tuesday — `TrieStorage` abstraction over `StateCache`**

- [ ] **Refactor**: `crates/eth-trie/src/storage/{hashed.rs,intermediate.rs,mod.rs}` — split TrieStorage into
  HashedNodeStorage + IntermediateStorage. (~total 80 min across 3 phases)
  - 🔧 **Why this matters**: a persistent MPT stores TWO physically distinct kinds of nodes — (a) hashed-state
    leaves (one row per account/slot) and (b) intermediate branch/extension nodes computed during state-root
    building. Conflating them in one trait forces every consumer to wade through the same K/V soup. reth's split
    allows MerkleStage to rebuild intermediate nodes incrementally from hashed state, never touching account-leaf
    storage. The trap: the refactor produces *deliberate* borrow-checker errors at every site that previously
    mixed read+write — those errors are the payoff, not the cost. Downstream: W22 MerkleStage (consumes both),
    W27+ storage-trie::mdbx (implements both atop MDBX), W92 block-stm (parallel HashedNodeStorage snapshots).

  #### Phase 1 — Problem, requirements, design (~25 min, no code yet)

  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/eth-trie/notes/20_trie_storage_split_prediction.md`:
    - P1: the trait-method split you'd reach for (predict the four `HashedNodeStorage` + five `IntermediateStorage` methods).
    - P2: predict whether your existing `eth-trie` v0.1 callers compile after the split (predict: ≥1 borrow-check error).
    - P3: predict the storage-cursor key shape for per-account isolation (`BTreeMap<B256, BTreeMap<B256, U256>>` vs flat `BTreeMap<(B256,B256), U256>` + range).
    - P4: predict whether a from-scratch rebuild root equals the incrementally-maintained root on first attempt.
    - P5: **list every Phase-2 test you predict your first attempt will fail.**
  - *Mental model*: split the one K/V soup into two typed surfaces so the source-of-truth direction (hashed is
    truth, intermediate is a derived index) becomes a *compile-time* fact, not a convention.
  - *Contract*: two traits — `HashedNodeStorage` (reads + account/storage cursors), `IntermediateStorage`
    (branch get/put/delete + trie cursors). `&self` reads, `&mut self` writes. Cursors return *owned* `(Key,Value)`
    clones (not borrows) so nested two-cursor loops don't hit borrow walls. NEVER the solution body.
  - *Back-of-envelope*: this is a type-refactor, not a hot path — perf is bar (b) optional. The load-bearing cost
    is the `intermediate_rebuilds_from_hashed` property's CI time: a 10k-account `debug_assert!` rebuild-after-each-insert
    is O(n²) and runs minutes; gate it behind a `cfg(feature = "expensive-checks")`, not every `cargo test`.

  - **Requirements** — write in `crates/eth-trie/notes/20_trie_storage_split_design.md`:
    - R1. `HashedNodeStorage` trait with `get_account`, `get_storage(account, slot)`, `account_cursor()`,
      `storage_cursor(account)`.
    - R2. `IntermediateStorage` trait with `get_branch(path)`, `put_branch(path, node)`,
      `delete_branch(path)`, `account_trie_cursor()`, `storage_trie_cursor(account)`.
    - R3. Load-bearing invariant: `IntermediateStorage` is rebuildable from `HashedNodeStorage` — given any
      `HashedNodeStorage` state, you can construct a fresh `IntermediateStorage` whose state-root matches the
      one maintained incrementally.
    - R4. Cursor keys in `account_trie_cursor` are nibble-prefix-ordered (lex by `Nibbles`).
    - R5. `storage_trie_cursor` is parameterized by `account: B256` — each account has its own storage trie.
    - R6. Hashed table is the source of truth; intermediate is a derived index. Writes go to hashed; W22+
      MerkleStage derives intermediate.
    - R7. In-memory test implementations (`MemoryHashed`, `MemoryIntermediate`) backed by `BTreeMap` to
      exercise the contract before MDBX backing arrives at W27.

  - **Design walk-through** — answer each:
    - D1. Trait method signatures: `&self` for reads, `&mut self` for writes — but in the MerkleStage where
      one thread reads hashed while another writes intermediate, what's the concurrency model? (Hint: defer
      to W22; for now, `&self` reads, `&mut self` writes; downstream wraps with `Arc<RwLock>` or per-shard.)
    - D2. Cursor lifetimes: `fn account_cursor(&self) -> impl Cursor + 'a` ties cursor to storage. Why does
      reth's cursor return *owned* `(Key, Value)` clones instead of borrowed slices? (Hint: nested loops
      with two cursors sharing storage hit borrow-checker walls.)
    - D3. Why parameterize `storage_trie_cursor(account)`? Sketch the alternative (a single global storage
      cursor) and explain why it breaks. (Hint: each account has its OWN storage trie root; a global cursor
      would yield interleaved storage slots from different accounts.)
    - D4. The "rebuildable from hashed" invariant (R3) — how do you TEST it? (Hint: insert 10k accounts via
      Hashed; rebuild Intermediate from scratch via MerkleStage's algorithm; compare to a parallel
      incrementally-maintained version. Both must produce the same root.)
    - D5. **Anti-shortcut audit**: the "obvious" reach is one `TrieStorage` trait with a `bulk_write(both)`
      method "for convenience" — one line on why it's wrong HERE: it re-conflates the source-of-truth
      direction the split exists to enforce; MerkleStage could then mutate hashed state mid-rebuild.
    - D6. **Pre-mortem (load-bearing)**: name the bad refactor that re-conflates the two traits (a `BulkWriter`
      extension trait that lets MerkleStage write both at once). Write the 4-step interleaving where it
      corrupts: rebuild reads hashed → bulk-write mutates hashed mid-rebuild → intermediate now derives from
      a half-mutated source → root drifts from every other client.

  - 🧮 **Paper drill** — write in `notes/20_trie_storage_split_drill.md`:
    - D-STRUCT on a 3-account `MemoryHashed` + `MemoryIntermediate` pair: draw both surfaces side by side
      (hashed = source-of-truth leaves, intermediate = derived branch index); hand-execute the
      `intermediate_rebuilds_from_hashed` step — for each of the 3 accounts, show which intermediate branch
      gets (re)derived — proving R3 (rebuild root == incremental root).
    - D-STRUCT on the borrow wall: draw the two-cursor nested loop (intermediate cursor live + a hashed
      `&mut self` write) and mark the exact step where the borrow checker rejects it — the
      `split_traits_forbid_write_while_iterating` payoff.

  - **Output**: `notes/20_trie_storage_split_design.md` with R1–R7 + D1–D6 + the two trait skeletons;
    plus `notes/20_trie_storage_split_drill.md` with the two-surface + borrow-wall drawings.

  #### Phase 2 — Write the failing tests (~15 min)

  - [ ] **(SENIOR, cross-primitive) `intermediate_rebuilds_from_hashed`** — insert 100 accounts via
    `MemoryHashed`; run MerkleStage's rebuild against an empty `MemoryIntermediate`; compare root to
    incrementally-maintained version. Composes the storage split with Friday's StateRoot. Exercises R3 + R6.
  - [ ] **(SENIOR) `storage_cursor_per_account_isolated`** — exercises R5.
    ```rust
    #[test]
    fn storage_cursor_per_account_isolated() {
        let (a, b) = (B256::repeat_byte(0xAA), B256::repeat_byte(0xBB));
        let mut s = MemoryHashed::default();
        s.put_storage(a, B256::from(U256::from(1)), U256::from(11));
        s.put_storage(b, B256::from(U256::from(2)), U256::from(22));
        let a_slots: Vec<_> = s.storage_cursor(a).collect();
        assert_eq!(a_slots.len(), 1, "cursor for A leaked B's slots: {a_slots:?}");
    }
    ```
  - [ ] **(NEWBIE) `account_trie_cursor_lex_ordered`** — insert branches at paths `[5]`, `[2]`, `[7]`;
    cursor yields `[2], [5], [7]`. Exercises R4.
  - [ ] **(SENIOR) `empty_hashed_yields_empty_root`** — empty `MemoryHashed` rebuilds to a
    `MemoryIntermediate` whose root = `EMPTY_ROOT_HASH = keccak(rlp(b""))`, NOT `B256::ZERO`. Exercises R3 boundary.
  - [ ] **(SENIOR, negative-control / test-the-test) `split_traits_forbid_write_while_iterating`** — proves the
    split actually enforces R6: a `trybuild`/`compile_fail` fixture holding an `IntermediateStorage` cursor while
    calling a hashed `&mut self` write must fail to compile with the exact borrow-checker error
    `cannot borrow ... as mutable ... also borrowed as immutable`. If this *compiles*, the split bought nothing —
    the test proves the refactor's payoff is real.
  - [ ] **(NEWBIE) `existing_callers_migrated`** — every test/example in `eth-trie` builds and passes with both
    traits in place; `cargo test -p eth-trie` green. Exercises R7.

  - 🪤 **Trap-test discipline reminders**:
    - *Cross-primitive trap*: `intermediate_rebuilds_from_hashed` composes the storage split with the StateRoot
      algorithm (Fri) — each green alone, the rebuild-vs-incremental equality fires on composition.
    - *Anti-shortcut trap*: a `BulkWriter` convenience trait passes every functional test but re-conflates the
      source-of-truth direction (caught by D6 pre-mortem + the borrow-fail test).
    - *Negative-control test-the-test*: `split_traits_forbid_write_while_iterating` proves the borrow-checker
      barrier (the refactor's whole point) actually fires, with the exact expected error string.

  #### Phase 3 — Make the tests pass, one failure at a time (~40 min)

  - **(SENIOR) When `storage_cursor_per_account_isolated` yields slots from BOTH accounts:**
    - Symptom: cursor for account A returns slots that should belong to account B.
    - Why: the storage cursor was implemented as a single global iterator with no account-scoping;
      `MemoryIntermediate` stored all storage tries in one flat `BTreeMap<(B256, B256), U256>` and the
      cursor didn't filter by the account prefix.
    - Fix: storage cursor's underlying iteration must constrain to keys with the given account prefix —
      either store storage in `BTreeMap<B256, BTreeMap<B256, U256>>` (per-account inner map) OR use a
      range query `(account, B256::ZERO)..(account, B256::MAX)`.
    - Muscle: each account's storage is a *separate* MPT — storage_cursor must scope by account, not
      iterate globally. Reapplies at: W27 storage-trie::mdbx (uses MDBX dup-sort tables to encode this
      shape), W31 persistent MPT.

  - **(SENIOR) When `intermediate_rebuilds_from_hashed` produces a different root than the incremental version:**
    - Symptom: rebuild-from-scratch root ≠ incrementally-maintained root after the same 100 inserts.
      Both implementations claim to follow the same MPT spec.
    - Why: one of three subtle bugs — (a) the incremental version forgot to update an ancestor when a
      leaf was inserted; (b) the rebuild uses a different nibble-encoding for keys; (c) the empty-root
      sentinel differs (`B256::ZERO` vs `KECCAK_EMPTY_ROOT`).
    - Fix: run a `debug_assert!` after each insert that the incremental root equals a from-scratch
      rebuild on the same data (gate behind `cfg(feature = "expensive-checks")`). The property is load-bearing.
    - Muscle: any "derived index" must have a property test that the from-scratch rebuild matches the
      incrementally-maintained version. Without it, the index drifts silently. Reapplies at: W20 state_root,
      W22 MerkleStage, W27 storage-trie::mdbx, W31 persistent MPT.

  - **(EXPERT) When `cargo test -p eth-trie` reports borrow-checker errors in migrated callers:**
    - Symptom: existing tests that used the unified `TrieStorage` no longer compile after the split;
      errors like "cannot borrow `*storage` as immutable because it is also borrowed as mutable" inside
      MerkleStage code. (This is exactly what `split_traits_forbid_write_while_iterating` predicted — the
      error is the designed outcome, not a mistake.)
    - Why: callers held a cursor from `IntermediateStorage` while trying to write via `HashedNodeStorage`
      on the same struct — the split traits enforce R3's source-of-truth direction (you can't write
      hashed while iterating intermediate without going through a controlled commit boundary).
    - Fix: refactor those call sites to use two separate borrow scopes — `{ let cursor = intermediate.cursor();
      ... drop(cursor); }` then write hashed. The compile error IS the lesson: conflating the two was
      always wrong.
    - Muscle: a refactor that splits a trait into two with disjoint mutability rules will produce
      borrow-checker errors at every site that previously mixed read+write — those errors are the
      payoff, not the cost. Reapplies at: W22 MerkleStage, W27 storage-trie::mdbx, W92 block-stm.

  - **#### 5-year failure mode** (write in `notes/20_trie_storage_split_followup.md` after green): when
    Verkle trie (EIP-6800) ships, the intermediate-vs-hashed distinction may flatten — Verkle witnesses
    are computed differently and there's only one "node" type. Migration: a third trait
    `VerkleNodeStorage` lives parallel to the two; MerkleStage's MPT-mode and a future VerkleStage's
    mode coexist via cfg-features. Trigger: Verkle activation, ~2027-2028.
- [ ] **Build**: `crates/eth-trie/src/storage/cached.rs` — `CachedStorage<C: StateCache>` delegating reads to
  `eth-storage-cache` (W14). (~total 75 min across 3 phases)
  - 🔧 **Why this matters**: `HashedNodeStorage` reads dominate the state-root walk — every leaf is a `get_account`.
    Hitting MDBX cold each time costs a B-tree descent + page fault (~1 µs). `CachedStorage` wraps a backing
    `HashedNodeStorage` and a W14 `eth-storage-cache` LRU so hot accounts (the dirty set this block) return in
    ~3 ns. The trap: a cache that does not invalidate on write returns stale account data and silently forks the
    chain. Mirror: reth's `CachedTrieCursor` layering over the DB cursor. Downstream: W22 MerkleStage (reads
    through this), W27+ storage-trie::mdbx (the real backing arrives here).

  #### Phase 1 — Problem, prediction, design (~20 min, no code yet)

  - 🎯 **Pre-prediction prompt** — `crates/eth-trie/notes/20_cached_storage_prediction.md`:
    - P1: the cache import + type (predict: `eth_storage_cache::StateCache<B256, Account>`).
    - P2: predict cold-MDBX vs cache-hit latency (predict ~1 µs vs ~3 ns ≈ 300× cliff).
    - P3: predict the concurrent first-call outcome when two readers miss the same key (thundering-herd / double-load).
    - P4: predict whether a write that bypasses the cache (writes backing directly) leaves a stale entry.
    - P5: **list every Phase-2 test you predict your first attempt will fail.**
  - *Mental model*: a read-through cache layer over `HashedNodeStorage` — `get_account` checks the LRU first,
    falls back to the backing store on miss, and inserts on the return path. Writes MUST invalidate.
  - *Contract*: `struct CachedStorage<S, C> { backing: S, cache: C }` where `S: HashedNodeStorage`,
    `C: StateCache<B256, Account>`; impls `HashedNodeStorage` by delegating, interposing the cache on
    `get_account`/`get_storage` and invalidating on the write path. NEVER the body.
  - *Back-of-envelope*: cold MDBX account read ≈ 1 µs (B-tree descent + likely page fault, ~300 ns syscall
    floor); W14 cache hit ≈ 3 ns (L1/L2). Cliff ratio ≈ 300×. A dirty block touches ~5k accounts; at a 90% hit
    rate that's 500 cold × 1 µs + 4500 hot × 3 ns ≈ 513 µs vs 5 ms all-cold — ~10× on the per-block walk.

  - **Requirements** — `crates/eth-trie/notes/20_cached_storage_design.md`:
    - R1. `CachedStorage<S, C>` implements `HashedNodeStorage` by delegating to `backing: S`.
    - R2. `get_account(addr)` checks `cache` first; on miss, reads backing, inserts into cache, returns.
    - R3. Any write (or the MerkleStage commit boundary) invalidates the affected cache entries — no stale reads.
    - R4. Cache is a *read-through* layer; it never becomes the source of truth (R6 from the split holds).
    - R5. Cursors bypass the cache (range scans pollute an LRU); only point `get`s go through it.
    - R6. The cache type is the W14 `eth-storage-cache`; do NOT re-implement an LRU here.

  - **Design walk-through**:
    - D1. Why do cursors bypass the cache? (Hint: a full account-trie cursor walk would evict the hot working set.)
    - D2. Invalidation granularity — per-key vs clear-all on commit? Sketch the trade-off (per-key is precise but
      needs the write set; clear-all is simple but cold-starts the next block).
    - D3. **Anti-shortcut audit**: the "obvious" reach is to make `CachedStorage` *own* the data and treat the
      cache as truth — one line on why wrong HERE: it violates R4/R6, the backing store is truth; a cache-as-truth
      design loses any entry the LRU evicts.
    - D4. **Pre-mortem (load-bearing)**: write the 4-step stale-read scenario: read account X (caches v1) → write
      X = v2 to backing without invalidating → re-read X returns cached v1 → state root computed from v1 forks
      the chain. If you can't write it, your invalidation is missing.

  - 🧮 **Paper drill** — write in `notes/20_cached_storage_drill.md`:
    - D-COST on a 5k-account dirty block at 90% hit rate: per-op cost table — cold MDBX `get_account` (~1 µs:
      ~300 ns syscall floor + B-tree descent + page fault) vs W14 cache hit (~3 ns L1/L2). Compute the cliff
      ratio (~300×) and the per-block walk cost: 500 cold × 1 µs + 4500 hot × 3 ns ≈ 513 µs vs 5 ms all-cold.
      State the budget verdict (~10× win from the cache layer).
    - D-COST on the stale-read NON-cost: trace the 4-step stale-read (read X→cache v1; write X=v2 no-inval;
      re-read X=v1; root from v1 forks) and label it "correctness-fatal, not a perf number" — invalidation is
      not optional.

  - **Output**: `notes/20_cached_storage_design.md` with R1–R6 + D1–D4 + the stale-read scenario;
    plus `notes/20_cached_storage_drill.md` with the cost ledger + cliff ratio.

  #### Phase 2 — Write the failing tests (~15 min)

  - [ ] **(NEWBIE) `cache_hit_returns_without_backing_read`** — instrument the backing with a read counter;
    second `get_account(X)` does NOT increment it. Exercises R2.
  - [ ] **(SENIOR, the core trap) `write_invalidates_stale_entry`** — exercises R3.
    ```rust
    #[test]
    fn write_invalidates_stale_entry() {
        let mut s = CachedStorage::new(MemoryHashed::default(), StateCache::with_capacity(1024));
        s.put_account(X, acct_v1());
        assert_eq!(s.get_account(X), Some(acct_v1()));   // caches v1
        s.put_account(X, acct_v2());                     // MUST invalidate cached v1
        assert_eq!(s.get_account(X), Some(acct_v2()), "stale cache returned v1 after write — consensus fork");
    }
    ```
  - [ ] **(SENIOR) `cursor_walk_does_not_pollute_cache`** — after a full `account_cursor()` walk, the cache's
    occupancy is unchanged (cursors bypass it). Exercises R5.
  - [ ] **(NEWBIE, cross-primitive) `cached_storage_satisfies_hashed_contract`** — run the same contract suite
    from the trait split against `CachedStorage<MemoryHashed, _>`; identical results to bare `MemoryHashed`.
    Composes the cache layer with the Tuesday split. Exercises R1.
  - [ ] **(SENIOR, negative-control) `cache_actually_caches`** — test-the-test: assert the backing read counter
    after 1000 repeated `get_account(X)` is ≤ a small constant (e.g. ≤ 5), not ~1000. If it's ~1000 the cache is
    wired but never hitting — the suite above would still pass functionally while the cache does nothing.

  - 🪤 **Trap-test discipline reminders**:
    - *Cross-primitive*: `cached_storage_satisfies_hashed_contract` reuses the Tuesday split's contract suite.
    - *Anti-shortcut*: cache-as-truth design (D3) passes reads, loses evicted entries — caught by R4/R6.
    - *Negative-control*: `cache_actually_caches` proves the cache is on the hot path, not bypassed.

  #### Phase 3 — Make the tests pass, one failure at a time (~30 min)

  - **(SENIOR) When `write_invalidates_stale_entry` returns v1 after writing v2:**
    - Symptom: `get_account(X)` returns the old `acct_v1()` after `put_account(X, acct_v2())`.
    - Why: the write path mutated the backing store but left the cached entry in place — read-through caches
      need explicit invalidation; the cache has no way to know the backing changed.
    - Fix: every write/commit calls `cache.invalidate(addr)` (or `cache.insert(addr, new)`) before returning.
    - Cost avoided: a stale account read feeds a wrong `storage_root`/`code_hash` into the state root — a silent
      consensus fork at the first affected block. (bar (b): correctness-fatal, not merely slow.)
    - Muscle: a read-through cache is only correct if every write invalidates; the backing store is truth, the
      cache must never outlive a mutation of its key. Reapplies at: W14 eth-storage-cache, W27 storage-trie::mdbx,
      W42 ExecutionStage state cache, W92 block-stm versioned reads.

  - **(SENIOR) When `cursor_walk_does_not_pollute_cache` shows the cache full of cold range entries:**
    - Symptom: after one `account_cursor()` walk over 1M accounts, the LRU holds the trie's tail, not the dirty set.
    - Why: the cursor delegated through `get_account`, inserting every scanned key into the cache and evicting
      the hot working set.
    - Fix: cursors read the backing directly, bypassing `cache`; only point `get_account`/`get_storage` go
      through it.
    - Muscle: range scans must bypass the point-lookup cache, or one walk thrashes the working set. Reapplies at:
      W27 storage-trie::mdbx cursors, W31 persistent MPT iteration.

  - **#### 5-year failure mode** (`notes/20_cached_storage_followup.md` after green): when the backing moves to a
    concurrent MDBX with multiple reader threads (W27+), the single-threaded read-through cache needs sharding or
    a concurrent map (W4 `concurrent` crate) to avoid a lock on every `get`. Trigger: first multi-threaded
    MerkleStage at W92 block-stm; migration: swap the W14 cache for a sharded variant keyed by `addr.high_byte()`.
- [ ] Commit + log (~10 min) — `Output: cargo test -p eth-trie storage_ green; split + cached layers both pass the HashedNodeStorage contract suite`.

**Wednesday — `TrieWalker` cursor**

- [ ] **Build**: `crates/eth-trie/src/walker.rs` — TrieWalker<C: TrieCursor> streaming traversal, mirroring
  `alloy_trie::walker::TrieWalker`. (~total 140 min across 3 phases)
  - 🔧 **Why this matters**: building a state root requires touching every dirty account's trie path AND only
    those paths — not the whole trie. `TrieWalker` is the streaming cursor that yields paths in lex order,
    skipping subtrees that haven't changed. This is the algorithm that makes incremental MerkleStage tractable
    on a 700M-account state — without it, every block would re-walk the full trie. Two traps: (1) the
    nibble-push/pop **stack invariant** (path must shrink in lockstep with the stack — drift it and the walker
    emits stale-prefixed paths), and (2) the **prefix-set skip predicate** is bidirectional, not equality.
    First attempts fail ≥1 test below. Downstream: Thursday's ProofRetainer (consumes walker output), Friday's
    StateRoot (drives walker for incremental root), W22 MerkleStage (glues walker + DB cursor).

  #### Phase 1 — Problem, requirements, design (~40 min, no code yet)

  - 🎯 **Pre-prediction prompt** — `crates/eth-trie/notes/20_walker_prediction.md`:
    - P1: the `Nibbles` inline container (predict: `SmallVec<[u8; 64]>` — keccak256 = 32 B = 64 nibbles max depth).
    - P2: predict `PrefixSet::contains_prefix` complexity if you reach for `Vec::contains` (predict: O(n), wrong) vs binary search (O(log n)).
    - P3: predict whether a naive `for i in (0..16).rev()` child loop yields lex-monotone output.
    - P4: predict by how many nibbles `current_key` must shrink on a single `stack.pop()` (predict: 1).
    - P5: **list every Phase-2 test you predict your first attempt will fail.**
  - *Mental model*: a depth-first streaming cursor that walks the intermediate trie, yields dirty paths in lex
    order, and reports cached hashes for clean subtrees so the hash builder never re-descends them.
  - *Contract*: `TrieWalker<'a, C: TrieCursor>` with `advance() -> Option<Nibbles>`, `hash() -> Option<B256>`,
    `children_are_in_trie() -> bool`; holds a `stack: Vec<StackItem>` and a `current_key: Nibbles`; filters
    against `changes: &'a PrefixSet`. NEVER the body.
  - *Back-of-envelope*: a full mainnet trie is ~700M nodes; a block touches ~1k–5k dirty paths each ≤64 nibbles
    deep. A correct walker is O(dirty × depth) ≈ 5k × 64 ≈ 320k node touches per block. The killer cost is a
    linear `PrefixSet::contains_prefix`: O(n) per check × O(depth) descents per dirty path = O(n²) ≈ 5k × 64 × 5k
    ≈ 1.6B comparisons — 800 ms instead of 50 ms. Binary search drops it to O(dirty × depth × log n).

  - **Requirements** — write in `crates/eth-trie/notes/20_walker_design.md`:
    - R1. `TrieWalker<'a, C: TrieCursor>` with `advance() -> Option<Nibbles>`, `hash() -> Option<B256>`,
      `children_are_in_trie() -> bool`.
    - R2. Walker yields paths in **strict lex (nibble-prefix) order** — output is monotone.
    - R3. Walker skips subtrees not in `changes: &'a PrefixSet`.
    - R4. `hash()` reflects the cached hash of the current branch node WHEN its children are clean
      (`tree_mask` bit at current child position is set).
    - R5. `Nibbles` type uses inline `SmallVec<[u8; 64]>` (max trie depth ≈ 64 nibbles — `keccak256` output
      is 32 bytes = 64 nibbles).
    - R6. `PrefixSet` is a sorted `Vec<Nibbles>` of dirty paths; `contains_prefix(nibbles) -> bool` is O(log n)
      via binary search.
    - R7. Empty trie: `advance` returns `None` immediately on a cursor whose `first()` is `None` — never panics.
    - R8. Walker holds an exclusive cursor for its lifetime; concurrent writes to the trie are NOT permitted
      while walker is alive (caller's responsibility).

  - **Design walk-through** — answer each:
    - D1. The walker's `stack: Vec<StackItem>` represents the path from root to current position. Each
      StackItem holds `(Nibbles, BranchNodeCompact, child_index)`. Sketch the advance algorithm: when do
      you push, when do you pop, when do you yield?
    - D2. The `current_key: Nibbles` field tracks the path from root to current cursor position. When you
      pop a stack frame, `current_key` must shrink — by how much? (Hint: each branch frame contributed
      exactly one nibble.)
    - D3. The `PrefixSet` (dirty-path filter): given dirty paths `[[1,2], [5]]`, which nibbles should the
      walker visit for the root branch (depth 0)? (Hint: only children at nibbles 1 and 5, never 0, 2, 3,
      4, 6...15.)
    - D4. `children_are_in_trie()` returns whether the current child position has its `tree_mask` bit set
      — meaning the child is a STORED intermediate node (DB-resident), not an embedded one. The hash
      builder uses this to decide "feed cached hash" vs "descend further". Write the formula.
    - D5. **Anti-shortcut audit**: the "obvious" reach is `PrefixSet::contains_prefix` = `self.paths.iter().any(...)`
      — one line on why wrong HERE: linear scan inside an O(depth)-per-dirty-path loop is O(n²); fails the 50 ms bar.
    - D6. **Pre-mortem (load-bearing)**: write the off-by-one in nibble push/pop that produces walker output
      containing paths the dirty set never asked for — e.g. `stack.pop()` without `current_key.truncate(stack.len())`
      leaves a stale nibble, so the next yielded path carries a prefix from the previous subtree → state root corrupts.

  - 🧮 **Paper drill** — write in `notes/20_walker_drill.md`:
    - D-WALK on a concrete 3-key trie (keys `0x12…`, `0x15…`, `0x57…`, so nibble paths share prefixes):
      draw the resulting branch/extension/leaf tree, then hand-compute each branch's `BranchNodeCompact`
      mask bits (state/tree/hash) and the root state root, step by step — making concrete why nibble order
      must be MSB-first / low-bit-of-mask-first.
    - D-STRUCT on the walker stack: with `PrefixSet = [[1,2],[5]]`, hand-execute `advance()` over that
      3-key trie — redraw `stack` + `current_key` after each push/pop, marking the `current_key.truncate(stack.len())`
      that must fire on every pop (the D6 off-by-one defense) and which children get skipped as clean.

  - **Output**: `notes/20_walker_design.md` with R1–R8 + D1–D6 + the advance pseudocode;
    plus `notes/20_walker_drill.md` with the hand-walked 3-key trie + stack trace.

  #### Phase 2 — Write the failing tests (~25 min)

  - [ ] **(NEWBIE) `empty_trie_advance_returns_none`** — walker over an empty cursor; `advance()` returns
    `None`, never panics. Exercises R7.
  - [ ] **(NEWBIE) `single_leaf_yields_just_leaf_path`** — trie with one account; walker yields exactly that
    path. Exercises R1.
  - [ ] **(SENIOR) `walker_skips_clean_subtrees`** — exercises R3.
    ```rust
    #[test]
    fn walker_skips_clean_subtrees() {
        let trie = ten_account_trie();
        let dirty = PrefixSet::from(vec![path_for(3), path_for(7)]);
        let yielded: Vec<Nibbles> = TrieWalker::new(trie.cursor(), &dirty).collect();
        // only paths for accounts 3 & 7 + shared ancestors — never account 0,1,2,4,5,6,8,9
        assert!(yielded.iter().all(|p| dirty.contains_prefix(p) || is_ancestor_of_dirty(p, &dirty)),
            "walker visited a clean subtree path: {yielded:?}");
    }
    ```
  - [ ] **(SENIOR, cross-primitive) `walker_output_is_lex_monotone`** — random 100-account trie with 20 dirty
    paths; collected `advance()` output is strictly increasing in `Nibbles` lex order. Composes walker with
    `Nibbles` ordering Friday's hash builder depends on. Exercises R2.
    ```rust
    #[test]
    fn walker_output_is_lex_monotone() {
        let out: Vec<Nibbles> = walker_over_random_trie(100, 20).collect();
        for w in out.windows(2) {
            assert!(w[0] < w[1], "walker output not lex-monotone: {:?} then {:?}", w[0], w[1]);
        }
    }
    ```
  - [ ] **(SENIOR) `hash_returns_cached_for_clean_subtree`** — clean branch node has its `tree_mask` bit set;
    walker's `hash()` returns the cached B256, not recomputed. Exercises R4.
  - [ ] **(SENIOR) `current_key_shrinks_on_pop`** — after walker pops a stack frame, `current_key` is exactly
    one nibble shorter than before. Exercises D6 (the off-by-one defense).
  - [ ] **(EXPERT, anti-shortcut) `bench_1k_dirty_on_1m_trie_under_50ms`** — criterion bench on 1M-leaf trie
    with 1k dirty paths; walker completes in <50 ms. A linear-scan `PrefixSet` passes every correctness test
    above but lands at ~800 ms here — the bar trap.
  - [ ] **(EXPERT, negative-control) `bench_walker_physically_plausible`** — paired test-the-test: assert the
    bench touches a plausible node count (`≥ dirty_paths` and `≤ total_nodes`) and the measured time is in
    `[100 µs, 2 s]`. A walker that early-returns (skips everything) measures ~0 and would "pass" the 50 ms bar
    while doing nothing — this catches it.

  - 🪤 **Trap-test discipline reminders**:
    - *Cross-primitive*: `walker_output_is_lex_monotone` couples the walker with the nibble ordering Friday's
      hash builder requires — wrong order here silently breaks Friday's root.
    - *Anti-shortcut*: `bench_1k_dirty_on_1m_trie_under_50ms` — the obvious linear `PrefixSet` reach passes
      correctness, fails the O(n²) bar.
    - *Negative-control*: `bench_walker_physically_plausible` proves the bench actually walks nodes, not a no-op.

  #### Phase 3 — Make the tests pass, one failure at a time (~75 min)

  - **(NEWBIE) When `walker_output_is_lex_monotone` reports an out-of-order pair like `[5, 2]` then `[5, 1]`:**
    - Symptom: collected output contains `[5, 2]` followed by `[5, 1]` — strictly decreasing within a
      subtree.
    - Why: the advance algorithm's child-iteration walks `child_index = 15` down to `0` (high-to-low)
      somewhere. Lex order requires `0` to `15` (low-to-high).
    - Fix: every place `advance` iterates a branch's children, walk `for i in 0..16` not `for i in
      (0..16).rev()`. Add a `debug_assert!(yielded > last_yielded)` after every advance call to catch
      future regressions.
    - Muscle: lex order on nibbles is low-to-high; any reverse iteration breaks state-root correctness.
      Reapplies at: W20 state_root.rs hash builder, W31 persistent MPT cursor walk.

  - **(SENIOR) When `walker_skips_clean_subtrees` yields paths in clean subtrees:**
    - Symptom: walker yields `[0, 1]` (a clean path) even though `PrefixSet` only contains `[acc_3, acc_7]`.
    - Why: the dirty-filter check (`changes.contains_prefix(stack_top_path)`) is missing OR the prefix
      set's binary search returns the wrong bound (e.g. `partition_point` for `>= path` vs `> path`).
    - Fix: at every push (descending into a subtree), `if !changes.contains_prefix(&new_path) { skip; }`.
      The `contains_prefix` check is "is there any dirty path P such that `new_path` is a prefix of P, OR
      `P` is a prefix of `new_path`?" — both directions matter.
    - Muscle: an incremental walker's skip predicate is bidirectional prefix-matching, not equality.
      Reapplies at: W20 state_root.rs incremental drive, W22 MerkleStage.

  - **(SENIOR) When `current_key_shrinks_on_pop` reports `current_key` unchanged after pop:**
    - Symptom: after `stack.pop()`, `current_key.len()` is the same as before — the leftover nibble from
      the popped frame is still there.
    - Why: missed the `current_key.truncate(stack.len())` (or equivalent) on pop. The walker emits paths
      starting with stale prefixes from the previous subtree.
    - Fix: every `stack.pop()` is paired with `current_key.truncate(stack.len())` — or use a
      drop-guard struct that pops both fields atomically.
    - Muscle: stack-tracked paths must shrink in lockstep with the stack — pair them via a guard struct,
      not two separate fields. Reapplies at: W20 state_root.rs, W31 persistent MPT cursor.

  - **(EXPERT, perf) When `bench_1k_dirty_on_1m_trie_under_50ms` reports 800 ms:**
    - Symptom: walker takes 800 ms instead of 50 ms. ~16× over threshold. (`bench_walker_physically_plausible`
      still passes — node count is right, time is in band — so it's a true regression, not a no-op walker.)
    - Why: `PrefixSet::contains_prefix` is implemented as a linear scan over the dirty paths Vec instead
      of binary search — O(n) per check × O(depth) descends per dirty path = O(n²).
    - Fix: `dirty.partition_point(|p| p < &probe)` and check both that-index and that-index-1 for prefix
      relationship — O(log n). Re-bench.
    - Cost avoided: ~750 ms per 1000-dirty-path walk; multiplied by 12 stages × 1000 blocks/min sync,
      that's hours of wasted compute per day.
    - Muscle: any "is X a prefix of any element of sorted-set Y?" query is `partition_point` + 2
      adjacent comparisons, never a linear scan. Reapplies at: W22 MerkleStage, W27 storage-trie::mdbx.

  - **#### 5-year failure mode** (write in `notes/20_walker_followup.md` after green): when Verkle tries
    (EIP-6800) ship, the walker's nibble-arity (16 children per branch) becomes node-arity (256 children
    per Verkle node). Migration: extract the arity as a const generic on `TrieWalker<const ARITY: usize>`.
    Trigger: Verkle activation, ~2027-2028.
- [ ] **Extend** (cadence touchpoint — async/Pin, C18): `crates/eth-trie/src/walk_stream.rs` — expose the
  `TrieWalker` as an async `Stream<Item = Nibbles>` so it composes with an async page source (paged DB / network
  node fetch). (~total 50 min, lighter secondary item)
  - 🔧 **Why this matters**: the Wednesday `TrieWalker` is a synchronous pull cursor — it assumes every node is
    already in memory. When the backing nodes arrive over a paged/async source (W27+ MDBX async reads, or a
    remote node fetch), the walker must *yield* at each page boundary and resume when the page loads. Wrapping it
    as a `futures::Stream` (`poll_next` yields a node, returns `Poll::Pending` while a page is in flight) is the
    minimal async surface that lets the same traversal drive an async fetcher. This is a spaced-repetition
    touchpoint for **async/Pin/Future (C18)** — last drilled W3, due again here per `concept_cadence.md` (the
    W3→W43 drought bridge). Mirror: `futures::Stream` (and reth's `TrieCursor` async variants). Downstream: W28
    B-tree async cursor (`poll_next` over leaf pages) reuses this exact pattern; W36 snapshot async export.
  - *Mental model*: a `Stream` adapter that owns the `TrieWalker`'s in-progress path stack and a borrow of the
    async node source; each `poll_next` either advances the walker (when the next node is resident) or returns
    `Pending` and registers the waker against the page load.
  - *Contract*: `pub struct WalkStream<'a, S: AsyncNodeSource> { walker_state: WalkerState, source: &'a mut S,
    pending: Option<S::Fetch> }` implementing `Stream<Item = Result<Nibbles, Error>>` via `poll_next`. The
    path-stack lives across `.await` points, so the future is `!Unpin` and must be pinned. NEVER the body.
  - 🧮 **Paper drill** — write in `notes/20_walk_stream_drill.md`:
    - D-ASYNC on `WalkStream::poll_next` across one page-load await: draw the generated state-machine enum —
      each state = a span between awaits (Idle / AwaitingPage / Yielding). List what is **stored/pinned across
      the await**: the in-progress walker path stack (`current_key` + the `Vec<StackItem>`) and the `&mut S`
      borrow of the node source. Mark WHY it must be pinned — the path stack is a self-referential live local
      held across the page-load suspension; if the future moved mid-await, the stack's internal references
      would dangle. Show where `poll_next` returns `Pending` (page in flight) vs `Ready(Some(path))` (node
      resident, walker advanced) vs `Ready(None)` (walker exhausted).
  - **Output**: `crates/eth-trie/src/walk_stream.rs` compiles; a `#[tokio::test]` drives `WalkStream` over a
    mock `AsyncNodeSource` that returns `Pending` once per page and asserts the streamed path sequence equals
    the synchronous `TrieWalker` output (same lex-monotone order). Plus `notes/20_walk_stream_drill.md` with the
    state-machine drawing. `Output: cargo test -p eth-trie walk_stream_ green`.
- [ ] Commit + log (~10 min) — `Output: cargo test -p eth-trie walker_ green; cargo bench -p eth-trie walker_1k_dirty <50 ms`.

**Thursday — `ProofRetainer` + EIP-1186 proofs**

- [ ] **Build**: `crates/eth-trie/src/proof/retainer.rs` — ProofRetainer mirroring `alloy_trie::proof::ProofRetainer`.
  (~total 140 min across 3 phases)
  - 🔧 **Why this matters**: EIP-1186 (`eth_getProof`) demands a Merkle proof of a specific account/slot.
    Naive approach: rebuild the trie, log every node — O(state size) per proof. Real approach: during
    state-root walk, *retain* only the nodes on the requested path. `ProofRetainer` is the side-channel
    observer of the hash builder that captures these nodes. Two traps: (1) the ancestor check must include
    **equality** (else the target leaf itself is dropped), and (2) retained bytes are **RLP wire form**, not
    compact DB form (else verification fails). Downstream: Friday's StateRoot orchestrator drives retainer
    alongside walker; Phase 5 RPC `eth_getProof` wraps this.

  #### Phase 1 — Problem, requirements, design (~40 min, no code yet)

  - 🎯 **Pre-prediction prompt** — `crates/eth-trie/notes/20_retainer_prediction.md`:
    - P1: the ancestor-check predicate you'd reach for (predict: `targets.iter().any(|t| t.starts_with(prefix))`).
    - P2: predict whether that predicate captures the target leaf itself (hint: `starts_with` when `prefix == target`).
    - P3: predict which byte form to retain — RLP wire or compact DB (predict: RLP).
    - P4: predict the `bench_100_targets_under_5ms` outcome with a linear `any()` retain check.
    - P5: **list every Phase-2 test you predict your first attempt will fail.**
  - *Mental model*: a side-channel observer hooked into the hash builder's RLP-emit boundary; for each
    intermediate node it sees, it keeps the node iff the node's path is an ancestor of (or equal to) any
    requested target. The retained set, in lex order, IS the EIP-1186 proof.
  - *Contract*: `ProofRetainer::new(targets: Vec<Nibbles>)`, `retain(prefix: &Nibbles, encoded: &[u8])`,
    `into_proof() -> Vec<Bytes>` (lex order, root first). Internal store is a `BTreeMap<Nibbles, Bytes>` for
    deterministic ordering. NEVER the body.
  - *Back-of-envelope*: a proof is O(targets × depth) ≈ 100 × 30 = 3000 retained nodes max. The retain hook
    fires for *every* intermediate node the builder emits (~node count). The killer is an O(T) `any()` per
    fire: at T=100 targets × ~30k builder emits = 3M comparisons — ~40 ms. Sorted targets + bounded binary
    search drops it to O(log T + matches) ≈ ~3000× faster on the retain hot path.

  - **Requirements** — write in `crates/eth-trie/notes/20_retainer_design.md`:
    - R1. `ProofRetainer::new(targets: Vec<Nibbles>)` — targets are nibble paths to the leaves we want
      proofs for; sorted lex internally.
    - R2. `retain(prefix: &Nibbles, encoded: &[u8])` — called by hash builder for every intermediate node;
      retainer decides whether to keep based on "is `prefix` an ancestor of (or equal to) any target?"
    - R3. Retained nodes are stored as **RLP-encoded bytes** (the wire form), NOT `BranchNodeCompact` (the
      DB form) — clients verify against the wire form.
    - R4. `into_proof() -> Vec<Bytes>` returns nodes in **strict lex order** of their paths (root first).
    - R5. Embedded children (RLP encoding <32 bytes) are NOT separately retained — they live inside the
      parent's encoding. R3+R4 imply this since the embedded child has no separate "path".
    - R6. Non-existence proofs: EIP-1186 also proves an account doesn't exist (returns empty value + path
      to nearest divergence). The retainer must capture the branch where the path diverges.
    - R7. Deterministic ordering: same (targets, trie) → same proof bytes, every call. `BTreeMap` over
      `HashMap` to guarantee.
    - R8. Storage-proof variant: a target inside an account's storage trie produces a separate
      account-proof + storage-proof chain.

  - **Design walk-through** — answer each:
    - D1. "Is prefix an ancestor of any target?" — write the formula. (Hint: `targets.iter().any(|t|
      t.starts_with(prefix))` for inclusion proofs; this MUST also fire when `prefix == t` to capture the
      target leaf; for non-existence, also retain if `prefix.starts_with(t)` at the divergence point.)
    - D2. Optimization: sorted `targets` + binary search makes the ancestor check O(log T) instead of
      O(T). Sketch the bsearch — what predicate?
    - D3. The hash builder hook: where does the retainer plug in? Sketch the callback shape — what
      arguments does the hash builder pass (RLP bytes + path), what does retainer do with them?
    - D4. **Anti-shortcut audit**: the "obvious" reach is to retain `BranchNodeCompact` (already in hand) —
      one line on why wrong HERE: clients verify `keccak(node) == parent_child_hash` over RLP bytes; compact
      bytes hash to a different value → every proof rejected.
    - D5. **Pre-mortem (load-bearing)**: write the scenario (4-6 steps) where a leaf node at the target path
      is missing from the proof: targets are paths; the retainer's check is "ancestor of target"; `starts_with`
      with `prefix == target` — if you accidentally use a strict-ancestor `len() <` guard, the target leaf is
      dropped → verifier sees the chain but no leaf → proof rejected.

  - 🧮 **Paper drill** — write in `notes/20_retainer_drill.md`:
    - D-WALK on a 3-leaf trie with target path `[1,2,5]`: hand-trace the hash builder's RLP-emit order, and at
      each emitted node decide retain/drop via `targets.iter().any(|t| t.starts_with(prefix))` — show the
      root + `[1]` + `[1,2]` branches AND the `[1,2,5]` leaf itself are kept (the equality case), proving the
      proof is not one node short.
    - D-WALK on the non-existence target `[1,2,9]` (branch at `[1,2]` has children 5,7 only): trace where the
      walk terminates AT the divergence branch, retaining it so the verifier sees "no child at nibble 9".

  - **Output**: `notes/20_retainer_design.md` with R1–R8 + D1–D5 + the missing-leaf scenario;
    plus `notes/20_retainer_drill.md` with the retain-decision hand-trace.

  #### Phase 2 — Write the failing tests (~30 min)

  - [ ] **(SENIOR, the inheritance test) `inclusion_proof_round_trip_mainnet`** — captured `eth_getProof` response
    for a known address at a fixed mainnet block; build trie from fixture; retain for that path; assert proof
    bytes match the captured response. Exercises R1, R2, R3.
  - [ ] **(NEWBIE) `proof_order_is_lex_root_first`** — proof Vec's elements are in lex order of their paths;
    first element is the root. Exercises R4.
  - [ ] **(SENIOR, cross-primitive) `proof_excludes_embedded_children`** — trie with one embedded child
    (encoding <32 bytes, the Monday D4 boundary); proof for that path contains the parent but NOT the embedded
    child as a separate entry. Composes the retainer with Monday's inline-vs-hash threshold. Exercises R5.
  - [ ] **(SENIOR) `non_existence_proof_terminates_at_divergence`** — target a path that doesn't exist; proof
    terminates at the branch where the path diverges, with no leaf node. Exercises R6.
  - [ ] **(NEWBIE) `same_targets_yield_byte_identical_proofs`** — call retainer twice on the same trie+targets;
    output bytes are identical. Exercises R7.
  - [ ] **(SENIOR) `storage_proof_includes_account_chain`** — account A with storage slot 0x3; proof for
    `(A, 0x3)` contains both the account-proof and storage-proof chains. Exercises R8.
  - [ ] **(SENIOR, the core trap) `target_leaf_node_present_in_proof`** — exercises the missing-leaf pitfall from D5.
    ```rust
    #[test]
    fn target_leaf_node_present_in_proof() {
        let (trie, target) = trie_with_known_leaf();
        let proof = retain_for(&trie, vec![target.clone()]);
        let leaf_node_path = target;     // the leaf's own path
        assert!(proof.iter().any(|n| node_path(n) == leaf_node_path),
            "proof has ancestors but is missing the target leaf node itself");
    }
    ```
  - [ ] **(EXPERT, anti-shortcut) `bench_100_targets_under_5ms`** — 100-target proof on a 10k-account trie in
    <5 ms. A linear `any()` retain check passes correctness, lands at ~40 ms — the bar trap.
  - [ ] **(EXPERT, negative-control) `bench_retain_physically_plausible`** — paired test-the-test: assert the
    retainer actually examined ≥ trie-node-count `retain` calls and the proof is non-empty for a valid target.
    A retainer hook that's never wired fires 0 times and would "pass" the 5 ms bar while producing an empty proof.

  - 🪤 **Trap-test discipline reminders**:
    - *Cross-primitive*: `proof_excludes_embedded_children` composes the retainer with Monday's `<32` inline
      threshold — passes each alone, fires on the shallow-trie composition.
    - *Anti-shortcut*: `bench_100_targets_under_5ms` — the obvious linear `any()` reach passes correctness,
      fails the O(T)-per-emit bar.
    - *Negative-control*: `bench_retain_physically_plausible` proves the hook is wired and firing, not skipped.

  #### Phase 3 — Make the tests pass, one failure at a time (~70 min)

  - **(SENIOR) When `target_leaf_node_present_in_proof` fails — proof is one node short:**
    - Symptom: proof contains the chain of branches from root to the leaf's parent, but the leaf node
      itself is missing.
    - Why: the retainer's predicate is a *strict* ancestor check (`prefix.len() < target.len() &&
      target.starts_with(prefix)`) — it excludes the equal case when `prefix == target` (the leaf itself).
    - Fix: change the check to `targets.iter().any(|t| t.starts_with(prefix))` — Rust's `slice::starts_with`
      includes the equal case, so the target leaf is captured.
    - Muscle: an "is X an ancestor of Y" check must include the equality case to capture the target
      node itself. Reapplies at: W20 state_root.rs (driving the retainer), W31 persistent MPT proof gen.

  - **(SENIOR) When `inclusion_proof_round_trip_mainnet` produces proofs that don't match the captured response:**
    - Symptom: client (separate verifier) rejects with "hash mismatch at node 3". Your proof's
      node-3 bytes are 8 bytes shorter than the captured response's node-3 bytes.
    - Why: you retained `BranchNodeCompact` bytes (DB form) instead of RLP-encoded bytes (wire form).
      The 8-byte difference is the compact form's mask-prefix vs RLP's list-header.
    - Fix: the hash builder emits RLP bytes — pass *those* into `retain`, not the compact form. Wire
      the hook on the RLP-emit boundary, not the BranchNodeCompact-create boundary.
    - Muscle: the proof wire-format is RLP everywhere; compact form is DB-internal only. Reapplies at:
      Phase 5 RPC `eth_getProof`, W31 persistent MPT (separate wire vs DB representations).

  - **(SENIOR) When `non_existence_proof_terminates_at_divergence` produces a proof too long:**
    - Symptom: target is `[1, 2, 5]` which doesn't exist (branch at `[1, 2]` has children at 3 and 7
      only, not 5); proof contains 5 nodes when 3 expected (root + `[1]` + `[1,2]`).
    - Why: the retainer's predicate kept retaining past the divergence point, walking into other
      subtrees that share `[1,2,5]` as a non-ancestor.
    - Fix: at the divergence branch, retain the branch itself (so verifier sees "no child at nibble 5")
      and STOP — don't descend into children that don't match the target.
    - Muscle: non-existence proofs terminate AT the divergence branch, not past it; verify with the
      "branch has no child at target's next nibble" check. Reapplies at: Phase 5 RPC `eth_getProof`.

  - **(EXPERT, perf) When `bench_100_targets_under_5ms` reports 40 ms:**
    - Symptom: 8× over threshold. The retainer's `retain` call does an O(T) linear scan over targets.
      (`bench_retain_physically_plausible` still passes — the hook fired the right number of times — so it's a
      true algorithmic regression, not an unwired hook.)
    - Why: `targets` is stored unsorted; `any(|t| t.starts_with(prefix))` is O(T) per call.
    - Fix: sort `targets` at construction; in `retain`, use `partition_point` to find the lex-bounded
      range of targets that COULD share a prefix, then check only those. Or: `targets.binary_search_by(|t|
      t.cmp(&prefix))` then walk forward while `t.starts_with(prefix)`.
    - Cost avoided: O(T) → O(log T + matches); at T=100 with 30-deep tries, ~3000× speedup on the
      retain hot path.
    - Muscle: every "any element of sorted set has property P" check is binary search + bounded scan,
      never a linear `any()`. Reapplies at: W20 walker.rs (PrefixSet), W22 MerkleStage.

  - **#### 5-year failure mode** (write in `notes/20_retainer_followup.md` after green): when Verkle tries
    (EIP-6800) replace MPT, proof shapes change entirely — Verkle proofs are O(1)-sized (constant ~600
    bytes regardless of trie depth) via polynomial commitments. `ProofRetainer` becomes obsolete; a
    `VerkleProofComposer` (different algorithm) replaces it. Trigger: Verkle activation, ~2027-2028.
- [ ] **Build**: `crates/eth-trie/src/proof/verify.rs` — `verify_proof(root, key, value, proof) -> Result<(), ProofError>`
  mirroring `alloy_trie::proof::verify_proof`. (~total 105 min across 3 phases)
  - 🔧 **Why this matters**: a proof is only useful if a *light client* can verify it without the full state.
    `verify_proof` walks the proof nodes from root, re-hashing each and checking it matches the parent's child
    pointer, terminating at the claimed key/value. This is the verifier side of Thursday's retainer — and the
    function a stateless client / bridge / light node actually runs. The trap: a verifier that trusts the proof's
    own claimed hashes instead of recomputing `keccak` accepts a forged proof. Downstream: Phase 5 RPC
    `eth_getProof` verification path; W91 consensus-engine stateless validation experiments.

  #### Phase 1 — Problem, prediction, design (~30 min, no code yet)

  - 🎯 **Pre-prediction prompt** — `crates/eth-trie/notes/20_verify_prediction.md`:
    - P1: the per-node check you'd reach for (predict: `keccak256(node_rlp) == expected_child_hash`).
    - P2: predict what happens if a node's RLP is <32 bytes (embedded — compare bytes directly, don't hash).
    - P3: predict the result of trusting the proof's claimed hash vs recomputing keccak (predict: forgeable).
    - P4: predict the non-existence path — what proves a key is absent.
    - P5: **list every Phase-2 test you predict your first attempt will fail.**
  - *Mental model*: a stateless walk — start with the trusted `root`, for each proof node check
    `keccak(node) == expected`, decode it, follow the nibble of `key` to the next expected child hash; the leaf
    must match the claimed value (inclusion) or the branch must lack the key's nibble (non-existence).
  - *Contract*: `pub fn verify_proof(root: B256, key: Nibbles, expected_value: Option<&[u8]>, proof: &[Bytes])
    -> Result<(), ProofError>`. Returns `Ok` iff the proof is internally consistent AND terminates at the claimed
    value (or proves absence when `expected_value` is `None`). NEVER the body.
  - *Back-of-envelope*: verification is O(depth) ≈ 30 keccak256 hashes per proof; keccak256 of a ~512 B node ≈
    a few hundred ns. ~30 × ~300 ns ≈ 10 µs per proof — fine for RPC. The trap is *correctness*, not speed: skip
    one re-hash and the verifier accepts forged state.

  - **Requirements** — `crates/eth-trie/notes/20_verify_design.md`:
    - R1. `verify_proof(root, key, expected_value, proof)` returns `Ok(())` for a valid inclusion proof.
    - R2. Each non-embedded proof node is re-hashed: `keccak256(node) == expected_child_hash`; mismatch → `Err`.
    - R3. Embedded children (<32 bytes) are compared by bytes, not hashed (R5/D4 from the retainer mirror).
    - R4. The walk follows `key`'s nibbles; the terminal leaf's value must equal `expected_value`.
    - R5. Non-existence: `expected_value = None` succeeds iff the proof terminates at a branch lacking the
      key's next nibble OR a leaf with a different path — and every node still re-hashes correctly.
    - R6. A forged proof (any node whose keccak ≠ parent pointer) returns a typed `Err`, never `Ok`.

  - **Design walk-through**:
    - D1. Why re-hash every node instead of trusting the proof's order? (Hint: the proof is attacker-supplied.)
    - D2. The embedded-child case: how does the verifier tell "this child pointer is an inline node" from "this
      is a 32-byte hash"? (Hint: same `<32` rule as Monday D4, on the RLP item length.)
    - D3. **Anti-shortcut audit**: the "obvious" reach is to decode the proof into a trie and compare roots —
      one line on why wrong HERE: that's O(proof) trie construction + you'd still have to verify hashes; the
      streaming re-hash walk is simpler AND is the only thing that actually proves the chain.
    - D4. **Pre-mortem (load-bearing)**: write the forged-proof scenario (4-6 steps): attacker swaps a leaf
      value but keeps the chain; if the verifier compares the leaf to `expected_value` *without* checking
      `keccak(leaf) == parent pointer`, it accepts the forgery → light client trusts fake state.

  - 🧮 **Paper drill** — write in `notes/20_verify_drill.md`:
    - D-WALK on the retainer's own 3-node proof for `[1,2,5]`: starting from the trusted `root`, hand-execute
      the verify walk — at each node compute `keccak(node) == expected_child_pointer`, follow `key`'s next
      nibble to the next pointer, terminate at the leaf value; show the chain of re-hashes that proves the
      whole path.
    - D-WALK on the forged proof: flip one byte in the leaf value, keep the chain — show the exact node where
      `keccak(leaf) != parent pointer` fires, proving a verifier that re-hashes (vs. trusts proof-claimed
      hashes) rejects the forgery.

  - **Output**: `notes/20_verify_design.md` with R1–R6 + D1–D4 + the forged-proof scenario;
    plus `notes/20_verify_drill.md` with the re-hash verify walk + the forged-node rejection.

  #### Phase 2 — Write the failing tests (~25 min)

  - [ ] **(NEWBIE, cross-primitive) `verify_accepts_retainer_output`** — feed the exact proof
    `ProofRetainer::into_proof()` produced (Thursday's first Build) into `verify_proof`; returns `Ok`.
    Composes verifier with retainer — the round trip is the whole point. Exercises R1.
  - [ ] **(SENIOR, the core trap) `verify_rejects_forged_leaf`** — exercises R2 + R6.
    ```rust
    #[test]
    fn verify_rejects_forged_leaf() {
        let (root, key, value, mut proof) = valid_inclusion_proof();
        // tamper: flip a byte in the leaf node's value, keep the chain bytes
        tamper_leaf_value(&mut proof);
        let r = verify_proof(root, key, Some(&value), &proof);
        assert!(matches!(r, Err(ProofError::HashMismatch { .. })),
            "forged leaf must be rejected by re-hash, got {r:?}");
    }
    ```
  - [ ] **(SENIOR) `verify_embedded_child_by_bytes`** — proof with an embedded (<32 B) child verifies by byte
    comparison, not by hashing the inline node. Exercises R3.
  - [ ] **(SENIOR) `verify_non_existence`** — a valid non-existence proof with `expected_value = None` returns
    `Ok`; the same proof with a non-`None` expected value returns `Err`. Exercises R5.
  - [ ] **(EXPERT, negative-control) `verify_rejects_truncated_proof`** — test-the-test for R6: drop the last
    proof node; verifier must `Err` (it can't reach the leaf), proving the walk actually consumes every node and
    doesn't short-circuit `Ok` on a partial chain.

  - 🪤 **Trap-test discipline reminders**:
    - *Cross-primitive*: `verify_accepts_retainer_output` round-trips the retainer (Thu) through the verifier.
    - *Anti-shortcut*: the decode-into-trie-and-compare approach (D3) "works" but doesn't prove the chain — the
      forged-leaf test catches the gap.
    - *Negative-control*: `verify_rejects_truncated_proof` proves the verifier consumes the whole chain.

  #### Phase 3 — Make the tests pass, one failure at a time (~30 min)

  - **(SENIOR) When `verify_rejects_forged_leaf` returns `Ok` on a tampered leaf:**
    - Symptom: a proof with a flipped leaf-value byte verifies as `Ok`.
    - Why: the verifier compared the terminal leaf to `expected_value` but never recomputed
      `keccak256(leaf) == parent_child_pointer` — it trusted the chain's structure.
    - Fix: re-hash every non-embedded node and check it equals the pointer the *previous* node handed down;
      only the trusted `root` is taken on faith.
    - Cost avoided: a light client / bridge accepting forged state = funds stolen on a stateless verifier.
      (bar (b): correctness-fatal.)
    - Muscle: a Merkle verifier trusts exactly one input (the root) and recomputes everything else; trusting any
      proof-supplied hash forfeits the security property. Reapplies at: Phase 5 RPC verification, W91
      consensus-engine stateless checks, W92 block-stm state validation.

  - **(SENIOR) When `verify_embedded_child_by_bytes` reports a hash mismatch on a valid embedded node:**
    - Symptom: a proof with an inline <32 B child fails with `HashMismatch` though the proof is valid.
    - Why: the verifier hashed the inline child and compared to a 32-byte slot — but the parent stored the
      child's *raw bytes* inline (Monday D4), so there's no hash to match.
    - Fix: when the parent's child slot is <32 bytes, treat it as the inline node directly (byte-compare/decode),
      do not `keccak` it.
    - Muscle: the inline-vs-hash boundary (`<32`) governs the verifier exactly as it governs the encoder —
      apply it on both sides. Reapplies at: W20 retainer.rs, W31 persistent MPT.

  - **#### 5-year failure mode** (`notes/20_verify_followup.md` after green): Verkle proofs (EIP-6800) verify via
    a polynomial-commitment opening, not a re-hash walk — `verify_proof` is replaced by `verify_verkle_opening`.
    Trigger: Verkle activation, ~2027-2028; migration: feature-gate the two verifiers behind the active fork.
- [ ] Test the retainer→verify round trip against EIP-1186 vectors + a captured mainnet `eth_getProof` response. (~30 min)
  — `Output: tests/eip1186_vectors.rs green; captured response verifies Ok`.
- [ ] Commit + log (~10 min) — `Output: cargo test -p eth-trie proof_ green`.

**Friday — `StateRoot` orchestrator**

- [ ] **Build**: `crates/eth-trie/src/state_root.rs` — StateRoot<S, I> with compute(). Heart of MerkleStage.
  Mirrors `reth_trie::StateRoot`. (~total 150 min across 3 phases)
  - 🔧 **Why this matters**: this is *the* algorithm that determines whether your node accepts or forks. Every
    block has a 32-byte `state_root` in its header — your node must compute byte-identical bytes. reth's
    `StateRoot` orchestrates: walker (which paths to visit) + hash builder (state machine that emits node
    hashes) + ProofRetainer (optional side-channel). One bug here = chain halt. **This is the week's
    cross-primitive trap**: it composes Monday's `BranchNodeCompact`, Wednesday's `TrieWalker`, and Thursday's
    `ProofRetainer` — each green alone; the composition fails on a reversed nibble walk, a wrong empty-root
    sentinel, a swapped Account RLP field, or a stale hash-builder field. Downstream: W22 MerkleStage (wraps
    `compute` for staged-sync), W27+ persistent backend swap, W31 storage-trie MPT, W92 block-stm (validates
    parallel exec by comparing state roots).

  #### Phase 1 — Problem, requirements, design (~45 min, no code yet)

  - 🎯 **Pre-prediction prompt** — `crates/eth-trie/notes/20_state_root_prediction.md`:
    - P1: the empty-trie root you'd reach for (predict the keccak-of-rlp-empty-list constant, NOT `B256::ZERO`).
    - P2: predict the Account RLP field order (predict `(nonce, balance, storage_root, code_hash)`).
    - P3: predict the empty-storage-root and no-code sentinels (`EMPTY_ROOT_HASH`, `KECCAK_EMPTY`).
    - P4: predict whether your first `compute()` matches mainnet block 1 (predict: ≥1 of the three sentinels/order is wrong).
    - P5: **list every Phase-2 test you predict your first attempt will fail.**
  - *Mental model*: a streaming orchestrator — the walker yields dirty paths in lex order, the hashed cursor
    supplies leaf values, the hash builder (a small state machine) folds them into branch hashes, and clean
    subtrees are short-circuited via the walker's cached hash. The output is a 32-byte root + `TrieUpdates`.
  - *Contract*: `StateRoot<S: HashedNodeStorage, I: IntermediateStorage>` with `compute() -> Result<(B256,
    TrieUpdates), Error>`; internally drives `TrieWalker` + `HashBuilder` + optional `ProofRetainer`. NEVER the body.
  - *Back-of-envelope*: from-scratch root over 700M accounts is infeasible (~minutes-to-hours per block).
    Incremental computes only the ~5k dirty paths × ~30 depth ≈ 150k node touches per block, feeding cached
    hashes for the other ~700M nodes via the `tree_mask` bit. Skipping clean subtrees is the difference between
    a 30 s and a sub-second root — the `tree_mask` bit IS the cache-validity flag.

  - **Requirements** — write in `crates/eth-trie/notes/20_state_root_design.md`:
    - R1. `StateRoot<S: HashedNodeStorage, I: IntermediateStorage>` with `compute() -> Result<(B256,
      TrieUpdates), Error>`.
    - R2. Empty trie computes to **`EMPTY_ROOT_HASH = 0x56e81f171bcc55a6ff8345e692c0f86e5b48e01b996cadc001622fb5e363b421`**
      (the keccak256 of RLP-encoded empty list).
    - R3. Hash builder processes leaves in **nibble-lex order** (driven by walker's R2 from earlier).
    - R4. Account leaves' value is `RLP(Account { nonce, balance, storage_root, code_hash })` — order matters
      and is fixed by yellow paper.
    - R5. Storage roots are sub-trie roots, computed by recursive call to `compute` on the per-account
      storage trie.
    - R6. Storage root for an account with no storage is `EMPTY_ROOT_HASH` (not `B256::ZERO`).
    - R7. Code hash for accounts with no code is `KECCAK_EMPTY = keccak256("")` (not `B256::ZERO`).
    - R8. Walker's `children_are_in_trie()` true → feed cached hash to hash builder, DO NOT descend.
    - R9. `TrieUpdates` is the set of `(path, new_BranchNodeCompact)` pairs the caller must persist after
      compute returns; allows MerkleStage to commit atomically.
    - R10. Pass `EthereumTests::TrieTests` AND mainnet block 1 reconstruction.

  - **Design walk-through** — answer each:
    - D1. Hash builder is a state machine with ~4 internal states (Updated, Hashed, Left, Right). Why a
      state machine and not a recursive function? (Hint: streaming input from the walker; can't hold the
      whole trie in memory.)
    - D2. Walker and hashed cursor advance in lockstep — when walker yields a leaf-prefix path, what
      does the hashed cursor do? Sketch the interleaving.
    - D3. Account RLP field order — write it out. What happens if you swap `balance` and `nonce`?
    - D4. Property test: "order of inserts doesn't affect final root" — write the assertion. Why is this
      load-bearing? (Hint: catches non-deterministic hash-builder state transitions.)
    - D5. **Anti-shortcut audit**: the "obvious" reach is to recompute the whole root from scratch each block
      (no walker, no cached hashes) — one line on why wrong HERE: O(700M) per block; sync takes months. The
      `tree_mask`/cached-hash short-circuit is non-negotiable.
    - D6. **Pre-mortem (load-bearing)**: name three places (each different) where `B256::ZERO` could silently
      substitute for `EMPTY_ROOT_HASH` or `KECCAK_EMPTY` and cause the state root to disagree with mainnet —
      write the 4-step trace for one of them through to the forked block.

  - 🧮 **Paper drill** — write in `notes/20_state_root_drill.md`:
    - D-WALK on a 3-account state, then re-root after a 1-key change: compute the full root from scratch
      (walker over all 3 paths → hash builder → root), then change ONE account's balance and re-run
      *incrementally* (walker visits only that dirty path, feeds the OTHER subtree's cached hash via the
      `tree_mask` bit) — show the incremental root is BYTE-IDENTICAL to a from-scratch recompute on the new
      state. This is the `insert_order_does_not_affect_root` + cached-hash invariant made concrete.
    - D-COST on the incremental-vs-full cliff at 10M accounts: per-block cost table — full recompute touches
      ~10M nodes (infeasible, ~10s+); incremental touches ~5k dirty × ~30 depth ≈ 150k node touches, the rest
      served by cached `tree_mask` hashes. Compute the cliff ratio (~60×+) and state the verdict: the cached-hash
      short-circuit is the difference between a sub-second and a multi-second root.

  - **Output**: `notes/20_state_root_design.md` with R1–R10 + D1–D6 + the hash-builder state diagram;
    plus `notes/20_state_root_drill.md` with the byte-identical re-root + the incremental-vs-full cost cliff.

  #### Phase 2 — Write the failing tests (~30 min)

  - [ ] **(NEWBIE) `empty_trie_yields_empty_root_hash`** — empty `HashedNodeStorage`; `compute()` returns
    `EMPTY_ROOT_HASH`, not `B256::ZERO`. Exercises R2.
    ```rust
    #[test]
    fn empty_trie_yields_empty_root_hash() {
        let (root, _) = StateRoot::new(MemoryHashed::default(), MemoryIntermediate::default()).compute().unwrap();
        assert_eq!(root, EMPTY_ROOT_HASH, "empty trie root must be keccak(rlp([])), not zero");
    }
    ```
  - [ ] **(SENIOR) `single_account_genesis_match`** — insert one known account from a public genesis JSON;
    computed root matches the published genesis state root. Exercises R3, R4.
  - [ ] **(EXPERT, the inheritance milestone, cross-primitive) `mainnet_block_1_reconstruction`** — from genesis
    state, apply block-1 transactions, compute root; matches `header.state_root` of mainnet block 1. Composes
    walker + retainer + branch-compact + sentinels + RLP order. Exercises R3–R10.
    ```rust
    #[test]
    fn mainnet_block_1_reconstruction() {
        let state = apply_block_1_to_genesis();           // fixture
        let (root, _) = StateRoot::new(state.hashed(), state.intermediate()).compute().unwrap();
        assert_eq!(root, MAINNET_BLOCK_1_STATE_ROOT,
            "computed root diverges from mainnet block 1 — node would fork");
    }
    ```
  - [ ] **(SENIOR, cross-primitive) `insert_order_does_not_affect_root`** — insert 1000 accounts in order A
    (root R_A); insert same accounts in random order (root R_B); assert `R_A == R_B`. Catches a nondeterministic
    hash-builder state transition or a non-monotone walker. Exercises commutativity.
  - [ ] **(SENIOR) `account_with_no_storage_has_empty_root_storage`** — account with empty storage trie; its
    `storage_root = EMPTY_ROOT_HASH`. Exercises R6.
  - [ ] **(SENIOR) `account_with_no_code_has_keccak_empty`** — EOA (no code); its `code_hash = KECCAK_EMPTY`.
    Exercises R7.
  - [ ] **(EXPERT) `walker_clean_subtree_uses_cached_hash`** — instrument the hash builder; assert that for a
    clean subtree (walker reports `children_are_in_trie() == true`), descent did NOT happen — cached hash was
    used directly. Exercises R8.
  - [ ] **(SENIOR) `trie_updates_set_matches_dirty_paths`** — after compute, `TrieUpdates` contains exactly the
    intermediate nodes that changed; persisting them to a fresh `IntermediateStorage` and re-computing yields the
    same root. Exercises R9.
  - [ ] **(EXPERT, negative-control) `state_root_fixture_is_not_trivially_zero`** — test-the-test: assert the
    expected mainnet/genesis root constants are themselves non-zero and not `EMPTY_ROOT_HASH` for non-empty
    fixtures, so a `compute()` that returns a constant can't accidentally match a mis-specified expected value.

  - 🪤 **Trap-test discipline reminders**:
    - *Cross-primitive*: `mainnet_block_1_reconstruction` + `insert_order_does_not_affect_root` compose the
      week's four Builds — each green alone, the composed root reveals nibble-order, sentinel, and RLP-order bugs.
    - *Anti-shortcut*: a from-scratch recompute (D5) is correct but O(700M)/block — caught by `walker_clean_subtree_uses_cached_hash`.
    - *Negative-control*: `state_root_fixture_is_not_trivially_zero` prevents a degenerate constant from masking a real divergence.

  #### Phase 3 — Make the tests pass, one failure at a time (~75 min)

  - **(NEWBIE) When `empty_trie_yields_empty_root_hash` returns `B256::ZERO`:**
    - Symptom: empty trie's root is `0x0000...0000` instead of `0x56e81f171bcc55a6ff8345e692c0f86e5b48e01b996cadc001622fb5e363b421`.
    - Why: `compute()` short-circuits on empty cursor with `B256::default()` (zero) instead of the
      keccak256 of RLP-empty-list constant.
    - Fix: define `pub const EMPTY_ROOT_HASH: B256 = b256!("56e81f171bcc55a6ff8345e692c0f86e5b48e01b996cadc001622fb5e363b421");`
      (or call `keccak256(rlp::encode_empty_list())` once and cache); return it on empty.
    - Muscle: zero and empty-root are NOT the same sentinel — many "empty" things in Ethereum hash to a
      specific non-zero value. Reapplies at: R6, R7, every empty-state edge case.

  - **(SENIOR) When `account_with_no_storage_has_empty_root_storage` fails because storage_root is `ZERO`:**
    - Symptom: account with no storage encodes with `storage_root = 0x0000...`; mainnet's same EOA
      encodes with `storage_root = 0x56e81f171bcc...`. Roots diverge from block 1.
    - Why: when populating `Account { ..., storage_root, ... }` for RLP encoding, the storage root was
      defaulted to `B256::ZERO` instead of `EMPTY_ROOT_HASH` when the account has no storage trie.
    - Fix: `Account::default().storage_root = EMPTY_ROOT_HASH;` — make the default sane. Audit every
      code path that builds an `Account` without a storage trie.
    - Muscle: the storage_root field on an Account is the root of that account's storage MPT; an empty
      MPT roots to `EMPTY_ROOT_HASH`, never zero. Reapplies at: W31 persistent MPT, W42 ExecutionStage,
      W92 block-stm.

  - **(SENIOR) When `mainnet_block_1_reconstruction` produces a root that differs from `header.state_root` by
    one nibble:**
    - Symptom: the leading nibble of your computed root differs from mainnet's. ~half the bits differ
      throughout — a single hash input was wrong.
    - Why: account RLP field order was `(balance, nonce, storage_root, code_hash)` instead of
      `(nonce, balance, storage_root, code_hash)`. Yellow paper appendix has the order.
    - Fix: explicit struct field order via `#[derive(RlpEncodable)]` matching yellow paper
      `(nonce, balance, storage_root, code_hash)`. Diff against `alloy_consensus::Account` byte-by-byte on a fixture.
    - Muscle: Ethereum consensus field orders are NOT alphabetical; they're locked by the yellow paper
      and any deviation breaks every state root. Reapplies at: W21 RLP `Header` encoding, W22 ExecutionStage
      block encoding, W92 block-stm.

  - **(SENIOR) When `insert_order_does_not_affect_root` reports different roots for the same set:**
    - Symptom: inserting `[A, B, C]` produces root R1; inserting `[C, A, B]` produces root R2 ≠ R1.
    - Why: hash builder state machine has nondeterministic transition on some leaf ordering — likely
      the walker yielded paths out of nibble-lex order in one of the two runs (W20 walker R2 violated),
      OR the hash builder's internal state isn't reset between runs.
    - Fix: trace the walker output for both runs; they MUST be identical permutations. If they differ,
      go fix the walker. If they're identical and the hash builder still produces different output,
      the hash builder has a stale field — audit `HashBuilder::new` for completeness.
    - Muscle: a hash builder over an MPT is deterministic IFF its input is deterministic AND its state
      resets correctly between runs. Reapplies at: W22 MerkleStage repeated execution, W92 block-stm
      parallel reconciliation.

  - **(EXPERT) When `walker_clean_subtree_uses_cached_hash` reports descent happened anyway:**
    - Symptom: instrumentation shows the hash builder called `compute` recursively on a clean subtree,
      taking 30+ seconds on full mainnet state — should have been instant.
    - Why: `walker.children_are_in_trie()` returns true (tree_mask bit set), but the orchestrator
      ignores it and descends regardless. Either the check is missing, or the cached hash isn't being
      fed into the hash builder.
    - Fix: `if walker.children_are_in_trie() { hb.feed_branch_hash(walker.hash().unwrap()); continue; }`
      — feed cached hash and skip the subtree.
    - Muscle: incremental state-root depends on cached hashes for clean subtrees; without them, every
      block recomputes from scratch. The `tree_mask` bit IS the cache validity flag. Reapplies at: W22
      MerkleStage incremental drive, W31 persistent MPT.

  - **#### 5-year failure mode** (write in `notes/20_state_root_followup.md` after green): when Verkle tries
    (EIP-6800) replace MPT, `StateRoot::compute` is replaced wholesale by a polynomial-commitment-based
    algorithm. Migration: the `TrieUpdates` shape changes (commitments instead of node bytes);
    every downstream consumer (W22 MerkleStage, W92 block-stm) needs a Verkle variant. Trigger:
    Verkle activation, ~2027-2028.
- [ ] Test: reconstruct block 1 mainnet state root from genesis + block 1 changes. (~30 min) —
  `Output: mainnet_block_1_reconstruction green; root == header.state_root`.
- [ ] Commit + log (~10 min) — `Output: cargo test -p eth-trie state_root_ green`.

**Saturday — `StorageRoot` + tag**

- [ ] **Build**: `crates/eth-trie/src/storage_root.rs` — `StorageRoot<S>` computing a single account's storage-trie
  root, mirroring `reth_trie::StorageRoot`. (~total 105 min across 3 phases)
  - 🔧 **Why this matters**: Friday's `StateRoot` calls *into* a per-account storage root for R5. `StorageRoot` is
    the same incremental walk scoped to one account's storage MPT (keys are `keccak(slot)`, values are
    `RLP(U256)`). It's a near-mirror of `StateRoot` but with two consensus-critical differences: the empty
    storage root is `EMPTY_ROOT_HASH` (R6 from Friday) and the value encoding is RLP-of-trimmed-U256, not an
    Account struct. The trap: encoding a storage value as a fixed 32-byte word instead of RLP-trimmed-big-endian
    forks the chain. Downstream: Friday's `StateRoot` (calls this for every account with storage), W31 persistent
    MPT, W42 ExecutionStage (storage writes).

  #### Phase 1 — Problem, prediction, design (~25 min, no code yet)

  - 🎯 **Pre-prediction prompt** — `crates/eth-trie/notes/20_storage_root_prediction.md`:
    - P1: the storage-value encoding you'd reach for (predict: `RLP(U256)` with leading zeros trimmed, NOT fixed 32 B).
    - P2: predict the empty-storage root (predict `EMPTY_ROOT_HASH`).
    - P3: predict the storage key form (predict `keccak256(slot.to_be_bytes())`, i.e. *hashed* slot).
    - P4: predict whether reusing the `StateRoot` walker scoped to one account "just works."
    - P5: **list every Phase-2 test you predict your first attempt will fail.**
  - *Mental model*: `StateRoot` restricted to a single account's storage MPT — same walker/hash-builder
    machinery, different leaf encoding and key derivation.
  - *Contract*: `StorageRoot<S: HashedNodeStorage>::new(account: B256)` with `compute() -> Result<(B256,
    TrieUpdates), Error>`; reuses `TrieWalker` + `HashBuilder` from Wednesday/Friday. NEVER the body.
  - *Back-of-envelope*: most accounts have 0 storage slots (→ instant `EMPTY_ROOT_HASH`); contracts have
    1–10⁶. The walk is O(dirty storage slots × depth) per account; the cost dominating `StateRoot` is the
    *number of accounts with dirty storage*, not any single storage root. Bar (b): correctness-focused; perf
    optional (the Wednesday walker bench already covers the hot loop).

  - **Requirements** — `crates/eth-trie/notes/20_storage_root_design.md`:
    - R1. `StorageRoot<S>::new(account)` + `compute() -> (B256, TrieUpdates)`.
    - R2. Empty storage trie → `EMPTY_ROOT_HASH` (not `B256::ZERO`).
    - R3. Storage key = `keccak256(slot)`; the trie is keyed by *hashed* slots (matching reth/geth).
    - R4. Storage value encoding = `RLP(U256)` with leading-zero bytes trimmed (RLP minimal-big-endian).
    - R5. Reuses `TrieWalker` + `HashBuilder`; no new traversal code.
    - R6. `StateRoot` (Friday) calls `StorageRoot::compute` for each account with storage; the returned root
      goes into the account's `storage_root` field.

  - **Design walk-through**:
    - D1. Why hash the slot key (`keccak(slot)`) instead of using the raw slot as the trie key? (Hint:
      uniform key distribution → balanced trie; raw sequential slots would skew depth.)
    - D2. RLP-of-trimmed-U256: what does `RLP(U256::from(1))` encode to vs a fixed 32-byte `0x00..01`? (Hint:
      `0x01` one byte vs 33 bytes — the trie hash differs.)
    - D3. **Anti-shortcut audit**: the "obvious" reach is to encode the storage value as the raw 32-byte word —
      one line on why wrong HERE: consensus encodes storage values as RLP-trimmed-U256; a fixed word forks every
      contract account.
    - D4. **Pre-mortem (load-bearing)**: write the 4-step trace where encoding `U256::from(1)` as 32 bytes
      instead of `0x01` produces a storage root that disagrees with mainnet → the account's `storage_root` is
      wrong → the *state* root is wrong → fork.

  - 🧮 **Paper drill** — write in `notes/20_storage_root_drill.md`:
    - D-WALK on a single contract account with one storage slot (slot 0, value `U256::from(1)`): hand-derive
      the storage trie key `keccak256(slot.to_be_bytes())`, encode the value as `RLP(U256)` = the single byte
      `0x01` (NOT a fixed 32-byte word), build the one-leaf trie, and compute the storage root by hand —
      showing it differs from `EMPTY_ROOT_HASH` and from the fixed-32-byte-word encoding.

  - **Output**: `notes/20_storage_root_design.md` with R1–R6 + D1–D4 + the value-encoding trace;
    plus `notes/20_storage_root_drill.md` with the hand-derived single-slot storage root.

  #### Phase 2 — Write the failing tests (~25 min)

  - [ ] **(NEWBIE) `empty_storage_root_is_empty_root_hash`** — account with no storage; `compute()` returns
    `EMPTY_ROOT_HASH`, not zero. Exercises R2.
  - [ ] **(SENIOR, the core trap) `storage_value_is_rlp_trimmed_u256`** — exercises R4.
    ```rust
    #[test]
    fn storage_value_is_rlp_trimmed_u256() {
        let mut s = MemoryHashed::default();
        s.put_storage(ACCT, keccak256(&slot_bytes(0)), U256::from(1));
        let (root, _) = StorageRoot::new(ACCT).compute_over(&s).unwrap();
        // a fixed-32-byte encoding of value 1 produces a DIFFERENT root than RLP(0x01)
        assert_eq!(root, KNOWN_SINGLE_SLOT_ROOT,
            "storage value must be RLP-trimmed U256 (0x01), not a fixed 32-byte word");
    }
    ```
  - [ ] **(SENIOR, cross-primitive) `storage_key_is_hashed_slot`** — inserting via raw slot vs `keccak(slot)`
    yields different roots; the consensus-correct one uses the hashed slot. Composes with the W19 keccak work
    and the trait split's `storage_cursor`. Exercises R3.
  - [ ] **(SENIOR, cross-primitive) `state_root_calls_storage_root_for_contract`** — a contract account with 3
    storage slots; Friday's `StateRoot` produces a root whose account encoding carries the `StorageRoot` output
    in the `storage_root` field. Composes StorageRoot into StateRoot. Exercises R6.
  - [ ] **(SENIOR, negative-control) `single_slot_root_differs_from_empty`** — test-the-test: a one-slot storage
    root must differ from `EMPTY_ROOT_HASH`. Guards against a `compute()` that ignores its input and always
    returns the empty constant (which would pass `empty_storage_root_is_empty_root_hash` while being broken).

  - 🪤 **Trap-test discipline reminders**:
    - *Cross-primitive*: `state_root_calls_storage_root_for_contract` + `storage_key_is_hashed_slot` compose
      StorageRoot into Friday's StateRoot and the W19 keccak path.
    - *Anti-shortcut*: the fixed-32-byte value encoding (D3) "works" structurally but forks consensus.
    - *Negative-control*: `single_slot_root_differs_from_empty` proves `compute()` reads its input.

  #### Phase 3 — Make the tests pass, one failure at a time (~30 min)

  - **(SENIOR) When `storage_value_is_rlp_trimmed_u256` produces a root that disagrees with mainnet:**
    - Symptom: the one-slot storage root differs from the known fixture; the difference traces to the leaf value bytes.
    - Why: the storage value was encoded as a fixed 32-byte big-endian word (`U256::to_be_bytes()`), but
      consensus encodes it as `RLP(U256)` with leading zeros trimmed — value `1` is the single byte `0x01`.
    - Fix: encode storage leaf values as `alloy_rlp::encode(value)` on the `U256` (trims leading zeros), not the
      raw 32-byte word.
    - Cost avoided: every contract account's `storage_root` wrong → state root wrong → chain fork at the first
      contract-touching block. (bar (b): correctness-fatal.)
    - Muscle: Ethereum storage values are RLP-trimmed-U256, never fixed-width words; the encoding is consensus,
      not a layout choice. Reapplies at: W31 persistent MPT, W42 ExecutionStage storage writes, W92 block-stm.

  - **(SENIOR) When `storage_key_is_hashed_slot` produces an unbalanced/wrong-root trie:**
    - Symptom: inserting at raw slot keys produces a root that disagrees with mainnet (and a skewed-depth trie).
    - Why: the trie was keyed by the raw slot index, but reth/geth key the storage trie by `keccak256(slot)`.
    - Fix: derive the trie key as `keccak256(slot.to_be_bytes())` before insertion.
    - Muscle: both account and storage tries are keyed by keccak of the identifier (address / slot), never the
      raw value — hashing is what gives the uniform key distribution consensus relies on. Reapplies at: W31
      persistent MPT, W42 ExecutionStage.

  - **#### 5-year failure mode** (`notes/20_storage_root_followup.md` after green): Verkle (EIP-6800) merges the
    account and storage tries into one unified key space, eliminating per-account storage roots. `StorageRoot`
    becomes obsolete; storage slots become leaves in the single Verkle tree. Trigger: Verkle activation, ~2027-2028.
- [ ] Pass simplest Ethereum trie test vectors end-to-end. (~30 min) — `Output: tests/eth_trie_vectors.rs green`.
- [ ] **Build**: `crates/eth-trie/docs/reth_delta/` directory — 3 design memos, one per reth-trie internal method that your `eth-trie` v0.2 does NOT mirror. Each memo: what reth-trie does, why your version doesn't need it (yet), what triggers adding it. Pick 3 from: `HashedPostState::extend`, `TrieUpdates::from_revert_state`, `TrieWalker::skip_subtree`, `ProofRetainer::with_targets`, `IntermediateStateRootState`, `StorageProof::default_with_root`. (~120 min)
  - 🔧 **Decomposition prompt**: today's `StorageRoot` ship completes eth-trie v0.2 — the seed → expansion arc that started W10. But reth-trie has 4-5 methods you deliberately didn't port. Without writing down WHY, future you (or a reader) won't know whether they were forgotten or skipped. The 3 memos make the omissions deliberate: each one names the upstream method, explains the trade-off, and pins the trigger that would force adding it (likely W31 storage-trie persistent backing, W44 storage-trie v1.0, or W91 consensus-engine integration). Without this artifact, the inheritance-ratio claim ("eth-trie covers reth-trie except for X/Y/Z") has no receipts.
  - 🎯 **Hardened acceptance**:
    - **3 markdown files** in `crates/eth-trie/docs/reth_delta/`: e.g. `hashed_post_state_extend.md`, `proof_retainer_with_targets.md`, `intermediate_state_root_state.md`. Pick 3 that touch DIFFERENT subsystems (state mutation, proof generation, incremental root) — not three flavors of the same omission.
    - **Each memo structure**: (a) reth's signature + 1-paragraph what it does; (b) your eth-trie's equivalent (or "not implemented"); (c) why omitted now (e.g. "incremental root only matters for persistent backing — defer to W31"); (d) trigger condition that would force adding it; (e) URL to reth's source for that method.
    - **No "I forgot this" memos**: every omission is either deliberate (workload-justified) or upcoming-deferred (W-number pinned). If you find an omission that's neither, add it to the v0.3 backlog instead of writing a memo.
    - **Inheritance ratio claim**: at the bottom of `crates/eth-trie/README.md`, an explicit "eth-trie v0.2 covers reth-trie modulo: [the 3 memo links]". This makes the ratio claim auditable.
    - **5-year failure mode**: when reth-trie itself refactors (e.g. parallel state root lands upstream — see https://www.paradigm.xyz/2024/04/reth-perf), some of your "deferred" methods become obsolete and new ones appear. State the audit cadence: a re-read at W44 (storage-trie v1.0 ship) and again at W94 (block-stm exec-vm ship) re-validates the delta.
  - *Mental model*: a mirror crate's value is in WHAT it mirrors AND what it deliberately doesn't. Capturing both is the honest report; capturing only what's done is marketing. The 3 memos are the discipline that turns an inheritance claim into an audit-ready position.
  - *Source*: https://github.com/paradigmxyz/reth/tree/main/crates/trie/trie/src (reth-trie source); your eth-trie crate sources.
  - **Expect to hit #1 — picking 3 omissions that all live in the same module**: 3 memos all about `ProofRetainer` variants. Symptom: the delta looks like "we don't do proofs deeply" — narrow. The reader can't tell whether the crate is broadly aligned or narrowly tracking one subsystem.
    - Fix: pick 3 omissions across different reth-trie modules (state, proof, walker). The delta then sketches the FULL inheritance landscape, not just one corner.
    - Muscle: a delta sample must span the upstream codebase's surface; sampling one module hides the rest of the gap. Reapplies at: every Reth-track mirror crate (eth-stage W22, storage-trie W44, exec-vm W94, consensus-engine W91).
- [ ] Tag `eth-trie v0.2.0`. (~5 min) — `Output: git tag eth-trie-v0.2.0; README inheritance-ratio claim present`.
- [ ] Commit + log (~10 min)

---

**Sunday — 🔁 Rebuild Day (~5h, ngày làm việc full — không tổng kết, thuần rebuild)**

> Cold re-implementation từ **file trống** — không xem lời giải cũ / blog / chat. Oracle = test suite của chính artifact (phải xanh). `cargo +nightly miri test` cho item concurrency. Ghi peek-log (kẹt ở đâu) vào `notes/rebuild_<item>.md`; help-surface phải co lại qua các rep.

- [ ] **R1 · Signed-int RwLock (+writer-fairness)** *(APEX, rep 1/4, ~5h)* — `crates/concurrent/src/rwlock.rs` *(gốc W013)*
  - oracle: loom suite + Miri (negative-control must-be-slow)

