# Week 94 — `exec-vm` v1.5 SHIP (block-stm parallel EVM, Reth spine) + final M24 PR push

> **Phase**: 5
> **Month**: M24
> **Track**: Half-1 Reth-spine (primary this week — the crypto-protocol spine / income-floor bridge / on-chain-settlement design surface) + Tempo (RWA-collateral option leg)
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: Reth `crates/engine` PR push; **Aptos block-stm read-only-globals exemption** (`aptos-block-executor` — "constants are never conflict edges") + **Aptos `BlockExecutor` long-lived worker pool** (`executor.rs` thread-pool reuse, not per-block spawn). `exec-vm` v1.5 ships at **bar (b)** (Reth-spine crate per §3 row 16 + the bar policy: Reth/db/Tempo = bar (b)).
> **Crate(s) touched**: Reth, Tempo, **`exec-vm` v1.0 → v1.5 SHIP** (`parallel-evm` opt-in feature): NET-NEW this week = `crates/exec-vm/src/parallel/globals.rs` (read-only-globals registry) + `crates/exec-vm/src/parallel/pool.rs` (long-lived worker pool); v1.5 wires together W91 `rwset.rs` + W92 `versioned_memory.rs` + W93 `executor.rs`. **No venue crate is touched this week** — exec-vm is the *spine*, not the venue half.
> **Bar**: (b)   ← `exec-vm` is Reth-track (bar (b) per §Bar policy). BUT the two NET-NEW components this week (`globals.rs`, `pool.rs`) are the parallel-EVM hot path: false-conflict classification and worker-pool reuse are perf-load-bearing, so both Builds carry **two-track perf cards** even though the crate bar is (b). The SHIP gate (≥2× low-conflict speedup) is unattainable without them — they behave like bar-(c) failures *for this crate*.
> **Feeds into**: **W96 M24 derivatives-infra readiness checkpoint** (exec-vm v1.5 is the Reth-spine flagship anchoring the crypto-infra-IC *bridge fallback*, §7 — NOT the goal; the controllable-outbound metric and the venue crates are the goal); **W103+ `perp-dex-core` capstone** (the parallel-EVM trace becomes a stable bench *workload sample* for the venue store + the on-chain-settle side of the hybrid CLOB design — the settle interface `SettlementId` per §4A wires a real chain into `exec-vm` later); `consensus-engine` v1.5+ (future — flips `parallel-evm` on by default after burn-in, the spine's L2-optionality leg); M25–M30 bet/bridge outreach (Reth-spine artifact that underwrites the crypto-infra-IC fallback and the on-chain-settlement narrative for derivatives-venue conversations).
> **References** (consult when stuck — NOT scheduled cover-to-cover): Aptos block-stm paper §4–5 (read-only globals / Estimate / scheduler) + `aptos-block-executor/src/executor.rs` (worker-pool reuse) + `aptos-mvhashmap/src/lib.rs`; Reth parallel-EVM PR (Paradigm) discussion thread (cite the PR number in the blog); your own W91 rwset / W92 versioned_memory / W93 executor design + followup notes; `rayon` `ThreadPool::install` docs; `crossbeam_channel::bounded` docs; RECONCILED_PLAN_v3.md §3 row 16 (exec-vm spine + block-stm), §4 `storage-trie`/`exec-vm`/`consensus-engine` reframe row, §5 M24 checkpoint, §7 (spine = income floor + on-chain-settle design + L2 optionality).
> **v3 modifications**: (1) **Frontmatter narrative re-aimed.** v2 framed exec-vm v1.5 as a "Tier-A HFT IC job" / Path-E anchor; v3 re-narrates it as the **crypto-protocol spine** — the income-floor *bridge fallback* (crypto-infra IC, §7), the **on-chain-settlement design surface** of the hybrid CLOB (the venue's settle side, §4A), and the **high-perf-L2 optionality** leg. The build (block-stm) is unchanged; the *why* changes. (2) **`mini-db` references removed.** v2 fed the parallel-EVM trace into a `mini-db` SEED (W95); v3 demotes `mini-db` (it is a deferred KV/query facade, default DO-NOT-BUILD — §4). The trace instead becomes a stable **bench workload sample for the `perp-dex-core` venue store** (the ScyllaDB leg) and the on-chain-settle side. Downstream pointers re-aimed to W96 / W103+ accordingly. (3) **M24 close re-framed.** Saturday's reflection closes M24 toward the **W96 derivatives-infra readiness checkpoint** (§5: matching-engine + oracle-mark + risk-engine at bar c; 7-system coverage ≥55; the *controllable outbound* metric — ≥20 targeted on-chain-derivatives conversations / ≥25% response / ≥3 Binance-alumni warm intros) — replacing the v2 "Five-Path decision / 1 HFT recruiter inbound" framing.

**The v3 spine framing this week locks in (read before Monday):** `exec-vm` is **Half-1 — the crypto-protocol spine**, and it pays for itself three ways even though it is *not* the venue half: (i) it is the **income-floor bridge fallback** (a crypto-infra IC seat on the Reth spine, if the crypto-MM cash seat isn't taken — §7); (ii) it is the **on-chain-settlement design surface** of the hybrid CLOB — the venue matches off-chain on the VSR log and *settles* on a chain, and that chain's executor is exactly this; the v1.0 capstone's `SettlementId`/`finality_status` signature (§4A) is designed so a real chain (this) drops in at v1.5 with no engine rewrite; (iii) it is **high-perf-L2 optionality** (a named target sub-category). Block-stm parallel execution is the spine's headline systems result. This week ships it at bar (b), honestly. It does **not** touch the venue crates — keep the two halves cleanly separated so the W96 checkpoint can audit each on its own axis.

---

## Monday — Final M24 Reth + Tempo PR volume (spine + RWA option)

- [ ] Reth: open 2–3 smaller PRs from the W93 final-feature backlog. (~60 min)
  - File(s): your Reth fork branches; link each PR in `notes/pr_log.md`.
  - Output: `notes/pr_log.md` lists each PR URL + one-line scope; `gh pr list --author @me` shows them open.
  - *v3 note*: each merged spine PR strengthens the **crypto-infra-IC bridge fallback** (§7) — the floor under the bet, not the bet. Tag them as spine in the log.
- [ ] [Tempo] Ship any final Tempo PRs ready before the M24 calibration (tempo-evm-ext / tempo-payment-lane follow-ups). (~60 min)
  - File(s): Tempo fork branches; log in `notes/pr_log.md`.
  - Output: PR URLs in `notes/pr_log.md`; CI green on each.
  - *v3 note*: Tempo is the **RWA-perps collateral-rails option leg** (§2). Keep it warm; it is *not* on the critical path.
- [ ] Commit + log (~10 min)

---

## Tuesday — `exec-vm` v1.5 pre-ship dry run (de-risk Wednesday)

> Wednesday is the SHIP. Tuesday de-risks it: run the Wed gates once today so a real failure (a determinism mismatch, a missing read-only-global) is found with a day of slack, not at tag time. This is the W93 executor followup's TRIGGER-condition check.

- [ ] Re-read the W91/W92/W93 exec-vm followup notes + the W94 pre-mortems (Wed Build D2/D3). (~20 min)
  - File(s): `notes/91_rwset_followup.md`, `notes/92_versioned_memory_followup.md`, `notes/93_executor_followup.md`.
  - Output: a 5-line "known-risk" list at the top of `notes/ship_blockers_w94.md` (carried into Wed's GO/NO-GO matrix).
- [ ] Dry-run the determinism gate on 20 of the 100 mainnet blocks (fast subset). (~40 min)
  - File(s): `crates/exec-vm/benches/traces/`, `cargo test --features parallel-evm parallel_matches_sequential -- --subset 20`.
  - Output: 20/20 byte-identical OR a captured failing block number in `notes/ship_blockers_w94.md` to fix before Wed.
- [ ] Dry-run the low-conflict speedup bench at 8 cores on ONE DEX block (smoke, not the full sweep). (~30 min)
  - Output: a single median speedup number recorded; if <1.8×, flag perf #1 (read-only globals) as the likely cause for Wed.
- [ ] Commit + log (~10 min)

---

## Wednesday — `exec-vm` v1.5 SHIP day (block-stm parallel EVM)

> Two NET-NEW Builds make the SHIP gate reachable, then the 7-dimension SHIP-day ritual. The Builds are the lesson; the ritual is the discipline. The headline gate — **8-core low-conflict speedup ≥2.0×** — is **unattainable** until both Builds below are green (the W93 executor alone gives ~0.5× because every tx false-conflicts on `BLOCKHASH`/`COINBASE`, and the per-validation-round `rayon::scope` re-spawn eats another ~5%).

### Reviews (light, parallel to the ship)

- [ ] 5+ reviews across Reth + Tempo, mixed. (~30 min)
  - Output: review URLs appended to `notes/pr_log.md`.

### Build 1 — read-only globals registry (the headline trap)

- [ ] **Build (bar (b) crate; perf gate is bar-(c)-class)**: `crates/exec-vm/src/parallel/globals.rs` — a per-block `ReadOnlyGlobals` registry so reads of block-level constants (`BLOCKHASH`, `COINBASE`, `TIMESTAMP`, `DIFFICULTY`/`PREVRANDAO`, `GASLIMIT`, `CHAINID`, `BASEFEE`) never enter `RwSet.reads` and thus never create conflict edges. (~total 110 min across 3 phases)
  - 🔧 **Why this matters**: mirrors Aptos block-stm's read-only-globals exemption — the single insight that turns block-stm from "theoretically correct, practically 0.5×" into ">2×". Traps: (1) every tx in any block reads `BLOCKHASH`+`COINBASE`, so if those reads are recorded, the validator sees N×N pseudo-conflicts and re-executes everything; (2) the obvious "skip recording" must STILL serve a consistent value to all tx_idx or determinism breaks; (3) `PREVRANDAO`/`DIFFICULTY` share an opcode slot post-Merge — classify by block context, not opcode number. Consumed by: W94 SHIP low-conflict speedup gate (blocked without this), W94 Build 2 (pool sees fewer re-exec rounds), consensus-engine v1.5+ default-on. **v3 note**: this is the *spine's* parallelism result — the same false-conflict-classification discipline reappears in the venue half at W74 matching-engine static reference data and W88 ledger VOPR read-only `lookup`. **First-attempt impls record the global reads and fail `globals_no_conflict_on_pure_dex_block` + the 8-core speedup bench — the debug session IS the lesson.**

  #### Phase 1 — Problem, prediction, design (~30 min, no code yet)
  - 🎯 **Pre-prediction prompt** — write sealed answers in `notes/94_globals_prediction.md`:
    - P1: which type you'd reach for to hold the exempt keys (exact import line, e.g. `use std::collections::HashSet;` vs a fixed array vs `crate::rwset::RwSet`).
    - P2: predicted re-execution count per tx on a 250-tx pure-DEX block IF global reads are recorded (answer in N×N terms) vs if exempt.
    - P3: predicted 8-core low-conflict speedup WITH global reads recorded (your honest guess; reality is ~0.5×).
    - P4: how a reader sees a *consistent* `COINBASE` value across all workers if the read is never recorded — name the mechanism.
    - P5: **list every Phase-2 test you predict your first attempt will fail.** Compare to reality after the bench runs.
  - *Mental model*: block-level constants are written exactly once (at block prelude, before any tx) and read by everyone. In an optimistic-concurrency validator, a "read" only matters if a *later-versioned write* can invalidate it. These keys have no in-block writer, so the read can never go stale — recording it is pure pseudo-conflict noise.
  - *Contract*: `pub struct ReadOnlyGlobals { /* immutable after block prelude */ }` with `fn seed(prelude: &BlockEnv) -> Self`, `fn is_exempt(&self, key: &StorageKey) -> bool`, `fn value(&self, key: &StorageKey) -> Option<Value>`. `RwSet::record_read(key)` calls `globals.is_exempt(key)` first; if exempt, the read resolves via `globals.value(key)` and is NOT pushed into `reads`. NEVER write the body here.
  - *Back-of-envelope* (bar-(b) crate but the gate is perf-load-bearing): on a 250-tx block where every tx does 2 global reads, recording them creates ≈250×249 = ~62k pseudo-conflict edges; the validator re-executes the whole block ~k times → effective speedup collapses to **~0.5× (worse than sequential)**. `is_exempt` itself must be cheap: a fixed-set membership check on ≤8 keys = a linear scan over 8 `StorageKey` (≤8 × ~4ns compares = ~32ns) is fine vs the ~480µs/tx exec budget — DO NOT reach for a heap `HashSet` (one alloc + hash per check, cache-cold bucket walk ~100ns) when a `[StorageKey; 8]` stack array is faster and zero-alloc.
  - **Requirements** R1..R6 — write in `notes/94_globals_design.md`:
    - R1. Seeded exactly once per block, before any tx executes; immutable thereafter (single-writer).
    - R2. `is_exempt` returns true for all 7+ block-level constant keys, false for any account/storage slot.
    - R3. An exempt read resolves to the SAME value for every tx_idx (consistency).
    - R4. An exempt read adds ZERO entries to `RwSet.reads` (no conflict edge).
    - R5. A non-exempt read of a key that *happens to equal* a global's numeric value is NOT exempted (classify by key identity/origin, not value).
    - R6. Post-Merge `PREVRANDAO` and pre-Merge `DIFFICULTY` map to the right registry entry by block context.
  - **Design walk-through** D1..D4 — answer in the design doc; no peeking at Aptos `executor.rs` until Phase 3:
    - D1. Property table for the holder type: `HashSet<StorageKey>` (alloc? yes; lookup ~100ns cold) vs `[StorageKey; 8]` linear scan (alloc? no; ~32ns) vs perfect-hash. Pick and justify.
    - D2. **Pre-mortem (load-bearing):** write the EXACT 5-line bad interleaving where global reads ARE recorded — tx0 reads COINBASE@v0, tx1 "writes" nothing but validator treats COINBASE as a versioned slot, tx2's read of COINBASE is flagged stale vs tx1's incarnation, cascade re-exec fires. Show the speedup-to-0.5× path.
    - D3. **Pre-mortem 2:** write the 4-line interleaving where exempt reads return INCONSISTENT values (tx0 sees COINBASE=A, tx5 sees COINBASE=B because `value()` reads a mutable cell) → determinism gate fails. Show why R1 immutability prevents it.
    - D4. **Anti-shortcut audit:** "just don't call record_read for any read that hits the account-zero address" — wrong (COINBASE is a real address; account-zero is unrelated). "Use a global `static HashSet`" — wrong (CHAINID/BASEFEE differ per block; must be per-block seeded). One line each.
  - **Output**: `notes/94_globals_design.md` with R1..R6, D1..D4, both bad-interleaving sketches.

  #### Phase 2 — Write the failing tests (~30 min)
  Each test traps a common naive impl. Annotate the tier. `crates/exec-vm/src/parallel/globals.rs` `#[cfg(test)]` + a bench assertion in `benches/parallel_vs_sequential.rs`.

  - **(NEWBIE) `globals_exempt_read_adds_no_conflict_edge`** — traps "record everything":
    ```rust
    #[test]
    fn globals_exempt_read_adds_no_conflict_edge() {
        let g = ReadOnlyGlobals::seed(&dex_block_env());
        let mut rs = RwSet::new(/*tx_idx*/ 7);
        rs.record_read_with_globals(coinbase_key(), &g);   // exempt path
        assert!(rs.reads().is_empty(),
            "exempt global read leaked into reads: {:?} — every tx false-conflicts",
            rs.reads());
    }
    ```
  - **(SENIOR) `globals_value_consistent_across_tx_idx`** — traps a mutable/non-seeded holder (R3):
    ```rust
    #[test]
    fn globals_value_consistent_across_tx_idx() {
        let g = ReadOnlyGlobals::seed(&dex_block_env());
        let v0 = g.value(&coinbase_key()).unwrap();
        let v9 = g.value(&coinbase_key()).unwrap();        // different "tx", same registry
        assert_eq!(v0, v9, "COINBASE differs across reads — determinism will break");
    }
    ```
  - **(SENIOR) `globals_does_not_exempt_collision_slot`** — traps "exempt by value, not identity" (R5):
    ```rust
    #[test]
    fn globals_does_not_exempt_collision_slot() {
        let g = ReadOnlyGlobals::seed(&dex_block_env());
        // a normal storage slot whose key numerically equals COINBASE's address word
        let collide = storage_slot_keyed_like_coinbase();
        assert!(!g.is_exempt(&collide),
            "a real storage slot got exempted — its writes would be silently ignored");
    }
    ```
  - **(EXPERT) cross-primitive trap `globals_no_conflict_on_pure_dex_block`** — composes globals.rs with W91 rwset + W93 executor; passes each in isolation, fires on the composition:
    ```rust
    #[test]
    fn globals_no_conflict_on_pure_dex_block() {
        // 250 DEX swaps on DISTINCT pools — zero real conflicts.
        let block = pure_dex_block(250);
        let stats = run_parallel(&block, /*globals*/ true);
        assert_eq!(stats.reexecutions, 0,
            "{} re-executions on a conflict-free block — global reads created false edges",
            stats.reexecutions);
    }
    ```
  - **(EXPERT) anti-shortcut / negative-control bench `bench_low_conflict_speedup_band`** — proves the bench measures real speedup AND that the obvious "record-everything" impl fails the BAR:
    ```rust
    // benches/parallel_vs_sequential.rs (criterion); also a guarded #[test] for CI fast-fail.
    #[test]
    fn bench_low_conflict_speedup_band() {
        let block = pure_dex_block(250);
        let seq = time_sequential(&block);
        let par = time_parallel_8core(&block);          // globals registry ON
        let speedup = seq.as_secs_f64() / par.as_secs_f64();
        // negative-control: bench must land in a physically-plausible band, not 0 or 100×
        assert!(speedup > 1.5 && speedup < 16.0,
            "speedup {speedup:.2}× outside plausible 8-core band — bench measures nothing");
        // anti-shortcut: the BAR gate itself
        assert!(speedup >= 2.0,
            "8-core low-conflict speedup {speedup:.2}× < 2.0× SHIP gate — \
             read-only globals not exempted?");
    }
    ```

  #### Phase 3 — Make the tests pass, one failure at a time (~30 min)
  Ordered failure cards, newbie first.
  - **(NEWBIE) When `globals_exempt_read_adds_no_conflict_edge` reports `reads = [coinbase_key]`:**
    - Symptom: the exempt read still landed in `RwSet.reads`.
    - Why: `record_read` pushed unconditionally; the exempt check was never wired before the push.
    - Fix: in `record_read_with_globals`, `if globals.is_exempt(&key) { return self.resolve_exempt(key, globals); }` BEFORE the `self.reads.push(...)`.
    - Cost avoided: ~62k pseudo-conflict edges per block → the difference between the feature working and not.
    - Muscle: in optimistic concurrency, the exemption check must guard the *recording* site, not post-filter. Reapplies at: W74 matching-engine static reference data (symbol metadata never conflicts), W88 ledger VOPR `lookup` op (read-only — never a conflict edge), W82 matching-engine STP exemption.
  - **(SENIOR) When `globals_value_consistent_across_tx_idx` returns two different values:**
    - Symptom: COINBASE read twice yields A then B.
    - Why: the holder stored a `&mut BlockEnv` or recomputed from a clock, so the value drifted between reads (R1/R3 violated).
    - Fix: `seed()` snapshots each constant into an owned, immutable `Value` once; `value()` returns a copy of the frozen snapshot.
    - Cost avoided: silent cross-tx determinism divergence — surfaces only in the byte-identical gate.
    - Muscle: a block-level constant is single-writer-at-prelude, immutable-after — freeze it, never recompute. Reapplies at: W92 versioned-memory Estimate immutability, W91 ledger op_num freeze, W82 matching-engine session-constant reference data.
  - **(SENIOR) When `globals_does_not_exempt_collision_slot` exempts a real slot:**
    - Symptom: a normal storage slot got `is_exempt == true`; its later write is dropped.
    - Why: exemption keyed on the numeric value or low bits instead of the (address, slot-class) identity.
    - Fix: classify by the *origin* of the read (opcode is BLOCKHASH/COINBASE/etc.) recorded at read time, not by the resulting key's value.
    - Cost avoided: a silently-ignored storage write → wrong state root (correctness, the worst class for a settlement chain).
    - Muscle: classify constants by provenance, not by value — value collisions are real. Reapplies at: W74 matching-engine reference-data classification, W88 ledger op-type tagging.
  - **(EXPERT) When `globals_no_conflict_on_pure_dex_block` reports `reexecutions = 61750`:**
    - Symptom: a conflict-free block re-executes ~N² times.
    - Why: composition gap — globals.rs is correct alone, but the W93 executor's validator reads `RwSet.reads`, which still contained globals because `record_read_with_globals` wasn't the path the executor called.
    - Fix: route the executor's SLOAD handler through `record_read_with_globals(key, &block_globals)`; thread `&ReadOnlyGlobals` into the per-tx exec context.
    - Cost avoided: ~62k pseudo-conflict edges → 0; re-execution count N²→0 on conflict-free blocks. This is the difference between the crate working and not.
    - Muscle: a correct component is inert unless the *caller* routes through it — wire the exemption at the only SLOAD site. Reapplies at: W93 executor read-resolution path, W92 versioned-memory read API, W91 rwset record sites.
  - **(EXPERT, perf — bar-(c)-class for this crate) When `bench_low_conflict_speedup_band` reports `speedup 0.52× < 2.0×`:**
    - Symptom: parallel is SLOWER than sequential on a conflict-free block.
    - Why: global reads recorded → N×N validation failures → whole-block re-execution dominates; the worker count makes it worse, not better.
    - Fix: the read-only-globals exemption (this whole Build); verify with `stats.reexecutions == 0`.
    - Cost avoided: speedup ~0.5× → ~2–3× on low-conflict blocks. The headline SHIP gate (8-core low-conflict ≥2.0×) is unreachable without it — **(bar (c) violation for this crate: the parallel feature is shippable-as-bar-b only if this holds).**
    - Muscle: in any optimistic-concurrency system, identify the block/session/init constants and exclude them from conflict tracking BEFORE tuning the scheduler. Reapplies at: W88 ledger VOPR `lookup`, W74 matching-engine static reference data, W82 matching-engine STP exemption, W92 versioned-memory read-only-base handling.

  #### 5-year failure mode
  Write in `notes/94_globals_followup.md` after green: a future EIP adds a *mutable* block-level opcode (e.g. a per-tx-updatable beacon-root slot, EIP-4788-style) — the registry's immutability assumption breaks. TRIGGER: any new opcode whose value can change mid-block. Migration (1 line): demote that key from `ReadOnlyGlobals` to a pre-seeded *versioned* slot in versioned_memory so it participates in normal conflict tracking.

- [ ] Commit + log (~10 min)

### Build 2 — long-lived worker pool (the hidden re-spawn trap)

- [ ] **Build (bar (b) crate; perf gate is bar-(c)-class)**: `crates/exec-vm/src/parallel/pool.rs` — a single process-lifetime worker pool the block-stm scheduler enqueues tx-exec tasks into, replacing per-block / per-validation-round `rayon::scope(...)` spawns. (~total 100 min across 3 phases)
  - 🔧 **Why this matters**: mirrors Aptos `BlockExecutor`'s reuse of one thread pool across blocks. Traps: (1) `rayon::scope` is cheap once but the block-stm validator re-enters it once *per re-execution round* — 5 rounds × ~50µs scope-setup on a 5ms block = ~5% overhead the optimizer can't elide (closures vary); (2) a fresh `ThreadPool::new` per block pays ~500µs cold pool materialization; (3) panic in a worker must not poison the pool for the next block. Consumed by: W94 SHIP speedup gate (the last ~5% to hold ≥2.0× on the median block, not just the lucky block), W93 executor (becomes the executor's task sink). **v3 note**: the "workers live for the process, tasks flow through a queue" pattern is the *same* discipline the venue half uses for `runtime-thread-per-core` (W85) shard threads and the W88 VOPR runner — the spine teaches it here at bar (b). **First-attempt impls call `rayon::scope` inside the validation loop and fail `pool_no_respawn_across_validation_rounds` + lose ~5% on the bench.**

  #### Phase 1 — Problem, prediction, design (~30 min, no code yet)
  - 🎯 **Pre-prediction prompt** — write sealed answers in `notes/94_pool_prediction.md`:
    - P1: the pool primitive you'd reach for (exact import: `rayon::ThreadPool` vs hand-built `crossbeam_channel::bounded` + N threads).
    - P2: predicted scope-setup cost per `rayon::scope` call (ns) and per-enqueue cost on a long-lived pool (ns).
    - P3: predicted overhead on a 5ms block that does 5 validation rounds, IF scope is re-entered each round (% of block time).
    - P4: what happens to the pool when one worker's tx exec panics — predict, then verify.
    - P5: **list every Phase-2 test you predict your first attempt will fail.**
  - *Mental model*: "spawn workers, do work, join" is a per-call pattern; block-stm does work in many rounds. Workers should outlive blocks; only *tasks* flow per round, through a queue. The pool is set up once at exec-vm startup.
  - *Contract*: `pub struct WorkerPool { /* N threads + a task channel */ }` with `fn global(n_workers: usize) -> &'static WorkerPool` (lazy once), `fn submit(&self, task: ExecTask)`, and a per-round barrier `fn drain(&self) -> RoundResult`. The scheduler `submit`s tx-exec tasks and `drain`s at round boundary. NEVER the body here.
  - *Back-of-envelope*: `rayon::scope` setup ≈ ~50µs (cross-thread queue init + work-item enqueue); cold `ThreadPool::new` ≈ ~500µs (thread spawn × N). Long-lived pool enqueue ≈ ~5ns (channel send) + ~25ns wakeup if a worker was parked. On a 5ms block × 5 validation rounds: re-entering scope = 5 × 50µs = 250µs = **5% overhead**; long-lived pool = 5 × (n_tasks × 5ns) ≈ negligible. The 5% is the gap between "≥2× on the lucky block" and "≥2× on the median block."
  - **Requirements** R1..R5 — write in `notes/94_pool_design.md`:
    - R1. Pool created exactly once for the process; subsequent `global()` calls return the same instance.
    - R2. Per-round scheduler interaction is enqueue + drain only — ZERO thread spawn after init.
    - R3. A panic in one worker's task is caught (`catch_unwind`) and surfaced as a failed task; the pool survives for the next block.
    - R4. `drain` is a correctness barrier: it returns only after every submitted task of the round has completed (no torn round).
    - R5. Worker count default = `min(num_cores, num_txs/4)` clamp, but the *threads* are fixed at pool size; the clamp limits in-flight tasks, not thread spawns.
  - **Design walk-through** D1..D4 — no peeking at Aptos `executor.rs` until Phase 3:
    - D1. Property table: `rayon::scope` per round (spawn? per-call queue init ~50µs; ergonomic) vs `rayon` global pool `install` (one pool; still per-`scope` setup inside) vs hand-built `crossbeam_channel::bounded` + parked workers (enqueue ~5ns; manual barrier). Pick and justify.
    - D2. **Pre-mortem (load-bearing):** write the 5-line trace where `rayon::scope` is called inside `for round in 0..k { rayon::scope(|s| ...) }` — each iteration pays ~50µs, k=5, on a 5ms block → 250µs added, bench shows 1.9× instead of 2.1×, gate flickers.
    - D3. **Pre-mortem 2:** a worker tx panics (e.g. an opcode `unreachable`), the pool thread dies, the next block submits to a dead worker → hang. Show how `catch_unwind` + worker-restart prevents it.
    - D4. **Anti-shortcut audit:** "just use `rayon::par_iter` over txs" — wrong (par_iter is one parallel pass; block-stm needs many dependency-ordered rounds with a shared monotonic scheduler). "Make a new pool per block, it's only 500µs" — wrong (×N blocks in a bench sweep + it serializes nothing but burns 500µs each). One line each.
  - **Output**: `notes/94_pool_design.md` with R1..R5, D1..D4, both pre-mortems.

  #### Phase 2 — Write the failing tests (~30 min)
  - **(SENIOR) `pool_is_singleton`** — traps "new pool per block" (R1):
    ```rust
    #[test]
    fn pool_is_singleton() {
        let a = WorkerPool::global(8) as *const _;
        let b = WorkerPool::global(8) as *const _;
        assert_eq!(a, b, "global() returned two distinct pools — workers re-spawned per call");
    }
    ```
  - **(EXPERT) `pool_no_respawn_across_validation_rounds`** — the core trap; counts thread spawns:
    ```rust
    #[test]
    fn pool_no_respawn_across_validation_rounds() {
        let spawns = SPAWN_COUNTER.load(Relaxed);            // test hook around thread::spawn
        let pool = WorkerPool::global(8);
        for _round in 0..5 {                                 // simulate 5 validation rounds
            for t in dummy_tasks(40) { pool.submit(t); }
            pool.drain();
        }
        let after = SPAWN_COUNTER.load(Relaxed);
        assert_eq!(after - spawns, 0,
            "{} threads spawned across 5 rounds — scope re-entered per round",
            after - spawns);
    }
    ```
  - **(SENIOR) `pool_survives_worker_panic`** — traps no `catch_unwind` (R3):
    ```rust
    #[test]
    fn pool_survives_worker_panic() {
        let pool = WorkerPool::global(8);
        pool.submit(ExecTask::panicking());                  // one task panics
        let r = pool.drain();
        assert!(r.failed_tasks() == 1, "panic not surfaced as failed task");
        // pool must still work for the next block:
        pool.submit(ExecTask::trivial());
        assert!(pool.drain().failed_tasks() == 0, "pool poisoned by prior panic");
    }
    ```
  - **(SENIOR) `pool_drain_is_full_barrier`** — traps a torn round (R4):
    ```rust
    #[test]
    fn pool_drain_is_full_barrier() {
        let pool = WorkerPool::global(8);
        let done = Arc::new(AtomicUsize::new(0));
        for _ in 0..200 { pool.submit(ExecTask::incr(done.clone())); }
        pool.drain();
        assert_eq!(done.load(Relaxed), 200, "drain returned before all tasks finished");
    }
    ```
  - **(EXPERT) negative-control bench `bench_pool_round_overhead_band`** — proves the bench catches scope re-spawn:
    ```rust
    #[test]
    fn bench_pool_round_overhead_band() {
        let block = small_block(50);                          // ~5ms sequential, ~5 rounds
        let t = time_parallel_8core(&block);                 // long-lived pool
        let per_round_overhead = measured_round_setup_ns();   // hook
        // negative-control: a long-lived pool's per-round setup must be sub-microsecond
        assert!(per_round_overhead < 1_000,
            "per-round setup {per_round_overhead}ns — pool is re-spawning workers");
        let _ = t;
    }
    ```

  #### Phase 3 — Make the tests pass, one failure at a time (~30 min)
  - **(SENIOR) When `pool_is_singleton` reports two pointers:**
    - Symptom: `global()` builds a fresh pool each call.
    - Why: no `OnceLock`/`OnceCell` guard; the constructor runs every call.
    - Fix: `static POOL: OnceLock<WorkerPool> = OnceLock::new(); POOL.get_or_init(|| WorkerPool::build(n))`.
    - Cost avoided: ~500µs cold pool materialization per block × every block in a bench sweep.
    - Muscle: process-lifetime resources are `OnceLock`-initialized, never per-call constructed. Reapplies at: W88 VOPR runner (workers spawned once), W90 io_uring journal (ring set up once), W85 runtime-thread-per-core shard threads (pinned at startup).
  - **(EXPERT, perf — bar-(c)-class for this crate) When `pool_no_respawn_across_validation_rounds` reports `5 threads spawned` (spawns scale with rounds):**
    - Symptom: thread spawns increment once per validation round.
    - Why: the scheduler called `rayon::scope(|s| ...)` (or `thread::spawn`) inside the per-round loop instead of submitting to a standing pool.
    - Fix: replace the per-round `scope` with `for t in tasks { pool.submit(t) } pool.drain();` against the `OnceLock` pool.
    - Cost avoided: ~250µs/block (5 rounds × ~50µs scope setup) → ~0; ~5% throughput recovery; combined with Build 1 this makes ≥2.0× hold on the *median* block, not just the lucky one. **(bar (c) violation for this crate if it flickers the gate.)**
    - Muscle: any "spawn, work, join" inside a hot loop is a perf trap — workers live for the process, tasks flow through a queue. Reapplies at: W88 VOPR runner, W90 io_uring ring, W85 runtime-thread-per-core shards.
  - **(SENIOR) When `pool_survives_worker_panic` hangs or poisons the pool:**
    - Symptom: the next block's `drain()` never returns, or the pool reports failures forever.
    - Why: the worker thread unwound on panic and died; `drain` waits on a dead worker.
    - Fix: wrap each task body in `std::panic::catch_unwind`; on catch, mark the task failed and continue the recv loop (or respawn the single dead worker).
    - Cost avoided: a single bad tx killing the pool for the whole node — a liveness outage.
    - Muscle: a shared worker pool must isolate task panics — one bad tx can't kill the pool. Reapplies at: W93 executor re-exec on poisoned tx, W83 runtime-thread-per-core task isolation.
  - **(SENIOR) When `pool_drain_is_full_barrier` sees `done < 200`:**
    - Symptom: `drain` returns early; some tasks still running.
    - Why: `drain` counted *submitted* not *completed*, or used a non-synchronizing load.
    - Fix: a per-round `AtomicUsize` completion counter + a `Condvar`/`Barrier`; `drain` blocks until completed == submitted with `Acquire` ordering.
    - Cost avoided: a torn round → the validator reads partial results → silent state corruption.
    - Muscle: a round barrier is a correctness primitive — count completions with acquire/release, never submissions. Reapplies at: W93 executor round boundary, W22 concurrent Barrier, W88 VOPR scenario join.

  #### 5-year failure mode
  Write in `notes/94_pool_followup.md`: if exec-vm later runs many blocks concurrently (e.g. speculative fork execution for the on-chain-settle side of the hybrid CLOB), one global pool serializes them. TRIGGER: >1 in-flight block at once. Migration (1 line): make the pool per-fork-executor (still `OnceLock` per executor instance, not per block).

- [ ] Commit + log (~10 min)

### `exec-vm` v1.5 wire-up + the 7-dimension SHIP-day ritual

- [ ] **Feature wire-up** (~25 min):
  - File(s): `crates/exec-vm/Cargo.toml` — `parallel-evm = ["dep:dashmap", "dep:rayon"]`; `crates/exec-vm/src/parallel/mod.rs` re-exports `BlockExecutor` behind `#[cfg(feature = "parallel-evm")]`; sequential v1.0 stays default.
  - Output: `cargo build -p exec-vm` (default) AND `cargo build -p exec-vm --features parallel-evm` both green; `CHANGELOG.md` v1.5.0 entry cites W91 rwset, W92 versioned_memory, W93 executor, W94 globals+pool.

- [ ] **Dimension 1 — Inheritance audit** (~30 min) — bar (b) target ≥0.70:
  - File(s): `notes/inheritance_audit_exec_vm_v1_5.md`.
  - Lineage (version-pinned): `exec-vm v1.0 (W68)` sequential base · `eth-primitives v0.5 (W7)` · `eth-consensus v0.5 (W41)` gas/opcode rules · `eth-storage-cache v0.5` · `eth-trie / storage-trie v1.0 (W44)` · `concurrent v0.5 (W22)` scheduler atomics. NET-NEW for v1.5: `parallel/rwset.rs (W91)`, `parallel/versioned_memory.rs (W92)`, `parallel/executor.rs (W93)`, `parallel/globals.rs (W94)`, `parallel/pool.rs (W94)`.
  - Output: `tokei --files crates/exec-vm/src/parallel/`; per-module CONSUMES/NET-NEW classification; ratio ≥0.70 recorded (sequential base + storage primitives carry the weight; parallel module is irreducibly net-new). 🎯 Hardened acceptance: ratio ≥0.70 is met or the scope is wrong.

- [ ] **Dimension 2 — Bar-(b) acceptance gate (GO/NO-GO)** (~75 min):
  - File(s): `notes/ship_blockers_w94.md` (the binary matrix).
  - G1 (~25 min) **Determinism gate (load-bearing)**: 100 mainnet blocks (DEX, USDC, NFT, MEV, mixed) — parallel byte-identical to sequential on ALL 100. `cargo test --release --features parallel-evm parallel_matches_sequential`. Expected: 100/100, 0 mismatch. Sequential-as-oracle in CI.
  - G2 (~15 min) **Low-conflict speedup gate**: 8-core DEX block median ≥2.0× (this is `bench_low_conflict_speedup_band`). `cargo bench --bench parallel_vs_sequential --features parallel-evm -- --filter low_conflict_8core`.
  - G3 (~15 min) **High-conflict regression bound**: 8-core USDC block ≥0.85× (max 20% slowdown — the intellectual-honesty number). `... --filter high_conflict_8core`.
  - G4 (~10 min) **Test-compat dual-feature**: ALL v1.0 tests pass under default AND `--features parallel-evm`. No skip/delete. `cargo test -p exec-vm --release` and `... --features parallel-evm`.
  - G5 (~10 min) **EF state-tests + memory ceiling**: 100% EF State Tests under both configs; parallel memory ≤4× sequential (`cargo bench parallel_memory_ceiling`).
  - Output: `notes/ship_blockers_w94.md` matrix — every gate GO, else STOP and defer tag:
    ```
    | Gate                              | Pass criteria  | Status | GO/NOGO |
    |-----------------------------------|----------------|--------|---------|
    | Inheritance ratio ≥0.70           | tokei + audit  |        |         |
    | Determinism 100/100 traces        | byte-identical |        |         |
    | 8-core low-conflict speedup ≥2.0× | bench          |        |         |
    | High-conflict regression ≤20%     | bench          |        |         |
    | Test compat both feature configs  | cargo test     |        |         |
    | EF state-tests 100% / mem ≤4×     | ef + bench     |        |         |
    | Reproducer command tested         | 2nd machine    |        |         |
    ```

- [ ] **Dimension 3 — TAG + push** (~10 min):
  - `git log --oneline crates/exec-vm/` since v1.0 reviewed; `git tag exec-vm-v1.5.0`; push. ★ THE MOMENT (only after ALL gates GO).
  - Output: tag visible on remote; `notes/exec_vm_v1_5_perf.md` records 4-core ≥1.6× / 8-core ≥2.0× / 16-core ≥3.0× low-conflict, ≤4× memory, P99 8-core ≤80ms, high-conflict ≥0.85×.

- [ ] **Dimension 4 — Downstream handoff + inheritance map** (~15 min):
  - File(s): `notes/inheritance_map.md`.
  - Output (v3): map updated — exec-vm v1.5 (parallel-evm feature) → consensus-engine v1.5+ (future, default-on after burn-in, the spine's L2-optionality leg) → **`perp-dex-core` (W103+) bench workload sample** (the parallel-EVM trace stresses the venue store + is the candidate executor behind the hybrid-CLOB on-chain-settle interface `SettlementId`/`finality_status`, §4A — wired in at v1.5, no engine rewrite) → bet/bridge portfolio (crypto-infra-IC fallback anchor). exec-vm v1.5 sits beside v1.0; consumers choose the feature. **(No `mini-db` arrow — `mini-db` is a deferred KV/query facade, §4.)**

- [ ] **Dimension 5 — Blog post draft** (~90 min) — "Block-STM in Rust: parallel EVM at bar (b)" in `notes/blog_block_stm.md`:
  - Sections: (1) why parallel EVM is hard (conflicts, ordering, why naive locking degenerates); (2) block-stm's execute-first/validate-after insight + upstream (Aptos paper, Reth parallel-EVM PR — cite the PR number, Sui object-model contrast); (3) RwSet + multi-version memory in Rust idiom (`Version{tx_idx, incarnation}`, dashmap+BTreeMap, Estimate/Value/Removed); (4) the read-only-globals insight (W94 Build 1 — the headline trap that recovers ≥2×) + the long-lived pool (W94 Build 2); (5) results — 4/8/16-core speedups, DEX 2–3× vs USDC ~0.9×, where it shines/fails, plot; (6) why opt-in feature flag; (7) lessons + caveats (read-only globals, MAX_VERSIONS cap, bench-reproducibility) **+ the v3 narrative tie-in: this is the crypto-protocol spine — parallel execution is the on-chain-settle engine the hybrid-CLOB venue (perp-dex-core) wires into its `SettlementId` interface, and the L2-optionality leg.**
  - Output: `notes/blog_block_stm.md` with all 7 sections drafted + reproducer command + hardware spec + raw-data link. 🎯 Hardened acceptance: the post states the high-conflict ~0.9× number prominently (overclaim = bar-(b) credibility violation).

- [ ] **Dimension 6 — Bet/bridge conversation 1-pager** (~25 min) — Reth-spine artifact, `notes/portfolio_exec_vm_v1_5.md`:
  - 1-line elevator; 3 technique bullets (RwSet+MVCC memory; optimistic executor + re-exec; read-only-globals classification + sequential fallback); outcome (2–3× @8c low-conflict, byte-identical on 100 traces, ≤20% high-conflict regression); inheritance (≥0.70). The 3 hardest pitfalls hit (named): (a) speedup<1× via read-only-globals false deps → registry (W94 Build 1); (b) Estimate-as-final leak → never return Estimate to reader (W92); (c) version-chain memory blowup → MAX_VERSIONS=256 + sequential fallback (W92). The 5 probe Qs + answer sketches: single-tx execution walk; two-tx hot-key conflict; production tuning; block-stm vs Sui object-parallelism; when parallel is WORSE + how handled.
  - 🎯 Hardened acceptance (v3): the 1-pager frames exec-vm as **(i) the crypto-infra-IC *bridge* anchor (§7 income floor) and (ii) the on-chain-settle engine for the derivatives venue** — NOT as a standalone HFT-job credential. Output: `notes/portfolio_exec_vm_v1_5.md` complete; recite the 5 Qs aloud once.

- [ ] **Dimension 7 — Methodology + close** (~30 min):
  - File(s): `notes/exec_vm_v1_5_bench_methodology.md`, `notes/block_stm_vs_alternatives.md`, `progress.md`.
  - Bench methodology: hardware spec (CPU model, core count, L1/L2/L3, RAM, NVMe), mainnet trace source (archive repo + commit SHA), 100-block selection rationale (DEX/USDC/NFT/MEV/mixed), exact `cargo bench` reproducer command, 30 runs/combo median+IQR, plot script (`scripts/plot_speedup.py`), raw JSON published. Alternatives table: Block-STM (Aptos/Reth) vs Sui object-based vs static R/W-set vs sequential (Geth) vs Sealevel (Solana) — one trade-off line each, cite in blog.
  - Output: both notes complete; `progress.md` records "exec-vm v1.5 SHIPPED (spine flagship); under-tested dimension = high-conflict regression bound, schedule more MEV-bot traces in M25." Note the v3 transition: the parallel-EVM trace becomes a **stable bench workload for the perp-dex-core venue store** (W103+); no `mini-db`.
  - MEMORY.md (1 line): "Closing M24 — Reth-spine flagship exec-vm v1.5 (W94, block-stm) ships beside ledger v1.0 (W92); spine is the income-floor bridge + the venue's on-chain-settle engine, NOT a job credential; W96 = M24 derivatives-infra checkpoint (matching+oracle+risk at bar c, coverage ≥55, controllable-outbound metric)."

---

## Thursday — Light: outreach on the spine angle (bridge + venue)

> Post-ship, take it lighter (the W92 ledger + W94 exec-vm cluster is the densest of the journey). One outreach pass, no new build.

- [ ] 3 cold-outreach messages citing exec-vm v1.5 (~45 min):
  - File(s): `notes/m24_outreach_block_stm.md` (draft + sent log).
  - Targets (v3): (1) a Reth/Paradigm parallel-EVM PR engineer (compare hot-key-classification notes, link your bench results + cite their PR) — warms the **crypto-infra-IC bridge fallback** (§7); (2) an Aptos block-stm author (Rust port at bar (b), same curve shape, ask feedback on MAX_VERSIONS=256); (3) **one on-chain-derivatives-venue engineer** — frame it as "the on-chain-settle engine behind a hybrid CLOB" (the venue's settle side, §4A), opening the derivatives-founder conversation thread the W96 controllable-outbound metric counts.
  - Output: `notes/m24_outreach_block_stm.md` logs 3 sent messages with links; 🎯 Hardened acceptance: each message references a specific bench number (not a generic pitch); at least one is logged against the **W96 outbound metric** (on-chain-derivatives conversation).
- [ ] Commit + log (~10 min)

---

## Friday — Final M24 PRs + reproducer hardening

- [ ] Verify the bench reproducer on a SECOND machine (or rented EC2/GCP node). (~45 min)
  - File(s): `crates/exec-vm/README.md` reproducer section; `notes/exec_vm_v1_5_bench_methodology.md`.
  - Output: the exact `cargo bench ... --traces ... --cores 1,2,4,8,16,32 --output ...` command runs clean on a fresh machine; any toolchain/trace-SHA gotcha added to README troubleshooting. 🎯 Hardened acceptance: speedup-curve shape matches the published plot within IQR (un-reproducible bench = bar-(b) violation).
- [ ] Close out any remaining Reth/Tempo PRs from Mon. (~30 min)
  - Output: `gh pr list --author @me` reviewed; merged/closed status logged in `notes/pr_log.md`.
- [ ] Commit + log (~10 min)

---

## Saturday — M24 close-out reflection (toward the W96 derivatives-infra checkpoint)

- [ ] Phase-5-close-out reflection (the spine + ledger flagship cluster is done). (~30 min)
  - File(s): `notes/post_w94_reflection.md`.
  - Output (v3): notes record — W92 ledger-deterministic v1.0 + W94 exec-vm v1.5 cap M24; the portfolio now spans **Half-1 spine flagships** (storage-trie, exec-vm seq+parallel, consensus-engine) and **Half-2 venue flagships** (matching-engine v1.5 VSR-replicated, marketdata-kernelbypass, messaging-aeron, ledger-deterministic v1.0 on the VSR log, consensus-vsr v1.0). The two halves are cleanly separated and each auditable on its own axis. **W95 = M24 calibration/reassessment; W96 = the M24 derivatives-infra readiness checkpoint** (§5): matching-engine + oracle-mark + risk-engine at bar (c); 7-system coverage ≥55; the **controllable outbound** metric (≥20 targeted on-chain-derivatives conversations, ≥25% response, ≥3 Binance-alumni warm intros) — *replacing the v2 "Five-Path decision / 1 HFT recruiter inbound."* **Phase 6 (W97+) opens the `perp-dex-core` capstone** (risk-engine v1.0 cross-margin first, §4A) — the parallel-EVM trace from this week becomes its bench workload + the on-chain-settle engine candidate.
  - 🎯 Hardened acceptance: the reflection names (a) the single under-tested dimension to revisit (high-conflict regression traces) AND (b) one concrete W96-outbound action seeded by Thursday's derivatives-venue message. *Mental model*: the close-out converts a ship into a forward TRIGGER (the W96 checkpoint), not a celebration; and it confirms the spine is the *floor*, the venue is the *goal*.
- [ ] Any outstanding items from the week (PR cleanup, note backfill). (~30 min)
- [ ] Commit + log (~10 min)

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
