# Week 124 — Recon-trip synthesis + application shortlist + M32 close

> **Phase**: 7
> **Month**: M32
> **Track**: (career: recon synthesis + applications) | HFT (Friday crate polish)
> **Hours target**: 40h/wk (M19-M34)
> **Mirror target**: (n/a — career synthesis week; Friday polish mirrors prior matching-engine/mini-db work, no new upstream module)
> **Crate(s) touched**: matching-engine v1.1 → v1.2 (final polish + bench-result refresh); mini-db v1.0 → v1.1 (final polish + capstone bench update)
> **Bar**: (n/a for the career items; matching-engine and mini-db retain their existing (c)/(b) bars on Friday — this week refreshes their bench numbers, it does not change scope)
> **Feeds into**: W125 active applications begin (this week's firm shortlist + staggered application schedule is the direct input); W126-W129 final rounds (Thursday matching-engine drill is the rehearsal); M32-end milestone (2900+ runtime hours, recon trip complete — README §1086); W121 flagship blog already shipped, so Friday's refreshed matching-engine bench numbers are the citation source if the blog gets an update.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): `notes/recon_notes.md` (W123-W124 trip log — the raw input for Monday synthesis); destination-geography cost-of-living + immigration portals (official sources only, lookup-when-stuck); README §"Inheritance Map" + §"APPENDIX E" (firm-tier mapping for the shortlist).
> **v2 modifications**: none

This is a career-synthesis + light-crate-polish week, not a Build week. There is no new crate, no new upstream mirror, and no engineered trap-test arc — manufacturing one would be padding. Every item below instead carries an explicit artifact file + an Output line that proves it done; the synthesis/strategy/drill items carry a `🎯 Hardened acceptance` pass criterion and a `*Mental model*` of why the item exists now. The week's job: convert the recon trip into runway/firm decisions, lock the application schedule for W125, rehearse the highest-leverage interview topic, and close M32 by refreshing the two capstone crates' bench numbers so applications cite current data.

**Monday — Recon-notes synthesis (runway impact)**

- [ ] Synthesize the recon trip into a plan-changing decision in `notes/recon_synthesis.md` — what did I learn that changes the plan, focused on cost-of-living + housing → adjusted runway. (~40 min)
  - 🎯 Hardened acceptance: `recon_synthesis.md` has a `## Runway delta` table comparing pre-trip vs post-trip monthly burn for ≥2 candidate geographies, each row sourced (cost-of-living figure carries a URL or an explicit `[ESTIMATE]` tag); a `## Runway months` line recomputes months-of-savings under the new burn; and a `## Decisions` section with ≥2 concrete changes (e.g. "drop geography X — rent +40% kills the runway margin", "geography Y viable if employer-sponsored housing").
  - *Mental model*: a recon trip that doesn't change a number on paper was a vacation. The single decision a recruiter conversation hinges on is "what comp do you need to relocate here" — and that answer is `new monthly burn × required runway months`, which this synthesis produces. Getting it wrong by ignoring post-trip housing reality means either lowballing yourself or quoting a number that no firm in the geography will meet.
  - Output: `notes/recon_synthesis.md` exists meeting the hardened-acceptance criteria; `grep -c '^|' notes/recon_synthesis.md` ≥ 3 (the runway-delta table rows).
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Monday synthesis commit.

**Tuesday — Firm shortlist (Tier A primary, Tier B floor)**

- [ ] Build the formal-application shortlist in `notes/firm_shortlist.md` — top 4-6 firms, classified Tier A (primary) / Tier B (floor). (~40 min)
  - 🎯 Hardened acceptance: `firm_shortlist.md` lists 4-6 firms, each with {tier A/B, the specific role/team, the geography + its W124-Monday runway verdict (viable/borderline/no), the warm-intro channel if any from W120/W122 outreach, and a one-line "why me" anchor tied to a shipped artifact}; ≥1 firm is Tier B explicitly tagged as the floor (the offer you take if nothing better lands). No firm appears whose geography Monday marked "no".
  - *Mental model*: a shortlist without a Tier-B floor is how candidates end up with zero offers after over-indexing on reaches. The floor exists so that W131's offer-decision is a *choice*, not a hope. Cross-referencing each firm against Monday's runway verdict prevents the classic trap of interviewing hard for a job in a city you can't afford on the comp they pay.
  - Output: `notes/firm_shortlist.md` exists meeting the hardened-acceptance criteria; `grep -c 'Tier [AB]' notes/firm_shortlist.md` between 4 and 6.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday shortlist commit.

**Wednesday — Application strategy + staggered schedule**

- [ ] Write the application approach + a dated, staggered schedule in `notes/application_schedule.md` — warm intro where available, direct recruiter otherwise; stagger so final rounds don't all collide. (~40 min)
  - 🎯 Hardened acceptance: `application_schedule.md` maps every firm from Tuesday's shortlist to {channel: warm-intro `[via <name>]` or `[direct recruiter]`, send-date in W125, expected-first-round week}; the send-dates are spread so that no two firms' expected final-rounds land in the same calendar week (cross-check against the W126-W129 final-round window); and a `## Collision check` line states the max number of overlapping final-rounds in any week (target ≤ 2).
  - *Mental model*: six final-rounds in one week means six under-prepared performances and burnout. Staggering converts a saturation risk (README §489: "Destination-tier interview cycle saturates Phase 7", 70% likely) into a paced pipeline. Front-loading warm-intro firms also matters: an intro that goes stale loses its referral value, so those get the earliest send-dates.
  - Output: `notes/application_schedule.md` exists meeting the hardened-acceptance criteria; the `## Collision check` line is present.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday schedule commit.

**Thursday — Interview-prep concentrated session: matching-engine deep-dive**

- [ ] Rehearse the matching-engine deep-dive at two depths into `notes/drill_matching_engine.md` — a 5-min and a 30-min explanation, every design decision + trade-off owned. (~90 min)
  - 🎯 Hardened acceptance: `notes/drill_matching_engine.md` contains a `## 5-minute` script (≤ ~500 words, hits: data structure choice for the order book, single-writer / sequencing model, latency budget with a real measured number, one trade-off you deliberately made) AND a `## 30-minute` outline (≥ 8 bullet topics each with the decision + the alternative rejected + why); ≥1 number in each version is a *measured* figure pulled from the crate's bench output, not a guess; and a `## Likely follow-ups` section anticipates ≥3 interviewer questions ("why not a B-tree for price levels?", "how do you handle a slow consumer?", "what breaks at 10× volume?") with one-line answers.
  - *Mental model*: HFT interviews probe whether you can defend a design under pressure or just describe it. The 5-min version is the screen ("can he summarize?"); the 30-min version is the on-site ("does he understand the trade-offs he made?"). Owning the *rejected alternatives* is the tell that separates someone who built it from someone who copied it — and it's exactly what Friday's bench refresh feeds, since "every number I quote is one I measured this week" is the strongest possible credibility signal.
  - Output: `notes/drill_matching_engine.md` exists meeting the hardened-acceptance criteria; both `## 5-minute` and `## 30-minute` headers present.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday drill commit.

**Friday — M32 capstone-crate polish: matching-engine v1.2 + mini-db v1.1**

- [ ] Final-polish matching-engine to v1.2 — refresh bench results so application/interview numbers are current. Edit `crates/matching-engine/Cargo.toml` (version bump) + re-run + record benches into `crates/matching-engine/benches/RESULTS.md`. (~60 min)
  - 🎯 Hardened acceptance: `cargo bench -p matching-engine` runs green; `crates/matching-engine/benches/RESULTS.md` has a dated W124 block with the current p50/p99 order-match latency and throughput, and a one-line delta-vs-previous note; `cargo test -p matching-engine` green; version is `1.2.x` in Cargo.toml. The number quoted in Thursday's drill matches this RESULTS.md (no stale figures cited in interviews).
  - *Mental model*: this is not new work — it is the final bench refresh before applications go out, so every latency number you quote in W125-W129 is one you measured in W124, not one from months ago that a sharp interviewer could date. matching-engine is bar (c); the polish does not change its scope (README §1098), it re-certifies the existing numbers.
  - Output: `crates/matching-engine/benches/RESULTS.md` has a W124-dated block; `cargo bench -p matching-engine` + `cargo test -p matching-engine` green; `grep -m1 version crates/matching-engine/Cargo.toml` shows `1.2`.
- [ ] Final-polish mini-db to v1.1 — capstone bench update. Edit `crates/mini-db/Cargo.toml` (version bump) + re-run + record into `crates/mini-db/benches/RESULTS.md`. (~45 min)
  - 🎯 Hardened acceptance: `cargo bench -p mini-db` green; `crates/mini-db/benches/RESULTS.md` has a dated W124 block with the current read/write throughput + a delta note; `cargo test -p mini-db` green; version `1.1.x` in Cargo.toml.
  - *Mental model*: mini-db is the database-track capstone (bar (b), README §1103). Refreshing its capstone bench alongside matching-engine means both flagship artifacts carry current, defensible numbers when the application links to the repos. Stale benches on a public repo read as an abandoned project.
  - Output: `crates/mini-db/benches/RESULTS.md` has a W124-dated block; `cargo bench -p mini-db` + `cargo test -p mini-db` green; `grep -m1 version crates/mini-db/Cargo.toml` shows `1.1`.
- [ ] Commit + log (version bumps + RESULTS.md). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday polish commit touching both crates' RESULTS.md.

**Saturday — Tempo activity check + M32 runtime-hours verification**

- [ ] [Tempo] Record the Path-D gate decision in `notes/tempo_path_d_check.md` — is Path D still active in background? Status with Tempo maintainers / any active design-partner conversations? (~30 min)
  - 🎯 Hardened acceptance: `tempo_path_d_check.md` opens with `Path D active: yes/no — <reason>`; if yes, lists ≥1 maintainer/design-partner conversation with date + current status; if no, states the gate criterion that failed (so the hedge stays honest rather than silently abandoned).
  - *Mental model*: Path D (Tempo) is a hedge that only pays off while its criteria hold. With applications starting next week, recording the gate decision now keeps the parallel option a deliberate choice — and if a design-partner conversation is live, it may itself become a Tier-B firm-shortlist entry rather than a separate track.
  - Output: `notes/tempo_path_d_check.md` opens with the `Path D active:` line; commit present.
- [ ] Verify the M32 runtime-hours milestone in `notes/m32_runtime_hours.md` — confirm 2900+ accumulated runtime hours (README §1086) and that the recon trip is logged complete. (~20 min)
  - 🎯 Hardened acceptance: `m32_runtime_hours.md` records the current accumulated runtime-hour total with the source of the figure (the runtime-hours ledger/log), states whether the 2900+ M32-end target is met (✓/✗ + gap if short), and confirms `recon trip: complete`. If short, names the crates whose soak runs need to extend in M33 to recover the gap.
  - *Mental model*: the 4000+-hour bar-(c) requirement by M36 is back-loaded; the M32 2900-hour checkpoint is the early-warning gauge. Catching a shortfall now (W124) means M33-M36 soak-run scheduling can recover it; discovering it at M36 means the bar (c) crates can't tag v1.0 (README: "missing the bar at v1.0 means the scope was wrong"). This is the milestone gate, not a vanity metric.
  - Output: `notes/m32_runtime_hours.md` records the hour total + ✓/✗ vs 2900 + `recon trip: complete`; commit present.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday Tempo + runtime-hours commit.

## Sunday — Rest + End Month 32 review

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
