# Week 117 — M30 Decision Gate + Blog #7 (Phase 6 retro)

> **Phase**: 6
> **Month**: M29
> **Track**: Public visibility push + 1500hr milestone
> **Hours target**: 40h/wk
> **Mirror target**: (decision gate + blog retro)
> **Crate(s) touched**: (none — decision gate + blog)
> **v2 modifications**: M30 gate now uses v2's expanded 5-question criteria (visibility, runtime hours, HFT crate quality, bar-(c) audit, 7-system coverage ≥75). See Decision Gates in README.

**Monday — Data collection**

- [ ] Runtime hours total. Target 2000 by today. (~10 min)
- [ ] PR portfolio: Reth count, Tempo count, HFT runtime, blog posts (#4-#7 should be done with #7 today). (~150 min)
- [ ] Inbound: any approaches? Recruiter pings? Maintainer DMs? (~30 min)
- [ ] Commit notes. (~10 min)

**Tuesday — M30 Decision review**

- [ ] Confirm Path E still right path: HFT destination-tier IC track. (~20 min)
- [ ] If inbound has been strong from Tempo / Reth / general crypto, reconsider Path D / Path A. (~30 min)
- [ ] Commit decision in `progress.md`. (~10 min)
  - **Why**: M30 is the *first hard branch point* after the v2 expansion — Path E (HFT tier-A IC) requires not just
    runtime hours but *bar-(c) infrastructure audit* clean, *zero-alloc audit* on hot paths, and *7-system coverage
    ≥ 75*. Soft signals (recruiter pings, inbound DMs) are necessary but not sufficient; the audit is the proof. This
    bullet is enriched to make the 5-question gate *concrete and unambiguous* — fuzzy gates get fudged.

  **M30 5-Question Decision Gate (v2)** — each question yields PASS/HOLD/FAIL; all five must be PASS or actionable
  HOLD to proceed on Path E.

  **Q1: Public visibility — is there inbound?**
  - Recruiter pings from tier-A HFT firms in last 90 days: target ≥ 3 (HOLD at 1-2, FAIL at 0).
  - Reth maintainer DMs or mentions in PR review threads: target ≥ 5 (any topic).
  - Blog post #4-#7 cumulative views > 10k; #7 (Phase 6 retro) > 3k.
  - Conference / podcast invitation: target ≥ 1.

  **Q2: Runtime hours trajectory — on track for 2000 by M30 end?**
  - Cumulative ops-rig runtime: target ≥ 2000hrs. Compute from monotonic-clock log, *not* wall-time (clock-skew safe).
  - Uptime SLO: ≥ 99.9% over the 2000hrs (≤ 2hrs total downtime allowed).
  - Ledger reconciliation drift events: 0.
  - Replica divergence events: 0 (any non-zero is a stop-the-line).
  - Synthetic feed sustained rate during the 2000hrs: ≥ 500k msg/sec average.

  **Q3: HFT crate quality — production-deployable at a real firm?**
  - matching-engine v1.1: P99 < 2µs single-symbol single-core at 500k orders/sec, certified per W111 bench protocol.
  - ledger-deterministic v1.0: zero reconciliation drift across all rig hours; VOPR cumulative scenarios ≥ 10M
    (the v2 cumulative-count bar).
  - messaging-aeron v0.7: sub-microsecond IPC P99; loss + reorder behavior identical to upstream Aeron reference for
    chosen scenarios.
  - marketdata-kernelbypass: 1M msg/sec sustained on rig hardware; XDP/AF_XDP path verified.

  **Q4: bar-(c) audit (infrastructure correctness) — the formalized checklist**
  - [ ] **VOPR cumulative scenarios ≥ 10M** by M30. Measured via `vopr_scenarios` Prometheus counter across all
    nightly + on-demand runs. Document in `audits/m30/vopr.md`: per-system count (matching, ledger, vsr, runtime,
    mmap-queue), seed coverage map.
  - [ ] **Zero-alloc audit on hot paths**. Run `dhat-rs` against matching-engine `process_order`, ledger `commit_batch`,
    aeron `send_publish`; expected output: *zero* heap allocations on the steady-state path (allocation only on
    bootstrap + on backpressure spill). Cross-check with `cargo bloat --release --crates` for any unexpected
    binary growth. Document in `audits/m30/zero_alloc.md`.
  - [ ] **Deterministic test harness operational** across runtime-thread-per-core + ledger-deterministic +
    consensus-vsr. For each: (a) seeded run reproduces byte-identical output across 3 hosts; (b) replay from any
    snapshot point reproduces; (c) wall-clock independence proven via 10× clock-rate variation showing no divergence.
    Document in `audits/m30/determinism.md`.
  - [ ] **Per-system v2 coverage score against the v2 score table** — see Q5; bar-(c) audit references the same table.
  - [ ] **ops-chaos scenario corpus** ≥ 50 distinct scenarios, all green for 30 consecutive nightly runs.
  - [ ] **Pre-mortem audit** for each of the 11 production crates: documented "what would cause a P0 in production"
    + "what guard exists" pairs ≥ 5 per crate.

  **Q5: 7-System coverage ≥ 75**
  - Score each of the v2's 7 systems (runtime, mmap-queue, vsr, ledger, matching, aeron, marketdata) against the
    v2 score table (correctness, performance, ops-readiness, docs, inheritance). Each system scored 0-15; total
    must be ≥ 75/105 (~71%).
  - System-by-system table committed to `audits/m30/coverage_table.md`.
  - Any system < 8/15 in any dimension blocks Path E unless explicitly waived with a remediation plan attached to
    `progress.md`.

  **Decision rule**: PASS on all 5 → Path E proceed (Phase 7 starts Monday). HOLD on 1 → 2-week remediation; FAIL on
  any → re-evaluate Path D vs Path E with mentor input. *No fudging the audit — Q4 is the gate that distinguishes
  "HFT-tier engineer" from "competent systems programmer".*

  - **Build steps for the audit week** (Tuesday alone):
    1. (~30 min) Pull data: VOPR counter from Prometheus, runtime hours from monotonic-log, blog stats, recruiter list.
    2. (~30 min) Run the dhat zero-alloc bench on each of the 3 hot paths; export the textual report.
    3. (~20 min) Determinism replay: re-run a fixed-seed scenario in each of runtime/ledger/vsr; SHA-256 the output
       and diff against the recorded baseline.
    4. (~15 min) Score the coverage table by walking the v2 score rubric for each system.
    5. (~15 min) Write the decision in `progress.md` with PASS/HOLD/FAIL per question + the consolidated decision.

**Wednesday — Blog post #7 draft: Phase 6 retro**

- [ ] "30 months in: 11 production crates, 2000 runtime hours, what worked." Honest retrospective. (~30 min)
- [ ] Commit. (~30 min)

**Thursday — Blog #7 edit + post**

- [ ] Post. Amplify. This is the most-likely-to-attract-inbound post; lean into the data (LOC, latency, uptime). (~30 min)
- [ ] Commit + log. (~10 min)

**Friday — Phase 7 kickoff prep**

- [ ] Phase 7 plan review. Application target list (Tier A firm A/B/C/D/E/F + Tier B firm A/B/C/D — no firm names
  committed yet). (~60 min)
- [ ] Commit notes. (~10 min)

**Saturday — Light week wrap**

- [ ] Reth + Tempo maintenance. (~30 min)
- [ ] Commit + log. (~10 min)

## Sunday — End Phase 6

- [ ] Full rest. (~5 min)
- [ ] Phase 7 starts tomorrow. (~30 min)

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
