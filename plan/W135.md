# Week 135 — First month at the bet: ramp into the core engine I was brought in to build

> **Phase**: 7 (bet-path — founding/core-engineer token-equity bet on an on-chain derivatives venue, or the crypto-MM cash bridge)
> **Month**: M35
> **Track**: (bet landing — first-month ramp at the on-chain-derivatives venue; portfolio maintenance is light by design)
> **Hours target**: 40h/wk (W73–W136 window; the 30h/wk Phase-7 cadence begins W137) — first-month ramp is front-loaded into the venue codebase; the personal workspace is in maintenance mode
> **Mirror target**: (n/a — bet-landing week; no upstream module. The "mirror" this week is the *venue's own* matching/risk/settlement core — read it the way you read TigerBeetle/reth: for named module boundaries, the apply path, and the determinism discipline)
> **Crate(s) touched**: (none new — light maintenance only on the Reth review queue + `perp-dex-core` capstone polish + Tempo skim)
> **Bar**: (n/a — no new Build this week; the bar discipline you carry IN is the artifact: you read the venue's hot path against bar (c) and your `perp-dex-core` v1.0 lock)
> **Feeds into**: W136 (deeper task on the venue's critical path — the 30/60/90 success contract written Monday becomes W136's self-review checklist; the codebase map seeded this week is the navigation substrate W136 deepens); W137–W138 (cultural integration + token/equity vesting milestones tracked — this week's recorded vesting/cliff terms are the input); the §6 ecosystem-checklist file (this week confirms, from the inside, whether the seven criteria that made this a GO still hold — core-criticality especially); the M36 gate (the first merged PR on the venue's core engine is the credibility artifact that converts "founding hire" → "contributor on the critical path").
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): the venue's internal onboarding + CONTRIBUTING / dev-setup guide (Tue–Fri, lookup-when-stuck); the venue's PR-review + CI conventions (Fri first-PR, lookup-when-stuck); README.md §4A (the v1.0 lock you carry in as a mental yardstick against the venue's real engine), §6 (the ecosystem checklist you now verify from inside), §7 (shots-on-goal — this is Bet #1; protect the runway and the next shot); https://github.com/paradigmxyz/reth (Sat review-queue triage — keep the inbound channel warm); your own `perp-dex-core` repo (Sat capstone-polish skim); https://github.com/tempoxyz/tempo (Sat maintenance skim, RWA-collateral option leg).
> **v3 modifications**: re-aimed from v2's "first month at new firm: generic ramp-up" to **first month at the on-chain-derivatives venue I placed Bet #1 on** (or the crypto-MM cash bridge per §7). The frontmatter, the 30/60/90 contract, the codebase map, and the first-PR target are all pointed at the **venue's matching/risk/settlement core** (the thing you were brought in to build), not a generic service. Two bet-specific items added: (Wednesday) a §6-checklist-from-the-inside verification — confirm core-criticality + token reality now that you can see the cap table and the codebase; (Saturday) `perp-dex-core` capstone-polish skim replaces generic personal-project maintenance — the capstone is now your moat narrative AND your "I have already built this" credibility on the venue's own engine. Reth review + Tempo skim retained as the transferable-core / option-leg keep-warm.

This is a **bet-landing week, not a Build week.** There is no new crate, no upstream mirror to clone, and no engineered trap-test arc — manufacturing one would be padding against the week's real job. The week's job is to convert the first weeks at the venue into recorded, reviewable artifacts: a written 30/60/90 success contract aimed at the *core engine*, a codebase-map of the venue's matching/risk/settlement path, a logged question budget spent while your "new founding hire" license is highest, a §6-checklist-from-the-inside verification, and a first merged PR on the critical path. Every item below carries an explicit artifact file + an Output line that proves it done; the learning/maintenance items carry a hardened-acceptance pass criterion and a mental model of why the item exists *now*. Personal-workspace maintenance is deliberately capped — your scarcest resource this month is ramp bandwidth, and the highest-leverage move is shipping inside the venue on the engine you spent three years preparing to build, not extending the portfolio.

**The bet-path framing you carry in (read before Monday):** per §7 this is **Bet #1** — a sequential shot, not all-in. Three things govern the month. (1) **Core-criticality is the bet's load-bearing assumption (§6.3):** you are here to build the engine that *is* the company (matching/risk/settlement), not a peripheral integration; the first month is where you confirm — from the inside — that the role actually is that. (2) **The runway protects the next shot (§7):** never quit a bridge before vest milestones; this week records the vesting/cliff terms so W137's vesting-milestone tracker has real numbers. (3) **Your `perp-dex-core` v1.0 is the unfair advantage:** you did not arrive to *learn* a perp engine — you arrived having built a replicated, cluster-VOPR'd one. The first month is where that shows: you read the venue's hot path against your own v1.0 lock (single VSR log, deterministic apply, incremental margin, waterfall+tripwire) and you spot the gaps fast. If the venue uses the bridge (crypto-MM cash seat) instead, the same artifacts apply with "venue" → "desk" and "matching/risk core" → "the execution/latency stack you were hired for."

---

## Monday — Manager / founder 1:1 — the 30/60/90 success contract (aimed at the core engine)

- [ ] Artifact: `onboarding/30_60_90.md`. In the first 1:1 with your manager (at a founding-stage venue this is often a founder or the eng lead), ask directly: **"What does success look like at 30, 60, and 90 days — specifically on the core engine?"** Capture the answer verbatim, then translate each horizon into a concrete, checkable criterion (e.g. "60d: owns the matching path" → "60d: I can land a change to the order-book hot path and ship it through CI without supervision"; "90d: owns risk" → "90d: I have made a margin/liquidation change reviewed and merged"). Record who owns each criterion and how it is measured at review time. (~30 min)
  - 🎯 Hardened acceptance: `onboarding/30_60_90.md` has exactly three dated horizons (30/60/90), each with ≥2 criteria, each criterion phrased as an observable outcome (not "learn the codebase" but "merged ≥3 PRs touching the matching/risk core"), and **≥1 criterion per horizon ties to the critical path** (matching / risk / liquidation / settlement / consensus), not just generic onboarding. Every criterion tagged with its owner and its measurement. No vague aspirations survive the translation pass.
  - *Mental model*: a founder's unspoken success bar is the single largest source of first-90-days anxiety and the most common cause of a quiet bad-fit signal — and at a founding-stage venue with token equity on the line, a bad-fit signal is far more expensive than at a salaried job (the bet, not just the paycheck, is at risk). Writing it down in the first 1:1 converts an ambiguous expectation into a contract you can self-audit weekly, and it feeds W136's self-review directly. Critically, by anchoring ≥1 criterion per horizon to the *core engine*, you are also testing §6.3 (core-criticality) from day one: if the founder's 90-day picture is all integrations and no engine, the bet's central assumption is wobbling — surface that early, while it is still cheap to renegotiate scope.
  - Output: `onboarding/30_60_90.md` exists with three horizons × ≥2 owned, measurable criteria each, ≥1 core-path criterion per horizon; `grep -c '^- ' onboarding/30_60_90.md` ≥ 6.
- [ ] Artifact: `onboarding/vesting_terms.md`. In the same week (1:1 or HR/founder follow-up), confirm and record the **token/equity vesting reality** while it is fresh and you have standing to ask: grant size/band, cliff date, vest schedule, entry FDV (if known), and any unlock terms. This is the §6.5 "token reality" criterion, now verifiable from the inside. (~15 min)
  - 🎯 Hardened acceptance: `onboarding/vesting_terms.md` records {grant, cliff date, vest schedule, entry FDV or "TBD-pre-TGE", unlock terms or "none disclosed"}; any deviation from the §6.5 sane-terms bar (≈1y cliff / 4y vest, ≥10× FDV headroom, no predatory unlock) is flagged with an explicit "⚠️ deviation" line. If terms differ materially from what the §6 GO decision assumed, that is logged as a follow-up, not buried.
  - *Mental model*: §7 says *never quit a bridge before the vest milestones of the live bet* — but you cannot track milestones you never wrote down. Recording the cliff date now turns a vague "sometime next year" into a dated line W137's vesting tracker keys off. And confirming the terms from the inside closes §6.5: a grant that looked founding-tier in the offer can quietly differ once paperwork lands; catch it in week one while renegotiation is still normal, not at the cliff when it is a grievance.
  - Output: `onboarding/vesting_terms.md` exists with all five fields + any ⚠️ deviation flag.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Monday 1:1 commit referencing `onboarding/30_60_90.md` + `onboarding/vesting_terms.md`.

---

## Tuesday — Codebase deepening: the venue's matching/risk/settlement core

- [ ] Artifact: `onboarding/codebase_map.md`. Map the venue's **core engine** so you can navigate it from memory: the top-level module boundaries (matching, oracle/mark, risk/margin, liquidation, settlement, consensus/replication), the path an order takes from ingest → match → fill → margin-update → settlement, the build + test + deploy commands, and the 3–5 files you'd touch most on the hot path. Be a sponge — the goal this month is breadth of orientation across the engine, not depth of mastery of one corner. Record open questions inline as `Q:` lines to feed Thursday's question budget. (~30 min)
  - 🎯 Hardened acceptance: `codebase_map.md` documents (a) ≥4 named module/service boundaries of the *core engine* with one line each on responsibility (must include at least the matching path and the risk/margin path), (b) one end-to-end order-lifecycle flow traced through ≥3 of those boundaries, (c) the exact build/test/run commands you ran yourself (not copied from a wiki untested), and (d) ≥3 `Q:` open-question lines. Every command listed must have actually been executed locally this week.
  - *Mental model*: the fastest way to stop being a net drain in month one is a private map that lets you answer "where does the margin recompute live?" without interrupting a teammate. Tracing one real order lifecycle end-to-end teaches more architecture than reading ten design docs, because it forces you to discover the gaps the docs paper over — and those gaps become Thursday's high-value questions while your "new founding hire" budget is still highest. You have an edge here most new hires lack: you built `perp-dex-core` v1.0, so you *know what the boundaries should be* (matching ⊥ oracle-mark ⊥ risk ⊥ liquidation ⊥ the single VSR log). Map the venue's reality against that mental model — the places they diverge from your §4A lock are exactly where the most interesting work (and the most dangerous bugs) live.
  - Output: `onboarding/codebase_map.md` exists meeting all four acceptance clauses; build/test commands recorded were run locally (terminal history confirms).
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday codebase-map commit.

---

## Wednesday — Continue the map + verify the §6 checklist from the inside

- [ ] Artifact: `onboarding/codebase_map.md` (extend). Continue the orientation pass: resolve ≥1 of yesterday's `Q:` lines by reading code (not asking yet — save asks for Thursday), and add a "first-PR candidates" section listing 2–3 small, safe, shippable changes you spotted on or near the core engine (a missing test on a matching edge case, a stale doc on the margin formula, a small refactor on a hot-path helper). These become Friday's PR shortlist. (~30 min)
  - 🎯 Hardened acceptance: ≥1 prior `Q:` line is now resolved (struck through with a one-line answer + the file/commit that answered it), and a "first-PR candidates" section lists 2–3 changes, each with the file path and a one-line scope estimate confirming it is genuinely small; **≥1 candidate touches the core engine** (matching/risk/liquidation/settlement), not just peripheral tooling — the first merge should land on or adjacent to the critical path.
  - *Mental model*: resolving your own questions by reading source before escalating builds the judgment that separates "asks good questions" from "asks lazy questions" — and it pre-stocks Friday's PR shortlist so the first-merge isn't a last-minute scramble. A merged PR in week one is mostly a function of having scouted a safe target early. Aim the candidate at the engine because the first-merge's *location* signals where you'll operate: a typo-in-the-README first PR reads as a peripheral hire; a missing-test-on-the-matching-edge first PR reads as a core-engine hire. You are establishing the §6.3 narrative through your commit graph, from PR #1.
- [ ] Artifact: `onboarding/checklist_from_inside.md`. Now that you can see the codebase, the cap table, and the team, re-run the §6 ecosystem checklist **from the inside** — the GO decision was made on outside information; this is the confirmation pass. For each of the seven criteria, mark ✓ / ✗ / ~ with one line of inside evidence. Pay special attention to **§6.3 (core-criticality)** — does the actual work you're being handed build the engine that *is* the company, or has it drifted toward peripheral integration? (~25 min)
  - 🎯 Hardened acceptance: `checklist_from_inside.md` has all seven §6 criteria (founding-tier allocation, tier-1 backing, core-criticality, multi-B FDV ceiling, token reality, remote-native, cycle timing) each tagged ✓/✗/~ with one line of *inside* evidence (something you could only know now that you're in). §6.3 carries an explicit verdict sentence. Any ✗ or ~ on a load-bearing criterion (3, 5, or 6) carries a follow-up action — escalate, renegotiate scope, or note the runway implication.
  - *Mental model*: §6 says a bet is GO only if all seven pass — but the GO call was made on the *outside* view (the offer, the cap table you were shown, the conversations). Month one is the only cheap window to verify it from the inside before you've sunk a year toward a cliff. The most common failure mode is the Airwallex anti-pattern in disguise: the role *sounded* core-critical, but the actual day-to-day drifts to glue code and integrations, and the founding-tier upside never materializes because you never touched the moat. Catching a §6.3 drift in week three (when you can still reshape scope, or quietly start preparing Bridge₁) is the difference between a bet and a stranded year. This is the single highest-leverage 25 minutes of the bet-path that isn't a Build.
  - Output: `onboarding/codebase_map.md` shows ≥1 resolved `Q:` + a 2–3 item first-PR-candidates section (≥1 core-engine) with file paths; `onboarding/checklist_from_inside.md` has all seven criteria tagged with inside evidence + a §6.3 verdict.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday continuation + checklist commit.

---

## Thursday — Ask many questions (the founding-hire budget is highest now)

- [ ] Artifact: `onboarding/questions_log.md`. The stupid-questions budget is highest in week 2–3 — spend it deliberately, and at a founding-stage venue it is even more valuable because the answers are often in someone's head, not a wiki. Batch the unresolved `Q:` lines from your codebase map and ask them (in the right channel, batched not drip-fed). Prioritize questions about the **core engine's design decisions** — *why* the venue made the matching/risk/settlement choices it made (e.g. "why batched settlement vs per-fill?", "is the margin recompute incremental or full per mark tick?", "single replicated log or split?"). Log each: question, who/where asked, the answer, and whether it revealed a doc gap worth a PR. (~30 min)
  - 🎯 Hardened acceptance: `questions_log.md` records ≥3 asked questions, each with {question, channel/person, answer, doc-gap? yes/no}; **≥2 questions are about core-engine design rationale** (not setup/tooling); ≥1 answer is converted into either a `codebase_map.md` correction or a new first-PR candidate. Questions are batched into ≤2 interactions, not scattered across the day.
  - *Mental model*: the "new founding hire" license to ask basic — and pointed — questions decays fast; by month two the same question reads as inattention. Cashing it in now, batched and logged, both accelerates your ramp and turns answers into reusable artifacts (map corrections, doc-gap PRs) instead of one-off Slack messages that vanish. Aim the questions at *design rationale* because that is where your `perp-dex-core` background converts to value fastest: when you ask "is the margin recompute incremental?" you're not just learning their system — you're surfacing (gently) whether they hit the back-of-envelope wall §4A predicts at scale (full recompute = 1M recomputes/s blows the budget). The best founding engineers earn trust in month one not by shipping volume but by asking the question that reveals they've already thought about the hard part.
  - Output: `onboarding/questions_log.md` has ≥3 logged Q&A entries (≥2 on core-engine rationale), ≥1 converted to a map fix or PR candidate.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday questions-log commit.

---

## Friday — First merged PR on (or adjacent to) the core engine

- [ ] Artifact: the PR itself + `onboarding/first_pr.md`. Pick one item from Wednesday's first-PR-candidates shortlist — small but it ships, and ideally on or adjacent to the core engine. Follow the venue's PR-review and CI conventions (consult their CONTRIBUTING / PR template, lookup-when-stuck). Record in `onboarding/first_pr.md`: the PR link, what it changed, the review turnaround, and any convention you tripped on (so it's a one-time lesson). (~30 min)
  - 🎯 Hardened acceptance: a real internal PR is opened (and merged, or at minimum approved + queued) against the venue's repo; `onboarding/first_pr.md` records the PR URL, the diff scope in one line, CI status, and ≥1 venue-specific convention learned. A draft that never opens does not count — the deliverable is a shipped change, however small. If the chosen candidate touched the core engine, note that explicitly; if circumstances forced a peripheral first-PR, note the core-engine PR you're scouting for next week.
  - *Mental model*: the first merged PR is the credibility inflection point of month one — it proves you can navigate the build, the review process, and the CI gauntlet, and it converts you from "founding hire" to "contributor" in the team's mental model. Keeping it small de-risks the first exposure to their conventions; the point is the round-trip through their process, not the size of the diff. At a founding-stage venue the bar is higher in one specific way: the team is small and every commit is visible to the people who control your token grant, so the *first* commit's location and quality disproportionately set your reputation. This is also the concrete artifact that retires the M36-gate question "is this a real core-engineer seat?": a merged, paid contribution on the venue's critical path is the proof that Bet #1 is live and you are on the moat, not the margins.
  - Output: `onboarding/first_pr.md` records a merged-or-approved internal PR URL + scope + CI status + ≥1 convention learned + a core-engine note.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Friday first-PR commit referencing `onboarding/first_pr.md`.

---

## Saturday — Personal workspace: light maintenance + capstone-as-credibility

- [ ] Artifact: `outreach/log_reth_reviews.md` (extend). One Reth PR review from the open queue — keep the transferable-core presence alive without cutting into ramp bandwidth. Pick a PR in an area you already know (storage/exec/consensus), leave one substantive, specific review comment, and log it: PR link, area, the substantive point you raised. (~120 min)
  - 🎯 Hardened acceptance: exactly one Reth PR reviewed with ≥1 substantive comment (a correctness/perf/design point, not a nit), logged in `outreach/log_reth_reviews.md` with PR link + area + the point raised. "LGTM" without a substantive comment does not satisfy the acceptance.
  - *Mental model*: month one at the venue is when your open-source visibility is most at risk of going dark — but per §7 the Reth spine is your *transferable core*, the thing that keeps you hireable if this ecosystem cools before the cliff (the §9 niche-cools risk). A single substantive review per week keeps the Reth relationship warm and your name in the contributor graph at near-zero cost, which is the inbound channel that re-bridges you toward Bet #2 if Bet #1 doesn't capture. Reviewing (not authoring) is the right tool now: it is bounded, asynchronous, and demonstrates judgment without competing with the venue's deadlines.
- [ ] Artifact: `notes/perp_dex_core_polish_log.md` (extend). One focused skim of your own `perp-dex-core` v1.0 repo — it is now doing double duty: it is your moat narrative (the §7 "I've already built the hard engine" credibility) AND your private yardstick against the venue's real engine. Check that the public README still leads with the v1.0 lock claims accurately (3-node VSR, single log, multi-instrument cross-margin with the 30% netting haircut, waterfall+tripwire, cluster-VOPR green), and note any gap between your design and what you learned the venue does this week (feeds the v1.5 tier-raiser narrative for Bet #1/#2). No new code — this is a keep-the-narrative-sharp pass. (~40 min)
  - 🎯 Hardened acceptance: `notes/perp_dex_core_polish_log.md` records the date, a ✓/✗ on each of the five v1.0 lock claims still being accurately represented in the public README, and ≥1 "venue-vs-my-design" observation captured this week (a place the venue's engine differs from your §4A lock — the seed of either a venue contribution or a capstone-v1.5 talking point). A stale README claim is flagged for a fix, not silently passed.
  - *Mental model*: the capstone stopped being a portfolio piece the moment you landed the bet — but it didn't stop being useful. It is now (a) the live proof, when a teammate doubts a design call, that you've shipped this exact hard thing replicated-and-VOPR'd, and (b) the staging ground for the v1.5 differentiator (RWA + multi-collateral + ADL) that §7 says raises the tier of Bet #1/#2. Forty minutes a week keeps both edges sharp without re-opening the build. The "venue-vs-my-design" note is the highest-value output: every place the venue diverges from your §4A lock is either a gap you can close for them (instant core-criticality credibility) or a confirmation your v1.5 roadmap is genuinely novel (moat narrative for the next conversation).
- [ ] [Tempo] Artifact: `notes/tempo_maintenance_log.md` (extend). If Tempo is still a live RWA-collateral option leg, one focused skim: check the issue/PR queue for anything touching your prior contributions, and recall (from memory, before opening docs) the 1–2 Tempo features most relevant to the venue's collateral/settlement side — RWA-perps collateral rails are the §2 option-leg-B fit. Log: what changed since last skim, ✓/✗/~ on the feature recall, and whether any item connects to the venue's work. (~30 min)
  - 🎯 Hardened acceptance: `notes/tempo_maintenance_log.md` records the date, ≥1–2 features recalled from memory and tagged ✓/✗/~ against the live source, and an explicit "no action needed" or a follow-up item — including whether any Tempo/RWA-collateral concept maps onto the venue's settlement path (the three-way-fit edge). A ✗ on a long-decayed feature is expected and is the signal of what to re-cement, not a failure.
  - *Mental model*: Tempo is an option leg now, so the goal is decay-detection plus opportunistic relevance-spotting, not deepening — a brief recall-then-verify loop surfaces what you've forgotten before it embarrasses you, and the "does this map to the venue's collateral side?" question keeps the §2 RWA-collateral edge alive as a potential v1.5 / venue-feature angle. Capping it at 30 min keeps maintenance from crowding out the first-month ramp, which is this month's actual priority.
  - Output: `notes/perp_dex_core_polish_log.md` has a dated entry (five lock-claim ✓/✗ + ≥1 venue-vs-design note); `notes/tempo_maintenance_log.md` has a dated entry with recall tags + a no-action/follow-up decision; `outreach/log_reth_reviews.md` has the new Saturday review entry.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday maintenance commit covering the Reth review + capstone-polish skim + Tempo skim.

---

## [v3.1 BFT apex — Track B] W135: latency-X — the BFT commit path meets p99 ≤ 2 ms (N=4 LAN)

> Track B continues (W129 Byzantine-proved the apex; W130–W134 sustained the seed-sweep). This week hits the **numeric MVP latency target X** (W118: p99 ≤ 2 ms committed-batch, 4-node LAN, median ~700 µs) — the last bound before the swap. Bar (c). May be built inside the bet (per Guardrail-1, the apex overlaps early-bet work). **Bench transport (named)**: the p99 ≤ 2 ms acceptance is measured over a **real NIC, kernel-TCP LAN** — the in-process deterministic bus is for VOPR, not the latency acceptance; loopback links on the 2-box rig are labeled as such per the README topology rule.
- [ ] **[Tue +90min · Thu +90min]** [Venue · BFT] **Build (bar c)**: `crates/consensus-vsr/benches/bft_commit_latency.rs` + the hot-path optimizations — drive the `BftReplica` 4-node cluster under load and tune to **p99 ≤ 2 ms** via: (1) **QC batch-verify** (O1 — `SignedPeers::verify_batch` over the 3 votes, ~50–80 µs/QC batched vs ~120–150 µs unbatched at 3 × ~40–50 µs/sig); (2) **zero-alloc message path** (votes/proposals/QCs from a pre-sized pool, `#[repr(C)]` wire structs — the W77/TigerBeetle discipline); (3) **pipelining** (propose each view on the high-QC, W126 — don't serialize on commit); (4) **persist-before-vote stays in the loop** (W126/W118: group-committed NVMe write ~20–100 µs/round on the critical path — budgeted, never silently dropped to make the bench look better). Measure with `latency-lab` (W111), coordinated-omission-corrected, p99.99 not p50. (~total 180 min)
  - 🔧 **Why this matters**: X is an *acceptance* criterion (W143), and it's the number that proves "trading-optimized BFT" is real, not aspirational. The derivation (W118): 2-chain commit ≈ 2 view-rounds × (~1 RTT LAN ~100–300 µs + QC verify ~50–80 µs batched + vote-state persistence ~20–100 µs group-committed, overlappable) ≈ ~700 µs median, p99 ~2 ms with pacemaker jitter. **O1 is load-bearing here, with honest numbers**: unbatched ed25519 verify costs 3 × ~40–50 µs ≈ ~120–150 µs/QC, ×2 QCs ≈ ~240–300 µs/commit vs ~100–160 µs batched — the batch path saves **~150–200 µs/commit, material against the ~700 µs p50** (not against the 2 ms p99 alone). What actually blows p99 to ~2.5–3 ms is the *composite*: unbatched verify + per-message allocs + non-pipelined serialization stacking (and BLS would be *worse* at N=4, the O1 finding). The trap: measuring p50/throughput and declaring victory — the venue cares about p99.99 tail under contention (the hft-review lens: coordinated omission, not mean).
  - **Tests/benches:**
    - `bft_commit_p99_under_2ms` — `latency-lab` histogram over a sustained load run; assert p99 ≤ 2 ms AND p50 ≤ ~700 µs (a p50 miss means the design is broken before discussing p99) **AND pacemaker-timeout incidence < 0.1% across the measured window (else the p99 measures the timeout constant, not the protocol)**. Coordinated-omission-corrected; run on the real-NIC kernel-TCP LAN transport (named above).
    - `qc_verify_is_batched_on_hot_path` — `cargo asm`/profile: QC formation calls `verify_batch` once, not 3× individual `verify`; a regression to per-vote verify is flagged (O1).
    - `bft_message_path_zero_alloc` — dhat over 1M consensus messages → 0 allocs on the steady commit path (pooled wire structs).
    - `bft_pipelined_throughput` — assert the apex sustains a target ops/s via pipelining (proposes on high-QC each view), not one-commit-at-a-time.
  - **Make-pass card (EXPERT · O1):** `bft_commit_p99_under_2ms` misses at ~2.5–3 ms → symptom: the p99 is a *composite* miss, not one hot spot — profile shows unbatched per-vote ed25519 verify (+~150–200 µs/commit vs batched), per-message allocs (allocator jitter spikes under burst), and serialization not overlapped with the pipeline (each view pays it serially) stacking into the tail; why: each is individually "too small to matter" against 2 ms, so none got fixed — the budget dies by a thousand ~100 µs cuts; fix: `verify_batch` (O1) once per QC + pooled `#[repr(C)]` wire structs + pipelined serialization — re-measure after EACH (attribute the win, don't bundle); cost avoided: blowing X → failing W143 acceptance → "BFT but too slow for the hot path" (the exact thing the apex exists to beat); muscle: at the quorum boundary, batch-verify; a p99 budget is defended line-item by line-item, tuned for p99.99 tail, never p50. Reapplies: W143 acceptance (X is a criterion), W136 hardening.
  - **Output**: `bft_commit_latency.rs`; p99 ≤ 2 ms met (the numeric X, recorded in `latency-lab`); batch-verify + zero-alloc + pipelining confirmed; X is now an evidenced number for W143.
- [ ] **[Sat +10min]** [Venue · BFT] Handoff note → `notes/135_bft_latency.md`: X met (p99 ≤ 2 ms, N=4 real-NIC kernel-TCP LAN, timeout incidence < 0.1%); the number + the `latency-lab` report path recorded for W143 acceptance. **Next W137: the live VSR→BFT swap into `perp-dex-core` (v2).** (~10 min)
- [ ] **[Sat +10min]** Commit + log (~10 min)

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.

This week's ritual carries one bet-path-specific check on top of the standard template: **re-read `onboarding/checklist_from_inside.md` and confirm §6.3 (core-criticality) is still ✓.** If it slipped to ~ or ✗ during the week, that is the single most important signal of the month — a founding bet whose role isn't core-critical is a salaried job in disguise, and §7 says protect the next shot. Note any action (renegotiate scope / start warming Bridge₁ / escalate to the founder) for Monday. Everything else this week — the 30/60/90 contract, the codebase map, the question budget, the first PR — exists to make this one verdict trustworthy.
