# Week 142 — Settle + first quarterly review prep

> **Phase**: 7
> **Month**: M36
> **Track**: Career (destination landing)
> **Hours target**: 30h/wk (W137-W144)
> **Mirror target**: (n/a — career landing week; no upstream module)
> **Crate(s) touched**: (none — maintenance only on Reth + Tempo + the operations rig)
> **Bar**: (n/a)
> **Feeds into**: W143 last full work week (this week's manager-1:1 score + quarterly-review draft set the talking points you walk in with); W144 M36 retrospective + plan close (the impact log and self-assessment built here become the raw material for the plan-close retro); the firm's actual first quarterly/performance review (the draft you assemble this week is the document you submit there).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): your firm's performance-review template/rubric (Tuesday self-assessment — use the real internal doc, not a generic one); your own `progress.md` + merged-PR history (impact-log source of truth); operations-rig dashboards (Wednesday runtime-hours capture); README §"Weekly HFT maintenance template" (recurring Mon/Wed/Sat HFT touchpoints).
> **v2 modifications**: none

This is a career/landing week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding. Every item below carries an explicit artifact file + an Output line that proves it done, and the two reflective/recall items (Monday 1:1 self-score, Tuesday self-assessment) carry a hardened-acceptance pass criterion and a mental model of why the item exists now. The week's job: you are ~6-8 weeks into the destination role; convert that runway into a defensible first-quarter impact narrative BEFORE the formal review window opens, so the review is a confirmation of evidence already on the table, not a scramble. The recurring HFT maintenance touchpoints (runtime alerts, dashboards, chaos drill) continue on their normal cadence — you are now at the 4000-hour finish line.

**Monday — Manager 1:1: how's it going from your side?**

- [ ] **[HFT] Weekend alert review** — artifact: append to `chaos_log.md` (or `ops_alerts.md`). 15 min: scan weekend runtime alerts on the operations rig — any P99 spikes, replica-divergence events, or disk-fill warnings? Record "clean" explicitly if nothing fired; an empty log is ambiguous, a dated "clean" line is evidence. (~15 min)
  - *Mental model*: the M36 finish line is 4000+ runtime hours with the bar-(c) static-memory + zero-allocation invariants intact. A silent weekend isn't proof the rig is healthy — only a dated, reviewed entry is. This is the same single-writer/observe-everything discipline the HFT crates were built on, applied to your own operations.
  - Output: newest line in `chaos_log.md` is dated this Monday and states either the alert + triage or `clean — no P99/divergence/disk alerts`.
- [ ] **Manager 1:1 prep + score** — artifact: `career/142_1on1_self_score.md` (private). Before the 1:1, score yourself honestly across the dimensions your manager actually evaluates on (delivery, code quality, collaboration, ownership, ramp speed). For each: a 1-5 self-score AND one concrete piece of evidence (a shipped PR, an incident you handled, a design doc) — a score with no evidence is a vibe, not an input. Open the dialogue in the 1:1 itself. (~30 min)
  - 🎯 Hardened acceptance: ≥5 dimensions scored 1-5; every score backed by exactly one named, datable artifact (PR link, incident ID, doc title) — zero unbacked scores; at least one dimension self-scored below your average (an all-4s-and-5s sheet means you didn't look hard enough).
  - *Mental model*: a first-quarter manager's read of you is still forming and is dominated by recency + legibility, not raw output. Walking into the 1:1 with an evidence-anchored self-score does two things: it shows you self-evaluate like a senior, and it lets you steer the narrative toward your strongest evidence before the manager's impression hardens. This sheet is also the seed of Tuesday's formal self-assessment.
  - Output: `career/142_1on1_self_score.md` exists meeting the hardened-acceptance criteria; `grep -c '^- ' career/142_1on1_self_score.md` ≥ 5.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday 1:1-prep commit.

**Tuesday — Quarterly review: self-assessment draft**

- [ ] **First-quarter self-assessment** — artifact: `career/142_q1_self_assessment.md` (private). Using the firm's real review template (see References) as the skeleton, draft your first formal self-assessment. Translate the Monday self-score + the impact log (Wednesday) into the firm's rubric language. Stay honest on geography/comp anonymity in any version that leaves your machine. (~45 min)
  - 🎯 Hardened acceptance: the draft maps onto every section the firm's template requires (no section left blank with "TBD"); each claimed strength cites a concrete artifact; the "areas to grow" section names ≥2 real gaps with a one-line plan each — not a humble-brag disguised as a weakness.
  - *Mental model*: the gap between a mid and a senior first-quarter review is almost never the work — it's whether the self-assessment speaks the firm's rubric language and lands evidence in the boxes the calibration committee actually reads. A correct self-assessment is a translation task from "what I did" to "what the rubric rewards"; doing it now, before the deadline, leaves time to gather any missing evidence.
  - *Back-of-envelope*: ~6-8 weeks in the role = roughly 240-320 working hours of observable output. The committee skims; budget that they will read ~3 bullets per section. So the draft must front-load the single strongest artifact per section — diminishing returns past the third bullet.
  - Output: `career/142_q1_self_assessment.md` exists, covers every required template section, meets the hardened-acceptance criteria.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday self-assessment commit.

**Wednesday — Impact log + ops dashboards**

- [ ] **[HFT] Operations dashboard check + runtime-hours capture** — artifact: update the HFT row in `progress.md`. 30 min: review the operations dashboards, capture the cumulative runtime-hours total, and update `progress.md`. At M36 the target is 4000+ hours; record the current number and the projected hit date. (~30 min)
  - *Mental model*: the 4000-hour bar-(c) requirement is a falsifiable claim about your stack's stability, and a number in `progress.md` is the only thing that makes it auditable at W144 plan close. Capturing it weekly turns "I think it's stable" into a tracked time series.
  - Output: `progress.md` HFT row shows this week's runtime-hours total + projected 4000-hour date; `git diff progress.md` shows the updated number.
- [ ] **First-quarter impact log** — artifact: `career/142_q1_impact_log.md` (private). Enumerate every shippable thing you did in the role so far: PRs merged, incidents handled, reviews given, docs written, onboarding friction you removed. For each, note the business/team outcome in one line (not the activity — the outcome). This is the evidence pool both Monday's score and Tuesday's self-assessment draw from; building it mid-week lets you back-fill any thin section. (~45 min)
  - 🎯 Hardened acceptance: ≥8 distinct entries each with {artifact link/ID, one-line outcome}; outcomes are stated as effects ("cut review turnaround for the team by routing X") not activities ("reviewed PRs"); at least one entry is a non-code contribution (mentoring, docs, process).
  - *Mental model*: people systematically under-log their own first-quarter impact because the small wins feel unremarkable in the moment and evaporate from memory by review time. A running impact log is the same write-ahead-log discipline as the `wal` crate: record the event when it happens, because reconstructing it later is lossy and expensive. This pool de-risks both the 1:1 and the formal review.
  - Output: `career/142_q1_impact_log.md` exists with ≥8 outcome-framed entries; `grep -c '^- ' career/142_q1_impact_log.md` ≥ 8.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday impact-log commit.

**Thursday — Reth/Tempo maintenance + open-source continuity**

- [ ] **Open-source continuity check** — artifact: `notes/142_oss_continuity.md`. Now in the role, your Reth/Tempo open-source presence is no longer a job-search engine but a long-term credibility asset. Spend the slot triaging: any review requests, broken CI, or stale PRs on your merged contributions that need a 15-minute response to stay a good citizen? Record what you touched (or an explicit "nothing actionable this week"). (~45 min)
  - *Mental model*: the inheritance principle says every component you built mirrors an upstream module — your standing in those upstream communities is the durable form of that bet, and it decays if you ghost the moment you land a job. A few minutes of maintenance keeps the door open for the next role and honors the maintainers who reviewed your PRs. This is relationship-WAL, not busywork.
  - Output: `notes/142_oss_continuity.md` dated this Thursday, listing each touched item or `nothing actionable this week`.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday OSS-continuity commit.

**Friday — Quarterly review draft: finalize + pressure-test**

- [ ] **Self-assessment finalize + adversarial read** — artifact: update `career/142_q1_self_assessment.md` to a submittable draft. Re-read Tuesday's draft against Wednesday's impact log: every strength claim must now point at a log entry; fill any section that was thin. Then do one adversarial pass: read it as a skeptical calibration committee and flag any claim a reviewer could dispute, tightening or cutting it. (~45 min)
  - 🎯 Hardened acceptance: every strength claim in the final draft traces to a specific `career/142_q1_impact_log.md` entry (no orphan claims); the adversarial pass produced ≥2 concrete edits (a softened overclaim, a cut vague line, or an added piece of evidence) recorded at the bottom under `## Adversarial pass`; the draft is marked `STATUS: submittable`.
  - *Mental model*: the failure mode of a self-assessment is not too little content, it's unbacked or disputable claims that a calibration committee discounts wholesale — one overclaim taints the credible ones around it. The adversarial read is the negative-control of career writing: you are stress-testing your own document to find the claim that breaks before the committee does, exactly like a VOPR run hunts the interleaving that breaks the system before production does.
  - Output: `career/142_q1_self_assessment.md` ends with a `## Adversarial pass` section listing ≥2 edits and a `STATUS: submittable` line.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday finalize commit.

**Saturday — Personal maintenance + HFT chaos drill**

- [ ] **[HFT] Weekly chaos drill** — artifact: append to `chaos_log.md`. 60 min: run this week's rotated chaos drill (process kill / network partition / clock skew / disk fill / slow subscriber — pick the next in rotation). Capture: scenario, expected behavior, observed behavior, recovery time, any divergence. A drill with no recorded recovery time isn't a drill. (~60 min)
  - 🎯 Hardened acceptance: the new `chaos_log.md` entry names the scenario, states expected vs observed behavior, records a measured recovery time, and ends with a pass/fail verdict; if fail, a one-line follow-up action is logged.
  - *Mental model*: back-pressure and crash-recovery are correctness properties of the bar-(c) stack, not nice-to-haves — the only way to know they hold is to inject the failure and measure the recovery, weekly, deterministically. As you approach the 4000-hour mark, a clean chaos-drill streak is the evidence that "stable" is earned, not assumed. This is the same deterministic-simulation lens (Reapplies at: every `recovery`/`consensus-*` crate) applied as an operations ritual.
  - Output: newest `chaos_log.md` entry meets the hardened-acceptance criteria; `git diff chaos_log.md` shows the dated drill result.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday chaos-drill commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
