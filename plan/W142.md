# Week 142 — Settle into the bet/bridge + first quarterly review prep **[v3]**

> **Phase**: 7 (Bet Placement / Bridge)
> **Month**: M36
> **Track**: Bet-path settling — first quarter inside the founding/core-eng role at a §6-checklist-passing on-chain-derivatives venue (OR the crypto-MM cash bridge, if Bet #1 wasn't placed at W131); Half-1 Reth-spine + `perp-dex-core` kept warm as the durable credibility asset
> **Hours target**: 30h/wk (W137–W144 window; the plan's final taper)
> **Mirror target**: (n/a — this is a bet-settling / review-prep week; no upstream module is mirrored. The only "source of truth" consulted is the venue's own internal review rubric + your `progress.md` + the `perp-dex-core` operations rig dashboards.)
> **Crate(s) touched**: (none built. Maintenance-only touchpoints on the venue/`perp-dex-core` operations rig + your retained Reth/Tempo OSS standing — no scope, no version tags.)
> **Bar**: (n/a for new work; the recurring `perp-dex-core` ops touchpoints continue to assert the bar-(c) static-mem / zero-alloc / cluster-VOPR invariants on the deployed venue)
> **Feeds into**: W143 (last full work week — this week's manager/founder 1:1 self-score + the quarterly-review draft set the talking points you walk in with); W144 (M36 retrospective + plan close — the impact log + self-assessment built here become the raw material for the plan-close retro, and the **Bet #2 thesis seeds** started Friday feed the W144 "each bet upgrades the next" close); the venue's actual first quarterly/performance (or, on the bridge, the first comp/scope) review — the draft you assemble this week is the document you submit there; the §7 shots-on-goal ladder (Bet #1 → Bridge₁ → Bet #2) — the vesting-milestone tracking + Bet #2 seeds keep the sequence live.
> **References** (consult when stuck — NOT scheduled as cover-to-cover tasks): your venue/firm's real performance-review template/rubric (Tuesday self-assessment — use the actual internal doc, never a generic one); README.md §1 (the four readiness bars — bar (a) ≥4000 runtime hours, bar (d) runway tracked), §5 M36 ("Bet #1 placed OR bridge engaged"), §6 (the ecosystem checklist — re-applied here as the lens for Bet #2 seeds and for sanity-checking the seat you're in), §7 (shots-on-goal ladder + the Dubai/SG flip-trigger), §4A (the `perp-dex-core` v1.0 lock the ops rig keeps green), §9 risk register (token-illiquidity / FDV-crater + "never quit a bridge before vest milestones"); your own `progress.md` + merged-PR history (impact-log + 4000-hour source of truth); the `perp-dex-core` ops dashboards (Wednesday runtime-hours capture); README §"Weekly HFT maintenance template" (recurring Mon/Wed/Sat venue-rig touchpoints).
> **v3 modifications**: (1) **Re-aimed from "destination landing (a job)" to "settling into the bet (or the bridge)."** v2 framed M36 as the first quarter at a landed Tier-A HFT IC role; v3 §5 M36 makes this the first quarter inside a founding/core-eng **token-equity bet** at an on-chain-derivatives venue — *or* the **crypto-MM cash bridge** if §6 didn't clear at W131. The review-prep skeleton is retained (it transfers verbatim to either destination) but every artifact is re-narrated around bet/bridge reality: **token/equity vesting milestones**, the **§6 checklist re-applied to the seat you're in**, and the **shots-on-goal ladder**. (2) **Adds an explicit vesting-milestone + runway tracker** (bar (d); §9 "never quit a bridge before vest milestones"). (3) **Adds Friday Bet #2 thesis seeds** (§7 "each bet upgrades the next") so W144's close has raw material. (4) **HFT maintenance touchpoints re-pointed** from a generic "operations rig" to the deployed **`perp-dex-core`** venue (or, on the bridge, the MM desk's stack you now operate) — the 4000-hour bar-(a) finish line and the §4A cluster-VOPR invariants are the same.

This is a **bet-settling / review-prep week, not a Build week.** There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one here would be padding, and the exemplar's discipline ("don't gold-plate") says a career/settling week earns its keep by *evidence*, not by a fabricated 3-phase Build. So every item below carries an explicit **artifact file** + an **Output line** that proves it done, and the two reflective/recall items (Monday 1:1 self-score, Tuesday self-assessment) carry a **🎯 hardened-acceptance** pass criterion and a *mental model* of why the item exists *now*.

**The week's job:** you are ~6–8 weeks into the destination — the founding/core-eng seat you bet on (or the crypto-MM bridge you engaged). Convert that runway into a defensible first-quarter impact narrative **before** the formal review window opens, so the review confirms evidence already on the table rather than scrambling for it. Two v3-specific threads run alongside the generic review prep: (i) **the bet is a tail bet, not a salary** — so this week you also start tracking the things that make a *bet* pay (vesting milestones, the §6 checklist still holding for the seat, runway) and you do NOT let the comfort of being landed lull you out of the §7 ladder; (ii) **the recurring `perp-dex-core` venue touchpoints continue** — you are now at the **4000-hour bar-(a) finish line** (§1), and a clean cluster-VOPR / chaos-drill streak is the evidence that "the venue is stable" is *earned*, not assumed.

> **Branch on which destination you're in (read before Monday).** If W131 placed **Bet #1** (§6 cleared): the "manager" below is your founder/lead, the "firm rubric" is the venue's review or a founder check-in, and the vesting tracker is **token/equity grant** milestones (cliff, vest schedule, TGE timing). If W131 engaged the **bridge** (crypto-MM cash seat, §7): the "manager" is your desk lead, the rubric is the MM firm's review, the vesting tracker is **cash-comp + any retention/bonus** milestones — and Friday's Bet #2 seeds carry *extra* weight, because the bridge exists to fund the *next* shot. Every item works for both; the branch only changes what the artifact contents point at.

---

## Monday — Manager/founder 1:1: how's it going from your side?

- [ ] **[venue-rig] Weekend cluster-VOPR + alert review** — artifact: append to `chaos_log.md` (or `ops_alerts.md`). 15 min: scan weekend `perp-dex-core` alerts on the deployed venue rig — any per-mark-tick p99.99 spikes, **VSR replica-divergence** events, **insurance-fund-tripwire** approaches, or disk-fill warnings? Record `clean` explicitly if nothing fired; an empty log is ambiguous, a dated `clean` line is evidence. (~15 min)
  - *Mental model*: the M36 finish line (§1 bar (a)) is **4000+ runtime hours** with the §4A bar-(c) invariants intact — static-memory, zero-allocation on the apply path, cluster-VOPR StateChecker green (linearizability + replica convergence + **insurance-fund tripwire NEVER hit**). A silent weekend isn't proof the venue is healthy — only a dated, reviewed entry is. This is the same single-writer / observe-everything discipline the venue crates were built on, applied to your own operations. *(On the bridge: substitute the MM desk's stack you operate; the discipline is identical.)*
  - Output: newest line in `chaos_log.md` is dated this Monday and states either the alert + triage or `clean — no p99.99 / VSR-divergence / tripwire / disk alerts`.
- [ ] **Manager/founder 1:1 prep + score** — artifact: `career/142_1on1_self_score.md` (private). Before the 1:1, score yourself honestly across the dimensions your manager/founder actually evaluates on (for a founding seat: delivery on the core engine, code/architecture quality, ownership of a critical-path component, ramp speed, judgment under ambiguity; for the bridge: latency-craft delivery, desk collaboration, ramp). For each: a 1–5 self-score AND one concrete piece of evidence (a shipped PR into the venue's core, an incident you handled, a design doc you authored) — a score with no evidence is a vibe, not an input. Open the dialogue in the 1:1 itself. (~30 min)
  - 🎯 **Hardened acceptance**: ≥5 dimensions scored 1–5; every score backed by exactly one named, datable artifact (PR link, incident ID, doc title) — zero unbacked scores; at least one dimension self-scored **below** your average (an all-4s-and-5s sheet means you didn't look hard enough); for a founding seat, at least one dimension is **"am I building the engine that IS the company"** (§6 core-criticality) scored honestly — if it's low, that's a signal, not a failure to hide.
  - *Mental model*: a first-quarter founder's read of you is still forming and is dominated by recency + legibility, not raw output. Walking into the 1:1 with an evidence-anchored self-score does two things: it shows you self-evaluate like a senior, and it lets you steer the narrative toward your strongest evidence before the impression hardens. In a *founding* seat there's a second function: this is also where you sense whether the §6 bet thesis still holds from the inside (is the allocation real? is the backing real? am I core?) — the 1:1 is your cheapest read on it. This sheet seeds Tuesday's formal self-assessment.
  - Output: `career/142_1on1_self_score.md` exists meeting the hardened-acceptance criteria; `grep -c '^- ' career/142_1on1_self_score.md` ≥ 5.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday 1:1-prep commit.

---

## Tuesday — Quarterly review: self-assessment draft

- [ ] **First-quarter self-assessment** — artifact: `career/142_q1_self_assessment.md` (private). Using the venue/firm's real review template (see References) as the skeleton, draft your first formal self-assessment. Translate the Monday self-score + the impact log (Wednesday) into the venue's rubric language. Stay honest on geography/comp anonymity in any version that leaves your machine (remember the Dubai/SG **flip-trigger** is still un-fired unless §7's warm-cluster / presence-premium condition arose). (~45 min)
  - 🎯 **Hardened acceptance**: the draft maps onto every section the template requires (no section left blank with "TBD"); each claimed strength cites a concrete artifact; the "areas to grow" section names ≥2 real gaps with a one-line plan each — not a humble-brag disguised as a weakness; for a founding seat, exactly one strength is framed as **product-shape judgment** (e.g. "drove the cross-margin netting-haircut decision," "owned the settle-interface boundary") not just throughput, because at a venue the judgment is the moat.
  - *Mental model*: the gap between a mid and a senior first-quarter review is almost never the work — it's whether the self-assessment speaks the rubric's language and lands evidence in the boxes the calibration committee (or founder) actually reads. A correct self-assessment is a translation task from "what I did" to "what the rubric rewards"; doing it now, before the deadline, leaves time to gather any missing evidence. At a founding venue the rubric is often *informal* — which makes this harder, not easier: you must infer what "performing" means and self-author the frame, exactly the senior move.
  - *Back-of-envelope*: ~6–8 weeks in the seat = roughly 240–320 working hours of observable output. The reviewer skims; budget that they read ~3 bullets per section. So the draft must front-load the single strongest artifact per section — diminishing returns past the third bullet.
  - Output: `career/142_q1_self_assessment.md` exists, covers every required template section, meets the hardened-acceptance criteria.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday self-assessment commit.

---

## Wednesday — Impact log + venue ops dashboards + 4000-hour capture

- [ ] **[venue-rig] Operations dashboard check + runtime-hours capture** — artifact: update the runtime-hours row in `progress.md`. 30 min: review the `perp-dex-core` ops dashboards (per-mark-tick p99.99 at the deployed account count, VSR commit latency, insurance-fund balance vs the declared 10%-taker accrual, projection-staleness metric on the analytics plane), capture the cumulative **runtime-hours** total, and update `progress.md`. At M36 the §1 bar-(a) target is **4000+ hours**; record the current number and the projected hit date. (~30 min)
  - *Mental model*: the 4000-hour bar-(c) requirement (§1 bar (a)) is a *falsifiable claim* about the venue's stability, and a number in `progress.md` is the only thing that makes it auditable at W144 plan close. Capturing it weekly turns "I think it's stable" into a tracked time series. The dashboards double as your live read on the §4A invariants in production — a drifting insurance-fund balance or a creeping p99.99 is the venue telling you something before the VOPR does.
  - Output: `progress.md` runtime-hours row shows this week's total + projected 4000-hour date; `git diff progress.md` shows the updated number.
- [ ] **Vesting-milestone + runway tracker** *(v3)* — artifact: `career/142_vesting_runway.md` (private). Record, for the seat you're in: (i) the vesting/cliff schedule and the **next milestone date** (token TGE / cliff date for a bet; retention/bonus date for the bridge); (ii) current **runway** (§1 bar (d): ≥6–12 months at each bet entry — is the floor intact?); (iii) a one-line §6 re-check on the seat ("does the seat still pass founding-tier allocation + tier-1 backing + core-criticality, or has the thesis decayed since W131?"). (~30 min)
  - 🎯 **Hardened acceptance**: names the next vesting milestone with a **date** (not "soon"); states runway in months against the ≥6–12mo bar; the §6 re-check lists all three of {allocation, backing, core-criticality} with a one-word verdict each (hold / watch / decayed) — and if any reads `decayed`, a one-line "what would trigger re-bridge or Bet #2" note (this is §7 + §9 "never quit a bridge before vest milestones" made concrete).
  - *Mental model*: a bet is a *tail bet*, not a salary (§7) — its value is illiquid token/equity gated on milestones you do not control, so the only defensive move is to *track them* and keep the income floor (§1 bar (d)) intact underneath. §9's failure mode "token allocation illiquid / FDV craters post-TGE" is mitigated only by the 2–3-shot structure + keeping bridge income through the cliff + sizing each bet so failure ≠ ruin. This tracker is the write-ahead-log of that discipline: the moment the seat's §6 thesis decays is the moment Bet #2 (Friday) stops being a someday-idea and becomes the plan.
  - Output: `career/142_vesting_runway.md` exists meeting the hardened-acceptance criteria.
- [ ] **First-quarter impact log** — artifact: `career/142_q1_impact_log.md` (private). Enumerate every shippable thing you did in the seat so far: PRs merged into the venue's core, incidents handled, reviews given, design docs written, onboarding/architecture friction you removed. For each, note the **outcome** in one line (not the activity — the effect on the product/team). This is the evidence pool both Monday's score and Tuesday's self-assessment draw from; building it mid-week lets you back-fill any thin section. (~45 min)
  - 🎯 **Hardened acceptance**: ≥8 distinct entries each with {artifact link/ID, one-line outcome}; outcomes stated as effects ("cut the margin-recompute hot path so a 2nd instrument fit the per-tick budget") not activities ("worked on margin"); at least one entry is a **non-code contribution** (mentoring, docs, an architecture decision you steered); at least one entry ties a contribution to the **company's core** (§6 core-criticality — proof you're on the engine, not the periphery).
  - *Mental model*: people systematically under-log their own first-quarter impact because the small wins feel unremarkable in the moment and evaporate from memory by review time. A running impact log is the same write-ahead-log discipline as the `wal` crate: record the event when it happens, because reconstructing it later is lossy and expensive. At a founding venue this pool does triple duty — it de-risks the 1:1, the formal review, *and* the W144 "each bet upgrades the next" narrative that justifies the allocation tier of Bet #2.
  - Output: `career/142_q1_impact_log.md` exists with ≥8 outcome-framed entries; `grep -c '^- ' career/142_q1_impact_log.md` ≥ 8.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday impact-log commit.

---

## Thursday — Reth/Tempo + `perp-dex-core` open-source continuity

- [ ] **Open-source continuity check** — artifact: `notes/142_oss_continuity.md`. Now settled in the seat, your Reth/Tempo open-source presence + the public `perp-dex-core` repo are no longer a *bet-search engine* but a **long-term credibility asset** (§1 bar (b): the venue + ≥3 supporting public artifacts are the durable proof that re-bridges and re-bets you if this seat's thesis decays). Spend the slot triaging: any review requests, broken CI, or stale PRs/issues on your merged Reth contributions or the `perp-dex-core` repo that need a 15-minute response to stay a good citizen? Record what you touched (or an explicit "nothing actionable this week"). (~45 min)
  - *Mental model*: the inheritance principle says every component you built mirrors an upstream module — your standing in those upstream communities + the public capstone is the *transferable core* (§7: "no skill stranded on a single ecosystem's death"). It decays if you ghost the moment you land the bet. A few minutes of maintenance keeps the door open for Bet #2 and honors the maintainers who reviewed your PRs. This is relationship-WAL, not busywork — and the public `perp-dex-core` is precisely the artifact a Bet #2 conversation will open with.
  - Output: `notes/142_oss_continuity.md` dated this Thursday, listing each touched item or `nothing actionable this week`.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday OSS-continuity commit.

---

## Friday — Quarterly review draft: finalize + pressure-test + Bet #2 thesis seeds

- [ ] **Self-assessment finalize + adversarial read** — artifact: update `career/142_q1_self_assessment.md` to a submittable draft. Re-read Tuesday's draft against Wednesday's impact log: every strength claim must now point at a log entry; fill any section that was thin. Then do one adversarial pass: read it as a skeptical calibration committee / founder and flag any claim a reviewer could dispute, tightening or cutting it. (~45 min)
  - 🎯 **Hardened acceptance**: every strength claim in the final draft traces to a specific `career/142_q1_impact_log.md` entry (no orphan claims); the adversarial pass produced ≥2 concrete edits (a softened overclaim, a cut vague line, or an added piece of evidence) recorded at the bottom under `## Adversarial pass`; the draft is marked `STATUS: submittable`.
  - *Mental model*: the failure mode of a self-assessment is not too little content, it's **unbacked or disputable claims** a reviewer discounts wholesale — one overclaim taints the credible ones around it. The adversarial read is the *negative-control* of career writing: you stress-test your own document to find the claim that breaks before the committee does, exactly like a cluster-VOPR run hunts the schedule that breaks the venue before production does (§4A: "a schedule that hits the tripwire defines the v1.5 trigger").
  - Output: `career/142_q1_self_assessment.md` ends with a `## Adversarial pass` section listing ≥2 edits and a `STATUS: submittable` line.
- [ ] **Bet #2 thesis seeds** *(v3)* — artifact: `career/142_bet2_seeds.md` (private). The §7 ladder is `Bet #1 → Bridge₁ → Bet #2 → capture` — each bet *upgrades the next* via reputation + network + capital. While the first quarter is fresh, seed the Bet #2 thesis: (i) which adjacency does *this* seat open (the team you're on, the founders you now know, the part of the stack you now own)? (ii) which 2–3 venues/ideas would clear the §6 checklist at a *higher* allocation tier than Bet #1, reachable via the network this seat builds? (iii) what would the **v1.5 capstone differentiator** (RWA features + multi-collateral + ADL, per §4A / W118–119) prove that raises your tier for Bet #2? Just seeds — this is W144's raw material, not a decision. (~45 min)
  - 🎯 **Hardened acceptance**: names ≥2 candidate Bet #2 directions, each with a one-line §6 sanity-check (does it plausibly clear founding-tier allocation + tier-1 backing + core-criticality?); names ≥1 concrete network edge this seat created that points at a higher-tier reachable venue; states one v1.5-capstone proof-point that would raise the Bet #2 allocation tier — all framed as hypotheses to revisit at W144, not commitments.
  - *Mental model*: the $20–100M outcome is a **deliberate tail bet structured as 2–3 sequential shots, never one all-in** (§7). The single most common way people fluff the ladder is to treat Bet #1 as the destination and coast — losing the compounding (network + rep + capital) that makes Bet #2 the higher-tier shot. Seeding the thesis *now*, six weeks in, is the cheapest possible insurance against coasting, and it costs one Friday slot. On the bridge, this item is *load-bearing*, not optional: the bridge's entire purpose (§7) is to fund the shot you're seeding here.
  - Output: `career/142_bet2_seeds.md` exists meeting the hardened-acceptance criteria.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday finalize + Bet #2-seeds commit.

---

## Saturday — Personal maintenance + [venue-rig] cluster-VOPR chaos drill

- [ ] **[venue-rig] Weekly cluster-VOPR chaos drill** — artifact: append to `chaos_log.md`. 60 min: run this week's rotated chaos drill against the deployed `perp-dex-core` cluster (VSR leader kill → view-change / single-node crash-restart / network partition / clock skew / disk fill / slow analytics-plane subscriber / **insurance-fund-tripwire schedule search** — pick the next in rotation). Capture: scenario, expected behavior, observed behavior, recovery time, any **VSR replica divergence** or **tripwire hit**. A drill with no recorded recovery time isn't a drill. (~60 min)
  - 🎯 **Hardened acceptance**: the new `chaos_log.md` entry names the scenario, states expected vs observed behavior, records a measured recovery time, and ends with a pass/fail verdict; if fail, a one-line follow-up action is logged; if the rotated scenario is the **tripwire search**, the entry states explicitly whether the insurance-fund tripwire was hit at the declared 10%-taker accrual rate (a hit is the §4A signal that the **v1.5 ADL/socialized-loss** path must engage — log it as a v1.5 trigger, not a failure).
  - *Mental model*: back-pressure, crash-recovery, and the insurance-fund waterfall are **correctness properties** of the §4A bar-(c) venue, not nice-to-haves — the only way to know they hold is to inject the failure and measure the recovery, weekly, deterministically. As you cross the 4000-hour mark, a clean cluster-VOPR streak is the evidence that "the venue is stable" is *earned*, not assumed — and it's exactly the artifact a Bet #2 conversation (Friday's seeds) opens with: "here's a multi-node hybrid CLOB that has survived N weeks of injected partition + crash + tripwire search with zero lost commits." This is the deterministic-simulation lens applied as an operations ritual. *(On the bridge: run the MM desk's chaos/failover drill instead; the recorded-recovery-time discipline is identical.)*
  - Output: newest `chaos_log.md` entry meets the hardened-acceptance criteria; `git diff chaos_log.md` shows the dated drill result.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday chaos-drill commit.

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
