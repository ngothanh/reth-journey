# Week 65 — Architectural engagement + [HFT] matching-engine multi-symbol + consensus-bft 3-phase vote + [v3] `log-distributed` ISR

> **Phase**: 4
> **Month**: M17
> **Track**: Multi (Reth + HFT + Tempo + Consensus + [v3] distributed-log)
> **Hours target**: 30h/wk (M1-M18)
> **Mirror target**: matching-engine symbol shards mirror LMAX Disruptor per-symbol single-writer rings + Seastar/Scylla thread-per-core sharding; consensus-bft prevote/precommit mirror tendermint-rs `vote_set.go` + `state.go` (Tendermint Annex A prevote/precommit rules); **[v3] `log-distributed` ISR mirrors Kafka's in-sync-replica set + high-watermark + KIP-101 truncation-on-leader-change** (the **off-hot-path** distributed-log learning artifact; **NOT** the venue hot-path log, which is the VSR log)
> **Crate(s) touched**: matching-engine (v0.7 → multi-symbol shards), consensus-bft (v0.1 scaffold → +prevote.rs +precommit.rs), **[v3] `log-distributed` v0.1 → +isr.rs** (in-sync-replica set + high-watermark over the W63 partitioned segments; consumes `consensus-raft` (W62) as the off-path ISR-controller analogue, plus W63 `partition`/`segment`)
> **Bar**: (c) for `matching-engine` (HFT critical path) and `consensus-bft` (Layer-1 substrate) — zero-allocation hot paths + static-memory invariants + deterministic-sim coverage are non-negotiable. **[v3] `log-distributed` is bar (b)** — off-hot-path; correctness + crash-recovery + determinism matter, sub-µs latency does not (that is the VSR log's concern). No Cost-avoided cards on the ISR Build.
> **Feeds into**: W66 matching-engine perpetuals layer (lives inside `SymbolShard`); W66 `log-distributed` consumer-group offset commit (rides this ISR high-watermark); W68 consensus-bft v0.5 ship (gated on prevote+precommit); W73 consensus-bft v1.0 (BLS aggregate signatures collapse VoteSet); W74 matching-engine v1.0 (per-shard raft replication on top of this skeleton) + consensus-vsr v0.5; W83+ ledger-deterministic per-shard state machine + VSR PrepareOk batching; **W84 `log-distributed` v0.5 (Kafka-fidelity off-path + VSR-log projection) — the ISR high-watermark is what the projection's bounded-staleness contract is defined against**.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): tendermint-rs `tendermint/src/vote.rs`, `consensus/src/vote_set.go` (slot-indexed tally), Tendermint paper Annex A (prevote/precommit/lock rules); LMAX Disruptor single-writer principle (Thompson, "Mechanical Sympathy"); Seastar sharded<> design note; libnuma `numa_alloc_onnode` / `numa_node_of_cpu` man pages; **[v3] Kafka replication design (`ReplicaManager`/`Partition.scala` high-watermark + ISR shrink/expand), KIP-101 (truncation on leader change), KIP-405 concepts**. Lookup only when a specific Phase-3 card stalls.
> **v2 modifications**: none
> **v3 modifications**: ADDITIVE — all v2 content (Reth arch engagement, matching-engine multi-symbol shards, consensus-bft prevote+precommit) is preserved unchanged. Woven in: **[v3] `log-distributed` `isr.rs`** — the in-sync-replica set + high-watermark layer over the W63 partitioned log, riding `consensus-raft` (W62) as the **off-path** ISR-controller analogue. This is README.md §A's "+ `log-distributed` v0.1 seed (W63–66)" landing its mid-window module: W63 built `partition`/`segment`; W65 adds `isr`; W66 adds consumer-group offset commit; W84 wires the VSR-log projection. It is **off the hot path** — the one hot-path order/event/settlement log is the VSR log (`consensus-vsr`, §3/§4/§9 "no second hot-path log, no primitive twice"). The R3 hot-path-leak lint from W63 is re-asserted here.

**Monday — GitHub discussions + [HFT] matching: multi-symbol shard plan + [v3] ISR seam sketch**

- [ ] `notes/65_arch_discussions.md` — browse 2-3 ongoing execution-layer architecture discussions (reth `Discussions` + recent `engine`/`payload` PRs). Note one open design question + your stance. Output: file with ≥3 thread links + 1 stance paragraph. (~30 min)
- [ ] [v3] `crates/log-distributed/notes/65_isr_topology.md` — sketch the ISR + high-watermark seam over the W63 partitioned log: the `ReplicaSet { leader, isr, leo[] }` shape, `HW = min LEO over current ISR`, the committed-read gate (`offset < HW`), and the **off-path** ISR-shrink control loop driven by `consensus-raft` (W62). Mark explicitly that this is the **off-hot-path** mirror — the one hot-path log is the VSR log. Output: file with the replica/HW diagram + the `HW = min(leo[r] for r in isr)` formula + a one-line VSR-contrast statement. Feeds Thursday's `isr.rs` Build. (~30 min)
- [ ] [HFT] `crates/matching-engine/notes/65_shard_topology.md` — sketch the per-symbol Disruptor-style topology: single-writer per symbol, multiple-reader fan-out, each shard owns its own `wal::Wal` segment, symbol selection by hash. Output: file with the mailbox diagram + the `route(symbol) -> shard_id` formula. Mirrors LMAX per-symbol ring; feeds Tuesday's `shard.rs` Build. (~30 min)
- [ ] Commit notes (~10 min)

**Tuesday — Substantive comment + [HFT] symbol-sharded engine impl**

- [ ] `notes/65_arch_comment.md` — draft + post one substantive architectural comment on a reth discussion (anchored to Monday's stance). Output: posted comment URL recorded in the file. (~30 min)
- [ ] [HFT] **Build**: `crates/matching-engine/src/shard.rs` — `SymbolShard { book: OrderBook, event_log: EventLog, ... }` and `Engine { shards: Box<[SymbolShard]>, symbol_to_shard, runtime }`. (~total 220 min across 3 phases)
  - 🔧 **Why this matters**: multi-symbol sharding is where the engine actually scales — single-symbol bench numbers are nice but production has 1000s of symbols. Mirrors LMAX Disruptor's single-writer-per-ring principle and Seastar/Scylla's thread-per-core sharding. With runtime-thread-per-core v0.5 shipped (W60), each symbol-shard pins to a core; no locks within a shard. **First-attempt impls fail ≥3 of the Phase-2 tests below** — the `Arc<RwLock<SymbolShard>>` reach adds ~10 ns CAS/access × 1 M/sec = 10 ms/sec of pure sync overhead per shard plus cross-shard cache-line ping-pong; NUMA-blind allocation makes shards 4-7 measure half the throughput of shards 0-3; a per-call hasher built freshly (e.g. `Box<dyn Hasher>` or a re-keyed `RandomState` per `route()`) costs ~50-150 ns of state init + keyed hashing on every submit, plus a heap alloc if boxed. The debug session is the lesson. Downstream: W66 perpetuals layer is per-symbol (lives inside `SymbolShard`); W74 raft-backed engine adds replication per-shard on top of this skeleton; W83+ ledger handles cross-symbol via a separate risk-shard process that broadcasts from `event_log`.

  #### Phase 1 — Problem, prediction, design (~55 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in `crates/matching-engine/notes/65_shard_prediction.md`. Compare to reality after tests run.
    - P1. Name the container type you'd reach for to hold the shards, sight-unseen. Write the exact field declaration line you'd type from memory (e.g. `shards: Vec<Arc<RwLock<SymbolShard>>>`). No commentary.
    - P2. Predict the p50 latency (ns) of `engine.submit(order)` for the routing path only (hash + modulo + channel push), assuming your first-attempt impl. One number.
    - P3. Predict the `8_shard_aggregate / single_shard` throughput ratio on your local hardware (single-NUMA-node dev box). One number, then a second number for a 2-NUMA-node host with NUMA-blind allocation.
    - P4. Predict the per-order allocation count on the dispatch path for your first attempt (hash route + enqueue). One number.
    - P5. Of the Phase-2 tests below, list every test name you predict your first attempt will fail. Reveal happens when you run the tests; the delta is the lesson.

  - *Mental model*: partition engine state so each symbol's `OrderBook` is owned by exactly one shard (one pinned core), touched by exactly one task. Cross-shard work flows through the runtime mailbox, never through a lock on the book. Single-writer-by-construction — the same discipline as an LMAX ring per symbol.

  - *Contract* (signature-level — NEVER the body):
    ```rust
    pub struct SymbolShard { book: OrderBook, event_log: EventLog, risk: RiskState, bcast: BroadcastSender }
    // SymbolShard is NOT Sync and is never wrapped in Arc/RwLock by the engine.
    pub struct Engine { shards: Box<[SymbolShard]>, hasher: FxBuildHasher, num_shards: u32, runtime: Runtime }
    impl Engine {
        pub fn submit(&self, order: Order) -> Result<(), SubmitErr>; // route + mailbox push, never touches a book directly
        pub fn cancel(&self, id: OrderId) -> Result<(), SubmitErr>;  // shard_id read from OrderId high bits
        fn route(&self, symbol: &Symbol) -> u32;                     // hash(symbol) % num_shards, stable
    }
    ```

  - *Back-of-envelope*: budget is 1 M orders/sec/shard, p99 ≤ 10 µs (W63 v0.5 single-symbol baseline). Submit routing path = 1 hash (~30-50 ns FxHash on a short symbol) + 1 modulo (~5 ns) + 1 channel push (~50 ns) ≈ 100-150 ns p50 — that leaves ~9.85 µs for actual matching. Two reference points, kept distinct: L1 access ~0.5 ns; NUMA-local main-mem ~100 ns; cross-NUMA-node fetch ~200 ns (Jeff-Dean main-mem 100 ns × ~2 for interconnect) = **400× cliff vs L1** / **~2× tax vs local main-mem** (the cliff that bites the hot path is the 100 ns local → 200 ns remote step on a book mutation that misses L1/L2). At ~3 cache lines touched/order, NUMA-blind allocation costs ~600 ns/order ≈ 60% of the per-order budget — that is the perf cliff this Build must avoid. `Arc<RwLock>` adds ~10 ns CAS + ~10 ns refcount per access = 20 ms/sec of pure sync waste per shard at 1 M/sec.

  - **Requirements** R1..R8 — write in `crates/matching-engine/notes/65_shard_design.md`:
    - R1. `SymbolShard` owns its `OrderBook`, `EventLog`, risk impl, and broadcast sender by value. No `Arc`, no `RwLock` around it. `SymbolShard: !Sync`.
    - R2. `Engine.shards: Box<[SymbolShard]>` where `len = num_shards` (not num_symbols). Typically `num_shards = num_cores` or `num_NUMA_local_cores`.
    - R3. **Symbol routing**: `route(&symbol) -> shard_id` via `hash(symbol) % num_shards`. Stable mapping; rebalance is offline-only at v0.5. Hasher constructed ONCE per `Engine`.
    - R4. **Single-writer per shard**: only the owning shard's task processes orders for its symbols. No cross-shard locking on the book; cross-shard work flows through the runtime mailbox (per-shard channel, W60 primitive).
    - R5. **NUMA-local memory**: each shard's `OrderBook`, `EventLog`, and per-call scratch buffers live on the same NUMA node as the shard's pinned core. Verified by `numastat -p <pid>` showing ≥95% local hits.
    - R6. **Linear scaling**: 8-shard aggregate throughput ≥ 6× single-shard throughput. (1.0× linear is unrealistic; 6× is the practical floor.)
    - R7. **Per-shard budget**: 1 M orders/sec/shard sustained; p99 ≤ 10 µs per shard.
    - R8. **Zero allocations on dispatch**: `MatchCommand` reuses a per-shard mailbox slab; no per-order alloc on the routing path. OrderId encodes shard_id in its high bits.

  - **Design walk-through** D1..D9 — answer each in the design doc; no peeking at LMAX Disruptor or CME Globex docs until Phase 3:
    - D1. Enumerate every container you could reach for, with a property table — fill in (Sync? per-access cost? allocs? blocking?):

      | choice | Sync? | per-access cost | cross-shard ping-pong? | verdict |
      |---|---|---|---|---|
      | `Vec<Arc<RwLock<SymbolShard>>>` | yes | ~20 ns CAS+refcount | yes (refcount dec) | ✗ |
      | `Box<[Mutex<SymbolShard>]>` | yes | ~25 ns lock | no | ✗ (still locks) |
      | `Box<[SymbolShard]>` + per-shard task | no | 0 (single-owner) | no | ✓ |

    - D2. The mailbox pattern: each shard has a `cross_shard::Channel<MatchCommand>` (W60 primitive). Submit flow: `engine.submit(order)` → `route(order.symbol) → shard_id` → `channel[shard_id].send(MatchCommand::Submit(order))`. Where does the shard's task LIVE? (a runtime task pinned to a core; it loops over `channel.recv()`.)
    - D3. NUMA allocation: write the per-shard init flow. The shard's task starts, learns its core, looks up the NUMA node via `numa_node_of_cpu(core_id)`, allocates `OrderBook` + `EventLog` on that node via `numa_alloc_onnode`. Why must the alloc happen on the SHARD's task, not on the engine constructor's thread?
    - D4. Hot-symbol problem: BTC-USDT routes to shard 5; 80% of volume hits shard 5; other shards idle. v0.5 mitigation? (pre-split into virtual sub-symbols.) v1.0 plan? (dynamic resharding with drain protocol.)
    - D5. Cancel routing: `cancel(order_id)` needs the owning shard. Embed `shard_id` in the OrderId's reserved bits (top 6 bits = shard_id, lower bits = sequence) vs a global `OrderId → shard_id` map. Trade-offs.
    - D6. Cross-shard concerns: an account is long BTC on shard 3 and short ETH on shard 7. Cross-margin check needs both. Who computes it at v0.5? (per-symbol margin only; deferred to a separate risk-shard process at W90+.)
    - D7. **Pre-mortem #1 (the Arc<RwLock<>> trap) — load-bearing:** write the EXACT 4-line trace showing `Arc<RwLock<SymbolShard>>` adds ~10 ns CAS per access × 1 M/sec = 10 ms/sec of pure sync overhead per shard even with NO contention, plus the cross-shard Arc-clone refcount ping-pong scenario. If you can't write the trace you can't prove the lock-free design beats it.
    - D8. **Pre-mortem #2 (NUMA tax) — load-bearing:** write the EXACT 5-line trace where `Engine::new` allocates all shards on node-0, then shards 4-7 pin to node-1 cores. Compute the per-access penalty (200 ns cross-node vs ~100 ns NUMA-local main-mem on a cache-missing book mutation — and contrast with the ~0.5 ns L1 hit it should have been) and the throughput hit on the affected shards.
    - D9. **Anti-shortcut audit:** for each motivation name the "obvious" reach and one line on why it's wrong HERE — (a) shared state → `Arc<RwLock>` (adds sync to a single-owner design); (b) routing → a fresh hasher per call: `ahash::RandomState::new()` (~30-80 ns state init from the process-global counter, NO alloc — but re-derives keys so the hash differs from the engine's, and re-doing it per submit is pure waste) or `Box<dyn Hasher>` (adds a genuine ~50 ns heap alloc per submit on top); construct once per engine instead; (c) cross-shard queue → one shared MPMC (couples unrelated shards under skew).
  - 🧮 **Paper drill** — write in `notes/65_shard_drill.md`:
    - D-LAYOUT on `SymbolShard`'s `OrderBook`+`EventLog` arena across a 2-NUMA-node host (shards 0-3 → node-0 cores, 4-7 → node-1 cores): draw each shard's per-shard arena byte map, mark the NUMA node each allocation lands on, and circle the cross-interconnect fetch (200 ns remote vs 100 ns local vs 0.5 ns L1 hit) that an `Engine::new`-on-node-0 allocation forces on shards 4-7 — call out the ~600 ns/order straddle (R5).
    - D-SS on `SymbolShard`: run the two-question worksheet — Q1 sharing `&SymbolShard` exposes the `OrderBook` to concurrent mutation → it must be `!Sync`; Q2 moving a shard into its pinned task needs `Send` on `book`+`event_log`. Derive the bound and write the rejected counterexample: the `Arc<RwLock<SymbolShard>>` reach that the `!Sync` single-writer-per-shard invariant (R1) correctly forbids (this is what `arc_rwlock_compile_fail` pins).
    - D-STRUCT on a 3-shard `Box<[SymbolShard]>` with symbols {BTC-USDT, ETH-USDT, SOL-USDT}: hand-execute `route(symbol)` then the mailbox `channel[shard_id].send(MatchCommand::Submit)` step by step, redrawing the per-shard mailbox slab after each push; show the linearization point is the single-owner task's `recv()` and why no lock on the book is needed.
    - D-COST on the `submit` routing path: per-op ledger (ns/cycles/allocs) for hash (~30-50 ns FxHash) + modulo (~5 ns) + channel push (~50 ns); compute the cliff vs the per-call `RandomState::new()` reach (~250-400 ns) and the NUMA-blind 600 ns/order tax; state whether 100-150 ns p50 fits the ~10 µs per-order budget (R7, R8).
    - D-HB on the cross-shard mailbox handoff: two columns (gateway-task casting `submit` → shard-task running `recv`+book-mutate). Draw the PO edges, mark the channel send's Release and the recv's Acquire, draw the RA edge proving the shard-task sees the order's fields; then the NEGATIVE — weaken the send to `Relaxed` and draw the interleaving where the shard reads a torn/half-published `Order`.
  - **Output**: `notes/65_shard_design.md` with R1–R8, D1–D9, the property table filled, the mailbox topology diagram, the NUMA-aware init flow, and both pre-mortem traces.

  #### Phase 2 — Write the failing tests (~50 min)

  Each test is engineered so a common naive impl FAILS it. Tier-tagged. Includes all three trap shapes.

  - [ ] **`single_shard_processes_orders_correctly`** (NEWBIE) — 1 shard, 1 symbol, 100 orders; assert all match correctly. Baseline; exercises R1, R4.
  - [ ] **`route_hash_stable`** (NEWBIE) — assert `route("BTC-USDT")` identical across 100 invocations. Exercises R3.
    ```rust
    #[test]
    fn route_hash_stable() {
        let e = Engine::new(8);
        let first = e.route(&Symbol::from("BTC-USDT"));
        for _ in 0..100 { assert_eq!(e.route(&Symbol::from("BTC-USDT")), first); }
    }
    ```
  - [ ] **`arc_rwlock_compile_fail`** (SENIOR, anti-shortcut) — `trybuild` test asserting `SymbolShard: !Sync`, so `Engine` cannot expose `Arc<RwLock<SymbolShard>>`. Pins the ownership invariant. Exercises R1.
    ```rust
    // tests/ui/shard_must_not_be_sync.rs — expected to FAIL compile
    fn assert_sync<T: Sync>() {}
    fn main() { assert_sync::<matching_engine::SymbolShard>(); }
    // expected stderr: `SymbolShard` cannot be shared between threads safely
    ```
  - [ ] **`dispatch_zero_alloc`** (SENIOR, anti-shortcut, **bar (c)**) — heaptrack-tagged: 1 M order submits across 8 shards; assert ≤ 10 allocations attributable to dispatch. Exercises R8.
  - [ ] **`submit_p50_under_200ns`** (SENIOR, anti-shortcut, **bar (c)**) — bench `engine.submit` routing; assert BOTH p50 ≤ 200 ns AND p99 ≤ 300 ns (target ~150 ns p50). Traps the per-call hasher construction on both percentiles: the per-call `RandomState::new()` reach lands ~250-400 ns and MUST fail p50; the boxed-hasher variant adds a ~50 ns heap alloc that widens the tail and fails p99 even when p50 sneaks under. Exercises R3, R8.
    ```rust
    #[test]
    fn submit_p50_under_200ns() {
        let (p50, p99) = bench_submit_route_latency_ns(); // shared harness, routing path only
        // 150 ns target + margin; the per-call RandomState::new() reach lands 250-400 ns and MUST fail.
        assert!(p50 <= 200.0, "submit p50 {p50:.0} ns > 200 ns — per-call hasher init on the route path");
        // per-call init tax widens the tail; a boxed-hasher heap alloc fails this even when p50 sneaks under.
        assert!(p99 <= 300.0, "submit p99 {p99:.0} ns > 300 ns — per-call hasher init/alloc jitter in the tail");
    }
    ```
  - [ ] **`8_shard_linear_scaling`** (EXPERT, **bar (c)**) — **precondition: a 2-NUMA-node host** (gate with `#[cfg]`/runtime skip — `if numa_node_count() < 2 { eprintln!("SKIP: needs 2 NUMA nodes"); return; }` — so it can never silently green on a single-NUMA dev box). 16 symbols across 8 shards (0-3 pin to node-0 cores, 4-7 to node-1 cores), 100k orders/shard; assert BOTH (a) aggregate ≥ 6× single-shard AND (b) per-shard parity: `min(shard 4-7 throughput) ≥ 0.9 × min(shard 0-3 throughput)`. A NUMA-blind first attempt allocates all shards on node-0, so shards 4-7 run at ~half throughput and fail clause (b) even if aggregate happens to look acceptable. Exercises R5 + R6.
    ```rust
    #[test]
    fn shard_8_linear_scaling() {
        if numa_node_count() < 2 { eprintln!("SKIP: 8_shard_linear_scaling needs a 2-NUMA-node host"); return; }
        let single = bench_single_shard_throughput();
        let per_shard = bench_8_shards_per_shard_throughput(); // [f64; 8], shards 0-3 on node0, 4-7 on node1
        let aggregate: f64 = per_shard.iter().sum();
        assert!(aggregate >= 6.0 * single, "aggregate {aggregate:.2e} < 6x single {single:.2e}");
        let node0_min = per_shard[0..4].iter().cloned().fold(f64::INFINITY, f64::min);
        let node1_min = per_shard[4..8].iter().cloned().fold(f64::INFINITY, f64::min);
        assert!(node1_min >= 0.9 * node0_min,
            "node-1 shards {node1_min:.2e} < 0.9x node-0 shards {node0_min:.2e} — NUMA-blind allocation");
    }
    ```
  - [ ] **`per_shard_p99_under_10us`** (EXPERT, **bar (c)**) — under the 8-shard workload, each shard p99 ≤ 10 µs. Traps the `Arc<RwLock>` reach. Exercises R7.
  - [ ] **`cross_shard_cancel_routes_correctly`** (EXPERT, cross-primitive) — composes routing (R3) with OrderId encoding (R5): submit 1000 orders across 8 shards; cancel each via OrderId-only API; assert each cancel reaches the correct shard. Passes if cancel uses the OrderId's encoded shard_id; FAILS if cancel re-hashes by a different key. Exercises R3 + R5.
  - [ ] **`skewed_workload_isolation`** (EXPERT, cross-primitive) — composes per-shard channels (W60) with routing: 80% of orders target one symbol (hot shard); assert other shards' throughput unaffected (≤5% delta). FAILS with a single shared MPMC. Exercises R4.
  - [ ] **`numa_local_alloc_verified`** (EXPERT, **bar (c)**) — 8-shard engine on a 2-NUMA-node host; assert `numastat -p` shows ≥95% local hits per shard. Exercises R5.
  - [ ] **`submit_bench_physically_plausible`** (SENIOR, **negative-control / test-the-test**) — proves the submit bench measures something real: assert the measured ops/sec lands in a physically plausible band `2e6..=5e8` ops/s. A bench that compiled away the route would report ≥1e10 (instant) and fail this control; a bench stuck on a syscall would report <2e6 and fail it too.
    ```rust
    #[test]
    fn submit_bench_physically_plausible() {
        let ops_per_sec = measure_submit_ops_per_sec(); // shared harness
        assert!((2e6..=5e8).contains(&ops_per_sec),
            "submit bench {ops_per_sec:.2e} ops/s outside plausible band — bench measures nothing or stalls on syscall");
    }
    ```

  - 🪤 **Trap shapes covered** (the three mandated shapes, mapped to the concrete tests above):
    - *Cross-primitive trap*: `cross_shard_cancel_routes_correctly` (routing R3 ∘ OrderId encoding R5 — each passes alone, the composition exposes the hash-mismatch) and `skewed_workload_isolation` (routing ∘ W60 per-shard channels — a shared MPMC couples shards only when composed under skew).
    - *Anti-shortcut trap* (bar (c)): `dispatch_zero_alloc` + `submit_p50_under_200ns` (the `Box`/per-call-hasher reach is correct but blows the alloc + latency budget — the 200 ns p50 / 300 ns p99 bounds sit cleanly below the 250-400 ns per-call-`RandomState::new()` reach, so even the non-boxed naive impl is trapped) and `arc_rwlock_compile_fail` (the `Arc<RwLock>` reach compiles-and-runs correctly but is forbidden by the ownership bar).
    - *Negative-control test-the-test*: `submit_bench_physically_plausible` (a route compiled away reports ≥1e10 and fails high; a syscall-stalled bench reports <2e6 and fails low — so a green `submit_p50_under_200ns` is trustworthy).

  #### Phase 3 — Make the tests pass, one failure at a time (~115 min)

  Ordered newbie → expert — the ladder you actually walk.

  - **(NEWBIE) When `route_hash_stable` reports a different `shard_id` on each call:**
    - Symptom: `route("BTC-USDT")` returns 3, then 7, then 1 — assertion fails on the second call.
    - Why: you constructed `ahash::RandomState::new()` (random per-process seed, actually re-seeded per call) inside `route()`. A randomly-seeded hasher is non-stable across calls.
    - Fix: hold a **fixed-seed** hasher (`FxBuildHasher` or `RandomState::with_seeds(const,...)`) as an `Engine` field; reuse across calls.
    - Cost avoided: routing correctness (a wrong shard means a lost order) — not a perf card, a correctness card.
    - Muscle: routing must be deterministic — a routing hasher is fixed-seed and engine-scoped, never random-per-call. Reapplies at: W74 raft per-shard request routing, W83 ledger AccountId → shard mapping.

  - **(SENIOR) When `submit_p50_under_200ns` reports ~250-400 ns p50 (and a wider p99):**
    - Symptom: submit path p50 ~250-400 ns — cleanly OVER the 200 ns bound and ~2-3× the ~150 ns target. Flame graph shows ~40% in `RandomState::new` + keyed-init (`hash_one` re-deriving its keys from a freshly-seeded state every call); if the naive impl boxed the hasher, an extra ~10% sits in `__rust_alloc`/`__rust_dealloc` and the p99 blows past 300 ns from allocator jitter.
    - Why: you constructed a fresh hasher on each `route()` call. `RandomState::new()` is not free at hot-path scale: the process-global atomic counter read + address reads + per-call keyed-hashing setup land the WHOLE route at ~250-400 ns p50 — comfortably above the 200 ns bound even with NO heap alloc. A `Box<dyn Hasher>` variant adds a real ~50 ns heap alloc/free on top, pushing the tail past the 300 ns p99 bound.
    - Fix: hold the hasher (fixed-seed `FxBuildHasher`) as a field on `Engine`; reuse. Route becomes ~30-50 ns hash + ~5 ns modulo + ~50 ns push ≈ 100-150 ns p50, well under 200 ns / 300 ns.
    - Cost avoided: ~100-250 ns/submit of redundant hasher-state init removed (the per-call `RandomState::new()` route is 250-400 ns; the reused-hasher route is 100-150 ns — a ~150 ns/submit saving, more if the naive impl boxed the hasher); at 1 M submits/sec that's ~100-250 µs/sec reclaimed (**bar (c) violation** when shipped — submit eats budget that belongs to matching). Allocs/submit: 0 → 0 for the `RandomState::new()` reach (it does not allocate, but still busts the latency bound); 1 → 0 only if the naive impl used a boxed hasher.
    - Muscle: any hot-path hasher is CONSTRUCTED ONCE per engine, not per call. Reapplies at: every engine-level hash routing in this project; W74 raft routing, W95 mini-db shard routing.

  - **(SENIOR) When `dispatch_zero_alloc` flags ~1 M allocs/sec:**
    - Symptom: heaptrack tags ~1 alloc/order on the dispatch path; the zero-alloc assertion fails.
    - Why: `MatchCommand::Submit(order)` boxes the order (or the channel allocates a node per send) on every dispatch.
    - Fix: use a pre-allocated per-shard mailbox slab (W60 bounded channel writes into a ring slot by value, no node alloc). `MatchCommand` is a `Copy`/small POD enum, not a `Box`.
    - Cost avoided: ~50 ns malloc/order × 1 M/sec = 50 ms/sec; closes allocator jitter on the hot path (**bar (c) violation** — allocator in the inner loop). Allocs: 1/order → 0.
    - Muscle: the dispatch path is static-memory — pre-allocate the mailbox slab; never `Box` per message. Reapplies at: W77 aeron term buffer, W85 marketdata RX queue, W92 ledger VOPR message pool.

  - **(EXPERT) When `per_shard_p99_under_10us` reports p99 ≈ 35 µs and the flame graph shows `Arc::clone` + `RwLock::write` at ~30% CPU:**
    - Symptom: per-shard p99 3.5× over budget; `Arc::clone`/`RwLock::write` dominate.
    - Why: you wrapped `SymbolShard` in `Arc<RwLock<_>>` "just in case". Every access pays atomic CAS on `read()`/`write()` (~10 ns uncontended) on EVERY order; Arc refcount inc/dec adds ~10 ns each; cross-thread decrements ping-pong the refcount cache line.
    - Fix: own `SymbolShard` **by value** inside the per-shard task. Shards live as `Box<[SymbolShard]>`; each shard's data is touched only by its owning task. Cross-shard work goes through `runtime::submit_to(shard_id, FnOnce)` (W60) — the mailbox is the synchronization point, not the shard. Add `arc_rwlock_compile_fail` to lock the invariant.
    - Cost avoided: ~20 ns sync/access × 1 M/sec ≈ 20 ms/sec removed per shard (**bar (c) violation** — lock on the critical path). Eliminates the lock as a scalability ceiling.
    - Muscle: thread-per-core = single-owner-by-design; reaching for `Arc<RwLock<_>>` is a smell that the ownership model is wrong — the mailbox is the synchronization boundary. Reapplies at: W74 raft-backed engine per shard, W77 aeron subscriber state, W83 ledger-deterministic per-shard SM, W95 mini-db memtable.

  - **(EXPERT) When `8_shard_linear_scaling` fails clause (b) — node-1 shards (4-7) at ~0.5× node-0 shards — and `numa_local_alloc_verified` shows ~40% local hits on shards 4-7:**
    - Symptom: on the 2-NUMA host the parity clause fires first (node1_min ~0.5× node0_min, < the 0.9× floor); aggregate may also dip toward ~3.2× (vs 6×). `perf c2c` flags 30-60% of cache lines as cross-NUMA-node fetches; shards 4-7 measure half the throughput of shards 0-3. (On a single-NUMA box the test SKIPs, so this failure only surfaces where it is real.)
    - Why: at the default Rust allocator, `Engine::new` allocates all shards on the thread that called `new` — likely node-0. When shard-7 pins to a core on node-1, every `book` mutation that misses cache crosses the interconnect: ~200 ns cross-node vs ~100 ns NUMA-local main-mem — a ~2× tax on every cache-missing access (and ~400× vs the ~0.5 ns L1 hit it should have been) on the critical path.
    - Fix: per-shard NUMA-local allocation. In the shard's init code (running on its own pinned task), look up `numa_node_of_cpu(core_id)`, allocate `OrderBook`, `EventLog`, and scratch via `numa_alloc_onnode(size, node_id)` (libnuma), or install a per-shard allocator bound to the node via the `Allocator` API. Verify with `numastat -p <pid>`.
    - Cost avoided: ~100 ns per touched cache line that crosses the interconnect (200 ns remote − 100 ns local); at 1 M orders/sec/shard × ~3 cache-missing lines/order ≈ 300-600 ns/order ≈ 30-60% of the per-order budget (**bar (c) violation** — cross-NUMA fetch in the hot path). Reclaims linear scaling.
    - Muscle: in a pinned thread-per-core design, every hot-path allocation must be NUMA-local to the owning shard; allocating on the calling thread is *wrong*. Reapplies at: W74 matching-engine `replication.rs`, W77 aeron term buffer (subscriber-local), W85 marketdata RX queue, W95 mini-db per-shard memtable.

  - **(EXPERT, cross-primitive) When `skewed_workload_isolation` shows shards 1-7 throughput drops ~40% when shard 5 is hot:**
    - Symptom: with 80% volume to one symbol on shard 5, other shards' throughput tanks. Expected isolation; got coupling.
    - Why: the shared cross-shard channel uses a single bounded MPMC queue; when shard 5's mailbox saturates, the producer's enqueue blocks/contends and slows the routing path for ALL shards. The bug only appears when routing is COMPOSED with a shared queue — each passes in isolation.
    - Fix: each shard owns its OWN cross-shard channel (W60). The gateway hashes to `shard_id`, picks `channel[shard_id]`, sends — no shared queue. Shard 5 saturation blocks only NEW orders for shard 5's symbols.
    - Cost avoided: removes head-of-line coupling; isolates a hot symbol's backpressure to its own shard (**bar (c)** back-pressure-as-correctness).
    - Muscle: per-shard isolation requires per-shard channels — a shared queue couples unrelated shards. Reapplies at: W74 per-shard raft log, W77 aeron per-Image flow, W85 marketdata per-subscriber socket.

  - **(EXPERT, cross-primitive) When `cross_shard_cancel_routes_correctly` shows some cancels lost:**
    - Symptom: a cancel for OrderId=4711 arrives at shard 2, but the order lives on shard 5 — cancel silently dropped.
    - Why: `cancel(order_id)` routes via a separate hash (`hash(order_id) % num_shards`), but the order was routed by `hash(symbol)`. The two hashes don't agree. The bug is the COMPOSITION of routing + ID design.
    - Fix: per D5, embed `shard_id` in OrderId's high bits at creation (top 6 bits = shard_id). `cancel(order_id)` extracts `shard_id` directly — no symbol lookup, no hash mismatch.
    - Cost avoided: a lost cancel is a stuck resting order = an exchange incident; correctness card.
    - Muscle: identifier design IS routing design — encode the routing key into the identifier so lookups need no map. Reapplies at: W74 raft per-shard request IDs, W83 ledger AccountId → shard mapping.

  #### 5-year failure mode
  Write in `crates/matching-engine/notes/65_shard_followup.md` after green: when v2.0 ships dynamic resharding (drain + transfer of a symbol mid-trading), the static `symbol_to_shard` map becomes a stale view during the transfer window. **Trigger:** first hot-symbol production can't handle by manual split. **Migration (1 line):** add an epoch field to the routing decision; orders carry their routing epoch; the shard rejects stale-epoch orders and bounces them to the gateway for re-route.
- [ ] Commit + log (~10 min)

**Wednesday — Proposal draft + consensus-bft prevote**

- [ ] `notes/65_evm_proposal.md` — draft a small design proposal for reth evm (scope: one concrete improvement from Monday's reading). Output: proposal draft with problem / proposed change / migration. (~30 min)
- [ ] **Build**: `crates/consensus-bft/src/prevote.rs` — prevote phase. 2f+1 prevotes for a value advance the validator to precommit. (~total 215 min across 3 phases)
  - 🔧 **Why this matters**: prevote is the first vote phase — validators announce their provisional choice. Mirrors tendermint-rs `vote_set.go` (slot-indexed tally) and Tendermint Annex A. Collecting 2f+1 same-value prevotes updates `valid_value` and moves the validator to precommit. **First-attempt impls fail ≥3 Phase-2 tests**: a naive `AHashMap<(h,r,step), Vec<Prevote>>` allocates per round and OOMs under Byzantine spam (DoS); an O(n) scan-per-insert threshold check is O(n²)/round; the split-value precommit logic commits V on 2/4 when it must precommit nil. The debug session is the lesson. Downstream: W65 Sat precommit reuses the same VoteSet shape; W68 v0.5 ship gated on this; W73 BLS variant collapses N prevotes into 1 aggregated signature but keeps the slot-indexing model.

  #### Phase 1 — Problem, prediction, design (~50 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in `crates/consensus-bft/notes/65_prevote_prediction.md`.
    - P1. Name the data structure you'd reach for to store votes per (height, round, step), sight-unseen. Write the exact field declaration line. No commentary.
    - P2. Compute `quorum = n − ⌊(n−1)/3⌋` (equals 2f+1 only on the 3f+1 lattice) for n ∈ {1, 4, 7, 10, 21}. Five numbers; no commentary.
    - P3. Predict the per-insert threshold-check cost (ns) for your first-attempt impl at n=21. One number.
    - P4. Predict the allocation count per round on the vote-ingest path for your first attempt. One number.
    - P5. List every Phase-2 test name you predict your first attempt will fail.

  - *Mental model*: a VoteSet is a bounded, slot-indexed tally — one slot per `ValidatorId` per (h, r, step) — that detects equivocation, maintains an O(1) per-value count, and survives adversarial spam by construction (fixed capacity, eviction on commit). It is NOT a growable log of votes.

  - *Contract*:
    ```rust
    pub struct Prevote { height: u64, round: u32, value_hash: Option<Hash>, validator: ValidatorId, signature: Sig }
    pub enum InsertOutcome { New, Duplicate, Equivocation(EquivocationProof) }
    pub struct VoteSet { /* fixed-capacity slot arena, per-value tally, evidence log */ }
    impl VoteSet {
        pub fn insert(&mut self, v: Prevote) -> InsertOutcome;     // zero-alloc steady state
        pub fn has_quorum_for_value(&self, h: u64, r: u32) -> Option<Option<Hash>>; // Some(Some(V)) | Some(None)=nil | None
        pub fn evict_below(&mut self, retain_floor: u64);          // on commit
    }
    ```

  - *Back-of-envelope*: at n=21 × 3 phases × ~1 round/block × ~1 block/sec, steady-state ingest ≈ 63 votes/sec/replica; under view-change/Byzantine spam ≈ 500 votes/sec. Each `Vec::push` malloc-chain ≈ 50 ns; at ~7 mallocs/round that's ~350 ns/round of allocator on the consensus path. An O(n) scan-per-insert at n=21 = 21 checks × 21 scan ≈ 441 ops/round (~250 ns/check); at n=100 (consortium) it's ~10k ops/round ≈ 30 ms/round — **catastrophic cliff** (O(n²)). Adversarial spam: 500 votes/sec × 200 B/vote × 3600 s ≈ 360 MB/hour of unbounded `Vec` growth → OOM. The fix (slot arena + running tally) makes all three flat.

  - **Requirements** R1..R8 — write in `crates/consensus-bft/notes/65_prevote_design.md`:
    - R1. `Prevote { height, round, value_hash: Option<Hash>, validator, signature }` is the wire type. `value_hash = None` means "prevote nil".
    - R2. **2f+1 same-value trigger**: when 2f+1 prevotes are collected for the SAME value V at round R, set `valid_value = V`, `valid_round = R`. If in `Step::Prevote`, cast `Precommit(V)` and transition to `Step::Precommit`.
    - R3. **2f+1 total without majority**: if 2f+1 prevotes collected for round R but no single value has 2f+1 → cast `Precommit(nil)`.
    - R4. **Equivocation detection**: a second prevote from the same validator at the same (h,r) with a DIFFERENT value_hash → record evidence (do NOT replace the first vote). Slashable in v1.0.
    - R5. **Slot-indexed VoteSet**: per (h, r, step), exactly `n` slots indexed by `ValidatorId`. Insert by slot index, not push. Second vote from same validator → equivocation/duplicate, NOT a second slot.
    - R6. **Bounded memory**: total ≤ `MAX_ROUNDS_IN_FLIGHT × 3 × n × sizeof(Prevote)`, known at compile time. Eviction: on commit of H, drop all entries with `h < H - RETAIN_WINDOW`.
    - R7. **Zero alloc on steady-state vote intake**: VoteSet is a pre-allocated arena; insert never visits the allocator.
    - R8. **Running per-value tally**: incremented on insert; threshold check is O(1) compare, not O(n) scan.

  - **Design walk-through** D1..D8 — answer in the design doc; no peeking at tendermint-rs `vote_set.go` until Phase 3:
    - D1. Sketch `Prevote`. Why `value_hash: Option<Hash>` not `Option<Value>`? (~200 B/vote at hash, ~kB+/vote at value — bandwidth at n=21.)
    - D2. VoteSet shape — pick ONE and justify against R5/R6/R7 with a property table (allocs? bounded? slot-indexed?):
      (a) `AHashMap<(h,r,step), Vec<Prevote>>` (naïve, unbounded),
      (b) `[[[Option<Prevote>; MAX_VALIDATORS]; 3]; MAX_ROUNDS]` (fixed 3D array, slot-indexed),
      (c) `BTreeMap<(h,r,step), [Option<Prevote>; n]>` (sorted height, slot-indexed within).
    - D3. Per-value tally: a `BTreeMap<Hash, u8>` per (h,r) updated incrementally. Why `BTreeMap` not `AHashMap`? (determinism — spec allows traversal in tests.) Or `[u8; 21]` indexed via a tiny lookup — trade-off?
    - D4. Threshold formula: `quorum = n − ⌊(n−1)/3⌋` (equals 2f+1 only on the 3f+1 lattice), `f = floor((n-1)/3)`. Compute for n ∈ {1,4,7,10,21}. When does n = 3f+1 exactly vs n = 3f+2 tighten the threshold?
    - D5. Equivocation handling: when you detect "same validator, same (h,r), different value", what do you store as evidence? (both signed votes — the cryptographic proof.) Where does evidence live (memory, then disk)?
    - D6. Eviction: write the on-commit cleanup in 4 numbered steps. What's `RETAIN_WINDOW` (64 rounds typical, enough for straggler replay)?
    - D7. **Pre-mortem #1 (unbounded Vec) — load-bearing:** write the EXACT 5-line trace where Byzantine validators flood prevotes for distinct synthetic heights, each round's `Vec` grows unboundedly, replica OOMs. Quantify: 500 votes/sec × 200 B × 3600 s = ?
    - D8. **Pre-mortem #2 (threshold-check storm) — load-bearing:** write the EXACT 4-line trace where naïve `votes.iter().filter(|v| v.value==target).count()` is O(n) per check, called per insert → O(n²) per round = 441 ops at n=21, ~10k at n=100. Compute the cost; identify the R8 fix.
    - D9. **Anti-shortcut audit:** (a) store votes → `Vec<Prevote>` per round (unbounded, DoS); (b) count → scan-on-query (O(n²)); (c) dedup → `HashSet<Prevote>` (allocs + still unbounded). One line each on why wrong HERE.
  - 🧮 **Paper drill** — write in `notes/65_prevote_drill.md`:
    - D-QUORUM on N=4 (f=1, quorum=2f+1=3): hand-tally a `VoteSet` for one (h=0, r=0) — validators {0,1,2} prevote V, validator 3 prevotes V'. Mark the per-value running tally as each slot fills; show the 2f+1-same-value quorum forming on V (the prevote→precommit advance, R2), and that any two such quorums on N=4 intersect in ≥1 honest node. Then trace the SPLIT case {V, V', nil, V} where total=4≥3 but no value reaches 3 → quorum-for-nil verdict (R3, the `prevote_2f1_split_value_precommits_nil` test). Finally inject equivocation: validator 1 sends a second prevote V'' → show the evidence record (both signed votes kept, first NOT replaced, no tally bump) — the equivocation-detection arm of the cert/tally (R4, R5).
    - D-STRUCT on the slot-indexed `VoteSet` with 4 concrete slots indexed by `ValidatorId`: hand-execute `insert` for each of new / duplicate / equivocation, redrawing the slot arena + per-value `BTreeMap<Hash,u8>` tally after each; show insert is by-index (no push, no alloc, R7) and the O(1) threshold-compare against `quorum` (R8) — the linearization is the slot write that flips the running count.
  - **Output**: `notes/65_prevote_design.md` with R1–R8, D1–D9, VoteSet shape decision + property table, both pre-mortem traces.

  #### Phase 2 — Write the failing tests (~50 min)

  - [ ] **`prevote_2f1_same_value_advances_to_precommit`** (NEWBIE) — 4-node sim; nodes 0,1,2 prevote V; assert node 0 → Precommit with `valid_value = V`. Exercises R2.
  - [ ] **`byzantine_prevote_nil_3_honest_still_advance`** (NEWBIE) — 4-node, 1 Byzantine prevotes nil, 3 honest prevote V; assert 2f+1=3 satisfied for V; honest precommit V. Exercises R2.
  - [ ] **`slot_indexed_no_duplicate_slot`** (SENIOR) — same validator sends 5 prevotes for the SAME value; assert only 1 slot occupied and the per-value tally is 1, not 5. Exercises R5.
    ```rust
    #[test]
    fn slot_indexed_no_duplicate_slot() {
        let mut vs = VoteSet::new(/*n=*/4);
        let v = prevote(/*validator*/1, /*value*/Some(H_V));
        for _ in 0..5 { vs.insert(v.clone()); }
        assert_eq!(vs.tally_for(0, 0, Some(H_V)), 1, "duplicate votes inflated the tally");
    }
    ```
  - [ ] **`equivocation_detected_and_evidenced`** (SENIOR) — Byzantine node 1 sends Prevote(V) then Prevote(V'); assert second returns `Equivocation`, evidence stored, first vote NOT replaced. Exercises R4.
  - [ ] **`prevote_2f1_split_value_precommits_nil`** (SENIOR) — 4-node; votes {V, V', nil, V}; assert node precommits **nil** (2f+1 total, no single-value majority), NOT V. Exercises R3.
    ```rust
    #[test]
    fn prevote_2f1_split_value_precommits_nil() {
        let mut vs = VoteSet::new(4); // f=1, quorum=3
        vs.insert(prevote(0, Some(H_V)));
        vs.insert(prevote(1, Some(H_VP)));
        vs.insert(prevote(2, None));        // nil
        vs.insert(prevote(3, Some(H_V)));   // V now has 2 (< 3), total = 4 ≥ 3
        assert_eq!(vs.has_quorum_for_value(0, 0), Some(None), "must precommit nil, not V (V has only 2/4)");
    }
    ```
  - [ ] **`vote_set_zero_alloc_on_insert`** (SENIOR, anti-shortcut, **bar (c)**) — heaptrack-tagged: 10k steady-state inserts; assert 0 allocations. Exercises R7.
  - [ ] **`vote_set_bounded_under_spam`** (EXPERT, **bar (c)**) — Byzantine floods 1 M votes for distinct (h,r); assert VoteSet RSS does not grow unbounded; eviction trims to `MAX_ROUNDS_IN_FLIGHT × 3 × n × sizeof(Prevote)`. Exercises R6.
  - [ ] **`eviction_on_commit_drops_old_entries`** (SENIOR) — fill to capacity; commit H; assert all entries `h < H - RETAIN_WINDOW` gone. Exercises R6.
  - [ ] **`bench_threshold_check_o1`** (EXPERT, **bar (c)**) — criterion: insert 21 prevotes, check 2f+1 after each; assert per-check ≤ 10 ns (O(1) running tally). Exercises R8.
  - [ ] **`bench_threshold_check_physically_plausible`** (SENIOR, **negative-control / test-the-test**) — proves `bench_threshold_check_o1` measures the real check, not a no-op: assert the measured per-check time lands in `0.5..=5000` ns. An optimized-away check would report ~0 ns (< 0.5) and a real O(n²) regression would report > 5000 ns; either fails the control, so a green `_o1` bench is trustworthy.
    ```rust
    #[test]
    fn bench_threshold_check_physically_plausible() {
        let ns = measure_threshold_check_ns(21);
        assert!((0.5..=5000.0).contains(&ns),
            "threshold-check bench {ns} ns implausible — optimized away (<0.5) or O(n^2) blowup (>5000)");
    }
    ```

  - 🪤 **Trap shapes covered** (the three mandated shapes, mapped to the concrete tests above):
    - *Cross-primitive trap*: deferred to Saturday's precommit Build, which composes with THIS VoteSet shape — `lock_persists_into_next_round` exercises precommit's lock (R4) ∘ this prevote handler's lock-priority logic; the two phases share the slot-indexed `VoteSet` and only the composition reveals a missing lock check. (Prevote in isolation has no second primitive to compose with; the slot-tally + threshold split-value logic IS the unit under test here.)
    - *Anti-shortcut trap* (bar (c)): `vote_set_zero_alloc_on_insert` + `vote_set_bounded_under_spam` (the obvious `AHashMap<key, Vec<Prevote>>` reach is correct but allocs per round and grows unbounded under Byzantine spam — a DoS the slot-arena closes).
    - *Negative-control test-the-test*: `bench_threshold_check_physically_plausible` (an optimized-away O(1) check reports <0.5 ns and fails low; a residual O(n²) scan reports >5000 ns and fails high — so a green `bench_threshold_check_o1` is trustworthy).

  #### Phase 3 — Make the tests pass, one failure at a time (~115 min)

  - **(NEWBIE) When `prevote_2f1_split_value_precommits_nil` shows the node precommitting V instead of nil:**
    - Symptom: votes {V, V', nil, V} → node precommits V, but V has only 2/4 (< quorum 3).
    - Why: your threshold check returns "has quorum for V" as soon as TOTAL count reaches quorum, conflating total-quorum with same-value-quorum. V has 2 votes; the check is wrong.
    - Fix: two independent checks, same-value FIRST: `if let Some((v, c)) = per_value.iter().find(|(_, c)| **c >= quorum) { precommit(v) } else if total_at(h, r) >= quorum { precommit(nil) }`.
    - Cost avoided: a safety-class correctness card — precommitting a sub-quorum value can split agreement.
    - Muscle: BFT threshold logic has TWO conditions per phase — same-value-quorum and total-quorum — they trigger different precommit values; same-value precedes total. Reapplies at: W65 precommit (mirror), W68 ship test.

  - **(SENIOR) When `slot_indexed_no_duplicate_slot` shows the per-value tally at 5 after 5 identical inserts:**
    - Symptom: same validator votes V five times; per-value count for V = 5 (should be 1). The 2f+1 threshold trips on one validator's re-deliveries.
    - Why: the insert path doesn't check "is the slot already filled with the same content?" — every insert increments the tally.
    - Fix: on insert, if `slot.is_some() && slot.value_hash == incoming.value_hash` → return `Duplicate` early WITHOUT incrementing. Distinct from equivocation (different content).
    - Cost avoided: prevents a single validator's network re-delivery from forging a quorum — safety-class.
    - Muscle: vote handling has THREE cases per (validator, h, r): new (insert+tally), duplicate (ignore), equivocation (evidence, no tally). Name them explicitly. Reapplies at: W65 precommit, W68 evidence.

  - **(SENIOR) When `equivocation_detected_and_evidenced` shows the first vote silently replaced by the second:**
    - Symptom: Byzantine sends Prevote(V) then Prevote(V'); test expected two votes in evidence, got just the second — the first was overwritten in the slot.
    - Why: the slot-indexed VoteSet overwrites on any duplicate insert from the same validator; the second vote took the slot.
    - Fix: before writing the slot, check `if slot.is_some() && slot.value_hash != incoming.value_hash` → push BOTH votes onto an `evidence: Vec<EquivocationProof>` (off the hot path), do NOT overwrite. The first vote stays the vote-of-record.
    - Cost avoided: destroying the second conflicting vote destroys the slashing proof — protocol-integrity card.
    - Muscle: equivocation evidence MUST preserve both conflicting votes — overwriting one destroys the proof. Reapplies at: W65 precommit equivocation, W68 lock.rs evidence collection, W74 raft (during leader change).

  - **(EXPERT, **bar (c)**) When `vote_set_zero_alloc_on_insert` reports ~7 allocs/round and `vote_set_bounded_under_spam` OOMs at ~5 minutes of flood:**
    - Symptom: heap profile shows `Vec<Prevote>` allocations dominating; RSS climbs 100 MB → 2 GB during spam; eventually OOM. ~7 mallocs/round on vote ingest.
    - Why: naïve `votes: AHashMap<(Height, Round, Step), Vec<Prevote>>` allocates a fresh `Vec` per round, pushes votes, and never frees until view-change. Byzantine spam turns this into a DoS vector.
    - Fix: pre-allocate `VoteSet` as a per-(h,r,step) **fixed-capacity slot array** indexed by `ValidatorId`. Insert by index, not push. Eviction: on commit of H, drop all (h,_,_) with `h < H - RETAIN_WINDOW`. Total bound `MAX_ROUNDS_IN_FLIGHT × 3 × n × sizeof(Prevote)` — compile-time known.
    - Cost avoided: ~7 mallocs/round (~350 ns) eliminated; closes the unbounded-growth DoS (**bar (c) violation** — unbounded memory under adversary).
    - Muscle: per-round protocol state is bounded by `n × max_rounds_in_flight` and slot-indexed by `ValidatorId`; never an unbounded `Vec` on the consensus hot path. Reapplies at: W65 precommit VoteSet (same shape), W68 lock.rs POL detection, W74 VSR PrepareOk BitSet, W83+ ledger replication.

  - **(EXPERT, **bar (c)**) When `bench_threshold_check_o1` reports ~250 ns/check at n=21:**
    - Symptom: each check ~250 ns; at n=21 with 21 inserts/round each triggering a check, ~5 µs/round of scanning. At n=100, ~30 ms/round — catastrophic.
    - Why: `has_2f1_prevotes_for_value` re-scans the entire vote set per insertion (O(n)/check, O(n²)/round).
    - Fix: maintain a running `per_value_count: BTreeMap<Hash, u8>` per (h,r), updated incrementally on insert; threshold check is an O(1) compare against `quorum`. Bounded: ≤ n+1 distinct values per (h,r).
    - Cost avoided: O(n) → O(1)/insert; saves ~250 µs/round at n=21, ~30 ms/round at n=100 (**bar (c) violation** — O(n²) on the consensus path).
    - Muscle: aggregate counts maintained incrementally beat scan-on-query whenever the query is hotter than the aggregation. Reapplies at: W65 precommit VoteSet, W67 raft match-index aggregation, W73 fork_choice descendant count, W74 VSR PrepareOk `count_ones`.

  #### 5-year failure mode
  Write in `crates/consensus-bft/notes/65_prevote_followup.md` after green: when v1.0 ships BLS aggregate signatures (W73), N prevotes for the same value collapse to 1 BLS-aggregated signature + a validator bitmask. **Trigger:** W73 BLS dependency lands. **Migration (1 line):** each (h,r,value) slot holds `AggregatedVote { sig: BlsSig, bitmask: BitSet<n> }` instead of N `Prevote`s; tallies become `bitmask.count_ones()`.
- [ ] Commit notes (~10 min)

**Thursday — Submit proposal + [v3] `log-distributed` ISR (off-hot-path)**

- [ ] `notes/65_evm_proposal.md` (continued) — post Wednesday's proposal as a GitHub discussion; record the URL in the file. Output: posted discussion URL + the framing paragraph used. (~30 min)
- [ ] **[v3] Build**: `crates/log-distributed/src/isr.rs` — the in-sync-replica (ISR) set + high-watermark over the W63 partitioned log: each `Partition` gains a `ReplicaSet { leader, followers, isr: ReplicaBitSet }`, a follower is **in-sync** iff its fetched offset is within `replica_lag_max` of the leader log-end-offset (LEO), the **high-watermark (HW)** = the minimum LEO across the *current* ISR, a record is **committed** (externally readable) iff `offset < HW`, and on leader change the new leader **truncates** uncommitted-above-HW records (KIP-101). ISR membership transitions are driven by `consensus-raft` (W62) as the **off-path controller** — never by the hot path. (~total 165 min across 3 phases)
  - 🔧 **Why this matters**: ISR + high-watermark is how Kafka makes a *replicated* log durable without paying synchronous-all-replica latency — it is the exact "commit = quorum-acked, reads only see committed" pattern, but built **off the hot path** as a learning artifact. It is a deliberate contrast to the venue's VSR log: VSR is the *one hot-path* replicated log (order/event/settlement, README.md §3/§4/§9); `log-distributed`'s ISR is the *off-path* mirror that later serves the read-side projection's bounded-staleness contract (W84). The ISR HW is built by **composing inherited primitives** — W63 `partition`/`segment` for the log, `consensus-raft` (W62) for membership decisions, `time` (W6) for the lag clock — **no primitive twice** (§3). The 2-4 traps a first attempt hits: (1) computing HW as the min LEO across **all** replicas (including lagging/dead ones) instead of across the **current ISR** — a single slow follower freezes the HW and commits stall forever; (2) exposing an above-HW (uncommitted) record to a reader — a dirty read that a leader-change truncation can then *un-write* (the Kafka "lost/duplicated message" class); (3) shrinking the ISR from inside the append/fetch hot loop instead of via the `consensus-raft` controller off-path — couples membership churn to throughput and (worse) makes ISR changes non-deterministic; (4) on leader change, the new leader keeps records above the old HW instead of truncating them (KIP-101) — two replicas diverge. Mirror: Kafka `Partition.scala` `maybeIncrementLeaderHW` + `ReplicaManager` ISR shrink/expand + KIP-101 truncation. Downstream: W66 consumer-group offset commit reads against this HW; W84 v0.5 the projection's bounded-staleness (≤500 ms/≤1000 ops) is measured *relative to the HW*; W113 perp-dex-core read-side analytics plane fans out from a projection gated by an op-token whose freshness floor IS the HW. **Bar (b)** — off-hot-path, correctness + recovery + determinism, **no Cost-avoided cards**. **First-attempt impls WILL fail ≥1 test below — that debug session is the lesson.**

  #### Phase 1 — Problem, prediction, design (~45 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in `crates/log-distributed/notes/65_isr_prediction.md`. Compare to reality after tests run.
    - P1. Write the exact expression you'd type from memory to compute the high-watermark, sight-unseen. Predict whether you reach for `min over all replicas` or `min over the current ISR` — and which one the naive impl picks.
    - P2. Predict what happens to the HW (and to `commit`) when ONE of three followers stalls indefinitely, under your first-attempt impl. One line.
    - P3. Predict where you'd put the ISR-shrink decision: inside `append`/`fetch` (hot loop) or in a `consensus-raft`-driven controller (off-path)? State which the naive impl reaches for and the determinism consequence.
    - P4. Predict, on a leader change where the old leader had records above HW, whether your first attempt keeps or truncates them — and the divergence consequence of keeping them.
    - P5. List every Phase-2 test name you predict your first attempt will fail. Reveal happens when you run the tests; the delta is the lesson.

  - *Mental model*: the ISR is the set of replicas "caught up enough" to count toward durability; the high-watermark is the **committed frontier** — the highest offset known durable on every ISR member. Readers see *only* `offset < HW`; everything above HW is provisional and may be truncated on leader change. Membership is a *control-plane* decision (a `consensus-raft` round, off-path), never a hot-loop side-effect. This is the off-path twin of VSR's commit point — and stating that contrast out loud is half the point of the crate.

  - *Contract* (signature-level — NEVER the body):
    ```rust
    pub struct ReplicaSet { leader: ReplicaId, isr: ReplicaBitSet, leo: [u64; MAX_REPLICAS] } // leo = log-end-offset per replica
    pub struct HighWatermark(u64); // committed frontier: offset < HW is externally readable
    impl ReplicaSet {
        pub fn on_fetch(&mut self, follower: ReplicaId, fetched_offset: u64);       // updates that follower's LEO
        pub fn high_watermark(&self) -> HighWatermark;        // = min LEO over the CURRENT isr — never over all replicas
        pub fn is_committed(&self, offset: u64) -> bool;      // offset < high_watermark().0
        // membership is decided OFF-path by the controller, applied here:
        pub fn apply_isr_change(&mut self, change: IsrChange); // shrink/expand, sourced from a consensus-raft decision
        pub fn on_leader_change(&mut self, new_leader: ReplicaId) -> TruncateTo; // KIP-101: returns the HW to truncate to
    }
    ```

  - *Back-of-envelope*: off the hot path, so the budget is **correctness + recovery**, not latency. ISR membership changes are rare (a follower stall/recovery, seconds-to-minutes apart) → routing them through a `consensus-raft` round (~ms) is fine. HW recompute is O(|ISR|) ≤ MAX_REPLICAS (a handful) per fetch — trivial. The cost that actually bites is **correctness**: a dirty read above HW that a later truncation un-writes is a data-loss bug, not a latency bug. Contrast deliberately with the VSR hot path (sub-µs, §4A) so the "off-path" boundary is felt, not just stated.

  - **Requirements** R1..R7 — write in `crates/log-distributed/notes/65_isr_design.md`:
    - R1. **HW = min LEO over the *current* ISR**, never over all replicas. A replica not in the ISR does not gate the HW.
    - R2. **Committed-read invariant**: `is_committed(offset)` ⇔ `offset < HW`; `read_from` (W63) used by external/consumer paths must **never** return an above-HW record. (Internal replication fetch may read above HW — that path is replica-to-replica, not external.)
    - R3. **ISR shrink/expand is off-path**: membership transitions are decided by a `consensus-raft` (W62) round and applied via `apply_isr_change`; **never** computed inside `append`/`on_fetch`. A stalled follower → controller shrinks the ISR → HW can advance again.
    - R4. **A stalled follower must NOT freeze commits**: when a follower's lag exceeds `replica_lag_max`, the controller removes it from the ISR (R3) so the HW resumes advancing across the remaining ISR.
    - R5. **Leader-change truncation (KIP-101)**: on `on_leader_change`, the new leader truncates its log to the last committed offset (the HW it can prove); records above the old HW are discarded — no replica retains an uncommitted record past a leader change.
    - R6. **Determinism**: given the same sequence of fetches + `consensus-raft` ISR decisions, the HW trajectory and truncation points are byte-identical across runs — no `HashMap` iteration, no wall-clock in the decision path (lag uses the injected `time` clock, W6).
    - R7. **No hot-path coupling / no primitive twice**: the W63 R3 lint holds (`matching-engine`/`ledger-deterministic`/`consensus-vsr` never import `log-distributed`); `consensus-raft` is used ONLY as the off-path ISR controller, never on a fetch/append; HW/log storage forward to W63 `partition`/`segment` and `wal`/`mmap-queue` — no re-implemented log.

  - **Design walk-through** D1..D6 — answer each in the design doc; no peeking at Kafka's `Partition.scala`/`ReplicaManager.scala` until Phase 3:
    - D1. **HW formula table** — fill in (correct? freezes on slow follower? Kafka-faithful?):

      | choice | HW = ? | freezes on 1 slow follower? | verdict |
      |---|---|---|---|
      | min LEO over ALL replicas | min(leo[*]) | yes — one straggler freezes commits | ✗ |
      | min LEO over current ISR | min(leo[r] for r in isr) | no — straggler is shrunk out of ISR | ✓ |
      | max LEO over ISR | max(leo[r] for r in isr) | no, but commits unacked data | ✗ (dirty) |

    - D2. The off-path control loop: write the 5-step flow `follower lag exceeds replica_lag_max` → `controller proposes ISR-shrink via consensus-raft` → `decision committed off-path` → `apply_isr_change` → `HW recomputed over the smaller ISR`. Why must the shrink decision go through `consensus-raft` and not happen inline in `on_fetch`? (determinism + every replica must agree on ISR membership.)
    - D3. The committed-read seam: which W63 API does an external read call, and where does the HW gate sit so an above-HW record is never returned to a consumer (R2)? Confirm the internal replication-fetch path is *separate* and allowed above HW.
    - D4. Leader-change truncation (KIP-101): write the 4 numbered steps from "new leader elected" → "log truncated to provable HW" → "followers re-fetch from the truncation point". Why keeping above-HW records causes divergence.
    - D5. **The VSR contrast (the load-bearing boundary statement):** in ≤5 lines, state how this ISR/HW differs from the venue's VSR commit point — (i) VSR is the *one hot-path* log (order/event/settlement); (ii) ISR/HW here is *off-path* and will later be a *projection* of the VSR log (W84), not a second source of truth; (iii) the VSR op-number is the identity (`SettlementId`, §4A), whereas the HW is only a freshness frontier for the analytics plane's bounded-staleness contract. If you can't write this contrast you can't defend the crate against the §9 "would duplicate consensus" risk.
    - D6. **Pre-mortem (the all-replica HW freeze + dirty-read truncation, load-bearing):** write the EXACT 6-line trace where (a) the HW is computed as `min LEO over all replicas`, follower-2 dies, the HW freezes, and `commit` stalls forever for healthy producers; AND (b) a sibling impl instead exposes an above-HW record to a consumer, follower-1 becomes leader, KIP-101 truncation removes that record, and the consumer has now read a message that **no longer exists** — quantify both as a data-loss/availability incident and name which requirement (R1 / R2 / R4 / R5) closes each.
    - D7. **Anti-shortcut audit:** for each motivation name the "obvious" reach and one line on why it's wrong HERE — (a) HW → `min over all replicas` (one straggler freezes all commits; min over current ISR instead); (b) ISR shrink → inline in `on_fetch` (non-deterministic + couples membership to throughput; route through `consensus-raft` off-path); (c) leader change → keep the longest log (retains uncommitted records → divergence; truncate to provable HW per KIP-101); (d) "publish fills straight into a `Partition` to mirror them" → that silently makes this a SECOND hot-path log (the W63 §9 trap; route through the VSR log and read a projection).
  - 🧮 **Paper drill** — write in `crates/log-distributed/notes/65_isr_drill.md`:
    - D-STRUCT on a 3-replica `ReplicaSet { leader=0, isr={0,1,2}, leo=[u64;3] }`: hand-execute the sequence `on_fetch(0,100); on_fetch(1,98); on_fetch(2,5)` then `apply_isr_change(Shrink(2))`, redrawing the `leo[]` array + `isr` bitset + recomputed `HW = min(leo[r] for r in isr)` after each step; show the HW frozen at 5 under min-over-ALL vs advancing to 98 after the stalled follower is shrunk out (R1, R4). Then hand-execute `on_leader_change`: leader has 0..100, HW=90, follower-1 (LEO=92) becomes leader → draw the `TruncateTo(90)` cut and the discarded 90..100 records on every replica (R5, KIP-101); mark the committed-read gate `offset < HW` so a reader never sees 95 (R2).
    - D-COST on the HW recompute path (off-path, bar (b) — footprint not latency): per-op ledger for `high_watermark()` as a min over a fixed `[u64; MAX_REPLICAS]`/`ReplicaBitSet` — count operations (O(|ISR|) compares, ~handful), allocs (must be 0, the `hw_recompute_does_not_allocate` negative control), and contrast with a `Vec`-per-recompute reach that reports thousands of allocs; state the budget is correctness + a fixed footprint, NOT sub-µs, and write the one-line VSR hot-path contrast (the cost that bites is a dirty read above HW, not latency).
  - **Output**: `crates/log-distributed/notes/65_isr_design.md` with R1–R7, D1–D7, the HW-formula table filled, the off-path control-loop flow, the KIP-101 truncation steps, the VSR-contrast paragraph, and the HW-freeze/dirty-read pre-mortem trace.

  #### Phase 2 — Write the failing tests (~45 min)

  Each test in `crates/log-distributed/tests/isr.rs`. Engineered so a naive first attempt FAILS at least one. Tier-tagged. Off-path → correctness/recovery/determinism, no latency cards.

  - [ ] **`hw_is_min_leo_over_isr`** (NEWBIE) — 3 replicas all in ISR with LEOs {10, 8, 12}; assert `high_watermark() == 8` (the min over the ISR). Exercises R1.
    ```rust
    #[test]
    fn hw_is_min_leo_over_isr() {
        let mut rs = ReplicaSet::new(/*leader*/0, /*replicas*/3);
        rs.on_fetch(0, 10); rs.on_fetch(1, 8); rs.on_fetch(2, 12);
        assert_eq!(rs.high_watermark().0, 8, "HW must be min LEO over the current ISR");
    }
    ```
  - [ ] **`committed_read_never_returns_above_hw`** (NEWBIE) — HW=8, attempt an external `read_from(9)`; assert it is NOT returned (out-of-committed-range), while `read_from(7)` succeeds. Exercises R2.
  - [ ] **`stalled_follower_does_not_freeze_hw`** (SENIOR, **the headline trap**) — 3 replicas, follower-2 stalls (LEO stuck at 5 while leader advances to 100); the controller shrinks the ISR to {0,1}; assert the HW advances past 5 (to min LEO over {0,1}), i.e. commits do NOT freeze on the dead follower. A naive `min over all replicas` impl freezes the HW at 5 and FAILS. Exercises R1 + R4.
    ```rust
    #[test]
    fn stalled_follower_does_not_freeze_hw() {
        let mut rs = ReplicaSet::new(0, 3);
        rs.on_fetch(0, 100); rs.on_fetch(1, 98); rs.on_fetch(2, 5); // follower-2 stalled
        // off-path controller decision (consensus-raft) shrinks ISR to {0,1}:
        rs.apply_isr_change(IsrChange::Shrink(/*remove*/2));
        assert!(rs.high_watermark().0 >= 98, "stalled follower froze the HW — naive min-over-all-replicas trap");
    }
    ```
  - [ ] **`isr_shrink_is_off_path_via_controller`** (SENIOR, **anti-shortcut**) — AST/grep lint over `isr.rs`: assert `on_fetch`/`append` do NOT mutate ISR membership directly; the only writer of ISR membership is `apply_isr_change` (fed by `consensus-raft`). Catches the inline-shrink trap (P3/R3). Exercises R3.
  - [ ] **`isr_change_uses_consensus_raft_not_hot_path`** (SENIOR, **inheritance gate**) — assert `cargo tree -p log-distributed -i consensus-raft` shows it as a dep used by the ISR controller, AND `cargo tree -p log-distributed -i consensus-vsr` is empty (the hot-path consensus is NOT reachable). Proves the off-path membership source + no-second-hot-path-consensus (R7). Exercises R7.
  - [ ] **`leader_change_truncates_above_hw`** (SENIOR, **anti-shortcut**) — leader has records up to offset 100, HW=90; follower-1 (LEO=92) becomes leader; assert `on_leader_change` returns `TruncateTo(90)` and records 90..100 are discarded on every replica (KIP-101). A naive "keep the longest log" impl retains 90..100 and FAILS. Exercises R5.
  - [ ] **`dirty_read_then_truncate_is_impossible`** (EXPERT, **the second headline trap**) — compose R2 + R5: a consumer reads only via the committed-read API; leader change truncates above-HW records; assert the consumer NEVER observed a record that truncation later removed (no read of a message that ceases to exist). FAILS if the read path exposed above-HW records. Exercises R2 + R5.
  - [ ] **`hw_trajectory_deterministic_across_runs`** (EXPERT) — replay the same fetch sequence + same `consensus-raft` ISR-decision sequence 10×; assert the HW trajectory and truncation points are byte-identical each run. Exercises R6; this is the contract W84's projection bounded-staleness depends on.
  - [ ] **`no_hot_path_import`** (EXPERT, **anti-shortcut**) — the W63 R3 lint, re-asserted on the ISR module: `matching-engine`/`ledger-deterministic`/`consensus-vsr` do not import `log-distributed`. Keeps the ISR off the hot path (R7).
  - [ ] **`hw_recompute_does_not_allocate`** (SENIOR, **negative-control / test-the-test**) — dhat-wrap 10k `on_fetch`+`high_watermark` cycles; assert 0 allocations on the HW recompute (it's a min over a small fixed `[u64; MAX_REPLICAS]`/bitset, no growable collection). Not a latency card — a correctness guard that the HW path didn't accidentally grow an unbounded structure; a `Vec`-per-recompute impl reports thousands of allocs and fails. Exercises R1 + R6.

  - 🪤 **Trap shapes covered** (the three mandated shapes, mapped to the concrete tests above):
    - *Cross-primitive trap*: `dirty_read_then_truncate_is_impossible` composes the committed-read gate (R2) with leader-change truncation (R5) — each passes alone, the composition exposes a read path that returned an above-HW record that truncation then un-writes; and `isr_change_uses_consensus_raft_not_hot_path` composes the W62 `consensus-raft` membership decision with the W63 partition log — the off-path-vs-hot-path boundary only surfaces in the composition.
    - *Anti-shortcut trap* (bar (b)): `stalled_follower_does_not_freeze_hw` + `leader_change_truncates_above_hw` + `isr_shrink_is_off_path_via_controller` (the obvious `min-over-all-replicas` / `keep-longest-log` / `inline-shrink` reaches are each plausible but wrong — they freeze commits, diverge replicas, or make membership non-deterministic).
    - *Negative-control test-the-test*: `hw_recompute_does_not_allocate` (a HW path that grew a `Vec` reports thousands of allocs and fails high — so a green ISR build is trustworthy as a fixed-footprint off-path component).

  #### Phase 3 — Make the tests pass, one failure at a time (~65 min)

  Ordered newbie → expert — the ladder you actually walk. Off-path, bar (b): **no Cost-avoided cards** (this is correctness/recovery, not a latency budget).

  - **(NEWBIE) When `stalled_follower_does_not_freeze_hw` shows the HW frozen at 5:**
    - Symptom: follower-2 stalls at LEO 5; the leader advances to 100; the HW never moves past 5; healthy producers' records never commit.
    - Why: `high_watermark()` computes `min LEO over ALL replicas` — the dead follower's stale LEO pins the frontier. (Kafka's exact "a slow replica must not stall the partition" lesson.)
    - Fix: compute HW as `min LEO over the *current* ISR`; the off-path controller (`apply_isr_change`) shrinks the stalled follower out of the ISR so the min is taken over the live set.
    - Muscle: a replicated commit frontier is the min over the *in-sync* set, never over all replicas — a straggler is evicted, not waited on. Reapplies at: W66 consumer-group committed-offset, W84 projection HW, W113 read-side freshness floor.

  - **(SENIOR) When `isr_shrink_is_off_path_via_controller` flags ISR mutation inside `on_fetch`:**
    - Symptom: the lint finds `self.isr.remove(..)`/`self.isr.insert(..)` inside the fetch/append path; ISR membership changes as a side-effect of throughput.
    - Why: it felt natural to shrink the ISR the moment a fetch shows a lagging follower — but that couples membership to the hot loop and makes ISR transitions non-deterministic (order-dependent on fetch interleaving).
    - Fix: `on_fetch` only updates LEO + lag; the *decision* to shrink/expand is proposed to and committed by `consensus-raft` (off-path), then applied via `apply_isr_change`. Membership has exactly one writer.
    - Muscle: control-plane decisions (membership) belong off the data path, behind a consensus round — never as a hot-loop side-effect. Reapplies at: W74 raft per-shard membership, W84 projection consumer-group rebalance, W113 cluster reconfiguration.

  - **(SENIOR) When `leader_change_truncates_above_hw` keeps records 90..100 instead of truncating:**
    - Symptom: follower-1 (LEO 92) becomes leader; it retains its records 90..92 (and a prior leader's 90..100 lingers on replica-0); replicas now disagree on what's at offset 91.
    - Why: the new leader trusted its own log length instead of truncating to the *provable* HW; uncommitted (above-HW) records were never durable across the ISR and must not survive a leader change (KIP-101).
    - Fix: `on_leader_change` returns `TruncateTo(HW)`; the new leader truncates to the last committed offset and followers re-fetch from there. Nothing above the old HW survives.
    - Muscle: on leadership change, the committed frontier is the only durable truth — truncate everything above it; a longer log is not a more-correct log. Reapplies at: W74 raft log truncation on leader change, W84 projection re-anchoring after failover.

  - **(EXPERT, cross-primitive) When `dirty_read_then_truncate_is_impossible` shows a consumer that read a now-truncated record:**
    - Symptom: a consumer observed offset 95 (above HW 90); a leader change truncated 90..100; offset 95 no longer exists, but the consumer already acted on it. This is the COMPOSITION of the read gate (R2) and truncation (R5).
    - Why: the external/consumer `read_from` path didn't gate on the HW — it returned an above-HW (provisional) record that truncation later un-wrote. Each of R2 and R5 looked fine alone; the bug is their interaction.
    - Fix: the committed-read API checks `is_committed(offset)` (offset < HW) before returning anything to a consumer; only the internal replica-to-replica fetch may read above HW. Re-run after truncation; the consumer's max-observed offset stays ≤ the (monotonic) HW.
    - Muscle: external readers see only committed (below-HW) records; provisional records belong to the replication path alone — a read that truncation can invalidate is a data-loss bug. Reapplies at: W66 consumer-group reads, W84 analytics-plane bounded-staleness (reads gated by the op-token/HW), W113 read-side fan-out.

  - **(EXPERT) When `hw_trajectory_deterministic_across_runs` produces two different HW trajectories:**
    - Symptom: identical fetch + ISR-decision sequences yield byte-different HW/truncation traces across runs.
    - Why: a `HashMap` over replicas (or a wall-clock read for lag) leaked into the HW/ISR decision path; per-process hash-seed randomization / real-time jitter reorders or perturbs the decision.
    - Fix: index replicas by a fixed `ReplicaId` over `[u64; MAX_REPLICAS]` / a `ReplicaBitSet` (no HashMap iteration); lag uses the injected `time` clock (W6), never `Instant::now()`. Same determinism rule as W63 recovery.
    - Muscle: any state a projection/VOPR will replay must be deterministic — no HashMap iteration, no wall-clock in the decision path. Reapplies at: W84 projection replay, W88 VOPR, W113 cluster-VOPR StateChecker.

  - **(EXPERT, anti-shortcut) When `isr_change_uses_consensus_raft_not_hot_path` shows `consensus-vsr` reachable from `log-distributed`:**
    - Symptom: `cargo tree -p log-distributed -i consensus-vsr` is non-empty — the off-path ISR crate can reach the hot-path consensus.
    - Why: a convenience import wired ISR membership through the venue's VSR machinery "since it's already there" — silently creating a path from the off-path log to the hot-path consensus (the §9 "would duplicate consensus" risk).
    - Fix: ISR membership uses `consensus-raft` (W62) ONLY; remove any `consensus-vsr` edge. Re-run the W63 R3 hot-path-leak lint. The off-path/hot-path boundary is a dependency-graph invariant, not a comment.
    - Muscle: the off-path log must be unreachable from the hot-path consensus and vice-versa — enforce the boundary in the dependency graph, not just in prose. Reapplies at: W66 consumer-group, W84 v0.5, the W97/W113 capstone inheritance audits.

  #### 5-year failure mode
  Write in `crates/log-distributed/notes/65_isr_followup.md` after green: when W84 wires the **VSR-log projection**, the ISR high-watermark stops being a *primary* commit frontier and becomes the *projection's* freshness frontier — the analytics-plane bounded-staleness contract (≤500 ms/≤1000 ops, README.md §4A) is measured as `VSR commit point − projection HW`. If the projection's ISR shrinks while the VSR log keeps committing, the projection HW lags and the bounded-staleness metric blows out (but the **control/risk plane never reads here** — it reads read-after-commit from VSR applied state, so no risk decision is affected). **Trigger:** first VSR-log projection lands (W84). **Migration (1 line):** thread the VSR op-number alongside the HW so the op-token (zookie) the analytics plane gates on is `min(projection HW, requested op-number)` — keeping the off-path projection's staleness *observable* without ever letting it serve a control-plane read.
- [ ] Commit notes (~10 min)

**Friday — Engage discussion + Tempo discussions reconnaissance**

- [ ] `notes/65_evm_proposal.md` (continued) — respond to feedback on the Reth proposal; capture each reviewer point + your reply. Output: ≥1 substantive reply recorded with the reviewer's concern. (~30 min)
- [ ] [Tempo] `notes/tempo_discussions.md` — scan the Tempo discussions tab; read one substantive thread fully (reconnaissance, not engagement). Output: file naming who's driving the design conversation + the open questions. (~30 min)
- [ ] Commit notes (~10 min)

**Saturday — Reth PR + consensus-bft precommit**

- [ ] `notes/65_reth_pr.md` — pick up one storage-or-evm good-first-issue / small PR; record the issue link + your plan. Output: file with issue URL + a 3-step fix plan (PR itself may land later in the week). (~30 min)
- [ ] **Build**: `crates/consensus-bft/src/precommit.rs` — precommit phase. 2f+1 precommits commits the block. (~total 220 min across 3 phases)
  - 🔧 **Why this matters**: precommit is THE binding vote — 2f+1 precommits for value V = COMMIT V at this height. Mirrors tendermint-rs precommit + lock logic (Annex A). This is where user-observed finality happens; the latency of this phase IS your block-time SLO. **First-attempt impls fail ≥3 Phase-2 tests**: committing on 2f+1 ANY instead of 2f+1 SAME breaks agreement; forgetting `locked_value` on precommit lets a later round commit V' → two commits; per-vote fsync burns the 300 ms WAN budget on disk-wait; O(n²) tally on the commit path. The debug session is the lesson. Downstream: W68 BFT v0.5 ship gated on this; W73 v1.0 adds HotStuff linearity but preserves the commit predicate; W74 matching-engine raft commit certificate has the same shape; W83+ ledger VSR PrepareOk batching reuses this exact disk-discipline pattern.

  #### Phase 1 — Problem, prediction, design (~55 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in `crates/consensus-bft/notes/65_precommit_prediction.md`.
    - P1. Name the durability call you'd reach for to make a precommit "safe", and WHERE you'd put it (per-vote? per-decision?). One line. No commentary.
    - P2. Predict the number of fsync calls for 10 commits at n=21 with your first-attempt impl. One number.
    - P3. Predict commit p99 latency (ms) on a 4-node LAN sim with your first attempt. One number.
    - P4. State, in one line, the two predicates a correct commit needs (you'll check this against the safety bug).
    - P5. List every Phase-2 test name you predict your first attempt will fail.

  - *Mental model*: precommit collects 2f+1 SAME-value votes into a durable commit certificate. The certificate — not the individual votes — is the artifact that fsyncs (once, on the decision). Casting a non-nil precommit ATOMICALLY locks `(value, round)`; the lock spans into prevote of later rounds. Safety = commit predicate AND lock invariant — both, always.

  - *Contract*:
    ```rust
    pub struct Precommit { height: u64, round: u32, value_hash: Option<Hash>, validator: ValidatorId, signature: Sig }
    pub struct CommitCertificate { height: u64, round: u32, value_hash: Hash, signatures: Vec<(ValidatorId, Sig)> }
    impl BftState {
        pub fn handle_precommit(&mut self, p: Precommit) -> Option<CommitCertificate>; // Some => COMMIT, fsync once
        fn cast_precommit(&mut self, v: Option<Hash>); // sets locked_value/locked_round atomically when v != nil
    }
    ```

  - *Back-of-envelope*: budget ≤ 300 ms WAN block-time at n=21; ≤ 1 ms LAN at n=4. Network lower bound ≈ 250 µs RTT × 3 phases ≈ 750 µs; the rest of the 300 ms is queueing + sig-verify + ONE commit-cert fsync. NVMe fsync = 50-1000 µs. Per-vote fsync at n=21 = ~21 fsyncs/round × 50-1000 µs = **1-20 ms/round** of disk stall — 30-99% of a single block budget burned on disk that should track network. The group-commit fix collapses this to one fsync/decision (~50 µs). O(n²) tally cost identical to the prevote card: ~250 µs/round at n=21, ~30 ms/round at n=100.

  - **Requirements** R1..R8 — write in `crates/consensus-bft/notes/65_precommit_design.md`:
    - R1. `Precommit { height, round, value_hash: Option<Hash>, validator, signature }` is the wire type.
    - R2. **Commit predicate**: 2f+1 precommits for the SAME value V at round R → COMMIT V at height H. Reset round to 0; reset locked state; advance height.
    - R3. **2f+1 mixed → round advance**: 2f+1 total precommits without a single-value majority → set the precommit timer; on timeout advance to round R+1.
    - R4. **Lock-on-precommit invariant**: when a validator casts `Precommit(V)` (not nil), it ATOMICALLY sets `locked_value = V, locked_round = round`. Subsequent rounds prevote `locked_value` unless valid_round release fires.
    - R5. **Equivocation detection**: identical to prevote (W65 Wed) — second precommit from same validator at same (h,r) with different content → evidence.
    - R6. **Single fsync per commit**: the commit certificate (2f+1 signed precommits for V) is fsync'd ONCE to the WAL; individual precommit votes are buffered in memory and NEVER fsync'd individually.
    - R7. **O(1) threshold check**: running per-value tally on each insert; threshold check is one compare, not an O(n) scan.
    - R8. **Latency budget**: ≤ 300 ms WAN at n=21; ≤ 1 ms LAN at n=4. p99 commit latency dominated by network RTT, NOT disk.

  - **Design walk-through** D1..D9 — answer in the design doc; no peeking at tendermint-rs precommit logic until Phase 3:
    - D1. Sketch `Precommit` and `handle_precommit`. Mirrors `Prevote` (W65 Wed) — same (slot-indexed tally, equivocation), different (locking on cast, commit vs precommit-emission on threshold).
    - D2. Commit certificate shape: `CommitCertificate { height, round, value_hash, signatures: Vec<(NodeId, Sig)> }` (2f+1 signed precommits). Where does it live (memory → WAL)? Why is IT the fsync artifact, not the individual votes?
    - D3. Lock-on-precommit: write the 4 numbered steps from "decide to precommit V" → "lock state updated". Why must lock-set and precommit-cast be atomic?
    - D4. fsync placement: write the 6-step flow "receive precommit" → "commit certificate fsync'd". Where is the SINGLE fsync? Cost if you fsync each incoming precommit?
    - D5. WAN budget breakdown at n=21, 300 ms target: ~250 µs RTT × 3 phases = ~750 µs lower bound. Where does the rest go (queueing, batched sig-verify, commit-cert fsync)? What's the slack?
    - D6. Round-advance trigger: after 2f+1 mixed precommits, precommit_timeout fires. Duration? (exponential backoff, start ~500 ms, max ~30 s.)
    - D7. **Pre-mortem #1 (per-vote fsync) — load-bearing:** write the EXACT 5-line trace where each precommit triggers a WAL append + fsync. Compute disk wait at n=21 with NVMe 50-1000 µs/fsync.
    - D8. **Pre-mortem #2 (O(n²) tally) — load-bearing:** write the EXACT 4-line trace where `has_2f1_precommits_for_value` is an O(n) scan per insert, called O(n) times/round → O(n²). Compute at n=21 and n=100.
    - D9. **Pre-mortem #3 (lock-set forgotten) — load-bearing:** write the EXACT 6-line trace where a validator casts Precommit(V) without setting locked_value, then in round R+1 casts Precommit(V'). Two honest validators commit different values. Where does R4 close the hole?
    - D10. **Anti-shortcut audit:** (a) durability → fsync per vote (1-20 ms/round disk stall); (b) count → scan-on-query (O(n²)); (c) "I'll lock later" → cast precommit then set lock (race window = safety hole). One line each.
  - 🧮 **Paper drill** — write in `notes/65_precommit_drill.md`:
    - D-QUORUM on N=4 (f=1, quorum=2f+1=3) across two rounds with a crashing/Byzantine leader: round 1, validators {0,1,2} cast Precommit(V) → hand-tally the running per-value count and show the 2f+1-same-value COMMIT predicate (R2) forming the `CommitCertificate { value_hash:V, signatures:[3 (id,sig)] }`; verify any two precommit quorums on N=4 intersect in ≥1 honest node (the no-double-commit safety). Then trace the Byzantine split: Byzantine sends Precommit(V) to {0,1} and Precommit(V') to {2,3} → show neither value reaches 3, NO commit, and the lock-on-precommit invariant (R4: locked_value=V,locked_round=1) carrying into round 2's prevote so the new leader can't get an honest validator to commit V' — the `byzantine_precommit_split_no_double_commit` safety verdict. State the high-QC each replica holds at the crash point.
    - D-STRUCT on the slot-indexed precommit `VoteSet` (same 4-slot shape as Wed's prevote) feeding the `CommitCertificate`: hand-execute `handle_precommit` for new / duplicate / equivocation, redrawing the slot arena + per-value tally; show the linearization point is the slot write that trips the 2f+1 threshold and emits the cert (the single fsync artifact, R6), and the O(1) compare (R7) replacing the O(n) scan.
    - D-COST on the commit path (bar (c)): per-op ledger (ns/cycles/fsyncs) for the per-vote-fsync reach (~21 fsyncs/round × 50-1000 µs = 1-20 ms/round disk stall) vs the group-commit-per-decision fix (1 fsync ≈ 50 µs); add the O(n²) tally cliff (~250 µs/round at n=21, ~30 ms/round at n=100); state whether ~250 µs RTT + 50 µs fsync fits the ≤1 ms LAN / ≤300 ms WAN budget (R6, R7, R8) — the bar (c) "fsync per decision, not per message" verdict.
  - **Output**: `notes/65_precommit_design.md` with R1–R8, D1–D10, the three pre-mortem traces, and a safety-proof outline tying R2 + R4 to the agreement property.

  #### Phase 2 — Write the failing tests (~50 min)

  - [ ] **`all_precommit_v_commits_v`** (NEWBIE) — 4-node, all 4 cast Precommit(V); assert COMMIT V; height advances. Exercises R2.
  - [ ] **`three_precommit_v_one_nil_commits_v`** (NEWBIE) — 3 cast Precommit(V), 1 nil; 2f+1 same-value satisfied; assert COMMIT V. Exercises R2.
  - [ ] **`two_v_two_nil_advances_round`** (SENIOR) — 2 cast Precommit(V), 2 nil; no single-value majority; assert round advances after timer fires (not hang). Exercises R3.
  - [ ] **`precommit_sets_lock_atomically`** (SENIOR) — node casts Precommit(V); assert `locked_value == V` AND `locked_round == round` AFTER broadcast. Exercises R4.
    ```rust
    #[test]
    fn precommit_sets_lock_atomically() {
        let mut s = BftState::new(4);
        s.cast_precommit(Some(H_V));
        assert_eq!(s.locked_value, Some(H_V), "lock not set on precommit");
        assert_eq!(s.locked_round, s.round, "locked_round not pinned to current round");
    }
    ```
  - [ ] **`lock_persists_into_next_round`** (EXPERT, cross-primitive) — composes precommit lock (R4) with W65-Wed prevote: node casts Precommit(V) at round 1; round advances to 2; node receives Proposal(X); assert node prevotes locked V, not X. Exercises R4 + prevote integration.
  - [ ] **`commit_cert_fsync_once_per_commit`** (SENIOR, anti-shortcut, **bar (c)**) — instrumented: 10 commits; assert exactly 10 WAL fsync calls (one per cert), NOT ~210 (one per individual precommit at n=21). Exercises R6.
    ```rust
    #[test]
    fn commit_cert_fsync_once_per_commit() {
        let (mut s, wal) = harness_n(21);
        run_commits(&mut s, /*commits*/10);
        assert_eq!(wal.fsync_count(), 10, "fsynced per-vote ({}) not per-commit", wal.fsync_count());
    }
    ```
  - [ ] **`byzantine_precommit_split_no_double_commit`** (EXPERT, **bar (c)** safety) — 4-node 1-Byzantine; Byzantine sends Precommit(V) to {0,1} and Precommit(V') to {2,3}; assert AT MOST one honest validator commits. Exercises R2 + R4.
  - [ ] **`safety_10_heights_with_byzantine`** (EXPERT, **deterministic-sim / VOPR-style**, **bar (c)**) — 4-node 1-Byzantine sim, 10 heights random Byzantine behavior, seeded RNG; assert no safety violation across all replicas. Exercises R2 + R4.
  - [ ] **`bench_threshold_check_constant_time`** (EXPERT, **bar (c)**) — criterion: insert 21 precommits, check after each; assert ≤ 10 ns/check. Exercises R7.
  - [ ] **`commit_latency_lan_under_1ms`** (EXPERT, **bar (c)**) — 4-node LAN sim; bench commit latency; assert p99 ≤ 1 ms. Exercises R8.
  - [ ] **`fsync_count_negative_control`** (SENIOR, **negative-control / test-the-test**) — proves the fsync instrumentation actually counts: run a path KNOWN to fsync twice, assert the harness reports exactly 2. If the counter is wired wrong it reports 0 or 1, so `commit_cert_fsync_once_per_commit` is trustworthy only when this control passes.
    ```rust
    #[test]
    fn fsync_count_negative_control() {
        let wal = InstrumentedWal::new();
        wal.append_and_fsync(b"a"); wal.append_and_fsync(b"b");
        assert_eq!(wal.fsync_count(), 2, "fsync counter miswired — count assertions below are meaningless");
    }
    ```

  - 🪤 **Trap shapes covered** (the three mandated shapes, mapped to the concrete tests above):
    - *Cross-primitive trap*: `lock_persists_into_next_round` composes precommit's lock-on-cast (R4) with the W65-Wed prevote handler — a validator that locks V at round 1 must prevote locked-V at round 2, not the new proposal X. Each phase passes its own unit tests; only the round-spanning composition exposes a missing lock check. `byzantine_precommit_split_no_double_commit` likewise composes the commit predicate (R2) with the lock invariant (R4) — either alone is insufficient for safety.
    - *Anti-shortcut trap* (bar (c)): `commit_cert_fsync_once_per_commit` + `commit_latency_lan_under_1ms` (the obvious "fsync each precommit for durability" reach is correct but burns 1-20 ms/round of disk stall on a path that should track network RTT — the group-commit-per-decision fix collapses it).
    - *Negative-control test-the-test*: `fsync_count_negative_control` (proves the fsync counter actually increments on a path KNOWN to fsync twice — a miswired counter reporting 0/1 would make `commit_cert_fsync_once_per_commit` meaningless). The bar (c) deterministic-sim trap is `safety_10_heights_with_byzantine` (VOPR-style, seeded RNG, 10 heights of random Byzantine behavior).

  #### Phase 3 — Make the tests pass, one failure at a time (~115 min)

  - **(NEWBIE) When `two_v_two_nil_advances_round` hangs forever — no round advance:**
    - Symptom: 2 nodes precommit V, 2 precommit nil; total = 4 ≥ quorum 3; no value has majority. Expected: timer fires, round advances. Actual: hang.
    - Why: you didn't set the precommit timer when 2f+1 total precommits arrive without a single-value majority; the state machine sits in `Step::Precommit`.
    - Fix: after the same-value-majority check fails, `if self.votes.count_precommits(h, r) >= quorum && self.step == Step::Precommit { self.set_timer(Step::Propose, self.precommit_timeout) }`. On expiry, advance round.
    - Cost avoided: liveness-class — without the timer the validator stalls forever (block production halts).
    - Muscle: BFT round-advance requires explicit timers — no commit AND 2f+1 total mixed = timer-driven retry. Reapplies at: W68 lock.rs round transition, W73 view change.

  - **(SENIOR) When `bench_threshold_check_constant_time` reports ~250 ns/check at n=21 (and ~30 ms/round at n=100):**
    - Symptom: each check O(n); at n=21, ~5 µs/round of scanning; at n=100, ~30 ms/round.
    - Why: `has_2f1_precommits_for_value` re-tallies the full vote set per insertion → O(n²)/round.
    - Fix: running `per_value_count: BTreeMap<Hash, u8>` per (h,r), updated incrementally; threshold check is O(1) compare. Mirror of the W65-Wed prevote fix.
    - Cost avoided: O(n) → O(1)/insert; saves ~250 µs/round at n=21, ~30 ms/round at n=100 (**bar (c) violation** — O(n²) on the commit path).
    - Muscle: aggregate counts maintained incrementally beat scan-on-query when the query is hotter than the aggregation. Reapplies at: W65 prevote VoteSet, W67 raft match-index aggregation, W73 fork_choice descendant count, W74 VSR PrepareOk `count_ones`.

  - **(SENIOR, **bar (c)**) When `commit_cert_fsync_once_per_commit` reports ~210 fsyncs for 10 commits and `commit_latency_lan_under_1ms` reports ~20 ms p99:**
    - Symptom: at n=21, ~21 fsyncs/round × 10 commits = ~210. `iostat -x 1` shows the WAL device pinned at ~21 IOPS during commit bursts; p99 tracks fsync time, not network RTT.
    - Why: naïve impl appends each incoming Precommit to the WAL and fsyncs to "ensure durability before increasing the tally". At n=21 × 50-1000 µs/fsync = 1-20 ms/round of pure fsync stall.
    - Fix: **group-commit on precommit votes** — buffer in memory, fsync ONCE when the 2f+1 threshold trips (forming the commit certificate). The certificate IS the durable record; individual votes need not survive a crash (replicas re-vote after recovery via view-change). Wire into the W26 `GroupCommit` primitive. Equivocation evidence (rare) uses a separate group-commit lane.
    - Cost avoided: ~(2n-1) fsyncs/round × 50-1000 µs ≈ 1-20 ms/round; commit latency drops to ~250 µs RTT + ~50 µs fsync ≈ 300 µs LAN (**bar (c) violation** — fsync per message on the finality path).
    - Muscle: never fsync per-message on a hot path; fsync per *decision* and reconstruct messages via group-commit. Reapplies at: W67 raft AppendEntries commit, W68 lock.rs (lock-state change fsync once), W74 matching-engine raft, W83+ ledger VSR PrepareOk batching, W92 ledger VOPR.

  - **(EXPERT) When `lock_persists_into_next_round` shows the validator prevoting X (new proposal) instead of locked V:**
    - Symptom: validator cast Precommit(V) at round 1, then in round 2 received Proposal(X) and prevoted X — ignoring its lock. This is the COMPOSITION of precommit (this Build) and prevote (W65 Wed).
    - Why: lock-on-precommit didn't fire (`locked_value` stayed `None`), OR the round-2 prevote logic (W65 Wed) doesn't check the lock first.
    - Fix: ensure R4 — the precommit-cast site SETS `locked_value` before returning. Confirm the prevote handler (W65 Wed Phase-3 #equivocation card / lock-priority) checks lock first. Both phases must agree on the lock contract.
    - Cost avoided: a missed lock lets a later round prevote a different value → potential double-commit (safety).
    - Muscle: the lock invariant spans prevote AND precommit — both phases must honor it. Reapplies at: every BFT lock-respecting code path; W68 integration, W73 HotStuff lock-on-prepareQC.

  - **(EXPERT, **bar (c)** safety) When `byzantine_precommit_split_no_double_commit` / `safety_10_heights_with_byzantine` show two honest validators committed different values:**
    - Symptom: 4-node 1-Byzantine; Byzantine sends inconsistent precommits to subgroups; honest {0,1} commit V while {2,3} commit V'. Agreement broken.
    - Why: either (a) commit predicate wrong (commit on 2f+1 ANY instead of SAME), OR (b) lock-on-precommit missing — validator 2 cast Precommit(V) at round 1 without locking, then cast Precommit(V') at round 2.
    - Fix: enforce R2 — commit only on `per_value_count[V] >= quorum`, never on `total >= quorum`. Enforce R4 — when casting any non-nil precommit, ATOMICALLY set locked_value/locked_round in the same block BEFORE broadcasting. Reproduce the failing trace; confirm both fixes.
    - Cost avoided: a double-commit is a chain fork — the worst BFT failure (**bar (c)** — deterministic-sim must catch this before ship).
    - Muscle: BFT safety = (commit predicate) AND (lock invariant) — both must hold; either alone is insufficient. Reapplies at: W68 BFT safety integration test, W73 HotStuff variant, W74 VSR safety.

  #### 5-year failure mode
  Write in `crates/consensus-bft/notes/65_precommit_followup.md` after green: when v1.0 ships HotStuff (W73), the 3-phase protocol becomes 2-phase pipelined (chained leadership); lock-on-precommit is replaced by lock-on-prepareQC. **Trigger:** HotStuff linearity required for n ≥ 100 (BFT's O(n²) message complexity becomes the bottleneck). **Migration (1 line):** extend `BftState` with a `kind: ProtocolKind` discriminant and fork the lock-logic per protocol.
- [ ] Commit + log (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
