# Week 85 — Three-crate Reth integration (Sepolia) + [HFT] `marketdata-kernelbypass` v0.1 scaffold + `runtime-thread-per-core` v1.0 SHIP

> **Phase**: 5
> **Month**: M22
> **Track**: Half-2 venue (primary — the exchange-facing ingest + L2 substrate) + Half-1 Reth-spine (income-floor + on-chain-settle design) + Tempo (RWA-collateral option, hard-capped)
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: Sepolia sync via own stack (Reth-spine deliverable) + `marketdata-kernelbypass` v0.1 (mirror of OpenOnload/Solarflare userspace stack + Corvil feed handler + DPDK `rte_eth_rx_burst` batch API) + `runtime-thread-per-core` v1.0 SHIP (mirror of Seastar `reactor` + Glommio `LocalExecutor`)
> **Crate(s) touched**: `consensus-engine`, `storage-trie`, `exec-vm` (Sepolia integration, Half-1 spine, bar (b)); **[NEW]** `crates/marketdata-kernelbypass/` v0.1 scaffold (Layer-4 venue ingest, bar (c)); `runtime-thread-per-core` → v1.0.0 SHIP (Layer-1/2 substrate, bar (c))
> **Bar**: (c) — `marketdata-kernelbypass` is the venue's exchange-facing ingest critical path; `runtime-thread-per-core` is the L1/L2 substrate that `matching-engine` (VSR-replicated), `consensus-vsr`, `ledger-deterministic`, `messaging-aeron`, and ultimately `risk-engine`'s per-mark-tick recompute all run on. Both are bar (c): zero-alloc hot path + deterministic-sim + 4000h-by-M36 non-negotiable. (Sepolia integration itself rides the bar-(b) Reth-spine crates.)
> **Feeds into**: epoll.rs → io_uring.rs (W86), AF_XDP.rs (W89), feed_handler.rs (W87), `marketdata-kernelbypass` v0.5 SHIP (W90); runtime v1.0 → `matching-engine` v1.5 on the VSR substrate (W82 bump), `marketdata-kernelbypass` v0.5 (W90), `consensus-vsr` v1.0 (W90), `ledger-deterministic` v0.7 VOPR (W88) / v1.0 (W92), `messaging-aeron` v0.7 (W84), and the §4A v1.0-locked `risk-engine` shard-by-account per-mark-tick recompute (W97–W99) + `perp-dex-core` 3-node VSR cluster (W103+); 1000h burn-in → W106 multi-node paper-trade-venue deploy prerequisite.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): `man recvmmsg`, `man epoll(7)` (EPOLLET semantics + starvation note), `man ip(7)` (IP_RECVERR, IP_ADD_MEMBERSHIP/mreqn), DPDK Programmer's Guide §"RX/TX Burst" (batch API rationale only), Seastar `reactor.cc` + Glommio `executor.rs`/`reactor.rs` (thread-per-core poll loop), Aeron `AgentRunner` (burn-in agent design), reth `NodeBuilder::launch` (boot ordering), RECONCILED_PLAN_v3.md §3 rows 2/12/13, §4A v1.0 capacity back-of-envelope (incremental margin / shard-by-account). Do NOT read cover-to-cover.
> **v3 modifications**: (1) **Frontmatter re-aimed** from the v2 "Tier-A HFT IC job" narrative to the v3 venue half — `marketdata-kernelbypass` is the exchange-facing ingest of `perp-dex-core`, and `runtime-thread-per-core` is the substrate the §4A back-of-envelope *names* as mandatory for the 100k-account / ~1M-orders/s `risk-engine` recompute (shard-by-account → ~62k recomputes/s/shard). (2) **"Feeds into" rewired to v3 destinations**: the replication substrate is `consensus-vsr` (NormalOperation, W74), not `consensus-raft`; downstream consumers are the VSR-replicated `matching-engine`, `ledger-deterministic` on the same VSR log, and the capstone — not the demoted `mini-db`. (3) **Thu blog re-pitched** at venue/founding-eng reviewers (the thread-per-core tail-latency story underwrites both the crypto-MM cash-seat bridge and the bet). (4) **Sat: the runtime det-harness shard-pinning seam is explicitly connected to the §4A "risk reads from local applied state on the leader shard, no consensus round-trip" lock** — `Shard::id` is the account-sharding key the capstone reuses. The *primitive work is north-star-invariant* ("no primitive twice"); only the narration, the inheritance/handoff targets, and one new shard-by-account design note change.

**[NEW] crate created**: `crates/marketdata-kernelbypass/`. Layer-4 venue-ingest primitive (the exchange-facing feed handler of `perp-dex-core`). v0.5 target W90.

**The v3 framing this week locks in (read before Monday):** two substrate primitives ship, and both are load-bearing for the §4A capstone lock, not generic HFT scaffolding. (i) `runtime-thread-per-core` v1.0 is the L2 substrate whose `Shard::id` becomes the **account-sharding key** for `risk-engine` — the §4A back-of-envelope says full portfolio recompute (100k accounts × 10 ticks/s = 1M recomputes/s) only fits with shard-per-core (16 shards → ~62k/s/shard ≈ 62ms/s/shard), and the **incremental-margin hot path is mandatory at this scale**, served from **local applied state on the leader shard with no consensus round-trip**. The cross-shard `submit_to` mailbox and the deterministic clock-injection harness you ship today are exactly what make that lock honest. (ii) `marketdata-kernelbypass` is the venue's exchange-facing ingest — the seam from external markets into the `oracle-mark` mark/index inputs and the order intake that rides the VSR log. Every design choice today is in service of those two seams.

> **Load note**: this week stacks two bar-(c) Build arcs (scaffold Mon, epoll.rs Fri) on top of THE Phase-5 Reth-spine deliverable (Sepolia sync — the real, open-ended debugging risk) AND a major SHIP ritual (runtime v1.0 Thu). The marketdata Builds are placed on the days Sepolia is least likely to block (Mon scaffold before sync starts; Fri epoll while waiting on sync fixes). Tue/Wed carry the boot-sequence + engine-flow design notes; Saturday is the loom/bench verification pass so no single day exceeds ~5.5h. Tempo is a hard-capped 4h stretch goal: if Sepolia is red, Tempo gets ZERO time.

## Monday — Integration architecture + marketdata scaffold

- [ ] **Design note**: `notes/85_toy_client_arch.md` — sketch the toy execution client wiring all 3 Reth-spine crates
  (`storage-trie` for state, `exec-vm` for execution, `consensus-engine` for fork-choice/engine-API). Name the
  data flow: EngineAPI `newPayload` → exec-vm `execute_block` → storage-trie `commit` → consensus-engine
  `fork_choice_updated`. Output: file exists with a labelled ASCII flow + the 3 trait boundaries
  (`StateProvider`, `BlockExecutor`, `ForkChoice`) the client depends on. (~30 min)
  - 🔧 **Why this matters (v3)**: this is the on-chain-settle side of the hybrid CLOB (§4A) — the same trait
    boundaries the capstone's settle-stub sits behind. Reading it now keeps the Half-1 spine warm (income-floor
    + L2 optionality) without putting it on the venue hot path.

- [ ] **Build**: `crates/marketdata-kernelbypass/Cargo.toml` — new workspace member (Layer-4 venue-ingest primitive,
  mirror of OpenOnload pluggable-transport layout). Deps: `time` (W6, monotonic clock for drop-rate windows),
  `backpressure` (W11, queue-capacity semaphore), `runtime-thread-per-core` (W85 Thu — pin NIC RX queues to shards
  via `Shard::id`). Optional features: `epoll` (default), `io-uring`, `af-xdp`.
  Output: `cargo metadata` lists the crate; `cargo check -p marketdata-kernelbypass` (default features, macOS)
  green; deps resolve to the workspace-pinned versions. (~20 min)
  - Crate-scaffold item (§6): no 3-phase arc. It holds the `Transport` trait + three transport impls
    (`epoll`/`io_uring`/`af_xdp`) + ITCH/FIX `parse.rs` + `feed_handler.rs` glue. Consumed by W86–W90 and, at the
    capstone, becomes the exchange-facing ingest feeding `oracle-mark` inputs + VSR-log order intake.

- [ ] **Build**: `crates/marketdata-kernelbypass/src/lib.rs` — the `Transport` trait + module headers for
  `epoll.rs`, `io_uring.rs`, `af_xdp.rs`, `feed_handler.rs`, `parse.rs`. (~total 3.5 hrs across 3 phases)
  - 🔧 **Why this matters**: this trait is the **L4 ingestion-path contract** — the OSS equivalent of
    Solarflare/Onload's userspace stack and DPDK's `rte_eth_rx_burst` batch API. Three transports are scaffolded
    NOW (epoll baseline, io_uring batching, AF_XDP zero-copy) so W86–W90 fill them in incrementally against ONE
    trait, giving apples-to-apples benchmarks; without all three baselined together the W89 Friday transport
    ranking is impossible. The two traps that bite here are **shape traps in the trait itself**: (1) `recv`
    (single-frame) vs `recv_batch` — a single-frame trait makes 1 syscall/packet and caps you at ~3M pps forever;
    (2) `type Frame = Vec<u8>` vs a borrowing GAT `Frame<'a>` — an owned-Vec frame allocates per packet. Both are
    bar-(c) violations baked into the *API surface*, so they can't be "optimized later" without rewriting every
    callsite. Downstream consumers: W85 Fri `epoll.rs`, W86 Tue `io_uring.rs`, W86 Sat `parse.rs`, W87 Tue
    `feed_handler.rs`, W89 Tue `af_xdp.rs`, W89 Fri bench, v0.5 SHIP W90 Tue, and the capstone venue-ingest
    (W103+). First-attempt trait designs WILL fail `transport_trait_uses_recv_batch_not_recv` and/or the
    Frame-lifetime compile test below — that debug session is the lesson.

  #### Phase 1 — Problem, prediction, design (~60 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE Phase 2)** — seal answers in
    `crates/marketdata-kernelbypass/notes/85_scaffold_prediction.md`; compare to reality after tests run:
    - P1. The trait method shape you'd reach for first, sight-unseen — write the exact signature line you'd type
      from memory (be honest: is it `fn recv(&mut self) -> Frame`?).
    - P2. Predict the syscall count per 256 received packets for your P1 design, and the resulting max pps on a
      300 ns/syscall budget.
    - P3. Predict what `type Frame` you'd alias — `Vec<u8>`? `Box<[u8]>`? a borrow? Predict allocs/packet.
    - P4. Predict whether `cargo check --all-features` compiles on macOS (no AF_XDP) for your first feature layout.
    - P5. **List every Phase-2 test you predict your first attempt will FAIL.** (Expect: the recv_batch trap and
      the Frame-lifetime trap at minimum.)
  - *Mental model*: a `Transport` is a **batched, zero-copy packet source**. It owns its receive buffers and hands
    the consumer a *borrowed* view of each frame valid only until the next batch. It is the producer; the feed
    handler is the consumer. Same producer-owns/consumer-borrows shape as a ring buffer — and the same shape the
    VSR-log projection fan-out (W84) uses to hand committed events to off-path readers.
  - *Contract* (signature-level only — NEVER the body):
    ```rust
    pub trait Transport {
        type Frame<'a>: AsRef<[u8]> where Self: 'a;          // GAT: borrows from self
        /// Drain up to `out.capacity()` frames; returns count. Frames valid until next call.
        fn recv_batch<'a>(&'a mut self, out: &mut ArrayVec<Self::Frame<'a>, 256>) -> usize;
        fn drops(&self) -> u64;                              // kernel-side drop counter
    }
    ```
  - *Back-of-envelope* (bar (c), Jeff-Dean numbers): target 100M pps peak (multicast burst), 50M pps sustained.
    - Single-frame `recv`: 1 syscall/packet × ~300 ns = **30 sec of syscall per wall-second at 100M pps —
      physically impossible**. Batch of 256 amortizes the syscall to ~1.2 ns/packet → 256× win. Cliff: 3M pps
      (syscall-bound) vs 100M+ pps (batched) ≈ **30× ceiling lift just from the trait shape**.
    - `Vec<u8>` frame: ~50 ns malloc + 1 cache-line allocator metadata per packet. At 50M pps =
      **2.5 sec malloc per wall-second** + allocator-lock contention. GAT borrow = 0 ns, 0 allocs.
  - **Requirements** R1..R7 — write in `crates/marketdata-kernelbypass/notes/85_scaffold_design.md`:
    - R1. All three transports satisfy the SAME `Transport` trait → apples-to-apples benchmarks.
    - R2. Feature flags (`epoll` default, `io-uring`, `af-xdp`) gate kernel-version-dependent code.
    - R3. No transport allocs on the hot path; UMEM/io_uring buffers pre-registered at startup.
    - R4. `Frame` borrows from the transport (GAT) — zero-copy; valid only until next `recv_batch`.
    - R5. `recv_batch` (NOT `recv`) — batched is the only viable API at 100M pps.
    - R6. macOS dev loop + Linux production both compile.
    - R7. v0.5 SHIP target W90 Tue: P99 ≤ 2 µs end-to-end.
  - **Design walk-through** D1..D6 — answer in the design doc; no peeking at OpenOnload/DPDK source until Phase 3:
    - D1. Sketch `Transport` trait + `EpollTransport`/`IoUringTransport`/`AfXdpTransport` stubs.
    - D2. **Primitive property table** — fill it in: for each candidate frame type (`Vec<u8>`, `Box<[u8]>`,
      `&'a [u8]` field-borrow, GAT `Frame<'a>`): allocs/packet? Sync/Send? can caller retain across batches?
      zero-copy? Pick the row with 0 allocs AND that encodes the retain-ban in the type.
    - D3. **Dx Pre-mortem (recv-per-packet trait shape, load-bearing):** write the EXACT bad trace —
      `recv() -> recvmmsg(1)` → `syscall` → `return 1 frame` → repeat 100M× → CPU pinned in
      `__GI___libc_recvmmsg`. If you can't write this 4-line trace, you can't prove `recv_batch` fixes it.
    - D4. **Dy Pre-mortem (Vec<u8> Frame allocates):** trace — `recv_batch` → `let f = packet.to_vec()` →
      `jemalloc::malloc(1500)` per packet → return owned Vec → caller drops → `free`. 50M malloc+free/sec.
    - D5. **Pre-mortem (feature combinatorial explosion):** `--all-features` pulls AF_XDP syscalls that don't
      exist on macOS; `cargo check --all-features` fails. Gate AF_XDP behind
      `#[cfg(all(target_os = "linux", target_arch = "x86_64"))]`.
    - D6. **Anti-shortcut audit (load-bearing):** the "obvious" reach for each motivation + one line why it's
      wrong HERE: (a) `recv() -> Vec<u8>` (obvious, idiomatic) — wrong: 1 syscall + 1 malloc per packet;
      (b) `Box<dyn Transport>` for the three impls — wrong: vtable dispatch per `recv_batch` defeats
      monomorphization; use generic `<T: Transport>` at the call sites; (c) `async fn recv` — wrong: per-poll
      `Box::pin` of the returned future on the hot path; keep it sync + batched.
  - **Output**: `notes/85_scaffold_design.md` with R1–R7, D1–D6, both bad-interleaving traces.

  #### Phase 2 — Write the failing tests (~30 min)
  Each test is engineered so a naive first trait will FAIL it. Tier-annotated. Real assertions.

  - **(SENIOR) `transport_trait_uses_recv_batch_not_recv`** — *anti-shortcut trap*: the obvious single-frame
    `recv` compiles and is correct, but fails the BAR (syscall-per-packet). Asserts the public trait has NO
    single-frame `recv` and DOES have `recv_batch`. A naive `fn recv(&mut self) -> Frame` fails this.
    ```rust
    #[test]
    fn transport_trait_uses_recv_batch_not_recv() {
        fn assert_batch_api<T: Transport>(t: &mut T) {
            let mut out: ArrayVec<T::Frame<'_>, 256> = ArrayVec::new();
            let n = t.recv_batch(&mut out);     // must exist, must take an out-buffer
            assert!(n <= out.capacity());
        }
        let src = include_str!("../src/lib.rs");
        assert!(!src.contains("fn recv(&mut self)"),
            "single-frame recv() leaked into public API — that is 1 syscall/packet, a bar-(c) violation");
        let _ = assert_batch_api::<EpollTransport>;
    }
    ```
  - **(EXPERT) `transport_frame_lifetime_bound_to_self`** — *anti-shortcut trap*: GAT compile-check. A naive
    `type Frame = Vec<u8>` (owned) compiles; a `type Frame<'a> = &'a [u8]` that the caller tries to retain across
    a second `recv_batch` must NOT compile. `trybuild` negative — the EXACT borrow error is asserted.
    ```rust
    // tests/ui/frame_retain_across_batch.rs — must FAIL to compile with E0502.
    fn retain(t: &mut EpollTransport) {
        let mut a: ArrayVec<_, 256> = ArrayVec::new();
        t.recv_batch(&mut a);
        let held = a[0].as_ref();          // borrow of t's buffer
        let mut b: ArrayVec<_, 256> = ArrayVec::new();
        t.recv_batch(&mut b);              // second &mut t while `held` is live
        let _ = held[0];                   // EXPECT: E0502 cannot borrow `*t` as mutable...
    }
    ```
  - **(SENIOR) `compile_default_macos`** — `cargo check` (default features) on macOS is green. Exercises R2,R6.
  - **(SENIOR) `compile_all_features_linux`** — `cargo check --all-features` on Linux is green; AND
    `cargo check --all-features` on macOS is green BECAUSE af-xdp code is `cfg`-gated, not feature-gated alone.
    Exercises R2, D5.
  - **(NEWBIE) `transport_stubs_return_empty_batches`** — calling `recv_batch` on a fresh stub returns 0 and does
    not panic (proves the stub is wired, ready for W86 to fill).
  - **(NEWBIE) `stub_impls_satisfy_trait`** — all three impls satisfy `Transport` (generic bound check compiles).
  - **(NEWBIE — negative-control "test-the-test") `recv_batch_respects_out_capacity`** — proves the batch test
    isn't vacuous: feed the stub a fake source of 1000 frames, assert `recv_batch` into a cap-256 ArrayVec returns
    exactly `min(available, 256)` and NEVER overruns. A stub that always returns 0 would pass
    `transport_stubs_return_empty_batches` by accident — this forces the cap arithmetic to be real once W86 fills
    it (marked `#[ignore]` until epoll lands; the assertion is written now).
    ```rust
    #[test] #[ignore] // un-ignored in W85 Fri once epoll.rs lands
    fn recv_batch_respects_out_capacity() {
        let mut t = EpollTransport::loopback_with_pending(1000);
        let mut out: ArrayVec<_, 256> = ArrayVec::new();
        let n = t.recv_batch(&mut out);
        assert_eq!(n, 256, "must fill the batch, not return 1 (single-frame regression) or 1000 (overrun)");
        assert_eq!(out.len(), 256);
    }
    ```

  #### Phase 3 — Make the tests pass, one failure at a time (~90 min)
  Ordered newbie→expert.

  - **(NEWBIE) When `stub_impls_satisfy_trait` reports `the trait bound EpollTransport: Transport is not satisfied`:**
    - Symptom: generic bound check fails to compile; the three stubs declare methods but don't `impl Transport`.
    - Why: stub structs exist but the `impl Transport for X` blocks (even empty/`todo!()`) aren't written.
    - Fix: add `impl Transport for EpollTransport { type Frame<'a> = &'a [u8]; fn recv_batch(...) -> usize { 0 } fn drops(&self) -> u64 { 0 } }` for each (returning 0 until W86).
    - Cost avoided: none yet (scaffold) — but locks the trait so W86–W89 don't each re-shape it.
    - Muscle: scaffold the trait + empty impls together so the contract is frozen before any impl optimizes.
      Reapplies at: W86 io_uring impl, W89 AF_XDP impl, W84 VSR-log-projection fan-out reader scaffold.

  - **(SENIOR) When `transport_trait_uses_recv_batch_not_recv` reports `single-frame recv() leaked`:**
    - Symptom: grep guard fires; the natural design was `fn recv(&mut self) -> Frame`. Once W86 fills it, profile
      shows `__GI___libc_recvmmsg` at 95% CPU and throughput plateaus at ~3M pps.
    - Why: 100M pps × ~300 ns syscall = 30 sec/sec — physically impossible. The shape, not the impl, is wrong.
    - Fix: `recv_batch<'a>(&'a mut self, out: &mut ArrayVec<Frame<'a>, 256>) -> usize`. All three transports batch
      naturally (epoll→`recvmmsg`, io_uring→multi-shot, AF_XDP→RX-ring drain). Single-frame recv is NOT public.
    - Cost avoided: ~300 ns syscall × 256 amortized to ~1.2 ns/packet — **256× syscall amortization at the trait
      level** (bar (c) violation if missed: caps the whole crate at ~3M pps, well below venue burst needs).
    - Muscle: in any kernel-bypass/batched-I/O API, batch belongs in the TRAIT, not a "later optimization" —
      adding it after the fact rewrites every callsite. Reapplies at: W86 io_uring multi-shot recv, W89 AF_XDP
      RX-ring drain, W92 io_uring journal group-commit, W84 VSR-log projection batch-read.

  - **(EXPERT) When `transport_frame_lifetime_bound_to_self` reports the trybuild case COMPILES (or `Vec<u8>`):**
    - Symptom: either `type Frame = Vec<u8>` (owned — `jemalloc::malloc` dominates once filled), OR the
      retain-across-batch trybuild case unexpectedly compiles (no lifetime tie → use-after-overwrite).
    - Why: owned `Vec<u8>` allocates per packet; a non-GAT or wrongly-bounded Frame lets the caller hold a frame
      while the next `recv_batch` overwrites the underlying buffer (silent data corruption).
    - Fix: `type Frame<'a>: AsRef<[u8]> where Self: 'a`; the `&'a mut self` on `recv_batch` makes the returned
      frames borrow `self`, so a second `recv_batch` while a frame is live is `E0502`. Encodes "consume before
      next batch" in the type.
    - Cost avoided: ~50 ns malloc + 1 cache-line metadata per packet (2.5 sec/sec at 50M pps) AND a class of
      silent use-after-overwrite corruption — **bar-(c) violation** (Vec<u8> on a hot-path API surface always is).
    - Muscle: in any zero-copy data-plane API, buffer owned by PRODUCER, borrowed by CONSUMER, retain-ban encoded
      in the lifetime. Reapplies at: W86 io_uring `ProvidedBuf<'a>`, W89 `UmemFrame<'a>`, W87 `FeedParser::Event<'a>`
      GAT, W84 VSR-log `CommittedEvent<'a>` projection borrow, W92 ledger Archive event borrows.

  - **(SENIOR) When `compile_all_features_linux` reports macOS `--all-features` fails on AF_XDP syscalls:**
    - Symptom: `cargo check --all-features` red on macOS; `error: cannot find function xsk_socket__create`.
    - Why: feature flag `af-xdp` alone isn't enough — the code is still compiled on macOS where the syscall
      doesn't exist.
    - Fix: gate the AF_XDP module body with `#[cfg(all(target_os = "linux", target_arch = "x86_64"))]` in ADDITION
      to the `af-xdp` feature; provide a `#[cfg(not(...))]` stub that `compile_error!`s only if `recv_batch` is
      actually called, not at compile time.
    - Cost avoided: dev-loop friction (macOS iteration dies) — keeps R6 (both platforms compile) true.
    - Muscle: feature flags gate *intent*, `cfg(target_os)` gates *availability* — you need both for
      kernel-version-dependent code. Reapplies at: W86 io_uring (Linux 5.6+), W89 AF_XDP, W92 io_uring fdatasync.

  #### 5-year failure mode
  Write in `notes/85_scaffold_followup.md` after green: TRIGGER — DPDK becomes the dominant kernel-bypass
  mechanism (poll-mode driver beats AF_XDP on tail latency by >20% in the W139 re-bench). Migration: add
  `DpdkTransport` as a 4th impl behind the same `Transport` trait — the trait shape (batch + borrowed frame)
  already matches `rte_eth_rx_burst`, so no trait change, only a new module + feature flag.

- [ ] **SEED-day ADR** — record the structural direction in `notes/adr_marketdata_kernelbypass.md`.
  Output: file exists with ADR-1..4, each with a Why + a Cost line. (~20 min)
  1. **ADR-1: Three transports behind one trait.** Why: apples-to-apples bench in W89; matches OpenOnload's
     pluggable transport. Cost: monomorphize at call sites (no `dyn`) to avoid vtable per `recv_batch`.
  2. **ADR-2: Feature flags + `cfg(target_os)` gate kernel-version code.** Why: macOS dev + Linux prod compile.
     Cost: every Linux-only module needs a macOS stub.
  3. **ADR-3: Frame lifetime borrows from transport.** Why: zero-copy. Cost: caller must `.to_vec()` to retain
     across batches; documented in trait doc.
  4. **ADR-4: ITCH/FIX parsers separated from transport.** Why: parser tested with synthetic bytes, transport
     with synthetic UDP; composition glues them in `feed_handler.rs`. (v3: this is the seam that becomes the
     venue's exchange-facing ingest into `oracle-mark` inputs + VSR-log order intake.)
- [ ] Commit + log (~10 min)

## Tuesday — Boot sequence (toy execution client)

- [ ] **Build/integration**: `crates/consensus-engine/src/node/startup.rs` (or the client bin) — implement node
  startup wiring the 3 Reth-spine crates per Monday's `notes/85_toy_client_arch.md`: open `storage-trie` DB,
  construct `exec-vm` executor with the loaded chain spec, initialise `consensus-engine` fork-choice from the
  persisted head. (~120 min)
  - 🔧 **Why this matters**: this is the first time the three bar-(b) Reth-spine crates run as ONE process; the
    trap is initialisation ORDER — fork-choice must read the persisted head from storage-trie BEFORE exec-vm is
    asked to execute, or the executor runs against an empty/genesis state and every block fails state-root
    validation. Mirror: reth `NodeBuilder::launch` ordering. Feeds: Wed engine-flow, Thu Sepolia sync. (v3: this
    is the boot path of the capstone's on-chain-settle side — the settle-stub behind §4A's `SettlementId` sits at
    exactly this engine-API boundary.)
  - 🎯 **Hardened acceptance**: `cargo run --bin toy-client -- --datadir /tmp/sepolia` boots and logs
    `head=<block#> state_root=<hash>` matching the persisted head (NOT genesis `0x000…`); a deliberate swap of
    init order (exec-vm before storage-trie head load) reproduces a `state root mismatch` log — capture that
    failure once in `notes/85_boot_order.md` so the ordering invariant is earned, not assumed.
  - *Mental model*: startup is a topological wiring of three trait objects; the only hard edge is "load head
    before executing".
- [ ] Commit + log (~10 min)

## Wednesday — Engine API → execution → storage flow

- [ ] **Integration**: drive one block end-to-end through the toy client — `engine_newPayloadV3` →
  `exec-vm.execute_block` → `storage-trie.commit` → `engine_forkchoiceUpdatedV3`. File:
  `crates/consensus-engine/tests/engine_flow_e2e.rs`. (~120 min)
  - 🔧 **Why this matters**: the engine-API handshake is where most home-grown clients silently diverge — the trap
    is returning `VALID` from `newPayload` before `commit` has durably persisted state, so a crash between the two
    loses the block but the CL thinks it's stored. Mirror: reth `EngineApi::new_payload`. Feeds: Thu Sepolia sync
    correctness. (v3: this `VALID`-only-after-`commit` invariant is the on-chain analogue of §4A's
    `finality_status` rule — `Final` is derived from the commit point, never declared early.)
  - 🎯 **Hardened acceptance**: the e2e test asserts (a) `newPayload` returns `VALID` ONLY after `commit` returns
    Ok; (b) the post-commit `state_root` equals the payload's declared `stateRoot`; (c) `forkchoiceUpdated`
    advancing to the new head is reflected by `storage-trie` head read in a fresh process (proves durability, not
    just in-memory). `cargo test -p consensus-engine engine_flow_e2e` green.
- [ ] **Design note**: `notes/85_engine_flow.md` — the 4-step flow + the one ordering invariant (persist before
  VALID). Output: file exists with the step list + invariant. (~30 min)
- [ ] Commit + log (~10 min)

## Thursday — Sync from testnet + [HFT] `runtime-thread-per-core` v1.0 SHIP

- [ ] **Attempt Sepolia sync** using own stack (PRIMARY Half-1-spine GOAL — THE Phase-5 spine deliverable).
  File/observation: `notes/85_sepolia_sync.md` logs blocks-synced + first divergence (if any). Output: either
  "synced to head N" or a captured first-failing-block trace with the failing stage named (consensus / execution
  / storage). (~30 min today; bulk of debugging Fri/Sat)

- [ ] **SHIP: `runtime-thread-per-core` v1.0.0** — Inheritance: `concurrent`, `time`, `backpressure`, `epoch-gc`.
  **[bar (c)]** (this is the L2 substrate the entire venue half sits on — see the §4A shard-by-account note)
  - 🔧 **Why this matters (v3)**: this is the foundation L1/L2 runtime crate hitting bar (c). The VSR-replicated
    `matching-engine` v1.5 (W82), `marketdata-kernelbypass` v0.5 (W90), `messaging-aeron` v0.7 (W84),
    `ledger-deterministic` v0.5+ (W83), `consensus-vsr` v1.0 (W90) ALL run on top of it — and per §4A the
    `risk-engine`'s per-mark-tick recompute (W97–W99) is **only feasible because of this crate**: the
    back-of-envelope (100k accounts × 10 ticks/s = 1M recomputes/s, ~1µs/account = ~1 CPU-sec/sec single-threaded)
    fits only with **shard-per-core, sharded by account** (16 shards → ~62k recomputes/s/shard ≈ 62ms/s/shard,
    comfortable) — and §4A's "risk reads from local applied state on the **leader shard**, no consensus
    round-trip" is exactly this crate's per-shard locality. v1.0 means: zero-alloc audit done (W83),
    deterministic test harness ready (W84), 1000h burn-in started (today). Bar (c) is non-negotiable: if the
    runtime introduces nondeterminism, every higher-layer VOPR (incl. the W108 cluster-VOPR) is invalidated.
  - *Mental model*: one pinned executor per OS thread; tasks never cross threads (no `Send` tax); all clock reads
    funnel through `time::Clock` so the deterministic harness can inject a fake clock. `Shard::id` is the
    account-sharding key the capstone's `risk-engine` reuses.
  - *Back-of-envelope* (bar (c)): cross-shard `submit_to` rtt p50 < 700 ns (was eventfd ~1 µs at v0.3) — budget
    assumes 1 cache-line transfer (~100 ns MESI) + queue push/pop (~25 ns) + wakeup. spawn must be alloc-free up
    to N bytes (SmallBox) — a per-spawn `Box::new` is ~50 ns + 1 alloc, which at 10M spawns/sec is 0.5 sec/sec
    malloc, a bar-(c) violation.
  - *Contract* (v1.0.0 — locked this week, SemVer commitment in README):
    ```rust
    pub struct LocalExecutor { reactor: Reactor, timer_wheel: HierarchicalWheel,
        task_slab: Slab<Task>, ready_queue: ArrayVec<TaskId, 4096> }
    pub fn spawn_typed<F: Future>(f: F) -> JoinHandle<F::Output>;  // alloc-free up to SmallBox<N>
    pub fn block_on<F: Future>(f: F) -> F::Output;
    pub struct Shard { pub fn id(&self) -> ShardId; pub fn submit_to(&self, dst: ShardId, msg: M); }
    pub mod det { pub use crate::det_harness::*; }                 // clock inject + ordering oracle
    ```
    **Invariants**: (i) one executor per OS thread, pinned; (ii) tasks never cross threads (no `Send`); (iii)
    `spawn` alloc-free for typed futures ≤ N bytes (SmallBox); (iv) all clock reads via `time::Clock` (W6/W23);
    (v) backpressure handles applied at ready-queue capacity 4096; (vi) **`Shard::id` is the stable sharding key**
    downstream (account-sharding for `risk-engine`, NIC-RX-queue pinning for `marketdata-kernelbypass`).
  - **Bar-(c) acceptance gate** (all must pass before tag):
    1. (~30 min) **Zero-alloc audit (methodology)**: `cargo run --bin dhat-runtime --release` runs the full
       integration test (matching-engine 1M cmds + ledger 1M Transfers) under dhat. Expected: **0 allocations** in
       `Reactor::poll`, `Executor::run`, `submit_to::send`, `submit_to::recv`, `timer_wheel::tick`. Allocs OK ONLY
       in: task-spawn (Slab::insert amortized-zero with pre-sized capacity), startup, shutdown.
    2. (~15 min) **Deterministic harness gate**: `cargo test -p runtime-thread-per-core --features det
       det_harness_replay` — 100 deterministic replays of one scenario produce byte-identical execution traces.
    3. (~15 min) Stress: 1M concurrent tasks across 8 shards complete in < 30 s; no panics.
    4. (~10 min) `submit_to` soak: 24h continuous 100k rpc/sec across 4 shards; 0 lost messages, 0 panics,
       RSS stable.
    5. (~10 min) Inheritance ratio audit ≥ 0.50 (L1/L2 substrate), documented.
    6. (~5 min) Tag `git tag runtime-thread-per-core-v1.0.0` + sign; push; update MEMORY.md + INDEX.md.
  - **Perf targets (numeric, falsifiable)** — record in `notes/runtime_v1_0_perf.md`:
    - `submit_to` cross-shard rtt p50 < 700 ns, p99 < 2 µs, p99.9 < 5 µs (improved from v0.3).
    - `spawn_local` throughput ≥ 10M tasks/sec. timer_wheel tick < 50 ns/fired timer.
    - Reactor poll loop ≤ 200 ns idle iteration (epoll edge-trigger). Memory ≤ 16 MB/shard @ 100k tasks.
    - **Per-shard recompute headroom (v3 — feeds §4A)**: a single shard sustains ≥ 100k trivial-task dispatches/sec
      with the budget intact — i.e. comfortably above the ~62k recomputes/s/shard the `risk-engine` will need at
      100k accounts / 16 shards. Record the number; this is the line the W99 `risk-engine` p99.99 report cites.
    - **1000h burn-in target**: 1000h continuous chaos-rig op by M36 (W139); start counter TODAY. Pass: 0
      zero-alloc-audit regressions, 0 panics, 0 hangs.
  - **Pitfalls / failure cards**:
    - **(SENIOR) When a dependency upgrade reintroduces a `Send` bound:** Symptom: build error
      `T cannot be sent between threads safely` after a dep bump. Why: a `!Send` thread-per-core invariant got
      silently relaxed. Fix: explicit `PhantomData<*const ()>` `!Send` markers on internal task types; CI runs
      `-D warnings` and `static_assertions::assert_not_impl_all!(Task: Send)`. Cost avoided: a cross-thread task
      migration would reintroduce data races the whole design exists to forbid (bar-(c) violation). Muscle: lock
      invariants with compile-time assertions, not comments. Reapplies at: W90 consensus-vsr replica pinning,
      W90 marketdata per-shard FeedHandler, W97 risk-engine per-shard account state.
    - **(EXPERT) When the dhat audit shows a hidden alloc in `Reactor::poll`:** Symptom: dhat reports N>0 allocs
      in poll; the war-story hit (a `Vec::push` in event-emit, like the v0.7 incident). Why: an event vector grew
      past its pre-sized capacity. Fix: pre-size the event buffer to the max fd count and `debug_assert!` it never
      reallocs; emit into a fixed `ArrayVec`. Cost avoided: ~50 ns malloc per poll × poll rate = latency-tail
      spikes (bar-(c) violation). Muscle: any hot-loop collection must be pre-sized and assert no-realloc.
      Reapplies at: marketdata epoll `recv_buf` (Fri), mmap-queue v1.0 poll, messaging-aeron poll, risk-engine
      per-tick scratch.
    - **(SENIOR) SemVer break post-tag:** Symptom: after tagging you discover `submit_to` needs a signature
      change. Fix: ship `1.0.0-rc.1` for 1–2 burn-in weeks, lock the API contract this week, then `1.0.0` final.
      Muscle: pre-release tags buy a contract-freeze window. Reapplies at: consensus-vsr v1.0 (W90),
      risk-engine v1.0 (W99), perp-dex-core v1.0 (W113).
    - **(NEWBIE) Inheritance double-count:** Symptom: 0.50 ratio "achieved" by counting test code. Fix: count
      only `src/` LOC (`tokei --files crates/runtime-thread-per-core/src/`), exclude `tests/`/`benches/`. Muscle:
      ratio counts production code only. Reapplies at: every SHIP audit (W90, W92, W99).
  - **Inheritance audit** (Layer 1/2 target ≥ 0.50 — substrate's net-new dominates by design):
    - Lineage with pins: `concurrent v0.5 (W22) + time v0.5 (W31) + backpressure v0.5 (W34) + epoch-gc v0.3 (W47)`.
    - Per-module: `reactor.rs` (NET-NEW + CONSUMES `concurrent::Atomic`), `executor.rs` (NET-NEW),
      `cross_shard.rs` (CONSUMES `concurrent` + eventfd — the `submit_to` mailbox), `timer_wheel.rs` (CONSUMES
      `time`), `det_harness.rs` (NET-NEW — clock injection + ordering oracle). Record in
      `notes/inheritance_audit_runtime_v1_0.md`. MEMORY.md note: "L2 substrate v1.0 has a LOWER ratio than L5 v1.0
      because the substrate IS the net-new; downstream consumers (risk-engine, matching-engine) carry the higher
      ratios."
  - **Downstream handoff** — bump every consumer's Cargo.toml to
    `runtime-thread-per-core = { path = "../runtime-thread-per-core", version = "1.0.0" }`:
    matching-engine v1.5 (W82 — VSR substrate runs on it), marketdata-kernelbypass v0.1 (W85 — pin NIC RX queues
    to shards via `Shard::id`), messaging-aeron v0.7 (W84 — publication per shard), ledger-deterministic v0.5+
    (W83), consensus-vsr v0.5+ (W74 — replica per shard). **v3 forward-handoff**: `risk-engine` v0.5+ (W90/W97)
    shards account state by `Shard::id` and reads margin from local applied state on the leader shard (§4A). Update
    `notes/inheritance_map.md`: runtime v1.0 is the L2 root.
  - **Acceptance**: tag pushed; inheritance ratio ≥ 0.50; burn-in counter started; CI gate enforces alloc audit +
    det-harness equivalence; all downstream Cargo.tomls bumped; `Shard::id` documented as the stable sharding key.

- [ ] **1000h burn-in deploy**: start the chaos-rig host running a continuous matching-engine load on runtime
  v1.0; start a persistent counter. Files: `notes/burn_in_log.md` (monotonic hours + restart timestamps),
  prometheus export of `runtime_poll_total`, `runtime_alloc_audit_failures`, `runtime_clock_skew`. Output: counter
  at hour 0 logged; metrics scrapeable. Pass criteria documented: 1000h continuous, 0 alloc-audit regressions, 0
  crash. (~60 min)
  - 🎯 **Hardened acceptance**: kill the burn-in host and restart it; the counter must NOT reset (persistent file
    + monotonic `*_total`), and the restart timestamp must be recorded — proves the 1000h count survives restarts
    (the trap that makes burn-in counts lie). This burn-in is the W106 multi-node paper-trade-venue prerequisite.

- [ ] **Blog post draft (~90 min)** — "Seastar in Rust — what changed, what didn't, where it bit" in
  `notes/blog_runtime_v1_0.md`. Output: file exists with §1 why thread-per-core in 2026 (tail-latency story —
  and v3: *why a derivatives venue's per-mark-tick risk recompute needs it*), §2 Seastar + Glommio (what each got
  right/wrong), §3 where I diverge (typed `submit_to`; det_harness baked in from v0.3; `Shard::id` as the
  account-sharding key), §4 the dhat methodology war-story (the hidden `Vec::push` in event-emit caught at v0.7),
  §5 bench results (`submit_to` p99 < 2µs across 4 shards; per-shard recompute headroom), §6 downstream consumers
  (matching-engine VSR substrate → risk-engine shard-by-account → perp-dex-core). Audience: **venue / founding-eng
  reviewers AND crypto-MM-desk engineers** — the tail-latency craft underwrites both the bet and the income-floor
  bridge (§7).

- [ ] [Tempo] **Secondary stretch goal** (HARD cap 4h total this week, not just today): attempt Tempo testnet
  sync using your stack with `tempo-tx-envelope` + `tempo-evm-ext` + `tempo-payment-lane` plugged in. Document
  blockers in `notes/tempo_sync_blockers.md`. Do NOT let this eat Sepolia time — if Sepolia isn't working, Tempo
  gets ZERO time. (Tempo is the RWA-collateral-rails option leg, §2/§4A v1.5 — keep it warm, off the critical
  path.) Output: `notes/tempo_sync_blockers.md` lists each blocker + which crate owns it. (~up to 240 min, gated)
- [ ] Commit + log (~10 min)

## Friday — Debug failures + marketdata: epoll baseline

- [ ] **Debug Sepolia sync** — work the first-divergence trace from Thu. File: `notes/85_sepolia_sync.md` updated
  with root cause + fix per failing stage. Output: blocks-synced count increases vs Thu; if a stage is fixed, name
  it. (~30 min, may expand if Sepolia is the bottleneck)

- [ ] **Build**: `crates/marketdata-kernelbypass/src/epoll.rs` — edge-triggered, `recvmmsg`-batched UDP receiver.
  THE baseline + correctness oracle for io_uring/AF_XDP. (~total 3.5 hrs across 3 phases)
  - 🔧 **Why this matters**: epoll is the portable baseline (kqueue shim on macOS for the dev loop) AND the
    known-good correctness oracle the other two transports are diffed against in W89. The bar-(c) traps here are
    all in the hot path: (1) level-triggered `EPOLLIN` instead of edge-triggered `EPOLLET` makes one `epoll_wait`
    round-trip per readable byte (~1 µs each → 10 sec/sec at 10M pps); (2) allocating the receive buffer per
    `recv_batch` (50 ns malloc + 10 µs zero-fill per 64 KB); (3) kernel `ENOBUFS` drops going undetected so you
    think you're loss-free when you're not. Mirror: OpenOnload epoll fast-path + Corvil feed-handler drain loop.
    Downstream: W86 io_uring.rs + W89 AF_XDP.rs are diffed against this; feed_handler default on macOS; and at the
    capstone this is the exchange-facing ingest that must not silently drop ticks feeding `oracle-mark`. First
    attempts WILL fail `epoll_edge_triggered_drain_loop` and `epoll_zero_alloc_on_hot_path`.

  #### Phase 1 — Problem, prediction, design (~45 min, no code yet)

  - 🎯 **Pre-prediction prompt** — seal in `crates/marketdata-kernelbypass/notes/85_epoll_prediction.md`:
    - P1. The `epoll_ctl` flags you'd register from memory (`EPOLLIN`? `| EPOLLET`?).
    - P2. Predict `epoll_wait` calls/sec at 10M pps for YOUR flag choice. (Level-triggered: ~1/byte. Edge: ~1/batch.)
    - P3. Predict where your receive buffer lives (per-call `vec![0u8; ...]`? struct field?) and allocs/sec.
    - P4. Predict whether your design detects a kernel UDP drop under load. (If you didn't set `IP_RECVERR`: no.)
    - P5. **List every Phase-2 test you predict your first attempt will FAIL.**
  - *Mental model*: an `EpollTransport` is a single-socket drain loop: one `epoll_wait`, then `recvmmsg` in a tight
    loop until `EAGAIN`, refilling one pre-allocated buffer the kernel writes into via `msg_iov`.
  - *Contract*: `impl Transport for EpollTransport`; `Frame<'a> = &'a [u8]` slices into `recv_buf`; `recv_batch`
    drains fully; `drops()` reads the `IP_RECVERR` errqueue counter.
  - *Back-of-envelope* (bar (c)): 5M pps loss-free single-thread target.
    - Level vs edge `epoll_wait`: ~1 µs/call. Level-triggered ≈ 1 call/packet → 10 sec/sec at 10M pps
      (impossible). Edge + drain ≈ 1 call/batch-of-256 → ~40 ms/sec. **Cliff: ~250×.**
    - Per-call 64 KB buffer: ~50 ns malloc + ~10 µs zero-fill. At 100k batches/sec ≈ 1 sec/sec just zeroing.
      Pre-allocated field: 0 allocs, cache-warm.
  - **Requirements** R1..R7 — write in `crates/marketdata-kernelbypass/notes/85_epoll_design.md`:
    - R1. Edge-triggered (EPOLLET) — drain socket fully on each notification.
    - R2. `recvmmsg` batches up to BATCH frames per syscall.
    - R3. Buffers pre-allocated at construction; hot-path alloc-free.
    - R4. Frames borrow from receiver — lifetime tied to next `recv_batch` (`Frame<'a> = &'a [u8]`).
    - R5. 5M pps loss-free on a single thread (M2 Pro via kqueue shim / Linux laptop).
    - R6. P99 batch fill ≥ 80% of BATCH (efficient batching).
    - R7. ENOBUFS drop detected via `IP_RECVERR` errqueue; export drop counter (so a dropped venue tick is never
      mistaken for "no tick").
  - **Design walk-through** D1..D6 — answer in the design doc; no peeking at OpenOnload source until Phase 3:
    - D1. Sketch `EpollTransport` struct (fd, epfd, `recv_buf: AlignedBox<[u8; MAX_FRAME*BATCH]>`,
      `msgvec: [mmsghdr; BATCH]`, `iovecs`, `drops: u64`).
    - D2. **Pre-mortem (level-triggered, load-bearing):** trace — `epoll_wait` returns EPOLLIN → `recvmmsg` reads
      BATCH → bytes still pending → `epoll_wait` returns EPOLLIN AGAIN immediately → repeat per byte-group. Pin
      the syscall in `epoll_wait`. If you can't write this, you can't prove EPOLLET fixes it.
    - D3. **Pre-mortem (recv_buf per call):** trace — `let buf = vec![0u8; MAX_FRAME*BATCH]` → malloc → memset
      zero-fill 64 KB → recvmmsg → drop buf → free. Repeat 100k/sec.
    - D4. **Pre-mortem (EPOLLET starvation):** one hot socket monopolises the drain loop, a low-rate socket
      starves. Round-robin across the events vector each iteration.
    - D5. **Pre-mortem (ENOBUFS undetected):** kernel drops UDP under load, app unaware → false "loss-free".
      `setsockopt(IP_RECVERR)` + read errqueue periodically into `drops`.
    - D6. **Anti-shortcut audit:** (a) blocking `recv` in a thread — wrong: no batching, 1 syscall/packet;
      (b) `tokio::net::UdpSocket` async recv — wrong: per-poll future box + no recvmmsg batching; (c) growing `Vec`
      for frames — wrong: realloc on hot path; use the pre-allocated buffer + `&[u8]` slices.
  - **Output**: `notes/85_epoll_design.md` with R1–R7, D1–D6, the level-triggered + per-call-alloc traces.

  #### Phase 2 — Write the failing tests (~30 min)
  Tier-annotated; each traps a common naive impl.

  - **(SENIOR — anti-shortcut + negative-control) `epoll_edge_triggered_drain_loop`** — asserts the registered
    flags include `EPOLLET` AND the recv path is a drain-until-`EAGAIN` loop. Negative-control half: run a
    synthetic 1M-packet burst and assert `epoll_wait` call count is `< packets/64` (proves batching is real, not
    that the test is vacuous). A level-triggered impl fails the call-count band.
    ```rust
    #[test]
    fn epoll_edge_triggered_drain_loop() {
        let mut t = EpollTransport::loopback();
        assert!(t.epoll_flags() & EPOLLET != 0, "must register EPOLLET, not level-triggered");
        let (sent, wait_calls) = t.drain_burst(1_000_000);   // sends 1M, returns epoll_wait count
        assert_eq!(sent, 1_000_000);
        assert!(wait_calls < 1_000_000 / 64,
            "epoll_wait called {wait_calls}× for 1M packets — level-triggered regression (expect ~1/batch)");
        assert!(wait_calls > 0, "zero epoll_wait calls means the test measured nothing");
    }
    ```
  - **(SENIOR — anti-shortcut) `epoll_zero_alloc_on_hot_path`** — heaptrack/dhat over a 1M-packet drain: **0
    allocations** after construction. A per-call `vec![0u8; ...]` fails.
    ```rust
    #[test]
    fn epoll_zero_alloc_on_hot_path() {
        let mut t = EpollTransport::loopback();
        let before = dhat::stats();          // alloc count snapshot
        t.drain_burst(1_000_000);
        let after = dhat::stats();
        assert_eq!(after.total_blocks - before.total_blocks, 0,
            "{} allocs on hot path — recv_buf must be a struct field, not per-call vec!",
            after.total_blocks - before.total_blocks);
    }
    ```
  - **(SENIOR) `epoll_recv_buf_pre_allocated_field`** — source guard: `recv_buf` is a struct field, allocated in
    `new()`, not inside `recv_batch`. (Compile + grep guard, like the trait test.)
  - **(SENIOR) `epoll_recvmmsg_batched_32`** — strace assertion: exactly 1 `recvmmsg` per up-to-BATCH frames (NOT
    BATCH× `recvmsg`). Exercises R2.
  - **(EXPERT — negative-control) `epoll_enobufs_visible_via_metric`** — overflow the kernel rcvbuf deliberately;
    assert `t.drops()` increments by the dropped count (±tolerance). Proves the drop counter is REAL: a stub
    `drops() { 0 }` fails. Exercises R7.
    ```rust
    #[test]
    fn epoll_enobufs_visible_via_metric() {
        let mut t = EpollTransport::loopback_small_rcvbuf(4096);  // tiny buffer forces drops
        let before = t.drops();
        flood(&mut t, 100_000);                                   // far exceeds rcvbuf
        assert!(t.drops() > before, "kernel ENOBUFS drops not surfaced — IP_RECVERR not wired");
    }
    ```
  - **(SENIOR — cross-primitive) `epoll_drop_window_uses_time_clock`** — composes epoll.rs with the `time` crate
    (W6): the drop-RATE window must be measured via `time::Clock`, NOT `Instant::now()` directly, so the
    deterministic harness can replay it. Asserts the rate computation takes a `&Clock`. (Passes each crate alone;
    fires when epoll bypasses the injected clock — the same det-clock discipline the venue's VSR core depends on.)
  - **(EXPERT — bar (c) bench) `epoll_5m_pps_loss_free_single_thread`** — criterion: synthetic UDP gen at 5M pps;
    loss < 0.05% AND throughput lands in a physically-plausible band (3M–20M pps/thread) so the bench fails fast
    if it measured nothing. Exercises R5.
  - **(SENIOR) `epoll_p99_batch_fill_80pct`** — bench: P99 batch fill ≥ 80% of BATCH (R6).
  - **(SENIOR) `epoll_so_reuseport_multi_core_scaling`** — 4 receivers via SO_REUSEPORT each get ~25% ±5% of a
    uniform flow (scaling sanity — mirrors the shard-per-core fan-in the runtime ships today).

  #### Phase 3 — Make the tests pass, one failure at a time (~110 min)
  Ordered newbie→expert.

  - **(SENIOR) When `epoll_edge_triggered_drain_loop` reports ~1 epoll_wait/packet (1M calls):**
    - Symptom: throughput collapses to ~500k pps; flamegraph shows `epoll_wait` at ~80% CPU; the call-count band
      assertion fires.
    - Why: registered `EPOLLIN` (level-triggered); every readable byte re-arms the notification → one `epoll_wait`
      round-trip (~1 µs) per byte-group; 10M × 1 µs = 10 sec/sec.
    - Fix: `epoll_ctl(EPOLL_CTL_ADD, fd, EPOLLIN | EPOLLET)`; on each notification drain via
      `while recvmmsg(...) == BATCH` until `EAGAIN`. One `epoll_wait` amortizes over thousands of packets.
    - Cost avoided: 40 ms/sec vs 10 sec/sec = **250× win** (bar-(c) violation if level-triggered: caps the
      baseline below its own 5M-pps target).
    - Muscle: edge-triggered + drain-fully is the ONLY correct pattern at ingress scale. Reapplies at: W86 io_uring
      multi-shot recv (drain on `CQE_F_MORE`), W89 AF_XDP RX-ring drain, W84 VSR-log projection drain loop.

  - **(SENIOR) When `epoll_zero_alloc_on_hot_path` / `epoll_recv_buf_pre_allocated_field` report per-call alloc:**
    - Symptom: dhat flags 64 KB allocations per `recv_batch`; `memset` in the top-3 perf samples.
    - Why: `let mut buf = vec![0u8; MAX_FRAME*BATCH]` per call = 50 ns malloc + 64 KB zero-fill (~10 µs); at 100k
      batches/sec ≈ 1 sec/sec just zeroing.
    - Fix: `recv_buf: AlignedBox<[u8; MAX_FRAME*BATCH]>` as a struct field allocated once in `new()`. Each
      `recv_batch` reuses it; the kernel overwrites it via `recvmmsg`'s `msgvec[i].msg_iov` pointing into the same
      buffer. Frame `&'a [u8]` slices borrow it — valid only until next `recv_batch`.
    - Cost avoided: 64 KB zero-fill + 50 ns malloc per batch (~1 sec/sec at 100k batches/sec) + cache-warmth
      (bar-(c) violation).
    - Muscle: any pre-sizable hot-path buffer lives as a struct field allocated at `new()`; `&mut self` lets the
      kernel overwrite in place. Reapplies at: W86 io_uring registered buffer ring, W89 AF_XDP UMEM, W84
      messaging-aeron `scratch_buf`, runtime Reactor poll buffer (Thu), risk-engine per-tick scratch (W98).

  - **(SENIOR) When `epoll_recvmmsg_batched_32` reports BATCH× `recvmsg`:**
    - Symptom: strace shows `recvmsg` once per frame instead of one `recvmmsg`.
    - Why: used `recvmsg` (single) in the drain loop instead of `recvmmsg` (vectored).
    - Fix: `recvmmsg(fd, msgvec.as_mut_ptr(), BATCH, MSG_DONTWAIT, NULL)`; one syscall returns up to BATCH
      `mmsghdr` with per-frame lengths.
    - Cost avoided: (BATCH-1) syscalls per batch (~300 ns each) — at BATCH=32, ~9.3 µs/batch reclaimed.
    - Muscle: vectored syscalls (`recvmmsg`/`sendmmsg`/`io_uring`) are how you batch at the syscall boundary.
      Reapplies at: W86 io_uring SQE batching, W84 messaging-aeron `sendmmsg` multicast.

  - **(EXPERT) When `epoll_enobufs_visible_via_metric` reports `drops()` stays 0 under flood:**
    - Symptom: you believe you're loss-free, but a downstream sequence-gap check (W87) shows missing ITCH seqs.
    - Why: kernel silently drops UDP on rcvbuf overflow; without `IP_RECVERR` the app never learns.
    - Fix: `setsockopt(IP_RECVERR, 1)`; periodically `recvmsg(MSG_ERRQUEUE)` and accumulate the drop count into
      `self.drops`. Surface via `drops()` so the feed handler can flag a gap.
    - Cost avoided: not latency — CORRECTNESS: silent data loss mistaken for a clean feed (a trading-grade defect;
      at the venue, a dropped tick into `oracle-mark` = a wrong mark = a wrong liquidation). Bar-(c): undetected
      drops invalidate the deterministic replay.
    - Muscle: a UDP data plane MUST surface kernel drops, or "loss-free" is a lie. Reapplies at: W87 feed-handler
      sequence-gap detection, W89 AF_XDP RX-ring drop counter, W84 messaging-aeron NAK on gap.

  - **(EXPERT) When `epoll_5m_pps_loss_free_single_thread` lands outside the plausible band (or 0 ops):**
    - Symptom: the criterion result is ~0 pps or implausibly high (e.g. 1e12) — the bench measured nothing
      (loopback short-circuited, or the generator never started).
    - Why: a negative-control failure — the bench harness is wrong, not the impl.
    - Fix: assert the throughput is inside 3M–20M pps/thread before trusting it; wire a real packet generator
      (separate thread sending to the loopback socket) and count received frames, not loop iterations.
    - Cost avoided: shipping a "5M pps" claim backed by a bench that measured a no-op (the worst kind of green).
    - Muscle: every perf bench needs a plausibility band + a real workload, or it lies. Reapplies at: every bar-(c)
      bench — W86 io_uring, W89 AF_XDP, W90 v0.5 P99, runtime perf targets (Thu), W99 risk-engine p99.99.

  #### 5-year failure mode
  Write in `notes/85_epoll_followup.md` after green: TRIGGER — epoll is deprecated for ingress as eBPF/XDP
  captures packets before the socket layer (re-bench at W139 shows epoll baseline > 2× the AF_XDP tail). epoll
  becomes legacy/dev-loop-only. Migration: keep epoll for the portable dev loop + correctness oracle; production
  paths default to io_uring or AF_XDP.

- [ ] [Tempo] 30 min only if Sepolia is green: continue Tempo sync attempt (counts toward the 4h cap). (~30 min)
- [ ] Commit + log (~10 min)

## Saturday — Sepolia debug close-out + marketdata verification pass + shard-by-account design note

- [ ] **Sepolia debug** (continued) — drive the sync as far as it goes; capture the final state in
  `notes/85_sepolia_sync.md` (synced-to-head OR the specific stage + block where it's blocked, with a next-step
  hypothesis). Output: a definitive status line + remaining-blockers list. (~110 min)

- [ ] **Verification pass: marketdata scaffold + epoll** — run the whole week's Build verification together (the
  Saturday loom/bench convention): `cargo test -p marketdata-kernelbypass`,
  `cargo test -p marketdata-kernelbypass --test ui` (trybuild), and the criterion benches with their
  plausibility-band assertions. Un-ignore `recv_batch_respects_out_capacity` now that epoll lands and confirm it
  passes. Also re-run the runtime v1.0 det-harness gate (`--features det det_harness_replay`) to confirm the
  shipped tag still replays byte-identically. Output: all scaffold + epoll tests green; the trybuild
  `frame_retain_across_batch.rs` emits the exact `E0502`; criterion lands in-band. Record in
  `notes/85_verification.md`. (~50 min)

- [ ] **[v3] Design note — shard-by-account seam for `risk-engine` (the §4A lock made concrete)**:
  `notes/85_shard_by_account.md`. Now that `runtime-thread-per-core` v1.0 ships with `Shard::id`, write the
  forward-design that W97–W99 will implement, so the substrate's contract is proven adequate *before* the
  capstone needs it. Output: a 1-page note covering — (~40 min)
  - the **back-of-envelope**: 100k accounts × 10 mark-ticks/s = 1M full recomputes/s; ~1µs/account ≈ 1 CPU-sec/sec
    single-threaded → only fits sharded; 16 shards (by account-id hash → `Shard::id`) ≈ 62k recomputes/s/shard ≈
    62ms/s/shard. Cross-check against the per-shard recompute-headroom number you recorded Thu.
  - **why incremental margin is mandatory, not a fallback** (§4A): at any higher mark cadence full recompute blows
    the budget; margin updates as a delta on each position/price change; full portfolio recompute reserved for
    circuit-breaker / parameter-change events.
  - **the read path**: risk reads margin from **local applied state on the leader shard — no consensus round-trip**
    (the control/risk staleness contract = read-after-commit); the cross-shard `submit_to` mailbox shipped today
    is the mechanism for cross-shard netting messages (BTC+ETH positions on different shards must net with the
    const 30% haircut, §4A).
  - **the one open implementation question to resolve at W97**: account-to-shard assignment stability under
    rebalancing (does an account ever migrate shards mid-epoch, and if so how is its applied state moved?). Flag
    it; do NOT solve it now.

- [ ] Commit + log (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
