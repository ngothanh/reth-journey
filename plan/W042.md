# Week 42 — [NEW] `txn` crate v0.5: 2PL + OCC + deadlock detect

> **Phase**: 3
> **Month**: M11
> **Track**: Multi
> **Hours target**: 30h/wk (M1–M18)
> **Mirror target**: classic 2PL/OCC txn manager (Gray & Reuter, Silo OCC, Cassandra/Scylla TWCS); `txn` mirrors no single upstream crate — it is the transaction substrate `storage-trie`, `ledger`, `mini-db`, `matching-engine` all inherit.
> **Crate(s) touched**: `txn` [NEW] → v0.5.0; `lsm-core` v0.5 → v0.6 (v2 TWCS additions); `storage-trie` (rewires write-tx onto `txn`)
> **Bar**: (c)   ← `txn` is Layer-1 substrate (README Bar policy): static-memory invariants + zero-allocation hot paths + VOPR-style determinism required at v1.0. lsm-core::TWCS additions are bar (b).
> **Feeds into**: W44 storage-trie v1.0 (write-tx wraps `txn::Txn`), W72 txn v1.0 (+2PC distributed), W74 matching-engine (per-symbol order-book mutations under raft — Order pool, state CAS, group-commit), W80 ledger-deterministic (Transfer pool, batch read-set, journal group-fsync), W95/W100 mini-db CAPSTONE (assembles wal+recovery+txn+bufpool; compaction-strategy matrix picks STCS/LCS/TWCS).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): Gray & Reuter *Transaction Processing* ch.7–8 (2PL, deadlock); Silo OCC paper (Tu et al. SOSP'13, §3 two-phase validation); Tarjan SCC (CLRS §22.5); ScyllaDB compaction KB https://opensource.docs.scylladb.com/stable/kb/compaction.html (STCS/LCS/TWCS config defaults); Geth `core/state` (statedb txn boundary, lookup-when-stuck only).
> **v2 modifications**: ADD lsm-core::TWCS (Time-Window Compaction Strategy); tag lsm-core v0.6

**[NEW] crate created**: `crates/txn/`. Transaction lifecycle and concurrency control. Inherited by storage-trie (
rewires existing tx logic), ledger (W80), mini-db (W95), matching-engine (W74 — for per-symbol order-book mutations
under raft). v1.0 with 2PC ships W72.

**Monday — Geth comparison study (background reading)**

- [ ] **Write**: `crates/txn/notes/42_geth_state_comparison.md` — targeted comparison (NOT cover-to-cover; use
  Geth `core/state` as a lookup reference). Answer exactly 4 questions that shape this week's `txn` design:
  (1) where is Geth's transaction/snapshot boundary in `StateDB` (Snapshot/RevertToSnapshot journal)?
  (2) how does Geth handle concurrent state access (it largely doesn't — single-threaded EVM; contrast with
  our 2PL/OCC)? (3) what is Geth's "dirty set" and how does it map to our OCC read/write-set? (4) why does
  Ethereum's execution model let Geth avoid a deadlock detector entirely (sequential tx execution within a
  block)? (~45 min)
  - 🎯 **Hardened acceptance**: the note names the exact Geth file (`core/state/statedb.go`) and the
    `journal`/`snapshot` mechanism, and states in one line per row why `txn` needs concurrency control that
    Geth doesn't. *Mental model*: this exists to set the contrast — `txn` mirrors classic multi-user DB
    concurrency (Gray & Reuter), not Ethereum's single-threaded execution, so we cannot copy Geth here.
  - **Output**: `notes/42_geth_state_comparison.md` exists with the 4 answered questions + the file/mechanism citation.
- [ ] **Build**: `crates/txn/Cargo.toml` workspace member. Deps: `time`, `wal`, `recovery`. (~20 min)
  *Moved from Tuesday: the decomposition note (~15 min) + scorecard scaffold (~10 min) added there pushed
  Tuesday past ~360 min; Monday had ~55 min of slack.*
- [ ] Commit notes (~10 min)

**Tuesday — `txn` crate scaffold + lifecycle**

- [ ] 🔧 **Decomposition prompt for the whole `txn` crate (silent test, applies Tue-Thu)**: the lifecycle, lock
  manager, deadlock detector, OCC validator, and commit path each silently require primitives from your
  W5–W17 toolkit. The spec names none of them. If you reach for `Arc::new(Txn)` per request, or a single
  `AtomicU64 next_id`, or fresh `HashMap`/`Vec` per detector scan, or per-fsync per commit — your bench will
  tell you which primitive you forgot. Decompose before building. File: `crates/txn/notes/42_decomposition.md`.
  (~15 min)
  - **Output**: `notes/42_decomposition.md` enumerates the ≥4 silently-required primitives (pooled/recycled
    `Txn` allocation, `CachePadded` id counter, reused deadlock-detector scratch, group-commit fsync
    amortization), each with the W5–W17 primitive it reuses and the bench that would have exposed its absence.
- [ ] 🎯 **Hardened acceptance for `txn` v0.5 (silent decomposition test, must hold at Fri tag)**:
  - **Latency distribution**: `begin` p50 ≤100ns, p99 ≤1µs, p99.9 ≤10µs; `commit` p50 ≤200ns (excl. fsync),
    p99 ≤10µs (group-commit window), p99.9 ≤2ms (NVMe fsync tail), p99.99 ≤10ms.
  - **Scale curve**: `begin`+`abort` (no-fsync leg) throughput at N=1,2,4,8,16 concurrent txns; aggregate at
    N=16 must be ≥10× N=1. Sublinear = unsharded `next_id` (CachePadded missing) or single-counter contention.
    (Durable `begin`+`commit` scaling is fsync-bound — judged by the Throughput row below, not this curve.)
  - **Throughput**: ≥100k commits/sec at N=16 on NVMe (group-commit amortizes fsync).
  - **Zero allocation on hot path (dhat)**: `begin`/`commit` produce zero heap allocations in steady state —
    Txn struct comes from a pool, LockGuard from slab, deadlock-detector scratch reused across scans.
  - **Loom-green**: state-machine CAS race (4 threads attempt commit + abort on same Txn) — exactly one
    terminal state wins, observers see consistent terminal state.
  - **Deadlock-detection latency**: scan completes within 2× of interval at 10k active txns; zero false
    positives under acyclic workloads.
  - **Cross-restart determinism**: kill -9 mid-commit (1000 trials); recovery decides committed-vs-aborted
    consistently with WAL contents.
  - **Graceful degradation**: under 10× sustained overload (lock convoy), wound-wait sheds via documented
    policy; no livelock; older txns always win.
  - **5-year failure mode**: state one shift (cross-DC replication, 256-core lock concurrency, MVCC version
    explosion) that would force a redesign.
  - File: `crates/txn/notes/42_v05_scorecard.md` (~10 min to scaffold the rows now; filled through the week).
  - **Output**: Friday's `git tag txn-v0.5.0` blocks on the filled scorecard — every row above carries a
    measured value + pass/fail; any red row blocks the tag.
- [ ] **Build**: `crates/txn/src/lifecycle.rs` —
  `Txn { id: TxId, started_at: Hlc, state: AtomicState, locks: LockSet, log_records: SmallVec<Lsn> }`. State enum:
  Active, Preparing, Committed, Aborted. (~total 155 min across 3 phases)
  - 🔧 **Why this matters**: Txn is the unit of concurrency control. State CAS race → torn state; HLC monotonicity
    violation → OCC breaks; heap-alloc per Txn → 5× slowdown at 100 k tx/sec. Downstream W42 LockManager, W42 OCC,
    W72 2PC, W80 ledger txn, W74 matching-engine all consume.

  #### Phase 1 — Problem, prediction, design (~35 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in
    `crates/txn/notes/42_lifecycle_prediction.md`; compare to reality after tests run:
    - P1. Name the primitive you'd reach for to hold the state field, sight-unseen. Write the exact import
      line (`use std::sync::atomic::...;` or `use crossbeam_utils::...;`).
    - P2. Predict `Txn::begin()` latency on a warm pool: one number in ns. Then predict it with a naive
      `Arc::new(Txn{..})` per begin. Two numbers; no commentary.
    - P3. Predict the concurrent-first-call outcome: 4 threads (2 commit, 2 abort) race the SAME Txn's state
      with a plain `state.store(new)` (no CAS). How many distinct terminal states can observers see?
    - P4. Predict `Hlc::now()` after the physical clock steps back 1 s mid-run with a naive
      `Hlc(SystemTime::now())`: does `started_at` go backward? yes/no + the resulting OCC symptom in one line.
    - P5. Of the Phase-2 tests below, list every test name you predict your first attempt will fail. The
      delta between this list and reality after `cargo test` is the lesson.

  - *Mental model*: a `Txn` is a state machine + identity + ordering for every transaction.
    Active → Preparing → Committed | Aborted; never backwards. The state CAS is the linearization point for
    commit/abort decisions; the HLC.started_at is the global ordering for OCC validation.

  - *Contract*: `Txn { id: TxId, started_at: Hlc, state: AtomicU8, locks: LockSet, log_records: SmallVec<[Lsn; 8]> }`;
    `fn begin(pool: &TxnPool) -> Arc<Txn>` — the `Arc<Txn>`s are PRE-CREATED at pool construction and cycled
    through the freelist via `Arc::into_raw`/`Arc::from_raw` (never `Arc::from_raw` over a bare slab slot — a
    bare slot has no `ArcInner` header); alternative contract: `fn begin(pool: &TxnPool) -> TxnHandle` with an
    `AtomicUsize` refcount embedded in the slot. Zero steady-state allocs either way.
    `fn transition(&self, prior: State, new: State) -> Result<(), State>` (CAS, returns observed terminal state
    on loss); `fn commit/abort(&self) -> Result<()>`. State enum `#[repr(u8)]`.

  - *Back-of-envelope* (bar (c)): target 100 k tx/sec sustained, 1 M/sec burst. Naive `Arc::new(Txn)` =
    malloc ~50 ns + refcount cell ~30 ns + free ~30 ns ≈ 80–110 ns/Txn → at 100 k/sec that is 8–11 ms/sec of
    pure allocator CPU AND, worse, allocator-lock contention spikes p99 begin() from ~100 ns to ~500 ns under
    1 M/sec burst (blows the 10 µs commit budget). Slab-pop is a single `SegQueue` CAS ≈ 10–15 ns =
    **5–8× cliff** and a flat tail. State CAS itself: ~7 ns uncontended (one `lock cmpxchg`), the SeqCst
    fence adds a store-buffer drain only on the contended path.

  - **Requirements** — write in `crates/txn/notes/42_lifecycle_design.md`:
    - R1. State enum with explicit `#[repr(u8)]`; backed by AtomicU8.
    - R2. **State transitions monotonic**: every transition is `compare_exchange(prior, new, SeqCst)`;
      loser observes the winner's terminal state.
    - R3. **HLC.started_at monotonic** — stores max(physical, prior_hlc + 1). NTP step back must not move
      started_at backward.
    - R4. **Slab-allocated Txn pool** — no heap alloc per Txn::new(). Pre-allocate max_concurrent_txns slab
      slots; free list via SegQueue<u32>.
    - R5. Lifecycle API: `begin`, `read`, `write`, `prepare`, `commit`, `abort`.
    - R6. log_records append-only; on abort, undo records emit but log_records stays for replay.
    - R7. Long-running txn deadline enforcement — txn manager kills active txns older than deadline.

  - **Design walk-through** — answer each:
    - D1. Why `#[repr(u8)]` on the enum? What does AtomicU8 buy?
    - D2. State CAS race: write the 4-thread scenario (2 try commit, 2 try abort); show how
      `compare_exchange(prior, new, SeqCst)` resolves it.
    - D3. HLC: write `Hlc::now() -> Hlc` algorithm. What happens on NTP step back?
    - D4. Slab design: write the `TxnPool` struct. Why SegQueue for the free list (not Mutex<Vec>)?
    - D5. Deadline policy: how does the txn manager find txns past deadline efficiently?
    - D6. **Pre-mortem (load-bearing)**: write the alloc-per-Txn trace at 100 k tx/sec × 80 ns alloc/free =
      8 ms/sec CPU; under burst (1 M tx/sec), arena lock contention spikes p99 to ~500 ns.
    - D7. **Anti-shortcut audit**: name the "obvious" reach for each motivation and why it is wrong HERE —
      (a) `Mutex<State>` for the state field (25 ns lock + can't be a linearization point a loser observes
      without holding the lock across the decision); (b) `Arc::new(Txn)` per begin (allocator on the hot
      path, see Back-of-envelope); (c) `Hlc(SystemTime::now())` raw (no monotonicity floor → OCC reversal).
  - 🧮 **Paper drill** — write in `notes/42_lifecycle_drill.md`:
    - D-STRUCT on the `Txn` state machine `Active → Preparing → {Committed | Aborted}`: draw the 4 states as
      nodes; hand-execute one full happy-path lifecycle (begin→prepare→commit) and one abort path
      (begin→abort), redrawing `state: AtomicU8` after each transition; mark the single transition that is the
      commit linearization point.
    - D-RMW on the state-word CAS: build the 4-thread step table (T1,T2 try `cmpxchg(Active→Committed)`;
      T3,T4 try `cmpxchg(Active→Aborted)`), columns = T1/T2/T3/T4/`AtomicU8 value`. Show the lost-update a
      plain `store(new)` causes (two terminal states observed) vs the one CAS winner; annotate where
      atomicity carries it (CAS-reads-latest) vs where SeqCst ordering carries the loser's observation.
    - D-HB on the lifecycle ordering: two columns (winner thread does CAS-to-Committed → release; loser
      thread does next-acquire → CAS). Mark the Release (the winning CAS / lock release) and Acquire (the
      loser's CAS / acquire); draw the RA edge; chain to verdict "loser sees the Committed terminal state."
      NEGATIVE: weaken the winning CAS to `Relaxed` and draw the interleaving where the loser observes
      `Active` and double-commits.
    - D-LIFE on the slab-pooled `Txn` value: timeline — birth (slab-pop, no malloc) → handles (the `Arc<Txn>`
      clones handed to LockManager + manager.active) → the single event that returns the slot to the
      `SegQueue<u32>` free list → assert "freed exactly once." Add the count-transition table for the `Arc`
      strong-count; flag the use-after-free if the slot is recycled while a clone is still live.
  - **Output**: `notes/42_lifecycle_design.md` with R1–R7, D1–D7, the bad-interleaving sketch from D2;
    plus `notes/42_lifecycle_drill.md` with the D-STRUCT/D-RMW/D-HB/D-LIFE worked drills.

  #### Phase 2 — Write the failing tests (~30 min)
  Each test is engineered so a naive first attempt FAILS it. Tier annotated (NEWBIE/SENIOR/EXPERT).

  - [ ] **(SENIOR)** `lifecycle_state_transitions_monotonic` — property test: 100 threads CAS-transition single Txn;
    final state ∈ {Committed, Aborted}; all observers see correct pre-state. Traps a `state.store(new)` impl
    (no CAS) → multiple terminal states. Exercises R1, R2.
  - [ ] **(SENIOR)** `lifecycle_hlc_monotonic_under_ntp_step_back` — simulate physical clock going backward 1 s; HLC
    must continue increasing. Traps `Hlc(SystemTime::now())`. Exercises R3.
  - [ ] **(EXPERT, anti-shortcut)** `bench_lifecycle_no_heap_alloc` — instrument allocator (dhat / a count-bumping
    `GlobalAlloc` shim); 100 k begin/commit cycles; assert allocator increments == 0 after pool warm-up.
    Correctness passes with `Arc::new(Txn)`; this **fails the bar**. Exercises R4.
  - [ ] **(EXPERT, miri/loom)** `loom_recycle_never_frees_live_handle` — `#[cfg(loom)]` model (and the same
    scenario under `cargo miri test`): thread A holds a clone of the pooled handle while thread B commits and
    recycles the slot; assert the slot re-enters the freelist ONLY after A's handle drops (refcount == the
    pool's own reference at recycle time), and miri reports no use-after-free on A's subsequent field read.
    Exercises R4.
  - [ ] **(NEWBIE)** `lifecycle_begin_to_commit_records_lsn` — begin → write → commit; assert log_records is non-empty;
    LSN matches WAL append. Exercises R5, R6.
  - [ ] **(SENIOR)** `lifecycle_deadline_kills_stale_txn` — begin Txn; wait past deadline; assert manager aborts it
    with timeout error. Exercises R7.
  - [ ] **(EXPERT, loom)** `loom_lifecycle_state_cas_race` — `#[cfg(loom)]`, 4 threads attempt various transitions; assert
    exactly one terminal state observed across all interleavings. Exercises R2.
  - [ ] **(negative-control, test-the-test)** `loom_lifecycle_cas_race_must_be_slow` — asserts the loom model
    above actually explores interleavings (`Instant::now()` wall time ≥ 100 ms); a loom test that finishes in
    <100 ms is exploring one schedule and proves nothing.
    ```rust
    #[test] #[cfg(loom)]
    fn loom_lifecycle_cas_race_must_be_slow() {
        let t0 = std::time::Instant::now();
        loom_lifecycle_state_cas_race(); // the model under test
        assert!(t0.elapsed().as_millis() >= 100,
            "loom model finished in {:?} — it explored ~1 schedule, not the full space; model is broken",
            t0.elapsed());
    }
    ```
  - [ ] **(negative-control, test-the-test)** `bench_lifecycle_begin_physically_plausible` — paired with the alloc
    bench: asserts warm-pool `begin()/commit()` throughput lands in a physically plausible band
    (`3e6 ≤ ops/s ≤ 5e8`); a number outside the band means the bench measured nothing (e.g. optimizer hoisted
    the loop) and the alloc assertion is meaningless.

  #### Phase 3 — Make the tests pass, one failure at a time (~90 min)
  Ordered newbie-first, expert-last — the ladder you actually walk.

  - **(SENIOR) When `lifecycle_hlc_monotonic_under_ntp_step_back` reports started_at going backward:**
    - Symptom: physical clock steps back 1 s (NTP correction); subsequent Hlc::now() returns a value
      < prior Hlc; OCC validation breaks because the version comparison reverses.
    - Why: HLC monotonicity violated — NTP step back makes `started_at` go backward.
    - Fix: HLC stores max(physical, prior_hlc + 1). On every `now()` call, compare physical clock
      against last-issued HLC; return max + 1 if physical regressed. The HLC's logical component
      absorbs the backward step.
    - Muscle: any system-clock-derived ordering must defend against clock step-backs; max with
      prior value is the canonical fix. Reapplies at: W14 time crate Hlc, W74 matching-engine
      order timestamps.

  - **(SENIOR) When `loom_lifecycle_state_cas_race` reports torn state (two threads see different terminal
    states):**
    - Symptom: 2 threads commit, 2 threads abort concurrently; some observers see Committed, some
      see Aborted; the "winner" is ambiguous.
    - Why: state CAS race — two threads attempt to commit + abort concurrently; without CAS one
      wins ambiguously. Without SeqCst, the loser's observation may not see the winner's terminal
      state.
    - Fix: every transition is `compare_exchange(prior, new, SeqCst)`; loser observes the winner's
      terminal state. On failure, the CAS returns the actual current value — the loser uses that.
    - Cost avoided: torn terminal state = double-commit / double-abort = a durability correctness bug
      (bar (c) violation), not a slowdown; the SeqCst fence costs only a store-buffer drain (~handful of ns)
      on the already-contended path.
    - Muscle: state machines under concurrency need SeqCst CAS at every transition; the loser must
      observe the winner's outcome. Reapplies at: W74 matching-engine order-state transitions, W11
      MPMC closed flag, W67 consensus-raft term-bump CAS.

  - **(EXPERT, anti-shortcut — bar (c) violation) When `bench_lifecycle_no_heap_alloc` reports allocator increments per Txn::new():**
    - Symptom: 100 k cycles → 100 k allocations; flamegraph shows ~8 ms/sec CPU on `malloc_zone_*`;
      under 10× burst load p99 begin() latency jumps from ~100 ns to ~500 ns, blowing the 10 µs
      commit budget.
    - Why: `TxnManager::begin()` does `Arc::new(Txn { ... })` which calls malloc (~50 ns) + Arc
      refcount cell allocation; on drop, free (~30 ns). At 100k tx/sec that's 80 ns × 100k = 8 ms/sec
      of pure allocator time = ~0.8% CPU one core, but symptom is worse: tail latency.
    - Fix: **pool the Arc itself, never conjure one from a bare slot** — two sound designs: (a) pre-create all
      `max_concurrent_txns` `Arc<Txn>`s ONCE at pool construction; the freelist is a `SegQueue` of pointers
      produced by `Arc::into_raw`; `begin()` pops one and `Arc::from_raw`s it (sound: the pointer WAS produced
      by `Arc::into_raw`, so the `ArcInner` header exists), resets the fields, returns it; recycle =
      `Arc::into_raw` back onto the freelist — 0 steady-state allocs. Or (b) skip `Arc` entirely: return a
      custom `TxnHandle` whose refcount is an `AtomicUsize` embedded in the slab slot. What is UB and NOT a
      fix: `Arc::from_raw` over a bare `Vec<UnsafeCell<Txn>>` slot — `Arc::from_raw` assumes an `ArcInner`
      (strong/weak counts) precedes the payload; a bare slot has none, so the first clone/drop scribbles the
      neighboring slot's memory.
    - Guard: `loom_recycle_never_frees_live_handle` (miri + loom, Phase 2) — recycle must never return a slot
      to the freelist while a live handle exists (refcount == the pool's own reference at recycle time).
    - Cost avoided: 80 ns/Txn → ~15 ns/Txn = **5× faster**; zero allocator contention; static memory
      invariant (bar (c) requirement for HFT critical path) preserved.
    - Muscle: "pre-allocate the steady-state working set" — if your N-active count has a known
      ceiling, allocate once, recycle slots forever. Reapplies at: W74 matching-engine Order pool,
      W80 ledger Transfer pool, W84 messaging-aeron Image pool, W90 marketdata Tick pool.

  - **5-year failure mode** (write in `notes/42_lifecycle_followup.md` after green): when
    pre-emptible userspace scheduling (PREEMPT_RT, or io_uring-based async schedulers) becomes
    standard, the slab + SegQueue pattern needs to be wait-free, not lock-free. State migration:
    swap to wait-free slot allocator.
- [ ] **Build**: `crates/txn/src/manager.rs` —
  `TxnManager { next_id: AtomicU64, active: DashMap<TxId, Arc<Txn>>, ... }`. (~total 155 min across 3 phases)
  - 🔧 **Why this matters**: every active txn is reachable from here. False sharing between next_id and
    DashMap shard pointers → a 2–4× throughput regression at 16 cores. Wrong ordering (remove from active before
    durable commit) → orphan txns in WAL on crash. Downstream W42 deadlock detector, W42 OCC, W74
    matching-engine reuse the registry pattern.

  #### Phase 1 — Problem, prediction, design (~35 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in
    `crates/txn/notes/42_manager_prediction.md`:
    - P1. Name the map type you'd reach for to hold active txns, sight-unseen. Exact import line.
    - P2. Predict 16-thread aggregate `begin()+abort()` (no-fsync leg) throughput with `next_id: AtomicU64`
      declared as a plain struct field adjacent to the map. One number in ops/sec. Then predict it with
      `CachePadded`. Sanity: a cross-core line transfer costs 80–200 ns, so the plain-field penalty is a
      small-multiple cliff (~2–4×), not orders of magnitude.
    - P3. Predict the concurrent outcome: does `next_id` placement next to DashMap shard pointers cause a
      correctness bug, a throughput bug, or both? One word + one line why.
    - P4. Predict what recovery sees if you `active.remove(id)` BEFORE the Committed record is fsynced and
      crash in that window. One line.
    - P5. List every Phase-2 test you predict your first attempt fails.

  - *Mental model*: a concurrent registry of active txns + a monotonic id generator.
    DashMap provides per-shard concurrency for the begin/end churn; the id generator is the only true
    global hotspot, so it must live alone on its cache line.

  - *Contract*: `TxnManager { next_id: CachePadded<AtomicU64>, active: DashMap<TxId, Arc<Txn>>, pool: TxnPool }`;
    `fn begin(&self) -> Arc<Txn>`; `fn commit(&self, txn: &Arc<Txn>) -> Result<()>`; `fn abort(&self, ..)`;
    `fn active_txns(&self) -> Vec<Arc<Txn>>` (cloned Arcs, never held entry guards); `fn recover(&self, wal)`.

  - *Back-of-envelope* (bar (c)): target ≥800 k begin+abort/sec at 16 cores (linear within 2×) — the NO-fsync
    leg. Durable begin+commit CANNOT carry an 800 k/s bar: the crate's own group-commit math caps it — a
    ≤16-deep group × ~100 µs NVMe fsync ⇒ ~160 k commits/sec ceiling — so the durable-throughput bar lives in
    Thursday's `commit.rs` bench (`bench_commit_durable_100k_n1000`: 100 k/s with N≥1000 outstanding txns).
    False-sharing arithmetic, honestly: the padded begin+abort op is ~100 ns of real work (id fetch_add + slab
    pop + shard insert/remove); false sharing of `next_id` with a DashMap shard pointer adds one
    L3/cross-core line transfer ≈ 80–200 ns to every contended op ⇒ ~180–300 ns/op ⇒ a **~2–4× cliff**
    (~800 k/s–1 M/s → ~200–400 k/s). NOT a 16× collapse: ~50 k/s would mean ~320 µs/op, unreachable from an
    80–200 ns line transfer — if you measure that, something else (a lock, a syscall) is in the loop.
    `CachePadded<AtomicU64>` costs 56 B (x86) / 120 B (aarch64) of pad — paid once, recovered every µs.

  - **Requirements** — write in `crates/txn/notes/42_manager_design.md`:
    - R1. `begin()` bumps next_id, creates Txn (slab), inserts into active.
    - R2. **`next_id: CachePadded<AtomicU64>`** — own cache line, no false sharing.
    - R3. `commit()` state CAS Active→Preparing; WAL prepare + fsync; CAS Preparing→Committed; release locks;
      remove from active.
    - R4. **Remove from active AFTER Committed record fsynced** — never before.
    - R5. `abort()` state CAS to Aborted; emit CLRs via recovery::UndoPass; release locks; remove from
      active.
    - R6. `active_txns()` iterator for deadlock detector — cloned Arc<Txn>s, not held DashMap entry guards.
    - R7. Recovery: scan WAL for txns missing Committed record → abort.
    - R8. 16-thread begin+abort (no-fsync leg) throughput ≥ 800 k/sec (linear scaling within 2×); the durable
      commit bar is Thursday commit.rs's `bench_commit_durable_100k_n1000` (100 k/s, N≥1000 outstanding).

  - **Design walk-through** — answer each:
    - D1. Why DashMap (vs Mutex<HashMap>)? What does sharding buy?
    - D2. `next_id` placement: write the struct layout showing CachePadded surrounded by DashMap shards.
      Why does false sharing matter?
    - D3. Commit ordering: write the 6-step sequence. Why does "remove from active" come last?
    - D4. DashMap entry guard discipline: when is it safe to hold a guard? When must you clone-out?
    - D5. Recovery scan: write the algorithm. What does "missing Committed record" mean (recovery::UndoPass
      interface)?
    - D6. **Pre-mortem (load-bearing)**: write the false-sharing trace at 16 cores — every begin() invalidates
      next_id's cache line; every commit()/remove reads a DashMap shard pointer from the SAME line; LLC
      roundtrips of 80–200 ns land on every contended op (~100 ns of real work → ~180–300 ns); throughput
      plateaus at ~200–400 k/sec, a 2–4× cliff off the padded ~800 k–1 M/s line.
    - D7. **Anti-shortcut audit**: (a) `Mutex<HashMap>` for active (single lock = serializes begin/end,
      kills 16-core scaling); (b) plain `AtomicU64` field (false sharing, see Pre-mortem); (c) returning a
      held `dashmap::Ref` guard to the deadlock detector (holds a shard lock across the whole scan).
  - 🧮 **Paper drill** — write in `notes/42_manager_drill.md`:
    - D-LAYOUT on `TxnManager { next_id: AtomicU64, active: DashMap<..>, pool: TxnPool }` with a PLAIN
      `next_id`: draw the byte map — offsets of `next_id` (8 B) and the first DashMap shard-pointer field,
      the padding, and the 64 B (x86) / 128 B (aarch64) cache-line boundary. Mark that `next_id` and a shard
      pointer share one line → every `begin()` `fetch_add` invalidates the line that `commit()`'s
      `active.remove()` reads. Then redraw with `CachePadded<AtomicU64>`: show `next_id` now owns its line
      (56 B / 120 B of pad) and the false-share is gone.
    - D-STRUCT on the active-txn registry over a concrete 2-shard DashMap with 3 live txns: hand-execute
      `begin(id=4)` (insert into shard `4 % 2`), `active_txns()` (clone all 3 `Arc`s out, drop guards), and
      `commit(id=2)` (remove from its shard); redraw the two shard buckets after each op. Mark the
      linearization point of `begin` (the insert) and show the 2-thread interleaving where holding a
      `dashmap::Ref` guard across the fsync would block the other thread's same-shard insert.
  - **Output**: `notes/42_manager_design.md` with R1–R8, D1–D7, the false-sharing bad-interleaving sketch;
    plus `notes/42_manager_drill.md` with the D-LAYOUT byte map + D-STRUCT registry trace.

  #### Phase 2 — Write the failing tests (~30 min)
  Tier annotated; at least one is a cross-primitive trap composing CachePadded (W4) inside this struct.

  - [ ] **(NEWBIE)** `manager_begin_bumps_next_id_monotonic` — 100 k begins; assert ids strictly increasing. Exercises
    R1.
  - [ ] **(EXPERT, anti-shortcut — bar (c))** `bench_manager_begin_16_thread_scales` — 16 threads each
    begin/ABORT (the no-fsync leg; the durable leg is Thursday's `bench_commit_durable_100k_n1000` — an
    800 k/s bar on a fsync-bound path would contradict the group-commit ceiling of ~160 k/s); assert aggregate
    ≥ 800 k/sec. Plain `AtomicU64` passes correctness, plateaus at ~200–400 k/sec (the 2–4× false-sharing
    cliff) → fails the bar. Exercises R2, R8.
  - [ ] **(SENIOR)** `manager_remove_after_durable_commit` — instrument: assert remove from active fires AFTER WAL fsync
    completes. Exercises R4.
  - [ ] **(SENIOR)** `manager_crash_before_committed_record_recovers_abort` — kill -9 after state Preparing but before
    Committed record fsync; assert recovery aborts the txn. Exercises R7.
  - [ ] **(SENIOR)** `manager_dashmap_guard_not_held_across_fsync` — instrument lock held duration; assert no DashMap
    guard held during WAL fsync. Exercises R6.
  - [ ] **(EXPERT, cross-primitive trap)** `manager_next_id_does_not_false_share_dashmap` — composes the W4
    `CachePadded` Build with this manager struct. `CachePadded` is correct in isolation, but a `#[repr(Rust)]`
    `TxnManager` may pack `next_id` so its 64 B line still overlaps an adjacent map field. Assert the address
    gap between `next_id` and the nearest other field is ≥ the target cache line:
    ```rust
    #[test]
    fn manager_next_id_does_not_false_share_dashmap() {
        let m = TxnManager::new();
        let nid = std::ptr::addr_of!(m.next_id) as usize;
        let other = std::ptr::addr_of!(m.active) as usize;
        let line = if cfg!(target_arch = "aarch64") { 128 } else { 64 };
        assert!(nid.abs_diff(other) >= line,
            "next_id at gap {} < cache line {} — every beg() invalidates a line the map reads",
            nid.abs_diff(other), line);
    }
    ```
    Exercises R2. (Pairs with the W4 lesson: padding is load-bearing → lock the layout with `repr(C)`.)
  - [ ] **(negative-control, test-the-test)** `bench_manager_begin_physically_plausible` — asserts the 16-thread
    bench lands in `1e5 ≤ ops/s ≤ 5e8`; a number outside means the bench measured nothing (e.g. all threads
    serialized on a test-harness mutex) and the scaling assertion is meaningless.

  #### Phase 3 — Make the tests pass, one failure at a time (~90 min)
  Ordered newbie-first, expert-last — the ladder you actually walk.

  - **(SENIOR) When `manager_crash_before_committed_record_recovers_abort` reports the txn marked Committed:**
    - Symptom: kill -9 between state Preparing and Committed-record fsync; on restart, the txn is in
      `active` but the WAL has no Committed record; recovery sees an orphan.
    - Why: remove from active before durable commit — removing then crashing leaves WAL with an
      in-progress txn that no one will abort.
    - Fix: remove only after Committed record is fsynced. Sequence: state CAS Active→Preparing → WAL
      append commit-record → WAL fsync → state CAS Preparing→Committed → release locks → remove from
      active. If crash in window before remove, recovery: scan active in manifest, check WAL for
      committed-record; if missing, abort and clean up.
    - Cost avoided: orphan txns in WAL on crash = a recovery correctness bug (bar (c) violation), not a
      slowdown; the extra ordering costs nothing at runtime.
    - Muscle: durability ordering: durable record FIRST, observability state next, cleanup last.
      Reapplies at: W26 WAL group commit, W74 matching-engine durable-order publish.

  - **(SENIOR) When `manager_dashmap_guard_not_held_across_fsync` reports guard held during fsync:**
    - Symptom: lock-duration instrumentation shows the DashMap entry guard held for ~1 ms during
      WAL fsync; other writers block on the same shard for that ms; under burst, write throughput
      collapses.
    - Why: DashMap entry held during long ops — holding a DashMap entry guard across a WAL fsync
      blocks other writers.
    - Fix: clone `Arc<Txn>` out of the guard before doing work. Pattern: `let txn = { let entry =
      active.get(&id); entry.clone() };  // guard dropped; do_work_on(txn);`.
    - Cost avoided: ~1 ms shard-lock hold per fsync × every concurrent writer on that shard = write
      throughput collapse under burst (bar (c) violation); clone-out drops it to a single Arc refcount bump
      (~5 ns).
    - Muscle: never hold a sharded-map guard across a slow op; clone the Arc and release the guard.
      Reapplies at: W42 LockManager wait-list, W74 matching-engine order-book shard access.

  - **(EXPERT, anti-shortcut — bar (c) violation) When `bench_manager_begin_16_thread_scales` reports plateau at ~300 k/sec:**
    - Symptom: 16-thread aggregate plateaus at ~200–400 k begin+abort/sec where the padded impl clears
      ~800 k–1 M/sec; `perf stat -e cache-misses,LLC-loads` shows the DashMap hot path dominated by L3 misses.
    - Why: declaring `next_id: AtomicU64` as a regular field places it on the same 64-byte cache line as
      the DashMap shard pointer array. Every `begin()` does `fetch_add(1)` (writes `next_id`'s line),
      which invalidates the line on every other core; every `commit()` does `active.remove()` (reads
      the DashMap shard pointer from the same line) — those readers then take a full L3 / cross-socket
      roundtrip ≈ 80-200 ns to refetch.
    - Fix: `next_id: CachePadded<AtomicU64>` (W4 pattern, `crossbeam_utils::CachePadded`); place each
      frequently-mutated atomic on its own 64 (or 128 on M-series) byte line. Same treatment for any
      `active.len()` cached counter.
    - Cost avoided: one 80–200 ns line transfer removed from every contended op (~180–300 ns/op → ~100 ns/op)
      = the **2–4× cliff** recovered; throughput ~200–400 k → ≥800 k begin+abort/sec; LLC-loads on the hot
      path drop ~95%.
    - Muscle: "every atomic that's written hot lives alone on its cache line" — and the corollary,
      every cold-read field can pack tightly. Reapplies at: W74 matching-engine per-symbol counters,
      W77 messaging-aeron publication seq, W84 runtime-thread-per-core shard scheduler counters, W90
      marketdata RX queue indices.

  - **5-year failure mode** (write in `notes/42_manager_followup.md` after green): when wait-free hash
    maps (e.g. ConcMap with no per-shard locks) stabilize in Rust, the DashMap pattern simplifies.
    State migration: swap DashMap → ConcMap; trigger when wait-free crate ships in 2026+.
- [ ] Commit + log (~10 min)

**Wednesday — `txn` 2PL + deadlock detect**

- [ ] **Build**: `crates/txn/src/locks.rs` — `LockManager` with shared/exclusive lock table per key. Wound-Wait deadlock
  prevention (newer transactions wound older ones). (~total 155 min across 3 phases)
  - 🔧 **Why this matters**: 2PL is the workhorse of pessimistic concurrency. Wound-Wait prevents deadlock by
    construction (older wounds younger). Wrong fairness → waiter starvation; VecDeque growth under burst →
    50× slower hot-key handling; per-waiter futex syscall → 12% CPU burning on wakes. W74 matching-engine
    order-book, W80 ledger inherit all three patterns.

  #### Phase 1 — Problem, prediction, design (~35 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in
    `crates/txn/notes/42_locks_prediction.md`:
    - P1. Name the container you'd reach for to hold per-key waiters, sight-unseen. Exact import line.
    - P2. Predict the cost of a 1000-waiter burst on a hot key with `waiters: VecDeque`: how many reallocs,
      and the total resize time in µs (use ~260 ns/grow under arena contention). Then predict it with a
      slab/SmallVec pool.
    - P3. Predict the syscall count of `release_all` waking 8 waiters with naive per-waiter `Waker::wake()`.
      Then predict it batched. Two numbers.
    - P4. Predict the failure mode of a shared/exclusive lock with NO fairness rule under a continuous
      stream of shared requests with one exclusive waiter at the head. One word.
    - P5. List every Phase-2 test you predict your first attempt fails.

  - *Mental model*: shared/exclusive locks per key, deadlock-free via Wound-Wait
    (older wounds younger), strict 2PL semantics (locks released only at commit/abort), waiter wake
    batching to amortize syscalls.

  - *Contract*: `LockManager { table: DashMap<Key, LockEntry> }`;
    `LockEntry { holders: Mode+Set, waiters: SmallVec<[Waiter; 8]> }`;
    `fn acquire(&self, txn: &Arc<Txn>, key: Key, mode: Mode) -> Result<LockGuard, Wounded>` (RAII guard,
    released only at commit/abort); `fn release_all(&self, txn: &Arc<Txn>)` (batched wake outside the entry lock).

  - *Back-of-envelope* (bar (c)): acquire target p99 ≤ 200 ns uncontended, ≤ 3 µs contended. Two perf
    cliffs: (1) `VecDeque` waiter growth — a 1000-waiter burst doubles capacity ~10× × ~260 ns/grow =
    ~2.6 µs of resize **with the per-key entry lock held**, blocking every other acquire on that key →
    p99 jumps to ~3 µs. (2) per-waiter `futex(FUTEX_WAKE)` ≈ 300 ns × 4 locks × 100 k commits/sec =
    400 k syscalls/sec = ~120 ms/sec = **~12% of one core burned on wakes**. Batched wake = O(1) syscall
    per release.

  - **Requirements** — write in `crates/txn/notes/42_locks_design.md`:
    - R1. Shared compatible with Shared; Exclusive exclusive of all.
    - R2. **Wound-Wait**: on conflict, if requester older than holder, wound the holder (CAS state to
      Aborted, release its locks); requester is granted.
    - R3. **Strict 2PL**: locks released only at commit/abort (RAII LockGuard).
    - R4. **FIFO waiter queue**: once an exclusive waiter is at the head, block new shared grants.
    - R5. **Slab-allocated waiter pool**: no per-waiter heap alloc; SmallVec<Waiter; 8> + spill to
      pre-sized Vec.
    - R6. **Batched wakes**: `release_all` collects wakers into stack-allocated SmallVec, drops lock,
      then wakes outside the critical section.
    - R7. Acquire latency p99 ≤ 200 ns uncontended; ≤ 3 µs under contention.
    - R8. Zero deadlocks in loom 4-txn 4-key race; zero starvation in stress.

  - **Design walk-through** — answer each:
    - D1. Compatibility matrix: write the 2×2 table.
    - D2. Wound-Wait: write the algorithm. Why does the requester wound the holder (not the other way)?
    - D3. FIFO waiter discipline: how do you ensure an exclusive waiter at the head doesn't get
      shared-grant starvation?
    - D4. Waiter pool: write the SmallVec spill logic. When is the slab-allocated pool used?
    - D5. Batched wakes: write the algorithm. Why drop the lock BEFORE waking?
    - D6. **Pre-mortem (load-bearing)**: write the VecDeque-growth burst trace (1000 waiters × 10 grows
      × 260 ns = 2.6 µs of resize overhead with per-key entry lock held). Then write the futex-per-wake
      trace (4 locks × 100 k commits/sec = 400 k syscalls/sec = 12% CPU).
    - D7. **Anti-shortcut audit**: (a) `VecDeque` waiters (heap-growth under burst, see Pre-mortem);
      (b) per-waiter `Waker::wake()` (one futex each); (c) re-timestamping a wounded txn on restart (it never
      ages into priority — destroys Wound-Wait's "eventually oldest" liveness argument; wounded txns MUST
      restart with their ORIGINAL timestamp).
  - 🧮 **Paper drill** — write in `notes/42_locks_drill.md`:
    - D-STRUCT on the 2PL lock table with 2 txns × 2 keys: T1 (id=10) wants X(k1) then X(k2); T2 (id=20)
      wants X(k2) then X(k1). Draw the `DashMap<Key, LockEntry>` (2 entries k1,k2; each `holders` +
      `waiters`). Hand-execute the strict-2PL schedule step by step, redrawing each `LockEntry` after every
      acquire/wound/release. Mark the **growing phase** (T1 holds k1, then k2 — lock count only rises) and the
      **shrinking phase** (release-all at commit — count only falls); show the wound that breaks the would-be
      deadlock and which holder gets aborted.
    - D-RMW on the lock-word CAS for one `LockEntry`: model `holders` as an atomic mode-word; build the
      step table where T1 and T2 race `cmpxchg(Free → Exclusive)` on k1. Columns = T1/T2/the lock word. Show
      the double-grant a plain `load; if free { store(Exclusive) }` causes vs the CAS that lets exactly one
      win; annotate atomicity (CAS-reads-latest) vs ordering (the acquire on the granting CAS).
    - D-HB on lock-release → next-acquire handoff: two columns (T1 releases X(k1) at commit; T2 then
      acquires X(k1)). Mark the Release (T1's release store / CAS) and the Acquire (T2's acquiring CAS); draw
      the RA edge; chain to verdict "T2 sees all of T1's committed writes guarded by k1." NEGATIVE: weaken the
      release to `Relaxed` and draw the interleaving where T2 acquires but reads T1's pre-commit value.
    - D-COST waiter-burst + wake ledger: (a) VecDeque growth — a 1000-waiter burst doubles capacity 1→1024 ≈
      10 grows × ~260 ns/grow (realloc + memcpy under arena contention) ≈ **~2.6 µs of resize with the
      per-key entry lock held**, vs slab/SmallVec pre-sized ≈ ~50 ns ⇒ **~50× cliff** on p99 acquire;
      (b) futex wake — per-waiter wake = 8 × ~300 ns syscalls vs 1 batched wake = ~300 ns ⇒ 8×/release, and
      at 4 locks × 100 k commits/sec = 400 k wakes/sec × ~300 ns ≈ 120 ms/sec ⇒ **~12% of one core** burned.
      Hand-compute all four numbers; verdict vs the acquire bar (p99 ≤200 ns uncontended / ≤3 µs contended).
  - **Output**: `notes/42_locks_design.md` with R1–R8, D1–D7, the burst + futex bad-interleaving sketches;
    plus `notes/42_locks_drill.md` with the D-STRUCT lock-table trace + D-RMW lock-word CAS + D-HB handoff +
    D-COST waiter-burst/wake ledger.

  #### Phase 2 — Write the failing tests (~30 min)
  Tier annotated. Cross-primitive trap composes the W42 slab/SegQueue (lifecycle) pool here.

  - [ ] **(SENIOR)** `lock_wound_wait_older_wins` — 4 txns race on 4 keys; assert older always wins; no deadlock.
    Exercises R2.
  - [ ] **(SENIOR)** `lock_strict_2pl_no_early_release` — instrument: assert lock held from acquire to commit; no
    release in between. Exercises R3.
  - [ ] **(SENIOR)** `lock_fifo_exclusive_no_starvation` — exclusive waiter at head; new shared requests; assert
    exclusive runs before next shared. Exercises R4.
  - [ ] **(EXPERT, anti-shortcut — bar (c))** `bench_lock_burst_no_vec_growth` — 1000 waiters in 1 ms burst on hot key;
    instrument heap allocator; assert no Vec growth (uses slab/SmallVec). `VecDeque` passes correctness, fails
    the bar. Exercises R5.
  - [ ] **(EXPERT, anti-shortcut — bar (c))** `bench_lock_release_one_syscall` — `release_all` of 8 waiters;
    assert exactly 1 wake syscall (not 8). Mechanism, named: a `cfg(target_os = "linux")` **futex-count shim**
    (test-only `AtomicU64` bumped on the wake path). Linux-runner note: darwin has no futex to count — this
    test runs on the Linux runner only. Exercises R6.
    ```rust
    #[cfg(target_os = "linux")]
    #[test]
    fn bench_lock_release_one_syscall() {
        let (mgr, holder) = eight_waiters_parked_on(key(1)); // 8 waiters blocked in futex_wait
        let before = FUTEX_WAKE_COUNT.load(Ordering::SeqCst); // linux futex-count shim
        mgr.release_all(&holder);
        let wakes = FUTEX_WAKE_COUNT.load(Ordering::SeqCst) - before;
        assert_eq!(wakes, 1,
            "release_all of 8 waiters issued {} futex_wakes — expected exactly 1 batched wake", wakes);
    }
    ```
  - [ ] **(EXPERT, cross-primitive trap)** `lock_waiter_pool_reuses_lifecycle_slab` — the waiter pool must reuse the
    same slab/`SegQueue<u32>` freelist pattern built in `lifecycle.rs`. Run 10 k acquire/release cycles; assert
    the global allocator counter is flat after warmup (the slab is the single source of waiter storage). Passes
    in isolation with a fresh `Vec` per entry; fails this composed no-alloc assertion. Exercises R5.
  - [ ] **(EXPERT, loom)** `loom_lock_4txn_4key_no_deadlock` — `#[cfg(loom)]`; assert no schedule blocks forever.
    Exercises R8.
  - [ ] **(negative-control, test-the-test)** `loom_lock_model_must_be_slow` — asserts `loom_lock_4txn_4key_no_deadlock`
    wall time ≥ 100 ms; a fast loom run explored one schedule and proves nothing about deadlock-freedom.

  #### Phase 3 — Make the tests pass, one failure at a time (~90 min)
  Ordered newbie-first, expert-last — the ladder you actually walk.

  - **(SENIOR) When `lock_fifo_exclusive_no_starvation` reports the exclusive waiter waiting forever:**
    - Symptom: exclusive waiter at head; continuous stream of shared requests; shared grants succeed
      (compatible with existing shared holders); exclusive never gets the lock.
    - Why: waiter starvation — shared-lock convoy forms; exclusive waiter never wakes.
    - Fix: FIFO waiter queue; once an exclusive waiter is at the head, block new shared grants. New
      shared requests join the waiter queue behind the exclusive waiter.
    - Cost avoided: unbounded starvation of the writer = a liveness correctness bug (bar (c) violation),
      not a slowdown; the fairness check is a single comparison on the grant path.
    - Muscle: any shared/exclusive lock with waiters needs explicit fairness — without it, the
      exclusive path can starve under read load. Reapplies at: W74 matching-engine read/write order
      book, W14 bufpool page-table RW.

  - **(SENIOR) When `lock_wound_wait_older_wins` reports livelock:**
    - Symptom: T1 and T2 repeatedly wound each other on k1/k2, both abort, both retry, and the cycle repeats —
      zero forward progress; the test's 1 s watchdog fires.
    - Why: the age discipline is broken. Under CORRECT Wound-Wait (Rosenkrantz et al.) mutual wounding under
      fixed ages is IMPOSSIBLE — wounding is one-directional (only a strictly older requester wounds a younger
      holder; a younger requester waits), and exactly one of any pair is older. Livelock appears only when the
      implementation breaks the discipline: (a) it wounds in BOTH directions (a younger requester also wounds),
      or (b) it assigns the wounded txn a NEW timestamp on retry — the txn never ages into priority and can be
      wounded forever.
    - Fix: wound iff `requester.ts < holder.ts` (older wounds; younger WAITS), and a wounded txn restarts with
      its ORIGINAL timestamp. A restarted txn keeps its age while ever-younger competitors arrive, so it
      monotonically becomes the oldest in any conflict set — the "eventually oldest" monotone property IS the
      liveness proof: once oldest, nothing can wound it and it wounds through to commit.
    - Cost avoided: livelock = zero forward progress (bar (c) violation), unbounded latency; the fix is free at
      runtime (one comparison direction + reusing the stored timestamp on restart).
    - Muscle: deadlock-prevention liveness rests on the age invariant — one-directional wound + restart with
      the ORIGINAL timestamp makes every txn eventually oldest; this exact invariant feeds W67 consensus-raft
      election-retry priority and W74 matching-engine order-cancel retry under wound.
      Reapplies at: W67 consensus-raft leader election, W74 matching-engine order-cancel retry.

  - **(EXPERT, anti-shortcut — bar (c) violation) When `bench_lock_burst_no_vec_growth` reports 10 reallocs per burst:**
    - Symptom: 1000 waiters in 1 ms burst → VecDeque grows ~10 times (1 → 2 → ... → 1024); each grow ~260
      ns under arena contention; total 2.6 µs of pure resize overhead with per-key entry lock held,
      blocking every other acquire on this key.
    - Why: `waiters: VecDeque` heap-growth under burst — VecDeque doubles capacity ~10 times under
      sustained push pressure; each grow holds the per-key entry lock, blocking *every other* acquire on
      this key for those 2.6 µs.
    - Fix: `SmallVec<[Waiter; 8]>` for the common case (≤8 waiters); spill to a pre-sized
      `Vec::with_capacity(max_waiters)` only when contention is detected. Bar (c): use a slab-allocated
      waiter pool keyed on `next_waiter_id` (no per-waiter heap alloc on the hot path); waiters live in
      a freelist.
    - Cost avoided: ~2.6 µs/burst → ~50 ns/burst = **50× faster burst handling**; p99 acquire latency
      drops from ~3 µs under contention to ~300 ns; no lock-entry contention amplification.
    - Muscle: "amortize the data-structure growth cost, or pre-size it" — VecDeque/Vec doubling is great
      average-case but catastrophic for tail latency. Reapplies at: W74 matching-engine OrderBook
      price-level queue, W84 runtime-thread-per-core cross-shard inbox, W77 messaging-aeron retransmit
      buffer.

  - **(EXPERT, anti-shortcut — bar (c) violation) When `bench_lock_release_one_syscall` reports 8 futex_wake syscalls:**
    - Symptom: release_all on 8 waiters → 8 futex_wake calls × 300 ns = 2.4 µs of syscall time per
      release; at 4 locks × 100 k commits/sec = 400 k syscalls/sec = ~120 ms/sec = 12% CPU on wakes
      alone.
    - Why: under Tokio default, `Waker::wake()` calls into the executor's notify path which ends up in
      `futex(FUTEX_WAKE)` (~300 ns per call). Plus `Waker::wake()` is move-by-value so it `Arc::drop`s
      internally.
    - Fix: batch wakes — `release_all` collects all wakers into a stack-allocated `SmallVec<Waker, 8>`,
      then drops the lock-entry guard, then iterates wakes outside the lock. For the cross-shard path
      (waiter on different core), use `crossbeam-channel` MPSC with single-element batching: write all
      wakes as one message, single futex wake at the channel boundary. Reduces syscalls from
      O(waiters) to O(1) per release.
    - Cost avoided: 4 syscalls × 300 ns → 1 syscall × 300 ns per release = ~75% reduction in syscall
      time; 120 ms/sec → 30 ms/sec; tail latency p99 on release drops from ~2 µs to ~500 ns.
    - Muscle: "syscalls in the hot path are tax — batch them or eliminate them." Reapplies at: W74
      matching-engine fill-notification fan-out, W77 messaging-aeron subscriber notifications, W83
      runtime-thread-per-core cross-shard wake batching.

  - **5-year failure mode** (write in `notes/42_locks_followup.md` after green): when MVCC + OCC
    becomes the dominant pattern (read-heavy workloads), 2PL is the fallback for write-heavy. State
    migration: keep LockManager for adversarial workloads; OCC for read-heavy default.
- [ ] **Build**: `crates/txn/src/deadlock.rs` — wait-for graph + cycle detection (Tarjan SCC) as a fallback for explicit
  deadlock detection. (~total 155 min across 3 phases)
  - 🔧 **Why this matters**: Wound-Wait prevents lock-only deadlocks; once you mix in OCC, cycles can form
    in subtle ways. The periodic detector scans the wait-for graph + breaks cycles. Per-scan allocation churn
    (1.3 MB malloc/free per tick) → foreground latency feedback. W74 matching-engine reuses Tarjan + scratch
    reuse pattern.

  #### Phase 1 — Problem, prediction, design (~35 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in
    `crates/txn/notes/42_deadlock_prediction.md`:
    - P1. Name the graph representation + cycle algorithm you'd reach for, sight-unseen. Exact import line
      (or "hand-rolled adjacency").
    - P2. Predict the per-scan allocation cost at 10 k active txns with a fresh `HashMap` + `Vec<Vec<_>>`
      built each scan: total bytes malloc'd + total ms of malloc/memset/free. Then predict it with reused
      scratch buffers.
    - P3. Predict what happens if a victim is chosen from a graph snapshot, but an edge in that cycle is
      released before the abort fires. One word (correct/false-positive/panic).
    - P4. In a cycle T1→T2→T3→T4→T1, predict which victim minimizes work-loss. One TxId.
    - P5. List every Phase-2 test you predict your first attempt fails.

  - *Mental model*: periodic detection of deadlock cycles in the wait-for graph;
    abort the youngest in each cycle. Tarjan SCC is O(V+E); the catch is buffer reuse across scans (1.3 MB
    per scan kills latency feedback) and re-validating the snapshot before aborting.

  - *Contract*: `DeadlockDetector { graph: HashMap<TxId, NodeId>, adj: Vec<Vec<NodeId>>, tarjan_stack: Vec<NodeId> }`
    (all scratch owned + reused); `fn run_once(&mut self, lock_table: &LockManager) -> SmallVec<[TxId; 4]>`
    (returns victims, clears scratch at entry, never reallocates in steady state).

  - *Back-of-envelope* (bar (c)): detector runs every 100 ms. A fresh-allocation scan at 10 k txns mallocs
    ~1.3 MB (HashMap + `Vec<Vec>` adjacency + Tarjan stacks) and frees ~1.3 MB = ~2 ms of malloc/memset/free
    per scan — and that allocator traffic **contends with foreground `begin()` slab/Arc allocs**, doubling
    p99 begin() latency for ~10 ms windows under a deadlock storm. Reused scratch (`.clear()`, keep capacity)
    = ~50 µs/scan = **~40× cliff** and zero foreground feedback.

  - **Requirements** — write in `crates/txn/notes/42_deadlock_design.md`:
    - R1. `run_once()` builds wait-for graph, runs Tarjan SCC, returns victim TxIds.
    - R2. **Victim policy**: within a cycle, kill the youngest (highest TxId).
    - R3. **Cycle re-validation before abort**: every edge in the chosen cycle still present at abort
      time; skip if not (avoid false-positive aborts).
    - R4. **Reusable scratch buffers** — `graph: HashMap`, `adj: Vec<Vec<NodeId>>`, `tarjan_stack:
      Vec<NodeId>`; `clear()` per scan, no malloc/free.
    - R5. Detector is monotone — only adds presently-valid wait edges.
    - R6. Scheduler: thread runs `run_once` every interval (100 ms default); on-demand trigger from
      lock-acquire timeout.
    - R7. Detection latency ≤ 2× interval; zero false positives under acyclic workloads.

  - **Design walk-through** — answer each:
    - D1. Wait-for graph construction: for each waiter (T, K, mode), add edge T → holder(K) (if
      incompatible). Write the algorithm.
    - D2. Tarjan SCC: sketch the algorithm; identify the scratch buffers it needs.
    - D3. Youngest-victim policy: why? What does it minimize (work-loss)?
    - D4. Cycle re-validation: write the check. When can a previously-detected cycle vanish?
    - D5. Scratch reuse: where does the DeadlockDetector own its buffers? When are they cleared?
    - D6. **Pre-mortem (load-bearing)**: write the per-scan allocation trace — 1.3 MB malloc + 1.3 MB
      free = ~2 ms of malloc/memset/free per scan; allocator contention with foreground begin() Arc
      allocs doubles p99 begin() latency for ~10 ms windows under storm.
    - D7. **Anti-shortcut audit**: (a) fresh `HashMap`/`Vec<Vec>` per scan (allocator storm, see Pre-mortem);
      (b) recursive Tarjan (stack overflow at 10 k-deep wait chains — use the iterative explicit-stack form);
      (c) aborting straight off the snapshot (false-positive aborts — must re-validate, R3).
  - 🧮 **Paper drill** — write in `notes/42_deadlock_drill.md`:
    - D-WALK on a concrete 2-txn cycle: T1(id=10) holds X(k1) and waits for X(k2); T2(id=20) holds X(k2) and
      waits for X(k1). Build the wait-for graph by hand from the lock table (for each waiter add edge
      `T → holder(K)`): draw nodes T1,T2 and the two edges. Run the cycle walk → detect the cycle T1→T2→T1;
      apply the youngest-victim policy → pick T2 (highest id). Then the **re-validation** step: T2 was already
      manually aborted between snapshot and abort — re-check every edge, find T2→T1 gone, skip the abort
      (no false-positive). Output the invariant: "detection is sound iff every cycle edge is re-confirmed
      present at abort time."
    - D-STRUCT on the reused-scratch detector over a 3-node graph (T1→T2→T3→T1): hand-execute the iterative
      Tarjan SCC step by step on the owned `adj: Vec<Vec<NodeId>>` + `tarjan_stack`, redrawing the explicit
      stack and lowlink array after each push/pop; then re-run a second scan after `.clear()` and show the
      buffers reuse the same capacity (no realloc) — the linearization concern here is that the graph snapshot
      is read non-atomically, so mark where a concurrently-added edge could appear as a phantom cycle.
  - **Output**: `notes/42_deadlock_design.md` with R1–R7, D1–D7, the per-scan alloc bad-trace;
    plus `notes/42_deadlock_drill.md` with the D-WALK wait-for-graph cycle + D-STRUCT Tarjan trace.

  #### Phase 2 — Write the failing tests (~30 min)
  Tier annotated.

  - [ ] **(SENIOR)** `deadlock_4txn_cycle_picks_youngest` — T1→T2→T3→T4→T1 cycle; assert T4 picked. Exercises R2.
  - [ ] **(SENIOR)** `deadlock_mixed_2pl_occ_detected` — synthesize OCC-version-conflict edge + 2PL wait-for edge
    forming a cycle; assert detected. Exercises R1.
  - [ ] **(SENIOR)** `deadlock_no_false_positive` — linear chain T1→T2→T3 (no cycle); assert no aborts. Exercises R7.
  - [ ] **(SENIOR)** `deadlock_edge_policy_wound_wait_vs_wait_die` — the detector must only add PRESENTLY-VALID
    wait edges (R5), and which edges exist depends on the prevention policy. The divergence case: an OLD
    requester (ts=10) hits a key held by a YOUNG txn (ts=20). Under Wound-Wait NO edge ever forms (the older
    requester wounds; nobody waits); under Wait-Die the older requester WAITS (edge old→young). A detector that
    hardcodes "requester always waits" fabricates edges Wound-Wait already resolved — phantom cycles, spurious
    victims. Exercises R5.
    ```rust
    #[test]
    fn deadlock_edge_policy_wound_wait_vs_wait_die() {
        let old_txn = txn_with_ts(10);
        let young   = txn_with_ts(20); // holds X(k1)
        // Wound-Wait: the strictly-older requester wounds — the graph must contain NO old→young edge.
        let g = graph_after_conflict(Policy::WoundWait, &old_txn, &young);
        assert!(!g.has_edge(old_txn.id, young.id),
            "Wound-Wait produced a wait edge for an OLDER requester — it must wound, not wait");
        assert_eq!(young.state(), State::Aborted, "the younger holder must be wounded");
        // Wait-Die: the older requester WAITS — this edge is exactly what the detector scans.
        let g = graph_after_conflict(Policy::WaitDie, &old_txn, &young);
        assert!(g.has_edge(old_txn.id, young.id),
            "Wait-Die: the older requester waits — the detector must see the old→young edge");
    }
    ```
  - [ ] **(EXPERT)** `deadlock_revalidation_skips_stale_cycle` — T1 in cycle observed; T1 manually aborts between
    detection and abort step; assert no spurious abort of other cycle members. Exercises R3.
  - [ ] **(EXPERT, anti-shortcut — bar (c))** `bench_deadlock_scratch_reuse_no_alloc` — 100 run_once calls; instrument
    allocator; assert allocator count unchanged after warmup. Fresh-alloc passes correctness, fails the bar.
    Exercises R4.
  - [ ] **(SENIOR)** `deadlock_latency_under_2x_interval` — synthesize cycle; assert detection within 200 ms (2 × 100
    ms interval). Exercises R7.
  - [ ] **(negative-control, test-the-test)** `bench_deadlock_scan_physically_plausible` — paired with the no-alloc
    bench: asserts a 10 k-txn scan completes in `10µs ≤ t ≤ 50ms`; outside that band means the synthetic graph
    is empty (nothing measured) or the scan is pathological, so the alloc assertion is meaningless.

  #### Phase 3 — Make the tests pass, one failure at a time (~90 min)
  Ordered newbie-first, expert-last — the ladder you actually walk.

  - **(SENIOR) When `deadlock_4txn_cycle_picks_youngest` reports an older victim:**
    - Symptom: cycle is T1→T2→T3→T4→T1; detector picks T1 instead of T4.
    - Why: victim choice is wrong — within a cycle, the work-minimizing choice is the youngest
      (highest TxId = least committed work).
    - Fix: victim choice: within a cycle, kill the youngest (highest TxId). The youngest holds the
      fewest locks (just started); aborting it loses the least committed work.
    - Muscle: deadlock-breaking minimizes work-loss; youngest = least-committed = correct choice.
      Reapplies at: W67 consensus-raft leader-election tiebreak.

  - **(EXPERT) When `deadlock_revalidation_skips_stale_cycle` reports a false-positive abort:**
    - Symptom: detector finds T1→T2→T3→T1; between detect and abort, T2 manually aborts (was
      wounded); detector aborts T3 anyway; T3 was no longer in a cycle.
    - Why: graph snapshot torn — building the graph reads lock state non-atomically; a snapshot may
      show a phantom cycle (edge added after a concurrent release). The detector aborts a victim that
      wasn't actually deadlocked.
    - Fix: re-validate the cycle before aborting (every edge still present); skip if not. Pseudocode:
      `for cycle in cycles { if cycle.iter().all(|edge| lock_table.still_has_wait(edge)) { abort_youngest(cycle); } }`.
    - Cost avoided: a false-positive abort kills a txn that wasn't deadlocked = wasted work + spurious
      retries (bar (c) correctness violation); the re-check is O(cycle length), negligible.
    - Muscle: any optimistic snapshot algorithm must re-validate at action time; the snapshot ages.
      Reapplies at: W42 OCC validation, W41 LCS pick-victim snapshot.

  - **(EXPERT, anti-shortcut — bar (c) violation) When `bench_deadlock_scratch_reuse_no_alloc` reports allocator churn per scan:**
    - Symptom: each `run_once` allocates ~1.3 MB (HashMap + Vec<Vec> + Tarjan stacks), frees ~1.3 MB. At
      10 k active txns × 100 ms tick = ~2 ms of malloc/memset/free per scan; under deadlock-storm,
      foreground p99 begin() latency doubles for ~10 ms windows.
    - Why: per-scan graph allocation churn — each `run_once` builds a fresh `HashMap<TxId, NodeId>` +
      `Vec<Vec<NodeId>>` adjacency + Tarjan's intermediate stacks; the allocation pressure feeds back
      into the global allocator → contention with foreground `begin()` Arc allocs.
    - Fix: **reusable scratch buffers** — `DeadlockDetector` owns persistent `graph: HashMap<TxId,
      NodeId>`, `adj: Vec<Vec<NodeId>>`, `tarjan_stack: Vec<NodeId>`. `run_once` calls `.clear()` (O(N)
      but no realloc/free) and reuses capacity. Optional: `BumpAllocator` (per-scan reset arena) for
      transient nodes — `arena.reset()` at scan end frees all in O(1).
    - Cost avoided: ~2 ms/scan of allocator work → ~50 µs/scan = **40× faster scan**; allocator
      contention with foreground vanishes; p99 begin() flat-lines.
    - Muscle: "reuse buffers across rounds when the workload is periodic" — anything that runs on a
      timer should own its scratch space. Reapplies at: W74 matching-engine snapshot serialization
      scratch buf, W84 runtime reactor per-tick batched events, W92 ledger VOPR per-scenario state.

  - **5-year failure mode** (write in `notes/42_deadlock_followup.md` after green): when wait-free
    OCC dominates and 2PL is rare, the deadlock detector becomes a debug-only assertion. State
    migration: gate detector behind `cfg(feature = "deadlock-debug")` once OCC ratio > 95%.
- [ ] Commit + log (~10 min)
  - *Note (day-load + filler-cut)*: Wednesday holds exactly two full Builds — `locks.rs` (155) + `deadlock.rs`
    (155) + commit/log (10) = 320 min (~5.3 h), under the cap. The standalone "Test: 4 txs in a cycle picks
    youngest" and "Read Cassandra TWCS doc" one-liners are folded — the test IS the deadlock Build's Phase-2
    `deadlock_4txn_cycle_picks_youngest` trap, and the Cassandra/Scylla doc is now a lookup pointer in the
    frontmatter References + the TWCS Build's Phase-1 design walk. The TWCS scaffold Build (130 min) moved to
    **Friday** and its drop-window delta (60) + inheritance audit (20) to **Saturday** to keep both Wednesday
    and Thursday under the ~5-6 h day cap.

**Thursday — `txn` OCC + write phase**

- [ ] **Build**: `crates/txn/src/occ.rs` — `OccValidator` validates read-set version stamps at commit. Used for
  read-heavy workloads. (~total 155 min across 3 phases)
  - 🔧 **Why this matters**: OCC dominates 2PL on read-heavy workloads (no read locks). BTreeMap for ReadSet
    → 20× slower than SmallVec at typical N=4-20 reads. Validation-write race → lost updates. Downstream W74
    matching-engine OrderBook depth, W80 ledger batch read-set, W95 mini-db memtable lookup cache reuse the
    SmallVec pattern.

  #### Phase 1 — Problem, prediction, design (~35 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in
    `crates/txn/notes/42_occ_prediction.md`:
    - P1. Name the container you'd reach for to hold a read-set of (key, version), sight-unseen. Exact import.
    - P2. Predict per-read insert + per-validate probe cost with `BTreeMap<Key, Version>` at N=16 reads. Then
      predict it with a sorted `SmallVec<[(Key,Version);16]>`. Four numbers in ns.
    - P3. Predict the concurrent outcome: 2 txns read key k@v_old, both update v_old→v_new, both validate
      with no lock between validate and apply. How many updates survive? One number + the anomaly name.
    - P4. Predict whether point-version tracking catches a concurrent insert into a range a txn scanned.
      yes/no + the anomaly name.
    - P5. List every Phase-2 test you predict your first attempt fails.

  - *Mental model*: read freely, track version stamps; validate at commit that no version changed; abort on
    conflict. Serializability via "any committed schedule equivalent to serial order"; OCC dominates 2PL on
    read-heavy workloads (no read locks).

  - *Contract*: `OccValidator`; `read_set: SmallVec<[(Key, Version); 16]>` (sorted on commit, binary-searched);
    `fn track_read(&mut self, key, version)`; `fn validate(&self, store) -> bool` (every (key, ver) matches
    `store.current_version(key)`); range predicates tracked as `RangePredicate { start, end }`. Read-only txns
    never abort.

  - *Back-of-envelope* (bar (c)): typical OCC txn = 4–20 reads. `BTreeMap` insert ≈ 100 ns (log N + node
    malloc, fragmented across allocations reached by indirection); at 2 M reads/sec × ~50 ns alloc = ~100 ms/sec
    = ~10% of one core on BTreeMap node alloc, and validation ~3× slower than a sorted Vec. Sorted
    `SmallVec` (inline, one cache line for small N): push ~5 ns, binary-search probe ~30 ns, **zero alloc** for
    N≤16 = **~20× build / ~3× validate** cliff. The crossover where BTreeMap wins is N≈64+.

  - **Requirements** — write in `crates/txn/notes/42_occ_design.md`:
    - R1. `validate(read_set, store)` returns true iff every (key, expected_version) matches
      store.current_version(key).
    - R2. **Validation + apply atomic with respect to commit point** — global serial validation lock OR
      2-phase OCC (lock-then-validate Silo-style).
    - R3. **ReadSet is `SmallVec<[(Key, Version); 16]>`** — sorted on commit; binary search in
      cache-line-resident array; zero alloc for common case (N ≤ 16).
    - R4. **Phantom reads handled** — range predicates tracked in read_set, not just point keys.
    - R5. **Memory cap on long read-set** — spill to Box<[(Key, Version)]> for large scans; fall back to
      2PL if abort rate too high.
    - R6. Read-only txns never abort.
    - R7. Read-only throughput ≥ 10× 2PL; abort rate proportional to write contention.

  - **Design walk-through** — answer each:
    - D1. The two-phase OCC algorithm: read, validate, write. Where's the linearization point?
    - D2. Serial validation lock vs 2-phase OCC: trade-offs. Why does Silo pick 2-phase?
    - D3. SmallVec sizing: why 16 inline slots? What workload has N > 16 (range scans)?
    - D4. Phantom reads: write the trace where range query misses a future insert; OCC validation
      misses the anomaly.
    - D5. Fallback to 2PL: write the policy. Abort rate threshold → switch strategy.
    - D6. **Pre-mortem (load-bearing)**: write the BTreeMap-alloc trace at 2 M inserts/sec × 50 ns
      alloc = 100 ms/sec = 10% CPU on BTreeMap node alloc; OCC validation 3× slower than textbook.
    - D7. **Anti-shortcut audit**: (a) `BTreeMap` read-set (per-read malloc, see Pre-mortem); (b) validate
      then apply with no lock between (lost-update race); (c) point-version tracking only (misses phantoms).
  - 🧮 **Paper drill** — write in `notes/42_occ_drill.md`:
    - D-WALK on 2 concrete OCC txns over keys k1@v5, k2@v3: T1 read-phase reads k1@v5,k2@v3 (read_set =
      [(k1,5),(k2,3)]); T2 read-phase reads k1@v5, writes k1→v6 and **commits first**. Hand-run T1's
      validation: probe `store.current_version(k1)` = 6 ≠ expected 5 → **abort T1**. Trace it again with the
      commit order swapped (T1 validates+applies before T2) → both commit, serial order T1<T2. Then the
      phantom case: T1 range-scans [a,z], records `RangePredicate{a,z}`; T2 inserts key c and commits;
      validation's range-overlap check catches it → T1 aborts. Output the invariant: "the committed schedule
      is equivalent to a serial order iff every read-set version still matches at commit."
    - D-HB on the validation→apply linearization: two columns (T1 does validate-read of `current_version(k)`
      then apply-store of the new version; T2 does the same on the same key). Mark the per-key write-lock
      acquire (Acquire) and release (Release) that Silo holds across validate+apply; draw the RA edge proving
      T2's validate reads T1's applied version. NEGATIVE: drop the write-lock and draw the interleaving where
      both T1,T2 validate against v_old between each other's apply → lost update (both commit, one write lost).
    - D-COST read-set ledger at N=16 reads: BTreeMap column — insert ≈ 100 ns (log-N walk + ~50 ns node
      malloc) × 16 ≈ **1.6 µs build + 16 allocs**, probe ≈ 100 ns (pointer-chase across fragmented ~280 B
      nodes) × 16 ≈ 1.6 µs validate; sorted-SmallVec column — push ~5 ns × 16 ≈ **80 ns build, 0 allocs**
      (**~20× cliff**), one in-place sort ~200 ns + binary-search probe ~30 ns × 16 ≈ 480 ns + 200 ns ≈
      ~0.7 µs validate (~3×). Hand-compute all four numbers; verdict: SmallVec fits the zero-alloc hot-path
      bar; BTreeMap busts it (at 2 M reads/sec, ~100 ms/sec of pure malloc).
  - **Output**: `notes/42_occ_design.md` with R1–R7, D1–D7, the BTreeMap-alloc bad-trace;
    plus `notes/42_occ_drill.md` with the D-WALK validation trace + D-HB validate/apply ordering + D-COST
    read-set ledger.

  #### Phase 2 — Write the failing tests (~30 min)
  Tier annotated.

  - [ ] **(NEWBIE)** `occ_read_only_no_abort` — 16 read-only txns simultaneously; assert all commit, no aborts.
    Exercises R6.
  - [ ] **(SENIOR)** `occ_write_write_one_commits` — 16 write-heavy on same key; assert exactly one commits per
    epoch. Traps validate-then-apply with no lock (lost update). Exercises R1.
  - [ ] **(EXPERT, anti-shortcut — bar (c))** `bench_occ_readset_smallvec_no_alloc` — 16 read OCC txn; instrument
    allocator; assert zero allocations for read_set build. `BTreeMap` passes correctness, fails the bar.
    Exercises R3.
  - [ ] **(EXPERT)** `occ_phantom_read_caught` — range scan; concurrent insert in range commits; validation aborts
    the scan. Traps point-only versioning. Exercises R4.
    ```rust
    #[test]
    fn occ_phantom_read_caught() {
        let store = store_with(&[("a", 1), ("m", 1), ("z", 1)]);
        let mut t1 = store.begin_occ();
        t1.range_scan("a".."z"); // must record RangePredicate { "a".."z" }, not just 3 point versions
        let mut t2 = store.begin_occ();
        t2.insert("c", 42);
        t2.commit().unwrap(); // commits INSIDE t1's scanned range, between scan and validation
        assert!(matches!(t1.commit(), Err(TxnError::Validation(Conflict::Phantom { .. }))),
            "t1 committed over a phantom — point-version tracking missed the range insert of \"c\"");
    }
    ```
  - [ ] **(SENIOR)** `occ_long_readset_fallback_2pl` — txn with 10 k reads; assert ReadSet spills + abort-rate
    triggers fallback. Exercises R5.
  - [ ] **(EXPERT, anti-shortcut — bar (c))** `bench_occ_readset_validation_10x_2pl` — read-heavy bench; assert OCC ≥
    10× 2PL throughput. Exercises R7.
  - [ ] **(negative-control, test-the-test)** `bench_occ_throughput_physically_plausible` — paired with the 10×
    bench: asserts both OCC and 2PL legs land in `1e5 ≤ ops/s ≤ 5e8`; a 2PL leg reading near-zero (the bench
    didn't actually contend) makes the "10×" ratio meaningless.

  #### Phase 3 — Make the tests pass, one failure at a time (~90 min)
  Ordered newbie-first, expert-last — the ladder you actually walk.

  - **(SENIOR) When `occ_write_write_one_commits` reports both txns committing (lost update):**
    - Symptom: 2 txns read key k version v_old, both update v_old → v_new1 and v_old → v_new2; both
      validate (each sees v_old); both apply; one update lost.
    - Why: validation-write race — between validate() and apply(), another txn commits same key.
    - Fix: validate + apply under a serial commit lock OR use 2-phase OCC (Silo-style) with
      lock-then-validate. Silo: acquire write-locks → validate read-set → apply writes → release. The
      lock prevents the race window.
    - Cost avoided: a lost update = silent data corruption (bar (c) correctness violation); Silo's
      per-key write-lock is held only for the µs-scale validate+apply, not the whole txn.
    - Muscle: OCC's validation + apply must be atomic — serial lock or per-key write-lock during
      validation. Reapplies at: W67 consensus-raft commit, W80 ledger txn commit.

  - **(EXPERT) When `occ_phantom_read_caught` reports the scan committing (anomaly):**
    - Symptom: T1 range-scans [a, z]; T2 inserts new key c in range and commits; T1's validation
      doesn't catch it (only point versions tracked); T1 commits with stale view.
    - Why: phantom reads — range query versions not captured.
    - Fix: track range predicates in read_set (predicate locks). For each range scan, record the
      `RangePredicate { start, end }` in read_set; at validation, check that no key was inserted in
      range since the scan.
    - Cost avoided: an undetected phantom = non-serializable schedule committed (bar (c) correctness
      violation); predicate tracking adds one range entry per scan, validated with one range-overlap check.
    - Muscle: range queries need predicate-level versioning, not point. Reapplies at: W74
      matching-engine order-book range queries, W95 mini-db range scans.

  - **(EXPERT, anti-shortcut — bar (c) violation) When `bench_occ_readset_smallvec_no_alloc` reports allocations per read:**
    - Symptom: 16 reads × 16 OCC txns = 256 allocations; allocator counter increments per read; OCC
      validation ~3× slower than textbook expectation.
    - Why: `BTreeMap<Key, Version>` for ReadSet — per-read insert ≈ 100 ns (BTreeMap log N + allocation
      amortized), per-validate walk ≈ 100 ns × N. For typical OCC txns of 4-20 reads, BTreeMap's
      branching overhead is *worse* than a sorted Vec: the cache footprint is fragmented across
      multiple node allocations (each B-node is ~280 B but reached by indirection). 2 M reads/sec ×
      ~50 ns alloc = ~100 ms/sec malloc time.
    - Fix: `SmallVec<[(Key, Version); 16]>` for ReadSet, sorted on commit. Insert is push (~5 ns);
      validate sorts in-place (~200 ns for 16 entries) then binary-searches each key in the store.
      **Zero heap alloc** for common case (N ≤ 16); spill to `Box<[(Key, Version)]>` for large scans.
      SmallVec is inline-allocated in the Txn struct → no pointer-chase, fits in one cache line for
      small N.
    - Cost avoided: 100 ns/insert → 5 ns/insert = **20× faster build**; 100 ns/probe → 30 ns/probe
      (binary search in cache-line-resident array) = **3× faster validate**; zero alloc on hot path.
    - Muscle: "sorted Vec beats BTreeMap up to N ≈ 64" — branchy log-N data structures only pay off at
      large N. Reapplies at: W74 matching-engine OrderBook level depth (sorted Vec per level), W80
      ledger batch read-set, W95 mini-db memtable lookup cache.

  - **5-year failure mode** (write in `notes/42_occ_followup.md` after green): when serializable snapshot
    isolation (SSI) becomes standard (CockroachDB-style), OCC's abort rate drops via dependency
    tracking. State migration: add SSI layer on top of OCC.
- [ ] **Build**: `crates/txn/src/commit.rs` — commit path: prepare WAL record → fsync → mark Committed → release locks.
  Abort path: emit CLRs via `recovery::UndoPass` if necessary. (~total 155 min across 3 phases)
  - 🔧 **Why this matters**: commit is the linearization point + durability fence. Single-fsync-per-commit
    caps throughput at 10 k/sec (10× below 100 k target). Serialization inside the fsync window halves
    throughput. Downstream W74 matching-engine durable order log, W80 ledger journal, W77 aeron archive
    reuse group-commit + parallel-serialize patterns.

  #### Phase 1 — Problem, prediction, design (~35 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in
    `crates/txn/notes/42_commit_prediction.md`:
    - P1. Name the primitive you'd reach for to make followers wait for the leader's fsync, sight-unseen.
      Exact import (`tokio::sync::Notify`? `crossbeam-channel`? a condvar?).
    - P2. Predict max commits/sec with one fsync per commit at NVMe fsync ≈ 100 µs. Then predict it with
      group commit (1 ms / 1000-txn batch). Two numbers.
    - P3. Predict the throughput hit of `bincode::serialize(&record)` INSIDE the group's serial section at
      500 ns/record × 1000 followers. One number (µs of serial work per batch) + the % throughput loss.
    - P4. Predict what a concurrent acquirer observes if you release locks BEFORE the state CAS to Committed.
      One line (the stale-state anomaly).
    - P5. List every Phase-2 test you predict your first attempt fails.

  - *Mental model*: commit must guarantee (a) WAL durable before observable, (b) lock release after state
    transition, (c) high throughput via group commit. The strict ordering and the "serialize outside the
    critical section" discipline are non-negotiable.

  - *Contract*: strict sequence `state CAS Active→Preparing → WAL append → WAL fsync → state CAS
    Preparing→Committed → release locks → remove from active`; group-commit leader does one
    `writev + fdatasync + notify_waiters()` per batch; each follower pre-serializes its record into a
    thread-local 4 KB buffer BEFORE entering the group.

  - *Back-of-envelope* (bar (c)): one fsync per commit at NVMe ≈ 100 µs caps throughput at ~10 k commits/sec
    = **10× below the 100 k target**. Group commit amortizes one fsync across the batch → ~5 µs/commit =
    **~20× uplift**. Second cliff: serializing inside the serial section = ~500 ns/record × 1000 followers =
    ~500 µs of serial work per batch = **~2× throughput loss**; pre-serialize in parallel so the section is
    pure I/O.

  - **Requirements** — write in `crates/txn/notes/42_commit_design.md`:
    - R1. Strict order: state CAS Active→Preparing → WAL append → WAL fsync → state CAS Preparing→Committed
      → release locks → remove from active.
    - R2. **WAL Commit record durable BEFORE state becomes Committed** — observability of Committed implies
      durability.
    - R3. **Lock release AFTER state transition** — concurrent acquire observes terminal state, not Preparing.
    - R4. **Leader-based group commit**: one leader, N followers; one fsync per batch. Tunable batch window
      (1 ms or 1000 txns).
    - R5. **Serialize outside the fsync window**: each txn serializes its commit record into a
      thread-local 4 KB buffer BEFORE entering the group; leader only does writev + fdatasync + notify.
    - R6. Crash-safe at kill-9 between any two steps; recovery decides committed-or-aborted consistently.
    - R7. 100 k commits/sec with group commit; group commit ≥ 5× single-fsync throughput.

  - **Design walk-through** — answer each:
    - D1. Write the 6-step commit ordering. For each crash window, what does recovery see?
    - D2. Why lock release AFTER state transition? Trace the race where releasing first causes a stale
      observation.
    - D3. Group commit algorithm: write the leader + follower pseudocode. How does the leader hand off
      to the next leader?
    - D4. Batch window tuning: 1 ms vs 1000 txns. When does each bound fire?
    - D5. Parallel serialization: write the thread-local buffer protocol. What happens if a buffer
      overflows?
    - D6. **Pre-mortem (load-bearing)**: write the single-fsync-per-commit trace at 100 k/sec target →
      10 sec of fsync per wall second (impossible). Then write the serialize-inside-fsync trace at 500
      µs serial × 1000 followers = 500 µs of serial work per batch (50% throughput loss).
    - D7. **Anti-shortcut audit**: (a) one fsync per commit (10× under target, see Pre-mortem);
      (b) serialize inside the serial section (halves group throughput); (c) release locks before the
      Committed CAS (stale-state observation by the next acquirer).
  - 🧮 **Paper drill** — write in `notes/42_commit_drill.md`:
    - D-STRUCT on the commit state machine + durability fence: draw the 6 steps as a sequence
      (`CAS Active→Preparing → WAL append → WAL fsync → CAS Preparing→Committed → release locks → remove
      from active`); hand-execute one full commit, redrawing `state` + "WAL durable?" + "in active?" after
      each step. Mark which single step is the linearization point (the Preparing→Committed CAS) and which is
      the durability fence (the fsync). Show the 2-thread interleaving where a concurrent acquirer that grabs
      the lock only AFTER the Committed CAS observes the correct terminal state.
    - D-WALK on the crash-window recovery decision: take the 6-step sequence and inject a `kill -9` at each
      of the 5 gaps; for each, hand-trace what the WAL contains (Commit record present? fsynced?) and what
      `recover()` decides (commit vs abort). Tabulate gap → WAL contents → recovery verdict; confirm the
      invariant "observable-Committed ⟹ Commit record durable" holds at every gap (the crash before fsync
      must recover as Aborted; the crash after fsync but before the CAS must recover as Committed).
    - D-COST group-commit ledger: hand-compute both legs — naive: 1 fsync/commit × ~100 µs ⇒ **~10 k
      commits/sec**; grouped 1-per-1000: one ~100 µs fsync amortized over 1000 commits ≈ 0.1 µs fsync
      share/commit, window-bound at the ~1 ms batch ⇒ **~100 k/sec**. Cliff ratio ≈ **10×**. Then the second
      loss: serialize-inside-the-critical-section = ~500 ns × 1000 followers = ~500 µs serial work per ~1 ms
      batch ⇒ **~2× throughput loss** — compute the batch wall-time with and without pre-serialization.
      Verdict vs the 100 k/s durable bar: grouped + pre-serialized fits; either regression busts it.
  - **Output**: `notes/42_commit_design.md` with R1–R7, D1–D7, the single-fsync + serialize-inside bad-traces;
    plus `notes/42_commit_drill.md` with the D-STRUCT commit sequence + D-WALK crash-window table + D-COST
    group-commit ledger.

  #### Phase 2 — Write the failing tests (~30 min)
  Tier annotated. Cross-primitive trap composes the W26 wal group-commit + this commit path.

  - [ ] **(SENIOR)** `commit_strict_ordering` — instrument; assert state Preparing AFTER WAL append, state Committed
    AFTER fsync, lock release AFTER state Committed. Mechanism, named: injected instrumentation — a recording
    WAL plus an **injected test Waker** on the lock-release path, each appending to one sequenced event log
    (no OS dependency; runs on darwin and Linux alike). Exercises R1, R2, R3.
    ```rust
    #[test]
    fn commit_strict_ordering() {
        let events = EventLog::new();
        let mgr = TxnManager::with_hooks(events.hooks()); // records WalAppend, WalFsync, StateCas(..), LockRelease
        let txn = mgr.begin();
        txn.write(key(1), val(1));
        mgr.commit(&txn).unwrap();
        let idx = |e: Event| events.index_of(e).unwrap();
        assert!(idx(Event::WalAppend) < idx(Event::StateCas(State::Preparing, State::Committed)));
        assert!(idx(Event::WalFsync)  < idx(Event::StateCas(State::Preparing, State::Committed)),
            "Committed became observable before the commit record was durable");
        assert!(idx(Event::StateCas(State::Preparing, State::Committed)) < idx(Event::LockRelease),
            "locks released before the terminal state was published — the next acquirer observes Preparing");
    }
    ```
  - [ ] **(SENIOR)** `commit_crash_after_fsync_recovery_committed` — kill -9 between fsync and state CAS; recovery
    sees Commit record in WAL; marks Committed. Exercises R6.
  - [ ] **(SENIOR)** `commit_crash_before_fsync_recovery_aborted` — kill -9 after WAL append but before fsync;
    recovery may or may not see the record; either way state is consistent (no half-commit). Exercises
    R6.
  - [ ] **(EXPERT, anti-shortcut — bar (c))** `bench_commit_group_5x_single` — A/B bench: single-fsync vs group-commit;
    assert group ≥ 5×. Literal textbook `append→fsync→store` passes correctness, fails the bar. Exercises R4, R7.
  - [ ] **(EXPERT — bar (c))** `bench_commit_durable_100k_n1000` — the durable-throughput bar, MOVED here from
    Tuesday's manager bench (an 800 k/s begin+commit bar contradicts group-commit physics: ≤16-deep groups ×
    ~100 µs NVMe fsync ⇒ ~160 k/s ceiling; Tuesday's 800 k/s bar now covers only the no-fsync begin+abort leg).
    Run with **N≥1000 outstanding txns** so the group window actually fills; assert sustained ≥100 k durable
    commits/sec on NVMe. Exercises R4, R7.
  - [ ] **(EXPERT, anti-shortcut — bar (c))** `bench_commit_serialize_outside_fsync` — instrument batch-internal time;
    assert 0 µs of serialization inside the fsync window. Exercises R5.
  - [ ] **(EXPERT, loom)** `loom_commit_state_lock_ordering` — `#[cfg(loom)]`; verify state CAS ordering relative to lock
    release. Exercises R3.
  - [ ] **(negative-control, test-the-test)** `bench_commit_group_physically_plausible` — paired with the 5× bench:
    asserts both legs land in `1e3 ≤ commits/s ≤ 5e6`; a single-fsync leg measured above ~10 k/sec means the
    bench skipped the real fsync (e.g. tmpfs/no-fdatasync), so the 5× ratio is fake.

  #### Phase 3 — Make the tests pass, one failure at a time (~90 min)
  Ordered newbie-first, expert-last — the ladder you actually walk.

  - **(SENIOR) When `commit_strict_ordering` reports lock release before state Committed:**
    - Symptom: instrumentation shows lock released while state still Preparing; another txn acquires
      the lock, observes Preparing in the previous holder, aborts based on stale state.
    - Why: lock release before state — another acquire grabs the lock, sees Preparing in the previous
      holder, aborts based on stale state.
    - Fix: state transition is the linearization point; locks held until then. Sequence: state CAS
      Committed → release locks. The release happens AFTER the CAS publishes the terminal state.
    - Cost avoided: a stale-state observation drives the next acquirer to a wrong abort decision (bar (c)
      correctness violation); reordering two lines is free at runtime.
    - Muscle: linearization point precedes lock release; never the other way. Reapplies at: W74
      matching-engine order-state + lock-release, W67 consensus-raft commit + replica notify.

  - **(EXPERT, anti-shortcut — bar (c) violation) When `bench_commit_group_5x_single` reports group throughput == single throughput (group commit
    not working):**
    - Symptom: throughput at 100 k attempted commits/sec actually delivers ~10 k commits/sec; each txn
      blocks ~100 µs on fsync; disk queue lock thrashes under 16 concurrent committers; throughput
      collapses to ~5 k/sec.
    - Why: textbook commit pseudocode reads `wal.append → wal.fsync → state.store(Committed)`, and if
      implemented literally, every txn issues its own fsync syscall. At NVMe fsync ≈ 100 µs, that caps
      throughput at 10k commits/sec/disk — 10× below the 100k target.
    - Fix: **leader-based group commit** (W26 wal Tuesday pattern). One committer becomes the leader,
      calls `fsync` once; followers wait on a `Notify` keyed to the LSN of the leader's tail. The first
      follower-after-fsync wakes all batched followers via single `Notify::notify_waiters()`. Tunable
      batch window (default 1 ms or 1000 txns, whichever first). Each follower's commit() blocks on
      `wait_for_lsn(my_tail_lsn)`; the leader does `fsync` + `notify_waiters()` once per batch.
    - Cost avoided: 100 µs/commit fsync → ~5 µs/commit amortized = **20× throughput uplift**; 100k
      commits/sec achievable on one NVMe; disk queue contention drops to zero.
    - Muscle: "the fsync is the cost — amortize it across as many independent commits as the durability
      semantics allow" — group commit is the canonical pattern. Reapplies at: W74 matching-engine
      durable order log batching, W80 ledger journal group fsync, W92 ledger io_uring batched
      submission, W77 messaging-aeron archive batched fsync.

  - **(EXPERT, anti-shortcut — bar (c) violation) When `bench_commit_serialize_outside_fsync` reports ~500 µs of serial work per batch:**
    - Symptom: batch-internal time shows 500 µs of serialization inside the critical section; group
      throughput halved from theoretical maximum.
    - Why: doing `bincode::serialize(&commit_record)` *inside* the group's serial section blocks all
      followers; serialization is ~500 ns for a typical commit record × 1000 followers = 500 µs of
      serial work per batch. Worse, if the serialization allocates (Vec<u8>), the malloc adds another
      ~80 ns per record.
    - Fix: each txn serializes its commit record into a **thread-local pre-allocated 4 KB buffer**
      *before* entering the group; the leader's job is purely `concat slabs → writev → fdatasync →
      notify`. Followers do all CPU work in parallel; the serial section is pure I/O.
    - Cost avoided: 500 µs serial → 0 µs serial per batch; group throughput uplift ~2×.
    - Muscle: "minimize what runs inside a critical section to literally only what must be
      serialized" — everything precomputable should be done in parallel before the section. Reapplies
      at: W74 matching-engine order-log batching, W80 ledger journal pre-serialize, W77
      messaging-aeron pre-batched publication writes.

  - **5-year failure mode** (write in `notes/42_commit_followup.md` after green): when persistent
    memory + transactional fsync (e.g. ext4 + io_uring `IORING_OP_FSYNC` with linked SQEs) becomes
    standard, group commit shrinks to single io_uring SQE chain. State migration: replace the leader
    loop with io_uring submission chain.
- [ ] Commit + log (~10 min)
  - *Note (day-load)*: Thursday holds two full Builds — `occ.rs` (155) + `commit.rs` (155) + commit/log (10) =
    320 min (~5.3 h). The TWCS scaffold Build moved to **Friday**, and its drop-window delta + inheritance
    audit to **Saturday**, to keep Thursday under the ~5-6 h cap.

**Friday — `txn` storage-trie integration + [NEW v2] lsm-core::TWCS scaffold**

- [ ] [NEW v2] **Build**: `crates/lsm-core/src/compaction/twcs.rs` — `TimeWindowCompactor { window: Duration }`. One SSTable per fully-elapsed window; drop entire window on TTL expiry. (~total 130 min across 3 phases)
  - 🔧 **Why this matters**: TWCS is the time-series workload optimum (write amp ≈ 1 vs STCS's 4×). Wrong
    out-of-order policy → silent invariant break. Wrong window granularity → millions of SSTs OR poor TTL
    granularity. Downstream W80 ledger journals, W95 mini-db time-series tier inherit.

  #### Phase 1 — Problem, prediction, design (~30 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in
    `crates/lsm-core/notes/42_twcs_prediction.md`:
    - P1. Predict TWCS write amplification vs STCS for a pure time-series append workload. Two numbers.
    - P2. Predict the failure mode of accepting an out-of-order (late) write straight into the active window
      with NO tag. One line (what does a downstream reader see?).
    - P3. Predict the number of manifest records + fsyncs to drop 1000 expired windows naively. Two numbers.
      Then predict it batched.
    - P4. For a 7-day TTL, predict the window granularity you'd choose and the SST count it implies vs a
      1-second granularity. Two numbers.
    - P5. List every Phase-2 test you predict your first attempt fails.

  - *Mental model*: for time-series data (every record carries a timestamp; records age out), bin SSTs by
    time window; when a window's age > TTL, drop the entire window without rewrite. Write amp ≈ 1 (each
    entry written once, never rewritten in compaction) — the time-series optimum vs STCS's ~4×.

  - *Contract*: `TimeWindowCompactor { window: Duration, ttl: Duration, grace_period: Duration }`;
    `fn window_id(ts: Timestamp) -> WindowId` (= `ts / window_secs`); `fn route(write) -> Result<WindowId, VeryLate>`;
    `fn drop_expired(now) -> ManifestRecord::BatchedDrop`; `fn compact_within(window)` (STCS-bounded, no cross-window merge).

  - *Back-of-envelope* (bar (b)): write amp ≈ 1 vs STCS ~4× = 4× less write bandwidth on time-series. The
    one cliff to avoid: dropping N expired windows as N manifest records → N fsyncs ≈ N × 100 µs (1000 windows
    ≈ 5 s of manifest churn during a TTL sweep); batch to one record = one fsync.

  - **Requirements** — write in `crates/lsm-core/notes/42_twcs_design.md`:
    - R1. All entries within an SST have timestamps in [window_start, window_end).
    - R2. Active window receives flushes; non-active windows are immutable.
    - R3. **Window drop** happens when `now - window_end > ttl`; drop is unlink + manifest record.
    - R4. **Out-of-order writes**: late writes within grace period go to active window; very late writes
      are rejected with typed error.
    - R5. **Within-window compaction** (STCS-like) if window has > N SSTs; bounded to that window only;
      no cross-window merge.
    - R6. **Batched drop**: drop 1000 windows as one manifest record.
    - R7. Write amp ≈ 1; TTL respected within 1 window.

  - **Design walk-through** — answer each:
    - D1. Window assignment: `WindowId = ts / window_secs`. Write the partition algorithm.
    - D2. Window granularity trade-off: too small (1 sec → millions of SSTs); too large (1 month →
      30-day drop granularity for 7-day TTL). Why `ttl/100`?
    - D3. Out-of-order policy: late = within X seconds of now-window_end (grace period). Very late =
      rejected. Write the policy.
    - D4. Within-window compaction: if window has 5 SSTs, merge to 1 — but bounded to the window. Why
      no cross-window?
    - D5. Drop batching: write the manifest record format. Why O(1) per window in dropping cost?
    - D6. **Pre-mortem (load-bearing)**: write the out-of-order-breaks-invariant trace — late record
      goes to active window; query reads from active for that key but the timestamp is in another
      window's range; invariant broken; downstream readers see ghosts.
    - D7. **Anti-shortcut audit**: (a) silently routing all late writes to active (breaks the window-range
      invariant, see Pre-mortem); (b) per-window drop manifest record (fsync storm); (c) 1-second window
      granularity for a 7-day TTL (millions of SSTs).
  - 🧮 **Paper drill** — write in `notes/42_twcs_drill.md`:
    - D-WALK on time-window bucketing over concrete timestamps: take `window = 3600 s`, `ttl = 3 windows`,
      `now = window 7`, and 6 records with ts {0, 3599, 3600, 7200, 7201, 25201}. Hand-compute each
      `window_id = ts / 3600` and bucket them into SSTs; draw the resulting windows 0,1,2,7 with their member
      records and verify R1 (every record's ts ∈ [window_start, window_end)). Then run `drop_expired(now)`:
      windows with `window_end < now - ttl` (i.e. windows 0..3) age out — list which SSTs unlink and confirm
      the batched-drop emits ONE manifest record. Finally route a late write ts=3500 (window 0's range) when
      window 7 is active: within grace → tag+active; past grace → typed reject. Output the invariant: "the
      window-range invariant holds because every write is routed by `ts/window_secs` or explicitly rejected."
  - **Output**: `notes/42_twcs_design.md` with R1–R7, D1–D7, the out-of-order bad-trace;
    plus `notes/42_twcs_drill.md` with the D-WALK time-window bucketing + drop + late-route trace.

  #### Phase 2 — Write the failing tests (~25 min)
  Tier annotated.

  - [ ] **(SENIOR)** `twcs_window_invariant_holds` — 1 M entries across 10 windows; assert every SST's ts range fits
    its window, INCLUDING the exact window-boundary second. Traps a naive route-late-to-active AND an inclusive
    upper bound (`ts == window_end` must route to the NEXT window — [start, end) is half-open). Exercises R1.
    ```rust
    #[test]
    fn twcs_window_invariant_holds() {
        let c = TimeWindowCompactor::new(Duration::from_secs(3600), /*ttl*/ Duration::from_secs(3 * 3600));
        // the boundary trap first: ts == window_end belongs to the NEXT window
        assert_eq!(c.window_id(ts(3599)), WindowId(0));
        assert_eq!(c.window_id(ts(3600)), WindowId(1),
            "ts == window_end routed into window 0 — an inclusive upper bound double-assigns the boundary second");
        write_1m_entries_across_10_windows(&c);
        for sst in c.ssts() {
            let w = c.window_of(&sst);
            assert!(sst.min_ts() >= w.start && sst.max_ts() < w.end,
                "SST {:?} ts range [{}, {}] escapes window [{}, {}) — a late write was routed untagged",
                sst.id(), sst.min_ts(), sst.max_ts(), w.start, w.end);
        }
    }
    ```
  - [ ] **(SENIOR)** `twcs_ttl_drops_windows_without_rewrite` — 10 windows + TTL=5 windows; advance time;
    instrument; assert 5 SSTs dropped + 0 bytes written. Exercises R3, R7.
  - [ ] **(SENIOR)** `twcs_late_write_within_grace_goes_to_active` — write with ts in an immutable window's range
    but within grace period; assert it goes to active. Exercises R4.
  - [ ] **(SENIOR)** `twcs_very_late_write_rejected` — write with ts past grace period; assert typed error.
    Exercises R4.
  - [ ] **(SENIOR)** `twcs_within_window_compaction_bounded` — 5 SSTs in one window; compact; output 1 SST; assert
    no other window touched. Exercises R5.
  - [ ] **(EXPERT, anti-shortcut)** `twcs_batched_drop_one_manifest_record` — drop 1000 windows; instrument; assert 1
    manifest record, 1 fsync. Per-window drop passes correctness, fails on fsync storm. Exercises R6.
  - [ ] **(negative-control, test-the-test)** `twcs_ttl_drop_actually_freed_bytes` — paired with the drop test:
    asserts post-drop on-disk byte count strictly decreased by ≈ the dropped windows' size; if bytes are
    unchanged the "drop" only edited the manifest and leaked the SST files, so the rewrite-free assertion is
    hollow.

  #### Phase 3 — Make the tests pass, one failure at a time (~75 min)
  Ordered newbie-first, expert-last — the ladder you actually walk.

  - **(SENIOR) When `twcs_window_invariant_holds` reports a late write in the active window's SST with ts
    outside the active window's range:**
    - Symptom: a late record with ts=2024-01-01T12:00 (in window N-2's range) lands in window N
      (active); window N's SST now contains a ts outside its [start, end); read of that key from
      window N returns stale data; downstream reader sees a value with the wrong timestamp.
    - Why: out-of-order writes — a late-arriving record with timestamp in an already-immutable
      window must go somewhere. Either it goes to active (breaking invariant) or write is rejected.
    - Fix: late writes (within grace period) go to active window AND are marked with a "moved-late"
      tag; very late writes are rejected with a typed error. Active window's tag bit indicates the
      window may contain late entries; readers respect the tag. Alternative (cleaner): reject all
      late writes; force callers to deal with their out-of-order data.
    - Muscle: time-window semantics need an explicit out-of-order policy; silent acceptance breaks
      invariants. Reapplies at: W77 messaging-aeron archive late frames, W80 ledger journal late
      transactions.

  - **(SENIOR) When `twcs_late_write_within_grace_goes_to_active` is rejected (false negative):**
    - Symptom: late writes within the documented grace period are rejected; integrating systems
      with mild clock skew lose data.
    - Why: grace period not configured / not implemented.
    - Fix: parameterize `grace_period: Duration`. A write with `now - ts <= grace_period` is
      "within grace", routed to active window. `now - ts > grace_period` is "very late", rejected.
      Default grace_period = window/4.
    - Muscle: time-bounded systems need explicit grace policy for clock skew + retries. Reapplies
      at: W67 consensus-raft heartbeat grace, W74 matching-engine market-data timestamp grace.

  - **(EXPERT, anti-shortcut) When `twcs_batched_drop_one_manifest_record` reports 1000 manifest records:**
    - Symptom: dropping 1000 windows emits 1000 separate manifest "remove SST" records; 1000 fsyncs
      ≈ 5 s of manifest churn during TTL sweep.
    - Why: manifest churn on drop — dropping 1000 windows at once = 1000 manifest records.
    - Fix: batch drop in a single manifest record. `ManifestRecord::BatchedDrop { window_ids:
      Vec<WindowId>, sst_ids: Vec<SstId> }`; one fsync covers the entire TTL sweep.
    - Muscle: any sweep operation must batch durable records; per-item fsync is a DoS-shaped failure
      mode. Reapplies at: W41 LCS compaction-result batching, W26 WAL segment-rotation batching.

  - **5-year failure mode** (write in `notes/42_twcs_followup.md` after green): when storage tiers
    differentiate cost/perf (hot SSD, warm HDD, cold tape), TWCS can integrate tier-migration on
    window age. State migration: introduce TwcsTieredCompactor; window age threshold triggers
    migration instead of drop.
- [ ] **Refactor**: `crates/storage-trie/src/write_tx.rs` — storage-trie's existing write-tx now wraps
  `txn::Txn` (slab-pooled), takes per-key locks via `txn::LockManager`, and persists via `txn::commit()`
  (group-commit). This is a genuine delta — storage-trie ALREADY has a write-tx (built earlier); you are
  rewiring its concurrency control + durability onto the new substrate, NOT rebuilding it. Delete storage-trie's
  ad-hoc per-key mutexes; keep its trie-node mutation logic untouched. (~60 min)
  - 🎯 **Hardened acceptance**: storage-trie's existing write-tx test suite stays green AND a new
    `storage_trie_write_tx_uses_txn_locks` test asserts two concurrent writers to the same trie key serialize
    via `txn::LockManager` (Wound-Wait), not the deleted local mutex. *Mental model*: this is the first
    inheritance edge of `txn` — proves the substrate composes with a real consumer before W44 storage-trie v1.0.
  - **Output**: `cargo test -p storage-trie write_tx green`; `grep -c "Mutex" src/write_tx.rs` == 0 (the
    ad-hoc locks are gone, replaced by `txn::LockManager`).
- [ ] Tag `txn v0.5.0`. (~5 min)
  - **Output**: `git tag txn-v0.5.0`; `cargo test -p txn` green; static-mem audit row (Sat) clean.
- [ ] Reth PR — pick something simple to keep velocity (good-first-issue or a doc/test fix in reth). (~150 min)
  - **Output**: PR URL recorded in `notes/pr_log.md`; CI green on the PR.
- [ ] Commit + log (~10 min)

**Saturday — Crate: property tests + fuzz + [NEW v2] TWCS drop-window**

- [ ] [NEW v2] **Build**: `crates/lsm-core/src/compaction/twcs.rs` (drop-window method) — `fn drop_expired(&mut self,
  now: Timestamp) -> ManifestRecord::BatchedDrop`. When `oldest_window.max_ts < now - ttl`: unlink every SSTable
  in the window AND emit ONE batched manifest record for the whole sweep (the W42 TWCS Phase-3 batched-drop fix).
  This is the genuine delta of Friday's TWCS Build Phase 1 (routing/invariant) — here you wire the unlink + manifest
  path. (~60 min)
  - **Output**: `cargo test -p lsm-core twcs_ttl_drops_windows_without_rewrite twcs_batched_drop_one_manifest_record
    twcs_ttl_drop_actually_freed_bytes` green — verifies 1 M time-series keys over 10 windows, TTL=5, drop half the
    data with 0 bytes rewritten, 1 manifest record, 1 fsync, and on-disk bytes actually shrank.
    *Note (filler-cut)*: the standalone "Test: 1M keys, TTL 5 windows" one-liner is folded into Friday's TWCS Build
    Phase-2 trap + negative-control tests — same assertion, now engineered to bite.
- [ ] [NEW v2] **Verify**: `crates/lsm-core/notes/42_inheritance_audit.md` — confirm lsm-core still inherits
  bufpool + wal + bloom; v2 LCS (W41) + TWCS (W42) are net-new strategies alongside STCS (W40). Then tag
  `lsm-core v0.6` (was v0.5). [bar b] (~20 min)
  - **Output**: audit note lists the 3 inherited crates + 3 compaction strategies; `git tag lsm-core-v0.6`.
- [ ] **Build**: `crates/storage-trie/tests/prop_mpt_invariants.rs` — proptest for MPT invariants now that
  storage-trie commits through `txn`: random insert/delete sequences under a `txn::Txn`; assert (a) root hash
  is deterministic for a given key-set regardless of insertion order, (b) commit/abort leaves the trie at the
  pre- or post-state, never a torn intermediate. (~45 min)
  - **Output**: `cargo test -p storage-trie prop_mpt_invariants` green over ≥1000 proptest cases.
- [ ] **Build**: `crates/txn/fuzz/fuzz_targets/lockmanager_no_deadlock.rs` — cargo-fuzz target: random
  lock/unlock/wound sequences across N keys and M txns must NEVER deadlock under Wound-Wait, and must never
  leave a lock orphaned after all txns terminate. (~30 min)
  - 🎯 **Hardened acceptance**: a watchdog thread fails the fuzz iteration if any acquire blocks > 1 s (a
    deadlock would block forever); after each iteration assert the lock table is empty. *Mental model*: fuzzing
    the deadlock-freedom invariant is the only way to gain confidence Wound-Wait holds under adversarial
    interleavings the loom model is too small to cover.
  - **Output**: `cargo fuzz run lockmanager_no_deadlock -- -max_total_time=300` survives 5 min with 0 crashes/hangs.
- [ ] **Build**: `crates/lsm-core/docs/compaction_strategy_matrix.md` — decision matrix mapping workload patterns to STCS / LCS / TWCS. With LCS shipped in W41 and TWCS shipping this week (Fri routing + Sat drop-window), the 3-strategy comparison is the consolidation artifact. Each strategy's write amp / read amp / space amp + recommended workload + failure mode. (~90 min)
  - 🔧 **Decomposition prompt**: W41 added LCS and this week adds TWCS (Fri routing + Sat drop-window) on top of W40's STCS. Three compaction strategies now ship in `lsm-core`; the W95 mini-db CAPSTONE picks among them per workload. Without the matrix, the choice at W95 will be reflexive ("use STCS, it's the default"); with the matrix, it'll be workload-driven. Also: this is interview gold — Tier-A DB/HFT firms ask "when would you pick TWCS over STCS?" — the matrix is the rehearsal artifact.
  - 🎯 **Hardened acceptance**:
    - **Table**: 3 columns (STCS / LCS / TWCS) × 6 rows (write amp, read amp, space amp, recommended workload, dominant cost, failure mode under burst). Each cell has a concrete value (e.g. STCS read amp ~3-10× under bursty workload; LCS read amp ~1.1× steady-state). Cite ScyllaDB doc URLs.
    - **Decision tree**: workload → strategy. 4 leaves: write-heavy general → STCS; read-heavy + can tolerate high write amp → LCS; time-series with fixed TTL → TWCS; mixed + capstone choice → SCYLLA-style ICS (out of scope, but documented as the upgrade path).
    - **Concrete examples from the workspace**: mini-db W95 default = STCS (general KV); a hypothetical "matching-engine trade log" tier = TWCS (time-series with daily retention); a hypothetical "vector-db sealed-segment storage" = LCS (read-heavy on stable data). Document why each picks what.
    - **Interview rehearsal section**: 3-min answer "when to pick TWCS over STCS for a time-series workload." Cover: drop-window compaction (TWCS drops whole windows on TTL expiry without rewrite), STCS-within-window for low write amp, single TTL discipline. Cite the ScyllaDB recommendation "use a single TTL value for any given table."
    - **5-year failure mode**: when learned compaction strategies (ML-tuned) become available, the matrix becomes a "starting policy" + learned correction. State migration: keep matrix as cold-start defaults; learned tuner overrides per-table.
  - *Source*: https://opensource.docs.scylladb.com/stable/kb/compaction.html (all three strategies + config defaults).
- [ ] **Verify**: `notes/static_mem_audit.md` — append row for `txn@v0.5.0`. The txn crate is the L3 transaction substrate consumed by storage-trie, ledger, mini-db; allocations in `lock_acquire` / `commit` / `rollback` hot paths amplify across all consumers. Grep + classify; expected zero hot-path allocations (LockManager uses `concurrent::AtomicCell` + slot-allocator for next_id; the WaitForGraph for deadlock detection uses a fixed-capacity adjacency list). (~30 min)
  - 🎯 **Hardened acceptance**: same schema as W26/W37 rows. Concrete txn row: hot path = `LockManager::acquire/release + Txn::commit/rollback + WaitForGraph::detect_cycle`. If any allocations found in WaitForGraph (likely candidate: a `Vec<TxId>` for the cycle visit set), document the fix (pre-allocated visit set of capacity = max concurrent txns).
- [ ] Commit + log (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
