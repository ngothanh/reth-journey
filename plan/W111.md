# Week 111 — [v3] `perp-dex-core` performance pass: per-mark-tick p99.99 at 100k accounts

> **Phase**: 6
> **Month**: M28
> **Track**: Half-2 venue (capstone hardening) — sustained operations + bet-outreach ramp begins
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: TigerBeetle `src/vsr/replica.zig` apply-loop + local-state read discipline (*the single hot-path source of truth*) + LMAX Disruptor latency-test methodology + Gil Tene "How NOT to Measure Latency" (coordinated omission) + Linux `perf` topdown / `rdtsc` tick-to-trade — **all routed through `latency-lab`**, the bar-(c) measurement substrate every venue crate's bench already consumes
> **Crate(s) touched**: `perp-dex-core` v0.9 → v0.95 [perf-pass]; `risk-engine` v1.0 (the per-mark-tick incremental-margin hot path — the convergence cell being driven to budget); `latency-lab` v1.0 (HdrHistogram + coordinated-omission + rdtsc + `perf` LLC/false-sharing/NUMA — *consumed, not extended*); `consensus-vsr` v1.0 (the single hot-path log — read-side discipline only, NOT re-touched); `oracle-mark` v1.0 (`MarkTick` producer — referenced as the tick source, not modified)
> **Bar**: (c)   ← the per-mark-tick risk recompute path is the HFT critical path of the whole venue. Single-digit-µs *per-account* delta budget, p99.99 *per-mark-tick* (fan-out across 100k accounts) the headline number, zero-allocation steady state, VOPR-backed determinism preserved through every optimization. **Missing the bar at this scale means §4A acceptance criterion #8 is unmet and the M30 bet-readiness gate fails — audit before tagging.** Each Build labels its bar inline.
> **Feeds into**: W112 Blog #5 (cross-margin + liquidation — *the principal artifact*; cites THIS week's per-mark-tick p99.99 JSON as the headline number); W113 hot-path PR review + ARM CI gate + ledger/aeron real-load polish (inherits this week's `latency-lab` provisioning verbatim); M30 **bet-readiness gate** (§4A acceptance #8 "scale demonstrated at the aggressive target" reads this week's `latency-lab` report); W120 production profile-capture posts; W124 `perp-dex-core` v1.0 final bench update.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): `latency-lab`'s own `record_corrected_with_max_interval` + rdtsc harness (W21–W24, M6); Gil Tene "How NOT to Measure Latency" (coordinated omission — the Thu bench); Brendan Gregg `perf`/flamegraph + topdown method (Mon profiling rig); TigerBeetle `replica.zig` apply-loop + local-state read (the read-after-commit-from-local-applied-state discipline, §4A); README.md §3 rows 9/13/21, §4 reference map, §4A v1.0 lock (locked interfaces + capacity target + acceptance #8), §5 capstone↔bets, §7 founding-eng bar, §9 resolutions, §A migration map.
> **v3 modifications**: (1) **Whole-week re-aim.** v2 W111 was a `matching-engine` *book-only* performance pass (v1.0 → v1.1: ring vs VecDeque, seqlock top-of-book, single-symbol P99 < 2µs). v3 elevates it to the **capstone-level** performance pass on `perp-dex-core`: the headline number is no longer single-symbol matching P99 but the **per-mark-tick p99.99 of the cross-margin recompute fanned out across ~100k accounts** (§4A acceptance #8). The book hot path was already tuned at W74/W82; the *unsolved* perf problem at the aggressive target is the risk convergence cell. (2) **Incremental margin is mandatory, not a fallback** (§4A back-of-envelope): a naïve full recompute = 100k×10 ticks/s = 1M recomputes/s blows the budget at any cadence above ~10/s; this week makes the **delta-on-each-position/price-change** path the steady state, with full recompute reserved for circuit-breaker / parameter-change events. (3) **Risk reads come from local applied committed state on the leader shard** (§4A control-plane contract) — no consensus round-trip per read, no second log. (4) **`latency-lab` is the measurement substrate** — consumed, never re-invented ("no primitive twice"); this week is its first capstone-scale exercise. (5) The single VSR hot-path log is read-side-only this week; Aeron Cluster + Kafka/`log-distributed` are explicitly **off the hot path** (the analytics-plane fan-out reads a *projection*, not the recompute path).

**The v3 lock this week proves out (read before Monday):** §4A acceptance criterion #8 — *"scale demonstrated at the aggressive target: ~100k accounts / 2 instruments / ~1M orders/s with the **incremental margin** hot path; full recompute only on circuit-breaker/param change. The latency report (`latency-lab`) shows the per-mark-tick p99.99 budget is met at this account count."* Everything this week serves that one sentence. The compute lever is **incremental margin** (delta, not recompute). The read lever is **local applied state on the leader shard** (read-after-commit, no consensus round-trip — §4A control-plane contract). The measurement instrument is **`latency-lab`** (coordinated-omission-correct, rdtsc, `perf` topdown — *consumed*, not rebuilt). The determinism gate from W74/W90 (integer-only, no `f64`, no `HashMap` iteration, no wall-clock) **must survive every optimization** — a perf win that breaks cross-replica byte-identity is a regression, not a win. "No primitive twice": this week reuses `latency-lab`, `consensus-vsr`'s applied-state read, `risk-engine`'s convergence cell, and `runtime-thread-per-core`'s shard-per-core — it builds no new measurement, consensus, or scheduling primitive.

---

## Monday — Identify the hot spot: per-mark-tick fan-out profiling (capstone scale)

- [ ] Re-read the §4A capacity back-of-envelope + acceptance #8; transcribe the budget into `notes/111_capacity_budget.md`: 100k accounts × 10 ticks/s = 1M recomputes/s naïve; shard-by-account 16 shards → 62.5k/s/shard; **why full recompute is fundamentally wrong above ~10 ticks/s** and incremental margin is mandatory. (~30 min)
  - 🎯 **Hardened acceptance**: the note states the per-mark-tick p99.99 *target budget* as one number (derive it: a mark tick must finish fanning out before the next — at the aggressive cadence the budget is the inter-tick interval minus headroom; state the number you will hold the week to), AND names the two complementary levers (incremental-margin = compute cost; local-applied-state read = read latency) with one line each on why *both* are required.
- [ ] [HFT] **Build (bar (c))**: `crates/perp-dex-core/profiling/profile_mark_tick.sh` + `crates/perp-dex-core/tests/mark_tick_profile_rig.rs` — a self-validating profiling rig, built **on `latency-lab`'s provisioning**, that refuses to produce a flamegraph of the per-mark-tick fan-out from a noisy/debug/un-isolated run. (~total 80 min across 3 phases)
  - 🔧 **Why this matters**: this is the Brendan-Gregg/LMAX rig discipline applied at *capstone* scale — the thing under the profiler is no longer a single-symbol match loop (that was W74/W111-v2) but the **risk convergence cell fanning a mark tick across ~100k accounts on shard-per-core**. At this scale the failure modes multiply: a flamegraph captured with SMT on attributes sibling-hyperthread contention to your fan-out loop; one captured cross-NUMA attributes remote-memory stalls to your delta math; a debug build attributes `Vec::push` bounds checks to your hot path. Two days optimizing the wrong function is the cheap version of the failure — the expensive version is shipping a "p99.99 met" claim to the M30 gate that evaporates on the reviewer's rig. **v3 note**: the rig does NOT re-implement perf orchestration — it *shells into `latency-lab`'s* provisioning + capture (turbo-off, `isolcpus`, prime-frequency, topdown counters), because `latency-lab` is the one measurement primitive (no primitive twice). First-attempt rigs fail ≥1 test below: they profile a debug binary found first in PATH, or report disjoint top-3 across runs because the bench shards weren't pinned, and that debug session IS the lesson. Downstream consumers: Tue (incremental-margin redesign) and Wed (local-applied-state read) are *driven by* this profile; Thu's per-mark-tick p99.99 bench reuses this rig's provisioning; W113 hot-path PR review + W120 production profile-capture inherit `profile_mark_tick.sh` verbatim.

  - 🔧 **Decomposition prompt (silent test)**: this spec doesn't name primitives. The rig needs isolated cores, alias-free sampling, topdown counters, NUMA-local feed-gen, a warm steady state — and it must drive *the capstone* (16 shards, 100k accounts, a real `MarkTick` stream from `oracle-mark`) not a microbenchmark. The spec names none of the building blocks: `latency-lab`'s provisioning + capture, `runtime-thread-per-core`'s shard pinning, `oracle-mark`'s tick producer, the §4A budget. If the top-3 functions disagree across runs, or IPC reads cache-bound where you predicted compute-bound, or the flamegraph shows the *full-recompute* path dominating (it should be near-zero in steady state — it only fires on circuit-breaker) — your decomposition missed something specific.
  - 🎯 **Hardened acceptance for the capstone profiling rig**:
    - **Scope gate (v3)**: the rig profiles the **per-mark-tick fan-out** (`risk_engine::on_mark_tick` → per-account delta), NOT a single-symbol match loop. A reviewer reading `top3.json` must see risk-convergence functions, not `match_and_execute`.
    - **Build discipline**: profiles ONLY `target/release` with `lto=true`, `codegen-units=1`, `debug="line-tables-only"`; asserts the binary's build profile via `file ... | grep "not stripped"` + Rust build-id parse, never the path string. Refuses a debug binary with a named error.
    - **Provisioning (delegated to `latency-lab`)**: turbo off, governor `performance`, SMT off; `isolcpus`+`nohz_full`+`rcu_nocbs` on the 16 shard cores; IRQs masked off shard cores; `oracle-mark` tick producer pinned to a *separate* NUMA-local core; `mpstat -P <core> 1` shows `nr_running == 1` on each shard core for the whole run.
    - **Steady-state discipline**: ≥30s of sustained 1M-orders/s ingest + 10 ticks/s before sampling; the full-recompute path must be near-zero self-time (it fires only on circuit-breaker — if it dominates steady state, the incremental path isn't wired).
    - **Alias-free sampling**: prime frequency (`-F 997`), cross-checked with a second prime (`-F 4001`); top-3 set intersection ≥ 2 of 3.
    - **Capture**: `perf stat` topdown (`-M Frontend_Bound,Backend_Bound,Bad_Speculation,Retiring`) + `cache-references,cache-misses,LLC-load-misses,dTLB-load-misses,node-load-misses` (NUMA) for 60s; `perf record -F 997 -g --call-graph dwarf` for 60s; flamegraph via `inferno-flamegraph`.
    - **Acceptance gates**: per-account delta IPC ≥ 2.0; LLC-miss% < 5%; `node-load-misses` (cross-NUMA) < 1% of loads (proves NUMA-local). If any misses, the script names it as the week's focus.
    - **Negative control**: the rig asserts it sampled *something real* (>0 samples, plausible cycle band for a 60s run) so an empty/crashed run can't pass vacuously.
    - **5-year failure mode**: state one shift (per-trade marks, AMD Zen-5/ARM-Neoverse rig with a different cache hierarchy, >100k accounts spilling per-shard working set out of L2) that retires the absolute IPC/LLC gates.

  #### Phase 1 — Problem, prediction, design (~25 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in `crates/perp-dex-core/notes/111_mark_tick_profile_prediction.md`:
    - P1. Write the exact `perf record` line you'd type from memory to profile the per-mark-tick fan-out (frequency, call-graph mode, duration). Then write the *wrong-but-tempting* one-liner (`cargo flamegraph` defaults).
    - P2. Predict the IPC of the per-account delta computation on your rig today (one number) and whether it is cache-bound or compute-bound at 100k accounts (one word). Reference band: a struct-of-arrays delta loop typically lands compute-bound (IPC 2.5–3.2); an array-of-structs / pointer-chasing position store lands cache-bound (IPC 1.2–1.8).
    - P3. Predict the top-3 self-time functions you expect the flamegraph to show in steady state. Three names; no commentary. (If `full_portfolio_recompute` is in your top-3, that is a *prediction of a bug* — it should be near-zero.)
    - P4. Predict the `node-load-misses`% (cross-NUMA) if the `oracle-mark` tick producer and the risk shards share a NUMA node vs sit on opposite nodes (two numbers).
    - P5. Of the Phase-2 tests below, list every test name you predict your first attempt will fail. Seal it.
  - *Mental model*: the profiling rig is a *correctness gate on measurement itself*, at capstone scale — it asserts the preconditions (release+LTO binary, 16 isolated shard cores, NUMA-local feed-gen, warmed steady state, alias-free sampling, topdown counters) before any flamegraph of the per-mark-tick fan-out is allowed to exist. It exists because every optimization Tue/Wed is only as trustworthy as the profile that motivated it, and the M30 bet-readiness gate quotes the resulting number.
  - *Contract*: `profile_mark_tick.sh <shards> <duration_s>` → exits non-zero with a remediation message if any precondition fails; on success emits `flamegraph.svg`, `perf-stat.txt` (topdown + IPC + cache-miss + dTLB-miss + node-miss), and `top3.json` (self-time functions of the fan-out path). `mark_tick_profile_rig.rs` shells out to the script and asserts on its outputs. NEVER inline perf logic into the test, and NEVER re-implement what `latency-lab` already provides — the script *delegates* provisioning to `latency-lab` and is the reusable capstone artifact.
  - *Back-of-envelope* (bar (c)): per-mark-tick fan-out across 100k accounts on 16 shards = ~6.25k accounts/shard/tick. At a per-account delta budget of ~1µs that is ~6.25ms/shard/tick — already most of the inter-tick interval at 10 ticks/s, with **zero headroom for tail**. One LLC miss per account = ~100ns ≈ 10% of the per-account budget; one cross-NUMA `node-load-miss` = ~150ns ≈ 15%. Cliff ratio: L2-resident position read ≈ 4ns vs LLC-miss ≈ 100ns = **~25× per access**; vs cross-NUMA ≈ 150ns = **~37×**. This is the cliff Tue/Wed must climb down. IPC interpretation: if the delta loop reads < 2.0 IPC it is memory-bound (position store layout); the lever is data layout (Tue), not micro-op tuning.
  - **Requirements** R1..R8 — write in `crates/perp-dex-core/notes/111_mark_tick_profile_design.md`:
    - R1. Profile ONLY `target/release` (`lto=true`, `codegen-units=1`, `debug="line-tables-only"`); assert build profile via `file` + build-id, not the path string.
    - R2. Provisioning **delegated to `latency-lab`**: turbo off, governor `performance`, SMT off, `isolcpus`+`nohz_full`+`rcu_nocbs` on 16 shard cores, IRQs masked; `oracle-mark` producer on a separate NUMA-local core; `mpstat` shows `nr_running == 1` per shard core. The script *calls* `latency-lab provision --check`; it does not re-implement it.
    - R3. Warmup ≥ 30s of sustained 1M-orders/s + 10 ticks/s before sampling; cross-checked via record-start minus binary-start; the full-recompute path verified near-zero self-time (incremental path is the steady state).
    - R4. Sampling frequency prime (`-F 997`); cross-checked with `-F 4001`; top-3 intersection ≥ 2 of 3.
    - R5. Capture topdown (`-M`) + cache + dTLB + `node-load-misses` for 60s; `perf record -F 997 -g --call-graph dwarf` for 60s; flamegraph via `inferno`.
    - R6. Repeatability: top-3 self-time functions ± 5% across 3 runs, same seed, same `MarkTick` stream.
    - R7. Acceptance gates: per-account delta IPC ≥ 2.0; LLC-miss% < 5%; `node-load-misses` < 1% of loads. Any miss → named as the week's focus.
    - R8. The rig drives the **capstone** (16 shards, 100k accounts, real `oracle-mark` ticks), not a microbench; `top3.json` reflects risk-convergence functions.
  - **Design walk-through** D1..D8 — answer in the design doc; no peeking at Gregg/`latency-lab` internals until Phase 3:
    - D1. Enumerate every profiling approach (perf-DWARF, perf-fp, `cargo flamegraph`, VTune, `samply`, cachegrind) with a property table (overhead? call-graph accuracy on Rust? per-event topdown? NUMA counters? needs root?). Pick perf-DWARF-via-`latency-lab` and justify.
    - D2. Why delegate provisioning to `latency-lab` rather than re-write it here? (Answer: no primitive twice; `latency-lab` is the bar-(c) measurement substrate reused by every venue bench — a divergent second provisioner means two definitions of "isolated," and the M30 numbers stop being comparable across crates.)
    - D3. Topdown method: what does Backend-Bound > 50% tell you vs Bad-Speculation > 20%? Which one points at Tue's data-layout fix and which at branch-heavy delta logic?
    - D4. NUMA: walk the scenario where the `oracle-mark` producer on a remote node makes the fan-out look 15% slower than it is — and the scenario where it doesn't matter (single-socket rig).
    - D5. Inclusive vs `--no-children` self-time when reading a *fan-out* flamegraph: why does inclusive time hide which per-account function is hot?
    - D6. Steady-state verification: how do you *prove* the incremental path (not full recompute) is what you profiled? (full-recompute self-time near zero; circuit-breaker count == 0 during the window.)
    - D7. **Pre-mortem (load-bearing):** write the 6-step trace where (a) you profile with the `oracle-mark` producer accidentally co-scheduled on a shard core, (b) flamegraph shows `recv_mark_tick` at 40% self-time, (c) you spend two days optimizing tick deserialization, (d) re-profile with the producer pinned to its own NUMA-local core, (e) self-time drops to 4% — the 40% was producer/consumer contention on a shared core, (f) R2's `nr_running==1` + separate-core check would have refused the run. Convince yourself R2 prevents this.
    - D8. **Anti-shortcut audit:** name the obvious reach for each precondition + one line on why it's wrong HERE — (a) `cargo flamegraph` one-liner (dev-ish build + fp call-graph → mislabels self-time on the deep fan-out stack); (b) `perf record -F 99` (aliases with the 10-ticks/s mark cadence — you'd literally miss the event you're profiling); (c) `taskset` alone without `isolcpus` (`ksoftirqd` preempts a "pinned" shard core); (d) re-implementing provisioning instead of calling `latency-lab` (two definitions of isolated → incomparable M30 numbers).
  - 🧮 **Paper drill** — write in `notes/111_mark_tick_profile_drill.md`:
    - D-CAP on 100k accounts × 10 mark-ticks/s fanned across 16 shards: fan-out the load (→1M recomputes/s naïve; →6.25k accounts/shard/tick), shard the arithmetic (per-account delta ns × accounts/shard → per-shard burst per tick), derive the sub-tick **p99.99** budget (inter-tick interval = 100ms minus tail headroom), and state whether incremental recompute becomes *mandatory*.
    - D-COST on one per-account delta read on this rig: per-op cost table (L2-resident position read ≈ 4ns vs LLC-miss ≈ 100ns vs cross-NUMA `node-load-miss` ≈ 150ns), the cliff ratio (~25× LLC, ~37× NUMA), and "fits / busts" the per-account budget by Xns — the cliff Tue/Wed must climb down.
    - D-LAYOUT on the per-shard position store: byte map with 64B cache-line boundaries (x86) / 128B (aarch64); mark which account's position straddles a line or false-shares with its neighbor, and which `node`'s memory it was first-touched on.
  - **Output**: `notes/111_mark_tick_profile_design.md` with R1–R8, D1–D8, the provisioning-delegation note, sample `perf` invocations, and the NUMA/pre-mortem traces; plus `notes/111_mark_tick_profile_drill.md` with the worked paper drills.

  #### Phase 2 — Write the failing tests (~20 min)

  Each test fails until the rig + binary + provisioning discipline is correct. Tier-annotated; a naive `cargo flamegraph` one-liner fails the EXPERT/SENIOR ones. Required trap shapes labeled 🪤.

  - [ ] **(SENIOR — anti-shortcut trap 🪤)** `script_refuses_non_release_binary` — invoke against `target/debug/perp-dex-core`; assert exit != 0 and the error names the build-profile mismatch. The naive script (invokes by name, finds debug first) profiles it and the flamegraph shows `Vec::push` bounds-checks at 20% self-time. Exercises R1.
    ```rust
    #[test]
    fn script_refuses_non_release_binary() {
        let out = run_profile_sh(&["--bin", "target/debug/perp-dex-core", "16", "5"]);
        assert_ne!(out.status.code(), Some(0), "rig profiled a debug binary");
        assert!(out.stderr.contains("build profile"), "error must name the cause, got: {}", out.stderr);
    }
    ```
  - [ ] **(SENIOR)** `delegates_provisioning_to_latency_lab` — assert the script invokes `latency-lab provision --check` (not a hand-rolled turbo/governor poke); the discipline is reused, not re-implemented. Exercises R2.
  - [ ] **(SENIOR)** `provisioning_check_blocks_when_turbo_on` — run with turbo enabled; assert exit with a remediation message. Exercises R2.
  - [ ] **(EXPERT — cross-primitive trap 🪤)** `top3_are_risk_convergence_not_match_loop` — parse `top3.json`; assert the top-3 are risk-engine fan-out functions and that `full_portfolio_recompute` self-time < 1% (steady state must be incremental). Composes the rig with the live capstone threading — fires if the incremental path isn't wired or the recompute path leaked into steady state. Exercises R3, R8.
  - [ ] **(EXPERT — cross-primitive trap 🪤)** `flamegraph_top3_stable_across_3_runs` — run 3× same seed + same `MarkTick` stream; assert top-3 intersection ≥ 2 of 3. Fires only when the shard cores aren't isolated (kernel threads preempt). Exercises R6.
  - [ ] **(EXPERT — anti-shortcut trap 🪤)** `freq_997_vs_4001_agree_on_top3` — profile `-F 997` then `-F 4001`; assert top-3 intersection ≥ 2. A single-frequency profile silently aliases with the 10-ticks/s mark cadence. Exercises R4.
  - [ ] **(SENIOR)** `numa_node_miss_pct_meets_threshold_or_alert` — parse `node-load-misses`; assert < 1% OR a named-focus failure ("cross-NUMA X% — producer not NUMA-local"). Exercises R7.
  - [ ] **(SENIOR)** `ipc_meets_threshold_or_alert` — parse delta-loop IPC; assert ≥ 2.0 OR "IPC X.X < 2.0 — this is the week's focus". Exercises R7.
  - [ ] **(SENIOR)** `llc_miss_pct_meets_threshold_or_alert` — < 5% OR explicit named-focus failure. Exercises R7.
  - [ ] **(NEWBIE)** `warmup_at_least_30s` — assert warmup ran ≥ 30s via record-start minus binary-start. Exercises R3.
  - [ ] **(EXPERT — negative-control "test-the-test" 🪤)** `rig_actually_samples_something` — assert `perf-stat.txt` reports > 0 samples AND total cycles land in a physically-plausible band for a 60s run on a 3.5GHz core (≥ 1e11, ≤ 2.5e11). Proves the rig measured something real, not an empty/crashed run that passes the "no-error" tests vacuously. Exercises R5.

  #### Phase 3 — Make the tests pass, one failure at a time (~35 min)

  Newbie failures first, expert last — the ladder you actually walk.

  - **(NEWBIE) When `warmup_at_least_30s` reports warmup = 0s:**
    - Symptom: record-start ≈ binary-start; the histogram captured cold first-touch faults on the 100k-account position store.
    - Why: the script launched the SUT and immediately attached perf with no warmup gate.
    - Fix: block on a `/proc`-readable "warm" marker the SUT writes after 30s of sustained ingest + ticks before `perf record` starts.
    - Cost avoided: a profile of page faults, not your fan-out (correctness-of-measurement).
    - Muscle: a profile of a cold process measures faults; warm before you sample. Reapplies at: W111 Thu p99.99 bench warmup, W113 PR-review bench protocol, W120 production capture.

  - **(SENIOR) When `delegates_provisioning_to_latency_lab` fails because the script hand-rolled turbo/governor pokes:**
    - Symptom: the rig works but `latency-lab provision --check` is never called; two definitions of "isolated" now exist.
    - Why: "it was faster to inline the three `echo > /sys/...` lines than wire the dependency."
    - Fix: delete the inline pokes; call `latency-lab provision --check` and fail on its non-zero exit. The provisioning primitive is built once.
    - Cost avoided: incomparable M30 numbers across crates (the whole point of `latency-lab` is one definition of a quiet rig). Re-merging measurement is the v3 "no primitive twice" trap at the measurement layer.
    - Muscle: measurement provisioning is a single primitive — consume `latency-lab`, never re-define it. Reapplies at: W111 Thu bench, W113 ARM CI gate, W120 production capture.

  - **(SENIOR) When `script_refuses_non_release_binary` profiles a debug binary anyway:**
    - Symptom: flamegraph shows `Vec::push`/bounds-check at 20% self-time.
    - Why: invoked by name; found `target/debug/perp-dex-core` first in PATH; never verified the profile.
    - Fix: `file <bin> | grep "not stripped"` AND parse the Rust build-id; refuse on mismatch.
    - Muscle: profiling discipline is a *script invariant*; never trust the operator to remember `--release`. Reapplies at: W111 Thu bench, W120 production capture.

  - **(EXPERT) When `top3_are_risk_convergence_not_match_loop` shows `full_portfolio_recompute` at 30% steady-state self-time:**
    - Symptom: the recompute path dominates the flamegraph even with no circuit-breaker firing.
    - Why: the incremental-margin path isn't wired — every mark tick triggers a full per-account recompute (the §4A anti-pattern made literal). This is *exactly* the bug the week exists to fix; the rig surfaced it before you optimized the wrong thing.
    - Fix: defer to Tue — Tue's incremental-margin delta path makes recompute fire only on circuit-breaker/param-change; re-profile to confirm recompute self-time < 1%.
    - Cost avoided: profiling and "optimizing" the full-recompute path — which is the *correct* path to keep slow-but-rare; the lever is to stop calling it, not to speed it up. (bar (c) violation if shipped: acceptance #8 demands the incremental path.)
    - Muscle: at capstone scale, the first profiling win is often "you're calling the wrong path," not "this function is slow." Reapplies at: W111 Tue incremental margin, W113 hot-path PR review.

  - **(EXPERT) When `flamegraph_top3_stable_across_3_runs` reports 3 disjoint top-3 sets:**
    - Symptom: run1/run2/run3 disjoint; the profile is noise.
    - Why: the 16 shard cores weren't isolated — `ksoftirqd`/kworker preempted; samples landed in kernel functions. `taskset` alone doesn't stop the kernel scheduling there.
    - Fix: kernel-boot `isolcpus`/`nohz_full`/`rcu_nocbs` on the shard cores; mask `/proc/irq/*/smp_affinity`; assert `mpstat` `nr_running == 1` per shard core. (This is `latency-lab provision`'s job — see the prior card.)
    - Cost avoided: two wasted days optimizing a function whose self-time was contention. (bar (c): an un-isolated rig disqualifies every number, and the M30 gate quotes these numbers.)
    - Muscle: profile noise is almost always a rig-provisioning failure; fix the rig before blaming the code. Reapplies at: W111 Thu bench, W113 PR review, W120 capture.

  - **(EXPERT) When `numa_node_miss_pct_meets_threshold_or_alert` shows 18% cross-NUMA misses:**
    - Symptom: `node-load-misses` = 18% of loads; per-account delta IPC reads low despite a clean cache-miss%.
    - Why: the `oracle-mark` producer and the risk shards landed on opposite NUMA nodes; every `MarkTick` crosses the interconnect, and the shard's position store was first-touched on the wrong node.
    - Fix: pin the producer NUMA-local to the shards it feeds; first-touch the per-shard position store on the shard's own node (`numactl --membind` / first-touch policy). Re-profile.
    - Cost avoided: a 15%-of-budget cross-NUMA tax that the cache-miss% alone would not reveal — invisible without `node-load-misses`. (bar (c): NUMA-remote memory disqualifies the p99.99 claim.)
    - Muscle: at fan-out scale, NUMA placement is a first-class budget line; measure `node-load-misses`, not just LLC. Reapplies at: W111 Thu bench NUMA placement, W113 cross-arch matrix, W120 capture.

  - **(EXPERT) When `ipc_meets_threshold_or_alert` shows IPC = 1.4 on the delta loop:**
    - Symptom: per-account delta IPC ~half the x86 ceiling; topdown shows Backend-Bound > 55%.
    - Why: the position store is array-of-structs / pointer-chasing — each account's position is a cache miss; the delta math waits on memory.
    - Fix (this week): Tue's struct-of-arrays / cache-line-packed position layout collapses the indirection; targets IPC ≥ 2.0 as a side effect.
    - Cost avoided: ~50% of cycles stalled on memory instead of computing margin — the dominant cost at 100k accounts. (bar (c) violation if shipped.)
    - Muscle: IPC < 2.0 + Backend-Bound high = memory-bound; data layout is the lever, not micro-op tuning. Reapplies at: W111 Tue layout, W113 hot-path PR.

  - **5-year failure mode** (write in `notes/111_mark_tick_profile_followup.md` after green): when the rig moves to AMD Zen-5 / ARM Neoverse (different cache hierarchy, prefetchers, IPC ceiling) OR marks go per-trade (cadence ≫ 10/s collapses the inter-tick budget), the absolute IPC ≥ 2.0 / LLC < 5% gates stop making sense. Migration trigger: rig hardware change or mark-cadence change. Name an architecture-relative gate ("IPC ≥ 50% of measured topdown theoretical peak") and a cadence-relative budget ("per-account delta ≤ inter-tick-interval / accounts-per-shard / safety").
- [ ] Commit + log (~10 min)

## Tuesday — Incremental margin: delta on each position/price change (the mandatory hot path)

- [ ] [HFT] **Build (bar (c))**: `crates/risk-engine/src/incremental_margin.rs` — replace the per-mark-tick **full portfolio recompute** with an **incremental delta**: maintain a running per-account margin aggregate (IM/MM contributions, netted across instruments at the const-30%-haircut) and, on each `MarkTick` / position change, apply only the **delta** for the affected positions. Full recompute is **reserved for circuit-breaker / parameter-change events**. (~total 90 min across 3 phases)
  - 🔧 **Why this matters**: this is the §4A back-of-envelope made real. A naïve full recompute = 100k accounts × 10 ticks/s = **1M recomputes/s** ≈ 1 CPU-second/second single-threaded; it *barely* fits at exactly 10 ticks/s on 16 shards and **blows the budget at any higher cadence**. Per §4A, *"at this scale the incremental-margin hot path is mandatory, not a fallback."* The delta path turns "recompute every account every tick" into "recompute only the contributions that changed" — typically a handful of positions per account, and only the accounts holding the marked instrument. But it introduces a sharp new failure class: **aggregate drift** — if any mutation path (open, close, partial-fill, funding-debit-from-`oracle-mark`, liquidation-partial) updates a position but forgets to update the running margin aggregate, the aggregate silently diverges from the truth, and a wrong aggregate is a **wrong liquidation decision**. The defense is the same discipline as W111-v2's `total_qty` choke point, raised to portfolio scope: a single `mutate()` choke point + a periodic *full-recompute reconciliation* that asserts `incremental == full` (and is itself the circuit-breaker path, so it's exercised, not dead). **v3 note**: this is *the* perf lever the week turns; Monday's profile motivates it, Thursday's p99.99 bench validates it, Blog #5 (W112) headlines it. Downstream consumers: W111 Wed's local-applied-state read serves the incremental aggregate in one load; W112 Blog #5 cites the incremental-vs-full speedup; the §4A acceptance #8 budget is met *only* with this path live.

  - 🔧 **Decomposition prompt (silent test)**: this spec doesn't name primitives. The per-account aggregate needs zero-alloc + cache-line packing (Mon's IPC finding); the per-instrument contribution math needs integer-only (the W74/W90 determinism gate — no `f64`, or replicas diverge and VOPR breaks); the reconciliation needs to *be* the circuit-breaker recompute (don't build two recompute paths — no primitive twice); the netting haircut is the const-30% from §4A (deterministic, not rolling correlation). The spec names none of them — your toolkit (struct-of-arrays layout, integer bps/lots/ticks, the `mutate()` choke pattern, the const-haircut lock) must combine to meet the budget *without* breaking byte-identity. If the incremental aggregate drifts from a full recompute, or `dhat` shows allocs on the delta path, or cross-replica hashes diverge — your decomposition missed something.
  - 🎯 **Hardened acceptance for the incremental-margin hot path**:
    - **Scope/lever gate (v3)**: `on_mark_tick` applies a **delta**, never a full recompute, in steady state; `full_recompute()` is called ONLY from the circuit-breaker / param-change path (a grep/CI assert: `on_mark_tick` does not call `full_recompute`).
    - **Netting lock (§4A)**: cross-instrument netting credits exactly **70% of the offset** (const 30% haircut); deterministic, integer-only, no rolling correlation in the core (CI grep: no correlation/`f64` in the delta path).
    - **Compute budget**: per-account delta ≤ ~1µs; per-mark-tick fan-out across the marked instrument's holders fits the §4A inter-tick budget on 16 shards (validated Thu).
    - **Zero allocation on the delta path (dhat-verified)**: zero heap allocations across 1M deltas. Per-shard aggregate store is fixed-capacity for 100k/16 ≈ 6.25k accounts/shard, pre-sized.
    - **Aggregate-equals-full invariant**: `incremental_aggregate(account) == full_recompute(account)` for 100% of accounts after every reconciliation cycle, across 100 random scenarios × 10k mutations. ZERO drift.
    - **Cross-replica byte-identical**: the incremental aggregate hash matches across 3 VSR replicas at every commit point (the W74/W90 determinism gate survives the optimization).
    - **Cross-arch byte-identical**: x86 + aarch64 on the same applied log → same aggregate hash (no `f64` in the delta path).
    - **Circuit-breaker / param-change recompute correctness**: on a param change (margin-rate bump) or circuit-breaker, the full recompute runs, and the incremental aggregate equals it afterward (reconciliation is the recompute path).
    - **VOPR-green**: the existing `perp-dex-core` VOPR suite passes with the incremental path live (10 min); no liquidation-decision divergence vs the full-recompute reference.
    - **5-year failure mode**: state one shift (per-trade marks; an RWA-perp whose correlation ≠ 0.7 invalidates the const haircut; >100k accounts spilling the per-shard aggregate out of L2) that forces a redesign.

  #### Phase 1 — Problem, prediction, design (~30 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in `crates/risk-engine/notes/111_incremental_margin_prediction.md`:
    - P1. Write the field declaration you'd reach for first for the per-account margin aggregate (a `HashMap<AccountId, Margin>`? a `Vec<Margin>` indexed by account? a struct-of-arrays?). One line from memory.
    - P2. Predict the speedup `full_recompute_per_tick / incremental_delta_per_tick` at 100k accounts, 2 instruments, when a mark tick touches only one instrument's holders (one number). Reference: incremental wins are workload-shaped — don't over-claim; a tick touching 50% of accounts wins less than one touching 5%.
    - P3. Predict the `size_of` your per-account aggregate and whether 6.25k of them fit in one shard core's L2 (one number + yes/no). Reference: 256KB–1MB L2; aim the working set < L2.
    - P4 (determinism): predict whether two replicas applying the same delta sequence produce byte-identical aggregates if a netting step uses `f64` for the 0.7 haircut (yes/no + why).
    - P5 (drift): name the mutation path you're most likely to forget to route through the aggregate choke point (open? close? partial-fill? funding debit? partial-liq?). Then **list every Phase-2 test you predict your first attempt will FAIL.** Seal it.
  - *Mental model*: the incremental aggregate turns portfolio margin from a *pure function recomputed every tick* into a *running sum maintained by deltas*, with a periodic full recompute as the source-of-truth reconciliation (which *is* the circuit-breaker path). The mark tick stops being "recompute 100k accounts" and becomes "for the holders of the marked instrument, apply the price delta to their cached contribution and re-net." It is deterministic by construction (integer-only, const haircut) so it survives VSR replication and VOPR.
  - *Contract* (signature only — NEVER the body):
    ```rust
    pub struct ShardMarginState {
        /* struct-of-arrays per-account aggregates, fixed-cap ~6.25k; integer-only; cache-line packed */
    }
    impl ShardMarginState {
        // steady state: apply only the delta for positions affected by this tick; integer-only, zero-alloc
        pub fn on_mark_tick(&mut self, instrument: InstrumentId, mark: MarkTick);
        // routed mutation choke point — the ONLY way a position changes; keeps the aggregate in sync
        fn mutate(&mut self, account: AccountIdx, f: impl FnOnce(&mut Position, &mut MarginAgg));
        // reserved: circuit-breaker / param-change ONLY — and the reconciliation reference
        pub fn full_recompute(&mut self, account: AccountIdx) -> MarginAgg;
        pub fn margin(&self, account: AccountIdx) -> MarginAgg;     // one load, no walk (Wed reads this)
        pub fn aggregate_hash(&self) -> [u8; 32];                   // cross-replica/cross-arch compare
    }
    // NOTE (§4A): netting credits exactly 70% of the offset (const 30% haircut) — integer bps, no f64,
    //             no rolling correlation in the core. Correlation → v1.5 (RWA milestone trigger).
    ```
  - *Back-of-envelope* (bar (c) — name the cost): full recompute = 100k × 10/s = **1M recomputes/s**; at ~1µs/account that's ~1 CPU-s/s — fits 16 shards at exactly 10/s with no tail headroom, and at 20/s it's 2 CPU-s/s = **over budget**. Incremental: a mark tick on BTC-PERP touches only BTC holders (say ~40k of 100k) and only their BTC contribution → ~40k deltas/tick × 10/s = 400k deltas/s, each ~200ns (one position read + integer re-net) = ~80ms/s aggregate ≈ **5ms/s/shard** — comfortably under budget with tail headroom. Determinism cost: a single `f64` in the 0.7 haircut = cross-arch divergence (x86 FMA ≠ ARM Neon) → VOPR red → acceptance #2 fails. Integer haircut (`x * 7 / 10` in bps with conservative truncation) = 0 divergence.
  - **The problem this primitive solves**: at the §4A aggressive target, full per-tick recompute is *fundamentally the wrong access pattern* (1M recomputes/s) — not a "tune it later" cost but an architectural ceiling. Incremental margin makes the per-mark-tick cost proportional to *what changed*, not to the account count. But it opens two failure classes: (a) **aggregate drift** (any un-choked mutation path silently desyncs the running aggregate → wrong liquidation); (b) **determinism breakage** (an `f64` haircut or `HashMap`-iterated aggregate breaks cross-replica byte-identity → VOPR red). The fix: a single `mutate()` choke point + a reconciliation that *is* the circuit-breaker full recompute (no second recompute path) + integer-only const-haircut netting.
  - **Requirements** — write in `crates/risk-engine/notes/111_incremental_margin_design.md`:
    - R1. `ShardMarginState` holds a fixed-cap (~6.25k accounts/shard) struct-of-arrays aggregate; cache-line packed; pre-sized, zero-alloc steady state.
    - R2. `on_mark_tick(instrument, mark)` applies **only the delta** for that instrument's holders; never a full recompute (CI grep: `on_mark_tick` does not reference `full_recompute`).
    - R3. **Single mutate() choke point**: every position change (open/close/partial-fill/funding-debit/partial-liq) routes through `mutate()`, which updates the position AND its margin contribution atomically. No path touches a position directly.
    - R4. **Const-30%-haircut netting (§4A)**: cross-instrument offset credited at exactly 70%, integer bps, no `f64`, no rolling correlation in the core.
    - R5. **Determinism gate (inherited from W74/W90)**: no `f64`/`f32`, no `HashMap`/`AHashMap` iteration, no `Instant::now()`/`SystemTime` in the delta path. CI grep + clippy `disallowed-types` enforce.
    - R6. **full_recompute() is the reconciliation AND the circuit-breaker path** — built once, called from both; never a second recompute implementation.
    - R7. **Aggregate-equals-full invariant**: after each reconciliation cycle, `incremental(account) == full_recompute(account)` for all accounts.
    - R8. **Zero alloc on the delta path** (dhat-verified): 0 heap blocks across 1M deltas.
    - R9. **Compute budget**: per-account delta ≤ ~1µs; per-mark-tick fan-out within the §4A inter-tick budget on 16 shards (validated Thu).
    - R10. **Cross-replica + cross-arch byte-identical** aggregate hash.
    - R11. **VOPR-green** with no liquidation-decision divergence vs the full-recompute reference.
  - **Design walk-through** D1..D10 — answer in the design doc; no peeking at TigerBeetle apply-loop until Phase 3:
    - D1. Sketch `ShardMarginState` as struct-of-arrays. Why SoA not AoS here? (Mon's IPC finding: the delta loop streams contributions; SoA keeps them cache-line-dense.) What's the per-account `size_of`? Do 6.25k fit in L2?
    - D2. Write the `mutate()` choke point. Why must EVERY mutation route through it? Name the five mutation paths.
    - D3. Write the `on_mark_tick` delta: given a price change on one instrument, which accounts' contributions change, and by how much (integer)? Why does it never call `full_recompute`?
    - D4. Const-30% haircut: write the integer netting (`offset * 7 / 10` in bps). Why integer, not `0.7_f64`? What rounding rule (truncate-toward-conservative) keeps it deterministic AND never over-credits?
    - D5. Reconciliation = circuit-breaker recompute: why is it the SAME `full_recompute()` called from both, not two implementations? (no primitive twice; and a divergent reconciler would mask the very drift it checks.)
    - D6. Determinism audit: name the banned types/calls; write the grep regex for the CI guard.
    - D7. **Pre-mortem #1 (aggregate drift — load-bearing):** write the 5-step trace where (a) a partial-liquidation reduces a position directly (bypassing `mutate()`), (b) the position shrinks but the cached margin contribution doesn't, (c) the account's MM is now overstated, (d) a tick that should liquidate doesn't (or vice versa), (e) R3's choke point + R7's reconciliation catch it. Convince yourself the choke point is the only safe mutation surface.
    - D8. **Pre-mortem #2 (recompute-still-on-hot-path):** write the 4-step trace where `on_mark_tick` "just calls `full_recompute` for the affected accounts to be safe," collapsing back to ~1M recomputes/s at high cadence. Show how R2's grep + Mon's profile catch it.
    - D9. **Pre-mortem #3 (f64 haircut → divergence):** write the 4-step trace where the 0.7 haircut is `f64`; x86 FMA ≠ ARM Neon; aggregates diverge across the cross-arch matrix; VOPR red; acceptance #2 fails. The fix: integer bps.
    - D10. **Anti-shortcut audit:** name the obvious reach + why it's wrong HERE:
      - *"`HashMap<AccountId, Margin>` for the aggregate"* → wrong: iteration non-determinism breaks cross-replica hash + pointer-chase misses. SoA indexed by `AccountIdx`.
      - *"`full_recompute` the affected accounts each tick, it's simpler"* → wrong: reintroduces the 1M-recomputes/s ceiling at any cadence > 10/s; the whole week's lever is the delta.
      - *"`0.7_f64` for the haircut, it's a ratio"* → wrong: cross-arch divergence; integer `* 7 / 10` bps.
      - *"two recompute paths — one fast-ish for reconcile, one full for circuit-breaker"* → wrong: a divergent reconciler hides drift; one `full_recompute()`, called from both (no primitive twice).
      - *"update the aggregate at each call site, the choke point is ceremony"* → wrong: one forgotten path drifts forever → wrong liquidation; single `mutate()` choke point.
  - 🧮 **Paper drill** — write in `notes/111_incremental_margin_drill.md`:
    - D-DET on the per-account margin recompute path: nondeterminism ledger — list EVERY source (the `0.7` haircut as `f64`, a `HashMap<AccountId,_>` aggregate iterated, any `Instant::now()`, x86 FMA ≠ ARM Neon rounding) and its deterministic replacement (integer `* 7 / 10` bps with conservative truncation, SoA indexed by `AccountIdx`, no clock), and prove `on_mark_tick` is a pure function of `(state, op)` — "replays bit-identically across replicas + x86/aarch64 because every source is eliminated."
    - D-LAYOUT on `ShardMarginState`: byte map of one per-account aggregate (IM/MM/net/equity integer fields) as struct-of-arrays; compute `size_of`, fit 6.25k accounts/shard against the L2 budget (256KB–1MB), and mark the 64B cache-line boundaries the delta loop streams across (SoA-dense, no AoS pointer-chase).
    - D-CAP on a BTC-PERP mark tick at 100k accounts / 2 instruments: full-recompute envelope (100k × 10/s = 1M recomputes/s ≈ 1 CPU-s/s — fits 16 shards at exactly 10/s, busts at 20/s) vs incremental (≈40k BTC-holders × 10/s = 400k deltas/s × ~200ns ≈ 5ms/s/shard); state the budget verdict and why incremental is mandatory above ~10/s.
  - **Output**: `notes/111_incremental_margin_design.md` with R1–R11, D1–D10, the SoA byte-layout diagram, the integer-haircut derivation, the choke-point map (5 mutation paths → `mutate()`), the drift + f64 + recompute-creep traces; plus `notes/111_incremental_margin_drill.md` with the worked paper drills.

  #### Phase 2 — Write the failing tests (~25 min)

  Tier-annotated; a naive impl fails the EXPERT/SENIOR ones. Required trap shapes labeled 🪤.

  - [ ] **(NEWBIE)** `delta_path_never_calls_full_recompute` — CI grep + a unit hook asserting `on_mark_tick` does not invoke `full_recompute` in steady state. Exercises R2.
    ```rust
    // CI companion: assert `on_mark_tick` body contains no `full_recompute(` call
    #[test]
    fn delta_path_never_calls_full_recompute() {
        let mut s = ShardMarginState::new_test(6_250);
        s.warm();
        let calls = count_full_recompute_calls(|| s.on_mark_tick(InstrumentId::BTC, mark(100_00)));
        assert_eq!(calls, 0, "on_mark_tick triggered {calls} full recomputes — steady state must be incremental");
    }
    ```
  - [ ] **(NEWBIE)** `netting_haircut_is_integer_70pct` — assert a fully-offsetting BTC/ETH pair credits exactly 70% of the offset via integer bps; `assert!` no `f64` in the result type. Exercises R4.
  - [ ] **(SENIOR — anti-shortcut trap 🪤)** `dhat_delta_path_zero_alloc` — the obvious `HashMap` aggregate or boxed contribution passes correctness but allocates; this fires on the BAR. Exercises R8.
    ```rust
    #[test]
    fn dhat_delta_path_zero_alloc() {
        let _p = dhat::Profiler::builder().testing().build();
        let mut s = ShardMarginState::new_test(6_250); s.warm();
        let before = dhat::HeapStats::get();
        for i in 0..1_000_000u64 { s.on_mark_tick(InstrumentId::BTC, mark(100_00 + (i % 50) as i64)); }
        let after = dhat::HeapStats::get();
        assert!(after.total_blocks - before.total_blocks <= 100,
            "delta path allocated {} blocks over 1M ticks — SoA + integer-only, no HashMap",
            after.total_blocks - before.total_blocks);
    }
    ```
  - [ ] **(EXPERT — cross-primitive trap 🪤)** `incremental_equals_full_after_reconcile` — composes the delta path with the full-recompute reconciliation; each is plausible alone, but a missed mutation path drifts only under composition. Exercises R3, R6, R7.
    ```rust
    #[test]
    fn incremental_equals_full_after_reconcile() {
        for seed in 0..100u64 {
            let mut s = ShardMarginState::new_test(6_250);
            let ops = random_mutations(seed, 10_000); // opens, closes, partial-fills, funding debits, partial-liqs
            for op in &ops { s.apply_mutation(*op); }
            for a in 0..s.account_count() {
                assert_eq!(s.margin(a), s.full_recompute(a),
                    "incremental aggregate drifted from full recompute at account {a}, seed {seed} \
                     — a mutation path bypassed mutate()");
            }
        }
    }
    ```
  - [ ] **(EXPERT)** `cross_replica_aggregate_hash_identical` — 3 replicas apply the same committed delta sequence; assert `aggregate_hash()` matches at every commit point. A `HashMap`-iterated aggregate diverges only under cross-replica composition. Exercises R5, R10.
  - [ ] **(EXPERT — anti-shortcut trap 🪤)** `cross_arch_aggregate_hash_identical` — same applied log on x86 + aarch64 CI; `assert_eq!(x86_hash, arm_hash)`. Traps an `f64` haircut. Exercises R4, R5, R10.
  - [ ] **(SENIOR)** `circuit_breaker_triggers_full_recompute_and_reconciles` — fire a param-change (margin-rate bump); assert `full_recompute` ran AND the incremental aggregate equals it afterward. Exercises R6, R7.
  - [ ] **(EXPERT — bar (c))** `per_account_delta_under_1us` — criterion bench; assert per-account delta P99 ≤ 1µs. Exercises R9.
  - [ ] **(EXPERT — anti-shortcut trap, bar (c) 🪤)** `incremental_beats_full_at_target` — A/B at 100k accounts / 10 ticks/s: assert `incremental_per_tick / full_per_tick ≤ 0.30` (and record the actual ratio; don't over-claim). The full-recompute path passes correctness but blows the per-mark-tick budget. Exercises R2, R9.
  - [ ] **(EXPERT — negative-control "test-the-test" 🪤)** `ab_bench_actually_runs_both_arms` — assert both `incremental_per_tick` and `full_per_tick` are non-zero and land in a plausible band (1µs–1s per tick) before computing the ratio, so a crashed/empty arm can't make the ratio test pass vacuously. Exercises R9.
  - [ ] **(SENIOR — bar (c))** `vopr_no_liquidation_divergence` — run `perp-dex-core` VOPR 10 min with the incremental path live; assert no liquidation-decision divergence vs the full-recompute reference. Exercises R11.
  - [ ] **(NEWBIE)** `lint_no_float_in_delta_path` — clippy `disallowed-types = ["f64","f32"]` on the delta module; CI asserts build fails on an injected `f64`. Exercises R5.

  #### Phase 3 — Make the tests pass, one failure at a time (~25 min)

  Ordered newbie → expert — the ladder you actually walk.

  - **(NEWBIE) When `netting_haircut_is_integer_70pct` credits 100% (no haircut):**
    - Symptom: a fully-offsetting BTC/ETH pair nets to zero margin; the const-30% haircut never applied.
    - Why: the netting step credited the raw offset; the §4A 70% rule wasn't wired.
    - Fix: credit `offset * 7 / 10` in integer bps, truncating toward conservative (under-credit, never over-credit). Document: correlation breaks toward 1 in a crash, so under-crediting is the safe default (§4A rationale).
    - Cost avoided: over-credited netting → under-margined accounts → an unbacked liquidation cascade (correctness; acceptance #1 fails).
    - Muscle: the netting haircut is a const, deterministic, integer, conservative-rounding lock — rolling correlation is v1.5 (RWA trigger). Reapplies at: W112 Blog #5 netting section, W124 v1.0 final.

  - **(NEWBIE) When `delta_path_never_calls_full_recompute` finds a `full_recompute` call inside `on_mark_tick`:**
    - Symptom: the grep/hook fires; steady-state ticks trigger full recomputes.
    - Why: "I called full_recompute for the affected accounts to be safe" — the §4A anti-pattern made literal.
    - Fix: `on_mark_tick` applies the price delta to cached contributions only; `full_recompute` is reachable solely from the circuit-breaker / param-change path.
    - Cost avoided: the 1M-recomputes/s ceiling reintroduced — the entire week's lever defeated. (bar (c): acceptance #8 demands the incremental path.)
    - Muscle: incremental means *the delta*, never "recompute the changed ones"; reserve recompute for rare events. Reapplies at: W111 Wed read path, W112 blog, W113 PR review.

  - **(SENIOR) When `dhat_delta_path_zero_alloc` reports ~120k allocs/sec:**
    - Symptom: heaptrack shows aggregate-related allocations dominating; per-tick P99 erratic (allocator compaction).
    - Why: the aggregate is a `HashMap<AccountId, Margin>` or each contribution is boxed; even integer-correct, the container allocates.
    - Fix: struct-of-arrays indexed by `AccountIdx`, fixed-cap ~6.25k/shard, pre-sized in `warm()`. Integer-only contributions, no `Box`/`Vec` per account. Delta path becomes alloc-free.
    - Cost avoided: ~120k mallocs/sec + cache pollution + tail jitter → 0 allocs; restores predictable per-tick P99. (bar (c): non-zero alloc on the hot path disqualifies the static-mem invariant.)
    - Muscle: every type on a fan-out hot path is `Copy`/pooled/SoA; never `HashMap`, never per-element `Box`. Reapplies at: W111 Wed read store, W113 hot-path memory budget.

  - **(EXPERT) When `incremental_equals_full_after_reconcile` drifts after ~140 mutations (a partial-liq):**
    - Symptom: the incremental aggregate diverges from `full_recompute` right after a partial-liquidation; one account's MM is wrong forever.
    - Why: the partial-liq path reduced the position directly, bypassing `mutate()`, so the cached margin contribution wasn't updated.
    - Fix: route the partial-liq (and every mutation) through the single `mutate(account, |pos, agg| ...)` choke point that updates position AND contribution atomically; no path may touch a position directly. The periodic reconciliation = the circuit-breaker `full_recompute`, so drift is caught and corrected.
    - Cost avoided: a silently-wrong aggregate → a wrong liquidation decision (real money; correctness). The most expensive class — a stale margin number is a wrong liquidation.
    - Muscle: a running aggregate has exactly one disciplined update surface; one bypassed path corrupts forever; reconcile against the source of truth. Reapplies at: W111-v2 `total_qty` choke (the origin), W112 cross-margin invariants, W113 order-book consistency.

  - **(EXPERT) When `cross_arch_aggregate_hash_identical` diverges between x86 and ARM:**
    - Symptom: same applied log → different aggregate hashes on x86 vs ARM; small at first, compounding.
    - Why: an `f64` somewhere in the netting (the 0.7 haircut, or a mark stored as `f64`); x86 FMA rounds differently than ARM Neon; cumulative drift diverges the aggregate.
    - Fix: integer-only (bps/lots/ticks); the haircut is `* 7 / 10`; `MarkTick` is an integer price; `#![deny(clippy::float_arithmetic)]` on the module.
    - Cost avoided: compounding cross-arch divergence → VOPR red → acceptance #2 ("the order/event log is the VSR log, cluster-VOPR proves it") fails. (bar (c) violation of cross-arch determinism.)
    - Muscle: cross-arch determinism = integer arithmetic only, everywhere on the replicated path. Reapplies at: W74 contract, W90 risk-engine, W112 blog determinism section.

  - **(EXPERT) When `incremental_beats_full_at_target` shows a ratio of 0.55 (expected ≤ 0.30):**
    - Symptom: incremental is only ~1.8× faster, not the expected ~3×+.
    - Why: a mark tick is re-netting *all* of each affected account's instruments, not just the changed contribution; the netting step itself walks every position even when only one moved.
    - Fix: cache the per-account *net* alongside the per-instrument contributions; on a tick, adjust the net by the single changed contribution's delta (O(1) per account), not by re-netting all instruments. Record the measured ratio; don't over-claim in the design doc.
    - Cost avoided: shipping a "3× win" you can't reproduce — credibility at the M30 gate; and a per-mark-tick budget that's still too tight at higher cadence. (bar (c) reality check.)
    - Muscle: incremental must be incremental *all the way down* — re-netting on each tick is a hidden full-ish recompute. Amdahl: the slowest sub-step caps the win. Reapplies at: W112 blog bench, W120 perf reviews.

  - **5-year failure mode** (write in `notes/111_incremental_margin_followup.md` after green): when marks go per-trade (cadence ≫ 10/s) the delta cost dominates and even incremental margin needs batching (coalesce ticks within a window before fanning out); when an RWA-perp with correlation ≠ 0.7 is added, the const-30% haircut becomes both wrong and dangerous (§4A trigger) and rolling empirical correlation must land — but that injects non-deterministic floating state the VSR cluster must replicate bit-identically, forcing a deterministic-fixed-point correlation representation. Migration trigger: per-trade marks OR the v1.5 RWA milestone. Name the redesign (tick-coalescing window + fixed-point rolling correlation in the replicated core).
- [ ] Commit + log (~10 min)

## Wednesday — Risk reads from local applied committed state on the leader shard (read-after-commit, no consensus round-trip)

- [ ] Re-read §4A "Projection staleness — two contracts": control/risk plane = **read-after-commit from local applied state on the leader shard**; analytics plane = best-effort bounded staleness + op-token (a *projection*, off the hot path). Transcribe the distinction into `notes/111_read_contracts.md`. (~25 min)
  - 🎯 **Hardened acceptance**: the note states, in one sentence each, (a) why a risk decision served from the analytics contract **fails** acceptance #5 (a stale tick = a wrong liquidation), and (b) why the control-plane read needs NO consensus round-trip (the leader shard reads its own snapshot of the VSR-*applied* log — the op is already committed-and-applied locally before the read sees it).
- [ ] **[v3.2] Boundary note — `txn` v1.1 Percolator MVCC is OFF this per-tick path.** Record in `notes/111_read_contracts.md`: the W94 MVCC snapshot read (~2 µs warm: a write-CF seek + a data-CF point-get) is for **off-tick consistent multi-position reads** (full recompute on circuit-breaker/param-change, reconciliation), NOT the per-mark-tick loop. Back-of-envelope (the trap a reviewer must reject): a 2 µs MVCC seek × ~6,250 accounts/shard/tick ≈ **~12.5 ms/tick** — ~100× over the <100 µs p99.99 budget. The per-tick path stays on the seqlock'd local applied-state read below (~10 ns). **CI/architecture assert**: `risk_read` (the per-tick path) has no edge into `txn::Percolator`/the MVCC snapshot API. (~10 min)
- [ ] [HFT] **Build (bar (c))**: `crates/perp-dex-core/src/risk_read.rs` — a lock-free, single-load **applied-state read** of an account's incremental margin aggregate, served from the **leader shard's local applied committed state** (no consensus round-trip, no cross-node read). The writer is the shard's apply-loop (single-writer per shard); readers are the liquidation check + margin check on the same shard. (~total 85 min across 3 phases)
  - 🔧 **Why this matters**: §4A's control/risk-plane contract is **read-after-commit from local applied state on the leader shard** — *"the risk engine reads its own snapshot of the VSR-applied log — no consensus round-trip per read."* This is the *read-latency* lever (Tuesday's incremental margin is the *compute-cost* lever; §4A: "both required"). The trap is reaching for a consensus read (linearizable read through the VSR protocol = a round-trip = the per-mark-tick budget destroyed) OR a cross-node read (network hop) OR a `Mutex`/`RwLock` that serializes every liquidation/margin reader behind the apply-loop writer through a full per-account critical section. The correct shape is the **seqlock** discipline (single-writer = the apply-loop, multi-reader = risk checks, all on the same shard core, all in L1) raised from W111-v2's top-of-book to the per-account margin aggregate — readers complete in ~10ns, the apply-loop never blocks, and the read is *exactly* as fresh as the locally-applied commit point (read-after-commit, by construction, with no round-trip). **v3 note**: this reuses W111-v2's seqlock pattern + `consensus-vsr`'s applied-state semantics — no new consensus, no new sync primitive (no primitive twice). The analytics plane (dashboards/history) reads a *projection* of the VSR log off the hot path with bounded staleness + an op-token — explicitly NOT this path. Downstream consumers: the liquidation engine's margin-ratio check `load()`s this; Thu's p99.99 bench exercises read+delta together; W112 Blog #5 documents the two-contract split as a principal-level design decision.

  - 🔧 **Decomposition prompt (silent test)**: this spec doesn't name primitives. The single-load read needs a seqlock (single-writer apply-loop, multi-reader risk checks); the aggregate must be 64B/one-line or the reader eats an LLC miss; the read must come from *local applied* state (no consensus, no network) so it needs the leader-shard co-location from `runtime-thread-per-core`; the memory ordering needs Release-on-close / Acquire-on-load or ARM tears. The spec names none of them. If the read P99 blows past ~50ns, or the read serves a *pre-applied* (consensus-pending) value, or loom finds a torn aggregate — your decomposition missed something.
  - 🎯 **Hardened acceptance for the local-applied-state read**:
    - **Contract gate (§4A)**: the read is served from the **leader shard's local applied committed state** — no consensus round-trip, no cross-node hop (a CI/architecture assert: `risk_read` has no edge into the VSR propose path or the network transport).
    - **Read-after-commit**: a read NEVER returns an aggregate from an op that isn't yet *applied* locally; it always reflects every op applied up to the read point (no bounded staleness on this plane).
    - **Single-load, lock-free**: `load()` is a seqlock read — 2 seq loads + the aggregate fields + 1 branch; readers never block the apply-loop writer.
    - **Coherent snapshot**: a returned aggregate is always from one generation, never torn across an apply.
    - **Read-side P99**: ≤ 50ns single-thread; ≤ 100ns under 8 concurrent risk readers; writer (apply-loop) side ≤ 100ns.
    - **64B / one cache line**: `const_assert!` size + alignment; no false sharing with adjacent accounts/symbols.
    - **Memory ordering**: writer's closing seq store = `Release`; reader's seq loads = `Acquire`; field stores between may be `Relaxed`. Loom-verified (single-writer, N-reader): no torn read across all interleavings. 1B-iteration stress on x86 AND ARM: zero torn reads.
    - **Bounded retry + fallback**: reader retry bounded (spin-hint → backoff → after 1024 retries a slow-path read from a durable backing copy + a `risk_read_slow_path_total` metric), so an apply-loop preemption can't livelock the liquidation check.
    - **Analytics-plane separation**: a sibling test asserts that the *analytics* read path (op-token, bounded staleness) is a DIFFERENT entry point and CANNOT be used for a liquidation decision (acceptance #5: a risk decision from the analytics contract fails).
    - **5-year failure mode**: state one shift (cross-shard portfolio margin where an account's positions span shards, so the read is no longer single-shard-local; or >100k accounts spilling the per-shard aggregate out of L1) that forces a redesign.

  #### Phase 1 — Problem, prediction, design (~25 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in `crates/perp-dex-core/notes/111_risk_read_prediction.md`:
    - P1. Name the read mechanism you'd reach for first, sight-unseen, for "risk reads the latest committed margin" (a consensus/linearizable read? a `Mutex<Aggregate>`? a cross-node RPC? atomics?). Write the line from memory.
    - P2. Predict the read-side P99 of your first choice vs a seqlock read (two numbers). Reference: a consensus read = 1 RTT ≈ 100µs–1ms; a `Mutex` under 8 readers ≈ 800ns P99.9; a seqlock ≈ 10ns.
    - P3. Predict the memory ordering each access needs: writer's closing seq store, reader's seq loads, the field stores between (three answers).
    - P4 (contract): predict what a liquidation check sees if it accidentally reads the *analytics* op-token path with 500ms staleness during a fast crash (one sentence — name the consequence).
    - P5. Of the Phase-2 tests below, list every test name you predict your first attempt will fail. Seal it.
  - *Mental model*: the risk read is a seqlock over the per-account margin aggregate, written by the shard's apply-loop and read by the same shard's liquidation/margin checks — all on one core, all in L1. "Read-after-commit from local applied state" falls out for free: the apply-loop writes the aggregate *after* it applies the committed op, so any read sees the latest locally-applied commit with **no consensus round-trip and no network hop**. It is the read-latency complement to Tuesday's compute-cost lever.
  - *Contract* (signature only):
    ```rust
    #[repr(align(64))]
    pub struct AppliedMarginRead { /* seqlock over MarginAgg; 64B; one cache line */ }
    impl AppliedMarginRead {
        // writer = the shard apply-loop, single-writer; called AFTER the committed op is applied locally
        fn store_applied(&self, agg: MarginAgg);     // wait-free w.r.t. readers
        // reader = liquidation / margin check on the same shard; lock-free, retrying; read-after-commit
        pub fn load(&self) -> MarginAgg;             // single-load fast path, bounded retry + slow-path fallback
    }
    // NOTE (§4A): this is the CONTROL/RISK plane (read-after-commit, no staleness). The ANALYTICS plane
    //             (bounded staleness ≤500ms/≤1000ops + op-token) is a SEPARATE projection, off the hot path,
    //             and is NOT usable for a liquidation decision.
    ```
  - *Back-of-envelope* (bar (c)): a consensus/linearizable read = 1 RTT ≈ 100µs–1ms — *thousands* of times the per-account budget; impossible per-account at 100k accounts. A `Mutex` read under 8 risk readers serializes behind the apply-loop's per-account critical section (~200ns) → ~800ns P99.9. Seqlock read = 2 atomic loads + ~4 field reads + 1 branch ≈ **~10ns** (all in one L1 line); writer = fetch_add + field stores + Release ≈ **~30ns**. Cliff ratio: consensus-read ~100µs vs seqlock ~10ns = **~10,000×**; Mutex-contended ~800ns vs ~10ns = **~80×**. The aggregate MUST be 64B/one line or the reader eats a ~100ns LLC miss, erasing the win.
  - **The problem this primitive solves**: at the §4A target a risk decision per mark tick across 100k accounts cannot pay a consensus round-trip per read (it would be ~10,000× over budget) and cannot pay a cross-node hop or a `Mutex` serialization. The §4A control-plane contract — read-after-commit from local applied state on the leader shard — *is* the answer, and a seqlock over the per-account aggregate is its mechanism: read-after-commit by construction (write-after-apply), lock-free, single-load, L1-resident. The failure classes: (a) reaching for a consensus/cross-node read (budget destroyed); (b) a `Mutex`/`RwLock` (reader serialization); (c) serving a *pre-applied* value (stale → wrong liquidation); (d) torn reads on ARM (weak memory). The fix: seqlock discipline + write-after-apply + Release/Acquire pairing + a hard separation from the analytics plane.
  - **Requirements** — write in `crates/perp-dex-core/notes/111_risk_read_design.md`:
    - R1. `AppliedMarginRead` is `#[repr(align(64))]`, size exactly 64B (`const_assert!`); no false sharing with adjacent accounts.
    - R2. Single writer = the shard apply-loop; debug-only `OnceCell<ThreadId>` panics on a second writer.
    - R3. Seqlock: `seq` even = stable, odd = write in flight; reader retries on odd or on a seq change across the read.
    - R4. Memory ordering: writer's closing `seq` store = `Release`; reader's two `seq` loads = `Acquire`; field stores/reads between may be `Relaxed`.
    - R5. **Write-after-apply (read-after-commit)**: `store_applied` is called by the apply-loop AFTER the committed op mutates the aggregate; a read therefore never reflects an un-applied op. NO consensus round-trip, NO cross-node read on this path (architecture assert: no edge into propose/transport).
    - R6. Bounded retry: > 16 spins → `spin_loop` hint + backoff; > 1024 retries → slow-path read from a durable `Mutex<MarginAgg>` backing copy + `risk_read_slow_path_total`. The apply-loop writes the backing copy after the fast path so it's durable when the fast path is jammed.
    - R7. Read-side P99 ≤ 50ns single-thread; ≤ 100ns under 8 readers; writer P99 ≤ 100ns.
    - R8. Loom (single-writer, N-reader): no inconsistent snapshot across all interleavings. 1B-iteration stress (1 writer + 4 readers) zero torn reads on x86 AND ARM.
    - R9. **Plane separation (§4A)**: the analytics read (op-token, bounded staleness) is a separate entry point; a compile/architecture guard prevents a liquidation decision from consuming it.
  - **Design walk-through** D1..D8 — answer in the design doc; no peeking at `seqlock.h`/TigerBeetle until Phase 3:
    - D1. Enumerate every read mechanism (consensus/linearizable read, cross-node RPC, `Mutex`, `RwLock`, double-buffer + atomic ptr swap, seqlock, RCU/epoch) with a property table (round-trip? reader blocks writer? read-after-commit? torn-read risk? cost?). Pick local seqlock and justify against the §4A contract.
    - D2. Why does "read-after-commit from local applied state" need NO consensus round-trip? (The op is committed-and-applied locally before the read; the read reflects the local apply point, not a quorum read.)
    - D3. Why even/odd seq + fetch_add to open? Walk the 4-step write.
    - D4. Why Release-on-close / Acquire-on-load? Walk the bad ARM reordering with Relaxed.
    - D5. Field layout: `MarginAgg` fields + 8-byte seq + padding into 64B. Draw the byte layout.
    - D6. Slow-path fallback: when readers retry forever (apply-loop preempted/SIGSTOP), what's the escape — `Mutex` backing copy vs drop the read? Argue (a dropped liquidation read is an availability failure on the risk plane).
    - D7. **Pre-mortem (load-bearing):** write the 8-step interleaving where (a) the apply-loop opens seq=1, (b) stores `mm`, (c) a liquidation reader sees seq=1, retries, (d) the apply-loop is preempted before closing, (e) seq stays odd, (f) every risk reader livelocks, (g) R6's bound trips the slow path, (h) the liquidation check completes from the durable copy. Convince yourself R6 saves the risk plane.
    - D8. **Anti-shortcut audit:** (a) "a linearizable VSR read — it's the freshest" → wrong: 1 RTT per read = ~10,000× over budget; read-after-commit from *local applied* state needs no round-trip; (b) "`Mutex<MarginAgg>`, it's simple" → serializes 8 risk readers behind the apply-loop critical section (~800ns P99.9); (c) "read the analytics projection, it's already there" → §4A: a risk decision from the bounded-staleness plane FAILS (a 500ms-stale tick = a wrong liquidation); (d) "skip the retry bound" → one apply-loop preemption livelocks every liquidation check.
  - 🧮 **Paper drill** — write in `notes/111_risk_read_drill.md`:
    - D-HB on the seqlock between the apply-loop writer and one liquidation reader: two columns (writer / reader), PO edges each side; mark the Release (writer's closing `seq.store(s+2, Release)`) and Acquire (reader's `seq.load(Acquire)`); draw the RA edge and chain whether the reader's `mm` read sees the writer's `mm` store; verdict sees / doesn't see. **Then the NEGATIVE:** weaken the closing seq store to `Relaxed`, draw the ARM interleaving where the reader sees `im=100` but stale `mm=0` at an even seq (the torn read).
    - D-STALE on two reads against `commit_point` / `applied_point`: a liquidation/risk read and an analytics dashboard read — assign each its REQUIRED contract (risk = read-after-commit from leader-local applied state, a stale risk read is a *correctness* bug; analytics = bounded-staleness ≤500ms/≤1000ops + op-token, metric-not-SLA); show the "new enemy" anomaly (a 500ms-stale tick → wrong liquidation) if the analytics contract is used for the risk read.
    - D-LAYOUT on `AppliedMarginRead`: byte map of the `MarginAgg` fields + the 8-byte `seq` + padding into exactly 64B (`#[repr(align(64))]`); mark the single cache line and confirm no adjacent account's read struct false-shares across the line, on 64B (x86) and 128B (aarch64).
    - D-LOOM on the single-writer / 2-reader model: write threads × shared-ops (1 writer storing one generation, 2 readers loading), the exact assertion ("every reader's `MarginAgg` is from one generation, never torn"), and the order-of-magnitude schedule count; negative-control note — a real loom run takes ≥100ms, so an instant run means loom didn't intercept the atomics.
  - **Output**: `notes/111_risk_read_design.md` with R1–R9, D1–D8, the byte-layout diagram, the write-after-apply sequence chart, the plane-separation guard, the 8-step pre-mortem trace; plus `notes/111_risk_read_drill.md` with the worked paper drills.

  #### Phase 2 — Write the failing tests (~20 min)

  Tier-annotated; required trap shapes labeled 🪤.

  - [ ] **(SENIOR)** `size_aligned_64` — `const_assert!(size_of::<AppliedMarginRead>() == 64)` AND `align >= 64`. Exercises R1.
  - [ ] **(SENIOR)** `single_writer_enforced` — debug build: a second thread calls `store_applied`; assert panic with a clear message. Exercises R2.
  - [ ] **(SENIOR — anti-shortcut trap 🪤)** `read_has_no_consensus_or_network_edge` — architecture/CI assert: `risk_read.rs` has no import/edge into the VSR propose path or the network transport (the §4A "no consensus round-trip" lock). The naive linearizable-read impl fails this. Exercises R5.
  - [ ] **(EXPERT — anti-shortcut trap, §4A 🪤)** `liquidation_cannot_read_analytics_plane` — assert a liquidation-decision call site CANNOT consume the analytics (op-token, bounded-staleness) read; a test that wires it fails to compile or trips a guard. A 500ms-stale read into a liquidation decision is acceptance #5's named failure. Exercises R9.
  - [ ] **(EXPERT)** `read_reflects_latest_applied_op` — apply ops 1..N to the aggregate; assert every `load()` after the apply of op k reflects op k (read-after-commit, never a pre-applied value). Exercises R5.
  - [ ] **(EXPERT — concurrency, loom 🪤)** `loom_no_torn_aggregate_single_writer_2_readers` — `#[cfg(loom)]`: 1 apply-loop writer + 2 risk readers; loom explores all interleavings; assert every reader's `MarginAgg` is from one generation (never torn across an apply). Exercises R3, R4, R8.
    ```rust
    #[cfg(loom)]
    #[test]
    fn loom_no_torn_aggregate_single_writer_2_readers() {
        loom::model(|| {
            let r = Arc::new(AppliedMarginRead::new());
            let w = { let r = r.clone(); loom::thread::spawn(move || {
                r.store_applied(MarginAgg { im: 100, mm: 50, net: 200, equity: 300 }); }) };
            let readers: Vec<_> = (0..2).map(|_| { let r = r.clone();
                loom::thread::spawn(move || {
                    let a = r.load();
                    let written = a == MarginAgg { im: 100, mm: 50, net: 200, equity: 300 };
                    let initial = a == MarginAgg::ZERO;
                    assert!(written || initial, "torn aggregate read: {a:?}");
                }) }).collect();
            w.join().unwrap();
            for rd in readers { rd.join().unwrap(); }
        });
    }
    ```
  - [ ] **(EXPERT — negative-control "test-the-test" 🪤)** `loom_model_actually_explores` — assert the loom run takes ≥ 100ms wall-time (proving it forked the scheduler, not a degenerate single schedule). Exercises R8.
  - [ ] **(EXPERT)** `slow_path_fallback_on_apply_loop_preempt` — simulate the apply-loop SIGSTOP'd after 1024 reader retries; assert the liquidation reader falls back to the `Mutex` backing copy AND `risk_read_slow_path_total` increments. Exercises R6.
  - [ ] **(EXPERT — anti-shortcut trap, bar (c) 🪤)** `read_p99_under_50ns_single_thread` — criterion bench, 1 writer + 1 reader; assert read P99 ≤ 50ns. The `Mutex`/consensus-read shortcut passes correctness but blows this by orders of magnitude. Exercises R7.
  - [ ] **(EXPERT — bar (c))** `read_p99_under_100ns_eight_readers` — 1 writer + 8 risk readers; each P99 ≤ 100ns. Exercises R7.
  - [ ] **(EXPERT — bar (c))** `one_billion_iter_stress_no_torn_reads` — 1 writer + 4 readers for 1B writer iterations; assert zero torn aggregates (run on x86 AND ARM). Exercises R8.

  #### Phase 3 — Make the tests pass, one failure at a time (~25 min)

  - **(SENIOR) When `read_has_no_consensus_or_network_edge` fails because `load()` calls into the VSR read path:**
    - Symptom: the architecture assert fires; `load()` issues a linearizable read through the consensus protocol.
    - Why: "the freshest read is a consensus read" — the instinct to make the read authoritative via quorum.
    - Fix: read from the leader shard's *local applied* aggregate (`store_applied` writes it after apply); the op is already committed-and-applied locally, so the local read is read-after-commit with no round-trip. Delete the consensus-read edge.
    - Cost avoided: ~10,000× over the per-account budget; the §4A control-plane contract violated (acceptance #5). The whole read-latency lever defeated.
    - Muscle: read-after-commit ≠ a consensus read; co-locate the reader with the local applied state and the round-trip vanishes. Reapplies at: W112 Blog #5 read-contract section, W113 read-path review.

  - **(EXPERT — §4A) When `liquidation_cannot_read_analytics_plane` compiles a liquidation reading the bounded-staleness path:**
    - Symptom: a liquidation decision consumes the analytics (op-token) read; under a fast crash it acts on a 500ms-stale margin.
    - Why: "a read is a read" — the two §4A contracts weren't separated at the type/entry-point level.
    - Fix: give the control/risk plane and the analytics plane distinct entry points; a guard (sealed trait / newtype) makes a liquidation call site unable to consume the analytics read. A risk decision from the analytics contract must fail (acceptance #5).
    - Cost avoided: a wrong liquidation from a stale tick — the canonical §4A failure; correctness + the principal-artifact narrative collapses.
    - Muscle: staleness contracts are a type-level distinction, not a comment; the risk plane is read-after-commit, the analytics plane is bounded-staleness, and they don't mix. Reapplies at: W112 Blog #5 (the two-contract story is a headline), W113 read-path review.

  - **(SENIOR) When `single_writer_enforced` doesn't panic on a second writer:**
    - Symptom: two threads call `store_applied`; readers intermittently see corruption; no panic.
    - Why: the single-writer convention was assumed, not checked.
    - Fix: `#[cfg(debug_assertions)] writer_thread: OnceCell<ThreadId>`; first `store_applied` sets it, later calls assert equality; release builds compile it out.
    - Muscle: single-writer invariants need debug-time enforcement; production trusts the shard-thread discipline. Reapplies at: W113 shard-thread invariants, W120 concurrency audit.

  - **(EXPERT — concurrency) When `loom_no_torn_aggregate_single_writer_2_readers` reports a torn aggregate:**
    - Symptom: loom prints a reader seeing `im=100` but `mm=0` (the OLD value) at an even seq.
    - Why: writer used `Relaxed` on field stores and the reader's `Acquire` on the closing seq didn't establish happens-before (no `Release` on close).
    - Fix: writer's closing `seq.store(s+2, Release)`; reader `seq.load(Acquire)` then field reads. The Release/Acquire pair makes the writer's field stores happen-before the reader's field reads when the seq check passes.
    - Cost avoided: silent torn margin reads → wrong liquidation decisions; invisible on TSO x86, fires on ARM. (bar (c) violation.)
    - Muscle: seqlock correctness = Release-on-close paired with Acquire-on-load; field stores may be Relaxed because seq is the sync point. Reapplies at: W111-v2 top-of-book (the origin), W113 lock-free queue, W120 atomics audit.

  - **(EXPERT) When `slow_path_fallback_on_apply_loop_preempt` livelocks the risk readers:**
    - Symptom: apply-loop SIGSTOP'd; liquidation checks spin forever at 100% CPU.
    - Why: the retry loop had no bound; it spun on `seq odd` indefinitely.
    - Fix: bound the retry → after 16 spins `core::hint::spin_loop`; after 1024 retries increment `risk_read_slow_path_total` and read the `Mutex<MarginAgg>` backing copy; the apply-loop writes that copy after the fast path so it's durable when the fast path is jammed.
    - Cost avoided: a livelock that wedges every liquidation/margin check when the apply-loop is preempted — an availability failure on the risk plane. (bar (c) violation.)
    - Muscle: every spin loop needs a bound + a fallback; unbounded spin is a livelock. Reapplies at: W111-v2 top-of-book retry, W113 lock-free queue retry.

  - **(EXPERT — perf) When `read_p99_under_50ns_single_thread` shows 180ns:**
    - Symptom: P99 ~3.6× target; profile shows the aggregate read causing a cache miss.
    - Why: `AppliedMarginRead` lives in a `Vec` whose allocator returned an unaligned base; `#[repr(align(64))]` is honored on the stack but `Vec` doesn't guarantee per-element 64B alignment on every allocator.
    - Fix: a 64B-aligned per-shard array (custom aligned alloc or `Box`-per-element with an alignment assert at construction); assert `(self as *const _ as usize) % 64 == 0`.
    - Cost avoided: ~100ns LLC miss per read = the seqlock win erased; green test, RED bench. (bar (c) violation.)
    - Muscle: align-to-64 needs allocation-time verification; the annotation alone isn't enough on the heap. Reapplies at: W111 Tue SoA aggregate alignment, W113 cache-line-aware structs.

  - **(EXPERT — concurrency) When `one_billion_iter_stress_no_torn_reads` reports 31 torn reads on ARM:**
    - Symptom: 1B iterations, 31 reads with fields from different generations — only on ARM.
    - Why: a field store after the Release close (e.g. a later-added `equity`) had no Release, so on weak memory it floated past the seq close.
    - Fix: keep ALL field stores before the single closing `seq.store(_, Release)`; a later-added field goes inside the same write window. Verify the 31 were pre-fix.
    - Cost avoided: a weak-memory bug invisible on x86 — would ship green and corrupt margin reads on ARM hosts. (bar (c) violation.)
    - Muscle: stress tests reveal weak-memory bugs invisible on x86; always run on ARM. Reapplies at: W113 ARM CI gate, W120 cross-arch matrix.

  - **5-year failure mode** (write in `notes/111_risk_read_followup.md` after green): when cross-shard portfolio margin lands (an account's positions span shards, so the margin read is no longer single-shard-local) the local seqlock no longer suffices — the read must compose per-shard partials, reintroducing a cross-shard coordination that a per-shard seqlock can't serve at read-after-commit freshness. Migration trigger: cross-shard accounts (account-sharding boundary crossed). Name the redesign (account-affinity sharding so a portfolio stays single-shard; or a per-shard partial-aggregate roll-up with a bounded cross-shard read epoch).
- [ ] Commit + log (~10 min)

## Thursday — Bench: per-mark-tick p99.99 at 100k accounts (coordinated-omission-correct, via `latency-lab`)

- [ ] [HFT] **Build (bar (c))**: `crates/perp-dex-core/benches/mark_tick_p99_99.rs` + `crates/perp-dex-core/tests/mark_tick_bench_meta.rs` — the headline number: **per-mark-tick p99.99 of the cross-margin recompute fanned out across 100k accounts** at 2 instruments / ~1M orders/s ingest / 10 ticks/s, captured with `latency-lab`'s coordinated-omission-correct HdrHistogram + rdtsc, made a permanent regression watchdog. This is the number the M30 bet-readiness gate (§4A acceptance #8) reads. (~total 75 min across 3 phases)
  - 🔧 **Why this matters**: this is the *contract* for §4A acceptance #8 — *"the latency report (`latency-lab`) shows the per-mark-tick p99.99 budget is met at this account count."* Without a documented, repeatable, omission-correct, rate-shaped number, the founding-engineer latency narrative (the hardest part of the bet) is hand-wave. The classic lies all apply at fan-out scale and are *worse* here: coordinated omission hides the queueing of a 100k-account fan-out behind a 10-ticks/s feed (the LAST account's true latency includes the whole fan-out wait, but uncorrected `record` logs only its own process time); insufficient warmup measures cold first-touch on the 100k-account store; a feed-rate mismatch measures "as fast as possible" not the §4A cadence. **v3 note**: the bench does NOT re-implement omission correction or rdtsc — it *consumes `latency-lab`'s* `record_corrected_with_max_interval` + rdtsc tick-to-trade (no primitive twice); this is `latency-lab`'s first capstone-scale exercise. First-attempt benches fail ≥1 test below — most commonly `bench_uses_latency_lab_corrected` (you called raw `record`, so the reported p99.99 is a fantasy) or `bench_measures_per_tick_fanout` (you measured per-account delta latency, not the per-mark-tick fan-out tail across all 100k holders — a different, easier, wrong number). Downstream consumers: this becomes the acceptance gate for any `perp-dex-core` PR (W113); W112 Blog #5 cites it as the headline; W124 v1.0 updates it; the M30 gate reads the result JSON.

  - 🔧 **Decomposition prompt (silent test)**: this spec doesn't name primitives. The bench needs `latency-lab`'s corrected recording + rdtsc; Monday's isolated-core provisioning; the live capstone (Tue's incremental margin + Wed's local read); a paced 10-ticks/s feed; a 1M-orders/s ingest background; the §4A budget as the pass/fail. It must measure the *per-mark-tick fan-out tail* (the slowest account in a tick's fan-out, corrected) — not per-account delta. The spec names none of them. If the corrected p99.99 ≈ the uncorrected (omission not applied), or the achieved rate ≠ 10 ticks/s + 1M orders/s, or the number measures per-account not per-tick — your decomposition missed something.
  - 🎯 **Hardened acceptance for the per-mark-tick p99.99 bench**:
    - **Measures the right thing (v3)**: the recorded latency is the **per-mark-tick fan-out completion** (tick arrival → last affected account's margin updated), at 100k accounts / 2 instruments / 1M-orders/s ingest / 10 ticks/s — NOT per-account delta, NOT single-symbol match.
    - **Via `latency-lab`**: uses `latency-lab`'s `record_corrected_with_max_interval(dt, expected = inter_tick_ns)` + rdtsc; never raw `record`, never a hand-rolled histogram (no primitive twice).
    - **Provisioning**: reuses Monday's isolated-core / NUMA-local / SMT-off rig (via `latency-lab provision`); feed-gen + tick producer on separate NUMA-local cores.
    - **Warmup**: ≥ 50k ticks (or ≥ 30s) before recording; adequacy = p50 stable < 5% across 2 runs (warms the 100k-account store first-touch).
    - **Rate-shaped**: ticks paced to 10/s, ingest to 1M orders/s; achieved rates within ±5% of target, recorded alongside the percentiles.
    - **Reports**: p50 / p99 / p99.9 / **p99.99** for the per-mark-tick fan-out from ONE HdrHistogram aggregating all ticks; emits `result.json { seed, accounts, instruments, ingest_rate, tick_rate, p50, p99, p999, p9999, achieved_tick_rate, achieved_ingest_rate }`.
    - **Determinism preserved**: the bench runs the live incremental path with the determinism gate intact (no `f64` snuck in for the bench).
    - **Acceptance gate**: per-mark-tick **p99.99 < the §4A inter-tick budget** (state the number from Mon's `notes/111_capacity_budget.md`); achieved rates within 5%; 7 consecutive nightly greens before tagging the perf-pass.
    - **Regression watchdog**: CI nightly with a 10% regression budget; `bench/baseline.json` committed; integer-comparison gate; failure blocks merge.
    - **5-year failure mode**: state one shift (per-trade marks collapsing the inter-tick budget; AMD/ARM rig changing the absolute number) that retires the absolute p99.99 gate in favor of a budget-relative one.

  #### Phase 1 — Problem, prediction, design (~20 min, no code yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE reading Phase 2)** — write sealed answers in `crates/perp-dex-core/notes/111_mark_tick_bench_prediction.md`:
    - P1. Name the harness call you'd reach for first (`b.iter`? `b.iter_custom`? `latency-lab`'s recorder?). Write the line from memory.
    - P2. Predict the uncorrected per-mark-tick p99.99 vs the `latency-lab`-corrected p99.99 under a fan-out across 100k accounts (two numbers). Reference: coordinated omission commonly hides a 10–100× gap; a fan-out tail hides more.
    - P3. Predict the achieved tick rate if you use `b.iter` while targeting 10/s (one number) — and what that measures instead.
    - P4. Predict whether you'd accidentally measure per-account delta latency instead of per-mark-tick fan-out (yes/no) and the consequence (one sentence: the easier number passes, the real one is unmeasured).
    - P5. Of the Phase-2 tests below, list every test name you predict your first attempt will fail. Seal it.
  - *Mental model*: a latency bench is a *measurement instrument* whose job is to reproduce production conditions (rate-shaped feed, omission-corrected percentiles, warmed steady state) so the per-mark-tick p99.99 you quote at the M30 gate survives contact with a production outage. At fan-out scale the instrument must measure the *tick's* tail (the slowest account in the fan-out), corrected for the queueing of ticks behind a slow tick — and it must do so through `latency-lab`, the one measurement primitive.
  - *Contract*: `mark_tick_p99_99` runs the live capstone (Tue incremental margin + Wed local read) at 100k accounts / 2 instruments / paced 1M-orders/s ingest + 10 ticks/s, records each *tick's* fan-out completion latency into one `latency-lab` HdrHistogram with omission correction (expected interval = the inter-tick interval), and emits `result.json`. `mark_tick_bench_meta.rs` introspects the bench (method calls, warmup count, what it measures, deps) so the discipline is enforced, not assumed.
  - *Back-of-envelope* (bar (c)): target 10 ticks/s = one tick every 100ms = the `expected_interval` for omission correction at the tick granularity; but each tick fans out across ~40k holders, and if a tick's fan-out runs long, the NEXT tick queues. Uncorrected `record` logs each tick's own fan-out time; corrected synthesizes the queueing of ticks delayed behind a slow one — the p99.99 you'd quote in Blog #5. Cliff ratio: uncorrected p99.99 (say ~5ms fan-out) vs corrected-under-a-stall (an apply-loop stall queues several ticks → tens of ms) = **10–100×** — the entire reason this card exists. The §4A budget (from Mon) is the pass/fail line; the corrected number is what's measured against it.
  - **Requirements** R1..R9 — write in `crates/perp-dex-core/notes/111_mark_tick_bench_design.md`:
    - R1. Bench pinned to isolated shard cores (reuse Mon's `latency-lab provision`); feed-gen + tick producer on separate NUMA-local cores.
    - R2. Measures the **per-mark-tick fan-out completion** (tick arrival → last affected account updated), NOT per-account delta. Reports p50/p99/p99.9/**p99.99** from ONE HdrHistogram aggregating all ticks — never average per-tick percentiles.
    - R3. Coordinated-omission correction via `latency-lab`'s `record_corrected_with_max_interval(dt, expected = inter_tick_ns)`. Never raw `record`; never a hand-rolled histogram.
    - R4. Warmup ≥ 50k ticks (or ≥30s); adequacy = p50 stable < 5% across 2 runs (warms the 100k-account store).
    - R5. Feed paced: ticks 10/s, ingest 1M orders/s (via `latency-lab`/`spin_sleep`); achieved rates within ±5%, recorded.
    - R6. Runs the LIVE incremental path (Tue) + local read (Wed) with the determinism gate intact; `tracing` at `LevelFilter::OFF`, hot-path spans feature-gated out (`--no-default-features --features=bench`).
    - R7. Fixed, recorded seed + account/instrument/rate config in `result.json` for apples-to-apples history.
    - R8. CI nightly, 10% regression budget; `bench/baseline.json` committed; integer-comparison gate; failure blocks merge.
    - R9. Acceptance: per-mark-tick p99.99 < the §4A inter-tick budget (the number from Mon); achieved rates within 5%; 7 consecutive nightly greens before tagging the perf-pass.
  - **Design walk-through** D1..D8 — answer in the design doc; no peeking at `latency-lab` internals / the LMAX methodology until Phase 3:
    - D1. Enumerate measurement approaches (criterion `iter`, `iter_custom` + `latency-lab` HdrHistogram, hand-rolled `Instant` loop, `perf` PEBS) with a property table (omission-safe? rate-shaped? per-tick fan-out tail? reuses `latency-lab`?). Pick `iter_custom` + `latency-lab` and justify.
    - D2. Why measure the per-mark-tick fan-out tail, not per-account delta? (The §4A acceptance is per-mark-tick; per-account delta is the easier, wrong number that hides the fan-out queueing.)
    - D3. Coordinated-omission deep-dive at fan-out scale: the 5-step trace where an apply-loop stall delays one tick's fan-out, the next 3 ticks queue, and uncorrected `record` logs four ~normal latencies instead of one huge + three queued. How does `latency-lab`'s corrected recording fix it?
    - D4. NUMA placement: tick producer + ingest feed same-NUMA vs cross-NUMA with the shards — which is production-representative? (Reuse Mon's finding.)
    - D5. Warmup discipline: smallest warmup that catches the 100k-account first-touch; the stability metric.
    - D6. Regression budget: argue 10% with a concrete example (too tight → fight noise; too loose → a real fan-out regression hides until the M30 gate).
    - D7. **Pre-mortem (load-bearing):** the 5-step trace where (a) you cite "per-mark-tick p99.99 = 4ms" in Blog #5, (b) production shows 60ms under a real apply-loop stall, (c) you discover the bench used raw `record` (no omission correction) AND measured per-account delta not the fan-out tail, (d) the real result was always "p99.99 = 40ms under realistic queueing," (e) R2 + R3 + R5 would have surfaced it. Convince yourself R2+R3+R5 prevent this — and that this is the exact failure that sinks a founding-eng conversation.
    - D8. **Anti-shortcut audit:** (a) `b.iter(|| engine.on_mark_tick(..))` "simplest" — measures max throughput of one tick, not 10/s, omits queueing; (b) raw `record(dt)` "it records latency" — coordinated omission makes the tail a fantasy; (c) measuring per-account delta — the easier number that isn't acceptance #8; (d) re-implementing the histogram/rdtsc — `latency-lab` is the primitive (no primitive twice).
  - 🧮 **Paper drill** — write in `notes/111_mark_tick_bench_drill.md`:
    - D-CAP on the per-mark-tick fan-out at 100k accounts / 10 ticks/s: derive the inter-tick interval (100ms = the `expected_interval`), the per-shard burst (≈40k holders / 16 shards × per-account ns), and the sub-tick **p99.99** budget under the §4A target; report the *tail*, not the mean, and verdict fits / busts.
    - D-COST on coordinated omission at fan-out scale: hand-trace the 5-step interleaving where an apply-loop stall delays one tick's fan-out and the next 3 ticks queue; tabulate uncorrected `record` (four ~normal latencies) vs `record_corrected_with_max_interval` (one huge + three synthesized) and compute the cliff ratio (uncorrected ~5ms vs corrected-under-stall tens of ms = 10–100×) — the number you'd quote at the M30 gate.
  - **Output**: `notes/111_mark_tick_bench_design.md` with R1–R9, D1–D8, the `BENCH.md` draft (host config + repro steps), the omission-at-fan-out trace, and the regression-budget rationale; plus `notes/111_mark_tick_bench_drill.md` with the worked paper drills.

  #### Phase 2 — Write the failing tests (~20 min)

  Tier-annotated; required trap shapes labeled 🪤.

  - [ ] **(NEWBIE)** `bench_emits_all_quantiles` — invoke bench; assert `result.json` contains p50/p99/p99.9/**p99.99** in ns plus `accounts`, `achieved_tick_rate`, `achieved_ingest_rate`. Exercises R2.
  - [ ] **(EXPERT — anti-shortcut trap, bar (c) 🪤)** `bench_uses_latency_lab_corrected` — instrument to capture recorder calls; assert `record_corrected_with_max_interval` (from `latency-lab`) was used, NOT raw `record`, and NOT a hand-rolled histogram. The naive bench emits a p99.99, but the number is a fantasy. Exercises R3.
    ```rust
    #[test]
    fn bench_uses_latency_lab_corrected() {
        let calls = capture_recorder_calls(run_bench_once);
        assert!(calls.iter().any(|c| c == "latency_lab::record_corrected_with_max_interval"),
            "bench must use latency-lab corrected recording, got: {calls:?}");
        assert!(!calls.iter().any(|c| c == "hdrhistogram::Histogram::record"),
            "raw record() = coordinated-omission fantasy");
    }
    ```
  - [ ] **(EXPERT — cross-primitive trap, bar (c) 🪤)** `bench_measures_per_tick_fanout_not_per_account` — assert the recorded latency unit is per-mark-tick fan-out completion (tick arrival → last affected account), not per-account delta. Composes the bench with the live fan-out — the easier per-account number passes its own check but is the wrong acceptance. Exercises R2.
  - [ ] **(SENIOR — anti-shortcut trap 🪤)** `bench_achieved_rates_within_5pct` — assert achieved tick rate ∈ [9.5, 10.5]/s AND ingest ∈ [0.95M, 1.05M]/s. The `b.iter` shortcut measures "as fast as possible" and fails this. Exercises R5.
  - [ ] **(SENIOR)** `bench_runs_at_100k_accounts_2_instruments` — assert the config under test is 100k accounts / 2 instruments (acceptance #8 scale), not a downscaled microbench. Exercises R1, R6.
  - [ ] **(SENIOR — bar (c))** `bench_p99_99_under_budget` — assert per-mark-tick p99.99 < the §4A inter-tick budget (read the number from `notes/111_capacity_budget.md`) OR a named-focus failure. Exercises R9.
  - [ ] **(SENIOR)** `bench_determinism_gate_intact` — assert no `f64` was introduced for the bench (clippy `disallowed-types`); the live path stays cross-arch deterministic. Exercises R6.
  - [ ] **(NEWBIE)** `warmup_at_least_50k_ticks` — assert ≥ 50k warmup ticks before recording. Exercises R4.
  - [ ] **(EXPERT — negative-control "test-the-test" 🪤)** `bench_actually_fanned_out` — assert the bench recorded ≥ (tick_rate × duration) ticks AND each tick touched a plausible holder count (>0, ≤ 100k); a crashed/empty fan-out that records nothing must not pass the percentile tests vacuously. Exercises R2.

  #### Phase 3 — Make the tests pass, one failure at a time (~15 min)

  - **(NEWBIE) When `warmup_at_least_50k_ticks` reports warmup = 0:**
    - Symptom: recording started cold; p99.99 dominated by first-touch faults on the 100k-account store.
    - Why: no warmup gate before recording.
    - Fix: run ≥ 50k ticks (or ≥30s) of sustained fan-out before the recorder starts; verify p50 stability < 5% across 2 runs.
    - Muscle: a cold fan-out measures page faults, not your code; warm the 100k-account store first. Reapplies at: W113 PR-review bench, W120 production capture.

  - **(EXPERT — measurement) When `bench_uses_latency_lab_corrected` finds raw `record`:**
    - Symptom: the corrected and uncorrected p99.99 are identical; the meta test fires.
    - Why: the bench called `hist.record(dt)` (or a hand-rolled histogram) instead of `latency-lab`'s corrected recorder; under a fan-out stall the queued ticks are never synthesized.
    - Fix: route every sample through `latency_lab::record_corrected_with_max_interval(dt, expected = inter_tick_ns)`; delete any hand-rolled histogram.
    - Cost avoided: a p99.99 that's a fantasy — the exact number you'd quote in Blog #5 and at the M30 gate, wrong by 10–100×. (bar (c): an omission-blind number disqualifies acceptance #8.)
    - Muscle: latency measurement is omission-corrected via the one `latency-lab` primitive, always. Reapplies at: W112 Blog #5 number, W113 PR-review benches, W120 regression detection.

  - **(EXPERT — measurement) When `bench_measures_per_tick_fanout_not_per_account` shows per-account delta:**
    - Symptom: the recorded unit is the per-account delta (~1µs), not the per-mark-tick fan-out tail (~ms); the p99.99 looks great and is the wrong number.
    - Why: it's easier to time a single delta than to time "tick arrival → last affected account updated"; the easier number passed its own check.
    - Fix: instrument the fan-out boundary — start the timer at tick arrival, stop when the last affected account's aggregate is updated; record that per tick. This is the acceptance-#8 quantity.
    - Cost avoided: shipping a number that answers an easier question than the M30 gate asks — credibility collapse in a founding-eng conversation.
    - Muscle: measure the quantity the acceptance criterion names, not the convenient proxy. Reapplies at: W112 blog, W113 PR review.

  - **(SENIOR — measurement) When `bench_achieved_rates_within_5pct` reports a tick rate of 4000/s:**
    - Symptom: the bench ran ticks as fast as possible (4000/s), not the §4A 10/s; the measured number is throughput-shaped, not latency-under-cadence.
    - Why: `b.iter` (or an unpaced loop) measures max throughput and omits inter-tick queueing.
    - Fix: pace ticks to 10/s and ingest to 1M orders/s via `latency-lab`/`spin_sleep`; record achieved rates; assert within ±5%.
    - Cost avoided: a latency number measured under the wrong load — production runs at the §4A cadence, not flat-out. Reapplies at: W113 PR-review protocol, W120 capture.

  - **(SENIOR — bar (c)) When `bench_p99_99_under_budget` fails (p99.99 over the §4A budget):**
    - Symptom: the corrected per-mark-tick p99.99 exceeds the inter-tick budget at 100k accounts.
    - Why: either the incremental path (Tue) or the local read (Wed) isn't fully wired, or a tail source (NUMA, alloc, an apply-loop stall) survives.
    - Fix: re-run Monday's rig to localize the tail (IPC / LLC / node-miss / which function); apply the Tue/Wed lever the profile points at; do not loosen the budget — the budget is the acceptance.
    - Cost avoided: tagging a perf-pass that doesn't meet acceptance #8 — the M30 bet-readiness gate fails on a number you can't move later without a redesign. (bar (c) violation.)
    - Muscle: the budget is fixed by the capacity target; you move the code to the budget, never the budget to the code. Reapplies at: W112 blog, W124 v1.0 final, M30 gate.

  - **5-year failure mode** (write in `notes/111_mark_tick_bench_followup.md` after green): when marks go per-trade the inter-tick interval collapses and the absolute p99.99 budget (and the omission `expected_interval`) must track the new cadence; when the rig moves to AMD/ARM the absolute number shifts. Migration trigger: mark-cadence change or rig change. Name a budget-relative gate ("p99.99 ≤ inter-tick-interval × headroom", with `expected_interval` derived from the live cadence, not hard-coded).
- [ ] Commit + log (~10 min)

## Friday — Zero-alloc audit + NUMA/cache pass on the per-mark-tick path; perf-pass tag

- [ ] [HFT] **Zero-alloc steady-state audit (bar (c))**: `cargo test --release dhat_perp_dex_steady_state -- --ignored` — run the live capstone (1M orders/s + 10 ticks/s) for a fixed window under `dhat`; assert ZERO heap allocations on the steady-state per-mark-tick path (delta apply + local read + fan-out). Any alloc is a named regression. (~40 min)
  - 🎯 **Hardened acceptance**: dhat report shows 0 steady-state blocks across the window (warmup/setup allocs excluded by the post-warmup snapshot); if non-zero, the report names the allocating call site. Output: `notes/111_zero_alloc_audit.md` with the snapshot diff and any culprits fixed. This is the static-mem invariant the whole venue rests on (TigerBeetle headline).
  - Note (v3): the most likely late-arriving alloc is in a *projection*/metrics path leaking onto the hot path — confirm metrics emission is off-hot-path (batched, on a separate core), not per-tick.
- [ ] [HFT] **NUMA + false-sharing pass (bar (c))**: re-run Monday's `profile_mark_tick.sh` and confirm `node-load-misses` < 1% and no false-sharing hotspot on the per-shard aggregate / `AppliedMarginRead` arrays (adjacent accounts must not share a 64B line across shard boundaries). (~40 min)
  - 🎯 **Hardened acceptance**: `perf-stat.txt` shows `node-load-misses` < 1% of loads AND no two hot aggregates straddle one cache line (verify size%64==0 + per-shard NUMA-local first-touch). Output: a line in `notes/111_zero_alloc_audit.md`. If a regression, fix the layout/placement before tagging.
  - *Mental model*: at 100k-account fan-out, NUMA-locality and cache-line ownership are budget lines as real as algorithmic complexity — Monday found them, Tue/Wed fixed the algorithm, Friday confirms the placement didn't regress.
- [ ] [HFT] **rdtsc tick-to-trade spot-check (via `latency-lab`)**: confirm the rdtsc tick-to-trade path (mark tick → resulting margin-driven action) is captured by `latency-lab` and lands in `result.json`; this is the mechanical-sympathy number that complements the corrected p99.99. (~20 min)
  - 🎯 **Hardened acceptance**: `result.json` carries an rdtsc tick-to-trade percentile alongside the per-mark-tick p99.99; the two corroborate (the corrected p99.99 ≥ the rdtsc median, as expected). Reuses `latency-lab`'s rdtsc harness — no new timing primitive.
- [ ] [HFT] **Perf-pass acceptance gate + tag** (~50 min): run all four days' gates as one ritual before tagging `perp-dex-core v0.95 [perf-pass]`:
  1. (~10 min) **Determinism survived**: 3-replica cross-replica + x86/ARM cross-arch aggregate-hash byte-identity still green (Tue) — a perf win that broke byte-identity is a regression, not a win.
  2. (~10 min) **Incremental-path gate (v3)**: `! grep -n 'full_recompute' crates/risk-engine/src/incremental_margin.rs` shows it only in the circuit-breaker path; steady-state ticks are deltas (acceptance #8 lever live).
  3. (~10 min) **Read-contract gate (§4A)**: `risk_read` has no consensus/network edge; liquidation cannot consume the analytics plane (Wed).
  4. (~10 min) **p99.99 gate**: the per-mark-tick p99.99 (corrected, via `latency-lab`) at 100k accounts is under the §4A budget (Thu); 7 consecutive nightly greens recorded (or the countdown started).
  5. (~5 min) **Zero-alloc + NUMA gate**: today's audits green.
  6. (~5 min) Tag `perp-dex-core v0.95 [perf-pass]`, push, update MEMORY.md + INDEX.md; record the headline number where W112/M30 will read it.
  - 🎯 **Hardened acceptance**: all six green; `notes/perp_dex_perf_pass_v095.md` records the per-mark-tick p99.99, the incremental-vs-full ratio, the read-side P99, the zero-alloc + NUMA confirmation, and the rdtsc tick-to-trade — the exact artifact W112 Blog #5 and the M30 gate consume.
- [ ] Commit + log (~10 min)

## Saturday — Bet-outreach ramp: turn the perf-pass into the M30 artifact + sustained ops

- [ ] [Bet] **Headline-number 1-pager** (`notes/portfolio_perp_dex_latency.md`) — the founding-eng/crypto-MM-desk artifact the M28 outreach ramp (§5) hands to a reader (~40 min):
  - 1-sentence elevator: "`perp-dex-core` sustains per-mark-tick p99.99 of <N> for cross-margin recompute fanned out across 100k accounts at 1M orders/s, via incremental margin + read-after-commit from local applied state on the leader shard — coordinated-omission-correct, on one VSR log."
  - 3 bullets on technique depth: (a) incremental margin (delta, not 1M recomputes/s) — the §4A capacity decision made up front; (b) read-after-commit from local applied state (no consensus round-trip) — the §4A control-plane contract; (c) the determinism gate survived every optimization (integer-only, byte-identical across 3 replicas + x86/ARM).
  - 1 bullet on judgment: the two-staleness-contract split (risk plane vs analytics plane) and why a liquidation served from the analytics plane fails.
  - 1 bullet on measurement rigor: coordinated-omission-correct, rate-shaped, NUMA-local, via the reused `latency-lab` primitive (no primitive twice).
  - 🎯 **Hardened acceptance**: the 1-pager cites THIS week's `result.json` numbers (no placeholders), names the three hardest pitfalls hit (aggregate drift, omission fantasy, NUMA tax), and is readable by a crypto-MM-desk reviewer in 3 minutes.
- [ ] [Bet] **Binance-alumni map + venue-shortlist touch** (§5/§7): add `perp-dex-core`'s latency number to the bet-outreach tracker; identify ≥2 venues/desks from the shortlist for whom "per-mark-tick p99.99 at 100k accounts" is the *exact* proof they'd want to see; note the one-line pitch per target. (~30 min)
  - 🎯 **Hardened acceptance**: `notes/bet_outreach_tracker.md` updated with the number + ≥2 targets + a one-line tailored pitch each; this is the M28 "bet-outreach ramp begins" deliverable (INDEX line), not a cold blast.
- [ ] **Sustained-ops check**: confirm the nightly p99.99 regression watchdog (Thu R8) is committed and the 7-green countdown is running; no other crate's nightly is red. (~20 min)
  - 🎯 **Hardened acceptance**: CI shows the `mark_tick_p99_99` nightly green and the baseline committed; record the green-count in progress.md.
- [ ] Commit + log (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
