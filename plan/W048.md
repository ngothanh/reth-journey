# Week 48 — Phase 3 close + M12 Decision Gate

> **Phase**: 3
> **Month**: M12
> **Track**: Multi (career/ops — decision gate)
> **Hours target**: 30h/wk
> **Mirror target**: Phase 3 exit assessment + M12 calibration (no upstream module — this is a decision gate, not a Build week)
> **Crate(s) touched**: (assessment week — no new code; reads metrics from the Layer-1 substrate + storage-trie v1.0 shipped W43-W44)
> **Bar**: (n/a) — decision-gate / assessment week; no crate ships, so no bar applies
> **Feeds into**: W49 (Phase 4 Mon revm push starts the morning after this gate); M18 velocity check (the 35-PR trajectory this week calibrates); M24 five-path decision (this gate is the first checkpoint on the trajectory that the M24 gate re-reads)
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): README §"M12 (W48 Wed) — Calibration only, no path change" (~line 890); README §"APPENDIX E week→crate table" W45-W48 row (~1248, "Reth maintenance + M12 decision"); README §"Inheritance Map" storage-trie inheritance ratio row (~811); README §"Bar policy (v2)" Layer-1 substrate list (~72-83)
> **v2 modifications**: none

**Why this week is light (no Build exercises):** this is a decision-gate / assessment week. Per the curriculum, M12 is **calibration only — no path change** (too early; the five-path decision is M24). The job this week is to (a) honestly score Phase 3 against its exit criteria, (b) update every North Star metric so the M18 and M24 gates inherit accurate numbers, and (c) make the three M12 gate decisions in `progress.md`. There is nothing to test, so there are no trap-test arcs — instead every item names its artifact + Output line, and every judgement item carries a hardened-acceptance bar so the gate cannot be passed by hand-waving. The single biggest risk this week is **comfortable self-scoring**: a number rounded up here corrupts the M18 and M24 gates that read it. Every hardened-acceptance bar below is written to make the dishonest number harder to write than the true one.

**Monday — Phase 3 reflection**

- [ ] **Assess**: `notes/48_phase3_exit_assessment.md` — score Phase 3 against its four exit criteria, criterion by criterion. (~30 min)
  - *Mental model*: Phase 3 (M7-M12) was the storage push — its headline deliverable was `storage-trie` v1.0 (W43-W44) plus the Layer-1 substrate crates seeded along the way (`time`, `backpressure`, `bufpool`, `wal`, `recovery`, `bloom`, `lsm-core`, `txn`). The exit assessment is the honest score that the M12 gate (Wed) and the M18 velocity check both read. A vague "felt good" here corrupts every downstream gate.
  - For EACH Phase 3 exit criterion (storage-trie v1.0 ✓/✗, 30 storage PRs ✓/✗, 1+ feature ✓/✗, the 8 primitive crates each at their target version ✓/✗) write: target → actual → gap → one-line remediation if short.
  - 🎯 Hardened acceptance: EVERY exit criterion has an explicit ✓ or ✗ with a number next to it (no "mostly", no blank); every ✗ has a one-line remediation that names a week or crate.
  - Output: `notes/48_phase3_exit_assessment.md` exists with all four criteria scored; `grep -c '✗' notes/48_phase3_exit_assessment.md` returns the miss count and `grep -A2 '✗' notes/48_phase3_exit_assessment.md` shows a remediation line naming a week/crate within 2 lines below each ✗.
- [ ] Commit notes. (~10 min)

**Tuesday — Metrics update**

- [ ] **Update**: `progress.md` (North Star table, M12 column) — populate every metric cell with actual shipped numbers. (~15 min)
  - *Mental model*: the North Star table is the single source of truth the M18/M24/M30/M36 gates read. Stale numbers here mean the M24 five-path decision is made on fiction. Targets at M12: 30 storage PRs, 1+ feature, storage-trie v1.0 ✓, and the 8 new primitive crates seeded (time, backpressure, bufpool, wal, recovery, bloom, lsm-core, txn).
  - 🎯 Hardened acceptance: the M12 column is fully populated (no blank cells); each primitive crate cell shows an actual shipped version (e.g. `bufpool v0.4`), not a target.
  - Output: `git diff progress.md` shows the M12 column changed this week; no cell in the M12 column is blank or shows a target-as-actual.
- [ ] **[Tempo] Update**: `progress.md` (M12 Tempo metrics block) — orientation depth, TIPs read, Tempo PRs merged. (~15 min)
  - *Mental model*: the Tempo track is a *parallel option* (Path D), not the primary path. Zero Tempo PRs at M12 is on-plan because the first Tempo PR is not due until W60-62. The trap here is reflex-padding a number to feel "on track" — that would corrupt the M24 Path-D criteria (which need ≥15 Tempo PRs, a number you cannot fake your way to). Targets at M12: orientation depth 3 (should be there), TIPs read 5 (count carefully against `notes/`), Tempo PRs merged 3 — but **flag if zero; 0 here is expected and acceptable, do NOT panic-claim or pad**.
  - 🎯 Hardened acceptance: Tempo PRs-merged cell shows the true count (0 is a valid, expected answer); if 0, the cell carries the note "on-plan, first Tempo PR W60-62"; orientation-depth and TIPs-read cells each cite the `notes/` files that substantiate them.
  - Output: the Tempo block in `progress.md` shows three populated cells; if PRs-merged is 0 the "on-plan, first Tempo PR W60-62" note is present; `git diff progress.md` shows the Tempo block changed.
- [ ] Commit notes. (~10 min)

**Wednesday — M12 Decision Gate**

- [ ] **M12 Gate** — `progress.md` (`## M12 Decision Gate` heading) — answer all three gate questions + the calibration line. (~30 min for the write-up; sub-questions estimated separately below)
  - *Mental model*: M12 is **calibration only — no path change** (too early; the five-path decision is M24). The output is not a pivot, it is three honest red/amber/green judgements + a one-line calibration each. This gate exists so a slow trajectory is caught at M12 (with 6 months of runway to M18) rather than discovered at M18 with no runway.
  - [ ] **Q1 — Reth velocity**: `progress.md` (M12 gate, Q1) — am I on track for 35 Reth PRs merged by M18 (need ~3 PR-bundles/month)? Compute actual PRs-merged-to-date ÷ expected-by-now. (~30 min)
    - 🎯 Hardened acceptance: a single ratio (actual ÷ trajectory) is written; if it is **below 60%**, the entry contains a concrete catch-up plan naming the weeks (W49-W54) where the deficit is recovered — not just "work harder".
    - Output: `progress.md` Q1 shows one numeric ratio; if <0.60 it is followed by a week-named catch-up plan (W49-W54).
  - [ ] **Q2 — Inheritance discipline**: `progress.md` (M12 gate, Q2) — did storage-trie v1.0 ship with ≥0.70 inheritance ratio (reused-from-substrate LOC ÷ total)? Read the actual ratio from the storage-trie ship notes (W44). (~30 min)
    - *Mental model*: this is the load-bearing question of the whole curriculum — "no primitive is built twice". A storage-trie that re-implemented bufpool/wal/recovery instead of inheriting them would have a low ratio, and that discipline failure *compounds through Phases 4-7* (exec-vm and consensus-engine inherit from the same substrate).
    - 🎯 Hardened acceptance: the actual ratio is a number (e.g. 0.74), sourced from the W44 ship notes; if **< 0.70**, the entry names which crate(s) were re-built instead of inherited and schedules the audit/refactor before Phase 4 deep work.
    - Output: `progress.md` Q2 shows a ratio sourced from the W44 ship notes; if <0.70 it names the re-built crate(s) and a scheduled audit week before W49.
  - [ ] **Q3 — Energy / sustainability**: `progress.md` (M12 gate, Q3) — are sleep, fitness, and day-job satisfaction each green over the past 4 weeks? (~30 min)
    - 🎯 Hardened acceptance: each of the three is marked R/A/G with a one-line basis; if **any one is red for ≥4 weeks**, a 2-week rest window is scheduled (named weeks) before Phase 4 deep work begins.
    - Output: `progress.md` Q3 shows three R/A/G marks each with a one-line basis; if any is red ≥4 weeks a named 2-week rest window appears.
  - [ ] **Calibration only**: `progress.md` (M12 gate, calibration line) — record the explicit "no path change at M12" decision + the within-Reth-plan calibration deltas (which weeks adjust, if any). (~30 min)
    - 🎯 Hardened acceptance: contains the literal line "M12: no path change — calibrate within Reth-primary plan" plus 0-3 concrete calibration deltas (each naming a week).
    - Output: `grep "no path change — calibrate within Reth-primary plan" progress.md` returns the literal line; 0-3 deltas follow, each naming a week.
- [ ] Commit notes. (~10 min)

**Thursday — Relationship stock-take**

- [ ] **Update**: `notes/maintainer_tracker.md` — record each maintainer's depth + last-interaction date; tag one mentor candidate. (~15 min)
  - *Mental model*: maintainer relationships are the gating input to the M24 Path-A decision (needs ≥3 Reth maintainers at Depth-3+) and to the W120 warm-network job outreach. Tracking depth now, mid-Phase-3, means the relationships are real and dated by the time they are load-bearing — not reconstructed from memory under pressure.
  - 🎯 Hardened acceptance: every maintainer has a Depth score (1-4) and the date of last substantive interaction; exactly one entry is tagged `[mentor-candidate]` with a one-line reason.
  - Output: `notes/maintainer_tracker.md` lists each maintainer with Depth + date; `grep -c '\[mentor-candidate\]' notes/maintainer_tracker.md` returns exactly 1.
- [ ] Commit notes. (~10 min)

**Friday — Final Phase 3 PRs**

- [ ] **Wrap**: `notes/48_phase3_pr_wrap.md` — rebase, address review comments, land or hand off every in-flight Phase 3 storage PR. (~30 min)
  - *Mental model*: Phase 4 (revm/exec-vm) starts Monday W49. Any storage PR left in a half-reviewed state will rot during the exec-vm push and the inheritance context will be cold. Closing the loop now keeps the storage-trie merge count (an M18 metric) honest.
  - 🎯 Hardened acceptance: every open Phase 3 PR has a terminal state (merged / closed / explicitly-parked-with-owner); no PR is left as "in review" without a named next action.
  - Output: `notes/48_phase3_pr_wrap.md` lists every open Phase 3 PR with a terminal state; no entry reads "in review" without a named next action; the resolved count reconciles against the M12 PR-merged number in `progress.md`.
- [ ] Commit + log. (~10 min)

**Saturday — Clean transition prep**

- [ ] **Plan**: `notes/48_phase4_transition.md` — define the storage-trie maintenance floor during Phase 4 + the Phase 4 entry conditions + pre-commit the W49 Monday revm task. (~30 min)
  - *Mental model*: during Phase 4 storage-trie drops to *maintenance mode* — the rule "code reading > code writing in Phase 3+" plus on-call interruptions mean heavy storage work would starve the revm reading hours. Defining the maintenance floor now prevents scope-creep back into storage during the exec-vm push.
  - 🎯 Hardened acceptance: states the weekly storage-trie maintenance ceiling (hours/wk) and the Monday-W49 first revm task is pre-committed by name (per the "pre-commit to Phase 4 Monday W49 task" risk mitigation).
  - Output: `notes/48_phase4_transition.md` names a maintenance ceiling in hours/wk and a single named W49-Monday revm task; the named task matches the W49.md Monday Build item.
- [ ] Commit notes. (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.

- [ ] End Phase 3 — full rest, no code. Phase 4 starts tomorrow (W49 revm push). (~30 min)
  - *Mental model*: the rest is load-bearing, not slack — Phase 4 is a 6-month exec-vm/consensus push and entering it depleted compounds into missed M18 velocity. The pre-committed W49 task means Monday starts with zero planning tax.
  - 🎯 Hardened acceptance: zero commits Sunday; the W49 Monday revm task is already pre-committed in `notes/48_phase4_transition.md` so Monday starts without a planning tax.
  - Output: `git log --since=Sunday --oneline` is empty for Sunday; `notes/48_phase4_transition.md` already contains the named W49-Monday task.
