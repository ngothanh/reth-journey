# Week 143 — Last full week of the plan (final maintenance + close prep)

> **Phase**: 7
> **Month**: M36
> **Track**: Career (destination landing)
> **Hours target**: 30h/wk (W137-W144)
> **Mirror target**: n/a (career/ops phase — no new crate; sustains the bar-(c) substrate built in Phases 4-5)
> **Crate(s) touched**: n/a (maintenance only on the workspace + Reth + Tempo)
> **Bar**: (n/a) — career/ops week. The work it *protects* is bar (c): the runtime-hours + chaos-drill discipline is exactly the "4000+ runtime hours by M36" + "deterministic-simulator/operational evidence" half of the bar-(c) policy (README Bar policy lines ~72-83).
> **Feeds into**: W144 (M36 retrospective + plan close) — every artifact this week produces (runtime-hours total, chaos-drill log, inheritance-audit prep, dep snapshot) is a metric W144 Tuesday/Saturday tallies; do the gathering HERE so close week is pure synthesis.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): README APPENDIX A "Weekly HFT maintenance template (W97-W144)" (Mon/Wed/Sat touchpoints) + "M36 end (W144): 4000+ runtime hours" + "[HFT] W144 Tue: M36 final audit"; https://tigerstyle.dev/ (Zero Dependencies — for the dep snapshot); prior `chaos_log.md` and `progress.md` HFT row (your own running record).
> **v2 modifications**: none

This is a career/ops week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding (SPEC §6 light-week rule). The current stub's six identical "Commit notes" lines are filler: a commit with nothing to commit proves nothing. This rework replaces them with the real, already-scheduled W97-W144 HFT maintenance cadence (README Appendix A) plus the W144-prep gathering, so every "Work" day produces a concrete operational artifact with an Output line. The week's job: close out 4000+ runtime hours cleanly and stage every plan-close metric so W144 only has to synthesize, not scramble.

**Monday — Weekend runtime-alert review (HFT Monday touchpoint)**

- [ ] Artifact: append to `ops/runtime_alert_log.md`. Review the weekend's runtime alerts across the live HFT stack (matching-engine, ledger-deterministic, messaging-aeron, marketdata-kernelbypass, mini-db on the rig): any P99 latency spikes, any replica-divergence events, any disk-fill warnings. For each alert, log {timestamp, crate, alert type, root cause or `[unresolved]`, action taken}. (~15 min)
  - 🎯 Hardened acceptance: `ops/runtime_alert_log.md` has this week's dated entry; every P99 spike and every divergence event is either explained with a root cause or explicitly tagged `[unresolved → W144 backlog]` — no silent gaps. If the weekend was clean, the entry says so explicitly (`no alerts; rig nominal`), because "no log" and "clean run" must not look identical.
  - *Mental model*: this is the bar-(c) "operational evidence" discipline in its final week — 4000 runtime hours mean nothing if a P99 cliff or a replica divergence was riding silently the whole time. Logging "clean" is as load-bearing as logging a failure, because next week's retrospective claims production-readiness and a single unexplained divergence event would falsify that claim in front of a hiring manager.
  - Output: `ops/runtime_alert_log.md` shows the dated W143 Monday entry; `tail -1 ops/runtime_alert_log.md` is from this week.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Monday alert-review commit.

**Tuesday — M36 inheritance-audit prep (stages the W144 Tue final audit)**

- [ ] Artifact: `notes/inheritance_audit_M36_prep.md`. README schedules "[HFT] W144 Tue: M36 final audit" — gather the numbers NOW so close week only captures. For each bar-(c) HFT crate, compute the inheritance ratio = inherited-primitive LOC / total crate LOC (the same metric audited at W74/W90/W117). Pull current LOC with `tokei` or `cloc` per crate; compare each ratio against its W117 (M30) snapshot. (~30 min)
  - 🎯 Hardened acceptance: the prep doc lists every HFT crate with {W117 ratio, W143 ratio, delta}; every crate still clears the ≥0.70 bar (the win line is ≥0.75); any crate that *dropped* below 0.70 since M30 is flagged with the PR/commit that diluted it. A ratio that went UP is also explained (did you delete bespoke code or inherit more?), so the number is understood, not just recorded.
  - *Mental model*: the inheritance ratio is the entire thesis of the 144-week plan — "every exercise is a reusable component mirroring an upstream module." This is the last measurement before plan close. If Phase 6-7's operational pressure quietly grew bespoke glue (the usual entropy), the ratio decay shows it; catching it here turns W144's audit from a scramble into a one-line confirmation, and gives the retrospective a defensible number to cite to interviewers.
  - Output: `notes/inheritance_audit_M36_prep.md` exists with one row per HFT crate {W117, W143, delta} and a flag column; `grep -c '|' notes/inheritance_audit_M36_prep.md` covers all 5 HFT crates + vector-db.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday audit-prep commit.

**Wednesday — Operations dashboard + runtime-hours capture (HFT Wednesday touchpoint)**

- [ ] Artifact: update the HFT row in `progress.md` and append to `ops/runtime_hours.md`. Check the operations dashboards; capture the cumulative runtime-hours total across the rig. M36 target is **4000+**. Record the running total + this week's increment + the per-crate uptime breakdown. (~30 min)
  - 🎯 Hardened acceptance: `ops/runtime_hours.md` shows a total that is on-track for ≥4000 by W144 (if short, the doc states the gap + the catch-up plan — e.g. extend rig run through the weekend); `progress.md` HFT row matches the same number (no two-source drift). The increment since last Wednesday is plausible (≤168 wall-clock hours/crate/week — a number larger than the week is a logging bug, not a win).
  - *Mental model*: 4000+ runtime hours is a hard, named bar-(c) close metric, not a vanity stat — it's the difference between "I wrote a matching engine" and "I ran a matching engine in production-like conditions for the equivalent of months without it falling over." The sanity check (increment ≤ wall-clock) is the negative control: an impossibly large jump means a counter reset or double-count, which would inflate the headline metric the retrospective stakes its credibility on.
  - Output: `ops/runtime_hours.md` cumulative total + W143 increment recorded; same number mirrored in `progress.md` HFT row.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday runtime-hours commit.

**Thursday — Workspace dep snapshot (stages the W144 Sat zero-dep close audit)**

- [ ] Artifact: `notes/zero_dep_snapshot_W143.md`. W144 Saturday runs the M36 zero-dep close audit (compare M30 vs M36 dep counts). Capture the M36-side snapshot now: run `cargo tree --depth 1` per workspace crate, count non-mirror deps per bar-(c) crate, and note any dep added since the W117 (M30) audit with its one-line rationale. (~30 min)
  - 🎯 Hardened acceptance: the snapshot lists per-crate `--depth 1` dep counts for every bar-(c) crate; each crate is ≤10 non-mirror deps (the TigerStyle close target); any dep added M30→M36 carries a rationale, and any "added for expedience without justification" is tagged `[M37 backlog]` rather than quietly kept. The workspace-wide non-mirror total is recorded against the committed close target (~150 across 30+ crates).
  - *Mental model*: the TigerStyle zero-dependency discipline was the leading indicator of bar-(c) quality at M30, and Phase 7's destination-landing pressure is exactly when "just add a dep" expedience creeps in. Snapshotting Thursday means W144's audit is a clean diff, not a from-scratch crawl; an unjustified new dep caught here is a 5-minute fix, the same one caught at v1.0-tag time is a re-audit. Cited reference: https://tigerstyle.dev/ (Zero Dependencies).
  - Output: `notes/zero_dep_snapshot_W143.md` exists with per-crate non-mirror dep counts + workspace total + any `[M37 backlog]` flags; `grep -c 'crate' notes/zero_dep_snapshot_W143.md` covers all bar-(c) crates.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday dep-snapshot commit.

**Friday — Tempo + Reth maintenance pass**

- [ ] Artifact: `notes/maintenance_W143.md`. Final routine maintenance pass on the still-active open-source surface: check Reth + Tempo for any open issues/PRs on your merged contributions (CI breakage, review follow-ups, dependency-bump requests on your code), and triage. Record each as {repo, issue/PR, status, action or `[no action — reason]`}. (~30 min)
  - 🎯 Hardened acceptance: `notes/maintenance_W143.md` lists every open item touching your merged Reth/Tempo contributions with a triage decision; nothing is left ambient. If everything is green, the doc states `no open follow-ups on merged contributions — surface stable`, so a quiet week is recorded as a fact, not an absence.
  - *Mental model*: your merged PRs are now living credibility, cited in the W120-W121 outreach and the W144 metrics tally (target: 85+ Reth, 38 Tempo). A merged PR that later red-CIs or gets a stale-review nag silently erodes exactly the credibility the whole career thesis rests on — the last full week is the moment to confirm the public record is clean before the retrospective points recruiters at it.
  - Output: `notes/maintenance_W143.md` exists with one triaged line per open item (or the explicit "surface stable" line); committed.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Friday maintenance commit.

**Saturday — Final chaos drill + last operations rig audit before plan close**

- [ ] Artifact: append to `chaos_log.md`. README schedules a rotated weekly chaos drill (process kill, network partition, clock skew, disk fill, slow subscriber) through W144 — run the final W143 drill and record results. Pick the drill that most stresses the weakest crate from this week's alert log. Capture {drill type, target crate, expected behavior, observed behavior, recovery time, pass/fail}. (~60 min)
  - 🎯 Hardened acceptance: `chaos_log.md` has the dated W143 drill entry with a measured recovery time and an explicit pass/fail; the *expected* behavior was written down BEFORE running (so the drill is a real test, not a post-hoc rationalization). A fail here is a finding, not an embarrassment — it goes to `[W144 backlog]` with the regression noted; a pass with recovery time within the documented SLO is the close-out signal.
  - *Mental model*: chaos drills are the operational analogue of the deterministic-simulator/VOPR half of the bar-(c) policy — they prove the back-pressure-as-correctness and recovery invariants hold under injected failure, not just on the happy path. Writing the expected behavior first is the negative control: if you only describe what happened after it happened, you can rationalize any outcome as "expected," and the drill measures nothing. The final drill is the last chance to find a recovery-path bug before the plan claims production-grade resilience.
  - Output: `chaos_log.md` shows the dated W143 drill with expected-before-observed + recovery time + pass/fail.
- [ ] Artifact: `notes/rig_audit_W143_close.md`. Last full operations-rig audit before plan close: confirm runtime-hours total is on track for the 4000 M36 target (cross-check Wednesday's `ops/runtime_hours.md`), confirm every HFT crate is still running on the rig, and capture the final stats snapshot (uptime, P99 per crate, divergence count, disk headroom). (~30 min)
  - 🎯 Hardened acceptance: `notes/rig_audit_W143_close.md` states the 4000-hour target status (on-track / gap + plan); lists each HFT crate as running/stopped with reason; the P99 + divergence-count snapshot is consistent with this week's `ops/runtime_alert_log.md` (no contradiction between the alert log and the audit). Any crate stopped/degraded is flagged for W144.
  - *Mental model*: this is the single most-important number of the close — the runtime-hours total is a headline metric in W144's tally and the named M36 bar-(c) gate. The cross-check against Monday's alert log and Wednesday's hours file is deliberate: three independent records of rig health that must agree, so the retrospective's "4000 hours, production-grade" claim is corroborated, not asserted.
  - Output: `notes/rig_audit_W143_close.md` exists with 4000-hour status + per-crate running state + final stats snapshot; cross-checks against `ops/runtime_hours.md`.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday rig-audit commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
</content>
</invoke>
