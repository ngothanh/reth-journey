# Week 143 — Last full week of the plan (final venue-rig maintenance + bet-close metric staging)

> **Phase**: 7 (bet-path — founding/core-eng token-equity bet on an on-chain derivatives venue; crypto-MM cash seat as the income-floor bridge)
> **Month**: M36
> **Track**: Bet-path close (destination = Bet #1 placed OR Bridge₀ engaged) — sustains the bar-(c) substrate built across Phases 5–6 (the `perp-dex-core` venue rig + the Reth spine + Tempo RWA-collateral option)
> **Hours target**: 30h/wk (W137–W144 window)
> **Mirror target**: n/a (career/ops week — no new crate). The work it *protects* mirrors TigerBeetle's operational discipline: the deterministic-simulator (cluster-VOPR) + runtime-hours evidence that makes a replicated venue claim *production-grade* rather than *demo-grade*.
> **Crate(s) touched**: n/a (maintenance only on the live venue rig — `perp-dex-core` 3-node VSR cluster + its assembled crates `matching-engine`/`oracle-mark`/`risk-engine`/`liquidation-engine`/`ledger-deterministic`/`consensus-vsr`/`log-distributed`/`runtime-thread-per-core` — plus the Reth spine + Tempo).
> **Bar**: (n/a) — career/ops week. The work it *protects* is bar (c): the runtime-hours + cluster-VOPR + insurance-fund-tripwire discipline IS the "≥4000 runtime hours by M36" + "deterministic-simulator/operational evidence" half of bar (a)/(b) (README.md §1 bars (a)+(b); §4A acceptance #2/#3/#7).
> **Feeds into**: W144 (M36 retrospective + plan formal close) — every artifact this week produces (cumulative runtime-hours total, cluster-VOPR drill log, insurance-fund tripwire status, inheritance-audit prep, zero-dep snapshot, bet-pipeline tally) is a metric W144 Tuesday's close-metrics table and Saturday's zero-dep audit consume. Do the gathering HERE so close week is pure synthesis, not a scramble. Also feeds the live Bet #1 / Bridge₀ decision still open from W131 — a clean rig week is the artifact a founding-eng or crypto-MM conversation is pointed at.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): README.md §1 (the four end-state bars), §4A (capstone v1.0 lock — the invariants the rig must still be holding: cluster-VOPR green, insurance-fund tripwire never hit, two staleness contracts, SettlementId finality), §5 (M30 bet-readiness gate metrics this week refreshes for the M36 close), §6 (ecosystem-selection checklist — the seven GO/NO-GO gates the bet pipeline is filtered against), §7 (shots-on-goal + Dubai/SG flip-trigger), §9 (risk register — the operational risks the drills probe); prior `chaos_log.md`, `notes/cluster_vopr_log.md`, `ops/runtime_hours.md`, `progress.md` venue row (your own running record); TigerStyle "Zero Dependencies" (https://tigerstyle.dev/) for Thursday's dep snapshot.
> **v3 modifications**: the v2 stub was a generic "HFT maintenance" week pointed at the v2 crate set (`mini-db` on the rig, "5 HFT crates", a v2 7-system tally) and a job-landing close. This rework re-aims every operational artifact at the **v3 capstone `perp-dex-core`** (the demoted `mini-db` is OFF the rig — its DB substrate lives *inside* the venue store) and the **bet-path close**: the cluster-VOPR drill replaces the generic single-crate chaos drill; the insurance-fund **tripwire-never-hit** invariant (§4A acceptance #7) and the **two-staleness-contract** boundary (§4A acceptance #5) are now first-class rig-audit checks; the metrics staged are the **bet-readiness** metrics (runtime hours, cluster-VOPR coverage, ecosystem-shortlist health, warm-conversation pipeline) not job-search metrics; the inheritance audit covers the **v3 domain crates** (`oracle-mark`/`risk-engine`/`liquidation-engine`) not the v2 HFT five.

This is a career/ops week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding (light-week rule). The v2 stub's six near-identical "Commit notes" lines were filler: a commit with nothing to commit proves nothing. This rework replaces them with the real, already-scheduled W97–W144 venue-rig maintenance cadence plus the W144-close gathering, so every "Work" day produces a concrete operational artifact with an Output line. **The week's job: close out ≥4000 runtime hours on the `perp-dex-core` rig cleanly, confirm the v1.0 invariants the whole bet narrative rests on are *still holding* under Phase-7 pressure, and stage every M36 close metric so W144 only synthesizes.** A single silently-broken invariant here (a cluster-VOPR divergence, an insurance-fund tripwire that quietly fired, a risk decision served from the analytics staleness contract) would falsify the bet-readiness claim in front of the exact founding-eng / crypto-MM conversations this week's artifacts are pointed at.

---

## Monday — Weekend venue-rig alert review (rig Monday touchpoint)

- [ ] **Artifact**: append to `ops/runtime_alert_log.md`. Review the weekend's alerts across the live `perp-dex-core` rig (the 3-node VSR cluster + the assembled venue crates). For each alert log `{timestamp, node/shard, crate, alert type, root cause or [unresolved], action taken}`. Cover specifically: (a) VSR commit-latency P99 spikes; (b) any **replica-divergence** event (cluster-VOPR StateChecker should make this impossible — a real one is a five-alarm finding); (c) any **insurance-fund tripwire approach** (fund coverage ratio dipping toward the next-shortfall point); (d) `risk-engine` per-mark-tick recompute budget breaches at the 100k-account scale; (e) projection-staleness alarms on the analytics plane (op-token lag > target). (~20 min)
  - 🎯 **Hardened acceptance**: `ops/runtime_alert_log.md` has this week's dated entry; every VSR P99 spike, every divergence event, and every insurance-fund-coverage dip is either explained with a root cause or explicitly tagged `[unresolved → W144 backlog]` — no silent gaps. If the weekend was clean, the entry says so explicitly (`no alerts; rig nominal; cluster-VOPR green; fund coverage ≥ declared-rate floor`), because "no log" and "clean run" must not look identical at close time.
  - *Mental model*: this is the bar-(a)/(b) "operational evidence" discipline in its final week — ≥4000 runtime hours mean nothing if a VSR commit cliff, a replica divergence, or a slow insurance-fund drain was riding silently the whole time. Logging "clean" is as load-bearing as logging a failure, because next week's retrospective stakes the *production-grade* claim (and the live Bet #1 / crypto-MM conversation) on it, and a single unexplained divergence event would falsify §4A acceptance #2/#7 in front of a founding-eng interviewer reading the rig.
  - **Output**: `ops/runtime_alert_log.md` shows the dated W143 Monday entry; `tail -1 ops/runtime_alert_log.md` is from this week.
- [ ] **Commit + log** (~10 min).
  - **Output**: `git log -1 --oneline` shows the Monday alert-review commit.

## Tuesday — M36 inheritance-audit prep (stages the W144 close-metrics table)

- [ ] **Artifact**: `notes/inheritance_audit_M36_prep.md`. W144's close-metrics table tallies the inheritance ratio — gather the numbers NOW so close week only captures. For each bar-(c) venue crate, compute the inheritance ratio = inherited-primitive LOC / total crate LOC (the same metric audited at W74/W90/W117). Pull current LOC with `tokei`/`cloc` per crate; compare each ratio against its W117 (M30 bet-readiness gate) snapshot. **Cover the v3 crate set**: `matching-engine`, `oracle-mark`, `risk-engine`, `liquidation-engine`, `ledger-deterministic`, `consensus-vsr`, `log-distributed`, `runtime-thread-per-core`, `messaging-aeron`, `marketdata-kernelbypass` — and the capstone `perp-dex-core` (whose ratio should be the highest of all, since it is assembly-over-primitives by construction). (~30 min)
  - 🎯 **Hardened acceptance**: the prep doc lists every venue crate with `{W117 ratio, W143 ratio, delta}`; every crate still clears the ≥0.70 bar (the win line is ≥0.75); `perp-dex-core` is highest (it is "no primitive twice" made literal — rows 1–15 of the §3 matrix consumed once). Any crate that *dropped* below 0.70 since M30 is flagged with the PR/commit that diluted it; a ratio that went UP is also explained (deleted bespoke glue? inherited more?), so the number is understood, not just recorded. The **convergence-cell crate `risk-engine`** gets a one-line note on whether its NET-NEW cross-margin/netting logic is still the irreducible core (it *should* carry more bespoke LOC than the others — it is the principal-defining artifact; a suspiciously high ratio there means the hard part leaked into a primitive).
  - *Mental model*: the inheritance ratio is the entire thesis of the 144-week plan — "every output crate is built once and consumed by the capstone; no primitive twice." This is the last measurement before plan close. Phase 7's bet-outreach pressure is exactly when operational glue grows quietly; catching the decay here turns W144's tally from a scramble into a one-line confirmation, and gives the retrospective a defensible number to cite to a founding-eng interviewer ("the cross-margin engine is the NET-NEW; everything under it is inherited and proven"). `risk-engine` is the one crate where a HIGH ratio is a warning, not a win — its whole point (§3 convergence cell) is that the hard part has no upstream to inherit.
  - **Output**: `notes/inheritance_audit_M36_prep.md` exists with one row per venue crate `{W117, W143, delta}` + a flag column + the `risk-engine` convergence note; covers all v3 domain + HFT crates and `perp-dex-core`.
- [ ] **Commit + log** (~10 min).
  - **Output**: `git log -1 --oneline` shows the Tuesday audit-prep commit.

## Wednesday — Runtime-hours capture + insurance-fund & staleness-contract spot-audit (rig Wednesday touchpoint)

- [ ] **Artifact**: update the venue row in `progress.md` and append to `ops/runtime_hours.md`. Capture the cumulative runtime-hours total across the `perp-dex-core` rig. **M36 target is ≥4000** (bar (a)). Record the running total + this week's increment + the per-node/per-shard uptime breakdown. (~25 min)
  - 🎯 **Hardened acceptance**: `ops/runtime_hours.md` shows a total on-track for ≥4000 by W144 (if short, the doc states the gap + the catch-up plan — e.g. extend the rig run through the weekend); `progress.md` venue row matches the same number (no two-source drift). The increment since last Wednesday is plausible (≤168 wall-clock hours/node/week — a number larger than the week is a logging bug, not a win — the negative control).
  - *Mental model*: ≥4000 runtime hours is a hard, named bar-(a) close metric — it's the difference between "I architected a perp-DEX-core" and "I *ran* a 3-node replicated venue in production-like conditions for the equivalent of months without it falling over." The ≤-wall-clock sanity check is the negative control: an impossibly large jump means a counter reset or double-count, which would inflate the headline metric the bet narrative stakes its credibility on.
  - **Output**: `ops/runtime_hours.md` cumulative total + W143 increment recorded; same number mirrored in `progress.md` venue row.
- [ ] **Artifact**: append to `notes/invariant_spot_audit_W143.md` — a fast spot-check of the two v1.0 invariants most likely to rot silently under sustained load, BEFORE Saturday's full cluster-VOPR drill. (a) **Insurance-fund tripwire (§4A acceptance #7)**: confirm the fund coverage ratio at the declared accrual rate (default 10% taker) has never crossed the tripwire across the week's live load; record the min coverage observed + the headroom to the tripwire. (b) **Two staleness contracts (§4A acceptance #5)**: confirm no control/risk-plane read (mark-into-liquidation, position-into-margin) was served from the analytics/bounded-staleness path during the week — a risk decision on the analytics contract is a *correctness bug*, not a perf nit. (~30 min)
  - 🎯 **Hardened acceptance**: the doc states (a) min insurance-fund coverage ratio this week + headroom to tripwire, with `tripwire NOT hit` explicit (or, if approached, the schedule that approached it flagged as the v1.5 ADL-trigger seam per §4A acceptance #7); (b) a yes/no that every control/risk read was read-after-commit from local applied state on the leader shard, and zero risk decisions touched the analytics contract. Either invariant breached = `[W144 backlog, blocks production-grade claim]`.
  - *Mental model*: these two are the invariants that make the venue *correct*, not merely fast — and they are the ones a sustained Phase-7 run is most likely to erode unnoticed (a slow fund drain looks fine until the one liquidation that exceeds it; a refactor quietly routes a risk read through the cheaper analytics path). §4A acceptance #5 says a risk decision from the analytics contract *fails the gate*; #7 says the tripwire must *never* be hit. Spot-checking Wednesday means Saturday's cluster-VOPR confirms a known-clean state instead of discovering a week-old correctness regression.
  - **Output**: `notes/invariant_spot_audit_W143.md` has the dated entry with fund-coverage min + tripwire status + staleness-contract yes/no.
- [ ] **Commit + log** (~10 min).
  - **Output**: `git log -1 --oneline` shows the Wednesday runtime-hours + invariant-spot-audit commit.

## Thursday — Workspace dep snapshot (stages the W144 Sat M36 zero-dep close audit)

- [ ] **Artifact**: `notes/zero_dep_snapshot_W143.md`. W144 Saturday re-runs the W117 `scripts/zero_dep_audit.sh` for the M36 zero-dep close audit (M30 vs M36 dep diff, the plan's last technical act). Capture the M36-side snapshot now: run `cargo tree --depth 1` per workspace crate, count direct non-mirror deps per bar-(c) crate, and note any dep added since the W117 (M30) audit with its one-line rationale. (~30 min)
  - 🎯 **Hardened acceptance**: the snapshot lists per-crate `--depth 1` direct-non-mirror dep counts for every bar-(c) crate (the v3 venue crate set + `perp-dex-core`); each bar-(c) crate is ≤10 non-mirror deps (the TigerStyle close target the W117 gate held); the workspace-wide non-mirror total is recorded against the committed close target (≤150 across 30+ crates). Any dep added M30→M36 carries a rationale; any "added for expedience without justification" is tagged `[M37 backlog]` rather than quietly kept. Special check: confirm `mini-db` carries **no rig/runtime dep** (it is the deferred, default-DO-NOT-BUILD KV facade — its substrate lives inside the venue store, so it should add zero deps to the live workspace; a dep there means it got built without the (a)∧(b)∧(c) trigger).
  - *Mental model*: the TigerStyle zero-dependency discipline was the leading indicator of bar-(c) quality at the M30 gate, and Phase 7's "just add a dep, I'm busy with the bet conversations" period is exactly when bloat creeps in. Snapshotting Thursday means W144's audit is a clean mechanical diff, not a from-scratch crawl; an unjustified new dep caught here is a 5-minute fix, the same one caught at the v1.0-tag-protecting close is a re-audit. Cited reference: https://tigerstyle.dev/ (Zero Dependencies).
  - **Output**: `notes/zero_dep_snapshot_W143.md` exists with per-crate non-mirror dep counts + workspace total + any `[M37 backlog]` flags + the `mini-db`-stays-zero check; covers all bar-(c) crates.
- [ ] **Commit + log** (~10 min).
  - **Output**: `git log -1 --oneline` shows the Thursday dep-snapshot commit.

## Friday — Reth/Tempo maintenance pass + bet-pipeline & ecosystem-shortlist health check

- [ ] **Artifact**: `notes/maintenance_W143.md`. Final routine maintenance pass on the still-active open-source surface: check Reth + Tempo for any open issues/PRs on your merged contributions (CI breakage, review follow-ups, dependency-bump requests on your code), and triage. Record each as `{repo, issue/PR, status, action or [no action — reason]}`. (~25 min)
  - 🎯 **Hardened acceptance**: `notes/maintenance_W143.md` lists every open item touching your merged Reth/Tempo contributions with a triage decision; nothing is left ambient. If everything is green, the doc states `no open follow-ups on merged contributions — surface stable`, so a quiet week is recorded as a fact, not an absence.
  - *Mental model*: your merged PRs (Reth = the crypto-protocol spine / the *secondary* crypto-infra IC bridge; Tempo = the RWA-collateral option leg) are living credibility cited in the bet outreach and the W144 metrics tally. A merged PR that later red-CIs or gets a stale-review nag silently erodes exactly the credibility the bridge fallback and the RWA-perps narrative rest on — the last full week is the moment to confirm the public record is clean before the retrospective points a recruiter or founder at it.
  - **Output**: `notes/maintenance_W143.md` exists with one triaged line per open item (or the explicit "surface stable" line); committed.
- [ ] **Artifact**: append to `notes/bet_pipeline_W143.md` — refresh the bet-readiness pipeline metrics for the M36 close (these stage the W144 close-metrics table and the still-open Bet #1 / Bridge₀ decision). Capture: (a) ecosystem shortlist health — re-run the §6 seven-gate checklist against the ≥5 shortlisted venues from W116/W117, flagging any that have moved (TGE'd, lost tier-1 backing, cooled); (b) warm-conversation pipeline — count active founding/core-eng conversations + crypto-MM (Wintermute/Jump-style) bridge conversations, with stage (intro / deep-dive / work-trial / offer); (c) §7 Dubai/SG flip-trigger status — has a warm-conversation cluster formed in one hub, or a presence-contingent offer premium appeared? (default: remote, no move). (~30 min)
  - 🎯 **Hardened acceptance**: `notes/bet_pipeline_W143.md` shows the §6 checklist re-applied to each shortlisted venue with a current GO/NO-GO per the seven gates (a venue failing any of the seven is a *bridge job, not a bet* — stated explicitly); the warm-conversation count split founders-vs-crypto-MM with stages; the flip-trigger marked fired/not-fired with the evidence. Any venue that dropped off the shortlist since M30 carries the reason (closing the loop to W117's shortlist).
  - *Mental model*: M36's end-state is "Bet #1 placed OR Bridge₀ engaged" (§5 M36 gate), and the decision is *still live* this late only if §6 hasn't cleanly passed yet — so the pipeline health IS a close metric, not a footnote. The §6 checklist is the discipline that prevents calling a late-equity bridge job a "bet"; re-running it the last full week is the final guard against a NO-GO opportunity being rationalized into a GO under close-week pressure. The flip-trigger check keeps the Dubai/SG decision *cheap and deferred* (§7) — it converts to a move only on the trigger, never pre-committed.
  - **Output**: `notes/bet_pipeline_W143.md` updated with §6 per-venue GO/NO-GO + warm-conversation count by stage + flip-trigger status.
- [ ] **Commit + log** (~10 min).
  - **Output**: `git log -1 --oneline` shows the Friday maintenance + bet-pipeline commit.

## Saturday — Final cluster-VOPR drill + last venue-rig audit before plan close

- [ ] **Artifact**: append to `notes/cluster_vopr_log.md` (and `chaos_log.md`). Run the **final cluster-VOPR drill** on the `perp-dex-core` rig — the deterministic-simulator discipline that is the operational heart of bar (b) (§4A acceptance #2/#3/#7; §9 "Cluster-VOPR can't be honest without all I/O behind interfaces"). The drill injects crash / restart / network-partition / clock-skew / slow-subscriber schedules and the StateChecker asserts: (i) **linearizability** across the 3-node VSR cluster; (ii) **replica convergence** (byte-identical applied state after the same committed op prefix — the order/event log IS the VSR log, no second hot-path log); (iii) **insurance-fund tripwire NEVER hit** across all explored schedules at the declared accrual rate; (iv) **SettlementId finality** holds (`Final = op_number ≤ commit point`, no side mapping table, resubmit = no-op). Pick the schedule family that most stresses the weakest area from this week's alert log. Capture `{schedule family, seed range, expected invariant, observed, schedules explored, recovery time, pass/fail}`. (~75 min)
  - 🎯 **Hardened acceptance**: `notes/cluster_vopr_log.md` has the dated W143 drill entry with the four StateChecker invariants each marked pass/fail, the number of schedules explored, and a measured recovery time; the *expected* invariants were written down BEFORE running (so it is a real test, not a post-hoc rationalization — the negative control). A tripwire hit is a *finding*, not an embarrassment: per §4A acceptance #7 it precisely defines the v1.5 ADL/socialized-loss trigger seam, so it goes to `notes/year4_outline.md` / `[W144 backlog]` with the schedule recorded — but it means the v1.0 "tripwire never hit" claim does NOT hold and the retrospective must say so. An all-green drill within the documented SLO is the close-out signal that bar (b) holds.
  - *Mental model*: cluster-VOPR is the operational analogue of TigerBeetle's VOPR extended from ledger-only to the whole venue (§3 row 21, §4A acceptance #7) — it proves linearizability, replica convergence, the insurance-fund waterfall, and SettlementId finality hold under *injected* failure, not just on the happy path. Writing the expected invariants first is the negative control: describe what happened only after it happened and you can rationalize any outcome as "expected," and the drill measures nothing. This final drill is the last chance to find a divergence or a tripwire-cross before the bet narrative claims production-grade resilience to a founding-eng interviewer — and the one schedule that hits the tripwire is not a failure of the plan, it is the documented seam where v1.5 begins.
  - **Output**: `notes/cluster_vopr_log.md` shows the dated W143 drill with expected-before-observed, the four invariants pass/fail, schedules explored, recovery time; cross-linked in `chaos_log.md`.
- [ ] **Artifact**: `notes/rig_audit_W143_close.md`. Last full venue-rig audit before plan close: confirm the runtime-hours total is on track for the ≥4000 M36 target (cross-check Wednesday's `ops/runtime_hours.md`), confirm all 3 VSR nodes + every assembled venue crate are still running, and capture the final stats snapshot (per-node uptime, VSR commit P99/P99.99, `risk-engine` per-mark-tick recompute P99.99 at the 100k-account scale, replica-divergence count = 0, insurance-fund coverage ratio, analytics-plane op-token staleness, disk headroom). (~30 min)
  - 🎯 **Hardened acceptance**: `notes/rig_audit_W143_close.md` states the ≥4000-hour target status (on-track / gap + plan); lists each VSR node + venue crate as running/stopped with reason; the P99/divergence/fund snapshot is consistent with this week's `ops/runtime_alert_log.md`, `notes/invariant_spot_audit_W143.md`, and Saturday's cluster-VOPR drill (no contradiction across the four records). Replica-divergence count must be 0 (any nonzero = falsifies §4A acceptance #2, escalate to `[W144 backlog]`). Any node/crate stopped or degraded is flagged for W144.
  - *Mental model*: this is the single most-important corroboration of the close — the runtime-hours total is a headline bar-(a) metric in W144's tally and the production-grade evidence the bet narrative rests on. Cross-checking against Monday's alert log, Wednesday's hours + invariant spot-audit, and Saturday's cluster-VOPR is deliberate: four independent records of rig health that must agree, so the retrospective's "≥4000 hours, 3-node VSR, cluster-VOPR green, tripwire never hit" claim is *corroborated*, not asserted — the difference between a credible founding-eng bet and a demo.
  - **Output**: `notes/rig_audit_W143_close.md` exists with ≥4000-hour status + per-node/per-crate running state + the final stats snapshot (incl. divergence-count = 0 + fund coverage); cross-checks against `ops/runtime_hours.md`, the alert log, the invariant spot-audit, and the cluster-VOPR drill.
- [ ] **Commit + log** (~10 min).
  - **Output**: `git log -1 --oneline` shows the Saturday cluster-VOPR + rig-audit commit.

## [v3.1 BFT apex — Track B] W143: BFT-apex ACCEPTANCE GATE (the bounded-MVP lock converges)

> Track B terminal week. This is the apex analogue of the W117 capstone gate: it **converges** the W118 bounded-MVP acceptance against the built+proven+swapped artifact (W119–W138). **No new mechanism** — it confirms every MVP bound is met. Passing this closes the committed terminal apex (scope-(a)); the plan's north-star trajectory is complete.

- [ ] [Venue · BFT] **BFT-apex acceptance gate** — confirm each W118 bounded-MVP criterion against its evidence; PASS/FAIL per row; a single FAIL means the apex is not accepted (remediate the named week). (~total 120 min)

  **The acceptance lock (W118 "Terminal Apex" + plan-of-record):**
  - [ ] **Protocol family** — pipelined HotStuff, **Jolteon 2-chain + Timeout-Certificate** (W121 TC, W123 2-chain commit — the **consecutive-round** rule: B commits iff QC(B) and QC(direct child B′) with B′.round == B.round + 1; `non_consecutive_child_qc_does_not_commit` green). Evidence: `bft/pacemaker.rs` (TC) + `bft/commit.rs` (2-chain rule).
  - [ ] **Fixed N = 4 (f = 1 Byzantine)** — `BftQuorum::threshold(4)=3` (2f+1 of 3f+1). Evidence: W119 `bft_threshold_matches_protocol`; the cluster harness runs N=4 throughout.
  - [ ] **Hot-path latency target X** — committed-batch **p99 ≤ 2 ms** (median ~700 µs), 4-node real-NIC kernel-TCP LAN, coordinated-omission-corrected, pacemaker-timeout incidence < 0.1%, via O1 batch-verify + zero-alloc + pipelining (persist-before-vote in the loop). Evidence: W135 `bft_commit_p99_under_2ms` + the `latency-lab` report.
  - [ ] **Byzantine scenario set** — the 8 scenarios (equivocation, vote-withholding, leader-equivocation/conflicting-QC, safety-attack, partition+Byzantine, stale-high-QC, orphaned-before-slot [O5], OpNumber-contiguity [O4]) StateChecker-green, with a planted-bug canary proving the harness has teeth. Evidence: W129 `bft_vopr_8_scenarios_statechecker_green` + `bft_vopr_finds_a_planted_bug`; W138 whole-venue re-run.
  - [ ] **Zero-rewrite swap** — VSR→BFT is the four-type-param change with an **empty `perp-dex-core` engine diff** (only the composition-root constructor differs). Evidence: W137 `swap_is_four_type_params_only` + `engine_diff_empty_vsr_vs_bft` (W128); `perp-dex-core` v2 = BFT-replicated, the v1.0 acceptance ladder holds under BFT (W138).
  - [ ] **Liveness** — `exactly_one_terminal_event_per_handle` (permanent-prune, W125) green under the Byzantine schedules (W129/W138) — the O5 "never neither" clause holds on the real BFT impl.
  - [ ] **O-items disposition** — O1 (auth: ed25519+batch shipped, BLS deferred to large-N behind the seam), O2 (pipeline depth absorbed into X), O3 (cert opaque, impl-only), O4 (dense OpNumber, impl-only), O5 (RESOLVED + amended + discharged), O6 (speculative apply, consciously deferred to v2). All accounted; none silently dropped.
  - 🎯 **Hardened acceptance**: a dated "BFT-APEX: ACCEPTED" line in `progress.md` citing each criterion's evidence week + the measured X; OR a named FAIL with the remediation week. The apex artifact = a HyperBFT-class, trading-optimized BFT engine swapped under the venue with zero engine rewrite — the founding-engineer **frontier** proof (scope-(a), the $20–100M-upper-band positioning, §1/§7).
  - *Mental model*: this gate is the trajectory commitment (W118) made real — "non-optional BFT apex" attaches to *this bounded artifact*, and W143 is where the numbers confirm it. The whole arc (W91 seam → O5 amendment → W118 design → W119–W138 build/prove/swap) was constructed so this gate is a *confirmation*, not a scramble.
  - **Output**: `progress.md` "BFT-APEX: ACCEPTED" with per-criterion evidence + measured X; `notes/143_apex_acceptance.md` mapping each W118 bound → its evidence week. **The committed terminal apex is complete.**
- [ ] [Venue · BFT] Commit + log. (~10 min)

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
