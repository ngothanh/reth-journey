# Week 138 — First month: midpoint check

> **Phase**: 7
> **Month**: M35
> **Track**: Career (destination landing)
> **Hours target**: 30h/wk (W137-W144)
> **Mirror target**: n/a (career phase — no crate is built this week)
> **Crate(s) touched**: n/a (maintenance only on Reth + Tempo; HFT runtime keeps accruing toward the M35 4000h-by-M36 gate)
> **Bar**: (n/a) — career/ops week, no Build exercise (per SPEC §6: depth ≠ padding; do not manufacture fake Builds)
> **Feeds into**: W139-W143 settling + permanent housing (the 30/60/90 plan written here is the contract those weeks execute against); W144 M36 retrospective + plan close (this week's midpoint snapshot is the first of two data points the retro plots).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): your own W135 `notes/30_60_90.md` (the success definition written in week 1 at the new firm); README §"Milestones" line "M35 end (W138): 3800+ runtime hours, first month at new firm complete"; README §"APPENDIX E" W135-W138 "First month at new firm".
> **v2 modifications**: none

This is a midpoint-check week, not a build week. Six weeks in, the only deliverable is an honest, written self-assessment against the 30/60/90 expectations set in W135, surfaced to your manager before the gap compounds. Every item below names the file it writes and the command/observation that proves it done. The HFT runtime fleet keeps running in the background toward the 3800h M35 milestone — verify the number, don't babysit it.

**Monday — Self-assessment against 30/60/90**

- [ ] Self-assessment: `notes/138_midpoint_check.md` — score yourself against each line of the W135 30/60/90 plan, RAG-tagged (🟢 on track / 🟡 slipping / 🔴 off). (~30 min)
  - 🎯 Hardened acceptance: every line of `notes/30_60_90.md` (from W135) has a matching 🟢/🟡/🔴 row in `notes/138_midpoint_check.md` with one sentence of evidence (a shipped PR, a meeting outcome, a metric) — no row tagged on vibes, no W135 expectation left unscored.
  - *Mental model*: the 30/60/90 plan is worthless if it's only consulted at day 90 — by then a 🔴 is a performance conversation, not a course-correction. The midpoint read at week 6 is the last cheap moment to catch a slip while it's still a "let me re-prioritize" rather than a "we need to talk." A 🟡 surfaced now is a Tuesday agenda item; a 🟡 discovered at day 90 is a review note.
  - Output: `notes/138_midpoint_check.md` exists with one RAG-tagged + evidenced row per W135 expectation; `grep -c '🔴\|🟡\|🟢' notes/138_midpoint_check.md` equals the line count of the W135 plan.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Monday self-assessment commit.

**Tuesday — Manager 1:1**

- [ ] Manager 1:1: bring the midpoint check, lead with the 🟡/🔴 rows and your proposed correction for each. (~30 min)
  - 🎯 Hardened acceptance: every 🔴 and every 🟡 from Monday's check is either (a) raised with a concrete proposed fix, or (b) explicitly logged as "deferred, with manager's agreement and reason" — none silently dropped. The 1:1 produces ≥1 written takeaway (a re-scoped expectation, a new owner, or an explicit "you're on track, keep going") recorded in the file.
  - *Mental model*: you raise the bad news, framed with a fix, before your manager discovers it — that converts the conversation from "explaining a gap" to "demonstrating ownership," which is exactly the signal that survives the 90-day review. A senior engineer 6 weeks in is judged less on output volume than on calibration: do you know where you stand without being told? This 1:1 is that proof.
  - Output: `notes/138_midpoint_check.md` gains a `## 1:1 takeaways` section with ≥1 dated, written outcome per non-🟢 row.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday 1:1-takeaways commit.

**Wednesday — Work + act on one correction**

- [ ] Execute the single highest-priority correction from Tuesday's 1:1 (re-scope, reach out to an owner, or pick up the work that closes the top 🟡/🔴). (~30 min)
  - 🎯 Hardened acceptance: the top-priority correction has a concrete first action taken today (a message sent, a task picked up, a doc started) — not just re-noted; the action is linked in the midpoint file.
  - *Mental model*: a 1:1 takeaway that isn't actioned within 48h decays into a "we talked about it" with nothing behind it. Closing the loop on the single most important item the same week is what makes the next 1:1 a progress report instead of a repeat.
  - Output: `notes/138_midpoint_check.md` `## 1:1 takeaways` top row gains a dated `→ action:` line with the artifact link.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday correction commit.

**Thursday — Work**

- [ ] Day-job work; capture any new 30/60/90-relevant signal (praise, friction, a scope shift) into the midpoint file as it happens. (~15 min capture)
  - *Mental model*: the midpoint file is now the living record the W144 retro will read — small daily signal captured in-the-moment beats a reconstructed-from-memory summary at day 90.
  - Output: `notes/138_midpoint_check.md` gains a dated `## week-6 signal` bullet (or an explicit "no new signal today" line).
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday signal commit.

**Friday — Work + HFT runtime milestone verification**

- [ ] Verify the M35 runtime milestone: confirm the HFT fleet has crossed 3800+ cumulative runtime hours and record the reading. (~20 min)
  - 🎯 Hardened acceptance: the reading is pulled from the actual runtime ledger/dashboard (not estimated), is ≥3800h, and is logged with the date and source; if <3800h, log the shortfall + the gap-to-4000h-by-M36 (W144) and what catches it up.
  - *Mental model*: the 4000h-by-M36 bar (README §"Bar policy": bar (c) "4000+ runtime hours by M36") is the credibility receipt for the HFT crates — it's not a vanity metric, it's the "this ran in anger" evidence a destination-tier HFT shop screens for. W138 is the M35 checkpoint on that ramp; catching a shortfall now leaves 6 weeks (W139-W144) to recover, vs discovering it at the W144 plan close when there's no runway left.
  - Output: `notes/138_midpoint_check.md` gains a `## runtime milestone` line: `<hours>h as of <date>, source: <ledger/dashboard>`; if ≥3800h, milestone met; else gap + recovery note recorded.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Friday runtime-milestone commit.

**Saturday — Personal projects: light maintenance (≤4 hrs/wk)**

- [ ] Reth: one PR review on the storage-trie / exec-vm area you owned in Phase 1-4. (~120 min)
  - 🎯 Hardened acceptance: the review leaves ≥1 substantive comment (a correctness/perf observation, not just LGTM) and the PR link + your comment are logged.
  - *Mental model*: 6 weeks into a day-job, personal OSS time has collapsed to ≤4 hrs/wk — that's the new normal, so the maintenance bar shifts from "ship features" to "stay credible and stay in the maintainer graph." One substantive review/week keeps your name in the Reth contributor stream that the entire backup-job channel (README warm-network) is built on, at a fraction of the time cost of authoring.
  - Output: `notes/138_midpoint_check.md` gains a `## reth maintenance` line with the reviewed PR URL + a one-line summary of your comment.
- [ ] Tempo: light skim if still in maintenance mode (conditional — skip if Path D is closed). (~30 min)
  - 🎯 Hardened acceptance: a one-line conditional decision is recorded — either "Path D active: skimmed `<area>`, status `<note>`" or "Path D closed: skipped, reason `<reason>`". No silent skip.
  - *Mental model*: Tempo maintenance is a hedge (Path D from the W120 outreach gate), not an obligation — recording the active/closed decision keeps the hedge honest and stops it from silently consuming the scarce ≤4h budget when it's no longer paying off.
  - Output: `notes/138_midpoint_check.md` gains a `## tempo` line with the conditional decision.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday maintenance commit; `progress.md` has the W138 entry.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
