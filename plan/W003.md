# Week 3 — Async/Pin/Future via `eth-network-codec`

> **Phase**: 1
> **Month**: M1
> **Track**: Reth (primary)
> **Hours target**: 30h/wk (M1-M18)
> **Mirror target**: `reth-eth-wire` framing layer + `tokio_util::codec::Framed`.
> **Crate(s) touched**: `crates/eth-network-codec/`
> **Feeds into**: [NEW] Side product — `BackpressureStrategy` enum (Saturday) is the seed for the `backpressure` crate extracted at W11.
> **v2 modifications**: none (W1-W14 unchanged from v1)

**Monday — Tokio fast track + transport scaffold**

- [X] Read Tokio tutorial cover-to-cover. (~90 min)
- [X] **Build**: `crates/eth-network-codec/src/transport.rs` — TcpStream wrapper + `LengthDelimitedCodec` with 1 MiB max
  frame. (~60 min)
- [X] Manual TCP echo via framed transport. (~30 min)
- [X] Commit + log (~10 min)

**Tuesday — Manual Future + `MessageRequest`**

- [X] Async Book ch1-7 in one go. (~90 min)
- [X] Watch Crust of Rust: Async/Await (full) — implement trivial executor. (~75 min)
- [X] **Build**: `crates/eth-network-codec/src/request.rs` — `MessageRequest<R>` future. (~60 min)
- [X] Counter Future applied: `RetryFuture<F: Future>`. (~30 min)
- [X] Commit + log (~10 min)

**Wednesday — Pin/Unpin via `MessageStream`**

- [X] Watch Crust of Rust: The Drop Check; read `std::pin` docs. (~75 min)
- [X] **Build**: `crates/eth-network-codec/src/stream.rs` — `MessageStream<C: Codec, IO>` implementing
  `tokio_stream::Stream`. Use `pin_project_lite`. (~60 min)
- [X] Demonstrate why MessageStream cannot be Unpin. Rewrite once with manual unsafe pin projection, then with
  pin_project_lite. Compare. (~45 min)
- [X] **Pin/Unpin internalization exercises** (scratch crate; each builds one specific aha):
  - [X] E1 — Self-ref bug: write `SelfRef { data: [u8;4], ptr: *const u8 }` where ptr aims into data; `mem::swap` two instances and dereference ptr → UB. Motivates Pin's existence. (~10 min)
  - [X] E2 — Auto-impl off-switch: `fn assert_unpin<T: Unpin>()` over u32/String/Vec/HashMap/TcpStream — all pass; then a struct with a `PhantomPinned` field → compile error. (~5 min)
  - [X] E3 — Pin elision: `fn touch<T: Unpin>(p: Pin<&mut T>) { p.get_mut(); }` works; drop the `Unpin` bound → compile error. Proves Pin is free for Unpin types. (~5 min)
  - [X] E4 — Manual projection failure: write `LoggedFuture<F>` that delegates `poll`. Naive `self.inner.poll(cx)` → error. Try `Pin::new(&mut self.inner)` with `F: Unpin`. Try `unsafe { map_unchecked_mut }`. Rewrite with `pin_project_lite`. Feel each option. (~15 min)
  - [X] E5 — Build `Map<F, G>` combinator with `pin_project_lite` (same shape as every `futures-util` combinator: `#[pin] future: F`, `op: Option<G>`, take-and-call on Ready). (~25 min)
- [X] `notes/06_pin_unpin.md` — worked example. (~25 min)
- [X] Commit + log (~10 min)

**Thursday — `EthMessage` enum + `Codec` trait**

- [X] **Build**: `crates/eth-network-codec/src/codec.rs` — `Codec` trait. (~60 min)
- [X] **Build**: `crates/eth-network-codec/src/message.rs` — `EthMessage` enum subset: Status, BlockHeaders,
  BlockBodies, NewBlock, GetBlockHeaders. (~45 min)
- [X] RLP placeholder (tagged-byte format; full RLP comes Week 5). (~30 min)
- [X] tokio TCP server with graceful shutdown. (~30 min)
- [X] Commit + log (~10 min)

**Friday — Token bucket as custom `Future` + per-peer rate limiting**

> **Why this exists**: a rate limiter caps how fast one peer can make the node do work (decode frames, serve
> requests), so a noisy or malicious peer can't saturate it. Token-bucket is the standard algorithm; this file is
> the seed of the `backpressure` crate extracted at W11. Upstream mirrors: `tower::limit::rate::RateLimit`, `governor`.

- [X] **Build**: `crates/eth-network-codec/src/rate_limit.rs` — `TokenBucket` as a custom `Future`. (~60 min)
  - *Mental model*: a bucket holds up to `capacity` tokens; tokens refill continuously at `refill_per_sec`. One
    rate-limited action costs one token — if one is ready, proceed now; if not, wait until one accrues.
  - *State to track*: capacity, current token count (fractional — tokens accrue smoothly), refill rate, the
    `Instant` of the last refill calculation, and a timer for when you're waiting.
  - *Contract*: a poll-style entry point — `poll_acquire(&mut self, cx) -> Poll<()>` — returning `Ready(())` once
    it has consumed a token, `Pending` otherwise. "Custom Future" = wrap that poll logic in a type that
    `impl Future`; whether the bucket *is* the future or hands out `Acquire` futures is your design call.
  - *poll, roughly*: (1) add tokens accrued since `last_refill`, clamp to `capacity`; (2) if ≥1 token, consume one,
    return `Ready`; (3) else work out time-to-next-token, arrange to be polled again then, return `Pending`. Step 3
    is the real exercise.
  - **Expect to hit #1 — lost wakeup**: step 3 returns `Pending` but never registers a `Waker`. Symptom: the
    future hangs forever — throughput flatlines to zero, no panic, no error.
    - Fix: hold a `tokio::time::Sleep` set to the next-token deadline and poll *it*; a `Sleep` that returns
      `Pending` registers the waker for you — only return `Pending` after it does.
    - Muscle: a `Pending` future must guarantee the task gets woken, or it is lost. Reapplies at: W11
      `backpressure` crate, W58 runtime timer wheel + io_uring timers.
  - **Expect to hit #2 — timer re-armed every poll**: a fresh `Sleep` is created on each `poll`, so its deadline
    keeps resetting. Symptom: the future stays `Pending` far longer than the configured wait, or never fires.
    - Fix: store the `Sleep` as a field; recreate it only after it fires or after a token is consumed.
    - Muscle: timer state must outlive a single `poll` call. Reapplies at: W58 runtime timer wheel, W59 runtime
      backpressure scheduling.
  - **Expect to hit #3 — wall-clock refill / unbounded accrual**: using `SystemTime` for elapsed time, or letting
    the token count grow past `capacity` while idle. Symptom: refill jumps or stalls when the system clock is
    adjusted; or after an idle gap a peer gets a burst far bigger than `capacity`.
    - Fix: use the monotonic `Instant` clock; clamp accrued tokens with `min(capacity, …)`.
    - Muscle: rate math uses monotonic time and a bounded reservoir. Reapplies at: W11 `backpressure`, W59 runtime
      backpressure scheduling.
  - *Pin note*: `tokio::time::Sleep` is `!Unpin` — holding one as a field makes your struct `!Unpin`, so you must
    pin-project it (`pin_project_lite`, `#[pin]`). Same lesson as Wednesday's `MessageStream`; reuse that shape.

- [X] **Build**: `RateLimitedStream<S: Stream>` (same file). (~75 min)
  - *Mental model*: wraps an inner `Stream` and only yields an item once the bucket grants a token — same wrapper
    shape as `MessageStream` (pin-project the inner stream, drive it in `poll_next`).
  - *Contract*: `impl Stream for RateLimitedStream<S>` with `Item = S::Item`; `poll_next` gates on the bucket, then
    pulls from `S`.
  - *poll_next, roughly*: poll the bucket's `poll_acquire`; if `Pending`, return `Pending`; once `Ready`, poll the
    inner stream and return its item.
  - **Expect to hit #1 — wasted token on a stalled stream**: a token is consumed, then the inner stream returns
    `Pending` or `None`, so the token bought nothing. Symptom: effective rate drifts below the configured limit;
    with `None`, the last token is burned at end-of-stream.
    - Fix: order it deliberately — e.g. only consume a token once an item is in hand. This is a design decision,
      not a one-line bug; decide and write down why.
    - Muscle: gate and source must be ordered intentionally. Reapplies at: W11 `backpressure` crate, W74
      matching-engine raft wire.
  - **Expect to hit #2 — projecting two pinned fields**: both the inner `S` and the bucket's `Sleep` need pinning;
    a naive `&mut self.stream` won't compile. Symptom: `cannot borrow as mutable` / `Unpin` errors.
    - Fix: `pin_project_lite` with `#[pin]` on the inner stream (and on the bucket if it owns the `Sleep`).
    - Muscle: every `!Unpin` field touched in a manual `poll` impl must be projected. Reapplies at: W30 runtime task
      struct, W58 runtime timer integration.

- [X] Test under load (1k concurrent peers). (~30 min)
  - *Setup*: spawn 1000 tasks, each with its own `RateLimitedStream` (per-peer = independent buckets) over an
    always-ready source.
  - *Assert*: over a fixed window each stream yields ≈ `rate × seconds` items, within ~±15% tolerance.
  - *Proves*: the limiter holds the configured rate **and** never deadlocks at scale — a lost wakeup (pitfall #1)
    shows up here as a hung task.

- [X] Commit + log (~10 min)

**Saturday — `BackpressureStrategy` + observability + tag v0.1.0**

> **Why this exists**: Friday's rate limiter caps how fast items *leave*; it says nothing about the backlog that
> piles up when a peer produces faster than the limiter releases. A bounded buffer governed by a
> `BackpressureStrategy` is the answer — when the buffer is full, the strategy picks who loses: the oldest queued
> item, the newest arrival, or the producer (forced to wait). This enum is the seed of the `backpressure` crate
> extracted at W11. Upstream mirrors: `tokio::sync::mpsc` bounded channel (`Block`), drop-oldest channels like
> `tokio::sync::broadcast` (`DropOldest`), `tower::buffer::Buffer`.

- [ ] **Build**: `crates/eth-network-codec/src/backpressure.rs` — `BackpressureStrategy` enum + a `BoundedBuffer<T>`
  that applies it. **[NEW]** Doc-comment the enum: "extracted into `backpressure` crate W11." (~45 min)
  - *Mental model*: a fixed-capacity queue sits between a fast producer and a slow consumer. While it has room,
    `push` just enqueues. Once full, the strategy decides the overflow policy — `DropOldest` evicts the front then
    enqueues the new item (freshness wins), `DropNewest` rejects the arrival (queue contents win), `Block` refuses
    to accept until the consumer drains a slot (no loss, producer pays).
  - *State to track*: a `VecDeque<T>`, a `capacity`, the chosen `BackpressureStrategy`, and — for `Block` — a
    `tokio::sync::Notify` (or semaphore) the consumer signals on every dequeue.
  - *Contract*: `enum BackpressureStrategy { DropOldest, DropNewest, Block }`. A `BoundedBuffer<T>` with a sync
    `try_push(&mut self, item: T) -> PushOutcome<T>` returning `Accepted` / `Dropped(T)` (the evicted or rejected
    item, so the caller can count it) / `Full(T)` when the strategy is `Block`; plus an `async fn push` that, on
    `Full`, awaits the `Notify` and retries.
  - **Expect to hit #1 — `Block` collapses into a drop**: implementing `Block` as a busy-spin, or worse, silently
    behaving like `DropNewest` because there is no wakeup path. Symptom: under load the `Block` buffer either pegs a
    core spinning or quietly loses items — indistinguishable from `DropNewest` in a test.
    - Fix: the consumer signals a `Notify`/semaphore permit on every dequeue; `Block`'s `async push` parks on that
      signal and retries — no spin, no loss.
    - Muscle: "wait" is a wakeup contract, not a loop — same lesson as Friday's lost wakeup. Reapplies at: W11
      `backpressure` crate, W59 runtime backpressure scheduling.
  - **Expect to hit #2 — `DropOldest` drops but doesn't insert**: evicting the front then forgetting to enqueue the
    new item, or enqueuing first and then evicting the item you just added. Symptom: the buffer steadily shrinks, or
    the newest items vanish while stale ones survive.
    - Fix: on full + `DropOldest`, `pop_front()` *then* `push_back(item)`; the invariant `len == capacity` must hold
      after every overflow.
    - Muscle: overflow policies must preserve the capacity invariant. Reapplies at: W11 `backpressure` crate.
  - **Expect to hit #3 — backpressure conflated with rate-limit `Pending`**: treating Friday's `poll_acquire`
    returning `Pending` as a buffer-overflow event. Symptom: drop counters tick up when the limiter is merely pacing
    output, not when the buffer is actually full — the metrics lie.
    - Fix: keep the two distinct — rate limiting paces *output*, the buffer absorbs *input* backlog; a drop is only
      ever a full-buffer event.
    - Muscle: name each backpressure mechanism by where it acts (source vs. sink vs. pacing). Reapplies at: W11
      `backpressure` crate, W74 matching-engine raft wire.

- [ ] Tracing spans for connection lifecycle. (~30 min)
  - *Mental model*: one span per accepted connection, opened on accept and closed on disconnect; every decode/serve
    event inside inherits it, so a log line carries `peer` + `remote_addr` without threading them by hand.
  - *Contract*: a `connection` span with fields `peer`, `remote_addr`; events at `accepted`, `handshake_ok`,
    `disconnected` (with reason). Per-connection work runs *inside* the span.
  - **Expect to hit — span guard dropped across `.await`**: holding a `let _g = span.enter()` guard across an
    `.await`. Symptom: the span looks entered but does not follow the task when it resumes on another worker — log
    lines after the await lose the span, or attribute to a different connection.
    - Fix: never hold an `Entered` guard across `.await`; attach the span to the future with `.instrument(span)`
      (or `#[instrument]` on the async fn) so it follows the task across every yield.
    - Muscle: thread-local context and `.await` do not mix — bind context to the future, not the thread. Reapplies
      at: W30 runtime task struct, W59 runtime scheduling.

- [ ] Prometheus metrics via the `metrics` crate. (~30 min)
  - *What to expose*: `connections_active` (gauge), `connections_total` (counter), `frames_decoded_total`
    (counter), `bytes_in_total` (counter), `backpressure_drops_total` (counter, labelled by strategy),
    `frame_decode_seconds` (histogram).
  - *Contract*: register a `metrics-exporter-prometheus` recorder; counters/gauges updated at the same call sites as
    the tracing events.
  - **Expect to hit — high-cardinality labels**: labelling a metric with `peer_id` or `remote_addr`. Symptom: with
    10k peers the exporter holds 10k+ distinct time series per metric — memory balloons and the `/metrics` scrape
    slows to a crawl.
    - Fix: labels are for bounded, low-cardinality dimensions (strategy name, message kind); per-peer identity
      belongs in tracing spans, not metric labels.
    - Muscle: metric labels must be bounded-cardinality; unbounded identity goes to logs/traces. Reapplies at: W11
      `backpressure` crate, W59 runtime observability.

- [ ] Load test with 10k concurrent connections. (~30 min)
  - *Setup*: spawn 10k tasks, each opening a framed connection through the server, each carrying its own connection
    span and a `BoundedBuffer` on a deliberately slow consumer so the overflow path is actually exercised.
  - *Assert*: `connections_active` peaks at 10k then returns to 0; `backpressure_drops_total` is 0 for `Block` and
    > 0 for `DropOldest`/`DropNewest` under the slow consumer; no task hangs (a `Block` lost wakeup shows up here as
    a stuck task).
  - *Proves*: the observability stack survives 10k-series load and the three strategies behave distinctly — `Block`
    loses nothing, the drop strategies bound memory.

- [ ] Tag `eth-network-codec v0.1.0`. (~5 min)
  - First tagged milestone: framing + futures + streams + rate limiting + backpressure + observability. Use an
    annotated tag (`git tag -a v0.1.0`) and note the commit in the log.

- [ ] Commit + log (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.

- [ ] Inheritance check. (~15 min)
