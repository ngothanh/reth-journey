# Week 112 — Blog post #5: mini-db inheritance + sustained operations

> **Phase**: 6
> **Month**: M28
> **Track**: (career/ops) Sustained operations + interview-prep ramp begins
> **Hours target**: 40h/wk (M19-M34)
> **Mirror target**: (none — this is the public-write-up + ops week for the `mini-db` CAPSTONE shipped W100; no upstream module is mirrored)
> **Crate(s) touched**: (none — `mini-db` v1.0 is frozen at W100; this week documents and operates it, does not edit it)
> **Bar**: (n/a) — career/ops/blog week. The crate it discusses, `mini-db`, is bar (b); no code is written here so no bar applies.
> **Feeds into**: W119 interview prep (the inheritance-ratio story and the dependency graph become a portfolio talking point), W121 flagship blog post (reuses the LOC-inheritance numbers + graph), W125 applications (blog #5 is a linked artifact in outreach)
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): README §"Inheritance Map" (lines ~699-828) for the canonical dependency edges; README §APPENDIX inheritance-ratio table (line ~819, `mini-db v1.0 ... ≥0.70`); your own `notes/` from W95-W100 for the as-built numbers.
> **v2 modifications**: none

**Monday — Blog post #5 draft**

- [ ] **Draft** `blog/2027-XX-XX-mini-db-inheritance.md` — "How 8 crates wrote my LSM database", centered on the inheritance discipline. Open with the dependency graph (mini-db ← lsm-core, wal, recovery, txn, bufpool, bloom, time, backpressure, runtime-thread-per-core). State the measured LOC-inheritance ratio (target ≥0.70; the W100 as-built was ~0.78) and HOW it was measured, not just the number. (~45 min)
  - 🎯 Hardened acceptance: draft contains (1) the rendered dependency graph with the 8+ named upstream crates, (2) the exact inheritance-ratio number AND the one-line method that produced it (e.g. `inherited_LOC / total_LOC` with what counts as "inherited"), (3) at least one concrete "this primitive was built once at W.. and reused untouched here" example. `ls blog/2027-XX-XX-mini-db-inheritance.md` shows the draft.
  - *Mental model*: This is the public proof that "build the primitive once" paid off — the single number (≥0.70) is the whole five-year thesis made measurable. It exists now because the CAPSTONE just shipped (W100) and the numbers are fresh; writing it now (vs at W121) means the flagship post can cite a lived, already-published artifact instead of reconstructing it.
- [ ] Commit `blog/2027-XX-XX-mini-db-inheritance.md` draft. (~10 min)
  - Output: `git log -1 --stat` shows the draft committed.

**Tuesday — Blog post #5 edit**

- [ ] **Edit pass** on `blog/2027-XX-XX-mini-db-inheritance.md` — tighten claims, verify every number against `notes/` and the README inheritance table, hold for review. (~40 min)
  - 🎯 Hardened acceptance: every quantitative claim in the post (ratio, LOC, crate count, runtime hours) has a checkable source noted inline or in a `notes/blog5_sources.md`; no number appears that you cannot point to its origin. Edit pass diff committed.
  - *Mental model*: A blog post about inheritance discipline that itself contains an unsourced number undermines its own thesis. The edit pass is where the post becomes citable in W121/W125 — an interviewer or maintainer who clicks through must find the claims hold.
- [ ] Commit + log (~10 min)
  - Output: `git log -1` shows the edit-pass commit; weekly log note added.

**Wednesday — Post + amplify**

- [ ] **Publish** `blog/2027-XX-XX-mini-db-inheritance.md` to the live blog; cross-post (canonical link), Twitter/X announcement thread, r/rust submission optional. Record the live URLs in `notes/blog5_distribution.md`. (~60 min)
  - 🎯 Hardened acceptance: `notes/blog5_distribution.md` lists the canonical URL + each cross-post URL with timestamps; the post resolves publicly (open the canonical link in a browser, confirm 200 + correct content).
  - *Mental model*: Distribution is the difference between a private note and a portfolio asset. The recorded URLs become the exact links pasted into W120/W125 outreach — capturing them now avoids re-hunting later.
- [ ] Commit + log (~10 min)
  - Output: `git log -1` shows `notes/blog5_distribution.md` committed.

**Thursday — Sustained operations check**

- [ ] **Ops check** — record the running uptime total in `notes/ops_uptime_log.md`. Target ~700-800 cumulative runtime hours by W112 end; confirm the trajectory still lands ≥2000 by M30 (W117). Note any rig outages and their cause. (~30 min)
  - 🎯 Hardened acceptance: `notes/ops_uptime_log.md` updated with this-week's cumulative-hours figure, the delta vs last week, and a one-line on-track / off-track verdict against the 2000-by-M30 line. If off-track, the note states the remediation.
  - *Mental model*: The 4000+ runtime-hours bar-(c) requirement (by M36) is only met by accruing hours continuously — a single number this week is meaningless; the SLOPE is the signal. Logging it weekly makes the M30 (W117) and M36 milestones a measurement, not a surprise.
- [ ] Commit notes (~10 min)
  - Output: `git log -1 --stat` shows `notes/ops_uptime_log.md` committed.

**Friday — Reth PR follow-through + Tempo skim + interview-prep kickoff**

- [ ] **Reth PR follow-through** — check open/in-flight Reth PRs for review comments; address any, or push the next small contribution. Log the state in `notes/reth_pr_tracker.md` (PR #, status, next action). (~40 min)
  - 🎯 Hardened acceptance: `notes/reth_pr_tracker.md` reflects the current state of every open PR with a concrete next action and date; if a PR has reviewer comments, they are either addressed (commit pushed) or a dated TODO is recorded.
  - *Mental model*: Merged Reth PRs gate the backup-job milestone (~W24-30 originally; momentum must not lapse). A weekly tracker keeps the contribution pipeline visible so a stale PR never silently rots before the W125 application window.
- [ ] **Tempo skim** — 15-min skim of Tempo repo activity since last check; note anything relevant to Path D in `notes/tempo_watch.md`. (~15 min)
  - 🎯 Hardened acceptance: `notes/tempo_watch.md` has a dated entry — either "no material change" or the specific change + whether it affects Path D viability.
  - *Mental model*: Path D (Tempo route) is a live backup option through Phase 7; a cheap weekly skim keeps it warm without derailing the primary track.
- [ ] **Interview-prep ramp kickoff** — interview prep formally begins W119; start the on-ramp now by stubbing `notes/interview_prep_plan.md` with the topic list (Rust concurrency/atomics, lock-free data structures, LSM/storage internals, systems design, HFT mechanical-sympathy) and which W004/W100 artifacts answer each. (~20 min)
  - 🎯 Hardened acceptance: `notes/interview_prep_plan.md` exists with ≥5 topic areas, and each topic names at least one concrete portfolio artifact (crate/week) that demonstrates it. This is the spine W119 fills in.
  - *Mental model*: Starting the prep map 7 weeks before W119 means interview prep is "fill in a plan," not "build a plan under time pressure." It also surfaces gaps now, while there is still time to remediate before applications open at W125.
- [ ] Commit + log (~10 min)
  - Output: `git log -1 --stat` shows the three Friday notes committed; weekly log updated.

**Saturday — Open issue triage on own repos**

- [ ] **Issue triage** — sweep your published crates for GitHub issues opened by external users; respond to each. Log dispositions in `notes/repo_issue_triage.md` (issue #, repo, response/disposition). (~30 min)
  - 🎯 Hardened acceptance: every open external issue has either a posted response or a recorded triage decision (wontfix/duplicate/needs-info) with rationale in `notes/repo_issue_triage.md`. If zero external issues exist, the note records that explicitly (a deliberate observation, not a skipped task).
  - *Mental model*: External issues are the first real signal that the "reusable production component" thesis holds — someone tried to USE the crate. Responsive triage is also visible maintainer behavior that strengthens the W125 application narrative.
- [ ] Commit + log (~10 min)
  - Output: `git log -1 --stat` shows `notes/repo_issue_triage.md` committed.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
