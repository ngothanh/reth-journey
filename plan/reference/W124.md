# Week 124 — Event-trip recon part 2 / post-trip wrap + §7 flip-trigger evaluation (no move)

> **Phase**: 7 (Bet Placement / Bridge)
> **Month**: M32
> **Track**: Bet-path — event-trip recon (part 2 of 2) + post-trip synthesis: convert the W123 in-person signal into a decision-grade **Bet-candidate ranking (§6-scored)**, a **§7 flip-trigger verdict** (warm-conversation cluster / presence-premium → prep-stays-prep OR convert-to-move), and a locked W125 outreach order — **NOT a relocation, NOT formal interviews** | Friday: light venue-artifact refresh (`perp-dex-core` bench numbers current before outreach)
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: (n/a — bet-path recon/synthesis week; no upstream crate module. Friday's refresh re-runs the *existing* `perp-dex-core` v1.0 / `risk-engine` v1.0 benches — no new upstream module is read)
> **Crate(s) touched**: (none net-new) — Friday refreshes bench numbers only: `perp-dex-core` v1.0 capstone bench (cluster commit p99 / per-mark-tick p99.99 at 100k accounts) + `risk-engine` v1.0 incremental-margin bench; **no scope change, no version bump beyond a `.z` patch** — the v1.0 lock (§4A) is unchanged
> **Bar**: (n/a for the recon/synthesis items — the only "bar" is whether each objective produces a sourced, falsifiable artifact line) | Friday's `perp-dex-core` / `risk-engine` benches retain their existing bar (c); this week re-certifies their numbers, it does not change scope
> **Feeds into**: W125 active outbound to on-chain-derivatives teams (this week's §6-scored Bet-candidate A–F ranking **is** the W125 outreach order; the warm-intro channels logged here are the W125 send-channels) — INDEX W125 "[v3] Active outreach … target list via §6 ecosystem-selection checklist"; W126 first founding/core-eng conversations (the warmest in-person leads from W123–W124 convert here; the §6 verdicts pre-filter who gets a deep conversation); W131 **Bet #1 decision** (the §7 flip-trigger verdict locked this week pre-decides the geography question so W131 is a clean GO/bridge call, not a city-and-bet entanglement); W132 conditional relocation logistics (only fires if THIS week's flip-trigger verdict = "convert"); the flagship blog (W121, already shipped — Friday's refreshed capstone bench numbers are the citation source if it gets an update before W125 outreach)
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): `README.md` §6 (the 7-point ecosystem-selection checklist — every Bet candidate is scored GO/bridge against it); §7 (shots-on-goal — Bet #1 is one of 2–3 sequential shots; the Dubai/SG geography block + the **explicit flip-trigger**: "≥N warm conversations cluster in one hub OR a presence-contingent offer premium appears"; "Until the trigger fires, Vietnam-remote remains the default"); §9 risk register ("Best venues want SG/Dubai presence → pre-stage, don't pre-commit"; "M24 outbound response rate low → it's controllable"); §5 M36 ("No pre-committed relocation — Dubai/SG decided at Bet #1 time per the §7 flip-trigger"); `notes/123_recon_*.md` (the W123 raw trip log — coffee-meeting / booth / side-event notes — the raw input for Monday synthesis); `notes/ecosystem_shortlist.md` (W115/W116 — the ≥5 §6-passing venues, now validated against people met in person); `notes/binance_alumni_map.md` + `notes/inbound.md` (W113/W120/W122 — the alumni graph + inbound funnel this trip activated); `notes/relocation_research.md` (W120 — visa + pre-TGE token-tax eligibility for BOTH hubs, the cost basis the flip-trigger reads); destination cost-of-living/visa portals (lookup-when-stuck, official/primary sources only)
> **v3 modifications**: (1) **Re-aimed from v2 "recon-trip synthesis + Tier-A-HFT application shortlist + M32 close" to v3 "post-trip wrap + §7 flip-trigger evaluation + Bet-candidate ranking."** The week no longer produces a firm-application shortlist for a *job*; it produces a **§6-scored Bet-candidate ranking** (real bets vs bridge jobs) and a **§7 flip-trigger verdict** that pre-decides geography for the W131 Bet #1 call. (2) **The Tuesday "firm shortlist (Tier A / Tier B floor)" → "Bet-candidate ranking (GO-bet vs bridge), scored against §6"** — Tier-A/Tier-B job framing is replaced by the §6 seven-point GO/bridge gate; the crypto-MM cash seat is the explicit *bridge floor* (§7), not a "Tier-B firm." (3) **The Friday capstone-crate polish → `perp-dex-core` v1.0 + `risk-engine` v1.0 bench refresh** (the v3 capstone), replacing the v2 `matching-engine v1.2` + `mini-db v1.1` polish — `mini-db` is demoted (§4) and is NOT a capstone artifact in v3. (4) **The Thursday "matching-engine deep-dive interview drill" → "the principal artifact deep-dive: the cross-margin + liquidation engine"** — the rehearsal target is the `risk-engine`/`liquidation-engine`/`perp-dex-core` story (the convergence cell, §3), the artifact that actually clears the founding-engineer bar, not a generic HFT matching-engine screen. (5) **"No move" is an explicit invariant of the week** — the flip-trigger verdict may be "convert to relocation prep," but the *move itself* is decided at Bet #1 time (W131/W132), never on post-trip enthusiasm.

This is a bet-path **recon-synthesis + flip-trigger-decision week**, not a Build week. There is no net-new crate, no upstream mirror to read, and no engineered trap-test arc — manufacturing one would be padding. The week's job: turn the raw W123 in-person trip log into three decision-grade artifacts that W125 and W131 consume directly — (i) a **§6-scored Bet-candidate ranking** that doubles as the W125 outreach order and cleanly separates GO-bets from bridge jobs; (ii) a **§7 flip-trigger verdict** (does a warm-conversation cluster or a presence-contingent offer premium exist in Dubai or SG — and therefore does prep convert to relocation prep, or stay prep?); and (iii) a refreshed set of **capstone bench numbers** so every figure quoted in W125 outreach was measured this week, not months ago. Every item below names its artifact file and carries an Output line that proves it done; the synthesis / §6-scoring / flip-trigger / drill items also carry a `🎯 Hardened acceptance` pass criterion and a `*Mental model*` of why the item exists now and what plan-number it moves. **The invariant that frames the whole week: this is synthesis, not relocation — you place no lease, sign nothing; the flip-trigger verdict only sets whether the move *decision* (made at Bet #1 time, W131) starts from "stay remote" or "be ready to move." See §7.**

---

## Monday — Recon-notes synthesis: in-person signal → plan-changing facts

- [ ] Artifact: `notes/124_recon_synthesis.md`. Synthesize the raw W123 trip log (`notes/123_recon_*.md` — coffee meetings, booth conversations, side-events/dinners) into facts that **change a number or a decision**, not a travelogue. The output is the substrate Tuesday's §6 ranking and Wednesday's flip-trigger verdict read from. (~50 min)
  - 🎯 **Hardened acceptance**: `124_recon_synthesis.md` contains (a) a `## Conversations` table — every substantive W123 conversation as one row: `{person/role, org, on-chain-derivatives bet OR crypto-MM bridge OR neither, warmth (cold/warm/hot), hub (Dubai/SG/remote), one concrete next-step}`; (b) a `## Plan-changing facts` section with ≥3 items each phrased as "X changes Y" (e.g. "Venue A is pre-TGE seed with Paradigm on the cap table → moves it from shortlist-candidate to scored Bet-candidate"; "two of the warmest founders are physically clustered in Dubai → flip-trigger evidence, evaluate Wed"); (c) a `## Bridge funnel` line counting the crypto-MM (Wintermute/Jump-style) cash-seat conversations separately from the bet conversations (the §7 income floor is a *different* funnel, scored differently). No row is "had a nice chat" with no next-step.
  - *Mental model*: a recon trip that doesn't change a fact on paper was a vacation (this is the same discipline as W123's framing, now applied post-trip). Two funnels exist and must not be merged: **bets** (founding/core-eng token-equity, §6-gated) and the **bridge** (crypto-MM cash seat, the §7 floor) — conflating them is how you talk yourself into taking a bridge as if it were a bet, the exact Airwallex anti-pattern §6 #1 warns against. The single fact every later step hinges on is "which conversations are real, warm, and which hub are they in" — that fact feeds Tuesday's ranking and Wednesday's trigger.
  - Output: `notes/124_recon_synthesis.md` exists meeting the criteria; `grep -c '^|' notes/124_recon_synthesis.md` ≥ 4 (the conversations table rows); the `## Bridge funnel` line present.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Monday synthesis commit.

## Tuesday — Bet-candidate ranking, scored against the §6 checklist (GO-bet vs bridge)

- [ ] Artifact: `notes/124_bet_candidate_ranking.md`. Build the ranked Bet-candidate list — every on-chain-derivatives venue surfaced by W123 + the W115/W116 `ecosystem_shortlist.md`, **each scored against the §6 seven-point checklist**, classified GO-bet (all 7 pass) vs bridge (any fail). This list IS the W125 outreach order. (~50 min)
  - 🎯 **Hardened acceptance**: `124_bet_candidate_ranking.md` lists 4–6 candidates, each a row with the **§6 scorecard** filled `pass/fail/unknown` across all seven points — {1 founding-tier allocation, 2 tier-1 backing (Paradigm/a16z-crypto/Dragonfly/Polychain/1kx/Multicoin/Jump/Wintermute on cap table), 3 core-criticality (I build the engine that *is* the company), 4 multi-billion-FDV ceiling, 5 token reality (pre-TGE, sane vesting, ≥10× headroom), 6 remote-native (Vietnam-remote works), 7 cycle timing} — plus {warm-intro channel from W113/W120/W123 if any, the hub, a one-line "why me" anchored to a *shipped* artifact (`perp-dex-core` v1.0 / the cross-margin engine), and the verdict `GO-bet | bridge | needs-data`}; ≥1 candidate is the explicit **crypto-MM cash-seat bridge floor** (§7 income floor — the seat you take to fund runway if no GO-bet lands, NOT a "Tier-B firm"); any candidate with an `unknown` on §6 #2 or #5 is tagged `needs-data` with the specific question to resolve in W125. No GO-bet verdict is assigned unless **all seven** §6 points read `pass`.
  - *Mental model*: in v2 this was a "Tier-A / Tier-B firm shortlist" for a *job*; v3 replaces it with the §6 GO/bridge gate because the terminal state is a *bet*, not employment (§0, §1 bar (b)/(d)). The §6 checklist exists precisely so W131's decision is a *choice between scored options*, not a hope — and so an exciting-but-late-equity venue is correctly demoted to "bridge," never mis-ranked as a bet (§6 #1 Airwallex anti-pattern). The bridge floor must appear on the list so that if every bet fails its §6 gate, runway is still secured via the crypto-MM cash seat (§7: "Never quit a bridge before vest milestones"; bridge funds the gap between shots). A `needs-data` tag is honest: cap-table backing and vesting terms are often not knowable until a real conversation — that's a W125 question, not a Tuesday guess.
  - Output: `notes/124_bet_candidate_ranking.md` exists meeting the criteria; every candidate row carries all seven §6 cells; `grep -c 'GO-bet\|bridge\|needs-data' notes/124_bet_candidate_ranking.md` ≥ 4; the crypto-MM bridge-floor row present.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday ranking commit.

## Wednesday — §7 flip-trigger verdict + staggered W125 outreach schedule

- [ ] Artifact: `notes/124_flip_trigger_verdict.md`. Evaluate the §7 flip-trigger against the W123 evidence and render a binding verdict: does prep stay prep (Vietnam-remote default holds) or convert to relocation prep (be ready to move *if* Bet #1 lands in that hub at W131)? The verdict pre-decides geography so W131 is a clean GO/bridge call. (~40 min)
  - 🎯 **Hardened acceptance**: `124_flip_trigger_verdict.md` has (a) a `## Trigger instrument` line restating the two §7 fire conditions verbatim — *"≥N warm conversations cluster in one hub"* and *"a presence-contingent offer premium appears (a tier-1 bet that pays materially more for on-site)"* — with **N declared as a concrete number** (e.g. N=3) so the verdict is falsifiable; (b) a `## Evidence` block tallying, per hub (Dubai / SG / neither), the count of *warm-or-hot* bet conversations from Monday's table and any explicit presence-premium signal; (c) a `## Cost basis` line pulling the runway delta for the triggering hub from `notes/relocation_research.md` (W120 visa + pre-TGE token-tax) — used **only to evaluate the trigger**, NOT to budget a move; (d) a `## Verdict` of exactly one of `PREP STAYS PREP (remote default holds)` / `CONVERT TO RELOCATION-PREP for <hub> (move decision still at Bet #1 time, W131)`, with the one-line reason tying to the declared N or the presence-premium. The verdict explicitly states the move itself is **not** decided this week.
  - *Mental model*: §7 is explicit that geography is decided **at Bet #1 time** and the hub *follows the bet* (Dubai = crypto-native/Binance-dense; SG = institutional/Asia) — so this week's job is NOT to decide a move, it is to set whether the W131 decision starts from "stay remote" or "relocation prep already in motion." Declaring N up front converts an emotional post-trip question ("Dubai felt amazing") into a falsifiable count, which is exactly the §9 mitigation "pre-stage, don't pre-commit." Getting this wrong in the over-committing direction = signing a lease for a bet that fails its §6 gate; getting it wrong in the under-committing direction = a presence-contingent tier-1 offer slips because no visa/tax homework was warmed. The verdict is reversible and cheap; the move is neither — keep them separate.
  - Output: `notes/124_flip_trigger_verdict.md` exists with the four sections; N declared numerically; `## Verdict` is exactly one of the two strings; commit present.
- [ ] Artifact: `notes/124_outreach_schedule.md`. Turn Tuesday's ranking into a dated, staggered W125 outreach plan — warm-intro channel where one exists (front-loaded, intros go stale), direct otherwise — staggered so first-conversations don't all collide and the bridge funnel runs in parallel without cannibalizing bet outreach. (~30 min)
  - 🎯 **Hardened acceptance**: `124_outreach_schedule.md` maps every GO-bet + needs-data candidate AND the bridge floor to `{channel: warm-intro [via <name>] or [direct], send-date within W125, expected-first-conversation window}`; warm-intro rows carry the **earliest** send-dates (a stale intro loses referral value — §7); a `## Collision check` line states the max number of expected first-conversations landing in any single calendar week (target ≤ 3, per the controllable-outbound discipline of the M24 gate §5/§9 "M24 outbound response rate low → it's controllable"); and a `## Parallel bridge` line confirming ≥1 crypto-MM bridge conversation is scheduled in parallel (the §7 floor is never paused waiting on a bet).
  - *Mental model*: front-loading warm intros is non-negotiable — the Binance-alumni map's value decays the longer an intro sits (§7, §C M31 "warm-network activation"). Staggering converts the §9-flagged saturation risk ("Destination-tier interview cycle saturates Phase 7") into a paced pipeline. Running the bridge funnel in parallel honors §7's "bridge income funds runway between bets" — you do not stop the floor to chase a ceiling, because a bet that fails §6 leaves you with nothing if the bridge went cold.
  - Output: `notes/124_outreach_schedule.md` exists with a per-candidate channel+date mapping, the `## Collision check` line, and the `## Parallel bridge` line; commit present.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday flip-trigger + schedule commit.

## Thursday — Principal-artifact deep-dive drill: the cross-margin + liquidation engine

- [ ] Artifact: `notes/124_drill_principal_artifact.md`. Rehearse the **principal-defining artifact** (the convergence cell, §3 row 9: `risk-engine` cross-margin + `liquidation-engine` + their assembly into `perp-dex-core`) at two depths — a 5-minute founding-eng-screen version and a 30-minute deep-dive — every design decision and trade-off owned, every number measured. This is the W126 conversation rehearsal. (~90 min)
  - 🎯 **Hardened acceptance**: `notes/124_drill_principal_artifact.md` contains a `## 5-minute` script (≤ ~500 words; hits: *why cross-margin has no spot analog* (§3 row 9 — the "different level vs spot"), the **per-mark-tick incremental-margin hot path** at ~100k accounts / 2 instruments (the §4A capacity decision — why full recompute is reserved for circuit-breaker/param events), the **const 30% netting-benefit haircut** as a deliberate deterministic v1.0 choice, and the **insurance-fund waterfall + tripwire-never-hit** invariant) AND a `## 30-minute` outline (≥ 8 topics, each: the decision + the alternative rejected + why) covering: (1) the convergence cell (jeff-dean-hard ∧ hft-review-hard ∧ domain-hard), (2) `SettlementId = the VSR op-number` (no side mapping table, free idempotency), (3) the two projection staleness contracts (control/risk = read-after-commit from local applied state on the leader shard; analytics = bounded-staleness + op-token), (4) why VSR not Raft/BFT (one hot-path log, CFT), (5) the RWA-aware *seams* present in v1.0 vs RWA *features* at v1.5 (the moat narrative), (6) cluster-VOPR proving linearizability + tripwire-never-hit, (7) the incremental-margin back-of-envelope, (8) where ADL/socialized-loss engages (exactly at the v1.0 tripwire = the clean v1.5 seam); ≥1 number in **each** version is a *measured* figure from Friday's refreshed bench (per-mark-tick p99.99, cluster commit p99) — flagged "to be refreshed Fri so this matches"; and a `## Likely follow-ups` section anticipating ≥4 founding-eng questions ("why a const haircut not rolling correlation?", "what happens at the insurance-fund tripwire?", "how does the risk read avoid a consensus round-trip?", "what breaks at 10× accounts?", "where does an RWA-perp slot in without a rewrite?") each with a one-line answer.
  - *Mental model*: founding-eng / bet conversations probe whether you can *defend* the hardest design under pressure, not describe it — and the hardest design is the convergence cell, the artifact that clears bar (b) (§3, §4A). The 5-min version is the screen ("can he state why cross-margin is the hard part?"); the 30-min version is the deep dive ("does he own the trade-offs — the haircut, the staleness contracts, the tripwire seam?"). Owning the *rejected alternatives* (rolling correlation, a second hot-path log, BFT, full recompute) is the tell that separates a builder from a copier — and it is exactly what Friday's bench refresh feeds: "every number I quote is one I measured this week" is the strongest credibility signal a pre-TGE founder can get from a candidate (§1 bar (b)). Drilling the RWA-seam answer matters disproportionately because it is the three-way-fit edge that justifies a *higher allocation tier* (§4A, §7).
  - Output: `notes/124_drill_principal_artifact.md` exists meeting the criteria; both `## 5-minute` and `## 30-minute` headers present; `## Likely follow-ups` has ≥4 Q/A.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday drill commit.

## Friday — Capstone bench refresh: `perp-dex-core` v1.0 + `risk-engine` v1.0 (numbers current before outreach)

- [ ] Refresh `perp-dex-core` v1.0 capstone benches so every figure quoted in W125 outreach + Thursday's drill is one measured *this week*. Re-run + record into `crates/perp-dex-core/benches/RESULTS.md`. **No scope change, no v1.0→v1.x bump** — this re-certifies the §4A-locked v1.0 numbers, it does not extend scope (RWA features / ADL / multi-collateral are the W118/W119 v1.5 work, NOT here). (~60 min)
  - 🎯 **Hardened acceptance**: `cargo bench -p perp-dex-core` runs green; `crates/perp-dex-core/benches/RESULTS.md` has a dated **W124** block recording the **3-node VSR cluster commit p50/p99**, the **per-mark-tick p99.99 at ~100k accounts / 2 instruments** (the §4A capacity target, via `latency-lab`), and a one-line delta-vs-previous note; `cargo test -p perp-dex-core` green **including the cluster-VOPR StateChecker invariants** (linearizability + replica convergence + **insurance-fund tripwire NEVER hit at the declared 10% accrual rate** — §4A acceptance #7); the version remains `1.0.x` (a `.z` patch at most — the v1.0 lock is unchanged). The numbers in Thursday's drill are updated to match this RESULTS.md (no stale figures cited in W125 outreach).
  - *Mental model*: this is NOT new work — it is the final bench re-certification before active outreach opens W125, so every latency number quoted in W125–W128 conversations is one measured in W124, not a months-old figure a sharp founding-eng could date (§1 bar (b): "≥ 3 supporting artifacts public" — stale artifacts read as abandoned). The cluster-VOPR re-run matters most: the **tripwire-never-hit** invariant is the moat claim (§9 "Insurance fund stubbed away kills the moat") — if a refresh ever surfaced a tripwire hit, that is not a polish failure, it is the precise definition of the v1.5 ADL trigger (§4A acceptance #7) and a scope finding, not a number to paper over. The v1.0 lock (§4A) is sacred this week: refreshing numbers must not become an excuse to creep RWA/ADL scope in early — that is the W118/W119 differentiator window.
  - Output: `crates/perp-dex-core/benches/RESULTS.md` has a W124-dated block (cluster commit p99 + per-mark-tick p99.99 @100k); `cargo bench -p perp-dex-core` + `cargo test -p perp-dex-core` (incl. cluster-VOPR) green; `grep -m1 version crates/perp-dex-core/Cargo.toml` shows `1.0`.
- [ ] Refresh `risk-engine` v1.0 benches — the principal-defining artifact's incremental-margin hot path. Re-run + record into `crates/risk-engine/benches/RESULTS.md`. (~45 min)
  - 🎯 **Hardened acceptance**: `cargo bench -p risk-engine` green; `crates/risk-engine/benches/RESULTS.md` has a dated **W124** block with the current **incremental-margin update cost per position/price change** + the **per-mark-tick portfolio-margin recompute p99.99 across all accounts** at the 100k-account / 2-instrument target + a delta note; `cargo test -p risk-engine` green including the cross-margin netting test (BTC-PERP + ETH-PERP nets risk crediting only **70% of the netting offset** — the const 30% haircut, §4A acceptance #1); version `1.0.x`.
  - *Mental model*: `risk-engine` is the convergence cell (§3 row 9 — jeff-dean-hard ∧ hft-review-hard ∧ domain-hard) and the artifact that *most* moves a founding-eng conversation, so its numbers must be the freshest of all. The incremental-margin figure is the one that proves the §4A capacity decision (full recompute is impossible at 100k accounts × mark cadence — incremental is mandatory, not a fallback); the netting test proves cross-margin is *real*, not single-instrument-hollow (§4A acceptance #1: "Single-instrument cross-margin is hollow and does not pass"). A stale bench on the principal artifact is the worst possible signal on a public repo a founder is evaluating.
  - Output: `crates/risk-engine/benches/RESULTS.md` has a W124-dated block (incremental-margin cost + per-mark-tick p99.99 @100k); `cargo bench -p risk-engine` + `cargo test -p risk-engine` green; `grep -m1 version crates/risk-engine/Cargo.toml` shows `1.0`.
- [ ] Commit + log (bench refreshes only — RESULTS.md blocks + at most `.z` patch bumps). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday refresh commit touching both crates' RESULTS.md.

## Saturday — Tempo/RWA-collateral hedge check + M32 runtime-hours verification

- [ ] Artifact: `notes/124_tempo_rwa_check.md`. Record the option-leg-B status — is the Tempo / RWA-perps-collateral-rails hedge (§2 option leg B; §4A v1.5 "Tempo collateral rails") still live in the background? Any maintainer / design-partner conversations? (~30 min)
  - 🎯 **Hardened acceptance**: `124_tempo_rwa_check.md` opens with `RWA-collateral option active: yes/no — <reason>`; if yes, lists ≥1 maintainer/design-partner conversation with date + current status, AND one line tying it to the v1.5 RWA-features narrative (the §4A "architected for RWA-perps" moat); if no, states the gate criterion that failed so the hedge stays a deliberate choice, not a silently-abandoned one.
  - *Mental model*: the Tempo/RWA leg is a *retained option*, not a critical-path commitment (§2, §10) — it pays off only while its criteria hold and only as the **v1.5 differentiator** (RWA collateral rails on the v1.0 seams). With bet outreach opening W125, recording the gate decision now keeps the parallel option deliberate; and a live design-partner conversation could itself become a §6-scored Bet-candidate (an RWA-perps venue) rather than a separate track — the three-way fit (systems ∧ latency ∧ fintech/RWA) is precisely the edge §4A says is "credible the moment Bet #1 conversations start."
  - Output: `notes/124_tempo_rwa_check.md` opens with the `RWA-collateral option active:` line; commit present.
- [ ] Artifact: `notes/124_m32_runtime_hours.md`. Verify the M32 runtime-hours checkpoint against the M30 2000h gate (§5) and the M36 ≥4000h bar (§1 bar (a)) — confirm accumulated runtime hours are on the back-planned trajectory and the recon trip is logged complete. (~20 min)
  - 🎯 **Hardened acceptance**: `124_m32_runtime_hours.md` records the current accumulated runtime-hour total with the source of the figure (the runtime-hours ledger/log), states whether the trajectory toward the **M36 ≥4000h bar-(a) requirement** is on track (✓/✗ + gap-to-trendline if behind — the M30 gate of 2000h is already past per §5, so M32 should be comfortably above it), confirms `recon trip W123–W124: complete`, and — if behind trend — names the crates whose soak/VOPR runs (e.g. `perp-dex-core` cluster-VOPR, `risk-engine` incremental-margin soak) extend in M33 to recover the gap.
  - *Mental model*: the ≥4000h bar-(a) requirement at M36 (§1) is back-loaded and the 2000h M30 gate is already cleared (§5) — the M32 checkpoint is the early-warning gauge that the *remaining* trajectory holds. Catching a shortfall now (W124) means M33–M36 soak scheduling can recover it; discovering it at M36 means bar (a) can't go green and the coverage matrix (§3) carries a red cell into the bet decision. The cluster-VOPR soak doubles as both runtime-hours and the §4A acceptance-#7 tripwire-never-hit evidence — so extending it is never wasted. This is the milestone gauge, not a vanity metric.
  - Output: `notes/124_m32_runtime_hours.md` records the hour total + ✓/✗ vs the M36-trajectory + `recon trip W123–W124: complete`; commit present.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday Tempo + runtime-hours commit.

## [v3.1 BFT apex — Track B] W124: `DetectEquivocation` (the 4th and final inner seam)

> Track B continues (W123 shipped the 2-chain commit rule). This week builds the **last of the four W91 inner seams** — `EquivocationPolicy`'s real impl. After this, all four BFT seams exist (`BftQuorum`, `SignedPeers`, `PipelinedHotStuffView`, `DetectEquivocation`) and the apex is one type-param swap from the engine. Bar (c). **Next week is the W125 HALT** (the permanent-prune liveness make-pass card — the W91/W118 flagged obligation); W126 happy-path, W127 handle wiring, W128 trait integration follow.

- [ ] **[Tue +90min · Thu +60min]** [Venue · BFT] **Build (bar c)**: `crates/consensus-vsr/src/bft/equivocation.rs` — `DetectEquivocation` impl of the W91 `EquivocationPolicy` seam: record the first `digest` per `(from, view, slot)`; a second **different** digest from the same replica at the same `(view, slot)` → `EquivocationVerdict::Equivocation(Evidence{from, view, slot, d1, d2})`. The `observe(...)` call is **already on the hot path** (W91 R8) — this swaps the inert `AssumeNoEquivocation` for the active detector; no new call site. (~total 150 min)
  - 🔧 **Why this matters**: equivocation (a Byzantine replica sending two conflicting messages for the same slot) is the canonical BFT fault VSR's crash model can't represent (W91 #4). Detection is what makes the W129 scenario #1 (equivocation) and #3 (leader-equivocation / conflicting QCs) provable. The whole reason W91 kept the `observe` call on the hot path under VSR (where it monomorphized to nothing) was so adding detection now is a **seam-impl swap, not a message-path rewrite** — the exact rewrite the seam exists to avoid. The trap: a `HashMap`-keyed first-digest store that allocates on the hot path or iterates non-deterministically (bar-c violation + cross-replica divergence in the evidence itself); use a bounded, deterministic per-`(view,slot)` structure. Evidence trap: storing only "saw two" without `d1`/`d2`/`sig`s — evidence must be **independently verifiable** (a third party checks both signed conflicting messages) for it to support slashing later.
  - *Contract* (bodies for the W91 stub): `fn observe(&mut self, from: ReplicaId, view: u64, slot: OpNumber, digest: Digest) -> EquivocationVerdict { match self.seen.first(from, view, slot) { None => { self.seen.put(from, view, slot, digest); Ok } Some(d) if d == digest => Ok, Some(d) => Equivocation(Evidence{from, view, slot, d1: d, d2: digest}) } }` — `seen` is a bounded per-active-view store (pruned as views finalize, ties to the W125 permanent-prune work), zero-alloc on the steady path.
  - **Tests:**
    - `conflicting_digests_same_slot_yield_evidence` — same `(from, view, slot)`, two different digests → `Equivocation(Evidence)` carrying both `d1`/`d2`. The single-honest-proposal path returns `Ok`.
    - `evidence_is_independently_verifiable` — the `Evidence` carries both **signed** conflicting messages (via `SignedPeers`, W119), so a third party re-verifies both signatures and confirms the equivocation without trusting the detector. (Composes Auth × equivocation; this is what makes evidence slashable in a future staking layer — out of scope per the locked boundary, but the evidence shape supports it.)
    - `observe_is_hot_path_zero_alloc` — dhat over 1M `observe` calls on the steady (no-equivocation) path → 0 allocs (bounded per-view store, no HashMap growth). Re-asserts the W91 back-of-envelope under the real detector.
    - `vsr_observe_still_elided` — the W91 asm test: `AssumeNoEquivocation::observe` stays monomorphized-to-nothing under the VSR `Replica` build; adding `DetectEquivocation` must not regress VSR's zero-cost property (the seam is per-impl).
  - **Make-pass card (SENIOR):** `observe_is_hot_path_zero_alloc` allocates → symptom: dhat shows per-call allocs on the steady path; why: `seen` is a `HashMap<(ReplicaId,u64,OpNumber), Digest>` that rehashes; fix: a bounded per-active-view array/slab keyed by `(replica, slot)` within the small active-view window, pruned as views finalize; cost avoided: per-message allocator pressure on the consensus hot path (bar-c violation); muscle: hot-path detection state is bounded + pruned, never an unbounded map. Reapplies: W125 permanent-prune liveness (the same per-view store is what gets pruned), W129 equivocation scenario.
  - 🧮 **Paper drill** — write in `notes/124_equivocation_drill.md`:
    - D-FAIL on a 4-node BFT cluster (3f+1, f=1) where replica R2 is Byzantine and sends `digest d1` to {R0,R1} and a different `digest d2` to {R3} for the SAME `(view=7, slot=42)`: table each component → failure mode (R2 equivocates) → blast radius (which honest replicas could commit conflicting values absent detection?) → recovery path (evidence captured by `observe`, R2's vote discarded / slashing-eligible) → invariant stressed (quorum-overlap honesty — two `2f+1` quorums share ≥1 honest node who refuses the second digest). Verdict: "survives the equivocation because the honest-quorum-intersection node sees both d1/d2 and yields `Evidence`."
    - D-QUORUM on the same 4-node cluster: draw replicas + their first-seen-digest store; mark the BFT quorum sets (`2f+1 = 3` of `3f+1 = 4`); trace `prepare(d1) → quorum → commit` for the honest line; inject R2's conflicting `d2`; show the NEGATIVE — a hypothetical non-intersecting quorum that would let `d1` and `d2` both commit (split-brain double-commit) and why `2f+1` overlap forbids it. Output: the schedule trace + each replica's commit-point + "no two conflicting commits because the overlap node is honest."
    - D-DET on the `seen` store hot path: trace one `observe(from, view, slot, digest)` call; list EVERY nondeterminism source a naive impl introduces — `HashMap<(ReplicaId,u64,OpNumber),Digest>` iteration order, rehash-triggered alloc, address-dependent probe order — and the deterministic replacement (bounded per-active-view slab keyed `(replica, slot)`, sorted/index-stable, zero-alloc steady path). Prove the verdict is a pure function of `(seen-state, message)` so the captured `Evidence{d1,d2}` is byte-identical across replicas (else the evidence itself diverges and is unverifiable). Output: the nondeterminism ledger + "evidence replays bit-identically because the store is bounded + deterministically ordered."
  - **Output**: `bft/equivocation.rs` with `DetectEquivocation` + verifiable `Evidence`; tests green; VSR zero-cost observe preserved. **All four W91 inner seams now have BFT impls** (`Replica<SM, BftQuorum, SignedPeers, PipelinedHotStuffView, DetectEquivocation>` is the apex replica).
- [ ] **[Sat +15min]** [Venue · BFT] **Milestone note** → `notes/124_bft_seams_complete.md`: all four inner seams shipped (W119 Quorum+Auth, W120–121 ViewManager, W122 QC, W123 commit rule, W124 Equivocation). The apex is now a four-type-param swap from the engine. **🛑 NEXT IS THE W125 HALT**: the `drain_abandoned` **permanent-prune liveness** obligation (the "exactly-one-terminal-event-per-handle → never neither" clause from W91, flagged at W118) must become an **explicit make-pass card** — tracking outstanding `ProposalHandle`s + detecting permanent prune vs temporary post-view-change state. Per the operating mode, W125 authoring HALTS for review before proceeding (it's the one place the BFT impl could surface an interface touch). (~15 min)
- [ ] **[Sat +10min]** Commit + log (~10 min)

---

## Sunday — Rest + End Month 32 review

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.

- M32-close check (against §5 / §C M32): this week converted the W123 in-person trip into (i) a §6-scored Bet-candidate ranking that is the W125 outreach order, (ii) a binding §7 flip-trigger verdict that pre-decides geography for the W131 Bet #1 call, and (iii) refreshed capstone bench numbers so W125 outreach quotes only this-week figures. The flip-trigger verdict — and the invariant that the *move* is still decided at Bet #1 time (W131/W132), never on post-trip enthusiasm — carries forward into M33 active outreach.

---

**Sunday — 🔁 Rebuild Day (~2h, ngày làm việc full — không tổng kết, thuần rebuild)**

> Cold re-implementation từ **file trống** — không xem lời giải cũ / blog / chat. Oracle = test suite của chính artifact (phải xanh). `cargo +nightly miri test` cho item concurrency. Ghi peek-log (kẹt ở đâu) vào `notes/rebuild_<item>.md`; help-surface phải co lại qua các rep.

- [ ] **R3 · seqlock applied-state risk read** *(APEX, rep 3/4, ~2h)* — `crates/perp-dex-core/src/risk_read.rs` *(gốc W111)*
  - oracle: loom suite + Miri (negative-control must-be-slow)

