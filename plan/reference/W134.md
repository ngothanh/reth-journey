# Week 134 — Start at the bet (or bridge): arrival, codebase decoder-ring, fleet continuity across the move

> **Phase**: 7 (Bet Placement / Bridge)
> **Month**: M34
> **Track**: Bet-path execution (founding/core-eng at an on-chain-derivatives venue OR crypto-MM cash bridge) — remote-first; hub only if the §7 flip-trigger fired in W132
> **Hours target**: 40h/wk (W118–W136 window; drops to 30h/wk at W137)
> **Mirror target**: n/a (bet-path week — no upstream module mirrored). The "mirror" this week is *your own workspace*: `perp-dex-core` + `risk-engine` + `liquidation-engine` + `consensus-vsr` are the decoder ring you read the new venue's matching/risk/settlement core through.
> **Crate(s) touched**: none net-new. Maintenance-only on the personal `perp-dex-core` fleet + Reth/Tempo spine nodes (runtime-hours continuity). Any new-venue code is theirs, not a workspace crate.
> **Bar**: (n/a) — bet-arrival / onboarding week, no Build/trap-test arc (SPEC §6 light-week rule). The bar this week is *evidentiary*: every day produces a written, checkable artifact, because "I onboarded" with nothing written is indistinguishable from a wasted week.
> **Feeds into**: W135–W136 first-month ramp (this week's venue-codebase map + ownership graph + first-PR pipeline test are the substrate the first *real* critical-path PR builds on); W137–W138 first-month midpoint at 30h/wk; the §1 bar-(c) ≥4000-runtime-hours commitment continues through M36 and must survive the move uninterrupted; the §7 shots-on-goal ladder (Bet #1 vesting milestones, Bet #2 thesis seeds at W139) all begin accruing from day one here; if this is the **bridge** rather than the **bet**, W139's Bet #2 thesis seeds and continued §6 outbound depend on the runway this seat secures.
> **References** (consult when stuck — not scheduled cover-to-cover): the new venue's onboarding wiki / monorepo README + ARCHITECTURE doc; your own `notes/` index from W1–W133 (the searchable two-year proof-of-work — *use it, don't re-derive*); `README.md` §4A (the v1.0 lock — your decoder ring for their matching/risk/settlement core), §6 (ecosystem checklist — the GO conditions you verified at W131, now re-confirmed on the inside), §7 (geography flip-trigger + bridge-vs-bet shots-on-goal); `INDEX.md` Phase-7 arc (W118–W144) for context.
> **v3 modifications**: **Entirely re-aimed from v2.** v2's W134 was "Arrival at destination + onboarding" for a **Tier-A HFT IC job** (the v2 Path-E terminal state). v3 makes this **"Start at the bet (or bridge)"** — arrival at a **founding/core-engineer seat on an on-chain-derivatives venue** (token-equity Bet #1, the v3 north star) *or* the **crypto-MM cash bridge** (the income floor, §1(d)/§7) if W131 did not produce a GO-bet. The codebase-map task is re-aimed from "generic new firm" to "their **matching / cross-margin / liquidation / settlement core**, read through your `perp-dex-core` decoder ring." Relocation is **conditional** (remote-first default; hub only if the §7 flip-trigger fired at W132) — *no pre-committed move*. Fleet-continuity is re-pointed at the **`perp-dex-core` cluster + Reth/Tempo nodes** (not a generic Reth/Tempo fleet). The bridge-vs-bet branch is made explicit every day.

> **Week intent**: This is a *landing* week, not a build week. The deliverable is not code — it is (a) a clean physical/legal/remote-setup arrival (conditional relocation only if triggered), (b) a written **decoder-ring map** of the new venue's core good enough that W135's first real PR doesn't start from zero, and (c) an **unbroken runtime-hours streak** on your own `perp-dex-core` + Reth/Tempo fleet across the move. The whole point of two years of from-scratch builds (§3 "no primitive twice") is that you arrive at a real venue already holding the mental model *and the failure modes* of their matching engine, cross-margin recompute, liquidation waterfall, and VSR-replicated log — you read their code as "known patterns, new names," not "new codebase, zero context." Every item below produces a concrete artifact and an Output line you can verify. **Branch note (read first):** if W131 placed **Bet #1** (a §6-passing founding/core-eng seat), the "venue" tasks are literal. If W131 engaged the **bridge** (crypto-MM cash seat, §1(d)/§7), substitute "crypto-MM desk's execution/strategy infra" for "the venue's matching/risk core" everywhere — the *structure* of every task (ownership graph, codebase decoder-ring, pipeline-test first PR, fleet continuity) is identical, and the bridge seat keeps Bet #1 outbound alive (W139+).

---

## Monday — Arrival + `perp-dex-core` fleet continuity (the expensive-to-fake evidence)

- [ ] `notes/134_arrival_checklist.md` — confirm physical / legal / remote-setup arrival is complete (conditional relocation only). (~30 min)
  - 🎯 **Hardened acceptance**: the file lists, each with ✓/✗ + a date: {**relocation status** — `REMOTE (no move)` OR `RELOCATED to <hub>` per the W132 §7 flip-trigger decision; if relocated: entered destination geography, short-term housing keys in hand, address/visa registration done; if remote: home-office + connectivity + redundant-power confirmed}, {pre-TGE token-tax treatment re-confirmed for the seat's jurisdiction (the §7 prep you did at W120, now live)}, {SIM/connectivity working}, {first-week working hours agreed with the venue across timezones}. Any ✗ carries a 1-line next-action with an owner + date — an open visa/tax item with no plan is a hidden blocker, not a note.
  - *Mental model*: the v3 geography call is **pre-stage, don't pre-commit** (§7) — so this checklist's *first* line is the relocation branch itself, and most weeks it will read `REMOTE (no move)`. The token-tax line is load-bearing in a way it never was in v2's salaried HFT job: a founding **token-equity** grant has tax consequences at vest/cliff that a relocation can change, and getting it wrong is a six-figure error on the single asset the whole plan back-plans to (§1(d), §6.5). Writing the checklist converts ambient relocation/tax anxiety into a finite, checkable list.
  - Output: `notes/134_arrival_checklist.md` exists with the relocation branch tagged, all line-items ✓/✗ + date; every ✗ has a next-action line; the token-tax line cites the W120 prep note.
- [ ] `notes/134_fleet_continuity.md` — verify the personal `perp-dex-core` cluster + Reth/Tempo nodes survived the move; runtime-hours streak unbroken. (~40 min)
  - 🎯 **Hardened acceptance**: record current cumulative runtime hours vs the W117/W132 baseline (≥2000h at M30; on the ≥4000h-by-M36 trajectory per §1(a)); confirm the **3-node `perp-dex-core` VSR cluster** stayed up across the relocation window (no view-change storm, no committed-op loss, insurance-fund-tripwire-still-never-hit invariant intact) AND the Reth/Tempo spine nodes stayed synced — OR document the gap (start/end timestamps, hours lost, which node, recovery action) and the catch-up plan. A silent streak break is a bar-(c) credibility problem at the *next* §6 conversation, not a footnote.
  - *Mental model*: the ≥4000-runtime-hours commitment (§1(a)) is a continuous obligation that does not pause for a physical/remote-setup move — and in v3 the fleet is not a generic Reth/Tempo fleet, it is **your capstone running live**: a multi-node VSR cluster carrying a paper-trade venue. That cluster *is* the artifact that clears bar (b) (§1(b)); a relocation is precisely when a long-running consensus cluster silently breaks (cloud-billing lapse → node evicted → 2-of-3 quorum → a stalled view-change you don't notice for days; or a home power-cut at the leader). Checking the **VSR commit-num is still advancing across all 3 replicas** on day one is cheap insurance on the most expensive-to-fake evidence you carry into Bet #1.
  - Output: `notes/134_fleet_continuity.md` records hours-now vs baseline + per-node up/down status + VSR cluster health (commit-num advancing, no divergence) + insurance-fund tripwire status; any gap has a timestamped catch-up plan.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Monday arrival + fleet-continuity commit.

---

## Tuesday — Day 1 at the bet/bridge: the ownership graph (the thing you can't reconstruct later)

- [ ] `notes/134_onboarding_day1.md` — access provisioning, founder/lead + team intros; capture the human + access + **cap-table-reality** map. (~40 min)
  - 🎯 **Hardened acceptance**: the file lists the founder(s)/eng-lead + each immediate teammate with {name, role, **what core component they own** — matching? risk? settlement? oracle? the chain integration?}; lists access granted so far {monorepo read, monorepo write, internal wiki, ticket system, comms, **testnet/devnet keys**} each tagged ✓/pending; records the one technical question you most want answered by end of week; AND — bet-specific — confirms in one line that the §6 GO conditions you verified *externally* at W131 still hold *internally* (founding-tier allocation confirmed in the actual grant doc; tier-1 backing visible on the real cap table; you own a **core** component, not a peripheral integration). A name with no "what they own" is just a name — capture the ownership map, it is the thing you cannot reconstruct later.
  - *Mental model*: the highest-value artifact of week one is not code, it is the **org + criticality map** — and for a *bet* (not a job) the §6 checklist (§6.1 founding-tier allocation, §6.2 tier-1 backing, §6.3 core-criticality) must survive contact with the inside. A 12-year engineer knows the codebase is learnable but the social/ownership graph silently gates everything and is written nowhere; for a founding seat the *additional* gate is "am I actually building the engine that **is** the company" (§6.3) — if day-one onboarding reveals you've been slotted onto a peripheral integration, that is the single most important signal of the week and it changes the W139 Bet #2 calculus immediately. Capturing the ownership map now is what lets W135's first real PR find the right reviewer instead of stalling.
  - *(Bridge branch)*: substitute "the desk's execution/strategy infra owners" for "the venue's core owners"; the §6 lines become "this is a *bridge*, not a bet — confirm runway terms (≥6–12mo, §1(d)) and that the seat does **not** block Bet #1 outbound (no exclusivity/non-compete that strands the §7 ladder)."
  - Output: `notes/134_onboarding_day1.md` lists founder/lead + ≥2 teammates with owned-component, an access checklist incl. testnet keys, 1 open technical question, and the §6-still-holds (or bridge-runway) confirmation line.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday onboarding commit.

---

## Wednesday — Day 2: the decoder-ring codebase map (their core ↔ your crates)

- [ ] `notes/134_codebase_map.md` — first read-only access; build the top-level map of the venue's core, **read through your `perp-dex-core` decoder ring**. (~60 min)
  - 🎯 **Hardened acceptance**: the map names the top-level services/modules of their core, the build + test entrypoint command (the one you'd actually run), the CI gate, and — critically — maps **each of their core components to the workspace crate you built that holds its mental model and failure modes**:
    - their **matching / order book** ↔ your `matching-engine` v1.0 (book + STP + mark-keyed triggers; you know the HashMap-iteration-divergence and apply-path-alloc traps cold);
    - their **cross/portfolio margin** ↔ your `risk-engine` v1.0 (per-mark-tick incremental margin at 100k accounts; you know the full-recompute-blows-the-budget cliff and the netting-haircut determinism trap — §4A);
    - their **liquidation + insurance fund** ↔ your `liquidation-engine` v1.0 (partial-liq + fee-accrued fund + waterfall + the tripwire-never-hit invariant);
    - their **mark / index / funding** ↔ your `oracle-mark` (mark-vs-index-vs-last; RWA-aware seam);
    - their **replicated log / consensus / settlement** ↔ your `consensus-vsr` + `ledger-deterministic` (is their order/event log the *same* log as settlement, the §4A "one hot-path log" pattern — or did they build a second hot-path log? **this is the single most important architectural question to answer this week**);
    - their **off-path fan-out / market-data** ↔ your `log-distributed` + `messaging-aeron` (is analytics a *projection* of the committed log with a bounded-staleness contract, or a second source of truth?).
    Every claim is from reading, not assumption; an unverified guess is tagged `[UNVERIFIED]` for Thursday's pairing to confirm.
  - *Mental model*: this is the entire payoff of the "no primitive twice" curriculum (§3). Your two years of from-scratch builds are a **decoder ring** — when their codebase has a per-mark-tick margin recompute, a liquidation waterfall, a VSR-replicated op-log, or a Kafka fan-out, you already hold the mental model *and the specific failure modes* (you wrote the trap tests yourself: the cross-replica state-hash divergence, the f64-cross-arch drift, the per-cmd-Prepare throughput cliff). Explicitly mapping their components to yours converts "new venue, zero context" into "known patterns, new names" — and immediately surfaces the highest-leverage finding a founding engineer can have in week one: **does their architecture match the §4A v1.0 lock you spent M25–M27 proving, or did they take a shortcut you know the failure mode of** (e.g. a second hot-path log, or rolling-correlation cross-margin injecting non-determinism into the replicated core)? That observation is your first credible contribution.
  - *Hardened acceptance, build proof*: you can state the exact command that builds + tests their core (verified by reading their docs/CI, not necessarily run yet if write access is pending).
  - Output: `notes/134_codebase_map.md` names top-level services + build/test command + CI gate + **all six component→crate mappings** (each with the workspace crate name) + the one explicit architectural question about their hot-path-log discipline, tagged `[UNVERIFIED]` for Thursday.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday decoder-ring commit.

---

## Thursday — Day 3: pair on a read-only walk through the matching/risk core (extract tribal knowledge)

- [ ] `notes/134_pairing_notes.md` — pair with the matching/risk/settlement owner on a read-only walkthrough; resolve Wednesday's `[UNVERIFIED]` flags; capture the tribal knowledge. (~75 min)
  - 🎯 **Hardened acceptance**: the file (a) **resolves the Wednesday hot-path-log question** with a verified yes/no — *is their order/event log the same log as settlement (§4A one-log lock), or are there two hot-path logs?* — and notes the consequence either way; and (b) records ≥3 pieces of tribal knowledge that are NOT in the docs (a determinism gotcha in their apply path, an undocumented build/devnet step, a "we always do X because Y" about their margin or liquidation cadence, a flaky-VOPR/sim workaround); each tagged with who told you and whether it should be written into the venue wiki. Pairing that produces zero undocumented insights means you weren't asking the questions only a newcomer who has *built this exact thing* can ask.
  - *Mental model*: pairing in week one is the cheapest possible time to extract tribal knowledge — you have a license to ask "dumb" questions you'll never get again — and your version of "dumb" questions is unusually sharp because you *built* the reference implementation: "does your cross-margin recompute incrementally or full-scan per mark tick — what's your account-count budget?" (you know the §4A back-of-envelope: 100k accounts × 10 ticks/s = 1M recomputes/s, full-scan blows it); "is the insurance-fund accrual a declared param or randomized in-sim?" (you know randomizing it asks the wrong question, §4A). The newcomer-who-has-built-it view of what's undocumented is genuinely valuable, and proposing those wiki edits is a legitimate, low-risk first contribution. The notes also seed W135's first independent PR: a task you've now seen end-to-end on the *actual* critical path you were brought in to own (§6.3).
  - Output: `notes/134_pairing_notes.md` resolves the one-log question with a verdict + consequence, lists ≥3 undocumented insights each with source person + "wiki-worthy? y/n", and names the candidate task for W135's first real PR.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday pairing commit.

---

## Friday — Day 4: first small PR (the pipeline test, near-zero stakes by design)

- [ ] First small PR against the venue's monorepo — scoped trivially small, by design. (~180 min)
  - 🎯 **Hardened acceptance**: the PR is genuinely small (a docs/ARCHITECTURE fix, a test addition, a tiny well-isolated change — ideally one that *uses* a finding from Wednesday/Thursday, e.g. documenting their hot-path-log discipline or adding a determinism-guard test you know belongs there), passes the venue's CI gate (the one you recorded Wednesday), and is opened against their actual review process — not merged-to-main cowboy-style. Record the PR link, the reviewer (the right one, from Tuesday's ownership graph), and the CI result. A first PR's job is to exercise the full local-dev → CI → review → land pipeline **once** while the stakes are near zero, NOT to ship something impressive.
  - *Mental model*: the first PR is a **pipeline test**, not a feature — its entire purpose is to discover where your dev loop breaks (devnet setup, missing access, CI quirks, their VOPR/sim harness, review etiquette, monorepo build times) on a change small enough that nobody cares if it takes three rounds. This is the same discipline as a **negative-control test** (the muscle from W4 onward): prove the pipeline works on a trivial input before trusting it on a real one. Doing this deliberately on Friday means W135's first *real* critical-path PR — the one that starts to justify the founding-tier allocation (§6.1, §6.3) — is not *also* fighting toolchain friction. Pick a PR that lands a finding from earlier in the week so even the pipeline test produces signal of competence to the team.
  - *(Bridge branch)*: identical structure against the desk's repo; the PR is still a pipeline test, and it still doubles as a quiet competence signal — useful even on a bridge seat where you may move on at the next Bet #1 GO.
  - Output: `notes/134_first_pr.md` records the PR URL + reviewer + CI status (green); the PR is open/approved in the venue's system; one line on what the pipeline test *revealed* (the dev-loop friction you now know to route around in W135).
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Friday first-PR commit.

---

## Saturday — Recover from the move + week-1 retro + Bet #2 seed (5-minute capture, then rest)

- [ ] Recover from the move; deliberate rest, no venue work — except a 5-minute retro that captures the week's signal. (~40 min)
  - 🎯 **Hardened acceptance**: jot a short week-1 retro in `notes/134_week1_retro.md` — {**biggest surprise** about the venue's core vs your §4A mental model; **biggest open risk** for week 2 (technical or §6-criticality); **one thing to ask the founder/manager Monday**; **§6-still-GO?** one line confirming the bet still passes the checklist from the inside, OR — bridge branch — that runway + non-exclusivity hold; **Bet #2 seed** — one sentence on what this seat teaches you about the *next* shot in the §7 ladder (each bet upgrades the next)} — then close the laptop. The retro is the only "work"; everything else this day is recovery by design.
  - *Mental model*: a relocation/remote-setup + a founding-seat onboarding is a high-cortisol week, and the single most common failure mode is burning the first weekend trying to "get ahead," which trades sustainable pace for one week of marginal progress and a worse week two — exactly the wrong trade when you're 40h/wk through W136 and have a ≥4000-hour continuous obligation to protect. The 3-to-5-line retro captures the week's signal (including the load-bearing §6-still-GO check and the first Bet #2 seed, §7) in five minutes so rest doesn't lose information; then rest is the productive act. The §6 line matters more here than in any v2 job week: a *bet* can fail the checklist from the inside, and if it does, the W139 Bet #2 thesis starts forming *now*, not at offer time.
  - Output: `notes/134_week1_retro.md` exists with the retro lines incl. the §6-still-GO (or bridge-runway) confirmation and the first Bet #2 seed; no venue-repo commits dated Saturday.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday retro commit.

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
