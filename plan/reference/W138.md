# Week 138 — First month at the bet/bridge: midpoint check

> **Phase**: 7
> **Month**: M35
> **Track**: (career — bet placement / bridge; first month at the bet OR crypto-MM bridge)
> **Hours target**: 30h/wk (M35–M36, W137–W144)
> **Mirror target**: (n/a — career-integration week; no upstream module is read cover-to-cover)
> **Crate(s) touched**: (none — maintenance only on `perp-dex-core` / Reth-spine / Tempo; the HFT critical-path crates keep accruing runtime hours passively. No new code this week.)
> **Bar**: (n/a) — career/ops week, no Build exercise (per SPEC §6: depth ≠ padding; do not manufacture a fake trap-test arc where none is warranted).
> **Feeds into**: W139 settling + **Bet #2 thesis seeds** (the midpoint snapshot written here is the first calibration data point Bet #2 sequencing reads — §7 "each bet upgrades the next"); W140–W143 settling + permanent housing (the 30/60/90 read here is the contract those weeks execute against, and a calibrated relationship is the precondition for committing to a lease/hub); W141 public artifact update (`perp-dex-core` + the bet); W144 M36 retrospective + plan close (this week's midpoint snapshot is the first of two data points the retro plots, and the runtime/coverage readings feed the close metrics: 4000h-by-M36, 8-system coverage ≥80).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): your own W135 `notes/30_60_90.md` (the success definition written in week 1 at the bet/bridge); `README.md` §1 (the four readiness bars — this midpoint reads against bar (a) coverage + bar (d) runway), §5 M36 ("Bet #1 placed OR bridge engaged"), §6 (ecosystem-selection checklist — the lens that decides whether this seat *stayed* a bet or became a bridge), §7 (shots-on-goal: each bet upgrades the next; never quit a bridge before vest milestones); your W137 `onboarding/team_map.md` (the relationship baseline this week calibrates against); your W120 `outreach/` logs (the expectations baseline).
> **v3 modifications**: the v2 file framed this as the midpoint of "first month at a new (HFT IC) firm," scored against README §"Milestones" and a Tier-A-HFT runtime screen. **v3 re-aims it to the bet path:** the seat is a **founding/core-eng bet OR a crypto-MM cash bridge** (§5 M36 outcome), not an HFT IC job; the midpoint check therefore scores *two* extra dimensions the v2 file lacked — (1) **token/equity vesting reality** (is the §6 allocation actually materializing as promised, or did the seat quietly drift from bet → bridge?) and (2) **core-criticality** (am I building the engine that *is* the company, or did I get slotted onto a peripheral integration?) — because at a bet those are the things that decide whether you stay, not raw output. The Saturday maintenance item is re-pointed from "Reth PR review (warm-network)" to "**`perp-dex-core` flagship-artifact health + one Reth-spine review**" (the capstone is now the portfolio centerpiece per §1 bar (b); Reth is the *bridge fallback*, not the headline). The runtime-milestone reading is reframed from "HFT-shop screening receipt" to "bar (a) coverage evidence + the latency-craft underwrite of the crypto-MM bridge (§2)." A new Wednesday/Friday thread seeds **Bet #2 signal capture** (§7).

This is a midpoint-check week, not a build week. Six weeks into the bet/bridge, the only deliverable is an honest, written self-assessment against the 30/60/90 expectations set in W135 — surfaced to your manager (or, at a founding seat, your co-founder/lead) before the gap compounds. **The v3 twist:** at a *bet*, the 30/60/90 is not just "am I performing" — it is "is this still a bet?" Two extra rows (vesting reality, core-criticality) test whether the §6 checklist that said GO at Bet #1 time is *still* true now that you're inside. A seat that drifts from "you'll build the matching/risk core" to "please wire up this dashboard integration" has silently become a bridge — and you want to catch that at week 6, while you still have leverage, not at the one-year cliff. Every item below names the file it writes and the command/observation that proves it done. The HFT runtime fleet + `perp-dex-core` artifact keep running in the background toward the M36 bars — verify the numbers, don't babysit them. Days are deliberately light (~3–4h/day): a midpoint read is wall-clock-paced reflection plus a handful of conversations, not effort-paced output; over-scheduling it is itself a misread of what week six is for.

---

## Monday — Self-assessment against 30/60/90 (+ the two bet-specific rows)

- [ ] **Artifact**: `notes/138_midpoint_check.md` — score yourself against each line of the W135 `notes/30_60_90.md` plan, RAG-tagged (🟢 on track / 🟡 slipping / 🔴 off), then **add the two v3 bet-specific rows** that the W135 performance plan did not cover. (~40 min)
  - 🎯 **Hardened acceptance**: (a) every line of `notes/30_60_90.md` (from W135) has a matching 🟢/🟡/🔴 row in `notes/138_midpoint_check.md` with **one sentence of evidence each** (a shipped PR, a meeting outcome, a metric — never a vibe); AND (b) two extra rows exist and are RAG-tagged with evidence:
    - `## vesting reality` — is the token/equity grant from the §6 checklist (founding-tier allocation, sane cliff/vest, pre-TGE entry) **actually materializing as documented**? Evidence = the signed grant doc / vesting schedule / cap-table line, not a verbal promise. 🔴 if the paper hasn't appeared six weeks in.
    - `## core-criticality` — am I building the engine that **is** the company (matching / risk / settlement core, per §6 item 3), or did my actual first-month work drift onto a peripheral integration? Evidence = the list of what you actually shipped/own vs. what the §6 GO-decision assumed you'd own.
  - *Mental model*: the 30/60/90 plan is worthless if it's only consulted at day 90 — by then a 🔴 is a hard conversation, not a course-correction. The week-6 read is the last cheap moment to catch a slip while it's still "let me re-prioritize" rather than "we need to talk." **The two extra rows are the v3 difference between a job and a bet:** at an HFT IC job, output is the only axis; at a founding/core-eng bet, the axes that actually determine your $20–100M outcome are *did the allocation real-ize* (bar (d) runway + §6 item 1/5) and *am I core* (§6 item 3) — and both can quietly degrade without anyone telling you. A bet that has become a bridge is fine **if you know it** (you re-engage outreach per §7) and catastrophic if you don't (you stay a year on a worthless grant). Week 6 is when that's still cheap to fix.
  - **Output**: `notes/138_midpoint_check.md` exists with one RAG-tagged + evidenced row per W135 expectation, **plus** the `## vesting reality` and `## core-criticality` rows; `grep -c '🔴\|🟡\|🟢' notes/138_midpoint_check.md` ≥ (line count of the W135 plan) + 2.
- [ ] Commit notes. (~10 min)
  - **Output**: `git log -1 --oneline` shows the Monday self-assessment commit.

---

## Tuesday — Manager / lead 1:1

- [ ] **1:1 with your manager** (or, at a founding seat, your co-founder/lead): bring the midpoint check, lead with the 🟡/🔴 rows and your proposed correction for each. (~40 min)
  - 🎯 **Hardened acceptance**: every 🔴 and every 🟡 from Monday's check is either (a) raised with a concrete proposed fix, or (b) explicitly logged as "deferred, with the lead's agreement and reason" — none silently dropped. **The `core-criticality` row is raised explicitly** ("here's what I understood I'd own at GO; here's what I've actually been on — can we re-align?") — this is the single highest-leverage sentence of the week. The 1:1 produces ≥1 written takeaway per non-🟢 row (a re-scoped expectation, a new owner, an explicit "you're on track", or a re-aligned scope toward the core).
  - *Mental model*: you raise the bad news, framed with a fix, before your lead discovers it — that converts the conversation from "explaining a gap" to "demonstrating ownership," the signal that survives any review and, at a founding team, the signal that gets you *more* core scope rather than less. A senior engineer six weeks in is judged less on output volume than on calibration: do you know where you stand without being told? **At a bet, surfacing the core-criticality question is not insubordinate — it is exactly the founder-mindset the §6 GO-decision was betting on.** A founder *wants* the core engineer to fight to stay on the core; silence reads as someone content to be peripheral.
  - **Output**: `notes/138_midpoint_check.md` gains a `## 1:1 takeaways` section with ≥1 dated, written outcome per non-🟢 row, and an explicit dated note on the core-criticality re-alignment (or confirmation you're already on the core).
- [ ] Commit notes. (~10 min)
  - **Output**: `git log -1 --oneline` shows the Tuesday 1:1-takeaways commit.

---

## Wednesday — Act on one correction + open the Bet #2 signal file

- [ ] **Execute the single highest-priority correction** from Tuesday's 1:1 (re-scope toward the core, reach out to an owner, or pick up the work that closes the top 🟡/🔴). (~40 min)
  - 🎯 **Hardened acceptance**: the top-priority correction has a concrete first action taken **today** (a message sent, a task picked up, a doc started) — not just re-noted; the action is linked in the midpoint file. If the top item was `core-criticality`, the first action is a concrete move toward the core (e.g. you picked up a matching/risk/settlement-adjacent task, or scheduled the scope conversation), not a note that you'll "raise it later."
  - *Mental model*: a 1:1 takeaway that isn't actioned within 48h decays into "we talked about it" with nothing behind it. Closing the loop on the single most important item the same week is what makes the next 1:1 a progress report instead of a repeat — and at a founding seat, fast loop-closure *is* the reputation that raises your allocation tier at the next grant refresh.
  - **Output**: `notes/138_midpoint_check.md` `## 1:1 takeaways` top row gains a dated `→ action:` line with the artifact link.
- [ ] **[Bet #2 seed]** Open `notes/138_bet2_signal.md` and start the standing signal file that W139's Bet #2 thesis-seeding will read. Capture, in one or two lines: what this first month has taught you about *what a good on-chain-derivatives bet looks like from the inside* (the thing you could not see as an outsider doing §6 on paper). (~20 min)
  - 🎯 **Hardened acceptance**: `notes/138_bet2_signal.md` exists with ≥2 concrete inside-view observations, each tagged as either `[upgrades-§6-checklist]` (a refinement to the ecosystem-selection filter you'd apply to Bet #2) or `[network]` (a person/team/venue you now know is worth tracking for the next shot). No generic platitudes — each line names a specific mechanism, person, or venue.
  - *Mental model*: §7's "each bet upgrades the next" is not automatic — it only happens if you *capture* what Bet #1 taught you while it's fresh. The single most valuable thing a first month inside a real venue gives you is the correction to your *outside* §6 model: which checklist items actually predicted reality and which were noise. Start logging it now, six weeks in, while the outsider/insider delta is still legible; by month 12 you'll have normalized to the inside view and the delta — the actual learning — will be invisible.
- [ ] Commit notes. (~10 min)
  - **Output**: `git log -1 --oneline` shows the Wednesday correction + Bet #2 seed commit.

---

## Thursday — Work + capture week-6 signal

- [ ] **Day-job work** at the bet/bridge; capture any new 30/60/90-relevant signal (praise, friction, a scope shift, a vesting/grant update) into the midpoint file as it happens. (~15 min capture, rest is the actual work)
  - *Mental model*: the midpoint file is now the living record the W144 retro will read — small daily signal captured in-the-moment beats a reconstructed-from-memory summary at day 90. A scope shift noticed Thursday and logged is a data point; the same shift reconstructed at retro is a guess.
  - **Output**: `notes/138_midpoint_check.md` gains a dated `## week-6 signal` bullet (or an explicit "no new signal today" line — silence recorded is still data).
- [ ] Commit notes. (~10 min)
  - **Output**: `git log -1 --oneline` shows the Thursday signal commit.

---

## Friday — Work + the M35 metric verifications (runtime + coverage)

- [ ] **Verify the M35 runtime milestone**: confirm the HFT critical-path fleet has crossed **3800+ cumulative runtime hours** (the M35 checkpoint on the bar (c) "4000+ runtime hours by M36" ramp) and record the reading. (~20 min)
  - 🎯 **Hardened acceptance**: the reading is pulled from the actual runtime ledger/dashboard (not estimated), is ≥3800h, and is logged with the date and source; if <3800h, log the shortfall **+ the gap-to-4000h-by-M36 (W144)** and what catches it up in the remaining six weeks.
  - *Mental model*: the 4000h-by-M36 bar is a credibility receipt with **two v3 readers**: (i) bar (a) core-skill mastery (`README.md` §1 — "≥ 4000 runtime hours" is a literal cell), and (ii) the **crypto-MM cash bridge** (§2), which is hireable specifically on Half-2's latency craft — "this ran in anger for thousands of hours" is the underwrite. W138 is the M35 checkpoint on that ramp; catching a shortfall now leaves six weeks (W139–W144) to recover, vs. discovering it at the W144 plan close with no runway left.
  - **Output**: `notes/138_midpoint_check.md` gains a `## runtime milestone` line: `<hours>h as of <date>, source: <ledger/dashboard>`; if ≥3800h, milestone met; else gap + recovery note recorded.
- [ ] **[Bet #2 seed]** Verify the **8-system coverage** reading (the W144 close metric is ≥80). Pull the current number from your coverage matrix tracking; this is the second M36-close metric and it tells you whether any reference-system cell quietly went stale during the first month of (lower-hours) bet/bridge work. (~15 min)
  - 🎯 **Hardened acceptance**: a current 8-system coverage number is logged with the date and the source artifact; if any single system dropped below its W117-gate level (≥65 at M30), it's flagged with a one-line "stale because…" note. No system left unscored.
  - *Mental model*: dropping from 40h/wk (through W136) to 30h/wk (W137+) is exactly when coverage silently erodes — you stop touching the crates you're not paid to touch. The §1 bar (a) "no red cells" requirement and the W144 ≥80 close don't pause for the day-job ramp. Reading it now, at week 6 of the lower-hours regime, is the cheap catch.
  - **Output**: `notes/138_midpoint_check.md` gains a `## coverage` line: `8-system coverage = <n> as of <date>, source: <matrix artifact>`; any stale cell flagged.
- [ ] Commit notes. (~10 min)
  - **Output**: `git log -1 --oneline` shows the Friday metrics-verification commit.

---

## Saturday — Light maintenance (≤4 hrs/wk): `perp-dex-core` artifact + one Reth-spine review

- [ ] **[`perp-dex-core`] flagship-artifact health check** — the capstone is the portfolio centerpiece (§1 bar (b)); confirm the public artifact still builds, the cluster-VOPR suite still passes, and the repo/README still reads as the founding-engineer proof a bet conversation lands on. (~120 min)
  - 🎯 **Hardened acceptance**: `cargo test -p perp-dex-core` (and the cluster-VOPR smoke target) run green on a clean checkout; the public repo's top-level README still leads with the v1.0 acceptance story (3-node VSR / multi-instrument cross-margin / partial-liq + insurance-fund waterfall / `SettlementId`=op-number / two staleness contracts / 100k-account incremental-margin scale). Log the green run + a one-line "still bet-ready" or a list of any rot to fix. **This is the artifact W141's public update edits — keep it from bit-rotting under the day-job.**
  - *Mental model*: six weeks into a bet/bridge, personal OSS time has collapsed to ≤4 hrs/wk — that's the new normal, so the maintenance bar shifts from "ship features" to "keep the bet-readiness proof alive and credible." `perp-dex-core` is the artifact that *got you the seat* and is the artifact Bet #2 will be evaluated on (§7); a flagship that silently stops compiling is a self-inflicted wound on the next shot. v1.5 features (RWA/ADL/multi-collateral, W118–W119) are the tier-raiser, but v1.0 has to *stay shipped* first.
  - **Output**: `notes/138_midpoint_check.md` gains a `## perp-dex-core health` line: green-run confirmation + "bet-ready" or the rot-list.
- [ ] **[Reth-spine] one PR review** on the `storage-trie` / `exec-vm` area you owned in Phase 1–4. (~60 min)
  - 🎯 **Hardened acceptance**: the review leaves ≥1 substantive comment (a correctness/perf observation, not LGTM) and the PR link + your comment are logged.
  - *Mental model*: the Reth spine is the **bridge fallback** in v3 (§2, §7 — crypto-infra IC if the crypto-MM seat isn't taken), not the headline. One substantive review/week keeps your name in the Reth contributor stream — the backup-bridge channel — at a fraction of the time cost of authoring. It is a hedge, kept warm, not a parallel career bet.
  - **Output**: `notes/138_midpoint_check.md` gains a `## reth maintenance` line with the reviewed PR URL + a one-line comment summary.
- [ ] **[Tempo]** light skim if still in maintenance mode (conditional — skip if the RWA-collateral option leg is closed). (~30 min)
  - 🎯 **Hardened acceptance**: a one-line conditional decision is recorded — either "RWA-collateral leg active: skimmed `<area>`, status `<note>`" or "leg closed: skipped, reason `<reason>`". No silent skip.
  - *Mental model*: Tempo maintenance is the **RWA-perps collateral-rails option** (§2 option leg B), a hedge — not an obligation. Recording the active/closed decision keeps the hedge honest and stops it from silently consuming the scarce ≤4h budget when it's no longer paying off. (If `perp-dex-core` v1.5 RWA work is imminent per W118, the Tempo leg may be worth keeping warm — note which.)
  - **Output**: `notes/138_midpoint_check.md` gains a `## tempo` line with the conditional decision.
- [ ] Commit + log. (~10 min)
  - **Output**: `git log -1 --oneline` shows the Saturday maintenance commit; `progress.md` has the W138 entry (midpoint snapshot summary: RAG counts, vesting/core-criticality verdict, runtime + coverage readings).

---

## [v3.1 BFT apex — Track B] W138: re-run the full venue cluster-VOPR against `perp-dex-core` v2 (whole venue under BFT)

> Track B continues (W137 swapped perp-dex-core onto BFT). W129 proved the *consensus* under Byzantine faults in isolation; W138 proves the *whole venue* (matching + risk + liquidation + ledger + BFT consensus) under the same fault set — the W108 cluster-VOPR, re-run against v2. Bar (c). This is the v2 analogue of the W117 acceptance gate's Half-A.
- [ ] [Venue · BFT] **Build (bar c)**: re-run the W108 cluster-VOPR StateChecker (I1 linearizability, I2 convergence, I3 insurance-fund-tripwire-never-hit, I4 finality-from-commit-point, I5 projection-contracts) **plus** the W129 8 Byzantine scenarios, against `perp-dex-core` **v2** (BFT backend). Assert every invariant that held under VSR still holds under BFT — the venue's correctness is consensus-impl-independent. (~total 165 min)
  - 🔧 **Why this matters**: the swap (W137) proved the *code* is unchanged; W138 proves the *behavior* is correct end-to-end under adversarial faults — that the insurance-fund waterfall, the cross-margin netting, the two staleness contracts, and `SettlementId` finality all survive Byzantine consensus, not just crash-fault VSR. The trap: assuming "consensus is correct (W129) + engine is unchanged (W137) ⇒ venue is correct" — composition can still surface bugs (e.g. a Byzantine-induced view change interacting with a liquidation cascade in a way neither tested alone); the whole-venue VOPR is the catch-all (the W103 Saturday "composition surfaces bugs the unit tests missed" muscle, at venue scale).
  - **Tests:**
    - `v2_cluster_vopr_all_invariants_green` — I1–I5 + the 8 Byzantine scenarios, StateChecker-green against perp-dex-core v2; the insurance-fund tripwire still never hit at the declared 10% rate under Byzantine schedules (I3 holds under BFT).
    - `v2_byzantine_liquidation_cascade` — a Byzantine leader + a liquidation cascade scheduled together; assert the waterfall + tripwire invariant holds (no fund-tripwire hit, no wrong liquidation from a stale read) — the composition test.
    - `v2_vopr_seeds_replay` — failing seeds replay bit-identically (the regression corpus carries from v1.0 to v2).
  - **Make-pass card (SENIOR):** `v2_byzantine_liquidation_cascade` hits the tripwire only under BFT → symptom: a Byzantine view-change delays a liquidation past the point the fund can cover; why: the apply path's liquidation timing assumed VSR's faster commit, and BFT's 2-chain pipeline depth (O2) delayed it; fix: liquidation triggers on `commit_point()` advance (consensus-agnostic), with the circuit-breaker accounting for the committed-not-yet-applied window — the same incremental-margin discipline, now over a deeper pipeline; cost avoided: a solvency incident introduced by the consensus swap; muscle: timing-sensitive venue logic keys on `commit_point()`/`applied_point()`, never on a consensus impl's latency profile. Reapplies: W143 acceptance, the v1.5 ADL engage point.
  - 🧮 **Paper drill** — write in `notes/138_v2_vopr_drill.md`:
    - D-QUORUM on a 4-replica BFT venue (n=4, f=1, so 2f+1=3) with one Byzantine leader: draw the replicas + logs, mark the 3-of-4 prepare/commit quorum sets, show two quorums intersect in ≥1 honest node, trace one liquidation op `prepare → 3-quorum → commit`, then inject the Byzantine leader equivocation + view-change (Timeout-Certificate) and verify safety (no two conflicting commits) and liveness (new honest leader makes progress); include the NEGATIVE — a 2-of-4 "quorum" that doesn't intersect honestly → the split-brain double-commit of the same liquidation.
    - D-VOPR on one seed driving the `v2_byzantine_liquidation_cascade` scenario: write the seed→decisions table (each PRNG draw → its fault: Byzantine equivocation / view-change delay / message reorder), hand-run a few safety-phase ticks under the Byzantine schedule then the liveness-phase heal, and list the invariants checked at every commit (I1 linearizable-log, I2 no-divergent-state-hash, I3 insurance-fund-tripwire-never-hit); then the nondeterminism-leak hunt — enumerate every leak (HashMap iteration over open positions, wall-clock in mark price, tokio on the sim path, f64 in margin) that would void byte-identical seed replay (the `v2_vopr_seeds_replay` test).
    - D-FAIL on the Byzantine-view-change × liquidation-cascade composition: a table — component (BFT consensus / liquidation engine / insurance fund) → failure mode (leader equivocates, view-change stalls commit) → blast radius (which account sees a stale-read wrong-liquidation? does any user see a tripwire hit?) → recovery path (re-elect honest leader, liquidation re-keys on `commit_point()` advance) → the invariant stressed (I3 fund-tripwire-never-hit over BFT's deeper 2-chain pipeline window); conclude "survives a single Byzantine replica with no solvency incident because liquidation timing keys on commit_point(), not the consensus latency profile" — or name the gap.
  - **Output**: `v2_cluster_vopr` green — the whole venue is Byzantine-correct under perp-dex-core v2; the v1.0 invariant set holds under BFT; seeds replay. The apex is now build-complete + Byzantine-proven + swapped + whole-venue-validated. **Remaining: W143 acceptance.**
- [ ] [Venue · BFT] Handoff note → `notes/138_v2_vopr.md`: whole-venue cluster-VOPR green under BFT; all v1.0 invariants consensus-impl-independent. **Next W143: BFT-apex acceptance gate.** (~10 min)
- [ ] Commit + log (~10 min)

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
