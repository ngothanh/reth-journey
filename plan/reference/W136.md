# Week 136 — First month at the bet/bridge: deeper task on the critical path

> **Phase**: 7 (Bet Placement / Bridge)
> **Month**: M35
> **Track**: Bet-path integration (week 2 at the bet/bridge — first *bigger* task on the critical-path engine I was brought in to build)
> **Hours target**: 40h/wk (W136 is the **last** 40h week of the plan; W137–W144 drop to 30h/wk)
> **Mirror target**: n/a — no upstream module this week. The "mirror" is now the bet's own production codebase: read it in sponge-mode (W135) to match house style, do not impose your own.
> **Crate(s) touched**: none new. The personal HFT stack (`perp-dex-core` capstone + supporting crates) is in **maintenance-only** mode — one substantive Reth PR review + the APPENDIX A ops/chaos touchpoints (Mon/Wed/Sat). No crate Build this week.
> **Bar**: (n/a) — bet-path integration week, no crate Build. The bar discipline (c for the HFT critical path) is kept alive only as the Saturday chaos/back-pressure drill on the personal rig.
> **Feeds into**: W137 cultural integration + **token/equity vesting-milestone tracking** (this week's `bet_onboarding/task1_*.md` task-execution notes feed both the "how does this venue differ" reflection AND the first vesting/milestone check — did the first bigger deliverable land inside the venue's expectations?); M35 **midpoint check** (W138) = first month complete + **3800+ runtime hours** on the personal rig (this week's Saturday rig read is one of the data points); the personal capstone's **4000-by-M36** ops-hours target (continuous); W141 public-artifact update (`perp-dex-core` + the bet) — kept credible only if the rig keeps running and the Reth relationship stays warm.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): the bet's internal onboarding/architecture docs (the deeper-task spike — lookup-when-stuck, do **not** pre-read end to end); APPENDIX A "Weekly HFT maintenance template" in /Users/thanhngo/tngo/projects/reth-journey/README.md (Mon/Wed/Sat touchpoints); personal ops dashboards (Saturday runtime-hours read); README.md §A (M31–M36 bet-path re-slotting), §6 (ecosystem checklist — the lens for "does this bet still pass?"), §7 (Dubai/SG flip-trigger — still being evaluated remotely).
> **v3 modifications**: (1) **Frontmatter + narrative re-aimed from the v2 "first month at a Tier-A HFT IC job" to the v3 bet-path** — this is week 2 at the **founding/core-engineering bet on an on-chain-derivatives venue (or the crypto-MM cash-seat bridge)**, and the first bigger task is on the *critical-path engine I was brought in to build*, not generic team work. (2) **"Feeds into" re-pointed to the v3 sequence**: W137 now carries **token/equity vesting-milestone tracking** (the bet's comp is equity/token, not salary — landing the first real deliverable is a vesting-relevant signal), and the public artifact downstream is **`perp-dex-core` (the capstone)**, not a generic portfolio. (3) The personal stack in maintenance is now the **`perp-dex-core` capstone + VSR/risk/liquidation crates**, and the §6/§7 evaluation (does the bet still pass the checklist; has the relocation flip-trigger fired) is folded into the Saturday reflection so the bet stays under continuous scrutiny even after starting. (4) The HFT maintenance touchpoints are kept verbatim — the rig's 3800-by-M35 / 4000-by-M36 hours are a hard checkpoint that does not pause because you started the bet.

This is a **bet-path integration week** (week 2 at the bet/bridge — first *bigger* task assigned and submitted), **not a Build week**. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding. Every item instead names its **artifact file + an Output line that proves it done**, and the substantive learning/judgment items carry a 🎯 hardened-acceptance pass criterion + a *mental model* of why the item exists now. The week's job: take the first non-trivial assignment **on the critical-path engine you were hired/allocated to build**, scope it honestly inside an unfamiliar codebase, ship it, and keep the personal capstone rig advancing toward the M35-midpoint **3800-hour** checkpoint without letting the new bet swallow it. The APPENDIX A HFT maintenance touchpoints (Mon/Wed/Sat) are folded in explicitly below so they don't silently lapse during the ramp.

**One framing note specific to the bet (read before Monday):** at a salaried job the first bigger task is a competence signal. At a **founding/core-eng bet with equity/token comp**, it is a *vesting-relevant* signal AND a two-way evaluation — you are also still checking whether the venue passes §6 (founding-tier allocation, tier-1 backing, core-criticality) now that you see it from the inside. Execute the task like a senior who respects the reviewer's time; observe the venue like an investor who can still walk. Both lenses are on this week.

---

**Monday — Bigger task assigned + scope it before touching code**

- [ ] Artifact: `bet_onboarding/task1_scope.md` (private). The first bigger task on the critical-path engine lands today. Before writing any code, write down: (1) the one-sentence problem statement in your own words, (2) the acceptance criteria as you understand them, (3) the ≤3 files/modules you believe it touches, (4) the single biggest unknown. Then take it to the assigner (founder/lead/desk-head) for a 5-min confirm — cheaper to be wrong on paper than in a PR. (~45 min)
  - 🎯 Hardened acceptance: `task1_scope.md` has all four sections filled AND a one-line "assigner confirmed / corrected: <delta>" stamp after the sync — an unconfirmed scope doc does not count.
  - *Mental model*: a senior's edge in an unfamiliar codebase is not typing speed, it's **scoping discipline** — restating the problem and the blast radius in writing surfaces the misunderstanding now, when it costs a 5-minute conversation instead of a rejected PR three days in. This is the same pre-mortem habit the Build weeks drilled (write the bad outcome before you start), reused on a bet-path artifact. On a critical-path engine the blast radius is real: name it before you touch it.
  - Output: `bet_onboarding/task1_scope.md` exists with the 4 sections + the confirm stamp.
- [ ] **[HFT] Monday touchpoint** (~15 min) — artifact `chaos_log.md` (append). Review weekend runtime alerts on the personal `perp-dex-core` rig: any P99 spikes, VSR replica-divergence events, insurance-fund tripwire approaches, or disk-fill warnings? Log "clean" or the specific event.
  - Output: `chaos_log.md` has a dated W136-Mon line stating clean-or-event.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday commit.

**Tuesday — Work the task: spike the unknown first**

- [ ] Artifact: `bet_onboarding/task1_journal.md` (private, append daily). Attack the single biggest unknown from Monday's scope doc **FIRST** — a throwaway spike branch is fine internally, but capture what you learned (the API that actually exists, the constraint you didn't know, the test/sim harness shape). Update the scope doc if the spike changed the blast radius. (~45 min logging on top of the work itself)
  - 🎯 Hardened acceptance: the journal's Tuesday entry names the unknown, the spike outcome (resolved / still-open + next probe), and any resulting edit to `task1_scope.md`'s file list.
  - *Mental model*: in a strange codebase the riskiest assumption is the one you can't see — **front-loading the spike converts the biggest unknown into a known early**, so the rest of the week is execution against a stable plan instead of discovering a wall on Thursday. Sponge-mode (W135) is still on: read the surrounding code to match the house style, don't impose your own. On a critical-path engine, the unknown is usually a determinism/ordering/back-pressure invariant — exactly the class your capstone trained you to smell out; use that nose, but confirm it against *their* code, not your priors.
  - Output: `bet_onboarding/task1_journal.md` has a dated Tuesday entry meeting the acceptance criteria.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday commit.

**Wednesday — Work the task: build against the confirmed scope**

- [ ] Artifact: `bet_onboarding/task1_journal.md` (append). Execute the core of the task against the (now spike-validated) plan. Log: what you finished, what's blocked + on whom, and one thing you learned about the codebase worth remembering (a convention, a gotcha, a useful internal tool, an invariant the engine relies on). (~30 min logging on top of the work)
  - *Mental model*: the daily journal is not busywork — it is the **raw material for Friday's PR description**, for the W137 "how does the venue differ" + vesting-milestone reflection, and for your own 30/60/90 self-eval. Capturing learnings while warm beats reconstructing them later, the same recall-while-fresh discipline the plan's `notes/` files enforced for crates. At a bet, the "what I learned about the engine" line compounds: it is how you go from new hire to person-who-understands-the-critical-path, which is the whole point of being a founding/core eng.
  - Output: `bet_onboarding/task1_journal.md` has a dated Wednesday entry with done / blocked-on-whom / one-learning.
- [ ] **[HFT] Wednesday touchpoint** (~30 min) — artifact `progress.md` (HFT row). Check the personal ops dashboards, capture the current runtime-hours total, and update the HFT row in `progress.md`. Note the gap to the M35-midpoint **3800-hour** target (W138).
  - 🎯 Hardened acceptance: `progress.md` HFT row shows a dated runtime-hours number AND the remaining hours-to-3800 delta.
  - *Mental model*: 3800 hours by M35-midpoint is a hard checkpoint, and runtime accrues only if the rig keeps running while you're absorbed in the new bet — the weekly read is the **early-warning system** that catches a silently-dead rig before it costs you a month of hours. The capstone's runtime story is also part of the public artifact (W141) and the credibility that *sourced this bet*; a dead rig quietly erodes the thing that got you the seat.
  - Output: `progress.md` HFT row updated with dated total + delta-to-3800.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday commit.

**Thursday — Work the task: self-review against acceptance criteria**

- [ ] Artifact: `bet_onboarding/task1_journal.md` (append). Before asking anyone to review, **diff your change against Monday's acceptance criteria line-by-line**: each criterion → ✓ met / partial / not-yet. Run the project's full local check (build + lint + tests + any deterministic sim/VOPR-style harness they have) exactly as CI would. Capture any criterion you cannot meet and why — surface it to the assigner **today**, not at review time. (~45 min)
  - 🎯 Hardened acceptance: Thursday journal entry maps every Monday acceptance criterion to ✓/partial/not-yet AND records the local check command + its pass/fail result; any not-yet criterion has a surfaced-to-whom note.
  - *Mental model*: self-reviewing against the written acceptance criteria is how you make your first bigger PR **boring** — the reviewer finds nothing surprising because you already found and named the gaps. Green-locally-before-review is the same "make the test pass, one failure at a time" loop from the Build arcs, applied to someone else's CI. On a critical-path engine, "I ran your determinism/sim harness and it's green" is the single highest-trust sentence you can hand a founder in your first month.
  - Output: `bet_onboarding/task1_journal.md` Thursday entry meets the acceptance criteria.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday commit.

**Friday — Submit: PR + a tight description**

- [ ] Artifact: `bet_onboarding/task1_pr.md` (private copy of the PR description). Open the PR. Write the description from the week's journal: **what + why + how-verified + which acceptance criteria it satisfies + any known follow-ups**. Match the house PR template/style you observed in W135. Request review from whoever the assigner pointed to. (~45 min)
  - 🎯 Hardened acceptance: the PR description states what/why/how-verified/criteria-covered/follow-ups; `task1_pr.md` mirrors it AND records the PR link + reviewer requested.
  - *Mental model*: at a small founding-stage venue the PR description is your **reputation surface for the people who weren't in the room** (other founders, investors who look at the repo, the next hire) — a crisp what/why/how-verified signals a senior who respects the reviewer's time, which is exactly the signal the first bigger task on the critical path is being watched for. This is the "Output line that proves it done" discipline from the whole plan, now graded by people whose equity you share.
  - Output: PR opened; `bet_onboarding/task1_pr.md` exists with the description + PR link + reviewer.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday commit.

**Saturday — Personal projects: capstone rig + Reth maintenance + bet-still-passes check**

- [ ] **[HFT] Saturday chaos drill** (~60 min) — artifact `chaos_log.md` (append). Run this week's rotated chaos drill on the `perp-dex-core` rig (rotate weekly: process kill, network partition, clock skew, disk fill, slow subscriber). Capture: which scenario, observed recovery behavior (VSR view-change / state-transfer / back-pressure), recovery time, and any replica divergence or insurance-fund tripwire approach. This is the bar-(c) "back-pressure / recovery as correctness" discipline kept alive **under operations**, not theory.
  - 🎯 Hardened acceptance: `chaos_log.md` has a dated W136 entry naming the scenario + observed recovery behavior + recovery time; a regression vs the prior run of that scenario is flagged, not buried.
  - *Mental model*: 4000+ runtime hours by M36 is only credible if the rig **demonstrably survives chaos** — a drill that's logged green every week without numbers is worthless in a founder/investor conversation. The rotated drill is the cheapest proof that the recovery/back-pressure invariants you built (`consensus-vsr` view-change, `ledger-deterministic` replay, insurance-fund tripwire-never-hit) still hold under real operations, and it is the live demonstration of the exact judgment the bet is paying equity for.
  - Output: `chaos_log.md` updated with the dated scenario + recovery-time entry.
- [ ] Operations rig check (~15 min) — artifact `progress.md`. Rig still running? Update runtime hours total; confirm trajectory toward **3800 by M35-midpoint (W138) / 4000 by M36**. If the rig is down, that is the week's priority, not the new bet's overflow.
  - Output: `progress.md` shows a dated runtime-hours total + up/down status.
- [ ] One Reth PR review (~120 min) — artifact: the GitHub review itself + a one-line note in `outreach/reth_activity.md`. Keep the maintainer relationship warm with one substantive review (not a rubber-stamp) — this is the **credibility channel the bet was sourced through**, and it must not lapse just because you placed the bet.
  - 🎯 Hardened acceptance: the review leaves ≥1 substantive comment (correctness/design, not nits-only); `outreach/reth_activity.md` logs the PR link + date + the substantive point raised.
  - *Mental model*: the inheritance-and-PR track is what got you here; a 6-week silence right after placing the bet reads as "got the seat, stopped caring." One real review per week is the **minimum-viable signal** that keeps the network and the open-source standing alive for Bet #2 — and per §A every bet upgrades the next, so the channel that sourced Bet #1 is also the channel that sources Bet #2.
  - Output: Reth PR review submitted; `outreach/reth_activity.md` has the dated entry.
- [ ] **Bet-still-passes check** (~20 min) — artifact `bet_decision/m35_recheck.md` (append). One short, honest paragraph from the inside-the-venue vantage: re-run the §6 ecosystem lens (founding-tier allocation intact? tier-1 backing still real? am I on the *core-critical* path or drifting to busywork?) and the §7 flip-trigger (is a warm-conversation cluster / presence-premium forming in Dubai/SG that would justify relocation, or is remote-first still right?). Log GREEN / WATCH / concern.
  - 🎯 Hardened acceptance: `m35_recheck.md` has a dated W136 paragraph that names a verdict (GREEN/WATCH/concern) for both the §6 bet-quality lens and the §7 relocation flip-trigger — a vague "going well" does not count.
  - *Mental model*: the cost of a bad bet is highest *before you're vested and relocated*. Week 2 from the inside is the cheapest, highest-information moment to confirm the venue still passes the filter you applied from the outside — and to keep the relocation decision a deliberate trigger, not drift. This is the same "decision gate, written down, with a verdict" discipline as the M-checkpoints, run as a personal weekly until the bet is unambiguously GREEN.
  - Output: `bet_decision/m35_recheck.md` updated with the dated GREEN/WATCH/concern verdict for §6 + §7.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.

---

**Sunday — 🔁 Rebuild Day (~1h, ngày làm việc full — không tổng kết, thuần rebuild)**

> Cold re-implementation từ **file trống** — không xem lời giải cũ / blog / chat. Oracle = test suite của chính artifact (phải xanh). `cargo +nightly miri test` cho item concurrency. Ghi peek-log (kẹt ở đâu) vào `notes/rebuild_<item>.md`; help-surface phải co lại qua các rep.

- [ ] **R4 · seqlock applied-state risk read** *(APEX, rep 4/4, ~1h)* — `crates/perp-dex-core/src/risk_read.rs` *(gốc W111)*
  - oracle: loom suite + Miri (negative-control must-be-slow)

