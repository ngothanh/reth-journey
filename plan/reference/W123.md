# Week 123 — Event-trip recon part 1 (Token2049 Dubai/SG-style) — compress network, source Bet #1, no move

> **Phase**: 7 (Bet Placement / Bridge)
> **Month**: M32
> **Track**: Bet-path — event-trip recon (part 1 of 2): compress the Binance-alumni / on-chain-derivatives network in person, source Bet #1 candidates against the §6 checklist, gather §7 flip-trigger evidence — **NOT a relocation, NOT formal interviews**
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: (n/a — network/recon week; no upstream crate module mirrored)
> **Crate(s) touched**: (none — recon trip; `perp-dex-core` v1.0 is the artifact you *carry*, not one you edit this week)
> **Bar**: (n/a) — no Build; the bar that matters is the §5 controllable-outbound metric (warm conversations logged), not a bar-(c) crate gate. Every objective must produce a sourced, falsifiable artifact line.
> **Feeds into**: W124 recon part 2 / post-trip wrap (this week's coffee-meeting + booth + side-event notes seed the W124 venue shortlist and the **§7 flip-trigger evaluation** — warm-conversation cluster / presence-premium); W125 active outbound to on-chain-derivatives teams (the Bet-candidate A–F ranking + §6 verdicts calibrated here become the W125 outreach order); W126–W129 founding/core-eng conversations & work-trials (the warmest in-person leads convert here); W130–W131 Bet #1 GO / bridge engage (this trip's signal is an input to that call); `notes/inbound.md` funnel (W122 inbound triage → in-person conversions logged here); `notes/ecosystem_shortlist.md` (W115/W116 — the ≥5 venues passing §6, validated against people met in person)
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): `README.md` §5 (M24/M30 — the controllable-outbound metric this trip feeds, and the M30 bet-readiness gate "shortlist ≥5 venues passing §6, ≥2 founding/core-eng conversations advanced"); §6 (the 7-point ecosystem-selection checklist — the GO/bridge gate every Bet candidate is scored against: founding-tier allocation, tier-1 backing, core-criticality, multi-B-FDV ceiling, token reality, remote-native, cycle timing — "GO only if all seven pass, otherwise it is a bridge job, not a bet"); §7 (shots-on-goal: Bet #1 is one of 2–3 sequential shots, not all-in; the geography block "pre-stage, do NOT pre-commit"; the **explicit flip-trigger**: ≥N warm conversations cluster in one hub OR a presence-contingent offer premium); §A Phase 7 ("Dubai/SG: cheap prep only; Token2049-style event trips; relocate only on the §7 flip trigger"); §9 risk register ("Best venues want SG/Dubai presence → pre-stage, don't pre-commit"; "Token allocation illiquid / FDV craters → §6 + 2–3-shot structure"); `notes/W122_geography_prestage.md` (W122 — Dubai/SG visa + pre-TGE token-tax facts on file, the homework you deploy in-person this week); `notes/inbound.md` (W122 — the warm contacts + tentative bet/bridge interest this trip meets face-to-face); `notes/ecosystem_shortlist.md` (W115/W116 — the §6-passing venue list); `notes/binance_alumni_map.md` (W113/W120 — the alumni graph this trip activates); destination cost-of-living/visa portals (lookup-when-stuck, official/primary sources only)
> **v3 modifications**: (1) **Re-aimed from v2 "destination geography recon for a Tier-A HFT job" to v3 "event-trip recon to source Bet #1."** The trip is no longer scouting a city to *move to*; it is an **event trip (Token2049 Dubai/SG-style) to compress network density without relocating** (§7), source on-chain-derivatives founding/core-eng bets, and gather **flip-trigger evidence**. (2) **Firm ranking → Bet-candidate ranking scored against the §6 seven-point checklist** — a candidate that fails §6 is a *bridge job*, not a bet, and is tagged as such. (3) **Cost-of-living model demoted to a thin "flip-trigger cost basis"** — you are NOT budgeting to live there; you only need the numbers IF the flip-trigger fires (and which hub). (4) **Two parallel funnels recognized**: founding/core-eng *bets* AND crypto-MM *bridge* seats (the §7 income floor) — both logged, scored differently. (5) **"No move" is an explicit invariant of the week** — relocation is decided at Bet #1 time per the §7 flip-trigger, never on event-trip enthusiasm.

This is a bet-path **event-trip recon week**, not a Build week. There is **no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding** (consistent with §A: W118–W119 carried the `perp-dex-core` v1.5 build; W122 was outreach + pipeline; this week is in-person recon). The week's job: turn the W122 inbound funnel and the W122 visa/token-tax pre-stage homework into **in-person bet signal** before active outreach opens W125, and produce three decision-grade artifacts that W124 and W125 consume directly: (i) a **Bet-candidate ranking scored against the §6 checklist** (which venues are real bets vs bridges), (ii) a **§7 flip-trigger evidence ledger** (does a warm-conversation cluster or presence-premium exist in one hub — Dubai or SG), and (iii) a thin **cost basis** that only matters if the trigger fires. Every item below names its artifact file and carries an Output line that proves it done; the framing, §6-scoring, and flip-trigger items also carry a hardened-acceptance pass criterion and a mental model of why they exist now.

**The invariant that frames the whole week (read before Monday):** this is **recon, not relocation** — you place no lease, sign nothing, and the move decision waits for Bet #1 time (§7). The terminal state is a **founding/core-engineer token-equity bet** (§1 bar (b)) with a **crypto-MM cash seat as the income floor** (bar (d)). So the in-person conversations point at **two** destinations — derivatives founders (the bets) and crypto-MM desks (the floor) — and the artifact you lead with is **`perp-dex-core`** (the replicated, cluster-VOPR'd hybrid CLOB whose centerpiece is the cross-margin + liquidation engine), not a list of crates. The §6 checklist is the gate: a contact is a **bet** only if it can plausibly pass all seven points; otherwise it is a **bridge** (crypto-MM cash) or a **fallback** (crypto-infra IC on the Reth spine).

---

## Monday — Pre-trip framing: objectives, §6 scorecard, flip-trigger instrument, pitch

- [ ] **Write the trip's four objectives** → `notes/123_recon_purpose.md` (private). Before travel, fix the objectives so
  each day's conversations are measured against them, not vibes: **(a)** compress network density in person — meet the warm
  Binance-alumni / on-chain-derivatives contacts from `binance_alumni_map.md` + `inbound.md`; **(b)** attend the event +
  ≥1 side-event / dinner (where founding bets actually get sourced, not the main stage); **(c)** source Bet #1 candidates —
  early founding/core-eng conversations scored against the §6 checklist (NOT formal interviews); **(d)** gather **flip-trigger
  evidence** (§7) — is there a warm-conversation cluster in one hub, or any presence-contingent offer premium? For each
  objective write the single concrete observation that marks it "achieved." (~30 min)
  - 🎯 Hardened acceptance: `recon_purpose.md` lists all four objectives (a)–(d), each with a one-line falsifiable success
    criterion (e.g. "(c) achieved = ≥3 distinct teams where I had a real founding/core-eng conversation AND scored them 1–7
    on the §6 checklist"; "(d) achieved = a written yes/no on whether ≥N warm conversations clustered in Dubai *or* SG, with
    the count"). No objective left as an unmeasurable aspiration. The file states **explicitly**: "This is an event trip, NOT
    a relocation; no lease, no commitment; move decided at Bet #1 time per §7." Output: `notes/123_recon_purpose.md` with
    objectives (a)–(d) each carrying an `achieved =` line; `grep -c 'achieved =' notes/123_recon_purpose.md` ≥ 4.
  - *Mental model*: an event trip with no written success criteria becomes a conference vacation with coffee — you return
    with impressions, not a sourced bet pipeline. Writing the four falsifiable criteria up front turns the trip into a
    **Bet #1 sourcing run** whose output (the §6-scored candidate ranking + the flip-trigger ledger) feeds W124's shortlist
    and W125's outreach order directly. Per §7, in-person event trips are *the* mechanism to compress network density
    **without** paying the relocation cost prematurely; an undefined trip wastes the one cheap chance to source Bet #1
    face-to-face.
- [ ] **Build the §6 per-candidate scorecard template** → `notes/123_six_point_scorecard.md` (private; the reusable
  instrument). Transcribe the §6 seven-point checklist into a blank per-candidate scorecard you can fill fast during/after
  each conversation: 1 founding-tier allocation, 2 tier-1 backing {Paradigm, a16z crypto, Dragonfly, Polychain, 1kx,
  Multicoin, Jump, Wintermute}, 3 core-criticality (I build the engine that *is* the company), 4 multi-billion-FDV ceiling,
  5 token reality (pre-TGE, ~1y cliff / 4y vest, ≥10× headroom), 6 remote-native (Vietnam-remote works, no colo), 7 cycle
  timing. Each item: pass / fail / unknown + a one-line evidence note. **Rule at the top: GO only if all 7 pass; otherwise
  it is a bridge job, not a bet — tag it `BRIDGE` not `BET`.** (~25 min)
  - 🎯 Hardened acceptance: the template carries all 7 §6 items verbatim with a pass/fail/unknown slot + evidence line each,
    and the GO-rule header ("all 7 or it's a bridge"). It is a genuinely *blank instrument* (filled later, not pre-judged) —
    copied once per candidate (A–F) Tue–Fri. Output: `notes/123_six_point_scorecard.md`;
    `grep -c 'pass / fail / unknown' notes/123_six_point_scorecard.md` ≥ 7; the GO-rule header line is present.
  - *Mental model*: the §6 checklist is the discipline that keeps "exciting conversation" from being mistaken for "real
    bet." Founding-tier allocation + tier-1 backing + core-criticality are the three that separate a Bet from an
    Airwallex-style late-employee-equity bridge job. Having the scorecard *ready before* the conversations means you score on
    evidence collected live (cap table, vesting terms, role scope) instead of reconstructing a halo a week later. This is the
    gate the W125/W126 outreach order is built on.
- [ ] **Seed the §7 flip-trigger evidence ledger** → `notes/123_flip_trigger_ledger.md` (private; the instrument). Per §7,
  set up the ledger you fill across the trip: two columns (Dubai, SG), rows = (warm-conversation count this trip, any
  presence-contingent offer premium spotted, any team that explicitly requires on-site, hub-specific visa/token-tax note
  from `W122_geography_prestage.md`). Write the threshold at the top: "FLIP to relocation-prep only if ≥N warm conversations
  cluster in one hub OR a presence-contingent offer premium appears; until then Vietnam-remote is the default." Pick your
  concrete N now (e.g. N=4) and justify it in one line. (~20 min)
  - 🎯 Hardened acceptance: `flip_trigger_ledger.md` has the two-hub table seeded, a numeric `N=` threshold with a one-line
    justification, and the explicit "until trigger fires, Vietnam-remote default; no move this trip" line. The trip *collects
    evidence for* the trigger; it does not *fire* it (W124 evaluates, W131 decides). Output:
    `notes/123_flip_trigger_ledger.md` with the two-hub table, a numeric `N=` threshold + justification, and the "no move
    this trip / Vietnam-remote default" line.
  - *Mental model*: §7's whole geography stance is "pre-stage, don't pre-commit." The flip-trigger is the falsifiable bar
    that converts prep→move — without a written N, "this hub feels electric" becomes a multi-thousand-dollar relocation on
    vibes. Seeding the ledger now forces every hub conversation to drop a countable data point, so W124 can read a number
    ("3 warm convos clustered in Dubai, 1 in SG — below N=4, hold remote") instead of a feeling. This is the single most
    expensive decision in Phase 7; instrument it before you board the plane.
- [ ] **Draft the in-person pitch one-liner (both funnels)** → `notes/123_pitch_oneliner.md` (private; the thing you
  actually say). Built from the `perp-dex-core` v1.0 artifact you carry: "I built a deterministic, VSR-replicated,
  multi-instrument cross-margin perp-dex core in Rust — 3-node cluster, cluster-VOPR'd, the order/event log *is* the VSR
  log, with RWA-aware seams. I'm looking for a founding/core-eng seat building the matching/risk/settlement engine that *is*
  the company." Plus a one-line **crypto-MM-bridge** variant (latency craft: sub-µs matching p99, tick-to-trade with
  coordinated-omission handled, kernel-bypass-aware feed) for desk conversations. (~20 min)
  - 🎯 Hardened acceptance: `pitch_oneliner.md` has (i) the bet pitch ≤2 sentences naming the v1.0 capstone's load-bearing
    claims (VSR-log-as-source-of-truth, multi-instrument cross-margin, cluster-VOPR, RWA-aware seams), (ii) a crypto-MM-bridge
    one-liner (latency/HFT-craft framing for the §7 income floor), (iii) the single hardest probe question each path will ask
    + your one-line answer. No buzzword soup; every claim maps to something in `perp-dex-core`. Output:
    `notes/123_pitch_oneliner.md` with the bet pitch, the bridge variant, and ≥1 probe+answer per path.
  - *Mental model*: at an event you get ~90 seconds before someone's attention moves; the pitch must compress the 2000-hour
    artifact into a claim a founding-eng listener can verify and a crypto-MM desk lead can map to latency craft. Carrying
    *both* variants is the §7 two-funnel discipline — bets and bridge are sourced at the same event from different rooms. The
    probe-answer prep is what turns "nice project" into "let's talk next week."
- [ ] Commit notes (private repo). (~5 min)

**Output for Monday**: `notes/123_recon_purpose.md` (4 objectives, `achieved =` each, no-move invariant) + `notes/123_six_point_scorecard.md` (blank 7-point template + GO-rule) + `notes/123_flip_trigger_ledger.md` (two-hub table + `N=`) + `notes/123_pitch_oneliner.md` (bet + bridge pitch + probe-answers).

---

## Tuesday — Travel + event day 1: warm Binance-alumni reconnect (network density)

- [ ] **Travel + check in to the event; confirm the day's meeting schedule** → update `notes/123_recon_notes.md` (private;
  **no real names** — record each candidate as Bet candidate A/B/C/D/E/F and each desk as Desk 1/2/… so the file is
  leak-safe). Reconfirm the W122-scheduled slots (the intro calls / coffees that the W122 `## Conversations log` proposed for
  the recon window), and pin today's ≥2 substantive conversations. (~30 min logistics)
  - Output: `notes/123_recon_notes.md` exists with today's confirmed meetings (coded A–F / Desk 1–N) + times.
- [ ] **Warm-alumni reconnect block (objective (a))** → `notes/123_recon_notes.md` + the flip-trigger ledger. Work the warm
  Binance-alumni / on-chain-derivatives contacts from `binance_alumni_map.md` + `inbound.md` in person — the highest-conversion
  channel. For each conversation log: candidate/desk code, role of person met, one thing learned about team/stack/engine-scope,
  one thing learned about hiring/founding posture, the path (`BET` vs `BRIDGE`), and a 1–5 "would I bet on this" gut score.
  Drop a countable data point into `123_flip_trigger_ledger.md` for any hub-located conversation (increment Dubai/SG warm
  count). **Target ≥2 substantive conversations today; ≥1 founding/core-eng if at all possible.** (~3–4h on the ground,
  ~30 min logging)
  - 🎯 Hardened acceptance: ≥2 conversations logged today with code + role + stack/engine-scope note + posture note +
    `BET`/`BRIDGE` tag + 1–5 score; **every real name encoded A–F / Desk 1–N** (no real names in the file); any hub
    conversation increments the matching warm count in the flip-trigger ledger. Output: ≥2 dated entries appended to
    `notes/123_recon_notes.md`; ledger warm count updated.
  - *Mental model*: warm intros convert far higher than cold (§1 bar (c)), and in-person reconnect is the only channel that
    surfaces what a venue's site and a job post hide — the *actual* engine scope (am I building the core, or an
    integration?), founder caliber, cap-table backers, and whether token/vesting terms are real. Front-loading the warm
    alumni on day 1 (they have the longest relationship and best conversion) maximizes the warm-conversation count the
    flip-trigger reads — and starts the §5 controllable-outbound numerator that W122 began.
- [ ] **Fill the §6 scorecard for any BET-tagged candidate met today** → `notes/123_six_point_scorecard.md`. For each
  candidate you tagged `BET`, copy the blank template and fill the 7 items pass/fail/unknown with live evidence (cap-table
  backer named? allocation band? core-critical or peripheral? remote-OK? pre-TGE + sane vesting?). Mark the verdict:
  **GO-eligible** (all 7 pass) / **needs-data** (any unknown) / **bridge** (any hard fail on 1/2/3). (~20 min)
  - Output: one filled 7-item block per BET candidate met today, each with a `verdict:` line.
- [ ] Commit notes (nightly). (~5 min)

**Output for Tuesday**: `notes/123_recon_notes.md` (≥2 coded entries, scores, BET/BRIDGE tags) + ledger warm counts updated + §6 scorecard filled for today's BET candidates.

---

## Wednesday — Event day 2: booth floor + on-chain-derivatives founder conversations (source Bet #1)

- [ ] **Booth-floor / main-event founder sourcing (objectives (b)+(c))** → `notes/123_recon_notes.md`. Work the floor and
  scheduled coffees toward **founding/core-eng** conversations on on-chain-derivatives venues — lead with the bet pitch from
  `123_pitch_oneliner.md`. **Target ≥2 substantive conversations; ≥1 founding/core-eng.** Log each with the full row (code,
  role, stack/engine-scope, founding posture, `BET`/`BRIDGE`, 1–5 score). Probe the §6-load-bearing facts live: who's on the
  cap table, what allocation band the founding-eng seat carries, whether I'd own the matching/risk/settlement core or a
  peripheral integration. (~4h on the ground, ~30 min logging)
  - 🎯 Hardened acceptance: ≥2 conversations logged today (code + role + scope + posture + tag + score); ≥1 tagged `BET`
    (founding/core-eng); real names encoded A–F / Desk 1–N. Output: ≥2 dated entries appended to `notes/123_recon_notes.md`;
    running conversation count stated at the top of the file.
  - *Mental model*: founding bets are sourced where founders actually are — the booth floor and the scheduled coffee, not the
    main-stage talk. Leading with the `perp-dex-core` claim (the engine that *is* a perp venue) is what makes a founder lean
    in: it demonstrates you've already built the company's core once. Probing the §6 facts *in the room* (cap table,
    allocation, core-criticality) is far higher-fidelity than reconstructing a halo a week later, and it is exactly the
    evidence the scorecard needs.
- [ ] **Fill / advance the §6 scorecard for today's BET candidates** → `notes/123_six_point_scorecard.md`. For each `BET`
  candidate from today, complete the 7-item block with live evidence and set the verdict (GO-eligible / needs-data /
  bridge). Honestly downgrade to `bridge` any candidate failing core-criticality / founding-tier / tier-1-backing — apply the
  gate strictly, not generously. (~25 min)
  - 🎯 Hardened acceptance: every `BET` candidate met Tue–Wed has a completed 7-item §6 block with a one-line verdict; any
    hard fail on §6 points 1/2/3 is downgraded to `bridge`. Output: `notes/123_six_point_scorecard.md`;
    `grep -c 'verdict:' notes/123_six_point_scorecard.md` ≥ (number of BET candidates so far).
  - *Mental model*: this is where "great conversation" becomes "is this actually a bet." The §6 gate is deliberately strict —
    all 7 or it's a bridge — because the Phase-7 failure mode is taking a late-employee-equity role and *calling* it a
    founding bet (the Airwallex anti-pattern §6 names). Scoring live, against evidence you can still ask follow-ups on,
    directly seeds W124's shortlist and W125's outreach order.
- [ ] **Increment the flip-trigger ledger per hub** → `notes/123_flip_trigger_ledger.md`. Drop a countable point per hub
  conversation: increment Dubai/SG warm count, note any presence-contingent premium ("they pay materially more for
  on-site"), note any team that *requires* on-site (a §6 #6 remote-native fail), attach the hub visa/token-tax fact.
  **Do not fire the trigger; only record evidence.** (~10 min)
  - Output: per-hub warm count updated; presence-premium yes/no noted; "no move this trip" invariant line unchanged.
- [ ] Commit notes (nightly). (~5 min)

**Output for Wednesday**: ≥2 more coded entries in `notes/123_recon_notes.md` (≥1 BET) + §6 blocks completed for Tue–Wed BET candidates + ledger warm counts incremented.

---

## Thursday — Event day 3: crypto-MM desk conversations (the §7 income floor) + side-event

- [ ] **Crypto-MM desk block (bridge funnel, objective (a)+(c))** → `notes/123_recon_notes.md`. Work the **crypto-MM cash
  desks** (Wintermute/Jump-style, remote) — the §7 income floor / bridge — leading with the latency-craft variant from
  `123_pitch_oneliner.md` (sub-µs matching p99, tick-to-trade + coordinated-omission, kernel-bypass-aware feed). **Target ≥2
  substantive conversations.** Log each as Desk 1/2/… with role, what they probe, remote posture, and a `BRIDGE` tag (these
  are the income floor, scored separately from the bets — do not rank them with the bets). (~3–4h on the ground, ~30 min
  logging)
  - 🎯 Hardened acceptance: ≥2 desk conversations logged today, each coded Desk N + role + probe note + remote posture +
    `BRIDGE` tag; the file now shows you worked *both* funnels (≥1 `BRIDGE` desk entry exists alongside the `BET` entries
    from Tue–Wed). Output: ≥2 dated `BRIDGE` entries appended to `notes/123_recon_notes.md`.
  - *Mental model*: the bridge is the *floor*, not the goal — but a real crypto-MM cash seat is what funds runway between
    bets (§7), so it must be sourced as deliberately as the bet, from a different room at the same event. It is underwritten
    by Half-2's latency craft (exactly why `latency-lab` was pulled to M6), so the desk conversation must lead with lived
    tail-latency numbers, not the venue narrative. Working both rooms is the §7 hedge: you leave with a Bet #1 pipeline AND a
    fallback income floor.
- [ ] **Attend ≥1 side-event / dinner (objective (b))** → `notes/123_recon_notes.md`. The side-event / dinner is where
  founding bets and warm intros actually deepen — log any new candidate/desk surfaced here (coded), and any warm intro
  offered for follow-up. Increment the flip-trigger ledger for any hub-located conversation. (~3h on the ground, ~15 min
  logging)
  - 🎯 Hardened acceptance: ≥1 side-event/dinner attended and logged; any new lead coded A–F / Desk N; any warm intro offered
    is noted with a `follow-up-W125` tag. Output: side-event entry in `notes/123_recon_notes.md`; ledger updated for any hub
    conversation.
  - *Mental model*: the main stage is theater; the dinner is where a founder decides whether to bring you into the founding
    conversation. Objective (b) exists precisely because the highest-signal interactions happen off-program. A warm intro
    sourced here is the leading indicator of the M30 gate ("≥2 founding/core-eng conversations advanced").
- [ ] **Start the thin flip-trigger cost basis (contingency only)** → `notes/123_cost_basis.md` (private; **thin —
  flip-trigger cost basis, NOT a moving budget**). If and only if a hub is accumulating warm conversations toward N, capture
  the minimum numbers to evaluate a *future* move to that hub: monthly rent for the tier you'd take, monthly burn (groceries
  + transport + utilities + healthcare/insurance), net delta vs Vietnam-remote — each line with a source (listing URL,
  asked-a-local, receipt) or `[UNVERIFIED]`. This is a *contingency* model, not a relocation plan. (~25 min)
  - 🎯 Hardened acceptance: `cost_basis.md` carries ≥5 sourced line items (rent, groceries, transport, utilities, one
    healthcare/insurance line) + a computed `net monthly burn` + `delta vs Vietnam-remote`, every number sourced or
    `[UNVERIFIED]`-tagged; the header states it is a **contingency cost basis used only IF the flip-trigger fires**, not a
    budget being committed to. If no hub is trending toward N, a one-line "trigger not approaching N → cost basis deferred to
    W124" is an acceptable completion. Output: `notes/123_cost_basis.md` with the contingency header + line items (or the
    explicit deferral line).
  - *Mental model*: in v3 you are not moving by default, so the cost model is demoted from "the deliverable" (its v2 role) to
    a contingency input — but it's still worth gathering *while you can read price tags in person*, because if the
    flip-trigger does fire at Bet #1 time you'll want a sourced burn number to (a) pick the hub and (b) counter a lowball
    with a concrete cost basis instead of a feeling. Tagging it explicitly as contingency prevents the v2 trap of letting the
    trip drift into apartment-hunting.
- [ ] Commit notes (nightly). (~5 min)

**Output for Thursday**: ≥2 `BRIDGE` desk entries + ≥1 side-event entry in `notes/123_recon_notes.md` + `notes/123_cost_basis.md` (contingency, ≥5 sourced lines or deferral) + ledger updated.

---

## Friday — Event day 4 (final): close warm leads, lock the metric numbers, last hub evidence

- [ ] **Final conversation sweep + warm-lead close** → `notes/123_recon_notes.md`. Take any remaining scheduled
  conversations; circle back to the strongest Tue–Thu leads in person to confirm a concrete next step (a W125/W126 call, a
  work-trial discussion, a warm intro to a co-founder). **Target ≥2 conversations today.** Tag every warm lead with its
  next action (`follow-up-W125` / `work-trial-W126` / `intro-pending`). (~4h on the ground, ~30 min logging)
  - 🎯 Hardened acceptance: by end of Friday `notes/123_recon_notes.md` records **≥8 logged conversations total** (≥2/day ×
    Tue–Fri) across **≥3 distinct candidate/desk codes**, each with code + role + scope note + posture note + `BET`/`BRIDGE`
    tag + 1–5 score; **every real name encoded A–F / Desk 1–N**; ≥2 entries tagged `BET` and ≥1 tagged `BRIDGE` (both funnels
    worked); no real venue/firm names in the file. Output: `notes/123_recon_notes.md` with ≥8 dated entries;
    `grep -c 'score:' notes/123_recon_notes.md` ≥ 8; ≥2 `BET` + ≥1 `BRIDGE` tags; every warm lead carries a next-action tag.
  - *Mental model*: the trip cost (flights + event pass + the week) is already sunk; under-booking the last day wastes the
    most expensive part. The close step converts a thread into a committed next step — exactly how §1 bar (c) "would bring
    you in" conversations get advanced and how the warm leads survive the gap to W125 active outreach instead of going cold.
- [ ] **Lock the per-hub flip-trigger numbers** → `notes/123_flip_trigger_ledger.md`. Finalize the warm-conversation count
  per hub (Dubai vs SG), record an explicit yes/no on "presence-contingent premium spotted," and confirm the count is
  comparable against the Monday `N=` threshold. **Do NOT make a relocation decision** (W124 evaluates, W131 decides). (~15 min)
  - 🎯 Hardened acceptance: the ledger has a numeric warm count per hub, a yes/no on presence-premium, and the count sits
    against `N=` — WITHOUT a relocation decision made; the "no move this trip / Vietnam-remote default" invariant still holds
    at the bottom. Output: `notes/123_flip_trigger_ledger.md`; `grep -c 'warm:' notes/123_flip_trigger_ledger.md` ≥ 2 (one
    per hub).
  - *Mental model*: the flip-trigger is the only thing that should ever convert prep→move, and it must be evidence-driven
    (§7). Forcing every hub conversation to drop a counted point produces a *number* W124 reads against N — "Dubai 3, SG 1,
    no presence-premium → below N=4, hold Vietnam-remote" — instead of an enthusiasm-driven lease. A recon trip that returns
    "Dubai felt amazing" with no count has failed objective (d).
- [ ] **Finish the §6 scorecards for all BET candidates** → `notes/123_six_point_scorecard.md`. Ensure every `BET`-tagged
  candidate from `123_recon_notes.md` has a completed 7-item block + verdict; close as many `unknown` slots as the remaining
  conversations allow; mark any candidate downgraded to `bridge`. (~20 min)
  - 🎯 Hardened acceptance: every `BET`-tagged candidate has a completed 7-item §6 block with a `verdict:` line; ≥1 candidate
    reaches **GO-eligible or needs-data** (a real bet path exists); any candidate failing core-criticality / founding-tier /
    tier-1-backing is correctly downgraded to `bridge`. Output: `notes/123_six_point_scorecard.md`;
    `grep -c 'verdict:' notes/123_six_point_scorecard.md` ≥ (number of BET candidates).
  - *Mental model*: the GO-eligible / needs-data / bridge split is what W124's shortlist and W125's outreach order are built
    on. A venue that looked GO on the W116 desk-research shortlist but turned out to offer a peripheral integration role must
    be caught *here*, in person, before W125 wastes outreach on it.
- [ ] Commit notes (nightly). (~5 min)

**Output for Friday**: `notes/123_recon_notes.md` at ≥8 entries (≥2 BET, ≥1 BRIDGE, every warm lead next-action-tagged) + `notes/123_flip_trigger_ledger.md` per-hub numbers locked vs `N=` + all BET §6 scorecards complete with verdicts.

---

## Saturday — Travel back + light debrief seed (protect Sunday's ranking quality)

- [ ] **Travel home** → no scheduled deep work. The recon week's heavy synthesis (the Bet-candidate ranking + the inbound /
  shortlist reconciliation) is intentionally deferred to the Sunday Weekly Ritual so it is done rested, not exhausted in an
  airport. (~travel)
  - Output: (no artifact — recovery/travel day; protects the debrief quality.)
- [ ] **5-minute debrief seed (optional, only if not exhausted)** → `notes/123_recon_notes.md`. While the conversations are
  freshest, jot a one-line "first impression" gut-rank of the top 2–3 candidate codes — a *seed* the Sunday ranking will
  re-derive properly from the §6 verdicts + flip-trigger numbers, not a final ranking. Skip without penalty if travel is
  draining. (~5 min)
  - 🎯 Hardened acceptance (soft): if written, the seed names ≤3 candidate codes with a one-line gut note each and is
    explicitly marked `SEED — re-derive Sunday from §6 verdicts`. If skipped, that is acceptable (Sunday re-derives from the
    artifacts regardless). Output: optional seed line in `notes/123_recon_notes.md`, or nothing.
  - *Mental model*: the most expensive post-event mistake is debriefing exhausted and mis-ranking the pipeline, so the real
    ranking waits for Sunday. But capturing a freshness-seed costs nothing and guards against the impressions blurring — as
    long as it is explicitly a seed, not the decision, the Sunday ritual still re-derives the ranking from the §6 verdicts
    and the flip-trigger count.
- [ ] Commit notes if anything was written. (~5 min)

**Output for Saturday**: travel home; optional freshness-seed line in `notes/123_recon_notes.md` (explicitly `SEED`), or nothing — Sunday's Weekly Ritual does the real debrief + Bet-candidate ranking + inbound/shortlist reconciliation (folding trip contacts into `notes/inbound.md`, reconciling `notes/ecosystem_shortlist.md` to keep ≥5 §6-passing venues, and producing the ranked `notes/123_bet_ranking.md` with the W125 outreach order + the `Flip-trigger read:` decision line that feeds W124).

---

## [v3.1 BFT apex — Track B] W123: the Jolteon 2-chain commit rule (advances `commit_point()`; O4 densification; O2 pipeline depth)

> Track B continues (W122 shipped QC formation + chaining). This week is the **commit rule** — the one place the BFT impl decides when an op is final, and the one place O2 (pipeline depth) and O4 (OpNumber densification) become concrete. Bar (c). The outer trait still exposes only `commit_point()`; the *rule* lives entirely in the BFT `Replica` core, invisible to call sites.

- [ ] **[Mon +120min · Sat +45min]** [Venue · BFT] **Build (bar c)**: `crates/consensus-vsr/src/bft/commit.rs` — the **2-chain commit rule**: a block `B` commits **iff `QC(B)` and `QC(B′)` exist where `B′` is `B`'s direct child AND `B′.round == B.round + 1` (consecutive rounds)**; ancestors commit transitively (the Jolteon/DiemBFT rule — lower latency than basic HotStuff's 3-chain). A direct-child QC certified across a TC gap does NOT commit `B` — that is exactly the unsafe case the published rule excludes. On commit, assign `B`'s ops **dense contiguous `OpNumber`s** (O4) and advance `commit_point()`. (~total 165 min)
  - **Phase 1 (compact — seal in `notes/123_commit_rule_prediction.md` before coding):**
    - *Prediction (sealed)*: B is certified at round r, a TC fires at r+1, B's direct *child* is certified at r+2 — predict whether B may commit, and what an adversary can do in the gap round if it does. Write it before reading the tests.
    - *Back-of-envelope*: the commit check on the hot path is O(1) — one parent-pointer chase + one round compare ≈ ~2 cache misses (~100–200 ns); an O(depth) chain walk per `try_commit` at pipeline depth d re-pays that d× per block for nothing.
    - *Pre-mortem (interleaving)*: drop the `B′.round == B.round + 1` check → B (round r) + child QC at r+2 commits B → but in the gap round r+1 a conflicting branch was certified by replicas that never saw QC(B) → two committed forks; the bug type-checks and passes every consecutive-schedule test.
  - 🔧 **Why this matters**: this is where the apex meets the frozen outer trait. (1) **O2 (pipeline depth)**: 2-chain means commit lags propose by ~2 views — invisible to call sites (the ledger waits on `drain_committed`, never assumes single-round-trip; W91 D6) but it *is* the latency the W135 X-target (p99≤2ms) must absorb. (2) **O4 (densification)**: BFT *height* can skip (a no-decision view), but the ledger requires contiguous `OpNumber` (journal replay + idempotency, W74 client_table) — so committed blocks' ops are mapped onto a **dense** `OpNumber` distinct from height, assigned *at commit*. The trap: committing on a 1-chain (block has a QC but no child QC yet) — that's not final and can be orphaned by a view change → a premature `Final` is a safety violation. Another trap: leaking BFT *height* (not dense `OpNumber`) across the trait boundary.
  - *Contract* (signature-level): `fn try_commit(&mut self, b: &Block) { let c = direct_child_of(b); if has_qc(b) && has_qc(c) && c.round == b.round + 1 { /* consecutive rounds — the Jolteon safety condition */ for op in b.ops { self.assign_dense_op_number(op); } self.commit_point = max(self.commit_point, b.last_dense_op); } }` — `commit_point()` (outer trait) returns `self.commit_point`; `assign_dense_op_number` is the O4 densifier (contiguous, monotonic, distinct from view/height); ancestors of a committed block commit transitively.
  - 🧮 **Paper drill** — write in `notes/123_commit_rule_drill.md`:
    - D-QUORUM on a 3f+1=4 replica BFT chain, blocks B@round r → B′@r+1 → B″ committed: draw the replicas + their logs, mark the BFT quorum (2f+1=3 of 4) certifying each block, show two quorums intersect in ≥1 *honest* node, trace B `prepare → QC → 2-chain commit` when B′ is B's direct child at consecutive round r+1; verify safety (no two conflicting commits) + liveness (the next consecutive QC pair makes progress). **Then the NEGATIVE**: B@r certified, TC fires at r+1, B's direct child certified at r+2 — show the QC(B)+QC(child) pair is NOT a 2-chain and B must stay uncommitted; exhibit the split-brain where dropping `c.round == b.round+1` lets a conflicting branch certified in the gap round r+1 (by replicas that never saw QC(B)) co-commit.
    - D-FAIL on the TC-gap committed-fork scenario (W129 #4): table each component (the round-r leader, the gap-round-(r+1) conflicting proposer, the r+2 child-QC, the StateChecker) → failure mode → blast radius (which replicas commit which fork; is it user-visible?) → recovery path (none — a committed fork is the one unrecoverable safety violation) → the invariant stressed (quorum overlap holds *within* a round only, not across a timeout gap); state "survives the gap iff the consecutive-round check rejects the non-2-chain."
    - D-SEAM on the `ConsensusBackbone` outer trait + the BFT commit rule impl: list the trait method (`commit_point() -> OpNumber`); audit that no BFT vocabulary (`view`/`height`/`round`/`QC`/`TC`) leaks across the boundary; show committed BFT *height* can skip (no-decision view) yet the densifier maps ops onto a contiguous `OpNumber` space, so the call site (ledger journal replay / W74 idempotency) sees the SAME dense space under VSR and BFT with zero call-site change → "swap-safe because consensus-internal numbering never crosses the seam."
    - D-COST on `try_commit` hot path: per-op cost table for the commit check — one parent-pointer chase (`direct_child_of`, ~1 cache miss ~100 ns) + one round compare (L1, ~0.5 ns) + the dense-OpNumber assign (~1 cache miss) ≈ ~2 cache misses (~100–200 ns); compute the cliff ratio vs the O(depth) chain-walk alternative re-paid per block at pipeline depth d (d× ≈ 200d ns); state the budget (W135 X-target p99 ≤ 2 ms) and that O(1) fits while the O(depth) walk busts it under deep pipelining.
  - **Tests:**
    - `two_chain_commits_block_with_consecutive_child_qc` — block at round r + its direct child QC'd at round r+1 → block commits, `commit_point()` advances to its last dense op. A block with only its own QC does **NOT** commit (1-chain insufficient). Traps the premature-Final safety bug.
    - 🪤 **trap `non_consecutive_child_qc_does_not_commit` (EXPERT)** — B certified at round r; TC at round r+1; B's *direct child* certified at round r+2 → assert B does **NOT** commit (a direct-child QC across a TC gap is not a 2-chain) — and stays uncommitted until a consecutive QC pair forms higher up the chain, at which point B commits *transitively as an ancestor*. Traps the `has_qc(b) && has_qc(child_of(b))` shortcut that drops the round check.
    - 🪤 **negative-control `commit_checker_has_teeth` (SENIOR)** — plant a `try_commit` without the `c.round == b.round + 1` condition and drive the TC-gap schedule above through the StateChecker; assert the checker REJECTS the planted commit (flags a commit across a TC gap) — proves the harness detects a non-consecutive commit, rather than the trap test passing vacuously on a schedule that never forks.
    - `op_number_dense_and_contiguous_across_skipped_views` (O4) — drive a schedule with a no-decision view (height gap); assert committed `OpNumber`s stay **contiguous** (no gap) even though BFT height skipped. (This is the W129 scenario #8 property — proven here at the unit level.)
    - `commit_point_monotonic_never_regresses` — across view changes, `commit_point()` never decreases (the outer-trait invariant from W91 behavioral contract). Composes with the W121 TC (a view change carries the highest high-QC, so commit can't regress).
    - `outer_trait_exposes_no_height` — `grep`/API check: the outer `ConsensusBackbone` exposes `commit_point() -> OpNumber` only; BFT `view`/`height` never crosses the boundary (acceptance #9 / O4 — dense `OpNumber` is the only log position callers see).
  - **Make-pass card (EXPERT · the consecutive-round safety card):** `non_consecutive_child_qc_does_not_commit` fails (B commits across the TC gap) → symptom: a block committed across a TC gap is forked by a conflicting branch certified *in the gap round* (the W129 safety-attack scenario, #4, reproduces exactly this); why: quorum intersection only constrains votes *within* a round — across a timeout gap a conflicting branch can be certified by replicas that never saw QC(B), so "QC(B) + QC(direct child)" alone proves nothing about the gap; fix: require `B′.round == B.round + 1` (consecutive rounds) in `try_commit`; cost avoided: a committed fork — the one unrecoverable safety violation; muscle: **"2-chain safety lives in the consecutiveness, not the chain length."** Reapplies at: W129 scenario #8 *and* #4 (safety-attack), W143 acceptance.
  - **Make-pass card (EXPERT · O2/O4):** `op_number_dense_and_contiguous_across_skipped_views` shows a gap → symptom: the ledger's journal replay panics on a non-contiguous op_num after a skipped BFT view; why: the impl exposed BFT height as the OpNumber, and height skipped; fix: densify at commit — committed ops get the next contiguous `OpNumber`, decoupled from view/height; cost avoided: ledger replay break + idempotency-table corruption (W74); **Cost avoided (bar-c, measured)**: the commit-rule check stays O(1) on the hot path — one parent-pointer chase + round compare ≈ ~2 cache misses (~100–200 ns) per `try_commit`, vs an O(depth) chain walk re-paid per block; muscle: the outer log position is dense and contiguous by construction; consensus-internal numbering never leaks. Reapplies: W129 #8 (Byzantine-leader skipped-view contiguity), W135 swap (the ledger sees the same dense OpNumber space under VSR and BFT — that's why the swap is transparent).
  - **5-year failure mode**: reconfiguration (N changes) breaks fixed-N OpNumber densification — trigger: a validator-set change lands (post-MVP); migration: an epoch-prefixed dense counter (`OpNumber = (epoch, dense)` mapped onto one monotonic space at the boundary).
  - **Output**: `bft/commit.rs` with the 2-chain rule + O4 densifier; tests green; O2 noted (pipeline depth feeds the W135 X-budget); O4 confirmed impl-only (dense OpNumber decoupled from height); the outer trait still exposes only `commit_point()`.
- [ ] **[Sat +10min]** [Venue · BFT] Handoff note → `notes/123_bft_track.md`: 2-chain commit (consecutive-round rule) + O4 densification shipped; O2 (pipeline depth) is a W135 latency-budget input, not an interface change. **Next W124: `DetectEquivocation` (the 4th and final inner seam)** — then the W125 permanent-prune-liveness HALT. (~10 min)
- [ ] **[Sat +10min]** Commit + log (~10 min)

---

## Sunday — Rest + Weekly Ritual (event-trip debrief + Bet-candidate ranking)

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md. This week's ritual carries the trip debrief: fold new trip contacts into `notes/inbound.md` (each tagged code + date + `BET`/`BRIDGE`); reconcile `notes/ecosystem_shortlist.md` against in-person evidence (promote GO-eligible, demote in-person §6 fails, keep ≥5 §6-passing venues per the M30 gate); and produce `notes/123_bet_ranking.md` — the `BET` candidates ranked by §6 verdict + gut score + engine-scope fit + token-reality headroom, ending with an explicit `W125 outreach order:` line, a `Flip-trigger read: hold remote / watch <hub> (N_dubai=_, N_sg=_ vs N=_)` decision line, and a separate `BRIDGE desks (income floor):` list. These three files are the inputs W124 (flip-trigger evaluation) and W125 (active outreach order) consume directly.
