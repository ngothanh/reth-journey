# Week 115 — Blog #6 (hybrid CLOB / on-chain settle interface) + ecosystem shortlist build (§6 checklist)

> **Phase**: 6
> **Month**: M29
> **Track**: Half-2 venue (the founding-engineer bet artifact) — public-visibility + bet-recon leg. Half-1 Reth-spine maintenance + Tempo (RWA-collateral option) kept warm.
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: (none — this is a write-up + recon week; no upstream module is mirrored. The post DESCRIBES the `perp-dex-core` v1.0 hybrid-boundary interface shipped W104/W113 — the `SettlementId(snapshot_epoch, op_number, checksum)` async-settle signature and the off-chain-match / on-chain-settle seam — but writes no production code. The §6 ecosystem-selection checklist from `README.md` §6 is the scoring rubric this week instantiates.)
> **Crate(s) touched**: (none — `perp-dex-core` is frozen at v1.0 from W113; this week documents its hybrid-settle interface, does not edit it. The only files written are `blog/`, `notes/`, and the ecosystem shortlist artifact.)
> **Bar**: (n/a) — career/visibility/bet-recon week. The crate the blog discusses (`perp-dex-core`, the hot-path is bar (c)) is not edited here, so no implementation bar applies. The bar this week is **citability + checklist discipline**: every claim in Blog #6 sourced, every shortlisted venue scored against all seven §6 gates.
> **Feeds into**: W116 (flagship-blog draft reuses the hybrid-CLOB diagram + the `SettlementId` interface section; the ecosystem-selection filter is *applied to ≥5 venues* — this week *builds* the candidate list it filters), W117 (M30 Bet-Readiness Gate requires "shortlist ≥5 + ≥2 founding-eng convos"; this week produces the shortlist the gate counts; Blog #7 retro cites Blog #6 as a published artifact), W121 (flagship blog post links Blog #6 as the canonical hybrid-settle write-up), W122/W125 (bet-outreach pastes the Blog #6 URL + the shortlist drives the target list), W131 (Bet #1 decision applies the §6 checklist this week's shortlist pre-scored)
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): `README.md` §4A (the locked `SettlementId`=VSR-op-number interface, `finality_status→{Pending,Final,Reverted}`, settle-stub behind the async signature, the off-chain-match/on-chain-settle hybrid boundary), §6 (the seven-gate ecosystem-selection checklist — the scoring rubric), §7 (shots-on-goal + Dubai/SG geography flip-trigger), §3 row 5 (the order/event log IS the VSR log; `SettlementId`=VSR op-number = consensus-derived identity + free idempotency); your own `notes/` from W103–W113 (the as-built `perp-dex-core` settle-interface numbers + cluster-VOPR results); Hyperliquid + dYdX v4 architecture docs (hybrid off-chain-match / on-chain-settle public write-ups, for the comparison framing); Token2049 venue/team lists (for the §6 candidate pool).
> **v3 modifications**: this week was the v2 "Blog post #6: vector-db inheritance + interview-prep deepening + LeetCode-Hard / systems-design-mock job-prep" week. v3 **re-aims it entirely**: (1) **Blog #6 subject swaps** `vector-db` inheritance → **the hybrid CLOB + on-chain settle interface** (`perp-dex-core`'s `SettlementId` async-settle signature + the off-chain-match/on-chain-settle seam) — the principal capstone artifact, not the option-leg AI crate. (2) **The job-prep half is deleted** (LeetCode-Hard ramp, the "design a market-data fan-out" HFT-screen mock, applications-anchored-on-Tier-A framing) — Phase 7 is the **bet path**, not a job hunt; those hours redirect to **building the §6 ecosystem shortlist** (the candidate pool W116 filters and W117's gate counts). (3) Reth-PR + Tempo maintenance are retained as warm-keep (spine = income floor + on-chain-settlement design + L2 optionality). (4) Frontmatter re-aimed from "destination-tier HFT screens" to "the founding-engineer bet."

**The v3 framing this week locks in (read before Monday):** Blog #6 is the **public proof of the hardest, most differentiated thing the capstone does** — not "I reused crates," but **"the order/event log, the settlement journal, and the replicated state machine are ONE VSR log, and settlement identity IS the VSR op-number, so the hybrid on-chain boundary needs no side mapping table and idempotency is free."** That is the founding-engineer claim. The §6 ecosystem shortlist is the **other half of bet-readiness**: a bet is GO only if all seven §6 gates pass, so the shortlist must score *every* candidate against *every* gate — building it now (W115) means W116 *filters* a real list and W117's gate *counts* a real number, instead of scrambling. Both halves serve the M30 Bet-Readiness Gate (W117) and the Bet #1 decision (W131). No code is written; the discipline is **citability** (every blog claim has a checkable source) and **checklist completeness** (no venue half-scored).

---

## Monday — Blog #6 draft: the hybrid CLOB + on-chain settle interface

- [ ] Re-read `README.md` §4A "Settlement" block + your `notes/` from W103–W104 (the as-built `SettlementId` interface) and W113 (the v1.0 ship); pull the exact signature and the cluster-VOPR finality results into `notes/115_blog6_source_facts.md`. (~45 min)
  - 🎯 **Hardened acceptance**: the note transcribes (1) the exact `SettlementId(snapshot_epoch: u64, op_number: u64, checksum: u128)` newtype + `settle(batch) -> SettlementId` + `finality_status(SettlementId) -> {Pending, Final, Reverted}` signature as built; (2) the `Final = (op_number <= VSR commit point)` rule and the one line on why there is **no side mapping table**; (3) the measured number from W113 (e.g. settle p99 latency, cluster-VOPR scenario count that proved `Reverted` unreachable in v1.0). Every fact has a `notes/` or §4A line reference next to it.
  - *Mental model*: the blog's load-bearing claim is the §4A lock — **identity IS the VSR op-number**, so the hybrid boundary is a thin interface, not a distributed-transaction nightmare. Sourcing the exact signature + the "no side mapping table / free idempotency" property *before* drafting means the post quotes the as-built artifact, not a half-remembered design. The `snapshot_epoch` field is the subtle part (it makes the id survive log compaction / view-change) — get it transcribed exactly or the post understates the design.
- [ ] **Draft** `blog/2027-XX-XX-hybrid-clob-onchain-settle.md` — *"One log, free finality: the hybrid CLOB settle interface where SettlementId IS the VSR op-number."* Structure: (1) the problem — a hybrid perp DEX matches off-chain (sub-tick CLOB) but settles on-chain; the naive design keeps a side mapping table `op → settlement → chain tx` and reinvents idempotency; (2) the v3 insight — the order/event log, the settlement journal, and the replicated SM are the **same VSR log** (cite §3 row 5), so `SettlementId = (snapshot_epoch, op_number, checksum)` and `Final = op_number <= commit point` with **no auxiliary state**; (3) the async-shaped signature (`settle → SettlementId`, `finality_status → {Pending, Final, Reverted}`) that lets v1.5 slot a real chain + challenge window in behind the *same* signature with no engine rewrite; (4) what cluster-VOPR proved (linearizability + replica convergence + `Reverted` unreachable in v1.0). (~60 min)
  - 🎯 **Hardened acceptance**: the draft contains (1) a hybrid-CLOB architecture diagram showing off-chain-match → VSR log → on-chain settle, with the VSR log labeled as the *single* hot-path log (orders + fills + settlement); (2) the exact `SettlementId` newtype + the `settle`/`finality_status` signatures rendered as a code block; (3) the explicit "no side mapping table, idempotency is free (resubmit = no-op via `checksum` dedup)" claim; (4) the one-paragraph v1.0→v1.5 forward-compat statement (real chain + challenge window into the same signature). `ls blog/2027-XX-XX-hybrid-clob-onchain-settle.md` shows the draft.
  - *Mental model*: this is the second public proof after Blog #5 (the cross-margin + liquidation engine, W112), and together they ARE the founding-engineer portfolio narrative for the M30 gate. Blog #6's thesis — "consensus-derived identity collapses the hybrid-settle problem to a newtype" — is exactly the kind of architectural claim a tier-1-backed venue's founding conversation probes. Writing it now (vs W121) means the flagship post (W121) and the bet-outreach (W122/W125) link a *lived, already-published* artifact, and the W116 flagship draft reuses this diagram verbatim.
- [ ] Commit `blog/2027-XX-XX-hybrid-clob-onchain-settle.md` draft + `notes/115_blog6_source_facts.md`. (~15 min)
  - Output: `git log -1 --stat` shows the draft + source-facts note committed.

## Tuesday — Blog #6 edit + publish + amplify

- [ ] **Edit pass** on `blog/2027-XX-XX-hybrid-clob-onchain-settle.md` — tighten every claim; verify each quantitative or architectural assertion (the `SettlementId` field types, the `Final = op_number <= commit point` rule, the cluster-VOPR scenario count, any latency number) against `notes/115_blog6_source_facts.md` and §4A. Cut any claim you cannot point to a source for. (~30 min)
  - 🎯 **Hardened acceptance**: every number and every interface claim in the post has a checkable source noted inline or in `notes/115_blog6_source_facts.md`; in particular the `SettlementId` signature in the post is byte-for-byte the §4A signature; no claim appears ("supports N chains", "sub-Xµs settle") that you cannot trace to an as-built measurement. Edit-pass diff committed.
  - *Mental model*: a post whose thesis is "the interface is provably simple because identity = op-number" is destroyed by one unsourced claim — a reader who clicks through (a founding-eng interviewer, a tier-1 fund's technical diligence) must find the `SettlementId` claim holds against the actual code. The edit pass is what makes the post citable in W116/W117/W121/W122/W125.
- [ ] **Publish + amplify** the post to the live blog; cross-post with canonical link, X/Twitter announcement thread (lead with the "SettlementId IS the VSR op-number → free finality" hook), r/rust + relevant on-chain-derivatives/crypto-infra channels. Record live URLs + timestamps in `notes/115_blog6_distribution.md`. (~35 min)
  - 🎯 **Hardened acceptance**: `notes/115_blog6_distribution.md` lists the canonical URL + each cross-post URL with timestamps; the canonical link resolves publicly (open in a browser, confirm 200 + the diagram + the code block render). The X thread's lead tweet states the one-line thesis (op-number = settlement identity).
  - *Mental model*: distribution converts a private note into a portfolio asset and an outreach hook. The recorded URLs are the exact links pasted into W122/W125 bet-outreach and the W117 gate evidence — capturing them now avoids re-hunting. The on-chain-derivatives channels (not r/MachineLearning, as in the v2 vector-db version) are where founding-eng readers actually are.
- [ ] Commit + log. (~15 min)
  - Output: `git log -1 --stat` shows `notes/115_blog6_distribution.md` committed; weekly log updated with Blog #6 published (artifact #N toward the M30 "≥3 supporting artifacts public" bar).

## Wednesday — §6 ecosystem-selection checklist: instantiate the scoring rubric + candidate pool

- [ ] **Build (artifact, no bar — discipline = checklist completeness)**: `notes/ecosystem_shortlist.md` — instantiate the seven-gate §6 ecosystem-selection checklist as a scoring template, then populate the **candidate pool** of on-chain-derivatives venues (and, separately, crypto-MM desks — the bridge target). This is the list W116 *filters to ≥5* and W117's gate *counts*. (~total 210 min across 3 phases)
  - 🔧 **Why this matters**: a bet is **GO only if all seven §6 gates pass** — otherwise it is a bridge job, not a bet (§6). The M30 Bet-Readiness Gate (W117) requires an "ecosystem shortlist ≥5 venues passing the §6 checklist" and the Bet #1 decision (W131) applies this exact checklist. If the shortlist is half-scored — venues with founding-tier allocation but no tier-1-backing check, or a "looks exciting" venue that was never run against the remote-native gate — then W116's filter is filtering noise and W131 picks a bridge job mislabeled as a bet (the **Airwallex anti-pattern** §6 explicitly warns against: late-stage, tiny employee equity → never a bet). Build the rubric once, score every candidate against every gate, so the downstream gates count real numbers.

  - 🔧 **Decomposition prompt (silent test)**: this is a recon artifact, not code — but it has the same failure surface as a sloppy benchmark. The seven gates must each be **operationalized into a checkable signal** (gate 2 "tier-1 backing" → "which of {Paradigm, a16z crypto, Dragonfly, Polychain, 1kx, Multicoin, Jump, Wintermute} is on the cap table, with a source link?"; gate 4 "multi-billion-FDV ceiling" → "named comparables: Hyperliquid/dYdX/GMX/Variational/Ostium FDV + TAM math"). A gate scored "✅" with no source is exactly a benchmark that reports a number it didn't measure. The single-most-load-bearing discipline: **a GO requires all seven; a single ❓ is a ❌ until sourced.**

  - 🎯 **Hardened acceptance for the shortlist artifact**:
    - **Rubric gate (the lock)**: `notes/ecosystem_shortlist.md` opens with the **seven §6 gates** transcribed verbatim as a scoring template, each with its operationalized signal: (1) founding-tier allocation; (2) tier-1 backing (named fund list); (3) core-criticality (I build the engine that *is* the company); (4) multi-billion-FDV ceiling (named comparables + TAM); (5) token reality (pre-TGE, ~1y cliff/4y vest, ≥10× FDV headroom, no predatory unlock); (6) remote-native (Vietnam-remote works, no colo); (7) cycle timing (seed–Series B / pre-TGE with a cycle tailwind in the vest window).
    - **Candidate pool size**: **≥10 candidate venues** scored (so W116 can filter *to* ≥5 — a pool of exactly 5 leaves no margin if any fail diligence). Plus a **separate crypto-MM-desk bridge list** (≥3: Wintermute/Jump-style remote cash seats — the §7 income floor, scored only against gates 6+7 since the bridge isn't a bet).
    - **Per-venue scoring completeness**: every candidate has **all seven gates marked** ✅/❌/❓ — no blank cells. Every ✅ has a **source** (link, doc, conversation date). Every ❓ has a **named diligence action** (who to ask / what to find) dated for follow-up.
    - **GO/NO-GO column**: each venue gets a GO (all seven ✅) / BRIDGE (fails a bet gate but viable as floor) / NO (fails a hard gate) verdict, with the binding gate named for every non-GO.
    - **Geography tag (§7)**: each GO/near-GO venue tagged crypto-native (→ Dubai hub) or institutional/Asia (→ SG hub) — *prep tag only, no commitment* — so the W120/W123 Dubai/SG cheap-prep and the §7 flip-trigger have a list to point at.
    - **5-year failure mode**: state one shift (the niche cools / a hub becomes presence-mandatory / a tier-1 fund exits) that would force re-scoring the whole shortlist.

  #### Phase 1 — Problem, prediction, design (~50 min, no scoring yet)

  - 🎯 **Pre-prediction prompt (do this BEFORE building the rubric)** — write sealed answers in `notes/115_shortlist_prediction.md`:
    - P1: which §6 gate do you predict the MOST candidate venues will fail? (gate 1 founding-tier allocation? gate 5 token reality / vesting? gate 2 tier-1 backing?) One gate + why.
    - P2: predict how many of your top-of-mind candidate venues will turn out to be **BRIDGE not GO** (i.e. fail a bet gate but survive as income floor). One number out of your pool.
    - P3: predict which gate is hardest to *source* (find a checkable signal for) — likely gate 5 (vesting/unlock terms, often private) or gate 1 (allocation band, pre-offer). Name it + the diligence channel you'll need.
    - P4 (anti-pattern): name the one venue you are personally most excited about, then predict which §6 gate it is most likely to silently fail — the Airwallex-anti-pattern self-check (excitement ≠ a passing bet).
    - P5: **list the gates you predict you'll be tempted to mark ✅ without a real source** ("everyone knows fund X backs them"). Seal it — these are the cells to be strictest on.

  - *Mental model*: the §6 checklist is the **single reusable gate applied before ANY bet** (§6 header). Its whole value is that it is *adversarial to your own excitement* — gate 3 (core-criticality) and gate 1 (founding-tier allocation) are the two that separate a real bet from a glamorous bridge job. The rubric is a function `venue → {GO, BRIDGE, NO}` and, like a deterministic SM, it must produce the same verdict regardless of how excited you were when you scored it — which means every cell needs an external source, not a vibe.
  - *Contract* (the scoring template shape, not code):
    ```
    | Venue | G1 alloc | G2 tier-1 | G3 core | G4 FDV | G5 token | G6 remote | G7 cycle | Verdict | Binding gate | Geo tag |
    |-------|----------|-----------|---------|--------|----------|-----------|----------|---------|--------------|---------|
    | <name>| ✅/❌/❓+src| ...       | ...     | ...    | ...      | ...       | ...      | GO/BRIDGE/NO | <gate> | Dubai/SG |
    ```
    - GO ⟺ all seven ✅. Any ❌ on a bet gate (1–5) ⇒ at best BRIDGE. Any ❓ ⇒ not GO until sourced (a ❓ is a ❌ for gate-counting at W117).
  - *Back-of-envelope* (the recon cost — name it): scoring 7 gates × ≥10 venues = ≥70 cells; ~3–5 min/cell to source ≈ 4–6h of diligence — too much for one Wednesday at depth. **Therefore Phase 3 scores the pool to first-pass (every cell marked, sources for the easy gates, named diligence actions for the hard/private gates 1+5)**; deep per-finalist diligence is W116/W126. The artifact this week is *complete coverage at first-pass depth*, not *deep diligence on all ten*.

  - **The problem this artifact solves**: without a pre-built, fully-scored shortlist, the M30 gate (W117) "≥5 passing §6" is unprovable and Bet #1 (W131) gets chosen on excitement (the Airwallex anti-pattern). The shortlist makes the bet decision **legible and adversarial** — it forces every venue through all seven gates *before* a conversation, so outreach (W122/W125) targets only GO/near-GO venues and the §7 geography prep points at a real list. The failure category it prevents: a "bet" that is actually a late-employee equity job no different from a bridge.

  - **Requirements** — write in `notes/115_shortlist_design.md`:
    - R1. Transcribe all seven §6 gates verbatim as the scoring template header, each with its operationalized checkable signal.
    - R2. ≥10 candidate on-chain-derivatives venues in the pool; separate crypto-MM-desk bridge list ≥3.
    - R3. Every candidate scored on all seven gates — no blank cells.
    - R4. Every ✅ carries a source (link / doc / conversation date); every ❓ carries a named, dated diligence action.
    - R5. GO ⟺ all seven ✅; otherwise BRIDGE (fails a bet gate, viable floor) or NO (fails a hard gate), with the binding gate named.
    - R6. Geography tag (§7): crypto-native → Dubai, institutional/Asia → SG; prep-only, no commitment.
    - R7. The artifact is feed-into-ready: W116 filters it to ≥5, W117 counts it, W131 applies it.

  - **Design walk-through** — answer each in `notes/115_shortlist_design.md`:
    - D1. Operationalize gate 2 (tier-1 backing): exact fund list from §6; what counts as a source (cap-table announcement, fund's portfolio page, a press round)?
    - D2. Operationalize gate 3 (core-criticality): how do you score "I build the engine that *is* the company" *before* a conversation? (Proxy: is the venue's core a matching/risk/settlement engine, or an app/integration layer on someone else's chain?)
    - D3. Operationalize gate 5 (token reality): which fields (pre-TGE? cliff/vest? entry-FDV headroom? unlock schedule) are public vs require a conversation? Mark the conversation-gated ones as first-pass ❓ + diligence action.
    - D4. **Pre-mortem #1 (the Airwallex anti-pattern — load-bearing)**: 5-step trace where an *exciting* venue (great product, big name) is scored GO because gates 1+5 were marked ✅ on reputation, not sourced — and the consequence: Bet #1 (W131) is placed on a venue offering *late-employee equity scraps* with a predatory unlock, i.e. a bridge job mislabeled as a bet, burning a shot-on-goal (§7: only 2–3 shots, never one all-in). The fix: gates 1 and 5 require an explicit source or stay ❓ (= ❌ for the W117 count); excitement is logged separately and never substitutes for a source.
    - D5. **Pre-mortem #2 (pool too small)**: 3-step trace where the pool is exactly 5, two fail deep diligence at W116/W126, and W117's gate ("≥5 passing") fails for lack of margin. Fix: pool ≥10 at first-pass so the filter has slack.
    - D6. **Anti-shortcut audit:** name the obvious reach + why it's wrong HERE:
      - *"Mark gate 2 ✅ because I'm pretty sure fund X is in"* → wrong: an unsourced cap-table claim is the highest-stakes ❓; mark ❓ + diligence action until you have the announcement.
      - *"Score only the 5 venues I like"* → wrong: no filter margin for W116; the pool must be ≥10 so 5 *survive* filtering.
      - *"Skip the crypto-MM bridge list — it's not a bet"* → wrong: §7's income floor IS bet-readiness (runway funds the shots); the bridge list is required, just scored on gates 6+7 only.
      - *"Tag a hub now to feel decisive"* → wrong: §7 is **prep-only, no pre-commit**; the tag is for the flip-trigger to point at, not a relocation decision.
  - 🧮 **Paper drill** — write in `notes/115_shortlist_drill.md`:
    - D-WALK on ONE concrete pool venue (pick the P4 "most excited about" venue from `115_shortlist_prediction.md`): execute the seven-gate scoring function `venue → {GO, BRIDGE, NO}` by hand, gate 1→7, writing each cell's ✅/❌/❓ + its source (or named diligence action) on paper; conclude the verdict and name the binding gate. Make the invariant concrete: "GO ⟺ all seven ✅ — a single ❓ is a ❌ until sourced."
    - D-WALK on the NEGATIVE (the Airwallex anti-pattern trace): re-score the SAME venue marking gates 1+5 ✅ on reputation alone (no source), watch it flip to a false GO, then show the downstream cost (a shot-on-goal burned on a bridge job mislabeled as a bet); name the missing edge — the unsourced cell — and the fix (downgrade to ❓ + diligence action).
  - **Output**: `notes/115_shortlist_design.md` with R1–R7, D1–D6, the operationalized gate signals; plus `notes/115_shortlist_drill.md` with the by-hand scoring trace + the Airwallex-negative trace.

  #### Phase 2 — Write the falsification checks (the "failing tests" for a recon artifact, ~40 min)

  Each check is engineered so a sloppy shortlist FAILS it. Tier tags: NEWBIE / SENIOR / EXPERT. Trap shapes labeled 🪤.

  - [ ] **(NEWBIE) `no_blank_cells`** — every candidate row has all seven gates marked ✅/❌/❓; a `grep` for empty gate cells returns nothing. (Exercises R3.)
  - [ ] **(NEWBIE) 🪤 `every_check_has_a_source_or_a_diligence_action`** — the trap: a ✅ with no source link/date, or a ❓ with no named follow-up. Walk every cell; any ✅-without-source or ❓-without-action is a FAIL. (Exercises R4 — the Airwallex guard.)
  - [ ] **(NEWBIE) `pool_size_at_least_ten`** — `assert candidate_venues >= 10` and `assert bridge_desks >= 3`. (Exercises R2/D5.)
  - [ ] **(SENIOR) 🪤 `go_requires_all_seven`** — the trap: a venue marked GO with a ❓ or ❌ on any gate. Verdict logic must be `GO ⟺ all seven ✅`; spot-check every GO row against its cells. (Exercises R5.)
  - [ ] **(SENIOR) 🪤 `excitement_did_not_become_a_source`** — the Airwallex self-check: take the venue from prediction P4 (the one you're most excited about) and audit its gates 1+5 — are they ✅ on a *source* or on enthusiasm? If enthusiasm, downgrade to ❓ + diligence action. (Exercises D4.)
  - [ ] **(SENIOR) `every_non_go_names_its_binding_gate`** — every BRIDGE/NO row names the specific gate that bound the verdict. A NO with no named gate is unfalsifiable. (Exercises R5.)
  - [ ] **(EXPERT) `bridge_list_scored_on_floor_gates`** — crypto-MM desks are scored on gates 6 (remote) + 7 (cycle) only, and explicitly NOT counted toward the "≥5 passing §6" bet shortlist (a bridge is not a bet). (Exercises R2/D6.)
  - [ ] **(EXPERT) `feed_into_ready`** — the artifact is consumable by W116 (filter to ≥5), W117 (count GO venues), W131 (apply checklist): the GO count is a single grep-able number, and each GO row is self-contained enough to drive outreach. (Exercises R7.)
  - [ ] **(NEWBIE) 🪤 `geography_tag_is_prep_only`** — negative control: assert NO cell anywhere states a relocation decision or commitment; the geo tag is a prep pointer for §7's flip-trigger, nothing more. A shortlist that "decides" Dubai is over-committing per §7. (Exercises R6/D6.)

  #### Phase 3 — Score the pool, one gate at a time (~120 min)

  Ordered newbie → expert — the order you actually fill the table.

  - **(NEWBIE) When `no_blank_cells` fails because three venues only have gates 1–3 scored:**
    - Symptom: the table has GO/BRIDGE verdicts on rows with blank gates 4–7.
    - Why: scored the gates that were top-of-mind (allocation, backing) and skipped the ones needing lookup (FDV comparables, vesting).
    - Fix: fill every cell — for gates you can't source first-pass, mark ❓ + a named diligence action; never leave blank. A blank cell is an unscored gate masquerading as a pass.
    - Cost avoided: a verdict computed over partial data → a mislabeled GO. Muscle: complete coverage first, depth second; a blank is worse than a ❓.

  - **(NEWBIE) When `every_check_has_a_source_or_a_diligence_action` fails on ~half the ✅ cells:**
    - Symptom: many gates marked ✅ with no link/date — "tier-1 backed" with no cap-table source, "founding-tier allocation" with no offer band.
    - Why: scored from reputation/memory, not from a checkable signal.
    - Fix: every ✅ gets a source (announcement, doc, conversation date) or is downgraded to ❓ + diligence action. The hardest two (gates 1+5, often private) will mostly be ❓ at first-pass — that's correct, not a failure.
    - Cost avoided: an unfalsifiable shortlist that can't survive W126 diligence. Muscle: a ✅ without a source is a ❓ in disguise; sourcing is the gate.

  - **(SENIOR) When `go_requires_all_seven` fails because a venue is GO with a ❓ on gate 5:**
    - Symptom: a venue you like is marked GO despite gate 5 (token reality / vesting) being ❓.
    - Why: rounded a ❓ up to ✅ because "the rest are strong."
    - Fix: enforce `GO ⟺ all seven ✅`; a single ❓ ⇒ not GO (it's a near-GO with an open diligence item). For the W117 count, ❓ = ❌.
    - Cost avoided: an inflated GO count at W117 → a bet placed on incomplete diligence. Muscle: the conjunction is strict — seven ANDs, no rounding.

  - **(SENIOR) When `excitement_did_not_become_a_source` fires on your P4 venue:**
    - Symptom: the venue you're most excited about has gates 1+5 ✅ on enthusiasm, not a source.
    - Why: the Airwallex anti-pattern — a glamorous late-stage venue *feels* like a bet but offers employee-scrap equity.
    - Fix: downgrade gates 1+5 to ❓ + diligence action; log the excitement separately so it never substitutes for a source. The verdict drops to near-GO pending diligence.
    - Cost avoided: burning a shot-on-goal (§7: only 2–3) on a bridge job mislabeled as a bet. Muscle: the checklist exists to be adversarial to your own excitement; the more you want it, the harder you source gates 1+5.

  - **(EXPERT) When `bridge_list_scored_on_floor_gates` reveals a crypto-MM desk counted toward the bet shortlist:**
    - Symptom: a Wintermute/Jump-style desk appears in the "≥5 passing §6" count.
    - Why: conflated the income floor (bridge) with a bet — a cash seat has no founding-tier token allocation, so gates 1/3/5 don't even apply.
    - Fix: bridge desks live on a separate list scored on gates 6+7 only; they fund runway (§7), they are never the bet. Remove from the bet count.
    - Cost avoided: an inflated bet-shortlist count + strategic confusion between floor and bet. Muscle: bridge ≠ bet; the income floor is bet-*readiness*, not a shot-on-goal.

  - **(EXPERT) When `feed_into_ready` shows W116 can't filter the artifact cleanly:**
    - Symptom: the GO count isn't a single grep-able number; rows aren't self-contained enough to drive outreach.
    - Why: scoring scattered across notes; verdicts not in one column.
    - Fix: one table, one Verdict column, one binding-gate column; the GO count is `grep -c GO`; each GO row carries enough (name, the engine they're building, the tier-1 backer, the geo tag) to seed a W122 outreach message.
    - Cost avoided: re-building the list at W116/W117. Muscle: a recon artifact is only done when the *next* week can consume it without re-derivation.

  - **5-year failure mode** (write in `notes/115_shortlist_followup.md` after the table is complete): when the on-chain-derivatives niche cools (a §9 risk) OR a top venue flips to presence-mandatory (kills gate 6 for Vietnam-remote) OR a tier-1 fund exits a backed venue (kills gate 2), the whole shortlist needs re-scoring. State the trigger (a quarterly re-score cadence + an event-driven re-score on any cap-table/relocation/cycle shift) and the fallback (the §7 transferable core keeps me hireable → re-bridge → re-score → re-bet; AI-infra/DB-infra optionality via `vector-db`/`lsm-core` if the niche dies entirely).
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --stat` shows `notes/ecosystem_shortlist.md` + the design/prediction/followup notes committed.

## Thursday — Shortlist depth pass + Binance-alumni cross-reference

- [ ] **Depth pass on the top GO/near-GO venues** — take the 5+ highest-scoring venues from Wednesday's table and resolve their ❓ cells where sourceable: dig the cap table (gate 2), find public token/vesting terms (gate 5), confirm the core-engine claim (gate 3) from the venue's own architecture posts. Update `notes/ecosystem_shortlist.md` in place; downgrade any that fail on deeper look. (~50 min)
  - 🎯 **Hardened acceptance**: each of the top 5+ venues has ≥1 previously-❓ cell now resolved to ✅/❌ with a source, OR a sharpened diligence action naming the exact person/doc to ask at W126; the GO count is re-tallied and the W116-filterable number is stated explicitly in the note ("X GO, Y near-GO pending one diligence item each").
  - *Mental model*: Wednesday built complete first-pass coverage; Thursday converts the *top of the pool* toward decision-grade so W116's filter (to ≥5) and W117's count rest on resolved cells, not open ❓s. Deep per-finalist diligence is still W126 (in conversation) — this pass closes the *publicly-sourceable* gaps so the conversations start from a higher base.
- [ ] **Cross-reference the Binance-alumni map** — for each top GO/near-GO venue, note whether a warm intro path exists via the Binance-alumni map (the §7 dual-purpose network: derivatives founders AND crypto-MM desks). Record intro paths in `notes/ecosystem_shortlist.md` (who → which venue → warmth level). (~30 min)
  - 🎯 **Hardened acceptance**: every top GO/near-GO venue has an intro-path cell — either a named warm contact + warmth level, or "cold, needs event-trip/outbound" (which routes to W120 warm-network activation + W123 Token2049 recon). The W117 gate's "≥3 Binance-alumni warm intros" and "≥2 founding-eng convos" can be back-counted from this column.
  - *Mental model*: a GO verdict with no intro path is a target you can't reach; pairing the §6 score with the §7 network turns the shortlist from "venues I'd want" into "venues I can actually get a founding conversation at." This column directly seeds W120 (warm-network activation) and the W117 gate's warm-intro count.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --stat` shows the depth-pass + intro-path updates to `notes/ecosystem_shortlist.md` committed.

## Friday — Reth PR maintenance + Tempo skim (spine = income-floor + on-chain-settlement design)

- [ ] **Reth PR follow-through** — check open/in-flight Reth PRs for review comments; address any, or push the next small contribution. Log state in `notes/reth_pr_tracker.md` (PR #, status, next action, date). (~35 min)
  - 🎯 **Hardened acceptance**: `notes/reth_pr_tracker.md` reflects every open PR with a concrete dated next action; reviewer comments are either addressed (commit pushed) or recorded as a dated TODO.
  - *Mental model*: the Reth spine is the **income-floor / on-chain-settlement-design / L2-optionality** half (§2), and the crypto-infra-IC fallback bridge (§7) rests on it — momentum must not lapse in a visibility-heavy week. Merged Reth PRs are also direct evidence for the secondary bridge if the crypto-MM seat isn't taken. A weekly tracker keeps the pipeline visible so no PR rots before the W122/W125 outreach window.
- [ ] **Tempo skim** — 15-min skim of Tempo repo activity since last check; note anything RWA-collateral-relevant in `notes/tempo_watch.md` (Tempo = the RWA-perps collateral rails option leg, §2 / §3 row 20). (~15 min)
  - 🎯 **Hardened acceptance**: `notes/tempo_watch.md` has a dated entry — either "no material change" or the specific change + whether it affects the RWA-collateral-rails optionality (which feeds the capstone v1.5 RWA features at W118).
  - *Mental model*: the Tempo / RWA-collateral leg stays a live option through Phase 7 — it's the fintech edge of the three-way fit and feeds the v1.5 RWA-features narrative. A cheap weekly skim keeps it warm without derailing the primary bet path.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --stat` shows the two Friday notes committed; weekly log updated.

## Saturday — Tempo maintenance + Tái-dựng: the §4A hybrid-settle interface (closed-book)

- [ ] **Tempo maintenance** — commit + log any Tempo upkeep. (~10 min)
  - Output: `git log -1` shows the Tempo maintenance commit; weekly log note added.
- [ ] **Tái-dựng (the §4A `SettlementId` / hybrid-settle lock)** — close `README.md`. From memory, write into `notes/recall_settlement_interface.md`: (1) the `SettlementId(snapshot_epoch, op_number, checksum)` newtype + what each field is for (esp. why `snapshot_epoch` exists); (2) the `Final = op_number <= VSR commit point` rule + why there's no side mapping table; (3) the three `finality_status` states {Pending, Final, Reverted} + which is unreachable in v1.0; (4) how v1.5 slots a real chain + challenge window into the *same* signature; (5) the two projection staleness contracts (control/risk = read-after-commit from local applied state on the leader shard; analytics = bounded-staleness ≤500ms/≤1000ops + op-number token). Then diff vs §4A and annotate each miss. (~60 min)
  - 🎯 **Hardened acceptance**: ≥4/5 items recalled with the correct mechanism BEFORE opening §4A, each written to `notes/recall_settlement_interface.md`; the diff-vs-source pass annotates every miss. Bonus (over-perform): recall the *free idempotency* property (resubmit = no-op via `checksum` dedup) and the const-30%-netting-haircut rationale (correlation→1 in a crash, under-credit is the safe default).
  - *Mental model*: Blog #6 (Mon–Tue), the flagship draft (W116/W121), and every founding-eng conversation (W126+) require quoting this interface from lived knowledge, not freshly-reread docs. The closed-book-first protocol proves the knowledge is *retained* — the `SettlementId`=op-number insight is the single most differentiated architectural claim in the whole portfolio (consensus-derived identity = free finality), so it must be reflexive before the bet conversations start. This is the same recall discipline as the v2 Qdrant-quantization drill, re-aimed at the artifact that actually defines the bet.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
