# Week 137 — First month: cultural integration + token/equity vesting milestones tracked

> **Phase**: 7 (Bet Placement / Bridge — M31–M36)
> **Month**: M35
> **Track**: (career — Bet #1 landing / first month at the on-chain-derivatives venue OR crypto-MM bridge)
> **Hours target**: 30h/wk (W137–W144 window — drops from 40h/wk at W136)
> **Mirror target**: (n/a — bet-integration week; no upstream module mirrored)
> **Crate(s) touched**: (none — maintenance only on Reth spine + Tempo + the `perp-dex-core` capstone + HFT critical-path crates; no new code)
> **Bar**: (n/a — career week; the bar-(c) runtime invariant is *measured* passively via Saturday's health-check, not advanced)
> **Feeds into**: W138 first-month midpoint check (M35 mid gate: cultural-integration log + a recorded vesting/cliff schedule are the soft evidence you've *landed the bet*, not merely *started*); W139 settling-into-the-role + Bet #2 thesis seeds (a calibrated read on the venue + a tracked vesting clock are the precondition for sketching how Bet #1 upgrades Bet #2 per §7); W140 permanent housing (a stable team relationship + a real sense of the venue's trajectory is what justifies committing to a lease / the §7 hub decision); ongoing — the venue's review-culture + the §6 token-reality facts captured this week govern every PR you open and every comp conversation for the rest of the bet.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): the venue's internal engineering handbook / onboarding wiki (review-style + culture norms — lookup-when-stuck); your own prior `outreach/` logs + the §6 ecosystem-selection checklist you applied to this venue at W116/W126 (the expectations + token-reality baseline you're now calibrating against); the venue's PR archive (Tuesday review-etiquette study — read 3–5 merged PRs, not the whole archive); your grant/offer letter + the cap-table/vesting terms you accepted at W131 (Wednesday vesting-tracker — read the actual document, not memory); README.md §6 (token reality: ≈1y cliff / 4y vest, ≥10× FDV headroom, no predatory unlock), §7 (shots-on-goal: never quit a bridge before vest milestones; each bet upgrades the next), §1 bar (d) (runway tracked).
> **v3 modifications**: (1) **Frontmatter migrated to v3 conventions** (Phase 7 = bet-path, not job-landing; "new firm" → "the bet/bridge venue"; Feeds-into re-aimed at the v3 INDEX sequence W138-midpoint → W139-Bet#2-seeds → W140-housing/§7-hub). (2) **NEW first-class thread: token/equity vesting-milestone tracking** (Wednesday becomes a vesting-tracker day, displacing the v2 "first small PR" item to Thursday) — per the INDEX W137 [v3] deliverable and §6 token-reality / §7 "never quit a bridge before vest milestones." (3) Mental models re-aimed from "look like you belong at a Tier-A HFT shop" to "land the founding/core-engineer bet": culture-reading is now also *de-risking the bet* (is this venue actually what §6 promised?), and the vesting clock is the financial spine of the whole shots-on-goal structure. (4) Saturday health-check re-narrated around the `perp-dex-core` capstone deploy (the public artifact backing the bet), not a generic HFT deployment.

This is a bet-integration week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding, and over-engineering week one of a founding/core-engineer seat is itself a cultural misread. The only *code* item is a 30-minute maintenance health-check (Saturday) that proves your public `perp-dex-core` capstone + HFT deploy is still alive and still accruing the runtime hours that back the bet narrative — not new development. Every other item is integration work in two braided strands: **(A) converting "started the bet" into "landed the bet"** (relationship map, review dialect, architecture orientation, expectation calibration), and **(B) the v3 addition — pinning down the financial reality of the bet** (the vesting/cliff clock, the §6 token-reality facts, the runway math) so the shots-on-goal structure in §7 has hard numbers under it instead of offer-letter vibes. Each item carries an explicit private artifact file + an Output line that proves it done; the reflection/learning items carry a hardened-acceptance pass criterion and a mental model of *why this belongs in week one and not later*. Days are deliberately ~3–4h (30h/wk now in force): cultural and financial integration are wall-clock-paced, not effort-paced — you cannot speedrun trust, and you cannot speedrun a cliff.

---

**Monday — Team lunch + relationship mapping (who builds the engine that IS the company)**

- [ ] Artifact: `onboarding/team_map.md` (private). Build relationships across the team — accept/initiate a team lunch or coffee, and turn it into a recorded map, not just a pleasant hour. For each teammate you talk to, log: name, role/ownership area, what they're currently working on, and one personal anchor (so the next conversation isn't cold). Mark who owns the systems nearest your future work — at a derivatives venue that means the matching / risk / settlement core (the §6 core-criticality systems), not peripheral integrations. (~70 min)
  - *Mental model*: in your first month at the bet your single most valuable asset is a mental map of who-owns-what and who-to-ask — it is the difference between being blocked for a day and being unblocked in a Slack message. Writing it down (vs. trusting memory under onboarding overload) is the same discipline as your W120/W126 outreach logs: a concrete artifact beats a vibe. The personal anchor is not small talk; it is the seed of the Depth-3 relationships that, when Bet #1 vests or stalls, become the warm channel that sources **Bet #2** (§7: each bet upgrades the next — and the network from bet *N* is half of what raises the tier at bet *N+1*).
  - 🎯 Hardened acceptance: `team_map.md` lists ≥3 teammates, each with {name, ownership area, current work, personal anchor}; at least one is tagged `[owns-core]` (owns a matching/risk/settlement system — the engine that *is* the company per §6, bordering your future scope).
  - Output: `onboarding/team_map.md` exists; `grep -c '\[owns-core\]' onboarding/team_map.md` ≥ 1.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday team-map commit.

**Tuesday — Code review etiquette (study the dialect, don't perform it)**

- [ ] Artifact: `onboarding/review_culture.md` (private). Read 3–5 recently-merged PRs in the team's most-active repo and watch how senior folks review. Capture the *norms*, not the code: comment tone (blocking vs. nit vs. question), granularity (line-level vs. design-level), how disagreement is resolved, what gets auto-approved vs. heavily scrutinized, who the de-facto gatekeepers are, and the SLA expectation for turning a review around. At a small founding-stage venue also note: how much process exists at all (a 6-person team's "review culture" may be two people DMing), and whether the deterministic-core discipline you'd expect (replay-safety, no-`f64`-in-the-replicated-path) is actually enforced in their PRs or merely aspirational. (~30 min)
  - 🎯 Hardened acceptance: `review_culture.md` answers ≥4 of the 6 norm questions {tone, granularity, disagreement-resolution, scrutiny threshold, gatekeepers, review SLA}, each backed by a specific PR link or quoted comment — no generic platitudes ("they're nice"), only observed behavior with evidence.
  - *Mental model*: every venue has an unwritten review dialect, and the fastest way to look like you've-always-been-here (vs. fresh founding-hire) is to match it before you submit your first PR — not after you've been corrected on it. Studying merged PRs is a read-only, zero-risk way to reverse-engineer the dialect; performing your own style and getting style-corrected in week one is the avoidable version. This is the same "observe the contract before implementing against it" reflex the Build weeks drilled (W74 "observe the VSR apply contract before bolting on a state machine"), applied to social code. As a *core* engineer you will soon BE a gatekeeper — reading how the current gatekeepers operate is reading your own near-future job.
  - Output: `onboarding/review_culture.md` exists meeting the hardened-acceptance criteria.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday review-culture commit.

**Wednesday — [v3] Token / equity vesting-milestone tracking (pin the financial spine of the bet)**

- [ ] Artifact: `bet/vesting_tracker.md` (private). This is the v3 first-class addition and the load-bearing item of the week. Pull the *actual* grant/offer document you accepted at W131 and turn its terms into a concrete, dated milestone schedule — not a remembered approximation. Record, with exact dates: (a) **grant size + instrument** (token allocation and/or equity, and which — a founding-tier token grant and an equity grant are different clocks); (b) **cliff date** (the §6 "≈1y cliff" — the single date before which leaving forfeits everything); (c) **vest schedule** (the "≈4y vest" cadence — monthly/quarterly tranches after cliff, with the next 3 tranche dates); (d) **TGE / unlock terms** if a token (pre-TGE entry was a §6 GO condition — record expected-TGE-window + any post-TGE lockup/unlock cliff, and check for predatory unlock-dumping-on-early-contributors, the §6 red flag); (e) **entry FDV + the ≥10× headroom check** (does the entry valuation still leave the §6 ≥10× runway?); (f) **runway math** (bar (d): how many months of runway you hold *to the cliff* — if the bridge-vs-bet math changes, this is the number that decides it). Flag every term that is *worse* than what §6 required as a GO, or that you can't actually find in the document. (~110 min)
  - *Mental model*: the entire $20–100M north star is a **shots-on-goal tail bet** (§7), and a bet you can't price is a bet you can't manage. The vesting/cliff schedule is the financial spine of that structure: §7's hard rule "**never quit a bridge before the vest milestones of the live bet**" is meaningless unless you know, to the date, when those milestones are — and the cliff is the difference between "I have a founding-tier allocation" and "I have nothing because I left at month 11." Writing it down in week one (while you can still ask HR/the founder to clarify a fuzzy term without it being awkward) converts the offer letter's vibes into the numbers that drive every later decision: when to start Bet #2 outreach (§7, W139), whether to take the §7 Dubai/SG presence-premium if it appears, and when bar (d) runway is actually at risk. The §6 checklist was applied *before* you joined on *promised* terms; this is the post-join audit that the terms are real — the same "predict, then check reality, then log the drift" loop the Build weeks ran, applied to your own comp.
  - 🎯 Hardened acceptance: `vesting_tracker.md` records all six items {grant size+instrument, cliff date, next-3 vest tranche dates, TGE/unlock terms, entry-FDV + ≥10×-headroom check, runway-months-to-cliff} with exact dates pulled from the actual grant document; every term that deviates from the §6 GO conditions (1y cliff / 4y vest / pre-TGE / ≥10× headroom / no predatory unlock) is explicitly flagged with a one-line note + an owner-to-ask; and a calendar reminder is set for the cliff date and the next vest tranche (Output line proves it).
  - Output: `bet/vesting_tracker.md` exists meeting the hardened-acceptance criteria; a cliff-date + next-tranche reminder exists (e.g. `grep -i 'cliff' ~/.calendar-or-equivalent` or a `bet/vesting_reminders.ics` committed alongside).
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday vesting-tracker commit.

**Thursday — First small contribution (ramp via a real PR, in their dialect)**

- [ ] Artifact: `onboarding/first_pr.md` (private log) + one real PR in the venue's tracker. Pick the smallest genuinely-useful task you can fully own — a doc fix, a flaky-test stabilization, a small bug, a starter-issue — and open a PR for it, deliberately matching the review dialect captured Tuesday. The goal is not impact; it is to exercise the full local→CI→review→merge loop once while the stakes are tiny. Log: the task, the PR link, and which Tuesday-norm you consciously applied. (~120 min)
  - *Mental model*: the first PR's purpose is to de-risk the *pipeline*, not to impress — you want to discover the CI quirks, the required reviewers, the merge-queue rules, and the local-build gotchas on a change small enough that a mistake costs nothing. Founding/core engineers who wait for the "right big task" to make their first contribution stay unintegrated for weeks; shipping something tiny in week one converts you from observer to participant and gives the team a concrete reason to start trusting your judgment — which matters more here than at a big firm, because at a 6-person venue *your* judgment is load-bearing on the core engine within the quarter. This also surfaces the truth Tuesday only sketched: does their CI actually enforce the deterministic-core discipline, or will that be *your* job to introduce?
  - 🎯 Hardened acceptance: a PR is open (or merged) in the venue's tracker; `first_pr.md` names the task + PR link + the one Tuesday-norm consciously applied (e.g. "used a `question:` prefix per their non-blocking convention") + one observation about whether their CI enforces replay-safety/no-`f64` on the replicated path.
  - Output: `onboarding/first_pr.md` exists with a PR link; the PR is visible in the venue's tracker.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday first-PR commit.

**Friday — Architecture orientation + reflection: how does the bet venue differ from prior employers?**

- [ ] Artifact: `onboarding/system_map.md` (private). Spend the first part of the session orienting to ONE production system you'll touch soon — at this venue, ideally the matching / risk / settlement core (the §6 core-criticality system that is the company). Read its README/design doc, sketch its data flow (inputs → core transform → outputs), and write down the 3 things you don't yet understand as explicit open questions to ask their owner (from Monday's `[owns-core]` tag). Do not try to map the whole codebase — depth on the engine you were brought in to build beats breadth on ten peripheral services. (~70 min)
  - *Mental model*: a derivatives-venue codebase is too large to absorb by osmosis; the productive move is to go deep on the engine nearest your future work and ask *good* questions of its owner — a precise "I see the order flow feeds the matching path but I can't find where the risk engine reads the committed position state" earns respect from a core engineer, whereas "can you explain the whole thing?" signals you haven't done the homework. You spent W74→W113 building *your own* version of exactly this engine (`perp-dex-core`); use that as the lens — map their design against your locked v1.0 invariants (one hot-path log? read-after-commit for risk? incremental margin at scale?) so your open questions are sharp and your future contributions are informed, not naive.
  - 🎯 Hardened acceptance: `system_map.md` contains a data-flow sketch (inputs/transform/outputs) for ONE named core system plus exactly 3 specific open questions, each addressed to a named owner from `team_map.md`, and at least one question explicitly contrasts their design with a `perp-dex-core` v1.0 invariant you locked (e.g. "do they keep the order log and the settlement journal as one log, or two?").
  - Output: `onboarding/system_map.md` exists with the data-flow sketch + 3 owner-addressed open questions.
- [ ] Artifact: `onboarding/calibration.md` (private). Calibrate expectations: note what's *better*, what's *different* (explicitly framed as different, not worse — the week-one instinct to read difference as deficiency is the trap), and — the v3 addition — whether the venue is *actually what §6 promised* now that you're inside. Compare against the baseline you set at W116/W126 (the §6 checklist you applied to this venue) and your 12 years of prior employers along concrete axes: decision speed, on-call/incident culture, code-quality bar, autonomy vs. process, communication channels, pace expectations, and **core-criticality reality** (are you actually building the engine that is the company, or did the role drift toward integration work — §6 condition 3). (~30 min)
  - 🎯 Hardened acceptance: `calibration.md` covers ≥4 axes; every "different" entry is phrased neutrally with a concrete observation behind it (not "worse" / "annoying"); at least one entry explicitly flags a prior assumption that turned out wrong (so the calibration is real, not confirmation of priors); and exactly one entry answers "is the §6 core-criticality promise holding?" with evidence — because if the bet is quietly turning into a non-bet, week one is when you want to know, while Bet-#2 outreach and the bridge are still warm.
  - *Mental model*: the most common way a senior transplant self-sabotages in month one is importing the prior firm's norms as universal truths and silently judging the new venue against them ("at my old shop we'd never ship this"). Forcing yourself to log differences as *neutral* — and to catch at least one wrong prior assumption — rewires that reflex into curiosity, which is what actually lets you absorb the venue's strengths instead of resisting them. The §6 core-criticality check is the *bet-specific* layer on top: this is the human version of the Build weeks' "predict, then check reality, then correct the drift" loop, run on both the culture and the thesis. A bet that fails the §6 check in month one is not a sunk cost — §7 explicitly keeps the bridge and Bet #2 in reserve precisely for this.
  - Output: `onboarding/calibration.md` exists meeting the hardened-acceptance criteria.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday orientation+calibration commit.

**Saturday — Personal-artifact maintenance + capstone runtime-hours health-check**

- [ ] Artifact: append to `notes/runtime_hours_log.md` (private). [HFT] The runtime-hours invariant (bar (a): ≥4000 hours by M36) and the public `perp-dex-core` capstone that *is* your bet credential are both advancing passively on your already-deployed crates — but "passively" only holds if the deployment is actually alive. Run a 30-minute health-check: confirm the long-running `perp-dex-core` 3-node VSR cluster + Reth/Tempo + HFT critical-path processes are still up, scrape the current cumulative runtime-hours figure, and record it with the date so the M36 bar-(a) gate and the blog/flagship-artifact narrative are evidence-backed, not assumed. If anything has crashed/stalled — including an insurance-fund-tripwire alert or a VSR replica that fell out of quorum — that's the finding: log it and restart, don't paper over it. (~30 min)
  - 🎯 Hardened acceptance: `runtime_hours_log.md` has a new dated entry with the current cumulative hours figure and a per-process up/down status (incl. each VSR node + the capstone's cluster-VOPR/insurance-fund tripwire monitor); the figure is on track for ≥4000 by M36 (if not, the entry names the gap + the restart action taken).
  - *Mental model*: the 4000+-hour-by-M36 invariant is a *measured* claim — bar (c) crates earn it by actually running, and an unobserved deployment that silently died three weeks ago fails the milestone without warning and rots the one public artifact backing your founding-engineer credibility. A cheap weekly health-check is the back-pressure-as-correctness reflex applied to your own portfolio: at 30h/wk during bet-onboarding you cut maintenance time hard, but never to zero, or the runtime evidence + the live capstone that back your whole bet narrative decay unnoticed exactly when a Bet-#2 conversation or a comp review might ask to see them.
  - Output: `notes/runtime_hours_log.md` shows a new dated entry with cumulative hours + per-process (incl. per-VSR-node) status.
- [ ] Commit + log (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday maintenance commit.

## [v3.1 BFT apex — Track B] W137: the live VSR→BFT swap into `perp-dex-core` (= v2) — the four-type-param change

> Track B continues (W135 met latency-X). **This is the milestone the entire `ConsensusBackbone` design existed for**: swap `perp-dex-core`'s consensus backend from VSR to the BFT apex by changing the **four type params** — `Replica<SM, CrashQuorum, TrustedPeers, RoundRobinView, AssumeNoEquivocation>` → `Replica<SM, BftQuorum, SignedPeers, PipelinedHotStuffView, DetectEquivocation>` — with **zero edits** to matching/risk/liquidation/ledger/apply/settle. Bar (c). The result is `perp-dex-core` **v2 = BFT-replicated**.
- [ ] [Venue · BFT] **Build (bar c)**: wire `perp-dex-core` onto the `BftBackboneAdapter` (W128) in place of the VSR adapter — a one-line constructor change at the composition root (`PerpDexCore::new_on(BftBackboneAdapter::new(4))`), and prove the engine code is untouched. (~total 180 min)
  - 🔧 **Why this matters**: a non-empty engine diff here means 30 weeks of seam discipline (W91 four seams + O5 amendment + W103 acceptance #9 + W119–W128 BFT impls) leaked somewhere — the swap was supposed to be a *backend* change. An empty diff is the apex's defining proof and the strongest founding-engineer artifact: "I built the venue on a consensus interface so I could promote CFT→Byzantine consensus without rewriting the engine." The trap: a latent `consensus_vsr::` reference in the engine (W103 acceptance #9 not fully held) surfaces only now → a compile error or a forced edit; the fix is to route it through the trait (it should already be, from W103). Note: matching can stay optimistic/local; only settlement/state needs the BFT commit — so the swap doesn't regress matching latency.
  - **Tests:**
    - `perp_dex_core_v2_runs_on_bft` — `perp-dex-core` boots on a 4-node `BftReplica` cluster; ingests orders/marks/settles; commits via BFT; `SettlementId`/`finality_status` resolve from `commit_point()` (impl-agnostic, W104/W128).
    - `swap_is_four_type_params_only` — `git diff` of `perp-dex-core/src/` between the VSR-bound and BFT-bound builds touches **only** the composition-root constructor line; matching/risk/liquidation/ledger/apply/settle/read are byte-identical. **This is the apex acceptance's zero-rewrite criterion (W143).**
    - `v2_acceptance_ladder_holds_under_bft` — the W117 acceptance ladder (#1 netting, #3 fund waterfall, #4 SettlementId, #5 two projection contracts, #9 trait-binding) all still pass with the BFT backend — the venue's correctness is consensus-impl-independent.
  - **Make-pass card (EXPERT · the apex claim):** `swap_is_four_type_params_only` shows an engine edit → symptom: the BFT swap forced a change in `apply.rs`/`settle.rs`; why: a latent VSR coupling (a `view`/`op_num` assumption, or a direct `consensus_vsr` import the W103 grep missed); fix: route it through `ConsensusBackbone` (`commit_point()`/`drain_committed`/`drain_abandoned`), delete the coupling; cost avoided: the apex degrading from "swap" to "rewrite" — the single worst outcome, invalidating the whole seam thesis; muscle: a backend swap is empty-engine-diff or it isn't a backend swap. Reapplies: W138 v2 VOPR, W143 acceptance.
  - 🧮 **Paper drill** — write in `notes/137_perp_dex_core_v2_swap_drill.md`:
    - D-SEAM on the `ConsensusBackbone` trait + its two impls (`Replica<…,CrashQuorum,…,RoundRobinView,AssumeNoEquivocation>` VSR vs `Replica<…,BftQuorum,…,PipelinedHotStuffView,DetectEquivocation>` BFT): list the trait's methods (`commit_point`/`drain_committed`/`drain_abandoned`); audit that no quorum/auth/view/equivocation vocabulary leaks into the outer trait or the `apply.rs`/`settle.rs` call sites; show the BFT impl type-checks against the SAME trait + the single `PerpDexCore::new_on(BftBackboneAdapter::new(4))` call site with zero engine edits; conclude swap-safe + the inner type-params const-fold to one outer commit-batch indirection.
    - D-QUORUM on the BFT backend it swaps onto — 4 replicas (3f+1, f=1), a fixed delivery order, a leader-crash mid-prepare: draw the logs; mark the `2f+1`=3 quorum and show two quorums intersect in ≥1 honest node; trace one settlement `prepare → quorum → commit` resolving `commit_point()`; inject the leader crash and run the view-change (PipelinedHotStuff QC/timeout) to a new leader; verify safety (no two conflicting commits ⇒ the v1.0 netting/fund-waterfall ladder cannot double-settle) + liveness. Negative: a `f+1`-only "quorum" that doesn't intersect → the split-brain double-commit the BFT swap must forbid.
  - **Output**: `perp-dex-core` v2 (BFT-replicated) boots and trades on a 4-node BFT cluster; `swap_is_four_type_params_only` green (**empty engine diff — the apex's defining proof**); the v1.0 acceptance ladder holds under BFT.
- [ ] [Venue · BFT] Handoff note → `notes/137_perp_dex_core_v2.md`: VSR→BFT swap complete, four-type-params only, empty engine diff. **Next W138: re-run the full cluster-VOPR (W108) against perp-dex-core v2; W143: apex acceptance.** (~10 min)
- [ ] Commit + log (~10 min)

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
