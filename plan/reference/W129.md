# Week 129 — [v3] Remaining finalist conversations (founding/core-eng bet rounds + crypto-MM bridge tail)

> **Phase**: 7 (Bet Placement / Bridge)
> **Month**: M34
> **Track**: Bet-path (founding/core-engineer token-equity bet on an on-chain derivatives venue) — final-round bet conversations; crypto-MM cash-seat bridge in parallel
> **Hours target**: 40h/wk (W118-W136 window; drops to 30h/wk at W137)
> **Mirror target**: n/a (bet-path week — the conversation *is* the artifact; no upstream module to mirror)
> **Crate(s) touched**: n/a for the bet-path track (capstone maintenance only — keep `perp-dex-core` v1.0 + cluster-VOPR green and demo-ready; no new code, do NOT regress into a Build). **Track B (additive):** `model-check` [NEW v3.2, C2 — BFT model milestone] (Stateright model of the BFT apex: Jolteon 2-chain commit + TC view-change at N=4/f=1; safety-under-equivocation + liveness-after-GST; pairs 1:1 with the 8 Byzantine VOPR scenarios) + `ops-chaos` (`sim-storage`⊕Byzantine fault composition folded into the Byzantine VOPR — a Byzantine node AND a lying disk at once, C4)
> **Bar**: (n/a) — bet-path week, no shippable crate, no engineered trap-test arc; manufacturing one would be padding
> **Feeds into**: W130 bridge safety net (this week's `bet_pipeline.md` health verdict decides whether the crypto-MM cash seat gets activated); W131 **Bet #1 decision** (the consolidated `bet_decision_matrix.md` written Saturday — each venue scored against the §6 seven-gate checklist — is the single GO/NO-GO input); W132 resignation + (conditional) relocation logistics (the §7 Dubai/SG flip-trigger evaluation seeded here tells W132 whether a move is even on the table). README.md §5 (M36 gate: "Bet #1 placed OR bridge engaged"), §6 (ecosystem-selection checklist), §7 (shots-on-goal + geography flip-trigger).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): README.md **§6** (the seven-gate bet checklist — the spine of every conversation this week), **§7** (shots-on-goal structure + Dubai/SG flip-trigger), **§4A** (the `perp-dex-core` v1.0 lock — your deep-dive ammunition: 3-node VSR / the order log IS the VSR log, `SettlementId`=VSR op-number, const-30%-haircut cross-margin, fee-accrued insurance fund + waterfall + tripwire-never-hit, two projection staleness contracts, incremental-margin hot path at 100k accounts/1M orders/s, RWA-aware seams); your own `bet_prep/` dossiers from W120/W125-W128 (the venue recon, the §6 pre-scores, the Binance-alumni warm-intro map); flagship blog post (W121) + Blog #5 (W112, the cross-margin + liquidation engine) as the artifacts to point founders at; `risk-engine` (W97-W99) + `liquidation-engine` (W100-W102) + `perp-dex-core` (W103-W113) as the deep-dive vehicles you already shipped and cluster-VOPR'd; `relocation_research.md` (W120) for the hub visa/token-tax facts.

> **v3 modifications**: this file was the **superseded v2 plan** ("Final rounds, remaining firms + cycle close-out" — a job-landing/destination-landing week framed as employer-evaluates-candidate interviews, with a matching-engine deep-dive and an offer-decision matrix). v3 **re-aims it from job-landing to the bet-path** per INDEX W129 = "Remaining finalist conversations" and §5/§6/§7: (1) the rounds are **bet conversations**, not interviews — the load-bearing direction is reversed, **YOU evaluate the venue against the §6 seven-gate checklist** (allocation tier / tier-1 backing / core-criticality / FDV ceiling / token reality / remote-native / cycle timing) as hard as they evaluate you, because a bet that fails §6 is a *bridge job, not a bet*. (2) The deep-dive vehicle is upgraded from matching-engine to the **`risk-engine` cross-margin + `liquidation-engine` engine** (the principal-defining convergence-cell artifact, §3 row 9) on the **`perp-dex-core` v1.0** capstone — defended on the §4A locked invariants. (3) A **crypto-MM cash-seat bridge** track runs *in parallel* (§7 income floor underwritten by Half-2 latency craft), not as a fallback afterthought. (4) The Saturday output is a **§6-scored `bet_decision_matrix.md` + a `bet_pipeline.md` GO/NO-GO posture + the §7 Dubai/SG flip-trigger evaluation** — the inputs to W130's bridge decision and W131's Bet #1 GO/NO-GO, replacing the v2 "rank firms by fit" offer matrix.

This is an execution week, not a Build week — the continuation and tail of the W128 finalist cycle (venues 4, 5, 6, plus any parallel crypto-MM bridge round, each conditional on the W125 stagger schedule). There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding, and capstone code work this week is **maintenance only** (keep `perp-dex-core` v1.0 + cluster-VOPR green and demo-ready so a founder can be pointed at a live, passing artifact). The durable output each day is a **structured §6-scored debrief** captured the same evening while memory is fresh — a bet conversation's only durable output is the evidence you record, and that evidence is the sole input to W130's bridge decision and W131's GO/NO-GO. The new discipline this week, versus W128, is the **Saturday cross-venue §6 consolidation + the flip-trigger evaluation**: by the end of the week you must be able to say, per venue, GO-bet / bridge-only / pass — on §6 evidence, not recency — and whether the §7 Dubai/SG move trigger has fired.

Day estimates below cover the debrief + targeted recovery prep; the live conversation/work-trial itself is a full external block and is not double-counted in the hour budget. **"If scheduled" days are conditional** — if the W125 stagger left a slot empty, convert it to recovery + capstone maintenance + crypto-MM bridge prep; do NOT pad.

Each finalist bet debrief uses the same fixed template so Saturday's cross-venue §6 ranking can pattern-match across all venues:

```
## Venue N — <venue name> — <date> — finalist conversation / work-trial
### Technical deep-dive (the principal artifact)
- What I walked them through: risk-engine cross-margin (const-30% haircut netting BTC+ETH) / liquidation-engine (fee-accrued fund + waterfall + tripwire-never-hit) / perp-dex-core (3-node VSR; order log IS the VSR log; SettlementId=op-number; two staleness contracts; incremental margin @100k accts)
- Which §4A invariant I defended | Where they pushed | Delta→close-out (one drill)
### Their architecture / what they're building
- Their matching/risk/settlement stack | where MY engine maps onto it | core-criticality read: am I building the engine that IS the company, or a peripheral integration?
### Founder/team read
- Who I'd report to / build alongside | engineering bar | remote-native reality (Vietnam-remote actually works? colo/on-site creep?)
### §6 seven-gate scorecard (the load-bearing section — I evaluate THEM)
- 1 Founding-tier allocation: <band / unknown — ask> | 2 Tier-1 backing: <which of Paradigm/a16zcrypto/Dragonfly/Polychain/1kx/Multicoin/Jump/Wintermute, or none> | 3 Core-criticality: <yes/no + evidence> | 4 Multi-B FDV ceiling: <TAM + comparable: Hyperliquid/dYdX/GMX/Variational/Ostium> | 5 Token reality: <pre-TGE? cliff/vest? entry FDV ≥10× headroom?> | 6 Remote-native: <yes/no> | 7 Cycle timing: <seed–B / pre-TGE + tailwind?>
## §6 verdict: GO-bet (all 7 pass) / bridge-only (≥1 fails) / pass | ## vs prior venues: <stronger/weaker on allocation, backing, core-criticality, token headroom, remote>
```

The §6 verdict line is the whole point: **a venue is a GO-bet only if all seven gates pass; otherwise it is a bridge job, not a bet** (§6). Recording the verdict per venue the same night is what makes W131's GO/NO-GO a lookup, not a re-derivation under decision pressure.

---

**Monday — Finalist conversation / work-trial: venue 4 (founder + 2-3 core eng: technical deep-dive on the risk/liquidation engine, their-architecture walk, founder read, §6 probing)**

- [ ] Pre-flight: re-read `bet_prep/venue4_dossier.md` (who you'll talk to, their stack, their cap-table backers, and the **§6 questions you must get answered** — allocation band, backers, core-criticality, token vesting/cliff, remote reality). Skim `bet_prep/patches.md` so venue-1/2/3 fumbles don't repeat. (~30 min)
  - 🎯 Hardened acceptance: dossier names everyone in the conversation + their role; lists **≥3 venue-specific technical questions** (each tied to W123-W124 event-trip recon or the venue's public engineering writing / docs / GitHub — never generic "what's the culture"); AND carries an explicit **§6 information-gap list** naming which of the seven gates are still `[unknown — ask]` for venue 4 and the exact question that closes each (e.g. "gate 5 token reality: ask the FDV at last raise + cliff length"). Notes which carried-in patches apply to venue 4's likely deep-dive.
  - *Mental model*: a finalist bet conversation is **bidirectional and the direction that matters is reversed from a job interview** — you are sourcing the §6 evidence that decides GO/NO-GO at W131, and the founders who matter respect a candidate who underwrites the bet as hard as they underwrite the hire. The §6 gaps are the cheapest, highest-signal questions you can ask; leaving gate 1 (allocation band) or gate 5 (token reality) `[unknown]` past a finalist round is how people accept "bridge jobs dressed as bets" (the Airwallex anti-pattern, §6 gate 1). By venue 4 the pre-flight is mostly recall of an accumulating patch + §6-gap list, not a fresh scramble.
  - Output: `bet_prep/venue4_dossier.md` updated with the people list + ≥3 venue-specific technical questions + the §6 information-gap list + applicable-patch note; `git log -1 --oneline` shows the pre-flight commit.
- [ ] Same-evening debrief: write `bet_debriefs/venue4_final.md` while memory is fresh, using the fixed template. (~50 min)
  - 🎯 Hardened acceptance: debrief covers all four template sections (technical deep-dive / their-architecture / founder read / **§6 seven-gate scorecard**); the technical section records **which §4A invariant you defended** (e.g. const-30% haircut is deterministic-by-design so VSR replicates it bit-identically; the order log IS the VSR log = "no second hot-path log"; `SettlementId`=op-number = free idempotency; tripwire-never-hit + the v1.5-ADL seam) **vs where they pushed**; the §6 section fills all seven gates with a value or an explicit `[unknown — carry to W130]`; ends with a `## §6 verdict:` line (GO-bet / bridge-only / pass) and a `## vs prior venues` comparison line.
  - *Mental model*: the cross-margin + liquidation deep-dive is where the 128 prior weeks pay off or don't — a founder building a perp DEX probes whether you *own* the convergence cell (per-mark-tick portfolio recompute at 100k accounts, incremental margin mandatory not optional, the fee-accrued fund waterfall, the tripwire that *defines* the v1.5 ADL trigger) or just recite it. Writing the §6 verdict the same night, while venue 4 is vivid against venues 1-3, is what keeps W131's GO/NO-GO evidence-based instead of recency-biased — and it forces the honest call: a higher-comp venue that fails gate 5 (predatory unlock) ranks *below* a smaller one that passes all seven, because §6 is pass/fail, not a weighted average.
  - Output: `bet_debriefs/venue4_final.md` exists with 4 sections + `## §6 verdict` + `## vs prior venues` + a `## Close-out deltas` list of ≥2 items; `git log -1 --oneline` shows the Monday debrief commit.
- [ ] Capstone-maintenance touch (keep the artifact demo-ready). (~15 min)
  - 🎯 Hardened acceptance: confirm `perp-dex-core` v1.0 still builds and cluster-VOPR is green (or has a dated known-issue note); the flagship blog (W121) + Blog #5 (W112) links in `bet_prep/artifact_links.md` resolve — so any founder you point at a live artifact lands on a passing one, not a broken link mid-cycle.
  - *Mental model*: in a bet conversation the artifact *is* the resume; a green, live `perp-dex-core` + cluster-VOPR is the single highest-credibility thing a founder can click, and letting it rot mid-cycle undercuts the exact narrative you're selling. Maintenance here is keeping the bet's strongest evidence alive, not busywork.
  - Output: `bet_prep/artifact_links.md` checked; `git log -1 --oneline` shows the Monday maintenance/debrief commit.

**Tuesday — Recover + targeted patch from venue 4 deltas + crypto-MM bridge prep**

- [ ] Recover deliberately: no bet prep before noon — protect cognition for any Wednesday round. Then triage Monday's deltas: pick the 1-2 highest-leverage items and do a focused 60-min repair (re-derive the §4A invariant you fumbled, re-draw the architecture you got pushed on). (~75 min)
  - 🎯 Hardened acceptance: a one-line dated `recovery_log.md` entry confirms the morning was off-prep; each chosen delta from `venue4_final.md` is closed in `bet_prep/patches.md` with a dated, source-venue-labeled corrected answer or re-drawn diagram — a concrete artifact, not "reviewed"; if a venue-4 delta recurs across earlier venues, tag it `[recurring]` (recurring technical fumbles on the convergence cell are the highest-priority fix before venues 5/6).
  - *Mental model*: bet-conversation performance is a depletable resource like the hot path's L1 cache — running back-to-back finalist rounds with zero recovery is the human equivalent of thrashing, so the morning-off rule is back-pressure-as-correctness applied to yourself. A fumble appearing at two independent venues is a real gap in your model of your own engine (e.g. you can't crisply justify *why* the haircut is a const-30% and not rolling-empirical — the determinism/VOPR rationale in §4A), not bad luck; tagging recurrence forces a root-cause fix over memorizing one answer.
  - Output: `bet_prep/recovery_log.md` has a dated Tuesday entry; `bet_prep/patches.md` has the Tuesday patch entries with any `[recurring]` tags; `git log -1 --oneline` shows the Tuesday patch commit.
- [ ] Crypto-MM bridge prep (the income floor runs in parallel, §7): refresh `bet_prep/bridge_cryptoMM.md` — which desks (Wintermute / Jump-style / crypto-MM) are still live in the bridge pipeline, what the latency-craft pitch is (`latency-lab` tick-to-trade, `matching-engine` <1µs p99, `marketdata-kernelbypass`), and the next concrete action per desk. (~30 min)
  - 🎯 Hardened acceptance: `bet_prep/bridge_cryptoMM.md` lists each live crypto-MM desk with {stage, latency-craft hook tied to a specific shipped artifact, next action + by-when}; states the runway math (§1 bar (d): ≥6-12 months at bet entry) so W130 knows whether the bridge is *needed now* or *held in reserve*.
  - *Mental model*: §7 makes the crypto-MM cash seat the **primary bridge** — the income floor that funds runway *between* bets — not a consolation prize. Keeping it warm in parallel means if Saturday's pipeline verdict is "no GO-bet yet," W130 can activate the bridge from a live conversation, not a cold start. The bridge and the bets share one Binance-alumni map (§2), so this is cheap to maintain.
  - Output: `bet_prep/bridge_cryptoMM.md` updated; `git log -1 --oneline` shows the Tuesday bridge-prep commit.

**Wednesday — Finalist conversation / work-trial: venue 5 (full block, if scheduled)**

- [ ] Decision gate first: confirm whether venue 5's finalist round is scheduled this week (per the W125 stagger plan). Record the gate decision (yes/no + one-line reason) at the top of today's note. If NOT scheduled, convert this day to recovery + capstone maintenance + crypto-MM bridge advancement and skip the conversation items below — do not pad. (~10 min)
  - 🎯 Hardened acceptance: `bet_prep/venue5_gate.md` opens with `Venue 5 finalist scheduled: yes/no — <reason>`; if no, it names the concrete work done instead (e.g. "advanced Wintermute bridge convo to a take-home; re-ran cluster-VOPR, green").
  - *Mental model*: the W125 stagger deliberately spread finalist rounds so no week holds more than two live deep-dives — venue 5 may legitimately fall in W130. Writing the gate down keeps the conditional honest and prevents inventing a conversation that wasn't booked just to fill the day; an empty slot here is the stagger control working (§9 "space conversations, drop weakest opportunities"), and it converts cleanly into bridge progress, which is never wasted.
  - Output: `bet_prep/venue5_gate.md` records the yes/no decision; `git log -1 --oneline` shows the gate commit.
- [ ] (If scheduled) Pre-flight: re-read `bet_prep/venue5_dossier.md` + skim `bet_prep/patches.md`, with the §6 information-gap list loaded so you close the open gates this round. (~30 min)
  - 🎯 Hardened acceptance: venue 5 dossier meets the venue-4 bar (people + ≥3 venue-specific technical questions + §6 information-gap list with the closing question per `[unknown]` gate), AND notes which Tuesday patches apply to venue 5's likely deep-dive; any `[recurring]` tag is rehearsed first.
  - *Mental model*: venue 5 should be measurably sharper than venue 4 because you arrive with three §6-scored debriefs and a patch list — the cycle is a learning loop, and the pre-flight is where last round's lesson (and the still-open §6 gates) get loaded before they can repeat or stay unanswered.
  - Output: `bet_prep/venue5_dossier.md` updated to the venue-4 bar; `git log -1 --oneline` shows the pre-flight commit.
- [ ] (If scheduled) Same-evening debrief: write `bet_debriefs/venue5_final.md` using the fixed template. (~50 min)
  - 🎯 Hardened acceptance: same 4 sections as venue 4, PLUS the `## §6 verdict` line and a `## vs prior venues` line ranking venue 5 against venues 1-4 on allocation tier, tier-1 backing, core-criticality, token headroom, and remote-native reality; every §6 gate filled or explicitly `[unknown — carry to W130]`.
  - *Mental model*: §6 evidence is only capturable while the round is fresh; by W131 the venues blur. Logging the seven-gate scorecard now is what makes the Bet #1 decision a pass/fail lookup rather than a recency-driven guess under deadline.
  - Output: `bet_debriefs/venue5_final.md` with 4 sections + `## §6 verdict` + `## vs prior venues` + `## Close-out deltas`; `git log -1 --oneline` shows the Wednesday debrief commit.

**Thursday — Recover + targeted patch from venue 5 deltas (or bridge + maintenance if venue 5 not scheduled)**

- [ ] Recover, then triage Wednesday's deltas and repair the top 1-2 before any Friday round. If venue 5 was not scheduled, spend this as a deliberate recovery + crypto-MM bridge advancement + capstone maintenance day and log it as such. (~75 min)
  - 🎯 Hardened acceptance: if venue 5 ran, each chosen delta is closed in `bet_prep/patches.md` with a dated, sourced corrected answer, any cross-venue recurrence tagged `[recurring]`; if venue 5 did not run, `bet_prep/recovery_log.md` has a dated Thursday entry naming the bridge/maintenance work done (e.g. "Wintermute take-home submitted; perp-dex-core rebased green") — no empty or padded item.
  - *Mental model*: the recovery day is capacity management for the part of the week that moves the bet needle — three-plus finalist deep-dives with no recovery is how candidates arrive depleted at the last (possibly best-fit) venue and underperform on the convergence-cell defense. Patching a measured fumble is far higher ROI than broad re-study; this is the debug loop applied to yourself — Wednesday produced the failing tests (stumbles), Thursday makes exactly those pass.
  - Output: `bet_prep/patches.md` (Thursday entries) or `bet_prep/recovery_log.md` (bridge/maintenance entry) updated; `git log -1 --oneline` shows the Thursday commit.

**Friday — Finalist conversation / work-trial: venue 6 (full block, if scheduled)**

- [ ] Decision gate first: confirm whether venue 6's finalist round is scheduled (per W125 stagger). Record yes/no + one-line reason at the top of the note. If NOT scheduled, convert to recovery + bridge + maintenance and skip the conversation items — do not pad. (~10 min)
  - 🎯 Hardened acceptance: `bet_prep/venue6_gate.md` opens with `Venue 6 finalist scheduled: yes/no — <reason>`; if no, names the bridge/maintenance/recovery work done instead.
  - *Mental model*: same stagger-honesty rule as Wednesday — venue 6 is the tail of the staggered schedule and may slip to W130; an unfilled slot here is the §9 risk control (don't bunch finalist rounds) doing its job, not a hole to plug. If empty, it is the cleanest day to push a crypto-MM bridge conversation forward, which §7 says is the income floor either way.
  - Output: `bet_prep/venue6_gate.md` records the yes/no decision; `git log -1 --oneline` shows the gate commit.
- [ ] (If scheduled) Pre-flight + same-evening debrief: re-read `bet_prep/venue6_dossier.md` + patches + §6-gap list, run the round, then write `bet_debriefs/venue6_final.md`. (~80 min: 30 pre-flight + 50 debrief)
  - 🎯 Hardened acceptance: dossier meets the venue-4 bar; debrief has all 4 sections + `## §6 verdict` + `## vs prior venues` ranking venue 6 across the full field; every §6 gate filled or `[unknown — carry to W130]`; any `[recurring]` technical fumble that survived to the last venue is flagged as a known, characterized limitation (not a surprise).
  - *Mental model*: venue 6 is the cycle's last data point — the goal is no longer to learn for a next round but to capture the cleanest possible §6 record, because tomorrow's consolidation, W130's bridge decision, and W131's GO/NO-GO all read this file. A fumble that recurred all the way to venue 6 is now a characterized limitation you can speak to honestly, not a panic.
  - Output: `bet_debriefs/venue6_final.md` with 4 sections + `## §6 verdict` + `## vs prior venues` + `## Close-out deltas`; `git log -1 --oneline` shows the Friday debrief commit.

**Saturday — Wrap the cycle: §6 cross-venue decision matrix + pipeline GO/NO-GO posture + Dubai/SG flip-trigger evaluation**

- [ ] Build the consolidated §6 decision matrix. Artifact: `bet_debriefs/bet_decision_matrix.md` (private). Read every `venueN_final.md` debrief (venues 1-6, however many ran) and collapse them into ONE table: one row per venue, columns for the **seven §6 gates** {1 founding-tier allocation, 2 tier-1 backing (which fund), 3 core-criticality, 4 multi-B FDV ceiling, 5 token reality (pre-TGE / cliff-vest / ≥10× headroom), 6 remote-native, 7 cycle timing} PLUS {technical-fit impression, the single strongest and single weakest §6 evidence point, **§6 verdict = GO-bet / bridge-only / pass**}. Then write a one-paragraph provisional ranking — explicitly naming which venues are GO-bet (all seven gates pass), which are bridge-only (≥1 gate fails), and which to drop. (~90 min)
  - 🎯 Hardened acceptance: `bet_decision_matrix.md` has one row per venue that completed a finalist round, every §6 cell populated (no blanks — an unknown is written `[unknown — ask in W130 follow-up]`, never left empty); each row carries an explicit `GO-bet` / `bridge-only` / `pass` verdict computed as **all-seven-gates-pass = GO-bet, otherwise NOT** (the §6 pass/fail rule, not a weighted score); the provisional ranking names a top GO-bet candidate (or states "zero GO-bets — bridge posture") with a one-line reason grounded in a cited debrief data point; the remote-native (gate 6) and token-reality (gate 5) cells are filled from `relocation_research.md` (W120) + the debriefs where the venue's geography/cap-table is known.
  - *Mental model*: by W131 the venues blur into one anxious memory; the only defense is to compress the fresh debriefs into one §6-comparable table NOW, while the distinctions are sharp. This matrix is the literal input to the W131 Bet #1 GO/NO-GO — building it Saturday means that decision is a pass/fail lookup, not a re-derivation under deadline. The §6 rule is load-bearing and counter-intuitive: a venue is **not** ranked by how good it feels; it is GO only if *all seven* gates pass, so a thrilling team with predatory token unlocks (gate 5 fail) or no tier-1 backing (gate 2 fail) is correctly a *bridge-only*, not a bet — which is exactly the trap §6 exists to catch.
  - Output: `bet_debriefs/bet_decision_matrix.md` exists with one fully-populated 7-gate row per completed venue + a per-row verdict + a provisional-ranking paragraph; `grep -c '^| ' bet_debriefs/bet_decision_matrix.md` ≥ (number of venues that ran) + 1 header.
- [ ] Pipeline GO/NO-GO posture + crypto-MM bridge readiness. Artifact: `bet_prep/bet_pipeline.md` (the W130 input). Record: how many finalist rounds actually ran vs the W125 stagger plan; a count of {GO-bet venues, bridge-only venues, dropped}; an energy/burnout self-rating (green/yellow/red); and an explicit verdict — `pipeline: ≥1 GO-bet candidate — drive to W131 decision` OR `pipeline: zero GO-bets — activate crypto-MM bridge in W130` (with the named desk + next action) OR `pipeline: needs widening — re-activate a dropped venue / lower target tier per §9`. (~30 min)
  - 🎯 Hardened acceptance: `bet_pipeline.md` states ran-vs-planned round count, the GO-bet/bridge-only/dropped tally, a green/yellow/red energy rating, and one explicit posture verdict that names W130's first action — so W130 starts with a known posture, not a guess; the runway math (§1 bar (d): ≥6-12 months) is restated so the bridge-vs-bet timing call is grounded.
  - *Mental model*: the cycle's biggest failure mode (§9 + §7) is reaching W131 with no GO-bet, no activated bridge, and a depleted candidate. §7 is explicit that the crypto-MM cash seat is the **primary bridge that funds runway between bets** — so "zero GO-bets" is a *normal, planned-for* branch (engage bridge, keep outbound), not a failure. A two-line posture verdict turns "I think it went okay" into a decision-ready stance and pre-commits the W130 action before exhaustion makes it feel impossible.
  - Output: `bet_prep/bet_pipeline.md` has the four required fields + the named W130 first action; `git log -1 --oneline` shows the pipeline commit.
- [ ] Dubai/SG flip-trigger evaluation (§7 geography — cheap, do NOT pre-commit a move). Artifact: append a `## §7 flip-trigger check` section to `bet_pipeline.md`. From the week's debriefs, evaluate the two §7 triggers: (a) do **≥N warm conversations cluster in one hub** (Dubai = crypto-native/Binance-dense bets; SG = institutional/Asia)? (b) has a **presence-contingent offer premium** appeared (a tier-1 bet paying materially more for on-site)? Record `flip-trigger: NOT fired — Vietnam-remote remains default` or `flip-trigger: FIRED (hub=Dubai/SG) — W132 evaluates relocation logistics`, citing the evidence. (~30 min)
  - 🎯 Hardened acceptance: the flip-trigger section states each of the two §7 triggers as fired/not-fired with a one-line evidence citation from a debrief; states the resulting default (`Vietnam-remote` unless a trigger fired); and if fired, names the hub (Dubai vs SG) per the §7 mapping and pulls the relevant visa/token-tax facts from `relocation_research.md` (W120) so W132 has a head start. If no GO-bet exists yet, the section explicitly states "flip-trigger moot until a GO-bet exists — Vietnam-remote default" (you don't relocate for a bridge or a non-bet).
  - *Mental model*: §7 is emphatic — geography is **pre-staged, never pre-committed**, and the decision point is *Bet #1 time*, not now. The flip-trigger is the discipline that prevents an expensive relocation on a hunch: you move only when warm conversations actually cluster in one hub or a tier-1 bet pays a real on-site premium. Evaluating it Saturday means W132's "relocation logistics (conditional)" starts with a yes/no already grounded in evidence, not anxiety — and the default answer (stay Vietnam-remote) is the correct one until a real GO-bet forces the hub question.
  - Output: `bet_pipeline.md` has a `## §7 flip-trigger check` section with both triggers evaluated + the move/no-move default + hub (if fired); `git log -1 --oneline` shows the Saturday consolidation commit.

---

## [v3.1 BFT apex — Track B] W129: Byzantine cluster-VOPR (the 8-scenario set, run under `BftReplica`)

> Track B continues (W128: apex fully built). This week **proves the apex under Byzantine faults** — extending the W108 cluster-VOPR harness with a **Byzantine fault generator** and running the W118-Friday 8-scenario set against the `BftReplica` cluster. This is the §4A-acceptance-#7 analogue for BFT (and the bounded-MVP "Byzantine scenario set"). Bar (c). W130–W134 sustain this VOPR (seed-sweep) alongside the bet decisions. **[C2 additive] The Byzantine VOPR is paired 1:1 with a `model-check` Stateright model** of the same protocol: the VOPR *samples* a huge fault space with one seed per run; the model *exhausts* a small bounded one across all interleavings — they catch different bugs, and together they earn the apex's safety claim (cited in Blog #4 / the flagship blog as the "exhaustive + sampled" verification story, and in the W143 acceptance).

## [v3.1 BFT apex — Track B] W129 (additive): `model-check` BFT-apex safety/liveness model (Stateright) — the exhaustive complement to the Byzantine VOPR

> **Day-load note**: this is an **additive** Build that pairs with the Byzantine VOPR below. It is the BFT milestone of `model-check` (the VSR model shipped W90). Stateright keeps formal methods **in-Rust, in-workspace** — buildable, not a separate toolchain. Scope bounded: model the *protocol*, not the impl; bound N=4/f=1 and op-depth so the state space stays finite (§9 risk: state-space explosion). ~3-phase Build, ~120 min, bar-(c)-supporting (it is how a bar-(c) consensus crate earns its safety claim; not itself bar-graded). Schedule it in a Track-B slot alongside the VOPR Build (Tue/Thu cont.), not on the bet-conversation days.

- [ ] [Venue · BFT] **Build (bar-(c)-supporting)**: `crates/model-check/src/bft.rs` + `tests/bft_model.rs` — a Stateright model of the BFT apex: **Jolteon 2-chain commit + TC view-change at N=4, f=1**. Exhaustively (bounded) check **safety under equivocation** (no two conflicting committed QCs) + **liveness after GST** (a decision is reached once the network heals). Pairs 1:1 with the 8 Byzantine VOPR scenarios below. (~total 120 min across 3 phases)
  - 🔧 **Why this matters**: the VOPR (W88/W108/this week) *samples* an enormous fault space with one seed; it can run a billion ticks and still miss the one adversarial interleaving that breaks safety. A model checker *enumerates* every reachable state up to a small bound — so it *exhausts* the corner the VOPR might never sample. Modeling the **protocol** (the state machine: proposals, votes, QCs, the 2-chain commit rule, the TC view-change) rather than the impl keeps the state space finite and the invariants crisp: **safety** = no two conflicting blocks ever both reach a committed QC (the equivocation attack); **liveness** = after GST (the partition heals / messages deliver within bound) a decision is always eventually reached. The trap: encoding the *implementation* (signatures, byte layouts, the real `Replica` struct) instead of the *protocol* abstraction → the state space explodes past any bound and the checker never terminates; OR over-abstracting so the model can't even express the equivocation it's supposed to reject. The negative control is load-bearing: the checker must **reproduce a planted safety bug as a counterexample** (e.g. weaken the 2-chain rule to 1-chain, or revert the quorum to n/2+1) — a model that finds nothing proves nothing. Downstream: the W143 apex acceptance cites both the model (exhaustive) and the VOPR (sampled); Blog #4 tells the "two checkers, different bugs" story.

  #### Phase 1 — Problem, prediction, design (~45 min, no code yet)

  - 🎯 **Pre-prediction prompt (BEFORE Phase 2)** — write sealed answers in `crates/model-check/notes/129_bft_model_prediction.md`:
    - P1. Predict the order-of-magnitude of the reachable state count at N=4, f=1, op-depth 2 (rough: states ≈ message-set powerset × view × per-replica log). One exponent. If it's astronomically large, name the abstraction you'll apply to shrink it (model QCs/votes as sets, not signed bytes).
    - P2. Predict whether the equivocation attack (leader double-proposes at one height) is reachable in your encoded transition relation. Yes/no — if no, your model can't express the bug it must reject.
    - P3. Predict the bounded depth at which `safety` (no two conflicting committed QCs) and `liveness` (decision after GST) each become checkable. Two numbers.
    - P4. Predict whether the planted-bug run (1-chain instead of 2-chain) yields a counterexample *trace* you can read. Yes/no + what the trace must show.
    - P5. List every Phase-2 test name you predict your first attempt will FAIL.
  - *Mental model*: a Stateright `Model` is `(State, Action, next_state)` + a set of `properties`. The state is the *protocol* snapshot — per-replica {current view, locked/high QC, log of proposals + votes seen} + the in-flight message set. Actions are protocol steps (propose, vote, form-QC, timeout→TC, deliver/drop a message). The checker does a bounded BFS over all action interleavings and, at every state, evaluates the invariants. **Safety** is an `always` property (`no two committed QCs on conflicting branches`); **liveness** is an `eventually` property under a GST assumption (after a bounded number of steps with no further drops, a decision exists). The model is the protocol abstraction, NOT the `BftReplica` impl — the impl is what the VOPR exercises.
  - *Contract* (signatures only — never the body):
    ```rust
    struct BftState { view: View, replicas: [ReplicaView; 4], in_flight: BTreeSet<Msg> } // BTreeSet: deterministic order
    enum Action { Propose { leader: Id, height: H, branch: BranchId }, Vote { who: Id, on: BlockId },
                  FormQc { height: H, branch: BranchId }, Timeout { who: Id }, Deliver(Msg), Drop(Msg) }
    impl Model for BftModel {
        type State = BftState; type Action = Action;
        fn init_states(&self) -> Vec<BftState>;
        fn actions(&self, s: &BftState, out: &mut Vec<Action>); // all enabled protocol steps (incl. the Byzantine equivocation)
        fn next_state(&self, s: &BftState, a: Action) -> Option<BftState>;
        fn properties(&self) -> Vec<Property<Self>>; // safety: no conflicting committed QCs; liveness: decision after GST
    }
    ```
  - *Back-of-envelope* (state-space bounding): at N=4 the message set is the powerset of {≤4 proposals × ≤4 votes × ≤2 QCs per height} per height × ≤2 heights × ≤2 views — without abstraction this is ~2^(dozens) and never terminates. Abstraction: model votes/QCs as *sets of replica-ids* (not signed bytes), collapse equivalent message orderings, bound op-depth to 2 and views to 2 → reachable states drop to ~10^4–10^6, BFS-able in seconds. The single Byzantine replica (f=1) is modeled as a replica whose `actions` include the *adversarial* steps (double-propose, vote-both-branches) the honest `actions` forbid — that one extra action set is what makes the equivocation reachable.
  - **Requirements** R1..R6 — write in `crates/model-check/notes/129_bft_model_design.md`:
    - R1. Model the **protocol** (proposal/vote/QC/2-chain-commit/TC-view-change), NOT the impl — no signatures, no byte layouts; QCs/votes are replica-id sets.
    - R2. N=4, f=1; quorum = 2f+1 = 3; the single Byzantine replica's `actions` include the adversarial steps (double-propose, conflicting-vote) honest replicas can't take.
    - R3. **Safety property** (`always`): no two QCs on conflicting branches ever both reach the 2-chain committed state — checked at every reachable state.
    - R4. **Liveness property** (`eventually`, under GST): after a bounded suffix with no drops, a decision (committed QC) is always reached.
    - R5. **Bounded + terminating**: op-depth ≤ 2, views ≤ 2, message multiplicities bounded; the checker terminates in CI within a wall-time budget; deterministic exploration order (`BTreeSet`, no HashMap).
    - R6. **Planted-bug negative control**: a `cfg`-gated variant weakens the 2-chain rule to 1-chain (or quorum to n/2+1); the checker MUST report a safety counterexample with a readable trace — proving the model bites.
  - **Design walk-through** D1..D5 — answer in the design doc; no peeking at Stateright examples / the VSR-model (W90) source until Phase 3:
    - D1. Protocol-vs-impl abstraction: property table — what to model (views, QCs as id-sets, the 2-chain rule, TC) vs what to abstract away (signatures, networking bytes, the real `Replica`). Argue each from "does it change reachable safety states?"
    - D2. Encoding the Byzantine replica: how `actions` for the f=1 node differ from honest nodes (the extra double-propose / conflicting-vote actions); show the equivocation is reachable (P2).
    - D3. Safety property as an `always` invariant: the exact predicate `∄ two committed QCs on conflicting branches`; how the 2-chain rule (two consecutive-round QCs) is what makes it hold.
    - D4. Liveness under GST: how Stateright expresses "eventually a decision" under a fairness/GST assumption; the bounded suffix that makes it checkable (R4).
    - **D5. Pre-mortem (load-bearing):** write the EXACT trace where (a) the model encodes the real signed-vote bytes, (b) the message set's cardinality explodes, (c) BFS never terminates within the CI budget, (d) you "fix" it by bounding depth to 1, (e) the equivocation needs depth 2 to manifest, (f) the model now passes green while *unable to reach the bug it exists to catch* — a false-green checker. Convince yourself R1 (protocol not impl) + R5 (depth ≥ 2) + R6 (planted-bug control) prevent every line.
    - Dy. **Anti-shortcut audit:** obvious reaches — model the impl (explosion, D5); `HashSet` for the message set (non-deterministic exploration → non-reproducible counterexamples); skip the planted-bug control (a green that proves nothing). Each is wrong HERE because the bar is a *terminating, reproducible, biting* exhaustive check.
  - 🧮 **Paper drill** — write in `notes/129_bft_model_drill.md`:
    - D-QUORUM on the BFT 2-chain rule at N=4, f=1: draw the 4 replicas + logs, mark the quorum (2f+1 = 3 of 3f+1 = 4), show two quorums intersect in ≥1 honest node; trace a **safe commit** (two consecutive-round QCs → committed) step by step; THEN hand-construct the **equivocation the model must reject** (the Byzantine leader double-proposes two conflicting branches at one height and tries to form two QCs) and write the exact invariant that forbids it (each honest replica votes once per round → the second QC can't get 2f+1 honest votes → no conflicting commit). Negative: a reverted n/2+1 quorum that does NOT intersect honestly → the split-brain double-commit the planted-bug control must surface.
    - D-LIN on a 3-op, 2-client BFT history committed via the 2-chain rule: build the partial order from the committed QCs, find the single linearization (or prove none exists for a planted-bad apply order), mark each op's linearization point at its commit — the property the model's safety invariant encodes (one totally-ordered committed log, no fork).
    - D-VOPR on the model↔VOPR pairing for ONE scenario (#4 safety-attack): write the seed→decisions table the *VOPR* explores for that scenario, then state which bounded states the *model* exhausts that the VOPR's single seed does NOT reach — the by-hand argument for "exhaustive + sampled catch different bugs"; list the nondeterminism leaks (HashSet message order, wall-clock, f64) that would void either checker's reproducibility.
  - **Output**: `notes/129_bft_model_design.md` (R1–R6, D1–D5, the abstraction + property tables, the D5 false-green trace); `notes/129_bft_model_prediction.md` sealed; `notes/129_bft_model_drill.md` with the worked drills.

  #### Phase 2 — Write the failing tests (~35 min)

  Tests in `crates/model-check/tests/bft_model.rs`. Each engineered so a naive model FAILS.

  - [ ] `bft_model_safety_green_within_bounded_depth` — **(EXPERT)** run the checker at N=4, f=1, depth 2; assert the safety property (no two conflicting committed QCs) holds across ALL reachable states AND the checker terminates within the CI wall-time budget. Exercises R3, R5.
  - [ ] `bft_model_liveness_after_gst` — **(EXPERT)** assert that under the GST/fairness assumption a decision (committed QC) is always eventually reached on every healed suffix. Exercises R4.
  - [ ] `bft_model_reproduces_planted_safety_bug` — **(EXPERT, negative-control, the canary)** with the `cfg`-gated 1-chain (or n/2+1-quorum) variant, assert the checker REPORTS a safety counterexample AND the returned trace shows the two conflicting committed QCs. A model that stays green here proves nothing. Exercises R6.
    ```rust
    let result = BftModel::planted_1chain_bug().checker().spawn_bfs().join();
    assert!(result.discovery("safety").is_some(),
        "checker found no counterexample on the planted 1-chain bug — the model does not bite");
    ```
  - [ ] `bft_model_equivocation_is_reachable` — **(SENIOR)** assert the Byzantine double-propose action is actually enabled and reachable in the transition relation (so the safety check is non-vacuous). Exercises R2.
  - [ ] `bft_model_exploration_deterministic` — **(SENIOR, cross-primitive)** run the checker twice; assert the explored-state count and any counterexample trace are byte-identical (BTreeSet ordering, no HashSet). Traps a HashSet message store. Exercises R5.
  - [ ] `bft_model_pairs_with_8_vopr_scenarios` — **(SENIOR)** assert each of the 8 Byzantine VOPR scenarios maps to a model property or reachable-state assertion (the 1:1 pairing), so the two verification methods cover the same protocol surface.

  #### Phase 3 — Make the tests pass, one failure at a time (~40 min)

  - **(EXPERT) When `bft_model_safety_green_within_bounded_depth` never terminates:**
    - Symptom: the checker runs for minutes and exhausts memory; the CI job times out.
    - Why: the state encoded the real signed-vote bytes / per-replica networking detail, so the reachable-state count exploded past any bound (the D5 trace).
    - Fix: abstract to the protocol — QCs/votes are replica-id sets, collapse equivalent message orderings, bound op-depth to 2 and views to 2. The state space drops to ~10^4–10^6, BFS-able in seconds.
    - Cost avoided: a model checker that can never finish is a model checker you delete; protocol abstraction is what makes exhaustive checking feasible.
    - Muscle: model the protocol, not the implementation — the impl is the VOPR's job. Reapplies at: W143 apex acceptance model, any future Stateright milestone.

  - **(EXPERT, negative-control) When `bft_model_reproduces_planted_safety_bug` stays green:**
    - Symptom: the 1-chain planted bug produces no counterexample; the checker reports the property holds.
    - Why: either the equivocation action wasn't reachable (over-abstraction), or the safety predicate was written so weakly it can't see the conflicting commit.
    - Fix: ensure the Byzantine `actions` include double-propose + conflicting-vote (D2), and the safety predicate is `∄ two committed QCs on conflicting branches` evaluated at every state. Re-run; the planted bug must now surface a readable trace.
    - Cost avoided: a false-green model that "verifies" safety while structurally unable to express the violation — the worst outcome for a verification tool.
    - Muscle: a checker is only trustworthy once it has *caught* a planted bug; the negative control is mandatory, not optional. Reapplies at: W90 VSR-model canary, W143 acceptance.

  - **(SENIOR, cross-primitive) When `bft_model_exploration_deterministic` shows different state counts across runs:**
    - Symptom: two checker runs report different explored-state counts or different counterexample traces.
    - Why: the in-flight message set is a `HashSet`, so BFS visits states in seed-randomized order and a counterexample search returns whichever it hit first.
    - Fix: `BTreeSet<Msg>` (and `BTreeMap` for any keyed state) so exploration order is deterministic and counterexamples are reproducible.
    - Cost avoided: a non-reproducible counterexample is un-debuggable — the same determinism discipline the VOPR enforces, applied to the model.
    - Muscle: a model checker's exploration must be deterministic so its counterexamples replay. Reapplies at: W90 VSR model, W143 acceptance.

  - **#### 5-year failure mode** — write in `crates/model-check/notes/129_bft_model_followup.md` after green: when the apex grows beyond N=4 (reconfiguration / dynamic membership in v2), the bounded state space at N=4 no longer covers the reconfiguration-window interleavings. TRIGGER: a safety claim about a view-change that *also* changes the membership set. Migration: a parameterized model over N with symmetry reduction (Stateright's state-equivalence) to keep the checker terminating, plus a dedicated reconfiguration-window property.
  - **Output**: `cargo test -p model-check bft_model_ green`; the safety + liveness properties green within bounded depth; the planted-bug control reproduces a readable counterexample trace; exploration is deterministic; `notes/129_bft_model_design.md` + `notes/129_bft_model_followup.md` complete. **The apex safety claim is now exhaustive (model) + sampled (VOPR).**
- [ ] [Venue · BFT] Commit + log (~10 min)

---

> Track B continues below: the Byzantine VOPR (sampled) that the model above pairs with.

- [ ] **[Tue +100min · Thu +100min]** [Venue · BFT] **Build (bar c)**: `crates/ops-chaos/src/bft_faults.rs` + `tests/bft_vopr.rs` — add Byzantine fault generators to the W108 harness (a replica can now *lie*, not just crash) and run the deterministic seed-sweep `vopr_sweep` over the 8 scenarios against `BftReplica`, with the StateChecker asserting safety + liveness. (~total 200 min) *Authoring-depth note: 8 fault generators in ~3.3h is thin — generators for scenarios #5–#8 may be authored in the W130–W134 sustain window if Friday overruns; scenarios #1–#4 + the planted-bug canary are the W129 floor.*
  - 🔧 **Why this matters**: VSR's VOPR (W88/W108) injects crash/partition faults (omission). BFT-VOPR must inject **commission** faults — a Byzantine replica that equivocates, withholds, forges, or proposes stale QCs — which the crash model can't represent. This is what *earns* the four BFT seams: each scenario targets a specific seam (equivocation→`DetectEquivocation`; withholding→`PipelinedHotStuffView` pacemaker; quorum→`BftQuorum`; auth→`SignedPeers`). The harness reuses the W108 `MessageBus`/`Storage`/`Clock` seams + single-PRNG-seed + StateChecker, so a found bug is replayable by seed (the deterministic-sim discipline). The trap: a Byzantine generator that only corrupts *transport* (drop/delay) — that's still omission; true Byzantine testing requires a replica running *adversarial logic* (proposing two blocks, signing a conflicting vote) inside the same `Replica` code path.
  - *The 8 scenarios (W118 Friday) — each: fault → invariant → StateChecker assertion:*
    1. **Equivocation** — leader double-proposes at one height → safety → `DetectEquivocation` emits `Evidence`; no conflicting commit.
    2. **Vote-withholding** *(pacemaker)* — leader/replica silent → liveness → TC forms, view advances, the silent leader's pending handles hit `drain_abandoned` (W125).
    3. **Leader-equivocation / conflicting QCs** — Byzantine leader attempts two QCs on conflicting branches → quorum-intersection → at most one QC per **view/round** (each replica votes once per round; the 2f+1-of-3f+1 honest overlap prevents the second). *Two QCs at the same HEIGHT in different rounds is a legal schedule (the O5 orphan path produces it) — conflicting COMMITS are prevented by the 2-chain consecutive-round rule, which is scenario #4's assertion, kept distinct (#1 = `DetectEquivocation` evidence emitted; #3 = QC-per-round uniqueness; #4 = commit-level no-fork).*
    4. **Safety-attack (conflicting commit)** — adversary schedules messages to commit two conflicting blocks → 2-chain safety → StateChecker linearizability holds; no fork.
    5. **Partition + Byzantine** — partition with a Byzantine node in the majority side → safety always, liveness on heal → no bad commit during partition; convergence after.
    6. **Stale-high-QC proposal** *(pacemaker)* — Byzantine new-view leader proposes extending an old QC → replicas reject non-high-QC proposals → the stale proposal gets no quorum (ties to W120 high-QC monotonicity, W121 TC carries max high-QC).
    7. **Orphaned-before-slot** *(the O5 case)* — force orphan via pacemaker view-change without supersession → `drain_abandoned` fires for exactly that handle, once; client gets `Abandoned`, not a hang (W125 permanent-prune, proven under Byzantine scheduling).
    8. **OpNumber contiguity under Byzantine view-changes** *(O4)* — Byzantine leaders skip/abort views → committed `OpNumber`s stay dense + contiguous (no gaps/dupes) — the ledger's replay/idempotency invariant survives adversarial view churn.
  - **Tests:**
    - `bft_vopr_8_scenarios_statechecker_green` — `vopr_sweep` runs all 8 over a seed range; StateChecker asserts: (safety) no two replicas commit different ops at the same `OpNumber`, no fork; (liveness) the cluster converges after each scenario heals; (the I1–I5 invariants from W108 + the Byzantine-specific ones above). f=1 throughout (N=4); a schedule violating safety prints its seed for exact replay.
    - `bft_vopr_finds_a_planted_bug` — plant a known bug (e.g. `BftQuorum::threshold` reverted to `n/2+1`) and assert the VOPR catches it (the safety-attack scenario forks) within the seed budget — proves the harness has teeth (the W108 "canary" discipline).
    - `bft_vopr_seed_replays_bit_identical` — same seed + same commit → bit-identical run (the deterministic-sim guarantee; a failing seed becomes a permanent regression).
    - **[C4 fold] `bft_vopr_byzantine_plus_storage_fault_composition`** — the W108 `sim-storage` faults (torn-write / bit-rot / misdirect / crash-corruption, behind the `Storage` seam, drawn from the same seed) are composed WITH the Byzantine fault generator: a node can be **Byzantine AND running a lying disk simultaneously**. Assert that across the seed-sweep, safety still holds (no fork, no two replicas commit different ops at the same `OpNumber`) and the `FaultAtlas` keeps ≥1 valid replica per block — so a Byzantine node whose disk also tears a write cannot, by combining commission + a corrupt read, manufacture a conflicting commit the 2f+1-honest overlap would otherwise forbid. The composed schedule is a pure function of the one seed (replayable).
      ```rust
      let report = vopr_sweep_bft(&scenarios, 0..N, VoprCfg::byzantine().with_storage_faults());
      assert!(report.safety_holds, "Byzantine+lying-disk composition forked the log — commission compounded a corrupt read into a bad commit");
      assert!(report.fault_atlas_invariant_held, "FaultAtlas let a Byzantine node + storage fault corrupt every replica of a block (unwinnable, not a real bug)");
      assert!(report.seed_replays_bit_identical, "composed Byzantine+storage schedule not replayable from its seed");
      ```
  - **[C4 fold] Requirement (additive)** — write into `notes/129_bft_vopr.md`: the Byzantine VOPR composes `sim-storage` faults (the W108/W88 model) with the Byzantine generator behind the SAME `Storage`/`MessageBus` seams and the SAME single PRNG seed, so a node may be **Byzantine (commission) AND backed by a lying disk (corruption) at once**. The StateChecker adds the composition assertion: safety (no fork) holds and the `FaultAtlas` ≥1-valid-replica-per-block invariant holds even under the compound fault; the canary is that a planted skipped-checksum on a Byzantine node's read must surface as a flagged corrupt read, not a silent bad commit. This is the BFT analogue of W108 I6 — the hardest fault model in the plan (adversarial logic ⊕ adversarial disk).
  - **Make-pass card (EXPERT):** `bft_vopr_8_scenarios` passes but `bft_vopr_finds_a_planted_bug` does NOT catch the reverted quorum → symptom: green VOPR that proves nothing; why: the Byzantine generator only perturbed transport (omission), never ran adversarial replica logic, so the conflicting-commit attack was never actually mounted; fix: the Byzantine replica runs real adversarial code (double-propose / conflicting-sign) inside the `Replica` path, not a transport corruption; cost avoided: false confidence at the apex acceptance (a "Byzantine-tested" engine that was only crash-tested); muscle: Byzantine VOPR requires adversarial *logic*, not just adversarial *network* — and a planted-bug canary proves it. Reapplies: W130–W134 sustained sweep, W143 acceptance.
  - 🧮 **Paper drill** — write in `notes/129_bft_vopr_drill.md`:
    - D-VOPR on seed `0x129A` driving scenario #4 (safety-attack): write the seed→decisions table (each PRNG draw → cluster op or Byzantine fault choice), hand-run the safety phase under the adversary's conflicting-commit schedule then the liveness phase (heal quorum → converge), list the per-commit invariants (linearizable-log, no-divergent-state-hash, OpNumber-contiguity, tripwire-never-hit), then the nondeterminism-leak hunt: enumerate every leak (HashMap iter over peers, wall-clock in the pacemaker, tokio on the sim path, OS-thread interleave, f64) that would void byte-identical seed replay.
    - D-QUORUM on scenario #3 (conflicting QCs) with N=4, f=1: draw the 4 replicas + logs, mark the BFT quorum (2f+1=3 of 3f+1=4), show two quorums intersect in ≥1 honest node, trace `propose → vote → QC` for the Byzantine leader's two conflicting branches at one round, and verify only one QC forms (each honest replica votes once per round). Negative: a quorum of size 2 (n/2+1 reverted) that does NOT intersect honestly → the split-brain double-commit the planted-bug canary must catch.
    - D-FAIL on scenario #5 (partition + Byzantine in the majority side): table each component (MessageBus partition / Byzantine replica logic / StateChecker) → failure mode → blast radius (which replicas see a bad value?) → recovery path (heal → re-converge) → invariant stressed (quorum-overlap holds so no bad commit during partition; convergence after heal); add the RAII fault-handle cleanup note (the abandoned-handle `drain_abandoned` from scenario #7 must not need a torn-down sim runtime).
    - **[C4 fold] D-FAIL on the Byzantine⊕storage composition** (a Byzantine node AND a lying disk on the same node): build the blast-radius table — for the compound fault, name {the Byzantine action (double-propose / conflicting-vote), the storage fault on that node (torn-write / bit-rot / misdirect / crash-corruption)} → combined blast radius (does commission + a corrupt read on ONE node reach a quorum for the same block? — it must not, because the other 2f+1−1 honest+intact replicas overlap) → recovery path (checksum-detect the corrupt block → state-transfer from a valid replica → honest quorum overrides the equivocation) → invariant stressed (2f+1 honest overlap AND `FaultAtlas` ≥1-valid-replica-per-block, TOGETHER). Conclude "a single node being Byzantine and lying about its disk cannot forge a conflicting commit, because the honest quorum's intact replicas both out-vote the equivocation and re-supply the corrupt block."
  - **Output**: `bft_faults.rs` + `bft_vopr.rs`; the 8 scenarios StateChecker-green under `BftReplica`; the planted-bug canary catches a reverted seam; seeds replay bit-identically. **The apex is now Byzantine-proven** (W118 bounded-MVP scenario set ✓).
- [ ] **[Sat +10min]** [Venue · BFT] Handoff note → `notes/129_bft_vopr.md`: the 8-scenario Byzantine cluster-VOPR is green; the harness reuses W108 + the W128 adapter. **W130–W134 sustain the seed-sweep** (run nightly alongside the bet decisions; failing seeds → regression corpus; any #5–#8 generators deferred from Friday land here first). **Next milestone W135–W142: latency-X + the live VSR→BFT swap into `perp-dex-core` (v2).** (~10 min)
- [ ] **[Sat +10min]** Commit + log (~10 min)

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md. Track B: **BFT apex Byzantine-proven** — 8-scenario cluster-VOPR green (incl. the O5 orphan case + O4 contiguity under adversarial view churn) with a planted-bug canary. Next: latency-X + the live swap into `perp-dex-core` (W135–W142).
