# Week 120 — Warm-network activation (Binance-alumni map → derivatives founders + crypto-MM desks) + Dubai/SG cheap prep

> **Phase**: 7 (Bet Placement / Bridge — the bet-path)
> **Month**: M31
> **Track**: Bet-path outreach (founding/core-eng token-equity bet, primary) + crypto-MM cash-seat bridge (income floor) + Dubai/SG pre-stage (cheap prep, no relocation)
> **Hours target**: 40h/wk (W118–W136 window; drops to 30h/wk at W137)
> **Mirror target**: (n/a — bet-path outreach + cheap-prep week; no upstream module to mirror. The Saturday recall item rehearses the **`risk-engine` cross-margin + `liquidation-engine` waterfall** narrative that anchors next week's flagship blog.)
> **Crate(s) touched**: (none — outreach + cheap-prep. The `perp-dex-core` v1.5 RWA/ADL/multi-collateral work resumes W118–119 context; this week converts the v1.0+v1.5 capstone into placed bets, it does not extend code.)
> **Bar**: (n/a — no Build. The two learning/recall items carry a hardened-acceptance pass criterion instead of a bar.)
> **Feeds into**: W121 flagship blog post (cites this week's recalled cross-margin/liquidation narrative + names the founding-eng contacts as soft endorsers + states the "architected for RWA-perps" seam); W122 inbound triage (replies from this week's outreach land in the ≥20-targeted-convos funnel with the response-rate metric); W123–124 event-trip recon (Token2049 Dubai/SG — this week's warm map is the in-person target list; the §7 flip-trigger is evaluated against the *cluster* this week seeds); W125–126 active outreach + first founding/core-eng conversations (apply §6 ecosystem checklist to this week's shortlist); W131 Bet #1 decision / bridge engage; W132 conditional relocation (only if the flip-trigger fires on the hub this week's prep covers); M30 Bet-Readiness Gate (W117) is already passed — this week is the first execution week off that gate.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): README.md §5 (M30/M36 gates), §6 (ecosystem-selection checklist — the 7-point GO filter), §7 (shots-on-goal + geography flip-trigger), §A (W118–120 re-slotting); https://github.com/tempoxyz/tempo (Saturday RWA-collateral recall verification, lookup-when-stuck); official Dubai (GDRFA / DET freelance + Golden Visa) and Singapore (MOM EP / ONE Pass / Tech.Pass) immigration portals + official tax-authority pages (FTA UAE, IRAS Singapore) for the Friday token-tax research — **official sources only**, lookup-when-stuck.
> **v3 modifications**: this week was re-aimed wholesale from the v2 "Tier-A HFT IC job outreach" to the **v3 bet-path**. (1) The four outreach days now target **on-chain-derivatives founders AND crypto-MM desks** sourced from the **Binance-alumni map** (§6.2 tier-1-backing + §6.3 core-criticality lens), not HFT recruiters. (2) The anchoring artifact shifts from "the receipt-decoding PR we co-reviewed" to **the `perp-dex-core` v1.0 capstone + the cross-margin/liquidation engine** (the principal artifact, §4(b)). (3) The Friday relocation research becomes **Dubai/SG cheap-prep** specifically — visa eligibility **+ pre-TGE token-tax treatment for *both* hubs** (§7 geography: pre-stage, do NOT pre-commit), wired to the flip-trigger, not a generic visa sweep. (4) Each outreach contact is now scored against the **§6 seven-point GO checklist** so next week's funnel is bet-graded, not lead-graded. (5) The Saturday recall rehearses the **capstone narrative** (cross-margin + liquidation + RWA-aware seam) instead of Tempo type-0x76 — Tempo is demoted to the RWA-collateral *option* leg.

**Read before Monday — why this is an outreach + cheap-prep week, not a Build week:** the M30 Bet-Readiness Gate (W117) is passed — `perp-dex-core` v1.0 is shipped at bar (c), cluster-VOPR is green, 2000h runtime is logged, and v1.5 (RWA features + ADL + multi-collateral, W118–119) is in flight. The constraint on placing Bet #1 is no longer *engineering*; it is **warm-network density + a bet-graded shortlist + visa/tax legibility for the two hubs**. So there is no crate, no upstream mirror, and no engineered 3-phase trap-test arc this week — manufacturing one would be padding. Instead **every item carries an explicit artifact file + an Output line that proves it done**, and the two learning/recall items (Friday token-tax, Saturday capstone-narrative recall) carry a hardened-acceptance pass criterion and a mental model of *why the item exists now*. The week's job: convert the accumulated Binance-alumni relationships + the capstone's founding-engineer credibility into **concrete intros graded by the §6 checklist** before next week's flagship blog ships, and make the Dubai/SG hub decision *cheap to flip* without pre-committing to a move.

**The §6 GO-filter every contact is scored against this week (so the funnel is bet-graded):** a contact is a **Bet lead** (not a bridge lead) only if their venue plausibly passes all seven — (1) founding-tier allocation, (2) tier-1 backing on the cap table, (3) core-criticality (I build the engine that *is* the company), (4) ≥$1B-FDV ceiling, (5) sane pre-TGE token reality, (6) remote-native (Vietnam-remote works), (7) cycle timing. Anything that fails core-criticality or remote-native is logged as a **crypto-MM bridge lead** (income floor), not a bet. This tag is the load-bearing output of the week.

---

## Monday — Outreach: Binance-alumni map → on-chain-derivatives **founders**

- [ ] Artifact: `outreach/log_binance_alumni_founders.md` (private). Activate the **Binance-alumni map** — reach out to 3–4 alumni who are now **founding or early at on-chain-derivatives venues** (or who can intro directly to such a founder), and with whom you hold a Depth-3+ relationship. Honest, no urgency: *"I've spent two years building a replicated, fault-tolerant perp-DEX core — multi-instrument cross-margin + a partial-liquidation engine with a fee-accrued insurance fund, on a 3-node VSR cluster, cluster-VOPR'd. I'm looking to place a founding/core-eng bet on an on-chain-derivatives venue. If your team (or someone in your network) is building the matching/risk/settlement core and wants a Rust systems engineer who's already shipped the spine, I'd love to talk. Portfolio + flagship writeup landing this week."* Log each: name, channel, date sent, the specific shared history you anchored the ask on, **and a preliminary §6 GO-score (which of the 7 pass / unknown)**. (~40 min)
  - 🔧 **Why this matters now**: the M30 gate is passed; the binding constraint is warm density into *checklist-passing* venues. A founder-level warm intro is the single highest-leverage channel in Phase 7 — it skips the cold-application screen and lands you in front of the person who decides founding-engineer allocation (§6.1). Anchoring on **the capstone** (not a PR) is what makes this a *bet* conversation, not a job application.
  - *Mental model*: a warm ask grounded in a concrete shipped artifact ("the cross-margin engine + cluster-VOPR runtime") converts far better than "let's connect" — it proves you've already built the company's hard part, which is the entire point of the prior 119 weeks. The Binance-alumni map is the densest source of tier-1-backed on-chain-derivatives founders (§6.2), which is why it leads the week.
  - 🎯 Hardened acceptance: `outreach/log_binance_alumni_founders.md` lists ≥3 contacts, each with {name + channel + date + shared-history anchor + a §6 GO-score line marking each of the 7 criteria pass/fail/unknown}; at least 1 contact scores ≥4/7 known-pass (a genuine bet lead, not a bridge lead).
  - Output: `grep -c '^- ' outreach/log_binance_alumni_founders.md` ≥ 3; each entry contains the literal token `§6:` followed by a 7-criteria scoreline.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday founder-outreach commit.

## Tuesday — Outreach: **crypto-MM desks** (the bridge / income floor)

- [ ] Artifact: `outreach/log_crypto_mm_desks.md` (private). Same core script, retuned to the **bridge** context — reach out to crypto-MM contacts (Wintermute / Jump-crypto / GSR / Amber-style desks, or Binance-alumni now on those desks). The bridge is the **income floor**, not the goal (§2), so the ask is honest about that: *"I'm primarily placing a founding-eng bet on an on-chain-derivatives venue, but I'm also exploring a crypto-MM cash seat as a bridge while that lands. My latency craft is real — kernel-bypass market-data path, p99.99-characterized matching engine, deterministic VSR-replicated state machine. If your desk has a Rust/low-latency seat, remote-friendly, I'd be glad to talk."* (~35 min)
  - 🔧 **Why this matters now**: §7 structures the outcome as **Bridge₀ → Bet #1** — the bridge funds runway between bets and is hireable *on the latency craft alone* (the `marketdata-kernelbypass` + `matching-engine` work), independent of whether Bet #1 lands this cycle. Seeding it in parallel this week means W130 ("bridge safety net activation if no GO-bet yet") is a warm pull, not a cold start.
  - *Mental model*: crypto-MM desks screen hard for latency signal; the tuned line ("p99.99-characterized matching engine + kernel-bypass feed handler") demonstrates you have the exact craft they pay for, not a generic Rust résumé. Keeping the bridge explicitly *warm but secondary* is the discipline that stops the income floor from quietly becoming the ceiling (the §6 "bridge job ≠ bet" trap).
  - 🎯 Hardened acceptance: `outreach/log_crypto_mm_desks.md` lists each contact with {desk + the specific latency-craft artifact referenced + date sent}, and a one-line top-of-file note `Bridge = floor not goal — Bet #1 (§6) remains primary`.
  - Output: `outreach/log_crypto_mm_desks.md` exists with ≥2 desk contacts each citing a named latency artifact; the floor-not-goal note is present.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday bridge-outreach commit.

## Wednesday — Outreach: tier-1 investors + prior colleagues who can vouch into a bet

- [ ] Artifact: `outreach/log_vouch_and_investors.md` (private). Two targets, same script: (a) **prior colleagues** now at on-chain-derivatives teams who can refer internally (strongest signal) or intro externally; (b) **light-touch reach to people one hop from tier-1 funds** (§6.2: Paradigm / a16z-crypto / Dragonfly / Polychain / 1kx / Multicoin / Jump / Wintermute) — not to pitch the fund, but to ask "which of your portfolio derivatives venues are hiring a founding/core engineer for the matching/risk core?" Tag each `[refer]` / `[intro]` / `[investor-hop]`. (~35 min)
  - 🔧 **Why this matters now**: an internal referral skips the screen and lands you in front of someone who can allocate founding-tier equity (§6.1 + §6.3). An investor hop is how you *discover* checklist-passing venues you don't already know — tier-1 backing (§6.2) is one of the seven GO criteria, so the investors literally hold the shortlist you're trying to build for W116/W125.
  - *Mental model*: a former colleague's internal referral carries built-in credibility (highest-leverage channel); an investor hop is the highest-*coverage* channel (it surfaces venues you'd never cold-find). Classifying each contact `[refer]/[intro]/[investor-hop]` keeps the leverage-vs-coverage split explicit so W125's active-outreach list is sorted by channel strength, not vibe.
  - 🎯 Hardened acceptance: `outreach/log_vouch_and_investors.md` lists each contact tagged `[refer]` / `[intro]` / `[investor-hop]` + firm/fund + date sent; ≥1 `[investor-hop]` entry names a specific §6.2 tier-1 fund.
  - Output: `outreach/log_vouch_and_investors.md` exists; `grep -cE '\[(refer|intro|investor-hop)\]' outreach/log_vouch_and_investors.md` ≥ 3; ≥1 line names a §6.2 fund.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday vouch/investor commit.

## Thursday — Bet-shortlist build + §6 checklist scoring (the funnel-grading day)

- [ ] Artifact: `outreach/bet_shortlist.md` (private). Consolidate Mon–Wed contacts into a **ranked bet shortlist** — one row per *venue* (not per contact), each scored against all **seven §6 GO criteria** (pass / fail / unknown). This is the artifact W116 ("ecosystem-selection filter applied to ≥5 venues") and W125 ("target list via §6 checklist") both consume; building it now, while the M30 gate is fresh, front-loads the filtering. **A venue is a GO-bet candidate only if all 7 plausibly pass; otherwise it drops to the bridge column.** (~40 min)
  - 🔧 **Why this matters now**: §5's M30 gate requires an "ecosystem shortlist ≥ 5 venues passing the §6 checklist" — this week's outreach is the raw material, and Thursday turns raw leads into the *scored shortlist* the rest of Phase 7 (W125–131) executes against. Without the §6 score, outreach is lead-generation; with it, it's bet-generation.
  - *Mental model*: the §6 checklist is the device that prevents the most expensive Phase-7 error — taking a **bridge job dressed as a bet** (the Airwallex anti-pattern in §6.1: late-stage, tiny employee equity, peripheral role). Scoring every venue on core-criticality (#3) and token-reality (#5) up front means W131's Bet #1 decision is a GO/NO-GO read off a table, not a gut call under offer pressure.
  - 🎯 Hardened acceptance: `outreach/bet_shortlist.md` contains a table with one row per venue and seven columns (the §6 criteria), each cell pass/fail/unknown; ≥1 venue is tagged `BET-CANDIDATE` (all-7 plausible) and any failing-#3-or-#6 venue is tagged `BRIDGE-ONLY`; a closing line states current count of BET-CANDIDATE venues toward the W117/W116 "≥5" target.
  - Output: `outreach/bet_shortlist.md` exists with the 7-column table; `grep -c 'BET-CANDIDATE' outreach/bet_shortlist.md` ≥ 1; the toward-≥5 count line is present.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday shortlist commit.

## Friday — Dubai/SG **cheap prep**: visa eligibility + pre-TGE token-tax treatment (BOTH hubs)

- [ ] Artifact: `geo/dubai_sg_cheap_prep.md` (private). §7 geography is **pre-stage, do NOT pre-commit** — so this is *cheap prep only*: confirm, for **both** Dubai and Singapore, (a) the **visa category** that fits a remote founding-engineer / self-sponsored path (Dubai: freelance/Green/Golden-Visa routes vs employer-sponsored; SG: EP / ONE Pass / Tech.Pass), with typical processing timeline + the **official portal URL**; and (b) the **pre-TGE token-tax treatment** (how a token grant / vesting allocation is taxed on receipt vs vest vs disposal in each hub) from the **official tax authority** (UAE FTA, Singapore IRAS). No move, no lease, no commitment — just legibility. Flag any number whose source you couldn't verify as `[UNVERIFIED]`. (~40 min)
  - 🔧 **Why this matters now**: §7 ties the hub to the bet — **Dubai = crypto-native / Binance-dense bets; Singapore = institutional / Asia-facing bets** — and the decision point is **Bet #1 time** (W131), not now. Having visa + token-tax legibility for *both* hubs ready means that when the §7 **flip-trigger** fires (≥N warm conversations cluster in one hub, or a presence-contingent offer premium appears), relocation is an execution detail, not a research scramble. It also makes a founder's first disqualifying question — "can you be in our hub, and what's your tax situation on the token grant?" — a non-issue.
  - 🎯 Hardened acceptance: `geo/dubai_sg_cheap_prep.md` covers **both** Dubai and Singapore, each with {a fitting visa category + processing timeline + official portal URL, and a pre-TGE token-tax treatment line + official tax-authority URL}; every timeline/tax claim carries a source link or an explicit `[UNVERIFIED]` tag — **no unsourced claims**; a closing line records the §7 flip-trigger condition ("convert to relocation only when ≥N warm convos cluster in one hub OR a presence-contingent offer premium appears").
  - *Mental model*: pre-staging is cheap optionality; pre-committing is an expensive bet on the *wrong variable* (geography) before the *right variable* (the actual Bet #1 and its hub) is known. The flip-trigger is the discipline that keeps Vietnam-remote the default until a real, hub-specific reason to move exists — exactly the §7 "lowest-risk, no single-ecosystem dependence" property.
  - Output: `geo/dubai_sg_cheap_prep.md` exists meeting the hardened-acceptance criteria for both hubs; the flip-trigger line is present.
- [ ] Commit notes (`geo/dubai_sg_cheap_prep.md` private). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday cheap-prep commit.

## Saturday — Capstone-narrative recall (rehearse the flagship-blog spine) + Tempo RWA-collateral touch

- [ ] **Tái-dựng (capstone narrative)**: artifact `notes/recall_capstone_narrative.md`. Close all docs and code. From memory, reconstruct the **principal artifact narrative** that W121's flagship blog and every founding-eng conversation hangs on, in five beats: (1) the **`risk-engine`** multi-instrument cross-margin model — single-collateral, BTC-PERP + ETH-PERP, **const 30% netting-benefit haircut** (and *why* a deterministic haircut, not rolling-empirical correlation: empirical correlation injects non-deterministic floating state VSR must replicate identically, and correlation→1 in a crash so under-crediting is the safe default); (2) the **incremental-margin hot path** (delta-on-change, mandatory at ~100k accounts, reads from local applied state on the leader shard — no consensus round-trip); (3) the **`liquidation-engine`** partial-liq + **fee-accrued insurance fund** (default 10% taker, declared param) + **waterfall** (position-margin → fund) + the **tripwire-never-hit invariant** (a hit *defines* the v1.5 ADL trigger); (4) the **`perp-dex-core`** assembly — 3-node VSR where **the order/event log IS the VSR log**, async settle via `SettlementId(snapshot_epoch, op_number, checksum)` with `finality_status → {Pending, Final, Reverted}`, **two staleness contracts** (control/risk = read-after-commit; analytics = bounded-staleness + op-number token); (5) the **RWA-aware seam** ("architected for RWA-perps — here's the seam: abstract oracle iface crypto-spot vs TradFi-market-hours, funding parameterized for carry/basis") shipped in v1.0, RWA *features* at v1.5. For EACH beat write the one-line problem it solves. THEN open your own crate docs (`risk-engine`, `liquidation-engine`, `perp-dex-core`) + README.md §4A and mark each recalled beat ✓ correct / ✗ missed / ~ partial, correcting any drift. (~60 min)
  - 🎯 Hardened acceptance: ≥4/5 beats recalled from memory before opening docs; the problem each solves is documented; each of the five v1.0-lock invariants (30% haircut · incremental margin · insurance-fund tripwire · VSR-log-is-the-log · two staleness contracts · RWA seam) is stated and tagged ✓/✗/~ against §4A. A single ✗ on the staleness-contract or `SettlementId` detail is expected-and-fine — it is the signal of what to re-cement before a founder conversation, not a failure.
  - *Mental model*: the flagship blog (W121) and the founding-eng conversations (W126) both **cite this narrative**, and outreach is happening THIS week. Recalling it now — *before* the blog draft and before the first founder reply lands — means the blog and the live conversation cite **lived architectural judgment with the problem-framing intact** (why 30% and not empirical; why the tripwire invariant; why one log not two), not regurgitated docs any reader could pull. The recall-then-verify loop surfaces any drift in the load-bearing invariants before it embarrasses you in front of a founder who *will* probe the netting-haircut choice.
  - Output: `notes/recall_capstone_narrative.md` lists all 5 beats with per-beat problem + ✓/✗/~ tag against §4A; ≥4 tagged ✓; the six v1.0-lock invariants each appear tagged.
- [ ] [Tempo] **RWA-collateral option touch** (light): append to `notes/recall_capstone_narrative.md` a 3–4 line note on where **Tempo** fits as the **RWA-perps collateral-rails option leg** (§2 option-leg B, §4A v1.5) — *not* the critical path. From memory name the v1.5 seam Tempo would plug into (multi-collateral + RWA collateral rails), then sanity-check against https://github.com/tempoxyz/tempo if a detail is shaky. (~20 min)
  - 🎯 Hardened acceptance: the Tempo note states (a) Tempo = RWA-collateral *option* leg, not critical path; (b) the specific v1.5 capstone seam it plugs into; (c) one line on why it stays warm-but-deferred (option value, not commitment).
  - *Mental model*: Tempo is a hedge with option value, retained cheaply — keeping the seam recalled means if a Bet #1 conversation turns toward RWA-collateral rails, you can speak to it credibly without it ever sitting on the v1.0 critical path. Writing "option, not commitment" down keeps the hedge honest.
  - Output: the Tempo option-leg note is present in `notes/recall_capstone_narrative.md` meeting the 3-point acceptance.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday recall commit.

---

## [v3.1 BFT apex — Track B] W120: `PipelinedHotStuffView` (1/2) — leader rotation + view/round structure

> Track B continues (W119 shipped `BftQuorum` + `SignedPeers`). W120–W121 build the **`ViewManager` seam** impl `PipelinedHotStuffView` (Jolteon 2-chain+TC). This week: leader rotation + the view/round structure + high-QC tracking. Bar (c). The `Replica` core is unchanged — only the `ViewManager` type param differs from VSR's `RoundRobinView`.

- [ ] **[Tue +90min · Thu +60min]** [Venue · BFT] **Build (bar c)**: `crates/consensus-vsr/src/bft/view.rs` — `PipelinedHotStuffView` impl of the W91 `ViewManager` seam: `leader_for`, the per-view round structure, and high-QC tracking (the highest QC a replica has seen, which a new leader extends). (~total 150 min)
  - **Phase 1 (compact — seal in `notes/120_bft_view_prediction.md` before coding):**
    - *Prediction (sealed)*: what may `leader_for(view, n)` legally read? Write the exact divergence trace if two replicas disagree on `n` for a single view — before reading the tests below.
    - *Back-of-envelope*: `leader_for` = one u64 modulo ≈ ~5 ns; `update_high_qc` = one view-compare + ~64-byte copy ≈ ~10 ns, 0 allocs — cost is irrelevant here; cross-replica *agreement* is the entire game.
    - *Pre-mortem (interleaving)*: replica A reads n=4 from local config, B reads n=5 after a config edit → at view 5, A expects leader 1, B expects leader 0 → each drops the other's proposal as "not the leader" → no quorum → timeout loop (liveness stall with zero crashed nodes).
    - 🧮 **Paper drill** — write in `notes/120_bft_view_drill.md`:
      - D-QUORUM on a 4-replica (`n=4`, `f=1`, `3f+1`) BFT cluster across views 4→5→6: draw the 4 replica logs; trace a block `prepare → 2f+1=3-vote QC → commit` under leader `view%4`; mark each replica's `high_qc`; then advance the view and hand-trace how the **new** leader at view 5 adopts the **highest QC** it has seen and extends *that* (not its own stale tip); verify safety (no two conflicting commits) and that two `2f+1` quorums intersect in ≥1 honest node. **The negative**: a leader that ignores the high-QC and proposes off a *lower* QC → draw the orphaned committed prefix → the fork. Verdict line: "the new leader is safe because it extends max(high_qc) ⇒ no committed block is orphaned."
      - D-STRUCT on the per-view round structure: draw the view/round state machine with 3 concrete views (a node's states within one view: `Propose → Vote → next-view`), redraw on each `leader_for(view, 4)` rotation; mark where `update_high_qc` mutates state and the linearization point (the single monotonic compare that commits "adopt this QC"). Output: the before/after state drawing across the 3 views.
      - D-FAIL on a stalled-leader scenario at view 5: a table — component (leader for view 5) → failure mode (crashes / stops proposing) → blast radius (no QC forms ⇒ all honest replicas stall on view 5) → recovery path (pacemaker timeout → `2f+1` Timeout-Certificate → view 6 with the next rotated leader) → invariant stressed (liveness under `f=1`, quorum overlap on the TC). Verdict: "the cluster makes progress because the pacemaker re-elects a live leader without orphaning the high-QC." (This sets up W121's `on_timeout` + `certify_new_view`.)
  - 🔧 **Why this matters**: pipelined HotStuff rotates the leader **every view** (unlike VSR's stable leader) — this is the property `test_routine_rotation_does_not_drain_pending` (W91) was built to protect: `role()` flips every height and **must not** trigger a drain. The high-QC is what makes the chain safe: a new leader extends the highest QC it knows, so an honest quorum's committed prefix is never orphaned. The trap: a non-deterministic leader function (e.g. `view % n` differing if `n` is read from a non-replicated source) → replicas disagree on who leads → liveness stall.
  - *Contract* (signature-level): `pub struct PipelinedHotStuffView { high_qc: Qc, n: usize } impl ViewManager for PipelinedHotStuffView { type ViewMsg = Timeout; type NewViewCert = Tc; fn leader_for(&self, view: u64, n: usize) -> ReplicaId { ((view % n as u64)) as ReplicaId } /* rotation; deterministic from (view, n) only */ fn on_timeout(&mut self, view: u64) -> ViewChangeAction { /* W121 */ } fn certify_new_view(&self, evidence: &[Timeout]) -> Option<Tc> { /* W121 */ } }` + `fn update_high_qc(&mut self, qc: Qc)` (monotonic — only adopt a strictly-higher QC).
  - **Tests:**
    - `leader_rotates_every_view_deterministically` — `leader_for(v, 4)` cycles 0,1,2,3,0,…; all replicas compute the SAME leader for any view (pure function of `(view, n)`). Traps a non-replicated `n`.
    - `high_qc_is_monotonic` — `update_high_qc` adopts a higher-view QC, rejects a lower/equal one. (A non-monotonic high-QC lets a stale leader orphan committed blocks — the stale-high-QC Byzantine scenario, W129 #6.)
    - `view_change_keys_role_not_drain` — composing with W91: a `role()` flip from a view advance does NOT drain `tx_pending` (re-assert `test_routine_rotation_does_not_drain_pending` under the BFT `ViewManager`). The behavioral swap-safety guarantee holds for the real BFT view manager, not just the stub.
    - 🪤 **negative-control `leader_agreement_check_has_teeth` (SENIOR)** — plant a `leader_for` salted by the local `ReplicaId` (the canonical nondeterminism bug) and assert `leader_rotates_every_view_deterministically` FAILS against the plant — proves the harness compares leaders *across* replicas, not a replica against itself (a self-comparison passes vacuously).
  - **Make-pass card (SENIOR):** `leader_rotates_every_view_deterministically` diverges across replicas → symptom: replica A and B pick different leaders for view 5; why: `n` (cluster size) read from a per-node config that drifted; fix: `n` is part of the replicated membership (consensus-managed), `leader_for` is a pure fn of `(view, replicated_n)`; cost avoided: dueling leaders → no progress; muscle: leader election is a deterministic fn of replicated state only. Reapplies: W121 pacemaker, W129 partition+Byzantine.
  - **Output**: `bft/view.rs` with leader rotation + high-QC; tests green; `Replica` core diff vs VSR build empty; `ViewMsg`/`NewViewCert` bound to `Timeout`/`Tc` (the W91 FIX-3 associated types — bodies land W121).
- [ ] **[Sat +10min]** [Venue · BFT] Handoff note → `notes/120_bft_track.md`: W120 shipped leader rotation + high-QC; **next W121: pacemaker (`on_timeout`) + Timeout-Certificate (`certify_new_view → Tc`)**. (~10 min)
- [ ] **[Sat +10min]** Commit + log (~10 min)

---

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md. Track B: `PipelinedHotStuffView` leader-rotation + high-QC shipped; next is the pacemaker + Timeout-Certificate (W121).
