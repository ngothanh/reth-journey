# Week 139 — Settling into the bet/bridge + Bet #2 thesis seeds (each bet upgrades the next)

> **Phase**: 7 (Bet Placement / Bridge — M31–M36)
> **Month**: M36
> **Track**: Bet-path landing (the destination is either Bet #1 — a founding/core-eng seat on an on-chain-derivatives venue — or Bridge₀ — a crypto-MM cash seat — per the §7 chain `Bridge₀ → Bet #1 → Bridge₁ → Bet #2`). This is a settling/recon week, not a Build week.
> **Hours target**: 30h/wk (W137–W144 window — the 40→30 step-down began W137)
> **Mirror target**: n/a (bet-path phase — no upstream module is mirrored this week)
> **Crate(s) touched**: n/a (maintenance only on the personal `perp-dex-core` rig; no version bumps. The capstone v1.5 — RWA features + ADL + multi-collateral, shipped W119 — is the portfolio artifact you *point at*, not code you touch this week)
> **Bar**: (n/a — settling week; no crate on the bar)
> **Feeds into**: W140 (permanent housing — hub if the §7 flip-trigger fired, else remote base — extends this week's housing shortlist); W141 (public artifact update on `perp-dex-core` + the bet — extends this week's Bet #2 thesis seeds and the scope-ownership log); W142–W143 (settle + quarterly-review prep — consume the 30/60/90 self-assessment); W144 (M36 retrospective + **Bet #2 prep** — the Bet #2 thesis-seed doc is the direct input; close metric is 7-system coverage ≥85); the personal rig's 2000h-by-M30 / sustained-runtime proof (Saturday tally keeps the counter honest into the M36 close).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): the firm's onboarding wiki + the 30/60/90 doc your manager set (Monday scope-ownership baseline, lookup-when-stuck); `RECONCILED_PLAN_v3.md` §7 (the multi-bet chain `Bridge₀ → Bet #1 → Bridge₁ → Bet #2`; "each bet upgrades the next") and §6 (ecosystem-selection checklist — the lens you apply to Bet #2 candidates); §A M31–M36 line (bet-path framing); destination-geography rental/tenancy portals (Thursday housing research — official + reputable-agency sources only); README §"Weekly HFT maintenance template (W97–W144)" + §"Monthly HFT polish checkpoints" M36 line (Saturday runtime tally target).
> **v3 modifications**: (1) **Track re-aimed "destination landing" → "bet-path landing."** The v2 stub framed this as settling into a *job*; v3 frames it as settling into **Bet #1 (founding/core-eng token-equity seat) OR Bridge₀ (crypto-MM cash seat)** per the §7 chain — the language, the scope-ownership lens, and the geography handling are re-narrated around that. (2) **New mandated deliverable: Bet #2 thesis seeds** (the INDEX [v3] line for W139). The Friday slot now produces a `bets/bet2_thesis_seeds.md` artifact applying the §6 checklist forward — "each bet upgrades the next" — which is the direct input to the W144 M36 Bet #2 prep. (3) **Geography handling made conditional on the §7 flip-trigger** (Dubai = crypto-native/Binance-dense; SG = institutional/Asia): the Thursday housing research now branches on *whether the flip-trigger fired* — permanent-housing-in-hub if relocated, else remote-base. (4) **Rig re-identified as `perp-dex-core`** (the v3 capstone), not the v2 "personal HFT rig"; the v1.5 differentiator (RWA features + ADL + multi-collateral) is the artifact this week's outreach points at.

This is a bet-path settling/recon week, not a Build week. There is **no crate, no upstream mirror, and no engineered trap-test arc** — manufacturing one would be padding (and the README explicitly carves these settling weeks out of the Build cadence). You are ~6–7 weeks into the destination (Bet #1 if a §6-passing venue said GO at W131, otherwise Bridge₀ — the crypto-MM cash seat that funds runway while Bet #1 outbound continues). The week has three real jobs: (a) **convert the first month's earned trust into owned scope** (the promotion-track question the 30/60/90 measures), (b) **seed the Bet #2 thesis** — because the v3 strategy is a *chain* (`Bridge₀ → Bet #1 → Bridge₁ → Bet #2`) where each placement upgrades network, allocation tier, and product-shape access for the next, so Bet #2 thinking starts *now*, not at exit — and (c) **convert temporary housing into a permanent plan**, branching on whether the §7 flip-trigger fired. Every item below carries an explicit artifact file + an Output line that proves it done; the three judgment/research items (Monday scope-ownership, Thursday housing, Friday Bet #2 seeds) each carry a hardened-acceptance pass criterion and a mental model of why the item exists *now*.

---

## Monday — Larger project ramp (own scope, not tickets)

- [ ] Artifact: `career/scope_ownership_W139.md` (private). The substantive work is now a larger project you own scope on (the ramp from "execute well-scoped tickets" to "own a larger project end-to-end" is the W135–W138 arc; this week is where it solidifies). Capture **before** you start the week: the project's stated goal in one sentence; the 3–5 sub-deliverables you now own (vs ones still owned by others); the explicit decisions that are now yours to make; and the named blockers/dependencies you must *drive* rather than wait on. This is the difference between "doing tickets" and "owning scope" — write it down so the boundary is explicit, not assumed. (~30 min)
  - 🎯 **Hardened acceptance**: `career/scope_ownership_W139.md` names the project goal in one sentence, lists ≥3 sub-deliverables each tagged `[mine]` / `[shared]` / `[theirs]`, and names ≥2 dependencies tagged `[I drive]` vs `[I wait]` — every dependency has an owner *name* attached, not a "TBD".
  - *Mental model*: at the 30/60/90 boundary the destination's implicit question is "can this engineer be handed an ambiguous project and turn it into shipped scope without supervision?" On the **bet path** the stakes are higher than on a job path: at a founding/core-eng seat, *owning the critical-path engine you were brought in to build* is the whole thesis of the seat (and the vesting/token-equity case), and on the crypto-MM bridge it is what converts Bridge₀ into a credential that strengthens Bet #1 outbound. Writing the ownership boundary down on day one of the larger project is how you answer the question deliberately instead of accidentally over-stepping (stepping on a peer's scope) or under-stepping (waiting for direction that won't come). The W144 retrospective scores exactly this transition.
  - Output: `career/scope_ownership_W139.md` exists meeting the hardened-acceptance criteria; `git log -1 --oneline` (private repo) shows the Monday commit.
- [ ] **[rig] Monday maintenance** (~15 min): review weekend runtime alerts on the personal `perp-dex-core` rig — any p99.99 spikes, replica/VSR divergence events, insurance-fund tripwire approaches, or disk-fill warnings? Append findings to `ops/alert_log.md` (even "clean weekend" is an entry, so the record is unbroken).
  - *Mental model*: the rig is now running with ≤4 hrs/wk of attention (the new normal from W138). A standing 15-min Monday alert-review is the minimum viable touch that keeps a silent failure from quietly voiding the rig's value as the live-proof behind the capstone — an unreviewed alert is an outage you didn't notice, and a 3-node VSR cluster that silently lost a replica is a credibility hole an interviewer will find.
  - Output: `ops/alert_log.md` has a dated W139-Mon entry (clean or with findings).
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday commit.

## Tuesday — Work (decision/escalation log)

- [ ] Artifact: append to `career/scope_ownership_W139.md` a `## Tue progress` section. Execute the day's substantive work on the owned project; at end of day log the one decision you made independently (and why) and the one thing you escalated (and why it crossed the escalate line). (~20 min logging, on top of the day's work)
  - *Mental model*: the decision/escalation split, logged daily, is the running evidence of calibrated judgment — too many escalations reads as "needs hand-holding," too few reads as "ran ahead of the team." At a founding seat the calibration is *also* the signal of whether you can hold the critical path; keeping the log makes it visible and gives Friday's self-review concrete material instead of vibes.
  - Output: `career/scope_ownership_W139.md` has a dated `## Tue progress` block with ≥1 `[decided]` and ≥1 `[escalated]` line (either may be "none today — <reason>").
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday commit.

## Wednesday — Work + rig runtime capture

- [ ] Artifact: append `## Wed progress` to `career/scope_ownership_W139.md` (same `[decided]`/`[escalated]` format). (~15 min logging, on top of the day's work)
  - Output: `career/scope_ownership_W139.md` has a dated `## Wed progress` block.
- [ ] **[rig] Wednesday maintenance** (~30 min): check the `perp-dex-core` rig's operations dashboards (runtime/VSR/insurance-fund/VOPR metrics — the W105 `ops-monitoring` panel); capture the running runtime-hours total and update the rig row in `progress.md`.
  - *Mental model*: the rig is the live artifact the bet-conversation 1-pager and the flagship blog (W121) point at — "a 3-node VSR-replicated hybrid CLOB that has run continuously since W106" is only a claim if the counter is current. A weekly Wednesday capture is the only thing that makes the W144 close-out number trustworthy rather than reconstructed-from-memory — and a flat-lined counter week-over-week is itself the signal that the rig has silently stopped.
  - Output: `progress.md` rig row shows an updated W139 runtime-hours total with date.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday commit.

## Thursday — Permanent-housing research (branches on the §7 flip-trigger)

- [ ] Artifact: `relocation/housing_plan_W139.md` (private). You are in temporary housing (if relocated) or your Vietnam-remote base (if not); this week begins the permanent-housing plan (W139–W143). **Branch on the §7 flip-trigger:**
  - **If the flip-trigger fired (you relocated to Dubai or SG at Bet #1 time):** capture for the destination hub — ≥3 candidate neighborhoods with {approx commute to office/co-working, indicative rent band, one pro + one con}; the tenancy mechanics that differ from Vietnam (deposit norms, contract length, what a landlord/agency requires from a recently-arrived foreign hire — proof of employment, local guarantor, bank reference); the **token-tax / visa interaction** for that hub (you confirmed eligibility for *both* hubs back at W120 as cheap prep — now make it concrete for the one you're in); and any timeline gotcha (notice on the temporary place, lease lead time).
  - **If the flip-trigger did NOT fire (Vietnam-remote default held — the most likely branch):** capture the remote-base plan instead — confirm the home-base setup that supports a 30h/wk remote bet/bridge seat (workspace, redundant connectivity, the time-zone overlap window with the team), and record the *standing* conditions under which a future relocation would still make sense (a presence-contingent Bet #2 offer premium, a warm-conversation cluster in one hub). Note that no move is committed.
  - Source any monetary/timeline figure from official tenancy portals + reputable local agencies; flag any number you couldn't verify. (~40 min)
  - 🎯 **Hardened acceptance**: `relocation/housing_plan_W139.md` states which branch applies (flip-trigger fired / did not) with a one-line reason; the **relocated** branch covers ≥3 neighborhoods each with {commute, rent band + source, pro, con} + deposit/contract/landlord mechanics + the hub's token-tax/visa note; the **remote** branch documents the home-base requirements + the standing relocation conditions; every monetary/timeline figure carries a source link or an explicit `[UNVERIFIED]` tag — no unsourced claims.
  - *Mental model*: permanent housing is the gate between "I took a bet" and "I live the bet sustainably" — getting it wrong (wrong commute, a lease you can't exit, a deposit you didn't budget, a token-tax surprise in a new hub) taxes every working week after, and the §7 doctrine is explicit that the hub *follows the bet* and only on the flip-trigger. Doing the research as a sourced, branch-aware plan now — while temporary housing or the remote default buys you slack — turns a high-stakes rushed decision into a deliberate one and feeds W140–W143 a concrete starting state instead of a blank page.
  - Output: `relocation/housing_plan_W139.md` exists meeting the hardened-acceptance criteria.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday commit.

## Friday — Work + week self-review + **Bet #2 thesis seeds**

- [ ] Artifact: append `## Fri self-review` to `career/scope_ownership_W139.md`. Re-read the week's decision/escalation log; answer in ≤5 lines: did I own the scope I claimed Monday, where did I over- or under-step, and what is the one scope-ownership adjustment for next week? Map the week against the 30/60/90 expectations (am I on track for this point in the ramp?). (~25 min)
  - 🎯 **Hardened acceptance**: the `## Fri self-review` block answers all three questions (owned-as-claimed? over/under-step? next-week adjustment?) and states one explicit on-track / off-track verdict against the 30/60/90 doc with a one-line reason — not a vibe.
  - *Mental model*: a weekly written self-review is the cheap version of the feedback you'd otherwise only get at a quarterly review — by then it's too late to course-correct. Catching an over-step (stepped on a peer's deliverable) or under-step (waited three days on a decision that was yours) inside the week is how the ramp stays on the track the W144 retrospective measures.
  - Output: `career/scope_ownership_W139.md` has a dated `## Fri self-review` block meeting the hardened-acceptance criteria.
- [ ] **[bet]** Artifact: `bets/bet2_thesis_seeds.md` (private). This is the v3-mandated deliverable (INDEX W139 [v3]: "Bet #2 thesis seeds — each bet upgrades the next"). The §7 strategy is a *chain* — `Bridge₀ → Bet #1 → Bridge₁ → Bet #2` — where each placement *upgrades the next* (network density, allocation tier, product-shape access, on-the-job market intel). Seed the Bet #2 thesis **now**, ~6 weeks into Bet #1/Bridge₀, while the inside view is fresh:
  - Record **what Bet #1/Bridge₀ is teaching you** that you couldn't see from outside: which sub-niche of on-chain derivatives is actually hard / actually defensible; which teams the people around you respect; what the next-tier product shape looks like.
  - List **2–3 candidate Bet #2 directions** (a higher-equity founding seat, a top-tier venue, or an adjacent niche — RWA-perps, structured products, on-chain options), each scored *lightly* against the §6 ecosystem-selection checklist (founding-tier allocation access, tier-1 backing, core-criticality, network warmth, product-shape readiness; crypto-MM cash is floor not goal).
  - Name the **one upgrade Bet #1 must deliver to qualify the chain step** (e.g. "TGE + token vesting that survives the cliff," or "warm intros to two tier-1 founders," or "shipped critical-path artifact I can point at").
  - This is *seeds*, not a plan — the full Bet #2 prep is W144. (~35 min)
  - 🎯 **Hardened acceptance**: `bets/bet2_thesis_seeds.md` records ≥3 inside-view learnings from Bet #1/Bridge₀; lists 2–3 candidate Bet #2 directions each with a one-line §6-checklist read (which criteria it plausibly passes/fails); and names the explicit "upgrade Bet #1 must deliver" gate. No candidate is committed — it is labelled a seed doc and dated.
  - *Mental model*: the multi-bet chain only compounds if you *capture the upgrade* each bet hands you. The single most valuable, most perishable input to Bet #2 is the inside view you have *right now* — six weeks in, before the role becomes normal and the outside-view sharpness fades. Founders who place sequential bets well are the ones who treated each seat as reconnaissance for the next; writing the seeds down on a Friday in M36 is how "each bet upgrades the next" stops being a slogan and becomes a document the W144 Bet #2 prep can actually build on.
  - Output: `bets/bet2_thesis_seeds.md` exists meeting the hardened-acceptance criteria; this is the named input to the W144 M36 Bet #2 prep.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday commit.

## Saturday — `perp-dex-core` rig maintenance (runtime tally + chaos drill)

- [ ] Artifact: append a dated W139 row to `ops/runtime_tally.md`. The `perp-dex-core` rig is still running its 3-node VSR cluster; record the latest cumulative runtime tally and confirm it advanced vs last week (a flat number = the rig stopped; investigate before logging). Note the rig's standing-uptime figure for the W144 close-out. (~15 min)
  - 🎯 **Hardened acceptance**: `ops/runtime_tally.md` has a dated W139 row with cumulative hours, a `Δ vs W138` delta that is `> 0` (or an explicit `[INVESTIGATE: flat]` flag), and the standing-uptime figure for the close-out.
  - *Mental model*: with personal-project time at ≤4 hrs/wk, the rig must run unattended and the only honest proof it did is a monotonically rising tally. This 15-min Saturday entry is the single source of truth the W141 public-artifact update and the W144 plan-close cite for the rig's sustained-runtime claim — if it isn't logged weekly it isn't real.
  - Output: `ops/runtime_tally.md` shows the W139 row meeting the hardened-acceptance criteria.
- [ ] **[rig] Saturday chaos drill** (~60 min): run this week's rotated cluster-VOPR-style drill on the rig (rotation: leader kill + view-change / network partition / clock skew / disk fill / slow-subscriber backpressure / insurance-fund-tripwire probe). Capture the drill type, what broke, recovery time, and any follow-up in `chaos_log.md`.
  - *Mental model*: the rig's value as portfolio + as the live-proof behind the cluster-VOPR claim (W108) depends on it being *known-resilient*, not just long-running. A weekly rotated drill is what lets you say "the cluster recovered from a leader-kill + view-change in N seconds with zero committed-order loss" with evidence — the kind of concrete claim that survives a founding-eng interviewer's follow-up, even mid-new-role.
  - Output: `chaos_log.md` has a dated W139 entry with {drill type, failure observed, recovery time, follow-up}.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
