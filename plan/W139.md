# Week 139 — Settling into new role

> **Phase**: 7
> **Month**: M36
> **Track**: Career (destination landing)
> **Hours target**: 30h/wk (W137-W144)
> **Mirror target**: n/a (career phase — no upstream module mirrored this week)
> **Crate(s) touched**: n/a (maintenance only on the personal HFT rig + Tempo; no crate version bumps)
> **Bar**: (n/a)
> **Feeds into**: W140-W143 settling weeks (this week's scope-ownership log + housing shortlist are the running state they extend); W144 M36 retrospective + plan close (the runtime tally and the 30/60/90 self-assessment feed the close-out); the personal HFT rig's 4000+ runtime-hours target by M36 (Saturday tally keeps the counter honest).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): your firm's onboarding wiki + the 30/60/90 doc your manager set (Monday scope-ownership baseline, lookup-when-stuck); destination-geography rental/tenancy portals (Thursday housing research — official + reputable-agency sources only); README §"Weekly HFT maintenance template (W97-W144)" + §"Monthly HFT polish checkpoints" M36 line (Saturday runtime tally target).
> **v2 modifications**: none

This is a career/settling week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding. You are ~6-7 weeks into the new firm, ramping from "execute well-scoped tickets" to "own a larger project end-to-end," and beginning the permanent-housing search (W139-W143). Every item below carries an explicit artifact file + an Output line that proves it done; the two judgment/research items (Monday scope-ownership, Thursday housing) carry a hardened-acceptance pass criterion and a mental model of why the item exists now. The week's job: convert the first month's earned trust into owned scope, and convert temporary housing into a permanent-housing plan — without letting the personal rig's runtime counter drift before the M36 close.

**Monday — Larger project ramp**

- [ ] Artifact: `career/scope_ownership_W139.md` (private). The substantive work is now a larger project you own scope on. Capture before you start the week: the project's stated goal in one sentence, the 3-5 sub-deliverables you now own (vs ones still owned by others), the explicit decisions that are now yours to make, and the named blockers/dependencies you must drive rather than wait on. This is the difference between "doing tickets" and "owning scope" — write it down so the boundary is explicit, not assumed. (~30 min)
  - 🎯 Hardened acceptance: `career/scope_ownership_W139.md` names the project goal in one sentence, lists ≥3 sub-deliverables tagged `[mine]` / `[shared]` / `[theirs]`, and names ≥2 dependencies tagged `[I drive]` vs `[I wait]` — every dependency has an owner name attached, not a "TBD".
  - *Mental model*: at the 30/60/90 boundary the firm's implicit promotion question is "can this IC be handed an ambiguous project and turn it into shipped scope without supervision?" Writing the ownership boundary down on day one of the larger project is how you answer it deliberately instead of accidentally over-stepping (stepping on a peer's scope) or under-stepping (waiting for direction that won't come). The W144 retrospective scores exactly this transition.
  - Output: `career/scope_ownership_W139.md` exists meeting the hardened-acceptance criteria; `git log -1 --oneline` (private repo) shows the Monday commit.
- [ ] **[HFT] Monday maintenance** (~15 min): review weekend runtime alerts on the personal rig — any P99 spikes, replica divergence events, or disk-fill warnings? Append findings to `ops/alert_log.md` (even "clean weekend" is an entry, so the record is unbroken). (~15 min)
  - *Mental model*: the rig is now running with ≤4 hrs/wk of attention (the new normal from W138). A standing 15-min Monday alert-review is the minimum viable touch that keeps a silent failure from quietly burning the M36 4000-hour target — an unreviewed alert is an outage you didn't notice.
  - Output: `ops/alert_log.md` has a dated W139-Mon entry (clean or with findings).
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday commit.

**Tuesday — Work**

- [ ] Artifact: append to `career/scope_ownership_W139.md` a `## Tue progress` section. Execute the day's substantive work on the owned project; at end of day log the one decision you made independently (and why) and the one thing you escalated (and why it crossed the escalate line). (~20 min logging, on top of the day's work)
  - *Mental model*: the decision/escalation split, logged daily, is the running evidence of calibrated judgment — too many escalations reads as "needs hand-holding," too few reads as "ran ahead of the team." Keeping the log makes the calibration visible and gives the Friday self-review concrete material instead of vibes.
  - Output: `career/scope_ownership_W139.md` has a dated `## Tue progress` block with ≥1 `[decided]` and ≥1 `[escalated]` line (either may be "none today — <reason>").
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday commit.

**Wednesday — Work**

- [ ] Artifact: append `## Wed progress` to `career/scope_ownership_W139.md` (same decision/escalation format). (~15 min logging, on top of the day's work)
  - Output: `career/scope_ownership_W139.md` has a dated `## Wed progress` block.
- [ ] **[HFT] Wednesday maintenance** (~30 min): check the personal rig's operations dashboards; capture the running runtime-hours total and update the HFT row in `progress.md`. (~30 min)
  - *Mental model*: M36 end (W144) targets 4000+ runtime hours. A weekly Wednesday capture is the only thing that makes the W144 close-out number trustworthy rather than reconstructed-from-memory — and a flat-lined counter week-over-week is itself the signal that the rig has silently stopped.
  - Output: `progress.md` HFT row shows an updated W139 runtime-hours total with date.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday commit.

**Thursday — Permanent-housing research**

- [ ] Artifact: `relocation/housing_shortlist.md` (private). You are in temporary housing; this week begins the permanent search (W139-W143). Capture for the destination geography: ≥3 candidate neighborhoods with {approx commute to office, indicative rent/buy band, one pro + one con}, the tenancy mechanics that differ from Vietnam (deposit norms, contract length, what a landlord/agency requires from a recently-arrived foreign hire — proof of employment, local guarantor, bank reference), and any timeline gotcha (notice period on the temporary place, lease start lead time). Source from official tenancy portals + reputable local agencies; flag any number you couldn't verify. (~40 min)
  - 🎯 Hardened acceptance: `relocation/housing_shortlist.md` covers ≥3 neighborhoods each with {commute, rent/buy band + source, pro, con}; documents the deposit + contract-length + landlord-requirements mechanics; every monetary/timeline figure carries a source link or an explicit `[UNVERIFIED]` tag — no unsourced claims.
  - *Mental model*: permanent housing is the gate between "I moved for a job" and "I live here" — getting it wrong (wrong commute, lease you can't exit, a deposit you didn't budget for) taxes every working week after. Doing the research as a sourced shortlist now, while temporary housing buys you slack, turns a high-stakes rushed decision into a deliberate one and feeds the W140-W143 settling weeks a concrete starting state instead of a blank page.
  - Output: `relocation/housing_shortlist.md` exists meeting the hardened-acceptance criteria.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday commit.

**Friday — Work + week self-review**

- [ ] Artifact: append `## Fri self-review` to `career/scope_ownership_W139.md`. Re-read the week's decision/escalation log; answer in ≤5 lines: did I own the scope I claimed Monday, where did I over- or under-step, and what is the one scope-ownership adjustment for next week? Map the week against the 30/60/90 expectations (am I on track for this point in the ramp?). (~25 min)
  - 🎯 Hardened acceptance: the `## Fri self-review` block answers all three questions (owned-as-claimed? over/under-step? next-week adjustment?) and states one explicit on-track / off-track verdict against the 30/60/90 doc with a one-line reason — not a vibe.
  - *Mental model*: a weekly written self-review is the cheap version of the feedback you'd otherwise only get at a quarterly review — by then it's too late to course-correct. Catching an over-step (stepped on a peer's deliverable) or under-step (waited three days on a decision that was yours) inside the week is how the ramp stays on the promotion track the W144 retrospective measures.
  - Output: `career/scope_ownership_W139.md` has a dated `## Fri self-review` block meeting the hardened-acceptance criteria.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Friday commit.

**Saturday — Personal sandbox maintenance**

- [ ] Artifact: append a dated W139 row to `ops/runtime_tally.md`. The operations rig is still running — record the latest cumulative runtime tally and confirm it advanced vs last week (a flat number = the rig stopped; investigate before logging). Note remaining gap to the M36 4000-hour target. (~15 min)
  - 🎯 Hardened acceptance: `ops/runtime_tally.md` has a dated W139 row with cumulative hours, a `Δ vs W138` delta that is > 0 (or an explicit `[INVESTIGATE: flat]` flag), and the remaining-gap-to-4000h figure.
  - *Mental model*: with personal-project time at ≤4 hrs/wk, the rig must run unattended and the only honest proof it did is a monotonically rising tally. This 15-min Saturday entry is the single source of truth the W144 plan-close cites for "4000+ runtime hours by M36" — if it isn't logged weekly it isn't real.
  - Output: `ops/runtime_tally.md` shows the W139 row meeting the hardened-acceptance criteria.
- [ ] **[HFT] Saturday chaos drill** (~60 min): run this week's rotated small chaos drill on the rig (rotation: process kill / network partition / clock skew / disk fill / slow subscriber). Capture the drill type, what broke, recovery time, and any follow-up in `chaos_log.md`. (~60 min)
  - *Mental model*: the rig's value as portfolio + as the 4000-hour proof depends on it being known-resilient, not just long-running. A weekly rotated drill is what lets you say "it recovered from a clock-skew injection in N seconds" with evidence — the kind of concrete claim that survives an HFT interviewer's follow-up, even mid-new-role.
  - Output: `chaos_log.md` has a dated W139 entry with {drill type, failure observed, recovery time, follow-up}.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
