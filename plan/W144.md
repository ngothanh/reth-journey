# Week 144 — M36 retrospective + Bet #2 prep + plan formal close

> **Phase**: 7 (Bet Placement / Bridge)
> **Month**: M36
> **Track**: Bet-path close — at the bet (founding/core-eng on an on-chain-derivatives venue) OR on the crypto-MM cash bridge; plan formal close
> **Hours target**: 30h/wk (W137–W144 window)
> **Mirror target**: (n/a — plan-close retrospective week; the Saturday `Đối chiếu` zero-dep audit mirrors TigerStyle "Zero Dependencies" + a `cargo-deny`/SBOM-class supply-chain check, diffed mechanically against the W117 M30 snapshot)
> **Crate(s) touched**: (none new — maintenance only on the venue half (`perp-dex-core` + matching/`oracle-mark`/`risk-engine`/`liquidation-engine`/`consensus-vsr`/`ledger-deterministic`) + the Reth spine + Tempo + the Layer-1/HFT bar-(c) crates)
> **Bar**: (n/a — retrospective/close week; no crate built, so no bar (c) acceptance arc — the only hardened gate is the Saturday zero-dep audit against the M30 baseline)
> **Feeds into**: **Bet #2 thesis** (each bet upgrades the next — §7 shots-on-goal: reputation + network + capital from Bet #1 raise the allocation tier reachable at Bet #2); the Year-4 personal-projects backlog (`notes/year4_outline.md` seeds the post-plan ≤4h/wk cadence + the `perp-dex-core` v1.5/v2 continuation); any M37 dep-cleanup backlog surfaced by the Saturday audit; the permanent inheritance-discipline habit that outlives the plan.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): `RECONCILED_PLAN_v3.md` §1 (four readiness bars — the close metric source-of-truth), §6 (ecosystem-selection checklist — re-run against the placed/targeted venue for the Bet #2 seed), §7 (shots-on-goal + geography flip-trigger), §4A (capstone v1.0 lock + v1.5 ladder — the artifact the retro measures); `notes/zero_dep_audit_M30.md` (W117 Saturday baseline to diff against); TigerStyle "Zero Dependencies" (https://tigerstyle.dev/); `cargo tree` / `cargo metadata` book sections (lookup only); `progress.md` (the 143 prior weekly logs, read in the Sunday close ritual — as a retro source, NOT cover-to-cover study).
> **v3 modifications**: (1) **Close metrics re-aimed from job-landing to the four bet-readiness bars (§1).** v2 closed on "ready to land a Tier-A HFT IC job" with a 7-system coverage = 85 quality gate as the single headline; v3 closes on **bars (a)–(d): core-skill mastery (coverage ≥ 85, ≥ 4000h), portfolio clears the founding-eng bar (`perp-dex-core` v1.0 shipped at bar (c) + ≥ 3 public artifacts), activated network (≥ 5 warm intros / ≥ 2 "would bring you in"), financial runway (bridge or bet income floor)**. (2) **Capstone in the tally is `perp-dex-core` (the v3 swap), not `mini-db`** — the DB substrate is now consumed *inside* the venue store, and the `mini-db` KV/query facade stayed **deferred (DO-NOT-BUILD)**, which is itself a close-metric line ("did we resist the un-needed capstone?"). (3) **Forward artifact = Bet #2 thesis seeds**, not "next-chapter IC promotion at a destination firm" — the terminal state is the founding token-equity bet, and the close seeds the *next* shot, not a job ladder. (4) Frontmatter narrative re-aimed from "destination landing (job)" to "at the bet OR on the crypto-MM bridge."

This is the plan's **formal close week** — a retrospective/bet-path week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding. Every item below instead carries an explicit artifact file + an Output line that proves it done; the retrospective, metrics, and recall items carry a hardened-acceptance pass criterion and a mental model of why the item exists now. The week's job: turn three years of weekly logs into a recorded retrospective + a final close-metric tally **measured against the four v3 readiness bars**, confirm the zero-dep discipline held under Phase-7's bet-placement pressure, seed the **Bet #2 thesis** (each bet upgrades the next), and seed the post-plan Year-4 cadence — so the plan ends measured, not vibed.

**The v3 frame for this close (read before Monday):** v2 ended at *"ready to land a Tier-A HFT IC job."* v3 ends at *"a founding/core-engineer token-equity bet placed on an on-chain-derivatives venue — OR the crypto-MM cash bridge engaged while Bet #1 outreach continues."* The job is the income floor, not the goal (§1 bar (d), §7). So the close metrics are **the four bars**, the headline artifact is **`perp-dex-core` v1.0** (replicated, fault-tolerant, multi-node hybrid CLOB whose centerpiece is the cross-margin + liquidation engine — the principal-defining artifact, §4A), and the forward-looking artifact is the **Bet #2 thesis**, because the structure is 2–3 sequential shots, never one all-in (§7). Everything below is measured against that frame.

---

## Monday — Final retrospective draft (against the four readiness bars)

- [ ] **Retrospective — what worked**: artifact `notes/m36_retro.md` (section "What worked"). Name each win with the concrete evidence that proves it, not a vibe: the inheritance principle (every crate mirrors a named upstream — cite 2 examples, e.g. `consensus-vsr` ↔ TigerBeetle `replica.zig`, `ledger-deterministic` ↔ TigerBeetle); the **one-hot-path-log lock** (the order/event log IS the VSR log — "no primitive twice" held end-to-end into the capstone); the workspace layout; the Sunday rituals; the decision gates at M6/M12/M24/M30; the v3 reframe itself (re-aiming `mini-db` → `perp-dex-core` and elevating the three domain crates — and **resisting** the deferred `mini-db` facade). (~60 min)
  - 🎯 Hardened acceptance: each of the ≥5 wins has ≥1 concrete artifact citation (a crate path, a merged-PR number, a gate-decision note, a blog URL) — no claim stands without evidence; **at least one win is tied to a v3-specific call** (the capstone swap, the VSR-single-log lock, the domain-crate elevation, or the DO-NOT-BUILD discipline on the `mini-db` facade).
  - *Mental model*: a retrospective that lists wins without evidence is just self-congratulation; tying each win to an artifact makes it a reusable input for Bet #2 decisions and for explaining your method in bet/bridge conversations ("here's the principle, here's the crate it produced, here's why the venue spine is one log"). The plan's whole thesis was inheritance + no-primitive-twice — the retro must demonstrate, not assert, that it held all the way into `perp-dex-core`.
  - Output: `notes/m36_retro.md` "What worked" section lists ≥5 wins, each with ≥1 cited artifact/PR/path, ≥1 v3-specific.
- [ ] **Retrospective — what didn't**: same artifact `notes/m36_retro.md` (section "What didn't"). Candidates: budget overruns (W42/W63/W84 historically); Phase 5 tighter than budgeted; Tempo time crowding the Reth spine more than ideal; any v3-pass friction (e.g. `risk-engine` per-mark-tick incremental-margin hot path being harder than the back-of-envelope predicted at the 100k-account target; cluster-VOPR honesty requiring `MessageBus`/`Storage`/`Clock` seams retrofitted somewhere they weren't designed in). For each, name the root cause (not just the symptom) and whether it recurred. (~30 min)
  - 🎯 Hardened acceptance: each miss names a root cause and a recur/no-recur tag, cross-checked against `progress.md` for the cited weeks; **at least one miss is evaluated against a §9 risk-register line** (did the mitigation hold? e.g. "did `perp-dex-core` scope balloon despite the §4A v1.0 lock?").
  - *Mental model*: a miss without a root cause can't be prevented in Bet #1/#2 or Year 4; tagging recurrence separates one-off slips from systemic patterns (e.g. "Tempo crowding Reth" recurring = a structural time-allocation flaw, not bad luck). Cross-checking against §9 closes the loop: the risk register pre-named the dangers; the retro grades the mitigations.
  - Output: `notes/m36_retro.md` "What didn't" section, each miss with {root cause, recur/no-recur, §9-line-if-applicable}.
- [ ] **Retrospective — what I'd do differently**: same artifact `notes/m36_retro.md` (section "Differently"). Candidates: more aggressive scope-cutting on perpetuals features held to the v1.5 ladder; earlier ops/cluster-VOPR rig (W105 was fine, earlier would have caught replication issues sooner); pulling the §6 ecosystem-selection filter earlier into outreach so finalist conversations were pre-qualified. State each as an actionable rule, not a regret. (~30 min)
  - 🎯 Hardened acceptance: each "differently" is phrased as a transferable rule (an "if X then Y") that could apply to **Bet #2 or a future founding-eng project**, not a one-time apology.
  - *Mental model*: "I'd do X differently" is only useful if it becomes a rule you carry forward — Bet #2 and Year-4 work inherit these rules, so they must be portable (e.g. "if a domain feature isn't required for the v1.0 acceptance lock, it ships in v1.5 — the differentiator ladder is a scope weapon, not a wishlist"), not plan-specific.
  - Output: `notes/m36_retro.md` "Differently" section lists actionable transferable rules.
- [ ] Commit + log (~10 min) — Output: `git log -1 --oneline` shows the Monday retro commit.

## Tuesday — Final metrics tally (the four v3 readiness bars)

- [ ] **Close-metrics tally**: artifact `notes/m36_close_metrics.md` — grouped by the **four readiness bars (§1)**, one row per metric, columns {bar, target, actual, source-of-truth, met?}. Tally all of: (~90 min)
  - **Bar (a) — core-skill mastery**: 7-system coverage score (**target ≥ 85/100**); runtime hours (**target ≥ 4000**); coverage matrix (§3) has **no red cells** across {`jeff-dean`, `hft-review`, derivatives-domain}; bar (c) held on the HFT critical path (the 10 Layer-1/HFT crates).
  - **Bar (b) — portfolio clears the founding-eng bar**: `perp-dex-core` **v1.0 shipped (bar c)** — 3-node VSR, the order/event log IS the VSR log, multi-instrument cross-margin (BTC-PERP + ETH-PERP, const 30% netting-benefit haircut), partial-liq + fee-accrued insurance fund (10% taker, declared param) + waterfall, `SettlementId(snapshot_epoch, op_number, checksum)` async settle, two projection staleness contracts, ~100k accounts / ~1M orders/s with incremental margin, cluster-VOPR StateChecker green (tripwire never hit); **≥ 3 supporting public artifacts** (latency report, VOPR/cluster-VOPR bug-find writeups, flagship blog); blog posts (**target 7**); HFT/Layer-1 bar-(c) crates shipped (the matching-engine / `oracle-mark` / `risk-engine` / `liquidation-engine` / `consensus-vsr` / `ledger-deterministic` / `messaging-aeron` / `marketdata-kernelbypass` / `runtime-thread-per-core` / `latency-lab` set); **`perp-dex-core` v1.5 status** (RWA features / ADL / multi-collateral — shipped, in-progress, or deferred to the bet); **`mini-db` facade status = DO-NOT-BUILD honored** (a *positive* close metric: did we resist the un-needed capstone? substrate-in-venue proved DB internals without it).
  - **Bar (c) — activated network**: Binance-alumni warm intros logged (**target ≥ 5**); conversations advanced to "would bring you in" (**target ≥ 2**); ≥ 20 targeted on-chain-derivatives conversations / response rate ≥ 25% (the M24 controllable-outbound metric, rolled forward); Reth/Tempo maintainer relationships; HFT/crypto-MM engineers met IRL (event-trip recon).
  - **Bar (d) — financial runway**: bet OR bridge income floor secured; ≥ 6–12 months runway at the bet-entry point; Reth/Tempo merged-PR counts (the crypto-infra-IC bridge fallback evidence, §7).
  - 🎯 Hardened acceptance: every "actual" cell has a named source-of-truth (e.g. `gh pr list --author @me --state merged` count, the `runtime_hours.md` ledger, the cluster-VOPR CI artifact, the relationship CRM note, the §6 ecosystem-shortlist file) — no `?` left, and any unmet target carries a one-line reason; **each of the four bars has an explicit MET / NOT-MET verdict** (a plan that's green on (a)/(b) but red on (c)/(d) is a *bridge-engaged* close, not a *bet-placed* close — say which).
  - *Mental model*: these are the close metrics the entire 144-week plan was steering toward — but the steering target changed in v3 from "job-ready" to "**bet-ready** (the four bars)." An honest table (with sources so the numbers are reproducible, not remembered) is what lets you say in a Bet #2 conversation "I shipped a replicated multi-node perp-DEX-core at bar (c), here are the cluster-VOPR receipts, and here's the network + runway behind it." The four-bar verdict is the real close: it states whether the plan terminated at *Bet #1 placed* or *bridge engaged + Bet #1 outreach continuing* — both are valid plan-success states under §7 (2–3 shots, never one all-in).
  - Output: `notes/m36_close_metrics.md` grouped by the four bars, table fully populated (no `?`), each actual cited to a source-of-truth, unmet targets annotated, **a MET/NOT-MET verdict per bar + an overall "bet-placed vs bridge-engaged" close-state line**.
- [ ] Commit + log (~10 min) — Output: `git log -1 --oneline` shows the Tuesday metrics commit.

## Wednesday — Next-chapter plan + Bet #2 thesis seeds

- [ ] **Bet #2 thesis seeds**: artifact `notes/bet2_thesis_seeds.md`. The terminal state is the founding token-equity bet, structured as **2–3 sequential shots** (§7) — Bet #1 (placed or in-outreach) upgrades the next. Seed Bet #2: (a) the **reputation + network + capital delta** Bet #1 created (what allocation tier / ecosystem quality is now reachable that wasn't at M30?); (b) the §6 ecosystem-selection checklist **re-run against 2–3 candidate venues** for the next shot (founding-tier allocation, tier-1 backing, core-criticality, ≥$1B-FDV ceiling, token reality, remote-native, cycle timing); (c) the **capstone v1.5/v2 continuation** as the tier-raiser narrative (RWA features + multi-collateral + ADL + real-chain settle into the *same* `SettlementId` signature — the moat that justifies a higher Bet #2 allocation); (d) the **geography flip-trigger** state (§7: did warm conversations cluster in Dubai/SG, or did a presence-contingent offer premium appear? — note the trigger status, do not pre-commit). (~45 min)
  - 🎯 Hardened acceptance: ≥2 candidate Bet #2 venues each scored against all 7 §6 checklist items (GO/NO-GO per item); the v1.5/v2 continuation named with a concrete first next-step; the flip-trigger status recorded as fired / not-fired with the evidence.
  - *Mental model*: a single all-in bet is the high-variance-of-ruin path the plan explicitly rejects (§7); seeding Bet #2 *now*, at the close, captures the network and reputation delta while it's fresh and keeps the shots-on-goal structure live. Re-running §6 (not inventing new criteria) keeps the gate consistent — a bet is GO only if all seven pass, otherwise it's a bridge, not a bet. This is the only artifact that points at the *next* shot, so it must be concrete enough to act on the moment a Bet #2 window opens.
  - Output: `notes/bet2_thesis_seeds.md` with {Bet #1 delta, ≥2 §6-scored candidate venues, v1.5/v2 tier-raiser + next-step, flip-trigger status}.
- [ ] **Year-4 outline**: artifact `notes/year4_outline.md`. By now at the bet (founding/core-eng) or on the crypto-MM cash bridge. Define the sustainable post-plan cadence: personal projects + OSS at **≤ 4 hrs/wk**; the **vesting-milestone tracker** for the bet's token/equity (never quit a bridge before the live bet's vest milestones — §7); open-source (Reth + Tempo + the venue/HFT/Layer-1 crates) stays maintained but is no longer the day's primary work. Name the 2–3 concrete threads Year 4 continues (seeded from the Saturday M37 backlog + the Monday "differently" rules + the Bet #2 thesis): e.g. `perp-dex-core` v1.5 (the tier-raiser), the deferred `vector-db` v0.5 / `mini-db` facade ONLY if a §6-passing AI-infra/DB-infra bet appears (default: stay deferred), the Bet #2 outreach cadence. (~45 min)
  - 🎯 Hardened acceptance: outline states the weekly-hour cap, the **vesting-milestone tracker + the "never quit a bridge before vest" rule**, the OSS maintenance posture, and ≥2 named Year-4 threads each with a first concrete next-step; the deferred optionality (`vector-db` / `mini-db` facade) is explicitly marked **default-deferred** with its build-trigger condition.
  - *Mental model*: without a written cadence, "keep doing projects on the side" silently expands back to plan-level intensity and burns you out in a real bet/bridge role — the ≤4h/wk cap is a load-bearing constraint. Naming threads now prevents Year-4 drift into whatever's shiny, and the vesting tracker is what keeps the income floor intact through the bet's cliff (the §7 discipline that protects the whole shots-on-goal structure).
  - Output: `notes/year4_outline.md` with {hour cap, vesting tracker + never-quit-before-vest rule, OSS posture, ≥2 named threads + next-steps, deferred-optionality build-triggers}.
- [ ] Commit + log (~10 min) — Output: `git log -1 --oneline` shows the Wednesday next-chapter commit.

## Thursday — Work at the bet/bridge

- [ ] Day work at the bet (the core engine you were brought in to build) or the crypto-MM cash bridge (plan is in maintenance-only mode). Log the day's plan-adjacent activity (any OSS maintenance touch / `perp-dex-core` v1.5 continuation, if any) in `progress.md`. (~10 min) — Output: `progress.md` has a Thursday W144 entry (even if "no plan work today"), tagged with the close-state (bet vs bridge).

## Friday — Work at the bet/bridge

- [ ] Day work at the bet or bridge. Log in `progress.md`. (~10 min) — Output: `progress.md` has a Friday W144 entry.

## Saturday — Close + final zero-dep audit

- [ ] **Đối chiếu (final zero-dep audit)**: artifact `notes/zero_dep_audit_M36.md`, regenerated by re-running the W117 `scripts/zero_dep_audit.sh` (or `cargo xtask zero-dep-audit`) against today's workspace, then diffed against `notes/zero_dep_audit_M30.md`. This is the M36 close metric and the plan's last technical act. Per workspace crate: `cargo tree --depth 1`, compare direct non-mirror dep count + per-dep rationale to the W117 snapshot, document every addition/removal with a delta column. The new v3 domain + capstone crates (`oracle-mark`, `risk-engine`, `liquidation-engine`, `perp-dex-core`) are in scope — they were built between M24 and M30 and must hold the same bar-(c) ≤10-dep discipline. (~120 min)
  - *Mental model*: the TigerStyle zero-dep discipline was the leading indicator of bar-(c) quality at M30; M36 measures whether the discipline **held under Phase-7's bet-placement pressure** — which is exactly the "just add a dep, I'm busy landing the bet / interviewing for the bridge" period where bloat creeps in. The capstone is the riskiest crate here: a multi-node hybrid CLOB is a natural magnet for "convenience" deps. Re-running the same script the M30 gate used (rather than eyeballing) makes the diff mechanical and trustworthy. A clean diff is the proof the discipline became a habit, not a gate you only respected when watched — and it's part of the bar-(b) "portfolio clears the founding-eng bar" story (a reviewer who opens `perp-dex-core/Cargo.toml` sees a deliberate, audited dependency surface).
  - 🎯 Hardened acceptance:
    - **Comparison artifact** `notes/zero_dep_audit_M36.md`: side-by-side M30 vs M36 direct non-mirror dep counts per crate, with a delta column that flags any growth; the four v3 crates (`oracle-mark`, `risk-engine`, `liquidation-engine`, `perp-dex-core`) each have a row.
    - **No surprises**: every dep added between M30→M36 has a recorded rationale; any added by "expedience" without justification is flagged into `notes/year4_outline.md` as an M37 cleanup backlog item (closing the loop to Wednesday's outline).
    - **Close metric (committed number)**: workspace-wide direct non-mirror dep total ≤ **150** across the 30+ crates, AND ≤ 10 direct non-mirror deps per bar-(c) crate (the Layer-1/HFT + venue crates) — same threshold the W117 gate held; **`perp-dex-core` itself ≤ 10** despite being the assembly crate (it inherits, it does not re-pull).
    - **Mechanical**: the audit is regenerated by the W117 script in <30s, not hand-counted; cite the command run.
    - **Cited URL**: https://tigerstyle.dev/ (Zero Dependencies); diff explicitly against `notes/zero_dep_audit_M30.md`.
  - Output: `notes/zero_dep_audit_M36.md` exists with per-crate M30-vs-M36 delta table; workspace total ≤150 and every bar-(c) crate (incl. `perp-dex-core`) ≤10 (or each exception flagged into the M37 backlog).
- [ ] **Plan formal close**: append the close marker to `progress.md`. The plan formally ends today; the inheritance discipline + the shots-on-goal structure continue forever. (~30 min) — Output: `progress.md` final entry states "Plan formally closed W144" + the four-bar close-metrics summary line (coverage / hours / `perp-dex-core` v1.0 status / network warm-intros / runway) + the close-state ("Bet #1 placed" or "bridge engaged, Bet #1 outreach live").
- [ ] Commit + log (~10 min) — Output: `git log -1 --oneline` shows the Saturday close commit.

## Sunday — Rest + Plan Close Ritual

- [ ] **Full retrospective read** of `progress.md` from W1 to today — three years of weekly logs. Read for patterns, not completeness (treat it as a retro source, not a re-study). Note in `notes/m36_retro.md` (section "Three-year patterns") the 3–5 recurring patterns + what surprised you — including how the **v3 reframe at ~W73** (job → bet, `mini-db` → `perp-dex-core`) changed the back half versus the v2 weeks. (~60 min)
  - 🎯 Hardened acceptance: `notes/m36_retro.md` gains a "Three-year patterns" section with ≥3 named patterns + ≥1 surprise, each grounded in a specific week range from the logs; **at least one pattern spans the v2→v3 boundary** (e.g. "the primitive substrate W1–W72 was reused unchanged into the venue half — the reframe was directed, not destructive, exactly as INDEX promised").
  - *Mental model*: the weekly logs are the only ground-truth record of how the three years actually went (vs how you remember them); reading them end-to-end once, at the close, surfaces patterns no single week could show — and grounds the Monday "what worked / differently" claims in evidence rather than recency bias. The v2→v3 boundary is the single most important structural event in the plan; the close ritual is where you confirm the no-primitive-twice thesis paid off (the same Layer-0/1 crates fed both the v2 HFT job-track and the v3 venue bet-track without a rewrite).
  - Output: `notes/m36_retro.md` "Three-year patterns" section with ≥3 patterns + ≥1 surprise, each cited to a week range, ≥1 spanning v2→v3.
- [ ] The plan is done. The work continues — Bet #1 is live, Bet #2 is seeded. (~30 min) — Output: a final reflection line appended to `notes/m36_retro.md`; close the file.

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
