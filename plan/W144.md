# Week 144 — M36 retrospective + plan formal close

> **Phase**: 7
> **Month**: M36
> **Track**: Career (destination landing) — plan close
> **Hours target**: 30h/wk (W137-W144)
> **Mirror target**: (n/a — plan-close retrospective week; the Saturday `Đối chiếu` zero-dep audit mirrors TigerStyle "Zero Dependencies" + a `cargo-deny`/SBOM-class supply-chain check, diffed mechanically against the W117 M30 snapshot)
> **Crate(s) touched**: (none new — maintenance only on Reth + Tempo + the 10 bar-(c) Layer-1/HFT crates)
> **Bar**: (n/a — retrospective/career week; no crate built)
> **Feeds into**: Year-4 personal-projects backlog (`notes/year4_outline.md` seeds the post-plan ≤4h/wk cadence); any M37 dep-cleanup backlog surfaced by the Saturday audit; the permanent inheritance-discipline habit that outlives the plan.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): `notes/zero_dep_audit_M30.md` (W117 Saturday baseline to diff against); TigerStyle "Zero Dependencies" (https://tigerstyle.dev/); `cargo tree` / `cargo metadata` book sections (lookup only); `progress.md` (the 143 prior weekly logs, read in the Sunday close ritual — as a retro source, NOT cover-to-cover study).
> **v2 modifications**: M36 retrospective uses v2's 7-system coverage score (target 85/100) as one of the close metrics; the Saturday audit's close target is stated as a committed number, not "likely".

This is the plan's **formal close week** — a retrospective/career week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding. Every item below instead carries an explicit artifact file + an Output line that proves it done; the retrospective, metrics, and recall items carry a hardened-acceptance pass criterion and a mental model of why the item exists now. The week's job: turn three years of weekly logs into a recorded retrospective + a final close-metric tally, confirm the zero-dep discipline held under Phase-7 pressure, and seed the post-plan Year-4 cadence — so the plan ends measured, not vibed.

**Monday — Final retrospective draft**

- [ ] **Retrospective — what worked**: artifact `notes/m36_retro.md` (section "What worked"). Name each win with the concrete evidence that proves it, not a vibe: the inheritance principle (every crate mirrors a named upstream — cite 2 examples); the workspace layout; the Sunday rituals; the decision gates at M12/M24/M30; the HFT track addition at M15. (~60 min)
  - 🎯 Hardened acceptance: each of the 5 wins has ≥1 concrete artifact citation (a crate path, a merged-PR number, a gate-decision note) — no claim stands without evidence.
  - *Mental model*: a retrospective that lists wins without evidence is just self-congratulation; tying each win to an artifact makes it a reusable input for Year-4 decisions and for explaining your method in interviews ("here's the principle, here's the crate it produced"). The plan's whole thesis was inheritance — the retro must demonstrate, not assert, that it held.
  - Output: `notes/m36_retro.md` "What worked" section lists 5 wins, each with ≥1 cited artifact/PR/path.
- [ ] **Retrospective — what didn't**: same artifact `notes/m36_retro.md` (section "What didn't"). Budget overruns W42/W63/W84; Phase 5 tighter than budgeted; Tempo time crowded Reth more than ideal. For each, name the root cause (not just the symptom) and whether it recurred. (~30 min)
  - 🎯 Hardened acceptance: each miss names a root cause and a recur/no-recur tag, cross-checked against `progress.md` for the cited weeks.
  - *Mental model*: a miss without a root cause can't be prevented in Year 4; tagging recurrence separates one-off slips from systemic patterns (e.g. "Tempo crowding Reth" recurring = a structural time-allocation flaw, not bad luck).
  - Output: `notes/m36_retro.md` "What didn't" section, each miss with {root cause, recur/no-recur}.
- [ ] **Retrospective — what I'd do differently**: same artifact `notes/m36_retro.md` (section "Differently"). More aggressive scope-cutting on perpetuals features; earlier ops rig (W90 was fine, W80 would have caught issues sooner). State each as an actionable rule, not a regret. (~30 min)
  - 🎯 Hardened acceptance: each "differently" is phrased as a transferable rule (an "if X then Y") that could apply to a future project, not a one-time apology.
  - *Mental model*: "I'd do X differently" is only useful if it becomes a rule you carry forward — Year-4 personal projects inherit these rules, so they must be portable, not plan-specific.
  - Output: `notes/m36_retro.md` "Differently" section lists actionable rules.
- [ ] Commit + log (~10 min) — Output: `git log -1 --oneline` shows the Monday retro commit.

**Tuesday — Final metrics tally**

- [ ] **Close-metrics tally**: artifact `notes/m36_close_metrics.md` — a single table, one row per metric, columns {target, actual, source-of-truth, met?}. Tally all of: Reth PRs merged (target 85+); HFT crates shipped (5: matching-engine, ledger-deterministic, messaging-aeron, marketdata-kernelbypass, mini-db, plus vector-db v0.5); Tempo PRs merged (target 38); runtime hours (target 4000); blog posts (target 7); conferences attended (target 4); direct relationships — Reth maintainers (10), Tempo maintainers (6), HFT engineers met IRL (8+); **v2 7-system coverage score (target 85/100)**. (~90 min)
  - 🎯 Hardened acceptance: every "actual" cell has a named source-of-truth (e.g. `gh pr list --author @me --state merged` count, the `runtime_hours.md` ledger, the relationship CRM note) — no `?` left, and any unmet target carries a one-line reason.
  - *Mental model*: these are the close metrics the entire 144-week plan was steering toward; an honest table (with sources so the numbers are reproducible, not remembered) is what lets you say in an interview "I shipped 85 merged Reth PRs and 5 bar-(c) HFT crates over 3 years" with receipts. The 7-system coverage score is the v2 quality gate — below 85 means a system was under-built, which is a Year-4 backlog item, not a footnote.
  - Output: `notes/m36_close_metrics.md` table fully populated (no `?`), each actual cited to a source-of-truth, unmet targets annotated.
- [ ] Commit + log (~10 min) — Output: `git log -1 --oneline` shows the Tuesday metrics commit.

**Wednesday — Next-chapter plan**

- [ ] **Year-4 outline**: artifact `notes/year4_outline.md`. By now in a destination-tier IC role. Define the sustainable post-plan cadence: personal projects evenings + weekends at ≤4 hrs/wk; IC-promotion target within 18-24 months at the new firm; open-source (Reth + Tempo + the HFT/Layer-1 crates) stays maintained but is no longer the day's primary work. Name the 2-3 concrete projects/threads Year 4 continues (seeded from the Saturday M37 backlog + the Monday "differently" rules). (~45 min)
  - 🎯 Hardened acceptance: outline states the weekly-hour cap, the promotion-timeline target, the OSS maintenance posture, and ≥2 named Year-4 threads each with a first concrete next-step.
  - *Mental model*: without a written cadence, "keep doing projects on the side" silently expands back to plan-level intensity and burns you out in a real job — the ≤4h/wk cap is a load-bearing constraint, and naming threads now prevents Year-4 drift into whatever's shiny. This is the only forward-looking artifact the close produces, so it must be concrete enough to act on day one.
  - Output: `notes/year4_outline.md` with {hour cap, promotion target, OSS posture, ≥2 named threads + next-steps}.
- [ ] Commit + log (~10 min) — Output: `git log -1 --oneline` shows the Wednesday next-chapter commit.

**Thursday — Work at new firm**

- [ ] Day job at the destination firm (plan is in maintenance-only mode). Log the day's plan-adjacent activity (any OSS maintenance touch, if any) in `progress.md`. (~10 min) — Output: `progress.md` has a Thursday W144 entry (even if "no plan work today").

**Friday — Work at new firm**

- [ ] Day job at the destination firm. Log in `progress.md`. (~10 min) — Output: `progress.md` has a Friday W144 entry.

**Saturday — Close + final zero-dep audit**

- [ ] **Đối chiếu (final zero-dep audit)**: artifact `notes/zero_dep_audit_M36.md`, regenerated by re-running the W117 `scripts/zero_dep_audit.sh` (or `cargo xtask zero-dep-audit`) against today's workspace, then diffed against `notes/zero_dep_audit_M30.md`. This is the M36 close metric and the plan's last technical act. Per workspace crate: `cargo tree --depth 1`, compare direct non-mirror dep count + per-dep rationale to the W117 snapshot, document every addition/removal with a delta column. (~120 min)
  - *Mental model*: the TigerStyle zero-dep discipline was the leading indicator of bar-(c) quality at M30; M36 measures whether the discipline **held under Phase-7's destination-landing pressure** — which is exactly the "just add a dep, I'm busy with the new job" period where bloat creeps in. Re-running the same script the M30 gate used (rather than eyeballing) makes the diff mechanical and trustworthy. A clean diff is the proof the discipline became a habit, not a gate you only respected when watched.
  - 🎯 Hardened acceptance:
    - **Comparison artifact** `notes/zero_dep_audit_M36.md`: side-by-side M30 vs M36 direct non-mirror dep counts per crate, with a delta column that flags any growth.
    - **No surprises**: every dep added between M30→M36 has a recorded rationale; any added by "expedience" without justification is flagged into `notes/year4_outline.md` as an M37 cleanup backlog item (closing the loop to Wednesday's outline).
    - **Close metric (committed number)**: workspace-wide direct non-mirror dep total ≤ **150** across the 30+ crates, AND ≤10 direct non-mirror deps per bar-(c) crate (the 10 Layer-1/HFT crates) — same threshold the W117 gate held.
    - **Mechanical**: the audit is regenerated by the W117 script in <30s, not hand-counted; cite the command run.
    - **Cited URL**: https://tigerstyle.dev/ (Zero Dependencies); diff explicitly against `notes/zero_dep_audit_M30.md`.
  - Output: `notes/zero_dep_audit_M36.md` exists with per-crate M30-vs-M36 delta table; workspace total ≤150 and every bar-(c) crate ≤10 (or each exception flagged into the M37 backlog).
- [ ] **Plan formal close**: append the close marker to `progress.md`. The plan formally ends today; the inheritance discipline continues forever. (~30 min) — Output: `progress.md` final entry states "Plan formally closed W144" + the close-metrics summary line (PRs / crates / hours / coverage score).
- [ ] Commit + log (~10 min) — Output: `git log -1 --oneline` shows the Saturday close commit.

## Sunday — Rest + Plan Close Ritual

- [ ] **Full retrospective read** of `progress.md` from W1 to today — three years of weekly logs. Read for patterns, not completeness (treat it as a retro source, not a re-study). Note in `notes/m36_retro.md` (section "Three-year patterns") the 3-5 recurring patterns + what surprised you. (~60 min)
  - 🎯 Hardened acceptance: `notes/m36_retro.md` gains a "Three-year patterns" section with ≥3 named patterns + ≥1 surprise, each grounded in a specific week range from the logs.
  - *Mental model*: the weekly logs are the only ground-truth record of how the three years actually went (vs how you remember them); reading them end-to-end once, at the close, surfaces patterns no single week could show — and grounds the Monday "what worked / differently" claims in evidence rather than recency bias.
  - Output: `notes/m36_retro.md` "Three-year patterns" section with ≥3 patterns + ≥1 surprise, each cited to a week range.
- [ ] The plan is done. The work continues. (~30 min) — Output: a final reflection line appended to `notes/m36_retro.md`; close the file.

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
