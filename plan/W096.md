# Week 96 — M24 Derivatives-Infra Readiness Checkpoint (the bet-path gate)

> **Phase**: 5
> **Month**: M24
> **Track**: Multi (M24 readiness checkpoint — light week, no new code; evaluates the venue half against the bet-path)
> **Hours target**: 40h/wk (W73–W136 window)
> **Mirror target**: n/a — this is a strategic readiness gate, not a code week. It evaluates the artifacts produced by the mirrored venue crates (`matching-engine` ← Chronicle/LMAX + TigerBeetle VSR, `oracle-mark` ← Hyperliquid/dYdX mechanics, `risk-engine` ← TradFi SPAN/portfolio-margin, `ledger-deterministic` ← TigerBeetle, `messaging-aeron` ← Aeron, `marketdata-kernelbypass` ← AF_XDP/io_uring, `consensus-vsr` ← Viewstamped Replication) against the v3 §5 M24 criteria.
> **Crate(s) touched**: None (readiness review across all venue + spine crates; no source edits). The *only* deliverables are written artifacts: a calibrated readiness verdict + a controllable-outbound dashboard, both in `progress.md` / `notes/`.
> **Bar**: (n/a) — strategic-gate week; no crate is built or modified. The gate *consumes* bar-(c)/bar-(b) crates and asks whether they clear the M24 readiness line.
> **Feeds into**: W97 `risk-engine` v1.0 multi-instrument cross-margin (the principal-defining capstone leg — this gate confirms the prerequisites are green before the capstone window opens); W117 M30 **Bet-Readiness Gate** (re-checks this week's coverage trajectory + outbound metrics against the §6 ecosystem shortlist); the §A re-slotting (M25–M30 capstone window assumes M24 passed).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): RECONCILED_PLAN_v3.md §5 **M24 row** (the controllable-outbound criterion that *replaces* the v2 "1 HFT recruiter inbound"); §1 (four readiness bars a/b/c/d); §3 coverage matrix (the 7-system score this gate reads); §6 ecosystem-selection checklist (the shortlist seed); §7 shots-on-goal + Binance-alumni dual-target map; §9 risk register (M24 outbound-response-rate-low row); INDEX.md W96 line ("[v3] M24 checkpoint … ≥20 targeted on-chain-derivatives conversations, ≥25% response, ≥3 Binance-alumni warm intros").
> **v3 modifications**: (1) **The whole week is re-aimed from the v2 "Five-Path decision (A/B/C/D/E)" to the v3 M24 Derivatives-Infra Readiness Checkpoint.** v2 picked between Reth-core / post-Reth / catch-up / Tempo / Tier-A-HFT-IC; v3 has a single north star (the founding-engineer token-equity bet on on-chain derivatives infra) so the question is no longer "which path" but "**is the venue half on the bet-path trajectory, and is the controllable outbound machine running?**" (2) **The uncontrollable "1 HFT recruiter inbound" criterion is DELETED and replaced by a controllable outbound metric** (§5 M24): ≥20 targeted on-chain-derivatives conversations, response rate ≥25%, ≥3 Binance-alumni warm intros logged. (3) **The crate readiness gate reads the venue triad** — `matching-engine` + `oracle-mark` + `risk-engine` at bar (c), **not** the v2 Path-E "4 HFT crates" list — and the 7-system coverage threshold is **≥55** (§5 M24). (4) Path C (catch-up) survives, re-scoped to the *venue* deliverables, because a slip still forces a sprint before the capstone window. Tempo/`vector-db` collapse from "paths" to "retained option legs" (§2) — assessed, not chosen.

*This is a strategic readiness gate. There is nothing to build — the deliverable is a defensible, evidence-backed verdict written to `progress.md`: **GO into the M25–M30 capstone window**, or **catch-up sprint first**. Every item below names the file it writes and a hardened acceptance criterion (a concrete, checkable pass condition) so a hand-wavy "I feel ready" cannot pass. The trap this week defends against is not a race condition — it is **motivated reasoning**: declaring readiness you wish were true instead of readiness the evidence supports, and (the v3-specific trap) **mistaking inbound luck for a controllable pipeline** — replacing the thing you can't control (a recruiter emails you) with the thing you can (you ran 20 targeted conversations).*

**The v3 reframe this week locks in (read before Monday):** the v2 file asked "which of five careers?" — that question is dead. v3 has exactly one terminal state (§1): bet-readiness on an on-chain-derivatives venue, with a crypto-MM cash seat as the income floor. So this gate has exactly three jobs: **(1) Crate-readiness** — is the venue triad (`matching-engine` v1.5 + `oracle-mark` v0.5 + `risk-engine` v0.5) at bar (c), and is 7-system coverage ≥55? **(2) Controllable-outbound** — is the bet-path *pipeline* running (≥20 targeted convos / ≥25% response / ≥3 Binance-alumni warm intros), the metric that replaces uncontrollable inbound? **(3) Catch-up trigger** — if any venue prerequisite slipped, sketch the sprint that closes it *before* W97 opens the capstone. Saturday writes the GO / catch-up verdict; Sunday closes the 24-month frame and points W97 at the capstone.

---

## Monday — Crate-readiness audit, part 1: the venue triad at bar (c)

- [ ] [HFT] Venue-triad readiness table — `notes/96_crate_readiness.md`: evaluate the **three principal-defining-path crates** the §5 M24 row names, each as a hard pass/fail with the measured value. The triad is `matching-engine`, `oracle-mark`, `risk-engine` (NOT the v2 Path-E "4 HFT crates" list). (~60 min)
  - 🎯 **Hardened acceptance**: a 3-row table `[crate | target version | actual tagged version | bar | bar-(c) gate green? Y/N | evidence link]`. The targets are: `matching-engine v1.5` (W82, book/STP/triggers — VSR-replicated, **no funding/liquidation**), `oracle-mark v0.5` (W80, mark/index/funding + **RWA-aware seams**), `risk-engine v0.5` (W90, multi-instrument cross-margin with the **const 30% netting-benefit haircut**). Each "green" claim cites the actual tag (`git tag | grep`) and the determinism/VOPR/zero-alloc evidence file — not "I think it's done." A row whose version is below target is a **Path-C catch-up candidate** and must be flagged in the row.
  - *Mental model*: v2 measured "4 HFT crates for an HFT job." v3 measures "the venue triad for the founding-engineer bet." The triad is the convergence of all three lenses (§3 row 9, `risk-engine`, is the only cell that is `jeff-dean`-hard ∧ `hft-review`-hard ∧ domain-hard). This is the prerequisite check for the capstone window — if the triad isn't green, W97 (`risk-engine` v1.0) is being built on sand.
  - Output: `notes/96_crate_readiness.md` exists with the 3-row triad table + the per-row catch-up flag.
- [ ] [HFT] Separation-of-concerns audit — append to `notes/96_crate_readiness.md`: confirm the v3 crate split actually held. `matching-engine` must be **book-only** (no funding/liquidation/ADL — those moved to `oracle-mark`/`liquidation-engine`). (~30 min)
  - 🎯 **Hardened acceptance**: the CI scope-grep from W74/W82 is re-run and pasted: `! grep -rniE 'funding|liquidat|insurance|\bmargin\b|adl' crates/matching-engine/src/` returns clean (only `MarkTickObserved` consuming an input remains). A non-clean result is a v3 architecture regression — the crates re-merged — and **fails the gate** because it breaks the capstone's "no primitive twice" claim that the W117 bet-readiness gate depends on.
  - *Mental model*: the whole v3 reframe rests on the separation that makes the capstone defensible — book in `matching-engine`, mark/funding in `oracle-mark` (where the RWA-aware seam lives), risk in `risk-engine`, liquidation in `liquidation-engine`. If funding crept back into the book crate "to save a refactor," the v1.5 RWA narrative ("here's the seam") is dead. This audit catches that before the capstone consumes the crates.
- [ ] Commit notes (~10 min)

## Tuesday — Crate-readiness audit, part 2: the spine + the substrate floor

- [ ] [HFT] Substrate + spine readiness table — `notes/96_substrate_readiness.md`: evaluate the supporting venue substrate and the Reth spine the capstone leans on. These are not the *principal* artifacts but a slip here still blocks the capstone. (~45 min)
  - 🎯 **Hardened acceptance**: a table `[crate | target version | actual tag | bar | green? Y/N]` covering at minimum: `consensus-vsr v1.0` (W90 — the one hot-path log), `ledger-deterministic v1.0` (W92 — static-mem + io_uring + VSR replication), `messaging-aeron v0.7` (W84 — off-path projection fan-out), `marketdata-kernelbypass v0.5` (W90), `runtime-thread-per-core v1.0` (W85 — the shard-per-core floor the 100k-account target needs), `mmap-queue v0.5` (W79), `log-distributed v0.5` (W84 — off-path Kafka-fidelity + first VSR-log projection), and the Reth spine (`exec-vm v1.5` block-stm W94, `consensus-engine v1.0` W91). Each green/red with the tag. Any red below the capstone's needs is a Path-C candidate.
  - *Mental model*: §A's M25–M30 capstone window assembles matching + oracle + risk + liquidation + ledger + **VSR** + thread-per-core store. The capstone "assembles," it does not "build from scratch" — so every substrate crate must already be at its tagged version. This table is the parts-on-the-shelf check before the assembly line starts. The Reth spine is retained in full (§2 / §10 — "no time constraint forces trimming one half"); it stays a first-class row here, as the income-floor + on-chain-settlement-design asset, not a deprecated track.
- [ ] [HFT] One-hot-path-log invariant spot-check — append to `notes/96_substrate_readiness.md`: confirm the venue still has exactly one hot-path log (the VSR log), and that Kafka/`log-distributed` and Aeron-Cluster are off-path mirrors only. (~30 min)
  - 🎯 **Hardened acceptance**: a 3-bullet confirmation citing the actual code seams: (a) `matching-engine` proposes onto `consensus_vsr::NormalReplica` (one log); (b) `ledger-deterministic` runs on the *same* `NormalReplica<LedgerSM>` (no second consensus); (c) `log-distributed`/`messaging-aeron` read a *projection* of the committed VSR log, never an independent write target. A note that can't cite the seams (or finds a second hot-path log) fails — a second log is the §9 "log-distributed would duplicate consensus" risk materializing, and it sinks cluster-VOPR linearizability (W108).
  - *Mental model*: this is the §4 "one log on the hot path" lock — the resolution of "Kafka vs VSR." It is cheap to verify now and catastrophic to discover at the W108 cluster-VOPR drill. The gate exists to surface it at M24, not M27.
- [ ] Commit notes (~10 min)

## Wednesday — 7-system coverage score + Path-C trigger audit

- [ ] [HFT] 7-system coverage tally — `notes/96_coverage.md`: compute the §3 coverage-matrix score at the M24 column and compare against the **≥55** threshold (§5 M24). (~45 min)
  - 🎯 **Hardened acceptance**: a per-system row table `[reference system | coverage 0–100 | input mirror | output crate | this-axis evidence]` for the 7 venue-relevant systems (TigerBeetle/VSR, LMAX/Disruptor, Aeron, ScyllaDB/Seastar, Kafka, Hyperliquid/dYdX mechanics, AF_XDP/io_uring), plus the aggregate score. The aggregate must be stated as a single number and compared to 55 ("coverage = 61 ≥ 55 — PASS"). Cite the actual per-system numbers; "around 55" fails. **No red cells** in the §3 sense is the stronger bar to note (a red cell = a competency with no green axis).
  - *Mental model*: §5 sets the M24 threshold at ≥55 (the M30 gate at W117 raises it to ≥78, M36 to ≥85). The score is the objective backstop against optimistic self-assessment — it is the one number that can't be talked up. Reading it requires actually scoring each system on the {jeff-dean, hft-review, domain} axes, not eyeballing the crate list.
  - Output: `notes/96_coverage.md` with the per-system table, the aggregate, and the PASS/FAIL-vs-55 line.
- [ ] [HFT] Path-C (catch-up) trigger audit — `notes/96_path_C_catchup.md`: evaluate every catch-up trigger as a hard pass/fail with the measured value. v3 triggers (OR semantics): `matching-engine v1.5` NOT shipped, OR `oracle-mark v0.5` NOT shipped, OR `risk-engine v0.5` NOT shipped (the multi-instrument-netting one — single-instrument cross-margin does **not** count, §4A acceptance #1), OR `consensus-vsr v1.0` NOT shipped, OR `ledger-deterministic v1.0` NOT shipped, OR <100 runtime hours logged this phase, OR **7-system coverage <55**. (~30 min)
  - 🎯 **Hardened acceptance**: a 7-row table `[trigger | measured value | fired? Y/N]`. Path C is **TRIGGERED if ANY row fires** (OR, not AND). The note states the boolean OR result explicitly: "Path C triggered: Y/N." The coverage row must cite Wednesday's actual aggregate, not re-derive it loosely.
  - *Mental model*: Path C is not a path you *choose* — it is a sprint that gets *forced* when the plan slipped. In v3 it is re-scoped from the v2 generic deliverables to the **venue triad + substrate floor**, because a slip there directly delays the M25 capstone window (§A). The risk-engine trigger is deliberately the *multi-instrument* one: §4A acceptance #1 says single-instrument cross-margin "is hollow and does not pass," so a `risk-engine v0.5` that only does isolated margin fires the trigger.
- [ ] [HFT] If any trigger fired — append a dated 2–3-week catch-up sprint to `notes/96_path_C_catchup.md`: name the single most-behind venue deliverable and the minimal sprint to close it **before W97 opens the capstone window**. (~30 min)
  - 🎯 **Hardened acceptance**: IF Path C triggered, the note contains a dated sprint naming the most-behind crate + the §4A acceptance criterion it unblocks + the revised W97 start date. IF no trigger fired, one line: "no trigger fired — capstone window opens W97 on schedule" (still logged). Skipping this when a trigger DID fire is the failure mode — it would push an unready triad into the capstone.
  - *Mental model*: a triggered Path C reshapes W97–W98 before the capstone can honestly start. Sketching it now (vs. discovering the slip mid-capstone) means Saturday's GO/no-go is made with the real, post-catch-up picture in view.
- [ ] Commit notes (~10 min)

## Thursday — Controllable-outbound: build the bet-path pipeline (the v3 criterion that replaces inbound)

- [ ] [BET] Outbound-metric definition + baseline — `notes/96_outbound.md`: state the §5 M24 controllable-outbound criterion precisely, then measure today's baseline against it. The criterion (replacing the deleted "1 HFT recruiter inbound"): **≥20 targeted conversations with on-chain-derivatives teams, response rate ≥25%, ≥3 Binance-alumni warm intros logged.** (~45 min)
  - 🎯 **Hardened acceptance**: a 3-row dashboard `[metric | target | current | gap]` — (1) targeted on-chain-derivatives conversations: target 20; (2) response rate: target ≥25%; (3) Binance-alumni warm intros logged: target ≥3. Each "current" cell cites the actual count from a tracked list (a CRM tab / a `notes/outreach_log.md`), not an estimate. The note explicitly relabels any inbound that *did* arrive as "crypto-infra/derivatives OR HFT" (§5 M24) rather than treating it as the metric.
  - *Mental model*: this is the single biggest v2→v3 change to this week. v2's "1 HFT recruiter inbound" was uncontrollable — you can't make a recruiter email you, so the criterion rewarded luck and punished a strong candidate in a quiet market. v3 replaces it with a metric you fully control: *you* run 20 targeted conversations. The §9 "M24 outbound response rate low" row notes this is controllable — if the rate is low, the lever is ship the flagship blog earlier and widen Binance-alumni outreach, not "wait for inbound."
  - Output: `notes/96_outbound.md` with the 3-row dashboard + the inbound-relabel line.
- [ ] [BET] Binance-alumni dual-target map — append to `notes/96_outbound.md`: the §7 map points at **two** destinations, not one. List warm-intro candidates split by destination: (a) on-chain-derivatives **founders/early-joins** (the bets); (b) **crypto-MM desks** (the income floor — Wintermute/Jump-style, remote). (~30 min)
  - 🎯 **Hardened acceptance**: a 2-column list, ≥3 named contacts per column with `[name/handle | their venue/desk | relationship depth | warm-intro path | last-touch date]`. The bet column must include ≥3 that, if advanced, count toward the "≥3 warm intros" metric. A single-destination map (only founders, or only MM desks) fails — §7 explicitly requires both, because the MM seat is the floor that funds the runway between bets.
  - *Mental model*: §7's shots-on-goal structure is `Bridge₀ → Bet #1 → Bridge₁ → Bet #2`. The Binance-alumni map is the sourcing engine for *both* the bridge (crypto-MM cash) and the bet (founding-eng on a venue). Mapping only founders strands the income floor; mapping only desks strands the bet. The dual map keeps both shots live.
- [ ] [BET] First-pass §6 ecosystem-filter seed — append to `notes/96_outbound.md`: take any on-chain-derivatives venues already surfaced in the outbound list and run a *quick* §6 checklist pass (founding-tier allocation / tier-1 backing / core-criticality / multi-B FDV ceiling / token reality / remote-native / cycle timing). This is the W115–W116 shortlist's seed, not the full build. (~30 min)
  - 🎯 **Hardened acceptance**: ≥2 venues each scored 7-row pass/unknown/fail against §6, with a one-line note per row. The note states which of the 7 are *unknowable now* (e.g. token vesting pre-TGE) vs *checkable now* (remote-native, tier-1 backing). A venue that fails on remote-native (colo/on-site required) is flagged "bridge, not bet" per §6. Output anchors the W117 M30 "shortlist ≥5" requirement.
  - *Mental model*: §6 is the reusable gate applied before *any* bet — "a bet is GO only if all seven pass; otherwise it's a bridge job, not a bet." Seeding it at M24 means the M30 shortlist (≥5 venues passing the checklist) isn't built from cold at W115. The Airwallex anti-pattern (late-stage, tiny employee equity → never a bet) is the calibration anchor for the founding-tier-allocation row.
- [ ] Commit notes (~10 min)

## Friday — Bridge/floor + geography pre-stage + option-leg verdicts

- [ ] [BET] Income-floor readiness — `notes/96_bridge.md`: assess the **crypto-MM cash seat** as the primary bridge (§1 bar (d), §2, §7). It is underwritten by Half-2 latency craft (`hft-review`: matching, tail-latency, mechanical sympathy — exactly why `latency-lab` was pulled to M6). (~45 min)
  - 🎯 **Hardened acceptance**: a readiness checklist — (a) which `latency-lab` artifacts (rdtsc tick-to-trade, coordinated-omission report, p99.99 distributions on the matching/ring path) are public and citable as the seat's underwriting; (b) which crypto-MM desks are remote-friendly from Vietnam (≥2 named, with a specific posting or maintainer/recruiter note, not "probably remote"); (c) a one-line honest read on whether the latency portfolio is *seat-ready now* or needs the W112 cross-margin/liquidation blog first. The remote-from-Vietnam check is load-bearing (§7 geography) — a seat that requires relocation fails the default constraint even if technically attractive.
  - *Mental model*: §7 — "never quit a bridge before vest milestones of the live bet"; the bridge funds runway *between* bets. The crypto-MM seat is the *primary* floor (secondary fallbacks: crypto-infra IC on the Reth spine, Multiplier coast — no skill stranded). This note confirms the floor is real before the capstone window, so the M30 gate can decide "place Bet #1 or engage bridge" with the bridge already underwritten.
- [ ] [BET] Geography pre-stage check (cheap prep only, NO commitment) — append to `notes/96_bridge.md`: per §7, confirm **visa eligibility + pre-TGE token tax treatment** for *both* hubs (Dubai = crypto-native/Binance-dense; Singapore = institutional/Asia). No move, no lease. (~30 min)
  - 🎯 **Hardened acceptance**: a 2-row table `[hub | visa eligibility finding | pre-TGE token-tax finding | source]` for Dubai and Singapore, each cell a concrete finding with a cited source (a government page / a tax-advisor note), not "looks fine." The note explicitly states the §7 **flip-trigger** that would convert prep→move (≥N warm conversations cluster in one hub OR a presence-contingent offer premium) and confirms it has **not** fired (so Vietnam-remote remains the default). Pre-committing a relocation here fails — the decision point is Bet #1 time, not M24.
  - *Mental model*: §7 geography — "pre-stage optionality, do NOT pre-commit." The hub follows the bet, decided at Bet #1 time. The cheap prep now (visa + token tax for both) + event-trip recon (Token2049-style, W123) compresses network density without relocating. The flip-trigger is the explicit gate that keeps a premature move from happening on a vibe.
- [ ] [Option legs] Tempo + vector-db verdicts (assessed, not chosen) — append to `notes/96_bridge.md`: in v2 these were *paths*; in v3 they are *retained option legs* (§2 leg A AI-infra / leg B RWA-collateral rails). Book their standing honestly, then confirm they stay in BACKGROUND. (~30 min)
  - 🎯 **Hardened acceptance**: two one-line verdicts. (a) **Tempo** (`tempo-*` → RWA-perps collateral rails, the fintech edge / three-way fit): current PRs / maintainer relationships / engagement, and a verdict "retained option leg — RWA-collateral rails feed the capstone's v1.5 RWA features (§4A), kept warm in background, NOT on the critical path." (b) **`vector-db`** (→ AI-infra / real-time risk analytics / anomaly-on-flow, v0.5): verdict "retained optionality, slips to M31 buffer (§A), not built in the capstone window." Each verdict must name the cost side (weeks spent) and the benefit side (optionality realized / CV bullet), per a no-flinch retrospective. A verdict with only upside fails.
  - *Mental model*: v3 collapses the v2 "five paths" into one profile with two halves + two retained option legs (§2). Tempo and `vector-db` are no longer destinations you choose between — they are cheap fallbacks that keep "no skill stranded on a single ecosystem's death" (§7 lowest-risk objective). Booking their cost honestly here is how future bets stay well-calibrated.
- [ ] Commit notes (~10 min)

## Saturday — The verdict + Phase 5 close

- [ ] [HFT][BET] The readiness verdict — write to `progress.md`: a single **GO / catch-up-first** decision for the M25–M30 capstone window. Cite the controlling evidence from Mon–Fri (the triad table, the coverage-vs-55 number, the Path-C OR-result, the outbound dashboard). (~30 min)
  - 🎯 **Hardened acceptance**: `progress.md` contains a single line "M24 CHECKPOINT: GO — capstone window opens W97" OR "M24 CHECKPOINT: CATCH-UP FIRST — W97 slips to <date>", followed by ≥4 cited evidence bullets that each reference a specific number from this week's notes (e.g. "venue triad: matching v1.5 ✓ / oracle-mark v0.5 ✓ / risk-engine v0.5 ✓ — 3/3 bar (c); coverage = 61 ≥ 55; Path-C: not triggered; outbound: 17/20 convos, 29% response, 4 warm intros"). A verdict without cited per-criterion evidence fails. **If Path C triggered Wednesday, the verdict MUST be CATCH-UP-FIRST** — GO with a fired trigger is the headline failure mode (it builds the capstone on an unready triad).
  - *Mental model*: this is the artifact W97 consumes — it tells the capstone window to open on schedule or wait for a sprint. Unlike the v2 "pick one of five careers," there is no path choice to make (the north star is fixed); the only decision is *timing*: is the venue half ready to assemble into `perp-dex-core`, or does a prerequisite need closing first? The "GO requires no-fired-trigger" rule defends against the most common failure: declaring readiness to stay on schedule.
  - Output: `progress.md` has the "M24 CHECKPOINT: …" line + ≥4 evidence bullets citing this week's numbers.
- [ ] [HFT] Full Phase 5 review — `notes/96_phase5_review.md`: tally Phase 5 (M19–M24) deliverables against the v3 plan. Flag every slip with a one-line cause. (~60 min)
  - 🎯 **Hardened acceptance**: a table of every Phase-5 crate target `[crate | planned version | actual version | hours | slip?]` covering `consensus-vsr` v0.5→v1.0, `matching-engine` v1.0→v1.5 (book-only), `oracle-mark` v0.5, `risk-engine` v0.5, `log-distributed` v0.5, `messaging-aeron` v0.7, `ledger-deterministic` v1.0, `marketdata-kernelbypass` v0.5, `runtime-thread-per-core` v1.0, `mmap-queue` v0.5, `exec-vm` v1.5 (spine), `consensus-engine` v1.0 (spine), and runtime hours. Each slip carries a one-line cause. A review that lists only the wins fails — the slips are the input to whether Path C was correctly (not) triggered Wednesday.
  - *Mental model*: the Phase-5 review is the evidence base the Saturday verdict rests on. It exists to make the verdict auditable: anyone reading `notes/96_phase5_review.md` should reach the same GO / catch-up conclusion. Note explicitly that v3 added `oracle-mark` / `risk-engine` / `log-distributed` to Phase 5 (the venue domain crates) — their presence vs absence is the clearest signal of whether the v3 reframe actually landed in the build.
- [ ] [HFT] North Star + bet-path metrics tally — append to `notes/96_phase5_review.md` and update the North Star block in `progress.md`: runtime hours toward the 2000h M30 milestone, the 7-system coverage score (vs 55 now / 78 at M30), and the outbound dashboard (vs the §5 M24 targets). (~30 min)
  - 🎯 **Hardened acceptance**: three numbers appear with their thresholds inline (e.g. "runtime hours: 1,180 — on track for 2000 by M30"; "coverage: 61 / 55 M24 threshold — PASS, trajectory to 78 by M30"; "outbound: 17/20 convos, 29% response, 4 warm intros — convos BELOW, response & intros ABOVE"). The North Star block in `progress.md` is updated with the bet-path orientation (capstone window + outbound pipeline), not the v2 job-search orientation.
  - *Mental model*: these are the same numbers the W117 M30 Bet-Readiness Gate re-checks. Tallying them at M24 fixes the baseline so the M30 gate can measure delta. Recording the trajectory (coverage 55→78, hours →2000) reframes the gate as a waypoint, not an endpoint.
- [ ] [BET] Bet-path standing — append to `progress.md`: where the bet-path stands. The venue triad + substrate = the portfolio that clears bar (b); the Binance-alumni dual map + §6 seed = the network engine; the crypto-MM latency portfolio = the floor. (~30 min)
  - 🎯 **Hardened acceptance**: names the bar-(b) portfolio artifacts with their tags (the venue triad + `consensus-vsr` + `ledger-deterministic`), the single strongest artifact for a founding-eng conversation (the lead-with — almost certainly the `risk-engine`/cross-margin path per §3 convergence cell + §4A principal-defining), and one line on which bar (a/b/c/d) is furthest from green. A generic "the bet-path is strong" without the tags and the lead-artifact pick fails.
  - *Mental model*: under v3, these crates ARE the bet portfolio (not a job-search portfolio). Identifying the single strongest artifact now (the cross-margin/liquidation engine — the convergence cell) seeds the W112/W121 flagship-blog narrative and the founding-eng conversation lead. The four-bar read (§1 a/b/c/d) names which bar is the binding constraint to close by M30.
- [ ] Commit + log (~10 min)

## Sunday — End the 24-month frame + point W97 at the capstone

- [ ] [HFT] Milestone tally + rest — append a one-line dated tally to `progress.md`: the v3 venue + spine + substrate crate count shipped on top of the Layer-1/2/3/4 primitives at the 24-month mark. (~30 min)
  - 🎯 **Hardened acceptance**: the crate count is written as a single dated milestone line in `progress.md` (e.g. "M24: venue triad [matching/oracle-mark/risk-engine] + consensus-vsr + ledger-deterministic + messaging-aeron + marketdata-kernelbypass + log-distributed + Reth spine [exec-vm/consensus-engine] shipped on the Layer-1/2/3/4 primitive substrate"). The count is the fixed baseline the M30 (W117) and M36 (W144) gates measure delta against. (The rest itself is unmeasurable — the deliverable is the recorded milestone.)
  - *Mental model*: this is the 24-month checkpoint marker. Recording it makes the M30/M36 gates able to measure compounding output against a fixed baseline. Naming the venue crates explicitly (vs the v2 "11 product crates") records that the v3 reframe is now in the artifacts, not just the strategy doc.
- [ ] [HFT][BET] Capstone-window orientation declaration — append to `progress.md`: state the going-forward orientation that W97 inherits. (~30 min)
  - 🎯 **Hardened acceptance**: `progress.md` carries an explicit "going-forward (W97+): capstone window — `risk-engine` v1.0 multi-instrument cross-margin (principal-defining) → `liquidation-engine` v1.0 → `perp-dex-core` 3-node VSR assembly + cluster-VOPR; background: bet-path outbound pipeline + crypto-MM floor + Tempo/vector-db option legs" line that matches Saturday's verdict. **If Saturday's verdict was CATCH-UP-FIRST**, this line must instead state the catch-up sprint that runs *before* the capstone window opens, and the revised W97 focus.
  - *Mental model*: the rest of the plan (Phase 6 capstone, Phase 7 bet-path) ASSUMES the M24 GO. This declaration is the hinge between the written plan and the next 21 weeks — it tells W97 to open the capstone window on `risk-engine` v1.0 (the convergence-cell, principal-defining leg per §3/§4A), or to run a catch-up sprint first. Without it, a CATCH-UP verdict would leave W97 pointed at a capstone it isn't ready to build. The §A M25–M30 re-slotting (`risk-engine` v1.0 → `liquidation-engine` v1.0 → `perp-dex-core` assembly → 2000h + flagship blog + ecosystem shortlist) is the sequence this line commits W97 to.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
