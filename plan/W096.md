# Week 96 — Month 24 Decision (Five Paths)

> **Phase**: 5
> **Month**: M24
> **Track**: Multi (M24 decision gate — light week, no new code)
> **Hours target**: 40h/wk (M19-M34)
> **Mirror target**: n/a — this is a career decision gate, not a code week. It evaluates the artifacts produced by the mirrored crates (matching-engine ← Chronicle/LMAX, ledger-deterministic ← TigerBeetle, messaging-aeron ← Aeron, marketdata-kernelbypass ← Onload/ef_vi, consensus-vsr ← VSR/Viewstamped Replication) against the M24 five-path criteria.
> **Crate(s) touched**: None (decision review across all 11 product crates + 8 Layer-1/2/3/4 primitives; no source edits)
> **Bar**: (n/a) — career/decision-gate week; no crate is built or modified.
> **Feeds into**: W97+ daily focus redirect (the chosen path rewrites the orientation of Phase 6 + Phase 7); W117 (M30 Path-E confirmation gate re-checks this week's signals); the v2 default carries Path E through to v2 Phase 6+7.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): README §"M24 (W96 Fri) — Five-path decision (v2 criteria)" (~L899); README §"M24 worked example (W96 Fri) — Path E" (~L1410); README §"7-system coverage table" (M24 column, ~L377/L392); README §"Decisions Locked (v2)" items 6/12/14/15/17 for the conditional-Path-D and HFT-optionality framing.
> **v2 modifications**: M24 criteria now reference the v2 7-system coverage score (Path C triggers if <50; Path E requires ≥55); Path E adds runtime-thread-per-core v0.5, consensus-vsr v0.5, mmap-queue v0.1 to its strong-if list; Path E default carries through to v2 Phase 6+7.

*This is a decision-gate week. There is nothing to build — the deliverable is a defensible, evidence-backed path choice written to `progress.md`. Every item below names the file it writes and a hardened acceptance criterion (a concrete, checkable pass condition) so a hand-wavy "I thought about it" cannot pass. The trap this week defends against is not a race condition — it is **motivated reasoning**: picking the path you wish were true instead of the path the evidence supports.*

**Monday — Path A analysis (extend Reth core)**

- [ ] Path A write-up — `notes/96_path_A_reth_core.md`: what "extend Reth core 6-12 months" concretely looks like (which subsystems, which maintainers, expected PR cadence) and your honest likelihood of accepting a Reth-adjacent Tier A offer. (~60 min)
  - 🎯 Hardened acceptance: the note lists ≥3 named Reth subsystems you'd target, names the specific maintainers by GitHub handle for each, and ends with a single P(accept Reth-adjacent Tier A offer) probability in [0,1] with one sentence of justification. A note without a numeric probability fails.
  - *Mental model*: Path A is the "stay in the ecosystem you already have relationships in" option. It only beats Path E if Reth-adjacent hiring is warmer than HFT hiring for YOU specifically — so this note exists to force a concrete inbound-vs-inbound comparison, not a vibe.
  - Output: `notes/96_path_A_reth_core.md` exists with the 3-subsystem list, maintainer handles, and the P(accept) figure.
- [ ] Path A strong-if check — append to `notes/96_path_A_reth_core.md`: evaluate the gate "≥3 Reth maintainer Depth-3+ relationships, ≥1 feature merged, public visibility growing, no HFT inbound." (~30 min)
  - 🎯 Hardened acceptance: each of the four sub-conditions is marked ✓/✗ with the supporting number (e.g. "Depth-3+ relationships: 1 ✗ — only @gakonst at Depth-3"). A blanket "looks strong" without per-condition evidence fails. Note explicitly: the "no HFT inbound" clause INVERTS — if you have HFT inbound, Path A weakens even if the other three are green.
  - *Mental model*: README §M24 says Path A is strong "if ≥3 Reth maintainer Depth-3+, ≥1 feature merged, no HFT inbound." The strong-if is a falsifiable checklist, not a mood. Each unmet condition is a documented reason Path A is NOT your path.
- [ ] Commit notes (~10 min)

**Tuesday — Path B analysis (post-Reth systems)**

- [ ] Path B write-up — `notes/96_path_B_post_reth.md`: scope "pivot to post-Reth systems" (Chronicle Queue replacement, deeper Aeron, distributed mini-db) — which 1-2 systems, why, and what they buy you that Phase-5 crates don't already cover. (~30 min)
  - 🎯 Hardened acceptance: names ≤2 concrete target systems with a one-line "what new capability this adds beyond W74-W96 crates" for each. If the answer is "more of the same primitives I already shipped," the note must say so and conclude Path B is redundant — that honest conclusion passes; a vague "would be interesting" fails.
  - *Mental model*: Path B is "keep building distributed-systems primitives for another 12 months before ANY job change." Its real cost is opportunity cost: it delays the backup-job milestone. This note exists to make that delay explicit so it competes fairly against Path E's faster landing.
- [ ] Path B strong-if check — append to `notes/96_path_B_post_reth.md`: "strong if you want another 12 months of primitive-building before any job change." Answer honestly against the career goal (remote crypto-infra role, $120-150k, backup milestone ~W24-30 already past — you are at W96). (~30 min)
  - 🎯 Hardened acceptance: a single explicit yes/no on "do I want to defer the job search another 12 months?" with the trade named (12 more months of learning vs. 12 months of deferred income/landing). Given the backup milestone is already behind schedule, a "yes" must justify why deferral is acceptable now.
  - *Mental model*: the strong-if is a preference question, not a capability question — you CAN build more primitives; the question is whether you WANT to before landing a role. Naming the income/landing deferral keeps this from being a free "of course I love building" answer.
- [ ] Commit notes (~10 min)

**Wednesday — Path C analysis (catch-up)**

- [ ] Path C trigger audit — `notes/96_path_C_catchup.md`: evaluate every Path-C trigger as a hard pass/fail with the measured value. Triggers (v2): consensus-engine v1.0 NOT shipped, OR matching-engine v1.5 NOT shipped, OR Sepolia sync NOT green, OR <100 runtime hours, OR **7-system coverage score <50**. (~30 min)
  - 🎯 Hardened acceptance: a 5-row table, one row per trigger, each with column [trigger | measured value | fired? Y/N]. Path C is TRIGGERED if ANY row fires (OR semantics, not AND). The note states the boolean OR result explicitly. Reading "coverage score" requires pulling the M24-column number from README §coverage table — cite the actual number, not "around 55."
  - *Mental model*: Path C is not a path you CHOOSE — it is a path that gets FORCED on you when the plan slipped. This audit exists so you can't paper over a slip: a single fired trigger means you are behind and must catch up before Path E is honestly available. The 7-system coverage <50 trigger (v2) is the new objective backstop against optimistic self-assessment.
  - Output: `notes/96_path_C_catchup.md` with the 5-row trigger table and the OR-result line "Path C triggered: Y/N."
- [ ] If any trigger fired — append a 2-week catch-up sketch to `notes/96_path_C_catchup.md`: which specific deliverable is behind and the minimal sprint to close it before re-running the Path-E gate. (~30 min)
  - 🎯 Hardened acceptance: IF Path C triggered, the note contains a dated catch-up sprint naming the single most-behind deliverable and the gate it unblocks. IF no trigger fired, the note says "no trigger fired — Path C not in play" in one line and this sub-task is a no-op (still log it). Skipping this when a trigger DID fire is the failure mode.
  - *Mental model*: a triggered Path C reshapes W97-W98 before any other path can be honestly chosen — catching up is prerequisite, not optional. Sketching it now (vs. later) means Saturday's decision is made with the real, post-catch-up picture in view.
- [ ] Commit notes (~10 min)

**Thursday — Path D analysis (Tempo pivot, conditional)**

- [ ] [Tempo] Path D three-condition gate — `notes/96_path_D_tempo.md`: re-run W95 Friday's three-condition test as a hard AND. Path D is a REAL option ONLY if all three: ≥15 Tempo PRs merged AND ≥2 direct Tempo maintainer relationships (Depth ≥3) AND upstream substantively engaged with `tempo-payment-lane`. (~30 min)
  - 🎯 Hardened acceptance: a 3-row table [condition | measured value | met? Y/N] and the AND-result line "Path D real: Y/N." All three must be Y for Path D to be real — partial credit does not exist (AND semantics). "Substantive engagement" must be evidenced (a merged change / a design thread reply with follow-through), not "1 polite comment." The worked example (README ~L1419) shows this gate FAILING at 11 PRs / 1 relationship / no engagement — use it as the calibration anchor.
  - *Mental model*: Path D is the only conditional path with a binary unlock. The AND gate exists precisely to stop wishful pivoting into Tempo — the README worked example demonstrates the gate is designed to fail at realistic-but-insufficient numbers. If it fails, Path D does NOT vanish; it demotes.
  - Output: `notes/96_path_D_tempo.md` with the 3-row AND table and the "Path D real: Y/N" line.
- [ ] [Tempo] Path D demotion / shape — append to `notes/96_path_D_tempo.md`: IF real, sketch the next 6-12 months (direct application vs. build-in-public to attract inbound) AND a remote-friendliness check (Tempo is distributed; confirm remote roles open from Vietnam). IF not real, document that Path D collapses into Tier A/B as generic "stablecoin payments infra" track. (~30 min)
  - 🎯 Hardened acceptance: branches correctly on the gate result. IF real: names application-vs-build-in-public choice + a concrete remote-from-Vietnam finding (a specific job posting or maintainer note, not "probably remote-friendly"). IF not real: one line stating Path D → generic stablecoin-infra track inside Tier A/B, and that this is now a CV bullet, not a destination.
  - *Mental model*: the remote-from-Vietnam check is load-bearing for the career goal (remote crypto-infra from Vietnam) — a Tempo role that requires relocation fails the actual constraint even if technically attractive. This is where Path D most often quietly dies.
- [ ] Commit notes (~10 min)

**Friday — Path E analysis ([NEW] HFT destination-tier IC track) — NEW DEFAULT IF SIGNALS STRONG**

- [ ] [HFT] Path E definition — `notes/96_path_E_hft.md`: state the target precisely. Tier A HFT firm (A/B/C/D/E/F) as IC4/IC5 systems engineer; compensation destination-tier (top-of-band); geography destination-dependent (major financial hub, firm-specific). Note the tension with the career goal's remote-from-Vietnam constraint and how Phase 7 resolves it (remote-first HFT firms vs. relocation). (~30 min)
  - 🎯 Hardened acceptance: the note names the IC level targeted, the comp band as a number range, and a single sentence on the geography/remote tension (Path E's destination-geography default vs. the stated remote-from-Vietnam goal). Omitting the geography tension is the trap — Path E is the strongest TECHNICAL fit but has the hardest GEOGRAPHY fit, and the note must hold both.
  - *Mental model*: Path E is the v2 default destination because the 4 HFT crates are stronger artifacts for HFT firms than for Reth-adjacent firms. But "default" is not "automatic" — the geography constraint is the one place Path E can lose to Path A.
  - Output: `notes/96_path_E_hft.md` with IC level, comp range, and the geography-tension sentence.
- [ ] [HFT] Path E strong-if check (v2) — append to `notes/96_path_E_hft.md`: evaluate the v2 strong-if as a checklist with measured values: matching-engine v1.5 ✓, ledger v0.5 ✓, messaging-aeron v0.5 ✓, marketdata-kernelbypass v0.5 ✓, **runtime-thread-per-core v0.5 ✓ (v2)**, **consensus-vsr v0.5 ✓ (v2)**, **mmap-queue v0.1 ✓ (v2)**, ≥200 runtime hours, ≥1 blog post live, ≥1 inbound from HFT recruiter, **7-system coverage ≥55 (v2)**. (~30 min)
  - 🎯 Hardened acceptance: an 11-row checklist, each row [criterion | actual | ✓/✗]. Path E is "strong" only if the great majority are ✓ — explicitly count "N of 11 met" and state the threshold judgment. Crate versions must cite the actual tagged version (e.g. "matching-engine v1.5 — tag exists ✓"), runtime hours the actual logged number, coverage the actual M24-column score. A self-reported "all green" without per-row numbers fails.
  - *Mental model*: this is the gate that flips the default ON. README §M24 lists exactly these criteria; the v2 additions (runtime-thread-per-core, consensus-vsr, mmap-queue, coverage ≥55) raise the bar over v1. Counting "N of 11" forces an honest signal-strength read instead of a binary green/red wish.
- [ ] [HFT] Path E Phase 6+7 plan — append to `notes/96_path_E_hft.md`: Phase 6 (M25-M30): continue HFT track to v0.7 + add mini-db CAPSTONE + vector-db, reach 2000+ runtime hours by M30. Phase 7 (M31-M36): interview prep, applications, destination landing (and remote-first targeting). (~30 min)
  - 🎯 Hardened acceptance: a dated milestone line for each phase — Phase 6 names the crate-version targets and the runtime-hour target (2000 by M30, which is the M30 confirmation gate's input); Phase 7 names the application-window start and the landing target. The plan must connect to the W117 M30 gate (this is what that gate re-checks).
  - *Mental model*: choosing Path E is choosing this 12-month plan — so the plan must exist before the choice, not after. The 2000-runtime-hour target is the single hardest Phase-6 commitment and the M30 gate's pass/fail hinge; naming it now is committing to it.
- [ ] Commit notes (~10 min)

**Saturday — Decision + Phase 5 close + Tempo addendum close**

- [ ] The decision — write to `progress.md`: pick ONE path (A/B/C/D/E). Not "do both." Cite the controlling evidence from Mon-Fri notes (the strong-if counts, the Path-C OR-result, the Path-D AND-result). (~30 min)
  - 🎯 Hardened acceptance: `progress.md` contains a single line "M24 DECISION: Path X" followed by ≥3 cited evidence bullets that each reference a specific number from this week's notes (e.g. "Path E strong-if: 10 of 11 met; Path D AND-gate: failed at 11 PRs"). A decision without cited per-path evidence, or that names two paths, fails. If Path C triggered Wednesday, the decision must address it before any of A/B/D/E can be chosen.
  - *Mental model*: this is the artifact W97+ consumes — the chosen path redirects daily focus for the next 6-12 months. The "pick one" rule defends against the most common failure (hedging into "mostly E but keep D alive"), which dilutes both. Optionality lives in BACKGROUND maintenance, not in a split primary focus.
  - Output: `progress.md` has the "M24 DECISION: Path X" line + ≥3 evidence bullets citing this week's numbers.
- [ ] Full Phase 5 review — `notes/96_phase5_review.md`: tally Phase 5 (M19-M24) deliverables against plan — consensus-vsr, matching-engine v1.5, mmap-queue, ledger, messaging-aeron, marketdata, runtime hours. Flag any slips. (~60 min)
  - 🎯 Hardened acceptance: a table of every Phase-5 crate target [crate | planned version | actual version | hours | slip?]. Each slip carries a one-line cause. A review that lists only the wins fails — the slips are the input to whether Path C was correctly (not) triggered.
  - *Mental model*: the Phase-5 review is the evidence base the Saturday decision rests on. It exists to make the decision auditable: anyone reading `notes/96_phase5_review.md` should reach the same path conclusion.
- [ ] North Star + Tempo metrics tally — append to `notes/96_phase5_review.md` and update the North Star block in `progress.md`: final Tempo metrics (PRs merged, maintainer relationships, upstream engagement) and where they land vs. the 15-PR / 2-relationship thresholds. (~30 min)
  - 🎯 Hardened acceptance: the three Tempo numbers appear with their thresholds inline (e.g. "Tempo PRs: 11 / 15 threshold — BELOW"). The North Star block in `progress.md` is updated with the chosen-path orientation.
  - *Mental model*: these are the same numbers Thursday's Path-D gate used — tallying them in the close ensures the decision and the metrics agree (a Path-D-real claim with sub-threshold Tempo numbers is an internal contradiction to catch here).
- [ ] [Tempo] Tempo optionality verdict — append to `progress.md`: honest assessment — did the Tempo extension pay off as optionality, or did it cost Reth velocity without proportional return? (~30 min)
  - 🎯 Hardened acceptance: a one-line verdict ("Tempo: optionality realized" / "Tempo: net cost to Reth velocity") backed by the cost side (estimated weeks of Reth velocity spent on Tempo) AND the benefit side (CV bullet + network warming, or a real PR/relationship). A verdict with only the upside named fails — the question is explicitly cost-vs-return.
  - *Mental model*: this is a no-flinch retrospective. The plan permitted Tempo as optionality on the bet it might unlock Path D; if Path D's gate failed, this is where you book the cost honestly so future bets are better calibrated.
- [ ] [HFT] HFT track standing — append to `progress.md`: where the HFT track stands. matching-engine + ledger + messaging-aeron + marketdata = 4 production-tier crates that did not exist 24 months ago. (~30 min)
  - 🎯 Hardened acceptance: names the 4 crates with their current tagged versions and one line on which is the strongest interview artifact (the lead with for Phase-7 applications). A generic "the HFT track is strong" without the version tags and the lead-artifact pick fails.
  - *Mental model*: under the Path-E default, these 4 crates ARE the job-search portfolio. Identifying the single strongest artifact now seeds the Phase-7 application narrative.
- [ ] Commit + log (~10 min)

**Sunday — End 24-month frame**

- [ ] Milestone tally + rest — append a one-line tally to `progress.md`: 3 Reth + 5 HFT + 3 Tempo = 11 product crates shipped on top of 8 Layer-1/2/3/4 primitives. Then full rest / celebrate. (~30 min)
  - 🎯 Hardened acceptance: the 11-product-crate + 8-primitive count is written as a single dated milestone line in `progress.md`. (The rest itself is unmeasurable — the deliverable is the recorded milestone, not a "felt rested" claim.)
  - *Mental model*: this is the 24-month checkpoint marker — recording it makes the M30 and M36 gates able to measure delta against a fixed baseline. Naming the count also reframes a tired week as 24 months of compounding output.
- [ ] Default-path declaration — append to `progress.md`: state the going-forward default. Path E (HFT destination-tier IC track) if signals strong, with Reth maintenance + Tempo optionality continuing in BACKGROUND. If you picked A/B/C/D instead, write the one-line note that W97+ daily focus redirects accordingly and the plan structure is reused with refocused daily work. (~30 min)
  - 🎯 Hardened acceptance: `progress.md` carries an explicit "going-forward default: Path X (background: Y, Z)" line that matches Saturday's decision. If the Saturday decision was not Path E, this line must explicitly state the redirect (which crate/track becomes the W97+ daily primary). The remainder of the plan (Phase 6 + Phase 7) is written under Path E, so a non-E choice requires this redirect note to be actionable.
  - *Mental model*: the rest of this plan ASSUMES Path E. This declaration is the hinge between the written plan and your actual chosen focus — it tells W97+ whether to follow the plan as-written or to re-aim it. Without it, a non-E choice leaves the next 48 weeks pointed at the wrong target.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
