# Week 91 — consensus-engine v1.0 ship + [Tempo] both Tempo crates v0.1.0

> **Phase**: 5
> **Month**: M23
> **Track**: Multi (Reth flagship SHIP + Tempo crate SHIPs + v2 ledger VSR-replication + exec-vm v1.5 block-stm start)
> **Hours target**: 40h/wk
> **Mirror target**: Reth `consensus-engine`, Tempo crates, **TigerBeetle leader-replicates-follower-applies wire-up**, **Aptos block-stm read/write-set tracking**
> **Crate(s) touched**: `consensus-engine`, `tempo-evm-ext`, `tempo-payment-lane`, **`ledger-deterministic` (v1.0 replication)**, **`exec-vm` (v1.5 block-stm rwset)**
> **v2 modifications**: ADD ledger v1.0 ↔ consensus-vsr replication wire-up (Mon-Tue) and ADD exec-vm v1.5 block-stm read/write-set tracking (Thu-Fri).

**Monday — Final benchmarks**

- [ ] Commit + log (~10 min)
- [NEW v2] **ledger v1.0: integrate consensus-vsr replication (part 1)**
  - [ ] **Build**: `crates/ledger-deterministic/src/replication.rs` — wire ledger's `apply()` to consensus-vsr's `NormalReplica.commit()` hook. Leader replica applies + replicates; followers apply on commit. (~3 hrs)
    - **Why**: this is the load-bearing integration of v1.0 — without it, ledger is single-process; with it, ledger is a real distributed replicated state machine. Mirror TigerBeetle's structure: leader receives client op → replicates via VSR → all replicas apply on commit. The contract: bytes-on-the-wire are VSR messages; bytes-in-the-state-machine are ledger ops; the gap between is this file.
    - **Read first**: TigerBeetle `lib/tigerbeetle/src/replica.zig` `commit_journal()` / `commit_op_now()`; LIONS-VSR §4 "Operation Processing"; your own consensus-vsr `NormalReplica` API.
    - **Design**:
      ```rust
      pub struct ReplicatedLedger {
          sm: LedgerSM,
          replica: NormalReplica,
          pool: MessagePool,
          tx_pending: FixedMap<TransferId, oneshot::Sender<TransferResult>>,
      }
      impl ReplicatedLedger {
          pub fn submit(&mut self, op: LedgerOp) -> impl Future<Output = TransferResult>;
          fn on_committed(&mut self, op_num: OpNum, payload: &[u8]) {
              let op: LedgerOp = decode(payload);
              let result = self.sm.apply(&op);
              if let Some(tx) = self.tx_pending.remove(&op.id()) { tx.send(result).ok(); }
          }
      }
      ```
      **Invariants**: (i) ledger's `apply()` is invoked ONLY through `on_committed` — never directly, even on leader; (ii) leader and follower invoke `apply()` with byte-identical payload in same op-num order → byte-identical state_hash; (iii) `tx_pending` map exists only on leader (followers don't have client connections); (iv) op is durable BEFORE apply (VSR's prepare+quorum ack before commit).
    - **Build steps**: 1. (~15 min) `ReplicatedLedger::new(replica_id, peers, sm)` constructs. 2. (~25 min) `submit(op)`: leader path — assigns TransferId, encodes, calls `replica.client_request()`, registers oneshot in tx_pending, returns Future. 3. (~25 min) `on_committed` callback registered with replica; called by replica when op commits. 4. (~30 min) follower path: same `on_committed` callback fires on commit message receipt; apply identical op. 5. (~25 min) leader change handling: when replica's `Status::View` changes, current leader's tx_pending is in limbo; respond `Err(LeaderChanged)` to those clients. 6. (~25 min) snapshot replication: on follower-catchup, transfer LedgerSM snapshot + journal tail. 7. (~25 min) state_hash gossip: every N ops, replica gossip state_hash; mismatch → invariant violation. 8. (~20 min) tests: 3-replica, drive 100k Transfers via leader, all replicas converge.
    - **Pitfalls**: *Replication-latency tail*: **Symptom**: leader-to-follower-commit p99 hugely worse than apply p99. **Fix**: separate metrics for replication time + apply time; tune VSR batch_size + io_uring journal group-commit. *Ordering across replicas*: **Symptom**: follower applies op X before X-1 due to network reorder. **Fix**: VSR's op_num strictly ordered; followers buffer out-of-order and apply in sequence. *Snapshot-restore safety*: **Symptom**: follower restored from old snapshot; replays journal but state_hash mismatch. **Fix**: snapshot tagged with op_num at capture; replay strictly from snapshot.op_num + 1. *Double-apply on leader-change*: **Symptom**: client retries op after leader change; new leader applies it again. **Fix**: TransferId-based idempotency at SM layer — pure-replay safe. *tx_pending leak*: **Symptom**: client disconnects mid-op; sender drops; pending entry grows. **Fix**: TTL on pending entries; cleanup periodically.
    - **Test scenarios**: 3-replica config, 100k Transfers via leader → all replicas state_hash equal at end. Inject leader kill mid-batch → new leader elected, client retry succeeds. Network partition (leader isolated 5s) → minority blocks, majority re-elects, ops continue. State_hash gossip mismatch test: inject deliberate bug → mismatch detected within N ops.
    - **Acceptance**: 100k Transfers replicated; state_hash equal across replicas; p99 commit < 500μs for local cluster.
    - **Downstream**: v1.0 SHIP (W92) gates on this; consensus-engine v1.0 (W91 Wed) optionally swaps Raft for this stack; ops chaos (W105+) tests survival.


**Tuesday — DESIGN.md**

- [ ] consensus-engine DESIGN.md showing inheritance tree. (~30 min)
- [ ] Commit + log (~10 min)
- [NEW v2] **ledger v1.0: integrate consensus-vsr replication (test sweep)**
  - [ ] Test: 3-replica config, 100k Transfers via leader, all replicas converge to same state hash. (~60 min)

**Wednesday — Release tag**

- [ ] `consensus-engine v1.0.0` tag. Third Reth flagship deliverable. (~30 min)
  - **Why**: consensus-engine is the public Reth-shaped crate at bar (b): EL+CL handshake (Engine API JWT), payload builder, fork choice. v1.0 means PUBLISHABLE: workable for a real Sepolia sync. Mirror Reth's `crates/engine/tree` and `crates/engine/primitives` interface so the inheritance tree is auditable to a Reth reviewer.
  - **Read first**: Reth `crates/engine/tree/src/tree/mod.rs` (engine tree algorithm), `crates/engine/primitives/src/lib.rs`, Engine API spec (engine_newPayloadV3, engine_forkchoiceUpdatedV3).
  - **Design**:
    ```rust
    pub struct ConsensusEngine<DB: Database, EVM: ExecutorProvider> {
        chain: ChainSpec,
        fork_choice: ForkChoiceState,
        payload_builder: PayloadBuilder<EVM>,
        jwt_secret: JwtSecret,
        rpc: EngineRpcServer,
    }
    impl ConsensusEngine {
        pub async fn engine_new_payload(&mut self, p: ExecutionPayload) -> PayloadStatus;
        pub async fn engine_forkchoice_updated(&mut self, fc: ForkChoiceState, pa: Option<PayloadAttributes>) -> ForkChoiceUpdated;
        pub async fn engine_get_payload(&mut self, id: PayloadId) -> ExecutionPayload;
    }
    ```
    **Invariants**: (i) JWT auth on all engine_* RPC; (ii) fork-choice state transitions match Capella+Cancun spec; (iii) payload builder respects PBS/MEV-Boost separation: builder client API at /eth/v1/builder/header.
  - **Build steps**: 1. (~5 min) Cargo bump 1.0.0 + CHANGELOG. 2. (~10 min) inheritance audit grep `eth_consensus`, `eth_stage`, `exec_vm`, `storage_trie`, `consensus_bft`. 3. (~10 min) tag + sign + push. 4. (~5 min) update inheritance map.
  - **Pitfalls**: *Reth API drift*: **Symptom**: Reth introduces engine_newPayloadV4 mid-W91. **Fix**: pin to spec version; document.
  - **Test scenarios**: end-to-end Sepolia sync at least header phase under v1.0 + consumes ledger v1.0 as state backend for tests.
  - **Acceptance**: tag pushed; inheritance ratio ≥0.70; CHANGELOG complete.
  - **Downstream**: blog post fodder; recruitment artifact; W92-94 reviews leverage this as the "look at my Reth-shaped crate".
- [ ] **SHIP-day ritual for consensus-engine v1.0.0** (bar (b), the third Reth flagship; apply all 7 enrichment dimensions):
  1. **Inheritance audit (the ritual)** — bar (b) Layer 5 target ≥0.70:
     - Inheritance lineage with version pins: `eth-consensus v0.5 (W41) + eth-stage v0.5 (W56) + exec-vm v1.0 (W68) + storage-trie v1.0 (W44) + consensus-bft v0.5 (W68) + eth-rpc-types v0.3 + eth-network-codec v0.5 (W41)`.
     - (~30 min) Per-module classify. engine_api/server.rs CONSUMES eth-network-codec. payload_builder/ CONSUMES exec-vm + eth-stage. fork_choice/ CONSUMES eth-consensus + consensus-bft (optional tie-break). state_backend/ CONSUMES storage-trie.
     - (~10 min) Ratio target ≥0.70. Record in `notes/inheritance_audit_consensus_engine_v1_0.md`.
  2. **Bar-(b) acceptance gate**:
     1. (~20 min) **Sepolia header-sync gate**: `cargo run --release --example sepolia_header_sync` reaches current head in <2h on consumer hardware.
     2. (~15 min) **Engine API conformance gate**: `cargo test -p consensus-engine --release engine_api_v3_conformance` — all V1-V4 methods round-trip against Lighthouse fixture.
     3. (~15 min) **JWT gate**: HS256 round-trip; expired-token rejection; signature-mismatch rejection.
     4. (~15 min) **Fork-choice gate**: 100 random fork-choice scenarios; result matches reference spec.
     5. (~10 min) **No regression vs storage-trie v1.0 / exec-vm v1.0 benches**: criterion delta <5%.
     6. (~5 min) Tag `consensus-engine v1.0.0`, push.
  3. **Perf targets** — record in `notes/consensus_engine_v1_0_perf.md`:
     - engine_newPayload P99: <250ms for avg Sepolia block.
     - engine_forkchoiceUpdated P99: <50ms.
     - Sepolia header-sync: <2h to current head.
     - Memory: ≤2 GB during steady-state header sync.
  4. **Downstream handoff**:
     - Sepolia sync demo (W85+ retry post-v1.0).
     - `tempo-evm-ext v0.1 (today)`: registers Tempo precompiles against consensus-engine's exec-vm.
     - Recruitment artifact: the "Reth-shaped crate" link for IC outreach.
     - Update `notes/inheritance_map.md`: consensus-engine v1.0 as the Reth-track flagship.
  5. **Blog post draft (90 min)** — "A Reth-shaped consensus-engine crate from scratch" in `notes/blog_consensus_engine_v1_0.md`:
     - Section 1: Why a separate consensus-engine crate (cite Reth's modular structure).
     - Section 2: Upstream — Reth's `crates/engine/tree` + `crates/engine/primitives`.
     - Section 3: Where I diverge — pluggable consensus backend (raft / bft / vsr) via trait.
     - Section 4: The pitfall — Engine API V3 vs V4 spec drift mid-development.
     - Section 5: Sepolia sync timing.
     - Section 6: Downstream — Tempo extensions register via exec-vm; recruitment.
  6. **Interview 1-pager** (`notes/portfolio_consensus_engine_v1_0.md`):
     - 1-sentence elevator: "A Reth-shaped consensus-engine crate at bar (b); Engine API V1-V4 + JWT + payload-builder; Sepolia syncs in <2h."
     - 3 bullets on technique depth: (a) Engine API JWT spec adherence; (b) pluggable consensus backend (raft/bft/vsr); (c) payload-builder with PBS/Builder-API hooks.
     - 1 bullet on outcome: Sepolia header-sync <2h; engine_newPayload P99 <250ms.
     - 1 bullet on inheritance: 70% ratio — eth-consensus + eth-stage + exec-vm + storage-trie + consensus-bft all consumed.
     - 3 hardest pitfalls: (a) JWT clock-skew window (60s allowance); (b) fork-choice tie-break when both branches have same weight; (c) Engine API V3→V4 spec drift.
     - 3 interviewer probes: (i) "How does your engine handle a reorg?" — fork-choice + payload re-execution; (ii) "How would you swap raft for VSR?" — pluggable Backend trait; (iii) "How do you keep up with Engine API spec changes?" — pin to spec version, version-bump on breaking change.
  7. **Energy check + rest** (30 min): Reth flagship #3 ships. The Reth-track portfolio is now (storage-trie, exec-vm, consensus-engine) — three v1.0 Layer-5 crates. With Tempo extensions tagging today too, this is the densest milestone day of the journey.
- [ ] **Inheritance audit**: consensus-engine LOC vs LOC calling into eth-consensus / eth-stage / exec-vm /
  storage-trie / consensus-bft. Target ≥70%. (~30 min)
  - **Why**: bar (b) target ≥0.70 inheritance ratio because consensus-engine is meant to be a thin orchestrator over the already-shipped L3 crates. If it's <0.70 the design has duplicated logic that belongs deeper.
  - **Build steps**: 1. (~10 min) `tokei` total LOC in `crates/consensus-engine/src/`. 2. (~10 min) grep call-sites: `eth_consensus::`, `eth_stage::`, `exec_vm::`, `storage_trie::`, `consensus_bft::`, count LOC of the calling functions. 3. (~5 min) compute ratio; identify duplication if <0.70. 4. (~5 min) document in `notes/inheritance_map.md`.
  - **Acceptance**: ratio ≥0.70 documented.

- [ ] **Inheritance audit**: consensus-engine LOC vs LOC calling into eth-consensus / eth-stage / exec-vm /
  storage-trie / consensus-bft. Target ≥70%. (~30 min)
- [ ] Commit + log (~10 min)

**Thursday — Tempo crates ship**

- [ ] Integration example: full 3-crate Reth example. (~30 min)
- [ ] [Tempo] 1 hr: **Tag `tempo-evm-ext v0.1.0`**. Finalize at least TIP-1020 P256/WebAuthn precompile impls registered
  against exec-vm's registry. Test that registration works without forking exec-vm. (~60 min)
  - **Why**: tempo-evm-ext is the bar (b) Tempo-specific crate. v0.1.0 is the publishable scaffold: P256/WebAuthn precompile registered via exec-vm's pluggable precompile registry (so no exec-vm fork). This proves the architectural premise — your exec-vm is Tempo-extensible.
  - **Read first**: Tempo TIP-1020 spec (RIP-7212 base + Tempo extensions); RFC 8152 (COSE for WebAuthn key encoding); your exec-vm precompile registry from W31+.
  - **Design**:
    ```rust
    pub fn register_tempo_precompiles(registry: &mut PrecompileRegistry) {
        registry.register(0x0100, p256_verify);     // P256 sig verify
        registry.register(0x0101, webauthn_verify); // WebAuthn assertion verify
    }
    fn p256_verify(input: &[u8], gas: u64) -> PrecompileResult { /*...*/ }
    ```
    **Invariants**: (i) exec-vm crate UNCHANGED (registry is pluggable); (ii) gas cost matches TIP-1020 (3450 for P256); (iii) input parsing strict — reject malformed COSE.
  - **Build steps**: 1. (~15 min) implement `p256_verify` using `p256` Rust crate with deterministic ECDSA verify. 2. (~20 min) implement `webauthn_verify`: parse authenticator data, parse clientDataJSON, verify hash chain, verify signature over concat. 3. (~10 min) `register_tempo_precompiles(registry)` helper. 4. (~10 min) test: empty registry + register + invoke via opcode CALLPRE; works without forking exec-vm. 5. (~5 min) tag.
  - **Pitfalls**: *Non-determinism in p256 crate*: **Symptom**: some signature library uses RNG for nonce; here we only verify, but check. **Fix**: use only `Verifier`, not `Signer`; reject any path that needs RNG. *WebAuthn clientDataJSON parsing*: **Symptom**: malformed JSON not rejected. **Fix**: strict JSON + size cap (4KB).
  - **Test scenarios**: vector tests from RIP-7212 + TIP-1020 spec; gas accounting matches.
  - **Acceptance**: tag pushed; precompiles work via exec-vm without fork; tests green.
  - **Downstream**: Tempo paper-trade rig (W92) drives both EVM extensions and payment-lane; consensus-engine v1.0 imports tempo-evm-ext as optional feature.
- [ ] [Tempo] 30 min: **Tag `tempo-payment-lane v0.1.0`**. Finalize lane reservation strategy from W83. Update README
  documenting strategy and trade-offs vs upstream Tempo. (~30 min)
- [ ] [Tempo] Update workspace root README to document all 3 Tempo crates alongside the 13 Reth crates AND the 6 HFT
  crates AND the 8 Layer-1/2/3/4 primitive crates. (~30 min)
- [ ] Commit + log (~10 min)
- [NEW v2] **exec-vm v1.5: block-stm read/write-set tracking (background reading)**
  - [ ] Read Aptos block-stm paper + upstream Reth parallel-EVM PR discussion. Notes to `notes/block_stm_design.md`. (~60 min)

**Friday — Blog consideration**

- [ ] If ready, draft consensus-engine post. (~30 min)
- [ ] [Tempo] If writing Phase 5 blog, decide framing: "consensus-engine + Tempo payment lanes" reads more distinctive
  than just "consensus-engine." Lean into Tempo angle for distribution reach. (~30 min)
- [ ] Commit + log (~10 min)
- [NEW v2] **exec-vm v1.5: block-stm read/write-set tracking (impl)**
  - [ ] **Build**: `crates/exec-vm/src/parallel/rwset.rs` — per-tx read-set + write-set tracking. (~3 hrs)
    - **Why**: block-stm is the only credible path to a parallel EVM at bar (b). The core invariant: optimistically execute N txs in parallel, track per-tx read-set + write-set, validate at commit; on conflict re-execute. Mirror Aptos's block-stm paper and Reth's parallel-EVM PR (Paradigm). This file is THE building block — without it, no parallel exec.
    - **Read first**: Aptos block-stm paper "Block-STM: Scaling Blockchain Execution by Turning Ordering Curse to a Performance Blessing"; Reth `crates/revm/src/parallel.rs` PR; Diem's `block_stm.rs` original Move implementation.
    - **Design**:
      ```rust
      pub struct RwSet {
          reads: ArrayVec<(StorageKey, Version), MAX_READS>,
          writes: ArrayVec<(StorageKey, StorageValue), MAX_WRITES>,
      }
      pub struct StorageKey { addr: Address, slot: U256 }
      pub struct Version { tx_idx: u32, incarnation: u32 }
      // multi-version data: per StorageKey, list of (Version, Value)
      pub struct MultiVersionStore {
          slots: dashmap::DashMap<StorageKey, BinaryHeap<VersionEntry>>,
      }
      pub struct VersionEntry { tx_idx: u32, incarnation: u32, value: StorageValue }
      ```
      **Invariants**: (i) tx N reads version ≤ N (sees ALL completed earlier writes); (ii) tx N writes are tagged (N, incarnation) — incarnation increments on re-execution; (iii) read-set captures versions read so validator can detect "did anyone in [0, N) write to that key with a higher version after I read it?"; (iv) max size: txns reading more than MAX_READS reject + execute sequentially.
    - **Build steps**: 1. (~15 min) define `RwSet`, `Version`, `StorageKey`. 2. (~30 min) `MultiVersionStore::read(key, tx_idx) -> (Version, Value)` — walk version list, pick highest version < tx_idx that's `Committed` (not `Estimate`). 3. (~30 min) `MultiVersionStore::write(key, tx_idx, incarnation, value)` — insert into heap; if existing entry for (tx_idx, incarnation), replace. 4. (~30 min) `RwSet` instrumentation: revm's `Database` trait wrapped → each `storage()` call appends to reads; each commit appends to writes. 5. (~30 min) validation: replay reads; for each (key, version) check current store's read-version still equals; if not, mark conflict + re-execute. 6. (~25 min) `Estimate` vs `Committed` distinction: tx N in-progress writes are `Estimate`, finalize to `Committed` only after validation. 7. (~10 min) memory: stash incarnations per tx via Vec<RwSet> indexed by tx_idx.
    - **Pitfalls**: *Read-set collapse*: **Symptom**: tx reads 100 storage slots, RwSet grows huge → memory blows up. **Fix**: MAX_READS cap (e.g. 4096), fallback to sequential exec for large reads. *Write-set growth*: **Symptom**: same as above for writes; some contracts write 1000s of slots. **Fix**: MAX_WRITES cap; sequential fallback. *Hot-key contention*: **Symptom**: every tx reads/writes balanceOf(0xUSDC), every parallel exec re-executes due to conflict — speedup < 1×. **Fix**: classify "hot keys" upfront (per-block analysis); group hot-key txs into a sequential subgraph. *Estimate-as-final*: **Symptom**: tx reads Estimate value, commits using it; later that estimate gets invalidated. **Fix**: never return Estimate value to a reader — reader either waits or treats key as dependency.
    - **Test scenarios**: 1000-tx synthetic block, 5% conflict → re-execute rate < 10%, validation correct. Adversarial: all-write-same-key block → falls back to sequential, no deadlock. Property: parallel exec result == sequential exec result for any block.
    - **Acceptance**: round-trip parallel exec on synthetic blocks matches sequential exec byte-identically; conflict detection 100% accurate.
    - **Downstream**: W92 versioned memory builds the multi-version store; W93 executor consumes RwSet for validation.

**Saturday — Reth PR**

- [ ] Commit + log (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
