# Week 125 — Active applications begin

> **Phase**: 7
> **Month**: M33
> **Track**: Career (destination landing) — Path E Tier A HFT applications
> **Hours target**: 40h/wk (M19-M34)
> **Mirror target**: (n/a — career/applications week; no upstream module)
> **Crate(s) touched**: (none — maintenance only on Reth + Tempo; no new code this week)
> **Bar**: (n/a)
> **Feeds into**: W126 inbound triage + first-round scheduling (replies from this week's submissions land in the triage funnel); W128-W129 final-round interviews (staggering decided this week prevents same-week collisions); W131 offer decision (the firm shortlist sourced here is the candidate set); W132 resignation + relocation (the 30-day-notice plan drafted Thursday is executed once an offer lands); backup-job milestone W24-30 gate (merged-PR-backed warm intros from W120 are the inbound channel activated here).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): `outreach/log_*.md` from W120 (warm-intro contacts to activate); README §"Destination landing" (~line 1047, application→offer→arrival timeline) and §"Milestones" (M33 = 3300+ runtime hours, first/final rounds underway); README §"Risk register" (~line 489, "space interviews, drop weakest opportunities" + "negotiate notice in advance W125"); `relocation_research.md` from W120 (visa/sponsorship facts to pre-screen firms by tractable geography); destination-firm careers pages + recruiter channels (official sources only, lookup-when-stuck).
> **v2 modifications**: target firm list anchored on Tier A HFT firms (the Path E default destination per v2). Crate portfolio includes 3 net-new v2 crates — referenced in the per-firm application as portfolio evidence, not built this week.

This is a career/applications week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding. Every item below instead carries an explicit artifact file + an Output line that proves it done, and the planning/decision items carry a 🎯 hardened-acceptance pass criterion and a *Mental model* of why the item exists now. The week's job: convert 124 weeks of merged-PR credibility + the W120 warm-intro network into staggered, evidence-anchored applications to a Tier A HFT shortlist, while keeping Reth/Tempo maintenance and the 3000-hour runtime milestone on track. The single failure mode to design against is the W489 risk: saturating Phase 7 by bunching all final rounds into one week — so staggering is a first-class deliverable, not an afterthought.

**Monday — Submit applications to firms 1-2 + open the application tracker**

- [ ] Artifact: `applications/tracker.md` (private) — the single source of truth for the whole cycle. Create it with one row per shortlisted firm and columns: firm, tier (A/B), channel (`[refer]` / `[warm-intro]` / `[recruiter]` / `[direct]`), the specific portfolio claim you led with, date submitted, target-round window (to stagger), status. Then submit firms 1-2: pull the warm-intro contact from `outreach/log_*.md` where one exists, attach the portfolio (matching-engine + 2 other net-new v2 crates), and record both rows. (~40 min)
  - 🎯 Hardened acceptance: `applications/tracker.md` exists with a header row + ≥2 firm rows fully populated (no blank channel or portfolio-claim cell); each submitted firm's row carries a target-round window so the stagger plan is visible from row one.
  - *Mental model*: an application from a remote-from-Vietnam candidate is screened on signal density, not effort — leading each submission with ONE concrete portfolio claim ("a lock-free matching engine at v1.2 with criterion benches") tied to a warm intro is what 124 weeks of shipping was for. The tracker exists so that staggering (the W489 risk control) is a recorded decision per firm, not a vibe you discover too late when three final rounds collide.
  - Output: `applications/tracker.md` lists ≥2 submitted firms meeting the hardened-acceptance criteria; `grep -c '\[refer\]\|\[warm-intro\]\|\[recruiter\]\|\[direct\]' applications/tracker.md` ≥ 2.
- [ ] Activate warm intros for firms 1-2: send the intro-ask to the W120 contact for each, referencing the specific shared PR/issue. Log channel + date in the contact's row. (~20 min)
  - *Mental model*: the warm intro must land BEFORE or WITH the application, not after — a referrer's note in the recruiter's inbox the same day reframes you from "inbound resume" to "vouched-for candidate," which is the entire leverage of the W120 network. Sending it late wastes the relationship.
  - Output: `applications/tracker.md` firm 1-2 rows show channel `[warm-intro]`/`[refer]` + intro-sent date.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday applications commit.

**Tuesday — Submit to firms 3-4**

- [ ] Append firms 3-4 to `applications/tracker.md` (private) and submit them, same evidence-anchored format. Deliberately assign their target-round windows OFFSET from firms 1-2 so no two final rounds can collide in the same week (the W489 stagger control). Activate any warm intro that exists for each. (~40 min)
  - 🎯 Hardened acceptance: tracker now has ≥4 firm rows; the four target-round windows span ≥2 distinct weeks (verifiable by eye in the column) — bunching all four into one window fails this check.
  - *Mental model*: by firm 3-4 the temptation is to fire-and-forget to hit a count; resist it — Phase 7 saturates when you have six final rounds in one exhausted week and bomb all of them. Spreading the request windows now is cheap; un-bunching them after recruiters have scheduled is nearly impossible.
  - Output: `applications/tracker.md` lists ≥4 firms with windows spanning ≥2 weeks.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday applications commit.

**Wednesday — Submit to firms 5-6 + mid-cycle stagger audit**

- [ ] Append firms 5-6 to `applications/tracker.md` (private) and submit. Then do a one-pass stagger audit across all 6 rows: read the target-round windows as a column and confirm no week holds more than 2 final rounds; if a clash is forming, push the weakest-fit firm's window out (or drop it) and note the decision inline. (~40 min)
  - 🎯 Hardened acceptance: tracker has ≥6 firm rows; an explicit one-line audit verdict is written at the bottom (e.g. `Stagger audit (Wed W125): max 2 finals/week, clash on week X resolved by pushing firm Y`); no week carries >2 final-round windows.
  - *Mental model*: this audit is the concrete enactment of the README risk control "space interviews, drop weakest opportunities." Six Tier A applications is enough surface area — adding more raises collision risk without raising offer odds, so the deliverable here is a clean, spread schedule, not a longer list.
  - Output: `applications/tracker.md` shows 6 firms + a written stagger-audit verdict line.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Wednesday applications commit.

**Thursday — Day-job notice planning (no action yet)**

- [ ] Artifact: `career/resignation_plan.md` (private). Draft the 30-day-notice plan to execute ONLY once an offer is in hand (W132): the resignation-conversation talking points, the explicit offer of transition help, and the named day-job projects you'd want to wrap before leaving (with a one-line handoff note per project). Mark the file `STATUS: DRAFT — do not send until offer accepted` at the top. No action this week. (~30 min)
  - 🎯 Hardened acceptance: `career/resignation_plan.md` opens with the DRAFT/do-not-send banner; lists ≥1 specific wrap-before-leaving project with a handoff line; contains the transition-help offer sentence verbatim — so it's ready to execute, not improvise, under offer pressure.
  - *Mental model*: this is the README W491 risk control ("day-job exit lacks 30-day-notice grace — negotiate notice in advance W125"). Drafting the conversation now, calm and offer-free, means that when an offer lands you resign cleanly and keep the day-job bridge warm as a backup — improvising a resignation under offer-deadline stress is how bridges get burned. The banner enforces the "no action until offer" gate so a draft never leaks out early.
  - Output: `career/resignation_plan.md` exists meeting the hardened-acceptance criteria.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Thursday planning commit.

**Friday — Reth + Tempo maintenance**

- [ ] Artifact: append to `notes/maintenance_log.md`. Run the maintenance sweep so the public portfolio stays green DURING the application window (recruiters and referrers will click the repos this week): `cargo test` across the Reth + Tempo workspaces, address any dependency/CI breakage, and rebase any stale open PR. Record what was run + outcome. No feature work — green-keeping only. (~40 min)
  - 🎯 Hardened acceptance: `notes/maintenance_log.md` has a dated W125 entry recording `cargo test` outcome per workspace (pass/fail + fix), and any red CI is either fixed or has a one-line triage note; portfolio repos are demonstrably green this week.
  - *Mental model*: the application packet points referrers and recruiters straight at the live repos — a red CI badge or a broken `cargo test` discovered by a reviewer the same week you applied undoes the credibility the application was built on. Maintenance is reputation defense during the highest-visibility week of the journey, not busywork.
  - Output: `notes/maintenance_log.md` shows a W125 entry with per-workspace test outcome; CI green or triaged.
- [ ] Commit + log. (~10 min)
  - Output: `git log -1 --oneline` shows the Friday maintenance commit.

**Saturday — Operations rig: 3000-hour milestone audit**

- [ ] Artifact: `ops/runtime_hours_audit.md`. From the operations-rig logs, total cumulative runtime hours and confirm the M33-mid target of 3000+ is met (README milestone: M33 end = 3300+). Record current total, the daily/weekly accrual rate, and the projected date you cross 3300 — flag if the rate is behind the M33-end target so it's caught now, not at the milestone. (~30 min)
  - 🎯 Hardened acceptance: `ops/runtime_hours_audit.md` has a dated W125 entry with {current cumulative hours, accrual rate/week, projected 3300-hour crossing date, on-track? yes/no}; if behind, a one-line remediation (e.g. add a soak workload) is named.
  - *Mental model*: the 4000+-hours-by-M36 figure is a literal bar (c) gate (README §"Bar policy") and a milestone Tier A interviewers will probe ("how long has it actually run?"). Auditing the trajectory mid-M33 — not at M36 — leaves runway to add soak load if the rig is accruing too slowly, which is the only fix once you're behind on a wall-clock metric.
  - Output: `ops/runtime_hours_audit.md` shows the W125 entry meeting the hardened-acceptance criteria; current total ≥ 3000.
- [ ] Commit notes. (~10 min)
  - Output: `git log -1 --oneline` shows the Saturday audit commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
</content>
</invoke>
