# Week 47 — revm preview for Phase 4 + Reth maintenance

> **Phase**: 3
> **Month**: M12
> **Track**: Reth (primary) | Tempo (additive)
> **Hours target**: 30h/wk (M1-M18)
> **Mirror target**: revm (`revm::Evm`, `revm::Handler`, `revm::Context`, `revm::Inspector`) as the upstream exec-vm mirrors Phase 4 will push toward + `reth-evm` (`ConfigureEvm` trait) as the integration seam; tempo-evm-ext design notes mirror revm's precompile-registry extension point.
> **Crate(s) touched**: exec-vm v0.1 (read-only review — no version bump this week), tempo-evm-ext (design notes only — scaffold lands W54), storage-trie v1.0 (maintenance bug-fixes only)
> **Bar**: (b)   ← exec-vm and storage-trie are Reth-track bar (b); tempo-evm-ext is Tempo bar (b). No bar (c) work this week.
> **Feeds into**: W49-W51 exec-vm hardening (this week's gap-map becomes the W49 work queue), W53 complete opcodes, W54 exec-vm precompiles + tempo-evm-ext scaffold (this week's trait sketch is the W54 starting point), W58 exec-vm dispatch, W68 exec-vm v1.0, W91 tempo-evm-ext v0.1.0.
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): revm source `crates/revm/src/` (`Evm`, `Handler`, `Context`, `Inspector`); reth `crates/evm/src/` (`ConfigureEvm`, `EvmEnv`); revm book §Architecture; Tempo TIP-1020 (signature-verification precompile); exec-vm `notes/` from W9/W17.
> **v2 modifications**: none

> **Week shape**: This is a light orientation + maintenance week (W45-W48 = Reth maintenance + M12 decision in Appendix E). There is no new Build exercise — exec-vm is not version-bumped this week. The deliverables are (1) a *gap-map* that becomes the W49-W51 hardening work queue, and (2) a tempo-evm-ext trait sketch that becomes the W54 scaffold starting point. Every learning item below carries a hardened acceptance criterion so "re-read" produces a checkable artifact, not a vague pass.

**Monday — revm architecture refresher (with Phase 4 eyes)**

- [ ] Re-read revm architecture and capture it as a one-page map. File: `notes/47_revm_architecture_map.md`. Walk `revm::Evm` → `Handler` → `Context` → `Database`/`Journal` → `Interpreter`, naming for EACH the boundary type that crosses it and where the Inspector hook fires. Mirror target: this is the upstream exec-vm is climbing toward; Phase 4 (W49-W68) re-implements this layering. (~45 min)
  - 🎯 **Hardened acceptance**: `notes/47_revm_architecture_map.md` names all 5 layers (Evm/Handler/Context/Database/Interpreter), the 2 trait boundaries exec-vm must mirror (`Database` for state access, `Handler` for the execution loop), and one sentence per layer on "what exec-vm v0.1 has vs what's missing." Cross-check against revm `crates/revm/src/lib.rs` module tree — every pub module appears in the map or is explicitly marked "skip (Phase 4)."
  - *Mental model*: revm is the reference implementation exec-vm inherits its SHAPE from (not its code — exec-vm is a clean-room reimplementation for the learning arc). Re-reading it now, after having built v0.1 in W9/W17, surfaces the layering decisions that were invisible the first time through. The map is the contract Phase 4 builds against.
- [ ] Commit notes (~10 min)

**Tuesday — Identify revm learning gaps → the W49 work queue**

- [ ] Turn Monday's map into a prioritized gap-map. File: `notes/47_revm_gap_map.md`. For each revm subsystem exec-vm v0.1 does not yet have (precompiles, journaling/revert, EOF dispatch, gas accounting edge cases, access lists), write: the revm source file that owns it, the rough LOC, the dependency (which exec-vm piece must exist first), and the target week from Appendix E (W53 opcodes, W54 precompiles, W55 journaling). (~30 min)
  - 🎯 **Hardened acceptance**: `notes/47_revm_gap_map.md` lists ≥6 gaps, each with {revm source path, blocking dependency, target week}. The gap list reconciles with Appendix E rows W49-W68 — no gap is orphaned without a scheduled week, and no scheduled exec-vm week (W49-W55) lacks a feeding gap. This file IS the W49 Monday work queue.
  - *Mental model*: the gap-map is the bridge between "I read revm" and "I know what to build." Without it, W49 starts cold. Mirror discipline: each gap names its upstream revm owner so the Phase 4 reimplementation can be diffed against the reference for correctness.
- [ ] Commit notes (~10 min)

**Wednesday — reth-evm integration seam**

- [ ] Read `reth/crates/evm/src/` and document the `ConfigureEvm` seam. File: `notes/47_reth_evm_seam.md`. This is the trait reth uses to plug an EVM into the executor — it's how exec-vm will eventually be consumed by `consensus-engine` (W91). Capture: the `ConfigureEvm` trait methods, the `EvmEnv` type it threads, and the one place block-level config (basefee, blob params) enters. (~60 min)
  - 🎯 **Hardened acceptance**: `notes/47_reth_evm_seam.md` records the `ConfigureEvm` method signatures (`evm_with_env`, `evm_for_block` or current equivalent) and identifies the exact integration point exec-vm must expose to be consumed by consensus-engine without forking. One sentence: "exec-vm v0.1 exposes X / is missing Y to satisfy this seam." Verified against the actual `reth/crates/evm/src/lib.rs` trait def (note the reth commit/tag read, since this trait churns).
  - *Mental model*: exec-vm in isolation is a learning artifact; the value is it being a drop-in behind reth's `ConfigureEvm`. Reading the seam now tells me which exec-vm types must be public and which lifetimes must thread, so Phase 4 doesn't paint itself into a non-integrable corner.
  - 🧮 **Paper drill** — write in `notes/47_reth_evm_seam_drill.md`:
    - D-SEAM on the `ConfigureEvm` trait with two impls (here-now revm-backed `EthEvmConfig`, future exec-vm-backed config): list its methods (`evm_with_env`, `evm_for_block`/equivalent, the `EvmEnv` it threads); audit whether any revm-current-impl vocabulary (journal/inspector/handler-stack) leaks into the outer trait signatures, and show the exec-vm config type-checks against the SAME trait + the same `consensus-engine` call site with zero call-site changes — the swap-safety proof for W91.
    - D-WALK on one concrete block-config trace: take a single block with `basefee=7 gwei` + blob params set, and hand-trace where those values enter through `evm_for_block` → land in `EvmEnv`/`BlockEnv` → reach the interpreter, naming each boundary type crossed; the invariant it makes concrete ("block-level config enters at exactly one point because …").
- [ ] Commit notes (~10 min)

**Thursday — Reth PR maintenance**

- [ ] Address review feedback on the open Reth PR (or, if none open, pick one good-first-issue from `reth/crates/evm` or `reth/crates/trie` that the gap-map touched and open a draft). File(s): whatever the PR diff covers; log the PR URL + state in `notes/47_reth_pr_log.md`. Output: `notes/47_reth_pr_log.md` updated with PR number, current CI state, and the next reviewer action; if a new draft, the `gh pr create` URL recorded. (~50 min)
- [ ] Commit + log (~10 min)

**Friday — storage-trie maintenance**

- [ ] Triage and fix any open bug on storage-trie v1.0. File(s): the failing module under `crates/storage-trie/src/` + its test. Output: `cargo test -p storage-trie` green; if a real bug was found, a regression test named `regression_w47_<symptom>` is added and fails on `git stash` of the fix; if no bug exists, `notes/47_storage_trie_audit.md` records the audit (ran full test + clippy + a fuzz smoke, all clean) so "maintenance" is a checkable no-op, not a skipped item. (~30 min)
  - *Mental model*: storage-trie hit v1.0 at W44; this is the first maintenance touch. The discipline is that "no bugs found" must still produce evidence (the audit note), because a silent skip and a clean audit are indistinguishable in the log otherwise.
- [ ] Commit + log (~10 min)

**Saturday — Phase 4 prep: exec-vm state review + tempo-evm-ext trait sketch**

- [ ] Review exec-vm state and write the Phase 4 outline. File: `notes/47_exec_vm_phase4_outline.md`. exec-vm was seeded W9 (v0.0.1) and extended W17 (v0.1). Re-read its README + the opcode coverage matrix; produce the delta to Phase 4 v1.0 (W68). (~30 min)
  - 🎯 **Hardened acceptance**: `notes/47_exec_vm_phase4_outline.md` contains the opcode coverage matrix delta (implemented vs missing groups), and a one-line milestone per Appendix E exec-vm week W49→W68. Reconciles with Tuesday's gap-map — every gap appears in the outline timeline.
  - *Mental model*: this folds Monday's map + Tuesday's gaps into a single forward plan, so Phase 4 opens with a roadmap instead of a re-read.
- [ ] [Tempo] Sketch the tempo-evm-ext extension surface against TIP-1020. File: `notes/tempo_evm_ext_design.md`. Re-read TIP-1020 (signature-verification precompile) with exec-vm's precompile registry in mind. Specify the 3-4 traits and types tempo-evm-ext needs so it can be a *downstream* crate of exec-vm (inheriting, not forking — target ratio ≥0.85 per the Inheritance Map, audited W91 Thu). (~60 min)
  - 🎯 **Hardened acceptance**: `notes/tempo_evm_ext_design.md` names the extension seam exec-vm must expose (a `PrecompileRegistry` / `register_precompile` hook — exact name TBD against W54 scaffold) and the 3-4 tempo-evm-ext items: the precompile trait, the address→precompile mapping type, the TIP-1020 signature-verification precompile impl stub, and the wiring type that injects it into exec-vm's registry. Each item states which exec-vm type it inherits from, so the ≥0.85 inheritance ratio is provable at W91. No tempo-evm-ext type duplicates an exec-vm type.
  - *Mental model*: tempo-evm-ext (Layer 7, W91) must inherit exec-vm's execution path and only ADD precompiles/extensions — forking exec-vm would violate the inheritance principle and blow the 0.85 ratio audit. Sketching the seam NOW, while exec-vm's internals are fresh from the Saturday review, ensures the W54 scaffold exposes the right registration hook instead of bolting tempo logic into exec-vm core. This sketch is the W54 starting point.
  - 🧮 **Paper drill** — write in `notes/47_tempo_evm_ext_drill.md`:
    - D-STRUCT on the sketched `Precompile` trait + `PrecompileRegistry` hook: write out the trait with its associated types/methods (`fn run(&self, input, gas) -> PrecompileResult`, the address→precompile mapping), then decide object-safety — is `dyn Precompile` needed in the registry (boxed table) or can it stay generic; if `dyn`, mark which method shapes/`Self`-returns or generic params would break object-safety and how the registry's storage type forces the choice.
    - D-SEAM on the `register_precompile` hook with two consumers (exec-vm's own eth precompiles, tempo-evm-ext's TIP-1020 sig-verify precompile): show tempo-evm-ext registers through the SAME hook with zero exec-vm-core edits (no tempo vocabulary leaks into exec-vm), proving the ≥0.85 inheritance ratio — every tempo type either inherits an exec-vm type or is a pure addition behind the hook; flag any sketched item that duplicates an exec-vm type instead of inheriting it.
- [ ] Commit + log (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
