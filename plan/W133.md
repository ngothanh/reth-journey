# Week 133 — Day-job final 2 weeks + ramp-down (bet OR bridge engaged; relocation conditional)

> **Phase**: 7 — Bet Placement / Bridge
> **Month**: M34
> **Track**: (career — bet-path transition; no crate this week)
> **Hours target**: 40h/wk (W118–W136 window; drops to 30h/wk at W137)
> **Mirror target**: (n/a — career/transition week; no upstream module)
> **Crate(s) touched**: (none — maintenance only on the venue rig + Reth/Tempo spine; no new code)
> **Bar**: (n/a)
> **Feeds into**: W134 start at the bet (or bridge) — remote-first, hub only if the §7 flip-trigger fired (the hand-off doc, exit-clean state, and conditional-relocation-ready state from this week are its precondition); W135–W138 first month ramping into the core engine I was brought in to build (the operations-rig fate decision here is what keeps the 4000-runtime-hours-by-M36 milestone reachable while remote); W139+ Bet #2 thesis seeds (a clean, referral-eligible exit is a node in the §7 "each bet upgrades the next" network); the 4000-runtime-hours-by-M36 milestone (Tuesday rig decision is the gate that keeps it alive after any move).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): current employer's off-boarding / knowledge-transfer policy (Monday hand-off scope, Friday exit checklist — lookup-when-stuck); README.md §5 (M36 gate — Bet #1 placed OR bridge engaged), §6 (ecosystem-selection checklist — the GO/NO-GO that W131 already applied), §7 (geography: pre-stage, don't pre-commit; the explicit flip-trigger that gates relocation), §9 (risk register: "Best venues want SG/Dubai presence" mitigation); destination-geography relocation/visa/token-tax portals (Saturday packing + paperwork — official sources only, and ONLY if the flip-trigger fired); home-rig remote-management runbook from prior ops weeks (Tuesday rig-fate decision — the existing runbook, not new research).
> **v3 modifications**: (1) **Re-aimed from "destination landing (job)" to the bet-path.** v2 treated this as the final pre-relocation week before landing a Tier-A HFT IC job, with relocation as the default. v3 (§5 M36, §7) makes relocation **conditional on the §7 flip-trigger** — the default is Vietnam-remote, and W131 has already decided **Bet #1 OR bridge (crypto-MM cash seat)**, not "a job." Saturday's relocation block is now gated: it executes only if W132's flip-trigger fired; otherwise it collapses to a light remote-onboarding-prep block. (2) **Hand-off + exit re-narrated around the warm-network channel (§7).** The clean exit is no longer just professional hygiene — it is a node in the Binance-alumni / former-colleague referral map that sources Bet #2 ("each bet upgrades the next"). (3) **Frontmatter "Feeds into" re-pointed at the v3 INDEX sequence** (W134 = start at the bet/bridge remote-first; W139 = Bet #2 seeds), and the bridge-vs-bet branch is surfaced explicitly so the week works under either W131 outcome.

This is a career/transition week, not a Build week. There is no crate, no upstream mirror, and no engineered trap-test arc — manufacturing one would be padding (the exemplar's career-week discipline). Every item below instead carries an explicit artifact file + an Output line that proves it done, and the judgement items (Tuesday rig-fate, Saturday conditional relocation, Friday exit) carry a hardened-acceptance pass criterion and a mental model of why the item exists *now*, in the bet-path frame. The week's job: leave the current employer clean, well-documented, and **referral-eligible** (the §7 warm-network channel that sources Bet #2); lock the operations-rig fate so the runtime-hours target survives whatever W134 turns out to be; and arrive at W134 ready to start at the bet (or bridge) — remote-first, with a relocation executed **only if** the §7 flip-trigger actually fired. Wednesday/Thursday are deliberately the lightest days — the ramp-down buffer absorbing spillover from Monday hand-offs or Tuesday logistics.

**The branch this week sits on (read before Monday).** W131 produced one of two outcomes, and this week must work under both:
- **Branch A — Bet #1 placed** (§6 checklist passed GO): W134 is the first day at a founding/core-eng seat at a checklist-passing on-chain-derivatives venue. Relocation is *still* conditional — only if W132's flip-trigger fired (a warm-conversation cluster in one hub, or a presence-contingent offer premium). Default remains Vietnam-remote.
- **Branch B — bridge engaged** (no GO-bet yet): W134 is the first day on the **crypto-MM cash seat** (the income floor, underwritten by Half-2 latency craft), with Bet #1 outbound continuing in parallel. Relocation is almost certainly *not* triggered (a remote MM seat rarely demands presence).

Under both branches the four deliverables are identical: clean hand-off, rig-fate locked, exit referral-eligible, W134-ready. Only Saturday's relocation block changes shape (execute vs. collapse-to-remote-prep), gated on the flip-trigger.

---

**Monday — Day-job transition execution: hand-off + documentation**

- [ ] Artifact: `offboarding/handoff.md` (private, in current-employer repo or personal notes). Write the knowledge-transfer doc your successor/team needs: the systems you own, where each lives, the 3–5 non-obvious gotchas only you know (the "load-bearing tribal knowledge"), open threads with owner + next action, and credentials/access to be rotated or transferred. Cross-check against the employer's off-boarding policy for any mandated sections. (~90 min)
  - 🎯 Hardened acceptance: `offboarding/handoff.md` covers every system you own (a teammate could find and operate each from the doc alone), names ≥3 non-obvious gotchas, and lists every open thread with an explicit owner + next-action; zero items tagged `TODO` or `???` remain.
  - *Mental model*: the hand-off doc is the one artifact that converts "graceful departure" from an intention into a verifiable state — and in the v3 bet-path frame it is also a **node in the warm-network channel (§7)**. The colleagues who operate cleanly off your doc are exactly the people who give a warm intro when an on-chain-derivatives founder asks "do you know anyone who's built a matching/risk engine?" A clean hand-off is also the precondition for W134 arrival: you cannot ramp into the core engine you were brought in to build (Branch A) — or hit the ground at the MM seat (Branch B) — with a clear head while fielding "how does X work?" pings from the old job.
  - Output: `offboarding/handoff.md` exists meeting the hardened-acceptance criteria; a teammate (or a self-review with fresh eyes) confirms each owned system is operable from the doc alone.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Monday hand-off commit.

**Tuesday — Operations rig: fate decision (gate for the 4000-hours target)**

- [ ] Artifact: `ops/rig_fate_decision.md` (private). The rig accumulates the runtime hours that back bar-(c) substrate evidence (4000h by M36). Record the decision among three options with the gate reasoning written down, not assumed: **Option A** — ship the rig to the destination hub (capture logistics + cost + customs/token-hardware risk) — *only relevant if the §7 flip-trigger fired and you are actually relocating*; **Option B** — keep at home in Vietnam (requires the existing remote-management runbook to be sufficient for unattended operation) — *this is the default under v3, since Vietnam-remote is the default and the flip-trigger usually has NOT fired*; **Option C** — shut down, archive data, restart at destination/cloud on VMs (capture archive plan + cloud-cost estimate). Write the chosen option, the one-line reason, and the explicit consequence for the runtime-hours target at the top of the file. (~40 min)
  - 🎯 Hardened acceptance: `ops/rig_fate_decision.md` opens with `Decision: Option <A/B/C> — <one-line reason>` and `Runtime-hours impact: <reachable / at-risk / lost> — <why>`; all three options have their cost/risk captured (so the decision is a comparison, not a default); the file states whether the §7 flip-trigger fired (which is what makes Option A even a candidate); if Option B, the file links the remote-management runbook section that makes unattended operation safe.
  - *Mental model*: the rig exists to accumulate the 4000+ runtime hours by M36 (bar-(c) substrate evidence behind the venue) — a transition is the moment that target is most likely to silently die, because a powered-off rig in a packed-up apartment stops counting hours without anyone deciding it should. Writing the decision down (rather than letting the move decide by default) is what keeps the milestone honest. **v3 lean: Option B (keep at home in Vietnam)** is the default precisely because v3 makes Vietnam-remote the default and relocation conditional — under Branch B (bridge) or a non-triggered Branch A, you are not moving, so the rig stays put as a personal sandbox. Option A only enters the comparison if W132's flip-trigger actually fired. But the lean is not the decision — the file must show the comparison.
  - Output: `ops/rig_fate_decision.md` exists meeting the hardened-acceptance criteria; the runtime-hours impact line is filled; the flip-trigger status is stated.
- [ ] Commit notes (private). (~10 min)
  - Output: `git log -1 --oneline` shows the Tuesday rig-decision commit.

**Wednesday — Ramp-down + hand-off spillover buffer**

- [ ] Artifact: append to `offboarding/handoff.md` — close out any open thread or gotcha Monday surfaced after the fact (light buffer day by design; if nothing spilled, write `No open items — handoff complete as of <date>` and stop, do not pad). (~30 min)
  - 🎯 Hardened acceptance: every open thread from Monday's doc is now either resolved (with resolution noted) or explicitly reassigned to a named owner; the doc ends with a dated `handoff complete` line.
  - *Mental model*: a hand-off written in one sitting always leaks an item or two once you start actually executing the transition — Wednesday is the deliberate slack that catches them while you still have access, rather than discovering the gap from the destination timezone (or from the new bet's first sprint) after you've lost it. A leaked, unowned thread is the kind of thing that turns a referral-eligible exit into a "left us in the lurch" memory — the opposite of what §7's warm-network channel needs.
  - Output: `offboarding/handoff.md` ends with a dated `handoff complete` line; `git log -1 --oneline` shows the Wednesday commit.

**Thursday — Ramp-down + W134-readiness buffer (conditional-relocation OR remote-onboarding)**

- [ ] Artifact: `transition/checklist.md` (private) — start the W134-readiness checklist that Saturday will execute. **This checklist forks on the §7 flip-trigger status from W132:**
  - **If the flip-trigger fired (relocating):** enumerate the relocation paperwork list (passport/visa for the chosen hub — Dubai = crypto-native/Binance-dense, SG = institutional/Asia per §7; employment-or-bet engagement letter; **pre-TGE token tax-treatment paperwork** for the hub — the §7 cheap-prep item now coming due; lease/housing docs; customs forms incl. any rig-hardware declaration from Tuesday's Option A/C), shipping/packing tasks, and account/utility/address changes.
  - **If the flip-trigger did NOT fire (default — Vietnam-remote):** the checklist collapses to a **remote-onboarding-prep** list — bet/bridge engagement paperwork (offer/contract, token-grant or MM-seat terms, vesting/cliff dates to track per §6.5), remote work-setup verification (the rig per Tuesday's Option B, network, timezone-overlap plan with the new team), and the Binance-alumni / former-colleague contacts to log into the warm-network map (§7) now that the exit is clean.
  - List items now; tick them Saturday. Light buffer day by design — also absorbs any Tuesday rig-logistics spillover (booking shipping under Option A/C, archiving data). (~30 min)
  - 🎯 Hardened acceptance: `transition/checklist.md` opens with `Flip-trigger fired: <yes/no>` (sourced from W132) and then enumerates the correct branch's categories with at least the must-have items in each; under the relocating branch, the four categories (paperwork incl. visa + token-tax, packing, shipping, address/account changes) are present and any rig-logistics task implied by Tuesday's Option A/C appears here; under the remote branch, the three categories (engagement paperwork + vesting dates, remote-setup verification, warm-network logging) are present.
  - *Mental model*: transition failures are almost always a forgotten document or a missed deadline (a visa lead-time, a token-tax filing window, an unlogged warm contact), not a big-ticket miss — building the checklist Thursday (two days before execution) means Saturday is checking boxes, not discovering at the airport (or on day one of the bet) that something needed two weeks' lead time. The fork on the flip-trigger is what keeps v3 honest: most of the time you are NOT moving, and pretending otherwise wastes the buffer on relocation theater instead of remote-onboarding readiness.
  - Output: `transition/checklist.md` exists meeting the hardened-acceptance criteria; the flip-trigger status line is filled; `git log -1 --oneline` shows the Thursday commit.

**Friday — Last day at current employer: exit (referral-eligible)**

- [ ] Artifact: `offboarding/exit_log.md` (private). Conduct the exit interview with the gracious-departure goal: no bridges burned, the door left open. Log the conversation tone, any commitments made (e.g. "available for questions for 2 weeks"), and confirm the off-boarding checklist is complete (access revoked/transferred, equipment returned, final-pay/benefits clarified). Verify the Monday hand-off doc was actually handed to a named owner. (~40 min)
  - 🎯 Hardened acceptance: `offboarding/exit_log.md` records (a) the exit-interview outcome with tone noted, (b) every off-boarding-policy item ticked done with no `pending` left to you, (c) the named recipient of `offboarding/handoff.md`, and (d) any post-departure commitment with its expiry; the relationship is explicitly recorded as **referral-eligible** (logged into the §7 warm-network map with a one-line "who they could intro me to") — or, if not, the honest reason.
  - *Mental model*: the exit is the last impression and the seed of the next bet's network. In v3 the stakes are higher than a job reference: §7 structures the wealth outcome as **2–3 sequential shots where each bet upgrades the next via reputation + network**, and a clean, gracious exit converts a former manager or teammate into a future warm-intro node — possibly the one that sources Bet #2 from Vietnam without a relocation. A sloppy or bitter exit silently closes a door you'll want open. Verifying the hand-off reached a named owner is what makes "graceful departure" provable rather than aspirational, and closes the loop on Monday's artifact.
  - Output: `offboarding/exit_log.md` exists meeting the hardened-acceptance criteria; off-boarding checklist fully ticked; the relationship logged into the warm-network map; `git log -1 --oneline` shows the Friday exit commit.

**Saturday — W134-readiness: execute the checklist (relocation IF triggered, else remote-onboarding prep)**

- [ ] Artifact: tick through `transition/checklist.md` (the Thursday list), on the branch its `Flip-trigger fired:` line selected.
  - **Relocating branch:** execute packing tasks and do the final paperwork verification — every document physically present and in-date (visa, engagement letter, **token-tax paperwork**), every deadline-bound form submitted with lead time, address/account changes scheduled, rig-hardware customs declaration handled per Tuesday's decision. Mark each item done/blocked; for any blocked item write the unblock action + deadline.
  - **Remote branch (default):** execute the remote-onboarding verification — engagement paperwork signed and filed, vesting/cliff dates entered into the tracker (§6.5), remote work-setup confirmed operational (rig per Tuesday's Option B, network, timezone-overlap plan), and the warm-network contacts logged. Mark each item done/blocked the same way. (~40 min)
  - 🎯 Hardened acceptance: every item in `transition/checklist.md` is tagged `[done]` or `[blocked: <unblock action> by <date>]` — no item left untouched. Under the relocating branch, all paperwork in the "must-have for entry" category (visa, token-tax) is `[done]` and verified in-date, with zero blocked items in that category. Under the remote branch, all "must-have for day one" items (signed engagement, vesting dates tracked, rig operational) are `[done]`, with zero blocked items in that category.
  - *Mental model*: this is the execution of the W134-arrival precondition. Under the relocating branch a `[blocked]` must-have-for-entry item found on Saturday is recoverable; the same item discovered at the border is not. Under the (far more common) remote branch, the analogous failure is starting day one of the bet/bridge with unsigned terms or an untracked cliff date — equally avoidable with a binary have-it/don't check. Tagging rather than vaguely "getting ready" forces that binary check that catches the one missing item before it becomes a missed start.
  - Output: `transition/checklist.md` fully tagged with zero untouched and zero must-have-category blocked items (on whichever branch is active); `git log -1 --oneline` shows the Saturday prep commit.

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
