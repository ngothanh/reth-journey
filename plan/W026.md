# Week 26 — [NEW] `wal` crate v0.1 + reth storage architecture

> **Phase**: 3
> **Month**: M7
> **Track**: Reth (primary), Tempo (awareness)
> **Hours target**: 30h/wk (M1-M18)
> **Mirror target**: WAL-first durability (universal primitive — RocksDB WAL, Postgres XLOG, Aeron Archive, Chronicle Queue, TigerBeetle journal); `crossbeam-queue::SegQueue`; reth-db (read-side)
> **Crate(s) touched**: **[NEW]** `crates/wal/` v0.1.0; `concurrent` v? → +`SegQueue`
> **Bar**: (c)  ← `wal` and `concurrent` are both Layer-1 substrate (README §Bar policy). Two-track perf rules (SPEC §7) apply to every Build this week.
> **Feeds into**: storage-trie WAL integration (W27), recovery (W30), matching-engine order-log (W74), ledger commit journal (W80), consensus-raft replicated log (W64+), aeron archive (W83), mini-db (W95). `SegQueue` feeds group_commit (Tue), W30 mmap-queue, W37 skiplist (reclamation pattern).
> **References** (consult when stuck — not scheduled as cover-to-cover tasks): Gray & Reuter *Transaction Processing* §9 (WAL/ARIES); Mohan et al. *ARIES* (1992) §3-4 (LSN/prev_lsn/CLR); Vyukov bounded-MPMC writeup (per-segment seq); `crossbeam-queue::SegQueue` source (segment lifetime); Intel SDM §CRC32 (SSE4.2 `crc32`); TigerBeetle safety docs (disk-corruption stats); `reth-db/src/lib.rs` (table defs — Thu/Fri lookup).
> **v2 modifications**: none (W15-W28 unchanged from v1)

**[NEW] crate created**: `crates/wal/`. The first universal durability primitive. Will be inherited by storage-trie (
W27 WAL integration), ledger (W80), matching-engine (W74), consensus-raft (W64+ for snapshot logs), and mini-db (W95).

> **Load note**: Tuesday pairs the deepest concurrency primitive of the week (`SegQueue`, lock-free unbounded
> MPMC, ~200 min) with `group_commit`'s two impl phases (~155 min) that *consume* it — the two impls must land
> together so group-commit can fan-in lock-free. Two no-code blocks were pulled off Tuesday to honour the
> ~6h G8 cap: (1) the standalone SegQueue-vs-Mutex fan-in bench → Friday (the week's lightest day); (2)
> `group_commit`'s Phase-1 design+prediction (~45 min, no code, depends only on SegQueue's *interface* not its
> impl) → end of **Monday** as a design-ahead block. That lands Tuesday at ~365 min (seg_queue 200 +
> group_commit Phase 2/3 155 + commit 10) ≈ **6.1h** — in band. Coverage is preserved, not trimmed (SPEC §2).
> Mon (~5.9h after absorbing the group_commit design block), Wed (~5.3h), Thu (~4.5h after the reth-db read),
> Sat (~5h) are all within cap.

**Monday — `wal` crate scaffold + segment layout**

- [ ] **Build**: `crates/wal/Cargo.toml` workspace member (Layer-1 durability primitive, bar (c)). Mirror of
  RocksDB WAL / Postgres XLOG / Aeron Archive / TigerBeetle journal. Deps: `time`, `bufpool`,
  `eth-storage-cache` (Page), `concurrent` (SegQueue, added Tue); optional `crc32fast` for the software CRC
  fallback. Will hold `segment.rs`, `record.rs`, `group_commit.rs`, `checksum.rs`, `replay.rs`, `lib.rs`.
  Downstream consumers: W27 storage-trie, W30 recovery, W64 raft, W74 matching, W80 ledger, W95 mini-db. (~20 min)
  - Output: `cargo build -p wal` succeeds (empty lib); `cargo metadata` lists `wal` as a workspace member with
    the deps above. No 3-phase arc — nothing to test until segment.rs lands.
- [ ] **Build**: `crates/wal/src/segment.rs` — `Segment` is an mmap-backed file with header
  `{ magic, version, segment_id, first_lsn }`, body of length-prefixed records, footer `{ last_lsn, checksum }`. Default
  segment size 64 MiB. Pre-allocated. (~total 140 min across 3 phases)
  - 🔧 **Why this matters**: segment files are the *durability granule*. Pre-allocated via `fallocate` so writes
    never extend the file (avoiding metadata fsync). Bounded size enables truncation (rotate old segments out
    of disk). Mmap lets the kernel coalesce dirty pages; explicit msync gives commit barriers. This layout is
    canon in WAL design — RocksDB, Aeron Archive, Chronicle Queue, Postgres all use the same shape.
    Downstream: Tuesday group-commit consumes `Segment::append`; Wednesday replay consumes `scan`; W27
    storage-trie WAL integration; W29 recovery; W64 raft replicated log; W80 ledger.

  #### Phase 1 — Problem, prediction, design (~40 min, no code yet)

  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/wal/notes/26_segment_prediction.md`:
    - P1. The primitive you'd reach for to make append multi-producer-safe — exact import line
      (`use std::sync::atomic::{AtomicUsize, Ordering}` vs `use std::sync::Mutex` vs CAS-loop). Commit to one.
    - P2. Predicted single-thread append latency for a 32-B record via mmap (no msync) — your number in ns.
      (Reality after bench.)
    - P3. Predicted outcome when 16 producers all `fetch_add` the same offset concurrently — do ranges
      overlap or stay disjoint? State the invariant you're betting on.
    - P4. Predicted scan behaviour at the partial-tail: does your first impl stop at the torn record, or read
      past it into garbage? Predict which Phase-2 test catches the wrong answer.
    - P5. **List every Phase-2 test below you predict your first attempt will FAIL**, and why. Compare to
      reality after the tests run.

  - *Mental model*: a `Segment` is the durability granule — one pre-allocated, mmap-backed, append-only file.
    Pre-allocation (`fallocate`) means a write never extends the file, so no metadata fsync ever fires on the
    hot path. The atomic `write_offset` is the *only* shared mutable state; everything else is per-producer.

  - *Contract* (signature-level, NEVER the body):
    ```rust
    pub struct Segment { /* path, file, mmap: MmapMut, header, write_offset: AtomicUsize, ... */ }
    impl Segment {
        pub fn create(path: &Path, id: u64, first_lsn: Lsn, size: usize) -> io::Result<Self>;
        pub fn open(path: &Path) -> Result<Self, SegmentError>; // validates magic/version
        pub fn append(&self, rec: &[u8]) -> Option<Lsn>;        // None == doesn't fit, caller rotates
        pub fn scan(&self) -> SegmentIter<'_>;                  // stops at zero-len OR CRC mismatch
        pub fn state(&self) -> SegmentScanState;               // CleanEnd | TailTruncation { at }
    }
    ```

  - *Back-of-envelope* (bar (c)): NVMe random fsync ≈ 50–1000 µs (use 500 µs) → naive fsync-per-record ceils
    at ~2k rec/s. Target ≥1M rec/s single-thread = ≤1 µs/append. Budget for a 32-B append: `fetch_add` ≈ 5 ns
    (uncontended) → 25 ns (16-way contended, one CAS-class RMW), `copy_from_slice(32B)` ≈ 5 ns (L1), CRC of
    32 B ≈ 3 ns hw. Total ≈ 35 ns of work; the µs budget is 28× headroom — *provided* msync is NOT on this
    path. Cliff ratio: msync-per-record (≈2.5 µs) vs amortized (≈0 ns/record) = the entire throughput
    difference. fallocate of 64 MiB ≈ 50–200 ms — off the hot path via a 2-deep ready-pool (D1).

  - **Requirements** — write in `crates/wal/notes/26_segment_design.md`:
    - R1. `Segment` holds `path`, `file`, `mmap: MmapMut`, `header: SegmentHeader`, `write_offset:
      AtomicUsize`, `first_lsn: Lsn`, `last_durable_lsn: AtomicU64`.
    - R2. `SegmentHeader` is `#[repr(C)]` with `magic: [u8;8]` (`b"WALSEG01"`), `version: u32`, `flags:
      u32`, `segment_id: u64`, `first_lsn: u64`, `body_offset: u32 (=64)`, `body_len: u32`, padding.
    - R3. Segment file size == declared size at all times after creation (pre-allocated via fallocate).
    - R4. `write_offset` only advances; never rewinds. Atomic `fetch_add` is the reservation primitive.
    - R5. On open, magic byte sequence must match; if not, reject (junk/wrong-version).
    - R6. Body region initialized to zeros at fallocate; a zero `len` field marks EOF/tail.
    - R7. `append(record) -> Option<Lsn>` returns `None` if not enough space (caller rotates), else
      writes frame header + body + advances offset.
    - R8. `scan() -> SegmentIter` stops at zero-len frame OR checksum mismatch — both signal tail.
    - R9. Throughput: ≥1M records/sec single-thread (32-B payload); ≥5M/sec aggregate at 16 producers.
    - R10. Crash-recovery: 1000 random SIGKILL fault injections; reopen + scan returns all records up to
      last complete CRC-valid frame.

  - **Design walk-through** — answer each (no peeking at `crossbeam`/RocksDB source until Phase 3):
    - D1. fallocate cost: ~50-200ms per 64MiB file on slow disks. Where does this cost go on the hot
      path? (Hint: 2-deep ready-pool — pre-allocate the NEXT segment in the background.)
    - D2. Multi-producer offset claim: `fetch_add(len + frame_overhead)` returns the reserved start.
      What about producers that crash after reserving but before writing? (Hint: CRC + zero-init body
      → scanner treats CRC mismatch on non-zero-len frame as torn-write tail.)
    - D3. Property table — enumerate every offset-claim primitive: `Mutex<usize>` (Sync✓, ~25 ns lock,
      0 alloc, blocking✓), `CAS-loop on AtomicUsize` (Sync✓, ~5–50 ns under contention, 0 alloc,
      lock-free but *retries*), `fetch_add` (Sync✓, ~5–25 ns, 0 alloc, wait-free, single RMW). Which gives
      disjoint reservations *by construction*?
    - **D4. Pre-mortem (load-bearing):** write the EXACT 4-5 line bad interleaving for a CAS-loop claim:
      `T1 load off=0 → T2 load off=0 → T1 CAS 0→64 ok, writes mmap[0..64] → T2 CAS 0→64 FAILS, retries
      load off=64 → ` … now show the variant where the loop body computed the slice *before* the CAS and
      both wrote `mmap[0..64]`. If you can't write the overlap, you can't prove `fetch_add` prevents it.
    - **D5. Anti-shortcut audit:** (a) "just `Mutex<usize>` for the offset" — correct but serializes 16
      producers behind one ~25 ns lock → throughput cliff, fails the bar. (b) "`compare_exchange` loop" —
      correct only if the slice write happens AFTER the successful CAS; the obvious write-then-CAS overlaps
      (D4). (c) "`File::write_all` + per-record `fsync`" — correct durability, 500 µs/record, 2k rec/s
      ceiling = bar (c) violation. (d) "read back via `read(2)` after mmap write" — coherence hazard on some
      kernels; pick ONE access path (mmap for both).

  - 🧮 **Paper drill** — write in `crates/wal/notes/26_segment_drill.md`:
    - D-LAYOUT on a 4 KiB `Segment` instance: draw the byte map — header at `[0..64)` (`magic[0..8)`,
      `version[8..12)`, `flags[12..16)`, `segment_id[16..24)`, `first_lsn[24..32)`, `body_offset[32..36)=64`,
      `body_len[36..40)`, padding `[40..64)`), then body `[64..4032)`, footer `{last_lsn, checksum}` at the
      tail. Place three 32-B frames at `body_offset` 64, 112, 160 (each = 16-B frame header + 32-B payload) and
      mark exactly where the `>`-vs-`>=` bounds check refuses the frame that would spill past `body_len`.
    - D-RMW on the `write_offset` reservation: 2-thread step table, columns T1 / T2 / `write_offset` value.
      Both reserve a 48-B frame at offset 64. Show row-by-row that `fetch_add(48)` hands T1 `[64..112)` and T2
      `[112..160)` (disjoint by construction); then the CAS-loop variant where both `load 64`, both compute
      `mmap[64..112)`, both write → overlap. Annotate which guarantee carries it: atomicity (RMW reads-latest),
      not ordering.
    - D-UNSAFE on the mmap write + the open path: 3-column ledger (unsafe op | precondition | runtime fact).
      Rows: (1) `mmap[start..start+frame] = bytes` — precondition `start+frame <= body_len` — guaranteed by the
      D-RMW bounds check; UB if the check used the start only (SIGBUS on the unmapped page past EOF). (2)
      `transmute header bytes → SegmentHeader` on open — precondition `magic == b"WALSEG01"` && `version known`
      — guaranteed by R5 validation; UB on junk/short file. Flag the row whose guarantee is "the caller
      rotated before overflow" (name: `append`'s `None` return path).
  - **Output**: `notes/26_segment_design.md` with R1–R10 + D1–D5 + frame layout diagram + the bad-interleaving sketch; plus `notes/26_segment_drill.md` with the three worked drills.

  #### Phase 2 — Write the failing tests (~30 min)

  Each test is engineered so at least one naive impl FAILS it. Tier in parens. 🪤 marks the three required
  trap shapes.

  - [ ] **(NEWBIE)** `segment_create_then_open_round_trip` — create segment, write header, close, open,
    validate header bytes match. Exercises R2, R5.
  - [ ] **(NEWBIE)** `append_1m_small_records_scan_in_order` — single-thread append 1M 32-byte records;
    close; reopen; scan returns 1M records in append order. Exercises R7, R8.
  - [ ] **(SENIOR)** 🪤 **anti-shortcut** `append_until_full_returns_none` — the `>=`-vs-`>` boundary trap.
    ```rust
    #[test]
    fn append_until_full_returns_none() {
        let seg = Segment::create(tmp(), 0, Lsn(0), 4096).unwrap();
        let mut n = 0;
        while seg.append(&[0u8; 32]).is_some() { n += 1; }
        // The FIRST refusal must come BEFORE any byte spills past body_len.
        assert!(seg.write_offset() <= seg.body_len(),
            "offset {} spilled past body_len {} — bounds check used start, not start+frame",
            seg.write_offset(), seg.body_len());
        assert_eq!(seg.append(&[0u8; 32]), None, "must keep refusing once full");
    }
    ```
  - [ ] **(EXPERT)** 🪤 **anti-shortcut** `concurrent_append_16_producers_no_loss` — 16 threads × 100k
    records; the naive CAS-loop-with-early-write overlaps. Engineered to bite a CAS impl, pass a `fetch_add` impl.
    ```rust
    #[test]
    fn concurrent_append_16_producers_no_loss() {
        let seg = Arc::new(Segment::create(tmp(), 0, Lsn(0), 256 * 1024 * 1024).unwrap());
        std::thread::scope(|s| {
            for t in 0..16 {
                let seg = seg.clone();
                s.spawn(move || for i in 0..100_000u32 {
                    seg.append(&[(t as u8); 32]).expect("space"); let _ = i;
                });
            }
        });
        let recs: Vec<_> = seg.scan().collect();
        assert_eq!(recs.len(), 1_600_000, "lost records — claim ranges overlapped (CAS write-before-swap)");
        // No two records share a byte range: reservations were disjoint.
        let mut spans: Vec<_> = recs.iter().map(|r| (r.start, r.start + r.frame_len)).collect();
        spans.sort();
        for w in spans.windows(2) { assert!(w[0].1 <= w[1].0, "overlap at {:?}/{:?}", w[0], w[1]); }
    }
    ```
  - [ ] **(SENIOR)** `crash_injection_tail_truncation` — write 10k records; SIGKILL at a random byte;
    reopen; scan stops at the last CRC-valid frame, never reads garbage. Exercises R10.
  - [ ] **(NEWBIE)** `wrong_magic_rejects_with_error` — file with `magic = b"WRONGGGG"`; `Segment::open`
    returns `Err(InvalidMagic)`, no panic. Exercises R5.
  - [ ] **(EXPERT)** 🪤 **anti-shortcut (bar (c))** `bench_single_thread_1m_per_sec` — criterion bench;
    assert ≥1M rec/s on a 32-B payload. Fails any impl that msyncs per append. Exercises R9.
  - [ ] **(EXPERT)** `bench_16_producer_5m_per_sec_aggregate` — multi-thread bench; aggregate ≥5M/sec.
    Exercises R9.
  - [ ] **(SENIOR)** 🪤 **negative-control (test-the-test)** `bench_lands_in_plausible_band` — assert the
    single-thread bench result is inside a physically-possible window, so a bench that measures *nothing*
    (e.g. the loop was optimized away, or it accidentally fsyncs and reports 5 rec/s) fails loudly instead
    of silently "passing".
    ```rust
    #[test]
    fn bench_lands_in_plausible_band() {
        let ops_per_sec = run_append_microbench(); // same code path as the criterion bench
        assert!(ops_per_sec > 1.0e6 && ops_per_sec < 5.0e8,
            "throughput {ops_per_sec:.2e} ops/s outside [1e6, 5e8] — bench measures nothing \
             (loop elided, or msync snuck onto the path)");
    }
    ```

  #### Phase 3 — Make the tests pass, one failure at a time (~70 min)

  - **(EXPERT) When `concurrent_append_16_producers_no_loss` reports overlapping writes:**
    - Symptom: two threads' records partially overwrite each other. Final scan shows 1.55M records
      instead of 1.6M with garbled bytes at the overlap zone; the span-overlap assertion fires.
    - Why: `write_offset` used `compare_exchange` in a loop; under high contention, two threads observe
      the same offset, both compute their frame, both call `mmap[start..end].copy_from_slice(...)`.
      The CAS only protects the offset bump, not the write region.
    - Fix: use `fetch_add` as a reservation — `let start = write_offset.fetch_add(frame_size, Acquire);`
      atomically reserves a non-overlapping range. Then write into `mmap[start..start+frame_size]`
      without further synchronization (each producer's range is disjoint).
    - Cost avoided: silent data loss + corruption (a correctness bug, not slow); also removes the
      CAS-retry storm (~50 ns/retry × N spins under 16-way contention vs one ~25 ns `fetch_add`). (bar (c) violation)
    - Muscle: lock-free append-only logs use `fetch_add` for reservation, not CAS-loop; reservation
      ranges are disjoint by construction. Reapplies at: W30 mmap-queue cycle.rs claim, W31 mmap-queue
      term buffer, W77 aeron Publication.

  - **(SENIOR) When `crash_injection_tail_truncation` reads past the partial record into garbage:**
    - Symptom: scan returns the 10k valid records PLUS some garbage records (CRC errors logged but
      iteration continued).
    - Why: scan continued past the first CRC-mismatch frame instead of stopping. The "tail
      truncation" rule (R8) wasn't implemented.
    - Fix: in `SegmentIter::next`, on CRC mismatch, return `None` (end iteration). Distinguish in a
      `state()` accessor: `CleanEnd | TailTruncation { at: usize }`. Caller decides whether truncation
      is normal (crash recovery) or fatal (mid-segment corruption).
    - Muscle: an append-only scanner stops at the first integrity violation; never "skips and
      continues" because skipping admits an attacker who corrupts mid-segment to swallow data.
      Reapplies at: W26 replay.rs, W30 ARIES analysis, W30 mmap-queue ExcerptCursor.

  - **(EXPERT, perf) When `bench_single_thread_1m_per_sec` reports 300k/sec (bar (c) violation):**
    - Symptom: append latency is ~3.3 µs (300k/sec); target was ≤1µs (≥1M/sec). 3× too slow.
    - Why: `msync(MS_SYNC)` was called after every append; group commit not yet integrated. Each msync
      is ~2-3µs even on NVMe.
    - Fix: append does NOT msync. msync is the group-commit thread's job (Tuesday's
      `GroupCommit::flush_loop`). The segment's `last_durable_lsn` advances only when group-commit
      msyncs. For now, write a separate `flush_test` bench that confirms throughput when msync is
      separated.
    - Cost avoided: ~2.5 µs per append (a syscall on the inner loop); at 1M ops/sec, 2.5 sec/sec of CPU
      reclaimed. This is the canonical bar (c) violation — a syscall in the hot path.
    - Muscle: durability is amortized across many records (group commit); never per-record fsync on
      the append hot path. Reapplies at: W26 group_commit.rs, W80 ledger journal.

  - **(SENIOR) When `append_until_full_returns_none` returns `Some(lsn)` on the overflow attempt:**
    - Symptom: after the segment is exactly full, one more `append` returns `Some(lsn)` but the
      record's bytes spill past `body_len` into footer / out-of-bounds territory. SIGBUS on
      subsequent reads.
    - Why: the bounds check used `>=` instead of `>`. The reservation succeeded because `write_offset
      <= body_len` was true; then the write spilled.
    - Fix: bounds check is `if reserved_start + frame_size > body_len { return None; }`. Decrement
      `write_offset` back (or use CAS with a guard); rotation is the caller's responsibility.
    - Muscle: bounds-check the FULL reservation (start + size) against the limit, not just the start;
      then atomically refuse if it doesn't fit. Reapplies at: W30 mmap-queue claim, W31 term-buffer.

  - **5-year failure mode** (write in `notes/26_segment_followup.md` after green): when persistent
    memory (PMem/CXL.mem) becomes common, the mmap-then-msync model collapses — PMem is byte-addressable
    and durable, no msync needed. State the migration: a `SegmentBackend` trait abstracts the
    durability mechanism; `MmapNvmeBackend` and `PmemBackend` impls. Trigger: PMem capacity > 10× DRAM
    available at server-class price points.
- [ ] **Build**: `crates/wal/src/record.rs` — `WalRecord { lsn: u64, kind: RecordKind, payload: Bytes }`. RecordKind
  enum (Insert, Update, Delete, Begin, Commit, Abort, Checkpoint). (~total 140 min across 3 phases)
  - 🔧 **Why this matters**: the record is the *unit of durability*. Its fields are dictated by the recovery
    algorithm — ARIES needs `lsn`, `prev_lsn`, `tx_id`, `kind`, `payload`, `undo_next_lsn` for CLRs. Get this
    struct wrong and W29 recovery is unbuildable. The discriminated union of RecordKind is the only place
    where future record types can be added without breaking older readers (forward compat via reserved kind
    bits). Downstream: every wal user; W29 ARIES analysis walks records via this schema; W64 raft AppendEntries;
    W80 ledger; W83 aeron archive.

  #### Phase 1 — Problem, prediction, design (~35 min, no code yet)

  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/wal/notes/26_record_prediction.md`:
    - P1. How will you represent "no transaction"? Exact type (`tx_id: u64` with 0 sentinel vs
      `Option<TxId>` + presence flag). Commit before reading D1.
    - P2. Predicted single-thread encode latency for a 64-B-payload record — your number in ns.
    - P3. Predicted asm shape of `match record.kind` over 10 sparse `#[repr(u8)]` discriminants — jump
      table, or `cmp` chain? State which Phase-2 test verifies it.
    - P4. Derive-correctness: if you `#[derive(...)]` instead of hand-writing the wire codec, which test
      breaks? Predict it.
    - P5. **List every Phase-2 test you predict your first attempt will FAIL**, and why.

  - *Mental model*: a `WalRecord` is the unit of durability and the unit of *forward compatibility*. Its
    fields are dictated by the recovery algorithm (ARIES: `lsn`, `prev_lsn`, `tx_id`, `kind`, `undo_next_lsn`).
    The wire format must let an old reader skip a record it doesn't fully understand — at *flag* granularity —
    while still erroring on an unknown *kind* (which changes semantics).

  - *Contract*:
    ```rust
    pub struct WalRecord { pub lsn: Lsn, pub kind: RecordKind, pub tx_id: Option<TxId>,
        pub prev_lsn: Option<Lsn>, pub undo_next_lsn: Option<Lsn>, pub payload: Bytes }
    impl WalRecord {
        pub fn encode(&self, out: &mut impl bytes::BufMut);          // caller-supplied buffer, no alloc
        pub fn decode(buf: &[u8]) -> Result<(WalRecord, usize), RecordError>; // (record, bytes_consumed)
    }
    #[repr(u8)] pub enum RecordKind { Begin=0x01, Commit=0x02, /* ... */ Checkpoint=0x20, CLR=0x21 }
    ```

  - *Back-of-envelope* (bar (c)): target encode ≥10M/s (≤100 ns), decode ≥5M/s (≤200 ns). A per-call
    `BytesMut::with_capacity` ≈ 50 ns malloc + free ≈ 150 ns round-trip → at 10M/s that alloc *alone* is
    1.5 s/s of CPU; the caller-supplied-buffer contract drives it to 0. Enum dispatch: a dense jump table is
    ~1 predicted branch (≈0.5 ns); a `cmp` chain of 10 averages ~5 comparisons + a mispredict ≈ 5 ns. Cliff
    ratio at recovery startup (cold caches, 5M records): chain costs ~25 ms/s extra vs jump table.

  - **Requirements** — write in `crates/wal/notes/26_record_design.md`:
    - R1. `WalRecord { lsn, kind, tx_id: Option<TxId>, prev_lsn: Option<Lsn>, undo_next_lsn: Option<Lsn>,
      payload: Bytes }`.
    - R2. `RecordKind` is `#[repr(u8)]` with explicit discriminants: Begin=0x01, Commit=0x02, Abort=0x03,
      Insert=0x10, Update=0x11, Delete=0x12, Checkpoint=0x20, CLR=0x21, EndCheckpoint=0x22.
    - R3. `lsn` is strictly monotonic across all records in a wal.
    - R4. For a given `tx_id`, the chain `current.prev_lsn → prev.lsn` walks all records of that tx in
      reverse. First record of a tx has `prev_lsn = None`.
    - R5. CLRs always have `undo_next_lsn = Some(_)`; non-CLRs have it as `None`.
    - R6. Wire format is FIXED-SIZE little-endian: lsn(8) + kind(1) + flags(1) + tx_id(8|0) +
      prev_lsn(8|0) + undo_next(8|0) + payload_len(4) + payload. `flags` bitmap encodes which optional
      fields are present.
    - R7. Decoder rejects unknown kinds with `RecordError::UnknownKind` BUT a record with a future
      `flags` bit set passes through (forward compat at flag granularity).
    - R8. `payload: Bytes` (Arc-backed) — never `Vec<u8>` — so callers pass slices without allocating.
    - R9. Encode ≥10M rec/s, decode ≥5M rec/s single-thread.
    - R10. Round-trip: encode → decode → encode is byte-identical for any `WalRecord` value.

  - **Design walk-through** — answer each:
    - D1. Why `Option<TxId>` not `TxId = 0` for "no tx"? (Hint: tx_id 0 would collide with a real
      Checkpoint or system record.)
    - D2. Forward-compat strategy: a flag bit per optional field means adding a new field tomorrow
      doesn't break today's decoder. Sketch the flags layout.
    - D3. Varint vs fixed-size: varint saves bytes for small LSNs but costs ~30% decode throughput.
      Which does wal pick? (Hint: fixed.)
    - **D4. Pre-mortem (load-bearing):** write the EXACT trace where `tx_id: None` and `tx_id: Some(0)`
      become indistinguishable: `encode None → write 0u64 in tx_id slot → decode reads 8 bytes → returns
      Some(0)`. Now a Checkpoint record (system, tx_id=None) reads as belonging to tx 0 → ARIES analysis
      mis-attributes it. State the flag-bit fix.
    - **D5. Anti-shortcut audit:** (a) "use `tx_id: u64 = 0` for none" — sentinel collides with real
      tx 0 (D4). (b) "`#[derive(Serialize)]` / bincode for the wire format" — couples the on-disk format to
      a library version, varint-encodes (≈30% slower decode), and re-orders fields on struct edits → old
      segments unreadable; hand-write the fixed-LE codec. (c) "`payload: Vec<u8>`" — forces a copy on every
      construct; use `Bytes` (Arc-backed, shareable). (d) "validate `flags & !KNOWN == 0`" — rejects future
      flag bits, killing forward compat; read only known bits.
    - D6. Enum dispatch on hot path: `match record.kind` over 10 sparse variants — jump table or `cmp`
      chain? Verify with `cargo asm`. (Hint: sparse discriminants 0x03→0x10 defeat the dense table.)
    - D7: name the scenario where forgetting `#[repr(u8)]` causes old segments to become unreadable after a
      toolchain bump.

  - 🧮 **Paper drill** — write in `crates/wal/notes/26_record_drill.md`:
    - D-WALK on encoding ONE concrete CLR record `{ lsn: 0x0102, kind: CLR(0x21), tx_id: Some(7),
      prev_lsn: Some(0x00FF), undo_next_lsn: Some(0x00AA), payload: b"xy" }`: hand-encode the wire bytes
      field by field in fixed LE — `lsn(8)=02 01 00 00 00 00 00 00`, `kind(1)=21`, compute `flags(1)` (bits:
      tx_id-present | prev_lsn-present | undo_next-present → `0b0000_0111 = 07`), then `tx_id(8)`, `prev_lsn(8)`,
      `undo_next(8)`, `payload_len(4)=02 00 00 00`, `payload="xy"`. Then DECODE your own bytes back and assert
      byte-identical (R10). Now flip `flags` bit 0 off and re-decode: `tx_id` must come back `None` (skips 8
      bytes), proving the presence-flag distinguishes `None` from `Some(0)`.
    - D-LAYOUT on that same wire frame: draw the byte map with each field's offset + width; mark `FLAGS_OFFSET`
      (the byte the forward-compat test sets `0x80` on) and `payload_len`'s offset. Note: fixed-LE has NO
      padding by construction (contrast the in-memory `repr(u8)` struct), which is why the codec is
      reproducible run-to-run — the property the CRC-over-wire drill (Wed) depends on.
  - **Output**: `notes/26_record_design.md` with R1–R10 + D1–D7 + the wire-format byte diagram + the bad-trace sketch; plus `notes/26_record_drill.md` with the encode/decode walk + byte map.

  #### Phase 2 — Write the failing tests (~25 min)

  - [ ] **(NEWBIE)** `record_round_trip_1m_byte_identical` — 1M random records; encode → decode → encode
    byte-identical for each. Exercises R10.
  - [ ] **(SENIOR)** 🪤 **anti-shortcut** `tx_id_none_vs_zero_distinguished` — the sentinel-collision trap.
    ```rust
    #[test]
    fn tx_id_none_vs_zero_distinguished() {
        let mut buf = Vec::new();
        WalRecord { tx_id: None, ..ckpt() }.encode(&mut buf);
        let (r, _) = WalRecord::decode(&buf).unwrap();
        assert_eq!(r.tx_id, None, "None decoded as Some(0): used a 0 sentinel, not a presence flag");
        let mut buf0 = Vec::new();
        WalRecord { tx_id: Some(TxId(0)), ..ckpt() }.encode(&mut buf0);
        let (r0, _) = WalRecord::decode(&buf0).unwrap();
        assert_eq!(r0.tx_id, Some(TxId(0)), "real tx 0 must survive — flag distinguishes it from absent");
    }
    ```
  - [ ] **(NEWBIE)** `unknown_kind_returns_typed_error` — frame with `kind = 0xFE`; decoder returns
    `Err(UnknownKind(0xFE))`. Exercises R7.
  - [ ] **(SENIOR)** `truncated_record_returns_typed_error_no_panic` — frame missing last 5 bytes; decoder
    returns `Err(Truncated)`, never panics (no slice-index OOB). Exercises R7.
  - [ ] **(NEWBIE)** `aries_chain_walks_in_reverse` — write 100 records of one tx; walk `prev_lsn` chain;
    yields all 100 in reverse order. Exercises R4.
  - [ ] **(SENIOR)** `clr_has_undo_next_lsn_set` — encode a CLR; `undo_next_lsn` round-trips as Some(_);
    non-CLRs round-trip with None. Exercises R5.
  - [ ] **(SENIOR)** 🪤 **anti-shortcut** `forward_compat_unknown_flag_bit_decodes` — set a reserved flags
    bit; decoder strips the unknown flag but accepts the record (kind known). Bites a `flags & !KNOWN == 0`
    validator. Exercises R7.
    ```rust
    #[test]
    fn forward_compat_unknown_flag_bit_decodes() {
        let mut buf = Vec::new();
        WalRecord { kind: RecordKind::Commit, ..base() }.encode(&mut buf);
        buf[FLAGS_OFFSET] |= 0x80; // a flag bit this version never allocated
        let (r, _) = WalRecord::decode(&buf).expect("future flag bit must NOT error (flag-level fwd compat)");
        assert_eq!(r.kind, RecordKind::Commit);
    }
    ```
  - [ ] **(EXPERT, perf bar (c))** `bench_encode_10m_per_sec` — criterion; ≥10M encode/sec single-thread on
    64-B payload. Fails any per-call-alloc encoder. R9.
  - [ ] **(EXPERT)** `bench_decode_5m_per_sec` — criterion; ≥5M decode/sec single-thread. R9.
  - [ ] **(EXPERT)** 🪤 **anti-shortcut (bar (c))** `verify_jump_table_emitted_on_kind_dispatch` — `cargo
    asm` on the decode fn; assert the kind dispatch is a jump table, not a `cmp` chain. Sparse discriminants
    bite. Exercises D6.
    ```rust
    #[test]
    fn verify_jump_table_emitted_on_kind_dispatch() {
        let asm = cargo_asm("wal::record::decode_kind"); // helper wrapping `cargo asm`
        let cmp_count = asm.matches("\tcmp").count();
        assert!(cmp_count <= 2,
            "kind dispatch emitted {cmp_count} cmp instrs — a chain, not a jump table; \
             renumber discriminants densely or use a fn-ptr table");
        assert!(asm.contains("jmp") && asm.contains("[") /* indexed jump */,
            "no indexed jump found — dispatch is not a jump table");
    }
    ```
  - [ ] **(SENIOR)** 🪤 **negative-control (test-the-test)** `asm_probe_actually_loads_asm` — proves the
    `cargo asm` harness above isn't asserting over an empty string (which would make the jump-table test
    vacuously pass): `assert!(cargo_asm("wal::record::decode_kind").len() > 200, "asm probe returned <200
    chars — it captured nothing, every asm-shape test is a no-op");`.

  #### Phase 3 — Make the tests pass, one failure at a time (~80 min)

  - **(SENIOR) When `tx_id_none_vs_zero_distinguished` reports `Some(0)` instead of `None`:**
    - Symptom: encoding `WalRecord { tx_id: None, ... }` and decoding yields `tx_id: Some(0)`.
    - Why: encoder wrote `0u64` for the tx_id slot regardless of None vs Some; decoder reads 8 bytes
      always. No flag bit distinguishes "absent" from "present-but-zero".
    - Fix: `flags: u8` bitmap, bit 0 = "tx_id present"; if bit clear, encoder skips the 8 bytes and
      decoder reads 0 bytes for tx_id, returning None. Same scheme for prev_lsn and undo_next_lsn.
    - Muscle: optional fields in fixed-layout wire formats require a presence flag, never a sentinel
      value — sentinels collide. Reapplies at: W21 RLP `#[rlp(trailing)]`, W67 raft AppendEntries
      versioned fields.

  - **(EXPERT, perf) When `verify_jump_table_emitted_on_kind_dispatch` reports a 10-instruction `cmp` chain (bar (c) violation):**
    - Symptom: `cargo asm` shows `cmp 0x01; je L_begin; cmp 0x02; je L_commit; ...` — chain of
      comparisons.
    - Why: discriminants are sparse (0x01, 0x02, 0x03, 0x10, 0x11, ..., 0x22) — the compiler can't
      emit a dense jump table over a 256-entry array; the gap from 0x03 to 0x10 is too large.
    - Fix: TWO options — (a) renumber discriminants densely (0x01..0x09) so the compiler emits a jump
      table; (b) keep sparse discriminants but write an explicit `static DISPATCH: [fn(&[u8]) -> Result<_>;
      256]` lookup. (a) is simpler; (b) preserves the human-readable groupings.
    - Cost avoided: ~5 ns/decode in branch-mispredict; at 5M decode/sec, 25ms/sec CPU reclaimed; matters
      most at recovery startup (cold caches).
    - Muscle: enum dispatch on a hot path benefits from frequency-ordered + dense discriminants OR
      explicit function-pointer dispatch tables; verify in asm. Reapplies at: W42 txn operation
      dispatch, W74 matching-engine order-type match, W77 aeron frame-type dispatch.

  - **(SENIOR) When `forward_compat_unknown_flag_bit_decodes` fails with `Err(UnknownFlag)`:**
    - Symptom: setting `flags |= 0x80` (an unallocated bit) makes the decoder error.
    - Why: decoder validates that `flags & !KNOWN_FLAGS == 0`; future flag bits trip this.
    - Fix: decoder reads ONLY the known flag bits; ignores higher bits. The forward-compat contract:
      old code skips unknown flag bits silently, but must NOT skip unknown KIND bits (those alter
      record semantics).
    - Muscle: forward compat at flag granularity = ignore unknown flag bits; at kind granularity =
      error on unknown kinds. The two ARE different. Reapplies at: W21 RLP `#[rlp(trailing)]` (extra
      fields skipped, missing mandatory errors), W67 raft compatibility.

  - **(EXPERT, perf) When `bench_encode_10m_per_sec` reports 4M/sec (bar (c) violation):**
    - Symptom: encode latency 250ns/op; target ≤100ns. 2.5× too slow.
    - Why: each encode allocates a fresh `BytesMut::with_capacity(...)` — allocator overhead dominates.
    - Fix: encoder takes `&mut impl BufMut` from the caller; callers pool. Re-bench.
    - Cost avoided: ~150ns/op alloc (malloc+free in the hot path); at 10M ops/sec, 1.5sec/sec of CPU reclaimed.
    - Muscle: hot encoders always accept a caller-supplied buffer; allocating per-call defeats throughput.
      Reapplies at: W20 branch_compact.rs, W21 RLP encoders, W31 mmap-queue wire.

  - **5-year failure mode** (write in `notes/26_record_followup.md` after green): when ARIES-style
    physical undo gives way to MVCC-style multi-version log (Postgres-style), the `prev_lsn` /
    `undo_next_lsn` fields become useless and the chain shape changes. Migration: add a `RecordSchema`
    version byte in the segment header; `WalRecord::v2` lives alongside v1; readers dispatch on
    schema. Trigger: switching the recovery model from ARIES to MVCC-style versioning.
- [ ] **Design-ahead** (for Tuesday's `crates/wal/src/group_commit.rs`): complete `group_commit`'s **Phase 1
  only** today (no code). It depends solely on `SegQueue`'s *interface* (Tue's contract), not its impl, so
  designing it now is safe — and it pulls a no-code 45-min block off Tuesday (the week's heaviest day) onto
  Monday (G8 load balance). (~45 min)
  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/wal/notes/26_group_commit_prediction.md`:
    - P1. How will a producer learn its record is durable? Exact type (`oneshot::Receiver<Lsn>` per call vs
      a shared `durable_lsn: AtomicU64` watermark + `Notify`). Commit before reading D1.
    - P2. Predicted throughput at batch=1 and batch=1024 in records/sec; predicted fsync rate at 100k rec/s.
    - P3. Concurrent-first-call outcome: a caller `await`s an append, then *cancels* (drops the future)
      before fsync — is the record durable? Does the caller find out?
    - P4. Predict p99.9 latency if the flush loop drains the ENTIRE pending queue into one batch under burst.
    - P5. **List every Phase-2 test you predict your first attempt will FAIL**, and why.

  - *Mental model*: `GroupCommit` amortizes the one expensive operation (fsync ≈ 500 µs) across many records.
    Producers assign an LSN and fan into a `SegQueue` (Tue's primitive) lock-free; a single background thread
    is the *sole* fsync caller and advances a monotonic `durable_lsn` watermark. Durability is observed via
    the watermark, never via a per-call channel that dies when the caller cancels.

  - *Contract*:
    ```rust
    pub struct GroupCommit {
        pending: SegQueue<WalRecord>, durable_lsn: AtomicU64,
        durable_notify: Notify, next_lsn: AtomicU64, pending_count: AtomicUsize, /* soft cap */
    }
    impl GroupCommit {
        pub async fn append(&self, rec: WalRecord) -> Result<Lsn, AppendError>; // resolves when durable
        pub fn durable_lsn(&self) -> Lsn;                                       // monotonic watermark
        pub fn shutdown(self) -> Result<(), AppendError>;                       // drain + final fsync + join
    }
    ```

  - *Back-of-envelope* (bar (c)): fsync ≈ 500 µs. batch=1 → ≤2k rec/s. batch=1024 → 1024 records per
    500 µs fsync = ~2M rec/s ceiling on fsync alone; target ≥500k rec/s. The flush cadence (10 µs OR 1024
    records) bounds added latency: a record waits at most one flush window (~10 µs) + one fsync (~500 µs) →
    p99 ≈ 2 ms under sustained load. Cliff: an *uncapped* batch of 10k holds all waiters for one ~80 ms
    fsync → p99.9 = 80 ms vs 4 ms capped = 20× tail blowup. Per-record oneshot adds ~50 ns alloc/op; the
    watermark path is alloc-free.

  - **Requirements** — write in `crates/wal/notes/26_group_commit_design.md`:
    - R1. `GroupCommit::append(record) -> Lsn` returns only when fsync covering `record.lsn` completes.
    - R2. `durable_lsn: AtomicU64` is monotonic high-water mark; producers wait via `durable_notify`.
    - R3. Lsn assigned at producer side via `next_lsn.fetch_add(1)` BEFORE push (preserves arrival order).
    - R4. Fsync thread is the UNIQUE fsync caller; producers never block on disk.
    - R5. Cancellation safety: caller drops the future mid-append; the record's durability is observable
      via `durable_lsn` watermark, NOT a per-call oneshot (which dies with the future).
    - R6. Tail latency cap: batch size bounded at `max_batch=1024`; preemptive fsync when batch ≥ threshold.
    - R7. Backpressure: `pending_count` soft cap; producers await Notify when above; never OOM.
    - R8. Cross-segment batch: when current segment fills mid-batch, fsync current, rotate, continue.
    - R9. Graceful shutdown: drain pending, final fsync, join thread.
    - R10. ≥500k records/sec at batch=1024 on NVMe; ≥50k records/sec at batch=1; p99 append latency ≤2ms
      at 100k/sec sustained.

  - **Design walk-through** — answer each:
    - D1. Why is durability observed via a `durable_lsn` watermark instead of a per-record oneshot?
      (Hint: oneshot dies with the cancelled future; watermark survives.)
    - D2. The `max_batch` cap protects p99.9 — explain how a 10k-batch is worse than two 5k-batches.
    - D3. SegQueue (Tue) vs Mutex<Vec> baseline — what's the contention story at 4 producers?
    - **D4. Pre-mortem (load-bearing):** write the scenario where a cancelled producer's record IS durable
      but the caller doesn't know: `1) producer next_lsn.fetch_add→42, push; 2) caller drops the future
      (cancel); 3) oneshot Sender drops; 4) flush thread fsyncs record 42 → durable on primary's disk; 5)
      caller never learned → never replicated → follower (W64) never sees 42 → primary/replica diverge`.
      State the watermark fix.
    - **D5. Anti-shortcut audit:** (a) "per-call `oneshot` IS the durability signal" — dies on cancel (D4);
      use it as a fast-path Notify only, watermark is truth. (b) "drain the whole queue per flush" —
      unbounded batch → p99.9 cliff; cap at 1024. (c) "`Mutex<Vec<WalRecord>>` instead of SegQueue" —
      serializes 16 producers behind one lock; D3 contention story. (d) "producer msyncs in its drop to
      'be safe'" — breaks single-writer fsync; only the flush thread syncs. (e) "unbounded SegQueue, no
      soft cap" — OOM under fsync stall; pending_count + backpressure is a *correctness* primitive.

  - 🧮 **Paper drill** — write in `crates/wal/notes/26_group_commit_drill.md`:
    - D-HB on the commit→ack ordering for one record (lsn 42): two columns — flush thread / producer awaiting
      durability. Mark the Release (`durable_lsn.store(42, Release)` after the fsync returns) and the Acquire
      (`durable_lsn.load(Acquire) >= 42` in the producer's wake path). Draw the RA edge and chain: the producer
      that observes the watermark ≥ 42 also observes the fsync's side effect (the bytes are on disk). Verdict:
      a producer that woke on `durable_notify` is guaranteed the data is durable. NEGATIVE: weaken the store to
      `Relaxed` and draw the interleaving where the producer reads watermark=42 but the fsync's write hasn't
      published → reports durable on data that isn't.
    - D-COST on fsync amortization: per-op cost table at batch=1 vs batch=1024. fsync ≈ 500 µs (Jeff-Dean
      syscall + disk). batch=1 → 500 µs/record → ≤2k rec/s. batch=1024 → 500 µs / 1024 ≈ 0.49 µs/record →
      ~2M rec/s ceiling. Cliff ratio = 1024× on the fsync line. State the budget (≥500k rec/s) and that the
      capped batch fits; show the uncapped-10k-batch tail: one 80 ms fsync holds all waiters → p99.9 = 80 ms
      vs 4 ms capped = 20× tail blowup (this is the perf reason R6's cap exists).
  - **Output**: `notes/26_group_commit_prediction.md` (sealed P1–P5) + `notes/26_group_commit_design.md` with
    R1–R10 + D1–D5 + the flush_loop pseudocode + the divergence trace; plus `notes/26_group_commit_drill.md`
    with the commit→ack HB diagram + the fsync-amortization cost table. Tuesday picks up at group_commit Phase 2.
- [ ] Commit + log (~10 min)
  - *(The standalone "append 10k + reopen + scan" smoke test is folded into Phase-2's
    `append_1m_small_records_scan_in_order` + `crash_injection_tail_truncation` — no separate throwaway.)*

**Tuesday — `wal` group commit + [NEW] `concurrent::SegQueue` build**

- [ ] **Build**: `crates/concurrent/src/queue/seg_queue.rs` — `SegQueue<T>` unbounded MPMC, linked segments of N=32
  slots. Head and tail are `CachePadded<AtomicPtr<Segment>>`. Each segment carries a per-slot seq (same Vyukov idea as
  the bounded channel, per-segment). Memory reclamation: simple atomic refcount per segment for now; once `epoch-gc`
  lands W33, swap to `Guard::defer_destroy`. (~total 200 min across 3 phases)
  - 🔧 **Why this matters**: SegQueue is the unbounded MPMC fan-in primitive that everything write-side uses.
    Group commit (next task today) fans 16 producers into one fsync thread via this. W30 mmap-queue uses it
    for incoming write streams. W74 matching-engine order ingress relies on the same shape. Memory
    reclamation is the hardest part; doing it correctly here teaches the W33 EBR migration cleanly.

  #### Phase 1 — Problem, prediction, design (~45 min, no code yet)

  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/concurrent/notes/26_seg_queue_prediction.md`:
    - P1. The reclamation primitive you'd reach for — exact import (`Arc<Segment>` vs raw `AtomicPtr` +
      manual refcount vs "wait for W33 EBR `Guard::defer_destroy`"). Commit to one and predict its cost.
    - P2. Predicted single-producer push latency in ns; predicted per-push CAS count.
    - P3. The concurrent-first-call outcome: two producers both fill the last slot of a segment and both go
      to allocate the next segment. Predict what happens to the loser's `Box`.
    - P4. Derive/ordering prediction: with `next` stored `Relaxed`, predict what the consumer reads from
      slot 0 of the freshly-linked segment on weak hardware (ARM/loom).
    - P5. **List every Phase-2 test (incl. loom) you predict your first attempt will FAIL**, and why.

  - *Mental model*: `SegQueue<T>` is an UNBOUNDED MPMC — a linked list of bounded Vyukov rings (32 slots
    each). Bounded rings give backpressure-free wait-free slot claims; the linked list gives unbounded
    capacity. The entire difficulty is *segment lifetime*: who frees a segment when consumers have moved past
    it but a producer may still hold a stale pointer to it?

  - *Contract*:
    ```rust
    pub struct SegQueue<T> {
        head: CachePadded<AtomicPtr<Segment<T>>>,   // consumer end — its OWN cache line (W4 CachePadded)
        tail: CachePadded<AtomicPtr<Segment<T>>>,   // producer end — a DIFFERENT cache line
    }
    impl<T> SegQueue<T> {
        pub fn push(&self, v: T);                    // wait-free slot claim, lock-free segment link
        pub fn try_pop(&self) -> Option<T>;
    }
    ```

  - *Back-of-envelope* (bar (c)): target ≥5M push/s single-producer (≤200 ns). A push is: `fetch_add` on the
    slot index (~5 ns uncontended), `seq.store(Release)` (~5 ns), and *occasionally* a segment alloc
    (~50 ns malloc, amortized 1/32 ≈ 1.5 ns). If `head`/`tail` share a cache line, every producer push
    invalidates the consumer's line via MESI → ~40 ns coherence miss *per op* = the 200 ns budget blown 4×.
    A per-*push* refcount bump (extra CAS ~25 ns) similarly defeats budget; bump per-*segment* (1/32) instead.
    Cliff ratio: per-op refcount (~600 ns/push observed) vs per-segment (~200 ns) = 3×.

  - **Requirements** — write in `crates/concurrent/notes/26_seg_queue_design.md`:
    - R1. `SegQueue<T>` with `push(T)` and `try_pop() -> Option<T>`. Unbounded.
    - R2. `head: CachePadded<AtomicPtr<Segment>>`, `tail: CachePadded<AtomicPtr<Segment>>` — separate cache
      lines (no producer/consumer false sharing).
    - R3. Each segment holds 32 slots; per-slot `seq: AtomicU64` (Vyukov scheme); per-segment `next:
      AtomicPtr<Segment>` linking to successor.
    - R4. `next.store(new, Release)` paired with `next.load(Acquire)` so consumers see initialized
      next-segment data.
    - R5. Multiple producers racing to allocate the next segment: only ONE wins the CAS; losers drop their
      Box explicitly (no leak).
    - R6. Reclamation: per-segment atomic refcount. Producers bump on follow-next; consumers bump on
      follow-next; both decrement on departure. Only refcount-zero segments freed.
    - R7. Throughput: ≥5M push/sec single-producer; ≥2M aggregate at 16 producers.
    - R8. Loom-green: 2-producer + 2-consumer harness, no torn reads, no UAF, no leaks.

  - **Design walk-through** — answer each (no peeking at `crossbeam-queue::SegQueue` until Phase 3):
    - D1. Why is "who owns the node, when does it die" the central question of every lock-free DS?
      Property table: `Arc<Segment>` (safe, but atomic refcount on *every* clone/drop ~25 ns, and a cycle
      via `next` leaks), manual `AtomicU64` refcount (cheap if per-segment, UAF window if naive), EBR/W33
      (cheapest amortized, not built yet). Sketch each.
    - D2. The Vyukov per-slot seq scheme — what values does seq take through the slot's lifecycle?
      (Hint: 0 = empty, slot_idx after push, slot_idx + 1 after pop.)
    - **D3. Pre-mortem (load-bearing):** write the EXACT 5-line UAF: `1) consumer loads next→S, gets ptr;
      2) consumer about to bump S.refcount; 3) producer decrements S.refcount to 0; 4) producer frees S;
      5) consumer dereferences freed S → UAF`. Then write the second pre-mortem: `next` stored `Relaxed`,
      consumer follows it before the producer's `seq`-slot init is visible → reads `0xCDCD`. If you can't
      write both interleavings you can't prove the Release/Acquire + bump-while-linked fixes them.
    - **D4. Anti-shortcut audit:** (a) "`Mutex<VecDeque<T>>`" — correct, but one lock serializes all
      producers+consumers, ~25 ns + contention, no MPMC scaling. (b) "`Arc<Segment>` everywhere" — drowns
      the hot path in atomic refcount traffic and leaks on the `next` cycle. (c) "store `next` `Relaxed`
      because it's just a pointer" — pointer is fine, the *pointee's init* is not published (D3). (d) "bump
      refcount per push" — extra CAS per op, 3× slowdown; bump per segment-entry. (e) "head and tail in one
      struct without `CachePadded`" — false sharing (the W4 cross-primitive trap below).

  - 🧮 **Paper drill** (deepest tier — lock-free MPMC, 5 drills) — write in
    `crates/concurrent/notes/26_seg_queue_drill.md`:
    - D-STRUCT on a `SegQueue<u64>` instance with **4 slots per segment** (not 32, to fit on paper). Draw
      `head`/`tail` both pointing at Seg-A (4 slots, per-slot `seq`, `next=null`). Hand-execute, redrawing after
      each mutation: push 1,2,3,4 (fills Seg-A: each `slot[i].seq` goes 0→i after store); push 5 → producer
      finds Seg-A full, allocates Seg-B, CAS `A.next: null→B`, links, writes slot 0 of B; pop ×5 → consumer
      drains A slots 0..3, follows `A.next→B`, pops B slot 0. Then the 2-thread interleaving at the
      linearization point (the `tail` index `fetch_add` that commits a push): show two producers both at the
      Seg-A→Seg-B boundary and why exactly one wins the `next` CAS.
    - D-RMW on the head/tail slot-index claim: step table, columns producer-T1 / producer-T2 / `tail.index`.
      Both push into the 4-slot Seg-A. Show `fetch_add(1)` hands T1 index 0, T2 index 1 (disjoint slots, no CAS
      retry). Then the segment-alloc CAS sub-table: T1 and T2 both observe Seg-A full, both `Box::new(Seg-B/C)`,
      both `compare_exchange(A.next, null, mine)`; T1 wins, T2 sees `Err`, must `Box::from_raw` + drop its loser
      (the no-leak fix). Annotate: slot claim = atomicity (wait-free RMW); segment link = lock-free (CAS retry).
    - D-HB on the slot-data → ready-flag publish (Vyukov seq): two columns — producer / consumer. Producer:
      `slot[i].data = v` (plain write) then `slot[i].seq.store(i+1, Release)`. Consumer: spins on
      `slot[i].seq.load(Acquire) == i+1` then reads `slot[i].data`. Draw the RA edge; chain: the consumer that
      sees `seq==i+1` sees `data==v`. Verdict: no torn read. NEGATIVE: store `seq` (or `next`) `Relaxed` — draw
      the weak-hardware interleaving where the consumer follows the freshly-linked `next`/reads `seq` before the
      data/segment-header init publishes → reads `0xCDCD` (the exact bug `consumer_following_next_sees_initialized`
      traps).
    - D-LOOM on the 2P+2C model: write threads × shared-ops (2 producers each push 1 value, 2 consumers each
      `try_pop` once — keep it 2×~2 ops so it terminates), the exact assertion (the multiset of popped values ⊆
      pushed values, no value popped twice, no UAF, no leaked segment), and an order-of-magnitude
      interleaving-count guess (forcing one segment-alloc boundary so the `next`-publish + refcount races are in
      scope). Negative-control note: a real loom run is ≥100 ms — if `loom_2p_2c_safe` finishes in 2 ms, loom
      didn't intercept the atomics (the model has no shared atomic on the checked path) → vacuous.
    - D-UNSAFE on the per-slot init/read invariant + the AtomicCell padding hazard: 3-column ledger
      (unsafe op | precondition | runtime fact). Rows: (1) read `slot[i].data` via `MaybeUninit::assume_init`
      — precondition "a producer wrote it" — guaranteed by the D-HB seq-Acquire seeing `i+1`; UB if read on
      `seq==0`. (2) `Box::from_raw(loser_seg)` after CAS loss — precondition "this ptr was `Box::into_raw`'d by
      me and never linked" — guaranteed by the CAS returning `Err` (no other thread can reach it); UB if the
      winner's ptr is freed. (3) the slot stores a user `T` via the W4 **AtomicCell** (recall W4's Pod-hardening
      fed this) — precondition "`T`'s padding bytes are deterministic / not compared" — flag the row: a `T` with
      `repr(Rust)` padding hands AtomicCell uninit padding bytes → the W4 hazard reappears here; name the
      guarantee-keeper (AtomicCell's `lock-based fallback for non-Pod T`).
  - **Output**: `notes/26_seg_queue_design.md` with R1–R8 + D1–D4 + the segment lifecycle diagram + both bad interleavings; plus `notes/26_seg_queue_drill.md` with the 5 worked drills (D-STRUCT, D-RMW, D-HB, D-LOOM, D-UNSAFE).

  #### Phase 2 — Write the failing tests (~30 min)

  - [ ] **(NEWBIE)** `push_pop_round_trip_single_thread` — push 1M values, pop them all back; same order,
    no duplicates. Exercises R1.
  - [ ] **(EXPERT)** `multi_producer_multi_consumer_no_loss` — 8 producers × 100k pushes, 4 consumers; total
    popped = 800k, no duplicates, no losses. Exercises R3, R5.
  - [ ] **(EXPERT)** `concurrent_segment_alloc_no_leak` — drive the queue to allocate 1000 segments via 16
    producers; `dhat` shows zero leaked Boxes (all CAS losers reclaimed). Exercises R5.
  - [ ] **(EXPERT)** `loom_2p_2c_safe` — `#[cfg(loom)]` 2-producer + 2-consumer harness; loom explores; no
    UAF, no torn read, no lost wakeup. Exercises R8.
  - [ ] **(SENIOR)** `consumer_following_next_sees_initialized` — single-producer pushes 33 items (forces
    segment alloc); single-consumer pops all; assert no `0xCD` junk. Exercises R4.
  - [ ] **(SENIOR)** `refcount_zero_segment_freed` — push N items spanning 5 segments; pop all; assert 5
    segments were freed (dhat or explicit drop counter). Exercises R6.
  - [ ] **(EXPERT, perf bar (c))** `bench_5m_push_per_sec_single_producer` — criterion; ≥5M push/sec. Fails
    a per-push-refcount impl. Exercises R7.
  - [ ] **(EXPERT)** 🪤 **cross-primitive trap (composes W4 `CachePadded`)** `head_tail_on_distinct_cache_lines`
    — `SegQueue`'s `head` and `tail` each pass `CachePadded` in isolation, but if the *enclosing* struct is
    `repr(Rust)` the compiler may still pack them adjacently. Pins the offset like the W4 verbatim trap.
    ```rust
    #[test]
    fn head_tail_on_distinct_cache_lines() {
        let line = if cfg!(target_arch = "aarch64") { 128 } else { 64 };
        let h = std::mem::offset_of!(SegQueue<u64>, head);
        let t = std::mem::offset_of!(SegQueue<u64>, tail);
        assert!((t as isize - h as isize).unsigned_abs() >= line,
            "head@{h} tail@{t} within one {line}-B line — producer/consumer false-share; \
             #[repr(C)] on SegQueue, not just CachePadded on the fields");
    }
    ```
  - [ ] **(SENIOR)** 🪤 **negative-control (test-the-test)** `loom_actually_explored` — proves the loom run
    above explores a real interleaving space rather than short-circuiting (a loom test that finishes in 2ms
    explored nothing). Under `#[cfg(loom)]`, wrap the model so it records branch count / wall time and
    assert it is non-trivial.
    ```rust
    #[cfg(loom)]
    #[test]
    fn loom_actually_explored() {
        let start = std::time::Instant::now();
        loom::model(|| { /* same 2p/2c body as loom_2p_2c_safe */ });
        assert!(start.elapsed() >= std::time::Duration::from_millis(100),
            "loom finished in {:?} — it explored ~0 interleavings; the model has no shared atomics on the \
             checked path, so loom_2p_2c_safe is vacuous", start.elapsed());
    }
    ```

  #### Phase 3 — Make the tests pass, one failure at a time (~125 min)

  - **(SENIOR) When `concurrent_segment_alloc_no_leak` reports ~50 leaked Boxes after 16-producer burn-in:**
    - Symptom: dhat shows 50 unfreed `Box<Segment>` allocations.
    - Why: multiple producers race to allocate the next segment when current fills. Each allocates its
      own `Box::new(Segment::new())`, then CAS the `next` pointer. Only one wins; losers `Box::leak`
      their segment by simply returning.
    - Fix: try `next.load(Acquire)` FIRST — if non-null, another thread already wrote next; don't
      allocate. If null, allocate, CAS. On CAS failure, `unsafe { Box::from_raw(my_loser) }` then
      drop. The `from_raw` reclaims the heap; the explicit `drop` runs `Segment::drop`.
    - Muscle: in lock-free linked structures, every "I'll allocate the next node" path must reclaim
      its losing allocation; never let losers leak. Reapplies at: W37 skiplist tower extension, W74
      matching-engine order pool, W77 aeron Publication term-buffer linkage.

  - **(EXPERT) When `loom_2p_2c_safe` reports a UAF in the consumer:**
    - Symptom: loom prints `[T1: consumer follows next ptr] → [T2: producer frees segment S after
      refcount drop] → [T1: dereferences freed S]`.
    - Why: consumer reads `next.load(Acquire)`, gets a pointer to segment S; bumps refcount; but the
      bump and load aren't atomic together — producer may decrement-and-free between them.
    - Fix: refcount bump uses a "tagged-CAS" pattern: load+CAS the segment's refcount, ensuring the
      pointer is still in the linked list. If the segment's refcount has already hit zero, retry from
      head/tail load. The simpler fix: never expose the pointer to the consumer until the refcount is
      already bumped (the producer that linked it pre-bumps).
    - Muscle: every "follow a pointer to a refcounted node" must atomically bump-while-still-linked;
      a separate load + bump opens a free-between-them window. Reapplies at: W33 EBR Atomic<T>
      protection, W37 skiplist tower walk, W74 matching-engine pool.

  - **(EXPERT) When `consumer_following_next_sees_initialized` reads garbage values:**
    - Symptom: consumer pops slot 0 of the new segment, sees `0xCDCDCDCD` (uninit pattern) instead
      of the producer's value.
    - Why: `next` pointer stored Relaxed; on weak hardware (loom + ARM), producer's segment-header
      init (`seq` slots, `next` field) isn't yet visible to the consumer that already followed the
      `next` pointer.
    - Fix: producer's segment-init writes are followed by `next.store(new_seg, Release)`; consumer's
      `next.load(Acquire)` pairs. The Release/Acquire pair ensures all stores before the Release are
      visible after the Acquire.
    - Muscle: publishing a pointer to a freshly-initialized structure requires Release-store on the
      publisher and Acquire-load on the consumer — both, paired. Reapplies at: W4 Parker state
      transitions, W74 matching-engine order publish, W77 aeron frame publish, W80 ledger journal.

  - **(EXPERT, perf) When `bench_5m_push_per_sec_single_producer` reports 1.2M/sec (bar (c) violation):**
    - Symptom: push latency 800ns; target 200ns. 4× too slow.
    - Why: every push checks-then-bumps refcount on the tail segment — an extra CAS per push.
    - Fix: the producer doesn't need to refcount-bump on EACH push; bump once on segment-entry,
      decrement on segment-departure (when allocating the next segment). Same pattern works for
      consumers.
    - Cost avoided: ~600ns/push (one extra contended CAS per op); at 5M ops/sec, 3sec/sec CPU reclaimed.
    - Muscle: refcount granularity matters for hot-path cost; bumping per-operation is the
      anti-pattern, per-segment is the right granularity. Reapplies at: W33 EBR guard scopes (one
      guard per critical section, not per-op), W37 skiplist scope, W74 matching pool.

  - **(SENIOR, perf) When `head_tail_on_distinct_cache_lines` reports `head@0 tail@8` (bar (c) violation):**
    - Symptom: offsets 8 bytes apart despite both fields being `CachePadded`; producers and consumers
      false-share one line. `cargo test` green, the push bench silently regresses 30-70% under 2+ threads.
    - Why: `CachePadded` aligns and tail-pads each field, but the enclosing `SegQueue` is `repr(Rust)`;
      the compiler is free to place `tail` immediately after `head`'s payload, *inside* head's 120-byte pad.
      (Same root cause as the W4 CachePadded verbatim trap — padding is load-bearing, layout wasn't locked.)
    - Fix: `#[repr(C)]` on `SegQueue` so field order/offsets are fixed and the padding actually separates them.
    - Cost avoided: silent 30-70% multi-thread regression (MESI line bouncing ~40 ns/op); green test, RED bench.
    - Muscle: `CachePadded` on a field is necessary but not sufficient — lock the enclosing struct's layout
      with `#[repr(C)]` whenever padding is load-bearing. Reapplies at: W11 MPMC ring head/tail, W58
      matching-engine order book, W65 Disruptor sequencer/cursor, W77 aeron term metadata.

  - **5-year failure mode** (write in `notes/26_seg_queue_followup.md` after green): when W33 ships
    epoch-based reclamation (EBR), refcount becomes redundant. State the migration: behind a
    `cfg(feature = "ebr_reclaim")`, swap the refcount path for `Guard::defer_destroy`; the API stays
    the same. Trigger: W33 ships and W37 skiplist needs the same primitive at higher throughput than
    refcount allows.
- [ ] **Build**: `crates/wal/src/group_commit.rs` —
  `GroupCommit { pending: SegQueue<(WalRecord, oneshot::Sender<Lsn>)>, fsync_signal: Notify }`. Background fsync
  thread drains every 10 µs or 1024 records. Producers fan in lock-free; the fsync thread is the single consumer. (~total 200 min across 3 phases; Phase 1 ~45 min done Monday, Phase 2/3 ~155 min today)
  - 🔧 **Why this matters**: a single fsync costs 50-1000 µs on NVMe. At 100k records/sec naïvely, that's
    100k fsyncs/sec — impossible. Group commit amortizes: 100k records → 1k batches → 1k fsyncs/sec, each
    carrying 100 records. Pattern is canonical: PostgreSQL `XLOG_BUFFER_FLUSH`, Aeron `ConductorAgent`,
    Chronicle Queue's `TextWire`. Mistakes here are correctness bugs (lost durability) or latency bugs
    (caller waits 10ms for an fsync). Downstream: W29 recovery, W64 raft AppendEntries, W74 matching
    order-log, W80 ledger, W83 aeron archive, W95 mini-db.

  #### Phase 1 — Problem, prediction, design (~45 min) — **done Monday (design-ahead)**

  - This Build's Phase 1 (pre-prediction prompt, mental model, contract, back-of-envelope, R1–R10, D1–D5
    with the cancellation-divergence pre-mortem + anti-shortcut audit) was completed at the end of **Monday**
    — it depends only on `SegQueue`'s *interface* (Tue's contract), not its impl, so it is safe to design
    ahead. This balances Tuesday's load (the two impls stay paired here; the no-code design moves off the
    heaviest day). The sealed answers live in `crates/wal/notes/26_group_commit_prediction.md` and the design
    in `crates/wal/notes/26_group_commit_design.md`. Re-read those before writing the tests below.

  #### Phase 2 — Write the failing tests (~30 min)

  - [ ] **(NEWBIE)** `single_producer_durable_after_append_returns` — append returns; reopen segment;
    record present. Exercises R1.
  - [ ] **(SENIOR)** `durable_lsn_monotonic_under_burst` — 16 producers × 100k records; sample `durable_lsn`
    1k times; sequence is non-decreasing. Exercises R2.
  - [ ] **(SENIOR)** `lsn_arrival_order` — LSN assigned BEFORE push; final wal records' LSNs correspond to
    producer-arrival order. Exercises R3.
  - [ ] **(SENIOR)** 🪤 **anti-shortcut** `producer_never_calls_fsync` — single-writer-principle guard;
    asserts no `msync`/`fsync` on any producer-side stack frame. Bites the "msync on cancel" reflex.
    Exercises R4.
  - [ ] **(EXPERT)** 🪤 **anti-shortcut** `cancelled_producer_still_observes_durability` — the
    cancellation-vs-oneshot trap (D4).
    ```rust
    #[tokio::test]
    async fn cancelled_producer_still_observes_durability() {
        let gc = GroupCommit::open(tmp()).await;
        let lsn = {
            let fut = gc.append(rec(42));
            futures::pin_mut!(fut);
            // poll once to enqueue + assign LSN, then DROP (cancel) before fsync completes
            let _ = futures::poll!(&mut fut);
            gc.peek_assigned_lsn()
        }; // fut dropped here
        // the record must still become durable via the watermark, despite cancellation
        gc.wait_durable(lsn).await;
        assert!(gc.durable_lsn() >= lsn,
            "watermark never reached {lsn:?} — durability was tied to the oneshot and died on cancel");
    }
    ```
  - [ ] **(EXPERT, perf bar (c))** `max_batch_cap_holds_p99_under_burst` — produce 1M records in burst;
    p99.9 latency ≤ 4ms. Fails an uncapped-batch flush loop. Exercises R6, R10.
  - [ ] **(EXPERT)** `backpressure_above_cap_blocks_producer` — fsync-thread paused (test hook); producers
    block when pending exceeds the soft cap; no OOM. Exercises R7.
  - [ ] **(SENIOR)** `cross_segment_batch_no_loss` — batch mid-fill crosses segment boundary; both segments
    fsynced; all records durable. Exercises R8.
  - [ ] **(SENIOR)** `shutdown_drains_pending` — push 1k records, shutdown — all 1k durable after shutdown
    returns. Exercises R9.
  - [ ] **(EXPERT, perf bar (c))** `bench_batch_size_throughput_curve` — criterion sweep at batch=1,8,64,
    1024; near-log-linear scaling. Exercises R10.
  - [ ] **(EXPERT)** `vopr_1000_random_crash_injections` — TigerBeetle-style fault injection across 1000
    trials; every recovery converges to a state consistent with returned-from-append LSNs.
  - [ ] **(SENIOR)** 🪤 **negative-control (test-the-test)** `throughput_curve_is_monotone_and_banded` —
    proves the batch-size bench actually measures the amortization curve, not noise: assert batch=1024
    throughput is ≥4× batch=1 AND every point sits in [1e3, 5e6] rec/s. A flat or out-of-band curve means
    the bench fsync was stubbed out or the batch knob did nothing.

  #### Phase 3 — Make the tests pass, one failure at a time (~125 min)

  - **(EXPERT) When `cancelled_producer_still_observes_durability` reports the record never becomes durable:**
    - Symptom: producer's future is dropped (cancellation); the oneshot Sender drops; fsync thread
      receives a dead Sender, silently discards the record. Watermark never advances past that lsn.
      Replicas (W64) diverge from primary because the record exists on primary's disk but never
      reaches the follower.
    - Why: durability semantics were tied to the oneshot. Cancellation breaks the contract.
    - Fix: producer assigns lsn via `next_lsn.fetch_add(1)`, pushes the record. Even if the future is
      dropped, the record is in the queue; the fsync thread will durably write it; the watermark will
      advance past it. The oneshot becomes a fast-path Notify, never the source of truth.
    - Muscle: durability must be observable via shared state (monotonic watermark + Notify), never via
      per-call channels that die with the caller. Reapplies at: W64 raft replication watermark, W74
      matching-engine fill watermark, W80 ledger commit watermark.

  - **(EXPERT, perf) When `max_batch_cap_holds_p99_under_burst` reports p99.9 = 80ms (bar (c) violation):**
    - Symptom: bench shows median 200µs but p99.9 80ms; cause is one 10k-record batch holding all
      waiters for ~80ms of fsync.
    - Why: no max_batch cap; flush_loop drained the entire pending queue into one batch.
    - Fix: cap batch at 1024 per flush; if more pending, drain into multiple batches with one fsync
      each. Also: preemptive flush at 768-record threshold to keep p99 low under burst.
    - Cost avoided: p99.9 80ms → 4ms (20× tail); matters for any caller awaiting tail latency.
    - Muscle: amortization must be CAPPED — unbounded batching destroys p99.9. Reapplies at: W30
      Checkpointer (bounded flush size), W67 raft AppendEntries.

  - **(SENIOR) When `producer_never_calls_fsync` reports `msync` from producer-side stack:**
    - Symptom: stack trace shows producer thread called `msync` in some error path.
    - Why: producer's drop handler tried to "ensure durability" by msyncing on cancel.
    - Fix: remove all msync calls from producer paths; durability is the flush thread's job. Document
      the rule in the producer-side API doc-comment.
    - Muscle: a single-writer fsync design holds ONLY when the writer is actually unique; one stray
      msync from a producer breaks the amortization. Reapplies at: W30 Checkpointer (only checkpoint
      thread msyncs), W64 raft.

  - **(EXPERT) When `backpressure_above_cap_blocks_producer` reports OOM:**
    - Symptom: with fsync paused, SegQueue grows unbounded; process OOM-killed at ~10M pending.
    - Why: SegQueue is unbounded; no soft-cap check.
    - Fix: `pending_count: AtomicUsize` incremented on push, decremented on flush. Producer checks
      before push; if above soft cap, awaits a `space_notify`. Flush wakes `space_notify` each batch.
    - Muscle: unbounded queues are throughput primitives; bounded backpressure is a correctness
      primitive. Always combine. Reapplies at: W30 mmap-queue producer, W67 raft pipelining.

  - **5-year failure mode** (write in `notes/26_group_commit_followup.md` after green): when io_uring
    + persistent memory mature, group commit converges into per-record asynchronous durability (no
    batching needed). State the migration: a `WriteBackend` trait abstracts the durability model;
    `GroupCommitBackend` lives alongside `IoUringBackend` and `PmemBackend`. Trigger: NVMe replaced
    by PMem at server price points.
- [ ] Commit + log (~10 min)
  - *(Note: the standalone SegQueue-vs-`Mutex<Vec>` fan-in bench moved to Friday per the week's Load note —
    Tuesday already carries the two deepest Builds.)*

**Wednesday — `wal` checksums + replay**

- [ ] **Build**: `crates/wal/src/checksum.rs` — CRC32C per record + per-segment-footer rollup. (~total 140 min across 3 phases)
  - 🔧 **Why this matters**: storage hardware silently corrupts. SSDs (especially under power loss) flip bits
    and reorder writes. The only protection: end-to-end checksums on every record, verified on read. CRC32C
    (Castagnoli polynomial) is hardware-accelerated on x86 (SSE4.2 `crc32` instruction) and ARMv8 (CRC32C
    insn) → 10+ GB/s/core. Downstream: replay.rs (today, later) detects tail truncation, W29 recovery
    treats CRC mismatch as end-of-safe-replay, every other wal-using crate inherits.

  #### Phase 1 — Problem, prediction, design (~35 min, no code yet)

  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/wal/notes/26_checksum_prediction.md`:
    - P1. Which polynomial? Exact constant (`0xEDB88320` IEEE vs `0x1EDC6F41` reflected-Castagnoli). Commit
      before D1; predict the value of `crc32c(b"123456789")`.
    - P2. Predicted throughput (GB/s/core) for hw path vs software slice-by-8.
    - P3. CPU-feature gate: predict what happens if you `#[cfg(target_feature="sse4.2")]` and run the binary
      on a pre-SSE4.2 chip. State which Phase-2 test catches it.
    - P4. Predict whether CRC over the in-memory `WalRecord` struct is reproducible run-to-run. Why/why not?
    - P5. **List every Phase-2 test you predict your first attempt will FAIL**, and why.

  - *Mental model*: a wal that trusts disk bytes is one bit-flip from silent consensus divergence. CRC32C
    (Castagnoli) is the end-to-end integrity check, hardware-accelerated on x86 (SSE4.2 `crc32`) and ARMv8.
    Per-record CRC localizes corruption; the per-segment footer rollup detects whole-record drops.

  - *Contract*:
    ```rust
    pub const CRC32C_POLY: u32 = 0x1EDC6F41; // reflected Castagnoli — fixed by spec, not a choice
    pub fn crc32c(data: &[u8]) -> u32;        // runtime-dispatched hw/sw; data is the WIRE bytes
    pub struct SegmentChecksum(u32);
    impl SegmentChecksum { pub fn update(&mut self, record_crc: u32, len: usize); } // crc32c_combine rollup
    ```

  - *Back-of-envelope* (bar (c)): a 1 GB/s wal write stream must checksum 1 GB/s. hw `_mm_crc32_u64`
    processes 8 B/instr at ~1 instr/cycle → ~8 GB/s/core on a 3 GHz core; the CRC layer then costs ~12% of
    one core. Software slice-by-8 ≈ 1 GB/s = the *entire* core. Byte-at-a-time `_mm_crc32_u8` ≈ 1 GB/s →
    the 8× cliff: a chip that *supports* SSE4.2 but uses the u8 path runs CRC at software speed and saturates
    a core at 1 GB/s of wal. Known vector: `crc32c(b"123456789") == 0xE3069283` (IEEE gives 0xCBF43926 —
    the wrong-poly tell).

  - **Requirements** — write in `crates/wal/notes/26_checksum_design.md`:
    - R1. `pub fn crc32c(data: &[u8]) -> u32` returns CRC32C (Castagnoli polynomial `0x1EDC6F41`).
    - R2. Hardware path on x86_64 with SSE4.2 (`_mm_crc32_u64`); aarch64 with CRC32 extension
      (`__crc32cd` intrinsic); software fallback (slice-by-8) otherwise.
    - R3. Runtime detection via `is_x86_feature_detected!("sse4.2")`; never compile-time-gate alone
      (binary won't run on older CPUs).
    - R4. Per-record CRC covers EVERYTHING except the CRC field itself (lsn, kind, flags, prev_lsn,
      payload).
    - R5. On read, CRC mismatch → record is corrupt → stop scanning (caller treats as tail truncation).
    - R6. `SegmentChecksum::update(record_crc)` running combine via `crc32c_combine` (Mark Adler's
      formula); segment footer stores rollup.
    - R7. Throughput ≥8 GB/s/core on x86_64 SSE4.2; ≥6 GB/s on aarch64; ≥1 GB/s software fallback.
    - R8. Known vector: `crc32c(b"123456789") == 0xE3069283`.

  - **Design walk-through** — answer each:
    - D1. Why CRC32C and not CRC32-IEEE? (Hint: industry consensus + hardware acceleration.)
    - D2. Slice-by-8 software algorithm: how does it beat byte-at-a-time? (Hint: process 8 bytes per
      table lookup; 4 parallel tables.)
    - **D3. Pre-mortem (load-bearing):** write the SIGILL trace — `1) build with -C target-feature=+sse4.2;
      2) compiler emits crc32 instr unconditionally; 3) deploy to a pre-SSE4.2 CI runner; 4) first call →
      SIGILL, process dies`. And the flaky-CRC trace — `crc32c(&record as *const _ as &[u8])` over the
      in-memory struct includes uninit padding → two runs differ. State both fixes.
    - **D4. Anti-shortcut audit:** (a) "`crc32fast` crate's default" — that's CRC32-IEEE, the WRONG
      polynomial for wal interop (D1). (b) "`#[cfg(target_feature=...)]` gate" — compile-time, SIGILLs on
      old CPUs; use `is_x86_feature_detected!` runtime check. (c) "CRC the `WalRecord` struct bytes
      directly" — padding is uninit → non-reproducible; CRC the *serialized wire* bytes. (d) "`_mm_crc32_u8`
      in a loop" — 8× slower than `_mm_crc32_u64`; word-at-a-time + a 0..7 byte tail.

  - 🧮 **Paper drill** — write in `crates/wal/notes/26_checksum_drill.md`:
    - D-COST on the hw-vs-sw CRC path: per-byte cost table for a 1 GB/s wal stream. `_mm_crc32_u64` = 8 B/instr
      at ~1 instr/cycle on a 3 GHz core → ~8 GB/s/core → CRC layer ≈ 12% of one core. Software slice-by-8 ≈
      1 GB/s = the WHOLE core. `_mm_crc32_u8` (byte-at-a-time) ≈ 1 GB/s. Cliff ratio = 8× (u64 vs u8 path even
      on the SAME SSE4.2 chip). State the budget (checksum must keep up with 1 GB/s of appends) and that only
      the u64 path fits.
    - D-UNSAFE on the feature-gate + padding hazards: 3-column ledger. Row 1: emit the `crc32` instruction —
      precondition "CPU supports SSE4.2" — guaranteed by `is_x86_feature_detected!("sse4.2")` at RUNTIME; UB
      (SIGILL) if guaranteed only by compile-time `#[cfg(target_feature)]` and deployed to an older CPU. Row 2:
      `crc32c(&record as *const _ as &[u8])` over the in-memory struct — precondition "all bytes initialized" —
      NOT guaranteed (struct padding is uninit) → non-reproducible CRC; fix = CRC the serialized wire bytes
      (record.rs has no padding). Flag row 2's guarantee as broken.
    - D-WALK on the known vector: hand-walk `crc32c(b"123456789")` to the asserted `0xE3069283` using poly
      `0x1EDC6F41` (reflected Castagnoli) — at least sketch the init (`0xFFFFFFFF`), the per-byte reflected
      table-lookup step for the first 2–3 bytes, and the final XOR-out; then note the IEEE poly `0xEDB88320`
      gives `0xCBF43926` on the same input (the wrong-poly tell the test traps).
  - **Output**: `notes/26_checksum_design.md` with R1–R8 + D1–D4 + the polynomial constant + both traces; plus `notes/26_checksum_drill.md` with the cost table, feature-gate ledger, and the known-vector walk.

  #### Phase 2 — Write the failing tests (~25 min)

  - [ ] **(SENIOR)** 🪤 **anti-shortcut** `crc32c_known_vector` — the wrong-polynomial trap.
    ```rust
    #[test]
    fn crc32c_known_vector() {
        assert_eq!(crc32c(b"123456789"), 0xE3069283,
            "got {:#x}: if it's 0xCBF43926 you used CRC32-IEEE (0xEDB88320), not Castagnoli (0x1EDC6F41)",
            crc32c(b"123456789"));
    }
    ```
  - [ ] **(EXPERT)** `sw_matches_hw_on_1gb_random` — software and hardware impls return identical CRC over a
    1 GiB random buffer (cross-checks both paths). Exercises R2.
  - [ ] **(SENIOR)** 🪤 **anti-shortcut** `runtime_detection_no_sigill_on_old_cpu` — run the binary under an
    SSE4.2-disabled harness (qemu `-cpu` mask or a feature-flag override); assert it returns the correct CRC
    via the software path instead of SIGILL. Bites a compile-time gate. Exercises R3.
  - [ ] **(NEWBIE)** `single_bit_flip_detected` — flip 1 bit in a 1 KiB record at every bit position; CRC
    differs from original each time. Exercises detection power.
  - [ ] **(SENIOR)** `segment_rollup_detects_record_drop` — drop a record from the middle of a 100-record
    segment; recomputed rollup differs from stored footer rollup. Exercises R6.
  - [ ] **(SENIOR)** 🪤 **anti-shortcut** `padding_bytes_zeroed_in_crc` — CRC of the same record is
    reproducible across two runs (forces CRC-over-wire, not CRC-over-struct). Exercises D3.
  - [ ] **(EXPERT, perf bar (c))** `bench_8gb_per_sec_sse42` — criterion on x86 SSE4.2 hardware;
    ≥8 GB/s/core. Fails the u8-at-a-time path. Exercises R7.
  - [ ] **(SENIOR)** 🪤 **negative-control (test-the-test)** `bench_throughput_in_hw_band` — assert the CRC
    bench lands in [4e9, 2e10] B/s on SSE4.2 hardware. Below 2 GB/s means it silently fell back to software
    (the bench "passes" by measuring the wrong path); above 20 GB/s means the buffer was cached/elided.

  #### Phase 3 — Make the tests pass, one failure at a time (~80 min)

  - **(SENIOR) When `crc32c_known_vector` returns `0xCBF43926` instead of `0xE3069283`:**
    - Symptom: vector test fails; the returned value `0xCBF43926` is CRC32-IEEE on the same input,
      not CRC32C.
    - Why: implementation uses polynomial `0xEDB88320` (IEEE) instead of `0x1EDC6F41` (Castagnoli).
      The two polynomials produce different values for non-trivial inputs.
    - Fix: explicit `pub const CRC32C_POLY: u32 = 0x1EDC6F41;`. In the hardware path, use
      `_mm_crc32_u64` which IS Castagnoli; in software, build the table from `CRC32C_POLY`.
    - Muscle: cryptographic / checksum polynomials are fixed by the spec; using the wrong one is
      silent corruption (small inputs pass, large inputs diverge). Reapplies at: W31 wire schema
      header CRC, W83 aeron Archive position checksum.

  - **(SENIOR) When `runtime_detection_no_sigill_on_old_cpu` reports SIGILL on the emulated older CPU:**
    - Symptom: binary built with SSE4.2 target feature crashes with SIGILL on older hardware.
    - Why: `#[cfg(target_feature = "sse4.2")]` is COMPILE-time; the binary unconditionally executes
      the hw path.
    - Fix: `if is_x86_feature_detected!("sse4.2") { hw_path(data) } else { sw_path(data) }` — runtime
      branch. Document the perf cost (one cached check on first call).
    - Muscle: CPU-feature gates must be runtime-checked AND the binary must work on the older
      machine; compile-time `target_feature` is a perf knob, not a correctness gate. Reapplies at:
      W26 record decode dispatch, W74 matching-engine AVX2 paths.

  - **(SENIOR) When `padding_bytes_zeroed_in_crc` reports different CRCs on identical runs:**
    - Symptom: the same `WalRecord` constructed twice yields two different CRCs.
    - Why: `WalRecord` struct has implicit padding bytes; their values are whatever the stack had.
      CRC over struct bytes is non-deterministic.
    - Fix: CRC over the SERIALIZED bytes (the wire form from record.rs), not the in-memory struct.
      The wire form has no padding by construction.
    - Muscle: end-to-end CRC must cover the wire form, never the in-memory struct (which may have
      padding). Reapplies at: W27 storage-trie page CRC, W83 aeron frame CRC.

  - **(EXPERT, perf) When `bench_8gb_per_sec_sse42` reports 1.2 GB/s on SSE4.2 hardware (bar (c) violation):**
    - Symptom: bench reports software-fallback throughput on a chip that supports SSE4.2.
    - Why: runtime detection works but the hw path uses `_mm_crc32_u8` (one byte at a time) instead
      of `_mm_crc32_u64` (8 bytes at a time).
    - Fix: process input 8 bytes at a time via `_mm_crc32_u64`; handle the trailing 0..7 bytes via
      `_mm_crc32_u8` in a tail loop.
    - Cost avoided: 8× throughput cliff (~7 GB/s/core); at 1 GB/sec aggregate wal write, the CRC layer alone
      goes from 12% of a core to 100%.
    - Muscle: SIMD CRC paths process word-sized chunks, not byte-by-byte; the byte tail is a small
      separate loop. Reapplies at: W14 bufpool checksum, W26 replay.rs CRC verification.

  - **5-year failure mode** (write in `notes/26_checksum_followup.md` after green): when post-quantum
    hash functions (BLAKE3, SHA3-256 hardware) become commoditized, end-to-end CRC32C may give way to
    a 128-bit cryptographic hash for both integrity AND auth. Migration: `Integrity` trait abstracts
    CRC32C and a future BlakeIntegrity impl. Trigger: when wal needs both integrity AND
    tamper-evidence (e.g. raft replication across untrusted networks).
- [ ] **Build**: `crates/wal/src/replay.rs` — `Replayer<'a, S: SegmentStream>` yields `Result<WalRecord, ReplayError>`.
  Stops cleanly at the first checksum mismatch (partial-tail handling). (~total 140 min across 3 phases)
  - 🔧 **Why this matters**: replay is the ONLY code that runs after a crash. Its job: produce every record
    the application durably appended, in order, and STOP cleanly at the partial-tail. Any deviation = silent
    data loss (under-replay) or panic (over-replay reading uninit bytes). This is the single most-tested
    module in any durable storage system. Downstream: W29 ARIES analysis pass wraps `Replayer`; W64 raft
    follower replays leader's wal via this; W30 recovery v0.5 integrates as the read side.

  #### Phase 1 — Problem, prediction, design (~35 min, no code yet)

  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/wal/notes/26_replay_prediction.md`:
    - P1. The Item type your iterator yields — exact signature (`Result<WalRecord, _>` owned vs
      `Result<WalRecordRef<'a>, _>` borrowing mmap). Commit before D-bench; predict the throughput of each.
    - P2. Predicted scan throughput (GB/s) for the borrowing vs owning impl.
    - P3. Concurrent/crash outcome: file truncated mid-record so the mmap extends past file EOF — predict
      what your first read does (clean tail-exit vs SIGBUS). Name the test that catches the wrong answer.
    - P4. Derive prediction: a recycled segment has valid per-record CRCs from an OLD generation — does CRC
      alone catch it? What second-layer check do you need?
    - P5. **List every Phase-2 test you predict your first attempt will FAIL**, and why.

  - *Mental model*: `Replayer` is the ONLY code that runs after a crash. Its job: yield every durably-appended
    record, in order, and STOP cleanly at the partial tail — distinguishing tail-truncation (normal) from
    mid-segment corruption (fatal, operator territory). Under-replay = silent data loss; over-replay = SIGBUS
    on uninit bytes. It borrows from mmap; it never allocates per record.

  - *Contract*:
    ```rust
    pub struct Replayer<'a, S: SegmentStream> { /* ... */ }
    impl<'a, S: SegmentStream> Iterator for Replayer<'a, S> {
        type Item = Result<WalRecordRef<'a>, ReplayError>; // borrows mmap — zero alloc
    }
    impl<'a, S> Replayer<'a, S> { pub fn diagnostics(&self) -> ReplayState; } // CleanEnd | TailTruncation | Fatal
    ```

  - *Back-of-envelope* (bar (c)): a 64 MiB segment at ≥1 GB/s scans in ≤64 ms. Per record: CRC32C of payload
    (~3 ns hw for 64 B) + header parse (~5 ns) ≈ 8 ns/record → 25M rec/s ≈ plenty. The killer is allocation:
    yielding owned `WalRecord` with `Bytes::copy_from_slice` adds ~150 ns + a memcpy per record → 200 MB/s,
    a 5× cliff. Borrow from mmap and the alloc cost is 0. Bounds check (`offset+len > body_len`) is one
    compare (~0.5 ns) and prevents SIGBUS — never skip it.

  - **Requirements** — write in `crates/wal/notes/26_replay_design.md`:
    - R1. `Replayer: Iterator<Item = Result<WalRecord, ReplayError>>`.
    - R2. LSNs yielded are strictly increasing.
    - R3. Tail-truncation (corruption in the LAST record of the LAST segment) is a CLEAN exit: iterator
      returns `None`; state available via `replay.diagnostics()`.
    - R4. Middle-of-segment corruption is FATAL: returns `Err(CrcMismatch)`; iterator stops; operator
      must intervene.
    - R5. Segment-boundary continuity: `segment[N+1].first_lsn == segment[N].last_lsn + 1`. Gaps → fatal.
    - R6. Bounds-check every record-read against segment body length; never read past EOF (no SIGBUS).
    - R7. Generation field in segment header AND each record; generation mismatch on a recycled segment →
      tail truncation.
    - R8. Zero allocations on the scan path — records borrow from mmap; iterator yields slices, not
      owned bytes.
    - R9. Throughput ≥1 GB/sec on 64 MiB mmap'd segments.
    - R10. 1000 random corruption injections: no false positives (rejecting valid data) and no false
      negatives (accepting corrupt data).

  - **Design walk-through** — answer each:
    - D1. Why is tail-truncation NOT an error? (Hint: every clean shutdown after a crash leaves a
      half-written record; treating it as an error means every restart needs operator intervention.)
    - D2. How does the scanner distinguish "last record of last segment" (tail) from "any earlier
      corruption" (fatal)? (Hint: position within the segment + segment position within the wal.)
    - D3. Mmap bounds checking — what's the SIGBUS risk if you skip it?
    - D4. Generation field — what's a recycled-segment scenario it catches?
    - **D5. Pre-mortem (load-bearing):** the recycled-segment trace — `1) segment file reused, header
      generation=7; 2) new records (gen 7) written over the front; 3) crash leaves OLD gen-6 records near
      the tail with genuinely valid per-record CRCs; 4) replay reads gen-6 records, CRC passes, re-applies
      old data → silent corruption`. State the generation-field fix.
    - **D6. Anti-shortcut audit:** (a) "yield owned `WalRecord` via `Bytes::copy_from_slice`" — memcpy +
      alloc per record, 5× slower (bar (c) violation); borrow from mmap. (b) "treat any CRC mismatch as
      fatal" — turns every normal crash-restart into operator-intervention; only the FINAL record of the
      FINAL segment may be torn. (c) "skip the bounds check, mmap is the right size" — truncated file →
      mmap past EOF → SIGBUS *during recovery* (the worst time to crash). (d) "trust per-record CRC alone"
      — misses cross-generation reuse (D5); add a generation field.

  - 🧮 **Paper drill** — write in `crates/wal/notes/26_replay_drill.md`:
    - D-WALK on a concrete 3-segment replay: hand-trace Seg0 (lsn 1..3, all CRC-valid), Seg1 (lsn 4..6, record
      5 has a flipped bit), Seg2 (lsn 7..8, last record truncated by 5 bytes). Walk record by record marking the
      verdict at each: Seg0 → yield 1,2,3; Seg1 → yield 4, then record 5 CRC-mismatch in a MIDDLE segment →
      `Err(CrcMismatch)` FATAL (operator territory). Re-trace the variant where ONLY Seg2's last record is torn
      → yield 1..7, then clean `None` + `state = TailTruncation` (normal crash). The invariant it makes
      concrete: "only the final record of the final segment may be torn; corruption anywhere else is fatal."
    - D-STRUCT on the `ReplayState` machine: draw the states `Scanning → {CleanEnd | TailTruncation{at} |
      Fatal(CrcMismatch|SegmentGap)}` and which `(is_last_segment, is_last_record)` predicate transitions a CRC
      mismatch to TailTruncation vs Fatal. Hand-execute the generation-skew case: a recycled segment with valid
      per-record CRCs but `record.generation != segment.generation` → transitions to TailTruncation at the first
      old-gen record (CRC alone would silently re-apply old data).
    - D-BORROW on the zero-alloc scan: ownership/borrow graph — nodes = `mmap: MmapMut` (owner), `Replayer<'a>`
      (borrows mmap with `'a`), `WalRecordRef<'a> { payload: &'a [u8] }` (borrows mmap with `'a`). Dashed
      borrow edges; mark that each yielded `WalRecordRef` must not outlive the mmap, encoded by the `'a` on
      `Iterator::Item`. Show the type system rejects yielding owned-then-mmap-dropped because the `'a` bound ties
      the slice to the mmap — and that this is the zero-alloc property (`Bytes::copy_from_slice` would break it,
      5× cliff).
    - D-UNSAFE on the mmap bounds check: 3-column ledger. Row: read `mmap[offset..offset+frame_len]` —
      precondition `offset + frame_len <= body_len` — guaranteed by R6's bounds check BEFORE the deref; UB
      (SIGBUS on the unmapped page past a truncated file's EOF) if skipped "because the mmap is the right size."
      Flag: this is the worst time to crash (recovery crashing during recovery).
  - **Output**: `notes/26_replay_design.md` with R1–R10 + D1–D5 + the `ReplayState` enum; plus `notes/26_replay_drill.md` with the 3-segment walk, the state machine, the borrow graph, and the bounds-check ledger.

  #### Phase 2 — Write the failing tests (~25 min)

  - [ ] **(NEWBIE)** `happy_path_10k_records_across_3_segments` — replay yields exactly 10k in order. R1.
  - [ ] **(SENIOR)** 🪤 **anti-shortcut** `tail_truncation_clean_exit` — truncate last segment by 27 bytes
    mid-record; replay yields N-1 records; iterator returns None; state = `ClosedAtTailTruncation`. Bites a
    "every CRC mismatch is fatal" impl. Exercises R3.
  - [ ] **(SENIOR)** `mid_corruption_returns_fatal` — flip a bit in segment 2's record 500; replay yields
    seg1 + seg2[0..499]; then `Err(CrcMismatch)` (NOT a clean exit — this one IS fatal). Exercises R4.
  - [ ] **(SENIOR)** `segment_gap_detected` — delete segment 2; replay yields seg1, then `Err(SegmentGap)`. R5.
  - [ ] **(EXPERT)** 🪤 **anti-shortcut** `mmap_oob_treated_as_tail_truncation` — truncate file to remove
    final 1k bytes; replay clean-exits without SIGBUS. Bites a missing bounds check.
    ```rust
    #[test]
    fn mmap_oob_treated_as_tail_truncation() {
        let (wal, n) = write_segment_records(10_000);
        truncate_file(&wal, file_len(&wal) - 1024); // mmap now extends 1k past EOF
        let r = Replayer::open(&wal);
        let got: Vec<_> = r.by_ref().take_while(Result::is_ok).count(); // MUST NOT SIGBUS
        assert!(got < n && got > n - 50);
        assert_eq!(r.diagnostics(), ReplayState::TailTruncation { .. });
    }
    ```
  - [ ] **(EXPERT)** 🪤 **anti-shortcut** `generation_skew_recycled_segment_treated_as_trunc` — segment with
    valid per-record CRCs but old generation field; replay tail-truncates at the first old-gen record (CRC
    alone would silently re-apply). Exercises R7.
  - [ ] **(EXPERT, perf bar (c))** `zero_allocations_on_scan_path` — dhat; replay of 10k records produces
    zero heap allocations. Fails an owning iterator. R8.
  - [ ] **(EXPERT, perf bar (c))** `bench_1gb_per_sec_scan` — criterion on 64 MiB segment; ≥1 GB/sec. R9.
  - [ ] **(EXPERT)** `vopr_1000_random_corruption_no_false_positive_or_negative` — TigerBeetle-style fault
    injection; replay's verdict matches ground truth in all 1000 trials. R10.
  - [ ] **(SENIOR)** 🪤 **negative-control (test-the-test)** `vopr_harness_actually_injects` — proves the
    1000-trial fault harness is doing real damage: assert that with corruption injection turned ON, at least
    ~95% of trials hit a non-`CleanEnd` verdict; if nearly all trials report CleanEnd, the injector wrote to
    the wrong offset and the VOPR test is vacuous. Pair with: injection OFF → 100% CleanEnd.

  #### Phase 3 — Make the tests pass, one failure at a time (~80 min)

  - **(SENIOR) When `tail_truncation_clean_exit` reports `Err(CrcMismatch)` instead of clean exit:**
    - Symptom: iteration ends with a fatal error on the last partial record; caller bails out.
    - Why: replay can't tell tail-trunc from mid-trunc; treats every CRC mismatch as fatal.
    - Fix: track `is_last_segment` and `is_last_record_in_segment`. CRC mismatch in (last segment,
      last record) → return None + set state. Otherwise → Err. Test the "second-to-last segment is
      fully valid, last segment has truncation" case explicitly.
    - Muscle: in a durable log, only the FINAL byte of the FINAL segment can be torn; corruption
      anywhere else is operator-action territory. Reapplies at: W29 ARIES analysis, W64 raft log
      replay, W83 aeron archive replay.

  - **(EXPERT) When `mmap_oob_treated_as_tail_truncation` reports SIGBUS instead of clean exit:**
    - Symptom: process crashes with SIGBUS during replay on the truncated file.
    - Why: scanner reads `mmap[offset..offset+frame_len]` without bounds-checking against
      `segment.body_len`; the mmap region extends past file EOF; read into the hole = SIGBUS.
    - Fix: every record read `if offset + frame_len > body_len { return tail_trunc; }` before the
      mmap dereference. Treat OOB as tail-trunc, not as a fatal error.
    - Muscle: mmap-backed reads MUST bounds-check; SIGBUS during recovery is the worst possible
      time to crash (you can't recover from your recovery). Reapplies at: W27 mmap PageProvider, W30
      mmap-queue ExcerptCursor, W31 mmap-queue.

  - **(EXPERT) When `generation_skew_recycled_segment_treated_as_trunc` reports re-applied old records:**
    - Symptom: replay yields a sequence of records that includes some "old" ones (from a previous
      generation of a recycled segment) — silent re-application.
    - Why: segment was truncated + overwritten (recycled); the new records overwrote the start of
      the file but old records remained near the tail. Their CRCs were valid because they were
      genuinely valid records — just from the previous generation.
    - Fix: add `generation: u64` to both segment header AND each record header; segment's
      `generation` is the source of truth. On record read, if `record.generation !=
      segment.generation`, treat as tail-trunc (you've reached the boundary between new and old).
    - Muscle: per-record CRC catches in-record corruption but NOT cross-generation reuse; generation
      tagging is the second-layer defense. Reapplies at: W64 raft log recycling, W83 aeron archive
      segment reuse, W95 mini-db SSTable recycling.

  - **(EXPERT, perf) When `bench_1gb_per_sec_scan` reports 200 MB/sec (bar (c) violation):**
    - Symptom: 5× slower than target; profiler shows time in `Bytes::copy_from_slice` per record.
    - Why: iterator yields owned `WalRecord` with `payload: Bytes` constructed via
      `Bytes::copy_from_slice(mmap_slice)` — copies every payload.
    - Fix: yield `WalRecordRef<'a> { payload: &'a [u8] }` that borrows from mmap. Callers that need
      owned bytes call `.to_owned()` explicitly. Hot replay paths (ARIES analysis, raft replay)
      consume the slice in-place.
    - Cost avoided: ~150ns alloc + a payload memcpy per record; at 25M rec/sec, ~3.75 sec/sec of CPU reclaimed.
    - Muscle: replay iterators must borrow from mmap; allocating owned bytes per record is the
      throughput killer. Reapplies at: W30 mmap-queue ExcerptCursor, W31 wire schema decode, W80
      ledger journal replay.

  - **5-year failure mode** (write in `notes/26_replay_followup.md` after green): when io_uring +
    persistent memory mature, the mmap-based scan path may shift to direct-block-device + io_uring
    submission queue. Migration: `SegmentStream` trait abstracts the read mechanism; impls for mmap,
    io_uring, pmem. Trigger: NVMe replaced by PMem at server price points.
- [ ] Commit + log (~10 min)
  - *(The standalone "truncate + replay → tail-truncation diagnostic" smoke test is folded into Phase-2's
    `tail_truncation_clean_exit` + `mmap_oob_treated_as_tail_truncation` — no separate throwaway.)*

**Thursday — `wal` v0.1 ship + reth-db deep read part 1**

- [ ] **Build**: `crates/wal/src/lib.rs` — `Wal { dir: PathBuf, segments: Vec<Segment>, ... }` public API:
  `append(record) -> Future<Lsn>`, `replay() -> Replayer`, `checkpoint(lsn)`, `truncate_below(lsn)`. (~total 140 min across 3 phases)
  - 🔧 **Why this matters**: the public `Wal` facade is the single API surface users see. Encapsulation lets
    you swap mmap for io_uring (W40+) or O_DIRECT without breaking 6+ downstream crates. The API surface is
    small but the contract — what guarantees the wal offers — is dense. Downstream: W27 storage-trie
    integrates `Wal::append`, W29 recovery is built on `Wal::replay`, W64 raft uses wal for leader's log,
    W74 matching, W80 ledger, W83 aeron, W95 mini-db all consume this facade.

  #### Phase 1 — Problem, prediction, design (~40 min, no code yet)

  - 🎯 **Pre-prediction prompt** — write sealed answers in `crates/wal/notes/26_wal_facade_prediction.md`:
    - P1. On reopen, how do you compute `next_lsn`? Exact expression (`last_record.lsn` vs
      `last_record.lsn + 1` vs `active.first_lsn`). Commit before reading the failure cards.
    - P2. The sealed-segments map type — `RwLock<BTreeMap<..>>` vs `ArcSwap<BTreeMap<..>>`. Predict
      per-fetch latency for each.
    - P3. Concurrent outcome: a checkpoint runs while 100k appends are in flight — predict whether a "sharp"
      (pause-snapshot-resume) checkpoint loses any of them.
    - P4. Derive prediction: which Phase-2 test fails if `AppendError` is a single `IoError` variant?
    - P5. **List every Phase-2 test you predict your first attempt will FAIL**, and why.

  - *Mental model*: the `Wal` facade is the single API surface; it composes Segment + Record + GroupCommit +
    Checksum + Replay so downstream crates never touch the internals. This is the week's cross-primitive
    *integration* Build — every sub-module's invariant must hold under composition (off-by-one LSN recovery,
    lock-free sealed-segment lookup, fuzzy checkpoint, typed errors).

  - *Contract*:
    ```rust
    pub struct Wal { /* dir, active: Segment, sealed: ArcSwap<BTreeMap<SegmentId, Arc<Segment>>>, gc, next_lsn */ }
    impl Wal {
        pub async fn open(dir: &Path) -> Result<Self, WalError>;     // recovers next_lsn from active segment
        pub async fn append(&self, rec: WalRecord) -> Result<Lsn, AppendError>;
        pub fn replay(&self) -> Replayer<'_>;
        pub async fn checkpoint(&self, lsn: Lsn) -> Result<(), WalError>;   // fuzzy, non-blocking to appends
        pub async fn truncate_below(&self, lsn: Lsn) -> Result<(), WalError>;
    }
    ```

  - *Back-of-envelope* (bar (c)): aggregate ≥5M rec/s at 16 producers; replay-heavy random fetch must not
    serialize on a lock. `RwLock` read-acquire ≈ 50 ns even uncontended; `ArcSwap::load` ≈ 5 ns → 10× on the
    metadata path, and the RwLock becomes a scalability ceiling under concurrent replay+writer. Off-by-one in
    `next_lsn` is a *correctness* cliff (duplicate LSNs after every restart). Sharp checkpoint stalls the wal
    for the snapshot window → throughput drops to 0 during it.

  - **The problem this primitive solves**: today's modules (segment, record, group_commit, checksum,
    replay) are pieces. The Wal facade glues them: open-recovers from a directory, mints LSNs, dispatches
    to group_commit, rotates segments, drives checkpoints, truncates safe ranges. Downstream consumers
    interact ONLY with this facade — never with Segment, Record, or GroupCommit directly.

  - **Design walk-through** — answer each in `crates/wal/notes/26_wal_facade_design.md` (no peeking at the
    sub-modules' impls or reth-db until Phase 3):
    - D1. Property table for the sealed-segments map — enumerate every choice: `RwLock<BTreeMap>`
      (Sync✓, ~50 ns read-acquire even uncontended, 0 alloc/read, *blocks* writers vs readers, scalability
      ceiling under concurrent replay+writer), `Mutex<BTreeMap>` (Sync✓, ~25 ns, serializes readers too —
      worse), `ArcSwap<BTreeMap>` (Sync✓, ~5 ns load, COW alloc on *write* only, readers wait-free). Which
      one keeps replay-heavy random fetch off the writer's critical path *by construction*?
    - D2. Open-time `next_lsn` recovery — the active segment may hold written records past its header's
      `first_lsn`. What must `open()` do before minting the first new LSN? (Hint: scan, don't trust the header.)
    - D3. Checkpoint shape — sharp (pause-snapshot-resume) vs fuzzy (snapshot DPT/TT without pausing,
      record a `(begin_lsn, end_lsn)` window). Which one keeps append throughput non-zero during a checkpoint?
    - **Dx. Pre-mortem (load-bearing):** write BOTH exact failure traces — if you can't write them you can't
      prove the facade prevents them.
      - *Recovery off-by-one*: `1) last durable append = lsn 100000; 2) crash; 3) open() reads
        active.header.first_lsn = 99000 (the segment started there), sets next_lsn = 99000 WITHOUT scanning
        the segment's written records; 4) first new append reuses lsn 99000..100000 → duplicate LSNs → W29
        ARIES analysis double-applies every redo in that range → silent state corruption.` Fix: `open()` runs
        `Segment::scan()` over the active segment, `next_lsn = max(record.lsn) + 1` (and `= active.first_lsn`
        only when the active segment is empty).
      - *Sharp-checkpoint race*: `1) checkpoint() snapshots the durable_lsn watermark = 50000; 2) meanwhile
        100 appends (lsn 50001..50100) sit in the group-commit pending queue, not yet fsynced; 3) checkpoint
        record is written claiming "everything ≤ 50000 is captured, start replay here"; 4) the 100 in-flight
        appends fsync a moment later but are now BELOW the post-checkpoint replay floor → they vanish from
        post-checkpoint replay → 100 acknowledged writes lost.` Fix: fuzzy checkpoint — record a
        `(begin_lsn=50000, end_lsn=current_next_lsn)` window; ARIES re-scans the window so in-flight appends
        are never excluded.
    - **Dy. Anti-shortcut audit:** name the obvious reach per motivation and one line on why it's wrong HERE:
      - (a) "`next_lsn = active.header.first_lsn` on open" — ignores the records already written into the
        active segment → the off-by-one above → duplicate LSNs after every restart. Scan for `max(lsn)+1`.
      - (b) "`RwLock<BTreeMap>` for the sealed-segments map" — correct, but ~50 ns read-acquire even
        uncontended and a scalability ceiling under concurrent replay+writer; use `ArcSwap` (~5 ns load,
        COW on write) so replay-heavy random fetch never touches the writer's lock.
      - (c) "sharp pause-snapshot-resume checkpoint" — stalls the wal to 0 throughput for the snapshot
        window AND races the in-flight group-commit appends (Dx) → lost acknowledged writes; use a fuzzy
        checkpoint with a `(begin_lsn, end_lsn)` window.
      - (d) "single `AppendError::IoError` variant" — strips the caller of recovery affordances (can't tell
        ENOSPC from EACCES from a truly-broken fsync); use a typed `AppendError` enum
        (`OutOfDiskSpace | FsyncFailed { source } | ChannelClosed`).

  - 🧮 **Paper drill** — write in `crates/wal/notes/26_wal_facade_drill.md`:
    - D-STRUCT on the composition: draw the facade as a node graph — `Wal` owns `active: Segment`, `sealed:
      ArcSwap<BTreeMap<SegmentId, Arc<Segment>>>`, `gc: GroupCommit`, `next_lsn: AtomicU64`. Hand-execute one
      `append` through the seams (mint lsn via `next_lsn.fetch_add` → `gc.append` → eventual `Segment::append`
      → `durable_lsn` watermark) and one `open` recovery (list sealed segments, scan the active segment for
      `max(lsn)+1`). Mark which sub-module invariant each step relies on.
    - D-COST on the sealed-segments metadata path: per-fetch cost table — `RwLock<BTreeMap>` read-acquire
      ≈ 50 ns even uncontended (and a scalability ceiling under concurrent replay+writer) vs `ArcSwap::load`
      ≈ 5 ns wait-free. Cliff ratio = 10× on the replay-heavy random-fetch path; state that the writer's
      rotation/truncate does COW-build + atomic-swap so readers never touch the writer's lock. Budget: replay
      under writer load must not serialize → ArcSwap is mandatory.
    - D-LIFE on open-time `next_lsn` recovery (the off-by-one): a value-lifecycle line for the LSN counter —
      last durable append = 100000; crash; `open()` MUST scan the active segment (`Segment::scan`) and set
      `next_lsn = max(record.lsn) + 1 = 100001` (or `= active.first_lsn` only when the active segment is empty).
      Show the wrong transition (`next_lsn = active.header.first_lsn = 99000` without scanning) → next append
      reuses 99000..100000 → duplicate LSNs → W29 ARIES double-applies redo → silent corruption. Assert "each
      LSN minted exactly once across restarts."
  - **Output**: `notes/26_wal_facade_design.md` with D1–D3 + Dx (the recovery off-by-one AND sharp-checkpoint
    traces) + Dy (the four-item anti-shortcut audit) + the composition diagram (Segment + Record +
    GroupCommit + Checksum + Replay → Wal); plus `notes/26_wal_facade_drill.md` with the composition graph,
    the metadata cost table, and the LSN-recovery lifecycle.

  - 🎯 **Hardened acceptance (silent decomposition test — this is your R-list)**:
    - **Throughput**: ≥5M records/sec at N=16 producers (32 B payload); ≥200 MB/sec sustained at NVMe.
    - **Latency distribution**: append p50 ≤200ns, p99 ≤2µs, p99.9 ≤2ms (group-commit window), p99.99 ≤10ms.
    - **Scale curve**: per-producer throughput at N=1,2,4,8,16. At N=16 aggregate must be ≥10× N=1.
      Sublinear = unsharded write_offset.
    - **Zero allocation under dhat**: steady-state append produces zero heap allocations.
    - **Group-commit amortization**: under 100k records/sec sustained, fsync rate ≤1000/sec (1000× batch).
      Per-record fsync = fail.
    - **Loom-green**: 2-producer + group-commit harness, no lost-wakeup on commit-notify, no torn record.
    - **Graceful degradation**: under 10× sustained overload (50M records/sec offered) for 60s, system
      sheds via documented policy (backpressure via CreditFlowControl from W11 backpressure crate; OR
      typed Err). Never OOM, never drop records silently. State the policy.
    - **Crash-restart determinism**: kill -9 mid-burst at random points (1000 trials); recovery from each
      crash produces byte-identical Wal state (same set of LSNs durable, same record bytes per LSN).
    - **Cross-restart byte-identical**: replay on x86 + aarch64 from the same wal directory produces
      identical record streams (no f64, no HashMap iteration in recovery).
    - **5-year failure mode**: state one shift (NVMe → persistent memory, 10× producer count, replication
      across DCs) that would force a rewrite.

  #### Phase 2 — Write the failing tests (~25 min)

  - [ ] **(NEWBIE)** `open_empty_directory` — `Wal::open` on empty dir creates segment[0]; append works.
  - [ ] **(SENIOR)** 🪤 **cross-primitive trap (Segment scan ∘ facade recovery)** `open_recover_after_crashed_tail`
    — wal with 100k records + crashed mid-101001; reopen yields `next_lsn = 100001`; record 101001 is NOT
    present. Bites the off-by-one where the facade trusts the segment header's first_lsn instead of scanning
    the active segment's records (Segment::scan from Monday composed with recovery).
    ```rust
    #[tokio::test]
    async fn open_recover_after_crashed_tail() {
        let dir = write_then_kill(100_000, /*torn at*/ 100_001).await;
        let wal = Wal::open(&dir).await.unwrap();
        assert_eq!(wal.next_lsn(), Lsn(100_001),
            "next_lsn {:?} != 100001 — facade didn't scan the active segment for the max written LSN \
             (off-by-one → duplicate LSNs on next append)", wal.next_lsn());
        assert!(wal.replay().all(|r| r.unwrap().lsn != Lsn(100_001)), "torn record 101001 must be absent");
    }
    ```
  - [ ] **(NEWBIE)** `append_replay_round_trip` — append 10k records; close; replay returns the same 10k in order.
  - [ ] **(SENIOR)** `checkpoint_then_truncate_removes_old_segments` — checkpoint at lsn 5000;
    truncate_below(5000); sealed segments with last_lsn < 5000 deleted; 5001..10000 intact.
  - [ ] **(EXPERT)** 🪤 **cross-primitive trap (GroupCommit ∘ checkpoint)** `checkpoint_concurrent_with_appends_no_loss`
    — 100k appends in flight; checkpoint runs; all 100k either succeed before checkpoint OR visible after; no
    lost writes. Bites a "sharp" (pause-snapshot-resume) checkpoint that races the group-commit watermark.
  - [ ] **(NEWBIE)** `reopen_after_close_replay_intact` — append, close, reopen, replay; all durable present.
  - [ ] **(SENIOR)** `disk_full_propagates_typed_error` — simulate `ENOSPC`; append future resolves with
    `Err(AppendError::OutOfDiskSpace)`; subsequent appends fail fast.
  - [ ] **(EXPERT, perf bar (c))** `bench_200mb_per_sec_16_producers` — criterion; sustained ≥200 MB/sec on
    a 16-producer test on NVMe.
  - [ ] **(EXPERT, perf bar (c))** `bench_replay_lockfree_under_contention` — 16 replay threads + 1 writer;
    replay does not stall (no `RwLock` on the sealed_segments map). ~5 ns/fetch, not ~50 ns.
  - [ ] **(EXPERT)** `crash_restart_byte_identical` — 1000 random SIGKILLs during sustained 100k/sec writes;
    recovery from each is byte-identical to a clean-shutdown wal.
  - [ ] **(EXPERT)** `loom_2p_groupcommit_no_lost_wakeup` — loom test on the durable-LSN notify path; no
    torn record, no lost commit-notify.
  - [ ] **(SENIOR)** 🪤 **negative-control (test-the-test)** `crash_restart_harness_actually_kills_mid_write`
    — proves the 1000-SIGKILL harness kills during a write, not between clean batches: assert that across the
    1000 trials, ≥1 leaves a torn (CRC-failing) tail record before recovery. If every trial recovers a clean
    tail, the kill timing is wrong and `crash_restart_byte_identical` proves nothing.

  #### Phase 3 — Make the tests pass, one failure at a time (~75 min)

  - **(SENIOR) When `open_recover_after_crashed_tail` reports `next_lsn = 100000` instead of `100001`:**
    - Symptom: off-by-one; on restart, the next append reuses lsn 100000 which was the previous
      successful append.
    - Why: `next_lsn = last_appended` instead of `last_appended + 1`. Or worse: `next_lsn =
      active.first_lsn` when active segment had records (forgot to scan the active segment's records).
    - Fix: scan active segment's records via `Segment::scan()`; `next_lsn = max(last_record.lsn) + 1`.
      If active segment is empty, `next_lsn = active.first_lsn`.
    - Muscle: open-time LSN recovery scans the active segment to find the highest written LSN; "+1"
      is the next slot. Off-by-one here = duplicate LSNs after restart. Reapplies at: W29 ARIES
      analysis, W64 raft term restart.

  - **(EXPERT, perf) When `bench_replay_lockfree_under_contention` reports ~50ns per fetch instead of ~5ns (bar (c) violation):**
    - Symptom: replay throughput plateaus under writer load; `perf` shows lock contention on
      `sealed_segments`.
    - Why: `sealed_segments: RwLock<BTreeMap<SegmentId, Arc<Segment>>>` — every replay or random-access
      fetch takes a read lock. Even an RwLock's read-acquire is ~50ns.
    - Fix: `arc_swap::ArcSwap<BTreeMap<SegmentId, Arc<Segment>>>` for sealed segments. Writers (rotation,
      truncate) build a fresh BTreeMap and atomic-swap; readers do one atomic load (~5 ns).
    - Cost avoided: ~45ns/fetch (10×); eliminates the lock as the scalability ceiling for replay-heavy workloads.
    - Muscle: read-mostly metadata maps use copy-on-write atomic swaps (arc-swap), not RwLock.
      Reapplies at: W14 bufpool page table, W42 txn schema versions, W74 matching-engine symbol list.

  - **(EXPERT) When `checkpoint_concurrent_with_appends_no_loss` reports lost appends:**
    - Symptom: 100 of 100k appends are "lost" after checkpoint — present in pre-checkpoint state,
      absent in post-checkpoint replay.
    - Why: checkpoint took a "sharp" snapshot — paused appends, snapshotted DPT/TT, resumed. The
      paused appends queued in the group_commit queue with their old LSNs but their durability is
      gated on the pre-pause durable_lsn watermark.
    - Fix: fuzzy checkpoint — DPT/TT collected WITHOUT pausing appends; the checkpoint record carries
      `(begin_lsn, end_lsn)` defining a window of LSNs that "might or might not" be reflected. ARIES
      recovery's analysis pass tolerates fuzzy checkpoints by re-scanning the window.
    - Muscle: high-concurrency checkpoints must be fuzzy, not sharp; sharp checkpoints stall the wal
      and lose throughput. Reapplies at: W30 Checkpointer, W64 raft snapshot.

  - **(SENIOR) When `disk_full_propagates_typed_error` reports a generic `Err(IoError)`:**
    - Symptom: caller can't distinguish "disk full" from "permission denied" from "fsync truly broken".
    - Why: group_commit's fsync error propagation collapsed all errors into a single variant.
    - Fix: enum `AppendError { OutOfDiskSpace, FsyncFailed { source }, ChannelClosed, ... }`. The
      caller can decide: OutOfDiskSpace → backoff and retry after operator action; FsyncFailed →
      escalate; ChannelClosed → wal is dead.
    - Muscle: typed errors at the boundary; collapsing all I/O errors into a single variant strips
      the caller of recovery affordances. Reapplies at: W42 txn errors, W67 raft errors.

  - **5-year failure mode** (write in `notes/26_wal_facade_followup.md` after green): when io_uring +
    persistent memory mature, segment rotation + fallocate model becomes obsolete. Migration: a
    `WalBackend` trait abstracts open/append/replay/checkpoint; current `MmapWalBackend` lives alongside
    `IoUringWalBackend` and `PmemWalBackend`. Trigger: NVMe replaced by PMem.
- [ ] Tag `wal v0.1.0`. (~5 min)
  - Output: `git tag wal-v0.1.0`; `cargo test -p wal` and `cargo test -p concurrent seg_queue` both green;
    bar-(c) checklist (zero hot-path alloc, loom-green, VOPR-green, perf benches in band) satisfied before tagging.
- [ ] Read `reth-db/src/lib.rs` table definitions (lookup, not cover-to-cover) — note in
  `notes/26_reth_db_tables.md` how reth's `TableType`/`Compress`/`Decompress` codecs compare to your
  `WalRecord` wire format. (~45 min)
  - 🎯 Hardened acceptance: `notes/26_reth_db_tables.md` lists ≥5 reth tables + one sentence each on key/value
    encoding, and one paragraph contrasting reth's per-table codec with wal's single fixed-LE record codec.
  - *Mental model*: reth-db is the read side this week's PR work targets; understanding its table/codec model
    is the prerequisite for a credible storage PR (Sat).
- [ ] Commit + log (~10 min)

**Friday — reth-db deep read part 2 + first reth storage PR hunt**

- [ ] **Bench** (moved from Tue): `crates/concurrent/benches/seg_queue_fanin.rs` — criterion: SegQueue
  fan-in vs `Mutex<Vec<T>>` baseline at group-size 1, 8, 64, 1024 and producer counts 1,2,4,8,16. Expect
  SegQueue to win at ≥4 producers (Mutex serializes; SegQueue is wait-free per slot). (~60 min)
  - Output: `target/criterion/seg_queue_fanin/` report; a `notes/26_fanin_crossover.md` line stating the
    producer count where SegQueue overtakes Mutex (the contention crossover) + the measured numbers.
- [ ] Read `reth-db/src/lib.rs` transaction impl + cursor wrappers (lookup, not cover-to-cover) — append to
  `notes/26_reth_db_tables.md`. (~30 min)
  - 🎯 Hardened acceptance: note captures how reth's `DbTx`/`DbCursorRO` map onto MDBX read txns + the one
    invariant (cursor lifetime tied to txn) that a storage PR must not violate.
- [ ] Browse reth issues tagged `C-storage`/`A-db` — shortlist in `notes/26_pr_candidates.md`. (~30 min)
  - 🎯 Hardened acceptance: `notes/26_pr_candidates.md` lists ≥3 issues with URL + a one-line scope estimate each.
- [ ] Pick a good-first-issue or docs issue and claim it (comment on the issue). (~30 min)
  - Output: a comment posted on the chosen reth issue claiming it; issue URL recorded in `notes/26_pr_candidates.md`.
- [ ] [Tempo] 15 min after the Reth issue is claimed: scan `tempoxyz/tempo` issues filtered by storage, db,
  state-root labels. Bookmark 2-3 candidate "future second-PR" issues in `notes/26_pr_candidates.md`. Do NOT
  claim — Reth comes first. (~15 min)
- [ ] Commit notes (~10 min)

**Saturday — First reth storage PR work**

- [ ] Implement the claimed reth issue + submit the PR. (~150 min)
  - Output: a reth PR opened (URL recorded in `notes/26_pr_candidates.md`); `cargo test` green in the reth
    fork for the touched module; PR description links the issue.
  - 🎯 Hardened acceptance: PR passes reth CI (fmt + clippy + the relevant test target) on push.
- [ ] **Build**: `notes/static_mem_audit.md` (create or append) — row #1 for `wal` v0.1.0. Grep hot path (`append`, group-commit flush, replay iterator) for `Box::new` / `Vec::with_capacity` / `String::from`. Document every alloc found + classify {hot-path-violation, init-only, cold-path-only}. Zero hot-path allocs is the target; any violation gets a justified-exception line or a follow-up fix issue. (~30 min)
  - 🔧 **Decomposition prompt**: Thursday's hardened acceptance demanded "zero allocation under dhat in steady-state append" — Saturday is when the receipt gets filed. TigerStyle's "static memory allocation throughout" is the discipline; this audit is the workspace-wide ledger that proves it crate by crate. Row #1 today; W30 (recovery+runtime+mmap-queue), W42 (txn), W76 (aeron), W82 (matching v1.5), W92 (ledger v1.0) all append rows in the same shape. The accumulating file is the bar-(c) audit trail by M30.
  - 🎯 **Hardened acceptance**:
    - **Row schema** (stays this shape across the workspace): `| Crate@version | Hot path | Allocations found | Classification | Justification | Owner |`. Crate@version pins the audit to a tagged release.
    - **Concrete W26 row**: crate=`wal@v0.1.0`, hot path = `append + group-commit flush + replay`, allocations found = (count + line refs), classification = (hot-path-violation/init-only/cold-path), justification per line.
    - **Threshold**: zero hot-path-violation entries. If any found, follow-up issue filed and linked in the row.
    - **Tooling**: a `scripts/grep_hot_path_allocs.sh` or just documented grep commands so the audit is reproducible (`rg 'Box::new|Vec::with_capacity|String::from|vec!\[' crates/wal/src/`).
    - **5-year failure mode**: when allocator-aware APIs land (`#[allocator]` attribute, Rust RFC), the audit shifts from grep-based to compiler-enforced. State the migration: a `#[no_alloc]` attribute on hot-path functions becomes the audit's enforcement mechanism; grep is the bootstrap.
  - 🧮 **Paper drill** — write in `crates/wal/notes/26_static_mem_drill.md`:
    - D-COST on the three `wal` hot paths: a per-op alloc ledger — `append` (target 0 allocs: `fetch_add`
      reservation + `copy_from_slice` into mmap, caller-supplied buffer), group-commit flush (0 allocs:
      drains SegQueue, one fsync), replay-record (0 allocs: yields `WalRecordRef<'a>` borrowing mmap). For
      each, write the cost of the VIOLATION you're auditing against: a stray `Box::new`/`Vec::with_capacity`
      ≈ 50 ns malloc + ~150 ns round-trip; at 5M ops/s that one alloc is 1 s/s of CPU. Then classify the
      segment-rotation `Box::new(Segment)` — it fires 1/N records but is reachable from steady-state `append`,
      so it is hot-path (the "init-only misclassification" the Expect-to-hit card traps), not init-only.
  - *Source*: https://tigerstyle.dev/ (static memory allocation principle).
  - **Expect to hit #1 — "init-only" misclassification**: an allocation in `Wal::new` is genuinely init-only, but an allocation in `append`'s slow path (segment rotation) IS hot-path even though it fires once per N records. Symptom: the audit row claims zero hot-path violations but a benchmark shows allocation spikes on segment-roll boundaries.
    - Fix: "hot path" means any call site reachable from the steady-state operation (append, replay-record, group-commit), even if rarely taken. Segment rotation is hot-path. Recovery's redo loop is hot-path (called on every restart, possibly per-record). The narrow definition (only `init` is non-hot) is the load-bearing distinction.
    - Muscle: hot-path classification follows reachability from steady-state ops, not call frequency. Reapplies at: every bar-(c) crate's audit row.
- [ ] **Build**: `crates/wal/docs/page_cache_strategy.md` — design memo on the OS-page-cache vs O_DIRECT philosophical split. Kafka picks OS page cache + sendfile zero-copy; TigerBeetle picks O_DIRECT + own page cache + raw block device. Both pick deliberately. Your `wal`: which side? Why? (~90 min)
  - 🔧 **Decomposition prompt**: today shipped `wal` v0.1 with whatever IO strategy you picked Thursday (likely OS-mediated `pwrite + fsync`). But the choice itself was reflex, not deliberate. Two production codebases — Kafka and TigerBeetle — solve durability with OPPOSITE strategies, and both work. The memo forces a deliberate position: are you Kafka-side (rely on OS, gain throughput on large sequential, accept page-cache opacity) or TigerBeetle-side (own page cache via O_DIRECT, gain determinism, accept implementation complexity)? Without the memo, downstream consumers (W83 ledger, W95 mini-db) inherit your unexamined default; with it, the choice is documented and reapply sites can deviate consciously.
  - 🎯 **Hardened acceptance**:
    - **Table**: Kafka vs TigerBeetle vs your-wal. Rows: caching model (OS page cache / own), write path (pwrite+fsync / O_DIRECT / mmap+msync), read path (sendfile / direct / mmap), block alignment requirement, recovery cost on crash, predictability of p99/p99.9 latency.
    - **Where Kafka wins** (paragraph): large sequential streaming reads (sendfile + OS readahead is unbeatable on cold-cache); operator simplicity (OS handles caching policy).
    - **Where TigerBeetle wins** (paragraph): predictable tail latency (no OS-induced surprises), deterministic IO ordering (matters for VOPR replay), exact memory budget (own page cache is sized + bounded).
    - **Your wal's choice** (paragraph): which side, and why for THIS crate's workload (write-heavy append, occasional replay). Note specifically which downstream consumer (W83 ledger? W27 storage-trie? W95 mini-db?) forces the choice and could pull the other way.
    - **Migration plan**: if a future consumer demands O_DIRECT (e.g. ledger v1.0 at W90), what's the cost of bolting it on vs starting over? Wal's API should not foreclose the migration; state which API contracts make either path possible.
    - **Cited numbers**: Kafka's 2M writes/sec benchmark (Jay Kreps, LinkedIn); TigerBeetle's silent disk corruption stats (0.031% SSD/yr, 1.4% enterprise HDD/yr — argues for own-checksumming regardless of cache model).
    - **5-year failure mode**: when Linux io_uring + zero-copy fully matures, the OS-page-cache vs O_DIRECT debate may flatten (io_uring lets you ask for either explicitly per-op). State the trigger and migration.
  - *Mental model*: durability-engine architecture is a 2-axis decision (cache model × write path). The corners — Kafka (OS-cache, pwrite+fsync) and TigerBeetle (own-cache, O_DIRECT) — represent two coherent designs. Hybrid choices exist but inherit weakness from both axes. The memo picks a corner consciously.
  - 🧮 **Paper drill** — write in `crates/wal/notes/26_page_cache_drill.md`:
    - D-COST on the two durability corners for a write-heavy append workload: per-write cost + tail-latency
      ledger. Kafka-side (`pwrite + fsync` + OS page cache): write hits page cache (~100 ns mem), fsync ≈
      500 µs amortized by group-commit, but writeback is OS-scheduled → p99.9 has an uncontrolled tail from
      background flush. TigerBeetle-side (O_DIRECT, own page cache): write goes straight to the device at
      block alignment, no OS writeback surprise → predictable p99.9, at the cost of own-cache complexity +
      4 KiB alignment. State `wal` v0.1's choice and the budget that forces it (which downstream consumer —
      W83 ledger / W95 mini-db — could pull the other way), and the cliff: a "hybrid" inherits BOTH tails.
  - *Source*: https://developer.confluent.io/courses/architecture/data-replication/ (Kafka durability), https://docs.tigerbeetle.com/concepts/safety/ (TigerBeetle own-cache rationale + corruption stats), Kreps benchmark (LinkedIn engineering blog).
  - **Expect to hit #1 — picking a hybrid for safety**: a memo that says "we do BOTH" — own page cache + pwrite + fsync. Symptom: you get the latency tail of OS page cache (uncontrolled writeback) PLUS the complexity of own caching, with no real benefit. The "best of both worlds" failure mode.
    - Fix: pick a corner consciously. Hybrid is a third option, but justify it specifically (e.g. "wal uses OS-cache for v0.1 because operator simplicity dominates here; the consuming ledger v1.0 will own-cache through bufpool"). Each layer picks one corner; mixing happens at the architectural seam, not inside one component.
    - Muscle: durability engines pick one cache model. A "hybrid" without an explicit architectural seam is two implementations stacked. Reapplies at: W83 ledger v0.5 (likely TigerBeetle-side), W95 mini-db (deliberate per-tier choice).
- [ ] Commit + log (~10 min)

## Sunday — Rest + Weekly Ritual

See `Weekly Ritual Template` in /Users/thanhngo/tngo/projects/reth-journey/README.md.
